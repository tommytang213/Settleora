openapi: 3.1.0
info:
  title: Settleora API
  version: 0.1.0
  license:
    name: Apache-2.0
    identifier: Apache-2.0
  description: >
    OpenAPI contract for Settleora scaffold materialization. Generated mobile
    and web clients are produced from this file, and generated output
    must not be manually edited. This contract currently covers scaffold health
    checks, first-owner local bootstrap status/creation for fresh deployments,
    refresh-capable local sign-in, public refresh, current-session sign-out,
    current-account sign-out-all, current-account session list and per-session
    revocation, the minimal current-user auth read boundary, and guarded
    self-profile read/update endpoints, first personal and group bill create/read
    endpoints, bill submission and participant acknowledgement/rejection workflow endpoints,
    first bill revision proposal lifecycle endpoints, settlement candidate preview, first settlement request creation endpoints,
    settlement request list/get endpoints, settlement counterparty payment-details
    read endpoints, settlement-scoped counterparty payment QR content reads,
    settlement payment claim endpoints, receiver settlement payment confirmation endpoints,
    settlement dispute endpoints, settlement cancellation endpoints, settlement payment proof attach/list/content/remove endpoints,
    recurring bill template and forecast/draft-generation endpoints,
    personal and group bill attachment attach/list/content/remove endpoints, and settlement balance projection read endpoints,
    guarded group create/list/read/update and group member-management foundations
    plus guarded admin local-user list/read/create foundations; it is not the feature contract for
    general public registration, invitations, auth middleware, session management beyond
    current-session sign-out, current-account sign-out-all/list/revocation, and
    public refresh, broader user management, role assignment, group invitations,
    guest members, sync,
    OCR upload, UI behavior, or database behavior.
servers:
  - url: /
paths:
  /health:
    get:
      summary: Health check
      description: Placeholder health endpoint for future API availability checks.
      operationId: getHealth
      security: []
      responses:
        "200":
          description: Service health response.
          content:
            application/json:
              schema:
                type: object
                required:
                  - status
                properties:
                  status:
                    type: string
                    example: ok
        "400":
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /health/ready:
    get:
      summary: Readiness check
      description: Checks PostgreSQL, RabbitMQ, and storage connectivity without exposing dependency details.
      operationId: getHealthReady
      security: []
      responses:
        "200":
          description: Service is ready for requests that require PostgreSQL, RabbitMQ, and storage connectivity.
          content:
            application/json:
              schema:
                type: object
                required:
                  - status
                  - service
                  - checks
                properties:
                  status:
                    type: string
                    enum:
                      - ready
                    example: ready
                  service:
                    type: string
                    example: settleora-api
                  checks:
                    type: object
                    required:
                      - postgres
                      - rabbitmq
                      - storage
                    properties:
                      postgres:
                        type: string
                        enum:
                          - ok
                        example: ok
                      rabbitmq:
                        type: string
                        enum:
                          - ok
                        example: ok
                      storage:
                        type: string
                        enum:
                          - ok
                        example: ok
        "400":
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "503":
          description: Service is not ready because PostgreSQL, RabbitMQ, or storage connectivity is unavailable.
          content:
            application/json:
              schema:
                type: object
                required:
                  - status
                  - service
                  - checks
                properties:
                  status:
                    type: string
                    enum:
                      - unready
                    example: unready
                  service:
                    type: string
                    example: settleora-api
                  checks:
                    type: object
                    required:
                      - postgres
                      - rabbitmq
                      - storage
                    properties:
                      postgres:
                        type: string
                        enum:
                          - ok
                          - failed
                        example: failed
                      rabbitmq:
                        type: string
                        enum:
                          - ok
                          - failed
                        example: failed
                      storage:
                        type: string
                        enum:
                          - ok
                          - failed
                        example: failed
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/auth/bootstrap/status:
    get:
      summary: Bootstrap status
      description: Returns only whether first-owner local bootstrap is required because no auth account exists.
      operationId: getAuthBootstrapStatus
      security: []
      responses:
        "200":
          description: Bounded bootstrap setup status.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BootstrapStatusResponse"
        "400":
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/auth/bootstrap/local-owner:
    post:
      summary: Bootstrap first local owner
      description: Creates the first local owner account only while no auth account exists. This is not general public registration; clients must sign in normally after bootstrap succeeds.
      operationId: bootstrapLocalOwner
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BootstrapLocalOwnerRequest"
      responses:
        "200":
          description: Local owner bootstrap succeeded without returning credential or token material.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BootstrapLocalOwnerResponse"
        "400":
          description: The submitted bootstrap request is invalid.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Bootstrap is unavailable because an auth account already exists.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Bootstrap could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/auth/sign-in:
    post:
      summary: Local sign-in
      description: Authenticates a local account with an identifier and password, returning a refresh-capable opaque access session plus initial refresh-like credential only once on success.
      operationId: signInLocal
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LocalSignInRequest"
      responses:
        "200":
          description: Local sign-in succeeded and returned new credential material once.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LocalSignInResponse"
        "401":
          description: Sign-in failed without revealing account, credential, or policy state.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "429":
          description: Too many sign-in attempts.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/auth/refresh:
    post:
      summary: Refresh session
      description: Rotates a submitted refresh-like credential and returns a new opaque access-session token plus replacement refresh-like credential only on success.
      operationId: refreshSession
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RefreshSessionRequest"
      responses:
        "200":
          description: Refresh succeeded and returned replacement credential material once.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RefreshSessionResponse"
        "401":
          description: Refresh failed without revealing credential, account, session, or family state.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Refresh could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/auth/sign-out:
    post:
      summary: Current session sign-out
      description: Validates the submitted opaque bearer session token and revokes only that current session.
      operationId: signOutCurrentSession
      security:
        - SessionBearerAuth: []
      responses:
        "204":
          description: Current session was revoked. No response body is returned.
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/auth/sign-out-all:
    post:
      summary: Current account sign-out-all
      description: Validates the submitted opaque bearer session token and revokes all active sessions owned by the authenticated account, including the submitted current session.
      operationId: signOutAllCurrentAccountSessions
      security:
        - SessionBearerAuth: []
      responses:
        "204":
          description: Active sessions for the current account were revoked. No response body is returned.
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Sign-out-all failed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/auth/sessions:
    get:
      summary: Current account sessions
      description: Validates the submitted opaque bearer session token and lists safe metadata for active sessions owned by the authenticated account.
      operationId: listCurrentAccountSessions
      security:
        - SessionBearerAuth: []
      responses:
        "200":
          description: Active sessions owned by the authenticated account.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionListResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/auth/sessions/{sessionId}:
    delete:
      summary: Revoke current account session
      description: Validates the submitted opaque bearer session token and revokes one session owned by the authenticated account.
      operationId: revokeCurrentAccountSession
      security:
        - SessionBearerAuth: []
      parameters:
        - name: sessionId
          in: path
          required: true
          description: Session ID to revoke.
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: Session was revoked. No response body is returned.
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Requested session is unavailable to revoke.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/auth/current-user:
    get:
      summary: Current authenticated user
      description: Validates an opaque bearer session token and returns the minimal authenticated actor, profile, session, and system-role summary.
      operationId: getCurrentUser
      security:
        - SessionBearerAuth: []
      responses:
        "200":
          description: Current authenticated user summary.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CurrentUserResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/admin/users:
    get:
      summary: List admin users
      description: Lists safe user summaries for non-deleted profiles/accounts. Requires an authenticated SettleoraSession with system owner or admin role; ordinary users are forbidden. This is not public registration or invitation management.
      operationId: listAdminUsers
      security:
        - SessionBearerAuth: []
      responses:
        "200":
          description: Safe admin user summaries.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminUserListResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "403":
          description: The authenticated actor is not a system owner or admin.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/admin/users/local:
    post:
      summary: Create admin local user
      description: Creates a normal local-account user through an authenticated owner/admin path. The API derives the actor server-side, assigns only the system user role, and returns no session tokens, password material, credential metadata, provider payloads, audit metadata, storage paths, or invitation state. This is not public self-registration.
      operationId: createAdminLocalUser
      security:
        - SessionBearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateLocalUserRequest"
      responses:
        "201":
          description: Local user was created with the default system user role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminUserSummaryResponse"
        "400":
          description: The submitted local-user creation request is invalid.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "403":
          description: The authenticated actor is not a system owner or admin.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: A local user cannot be created with the submitted information.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Local user creation could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/admin/users/{userProfileId}:
    get:
      summary: Get admin user
      description: Returns one safe user summary for a non-deleted profile/account. Requires an authenticated SettleoraSession with system owner or admin role; ordinary users are forbidden.
      operationId: getAdminUser
      security:
        - SessionBearerAuth: []
      parameters:
        - name: userProfileId
          in: path
          required: true
          description: User profile ID to read.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Safe admin user summary.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminUserSummaryResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "403":
          description: The authenticated actor is not a system owner or admin.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: User profile or auth account is missing, deleted, or unavailable.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/bills:
    post:
      summary: Create personal bill
      description: Creates a draft personal bill for the authenticated actor. The API derives the creator, participant, and payer from the current session, keeps groupId null, calculates totals and resolved shares server-side, and rejects client-submitted ownership, participant, payer, group, file, or authorization IDs.
      operationId: createPersonalBill
      security:
        - SessionBearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePersonalBillRequest"
      responses:
        "201":
          description: Personal draft bill created for the authenticated actor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonalBillResponse"
        "400":
          description: The submitted personal bill request is invalid.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: The current profile is unavailable or not allowed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Personal bill creation could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
    get:
      summary: List personal bills
      description: Lists non-archived personal bills created by the authenticated actor where groupId is null. This first foundation endpoint is intentionally unpaginated; future bill list slices are expected to add pagination or filtering when needed.
      operationId: listPersonalBills
      security:
        - SessionBearerAuth: []
      responses:
        "200":
          description: Personal bills visible to the authenticated actor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonalBillListResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: The current profile is unavailable or not allowed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/bills/{billId}:
    get:
      summary: Get personal bill
      description: Returns one non-archived personal bill only when it was created by the authenticated actor and has groupId null. Missing, archived, group, and unrelated bills return the same safe 404.
      operationId: getPersonalBill
      security:
        - SessionBearerAuth: []
      parameters:
        - name: billId
          in: path
          required: true
          description: Personal bill ID to read.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Personal bill visible to the authenticated actor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonalBillResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Bill is missing, archived, a group bill, unrelated, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/bills/{billId}/attachments:
    post:
      summary: Attach personal bill file
      description: Uploads one purpose-specific receipt or supporting attachment for a visible personal bill. The authenticated actor is derived from SettleoraSession and must be the bill creator or owner/responsible editor. The API validates a bounded multipart upload, writes bytes through the storage lifecycle foundation, creates a receipt_image or supporting_attachment file object plus an expense_bill_attachments association, emits bounded audit, and returns safe metadata only. This is not a generic file API and never accepts or returns storage object keys, provider paths, provider URLs, original filenames, auth/session fields, OCR text, raw request bodies, or unrelated bill data.
      operationId: attachPersonalBillAttachment
      security:
        - SessionBearerAuth: []
      parameters:
        - name: billId
          in: path
          required: true
          description: Personal bill ID that should receive the attachment.
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/AttachBillAttachmentRequest"
      responses:
        "201":
          description: Bill attachment created for the visible personal bill.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillAttachmentResponse"
        "400":
          description: The submitted multipart upload is malformed, unsupported, empty, too large, or has mismatched declared content type and bytes.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Bill is missing, archived, deleted, unrelated, group-scoped, non-writable by the actor, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Visible bill state is not attachment-mutable.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Bill attachment upload could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
    get:
      summary: List personal bill attachment metadata
      description: Lists safe active receipt and supporting attachment metadata for one visible personal bill. The authenticated actor must be related to the bill as creator, owner/responsible editor, participant, or payer. Responses expose stable file IDs, bill ID, purpose, content type, size, and timestamps only, and exclude file bytes, storage object keys, provider paths, provider URLs, original filenames, auth/session fields, bill merchant/item details, OCR text, audit internals, and unrelated users.
      operationId: listPersonalBillAttachments
      security:
        - SessionBearerAuth: []
      parameters:
        - name: billId
          in: path
          required: true
          description: Personal bill ID whose active attachment metadata should be listed.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Safe bill attachment metadata visible to the authenticated actor. The list is empty when the bill is visible and has no active readable attachments.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillAttachmentListResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Bill is missing, archived, deleted, unrelated, group-scoped, inactive, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/bills/{billId}/attachments/{fileId}:
    delete:
      summary: Remove personal bill attachment
      description: Soft-removes one actor-owned active attachment from a visible attachment-mutable personal bill. The authenticated actor is derived from SettleoraSession and must be the bill creator or owner/responsible editor and the owner/creator of the linked file object. The API sets the association removal timestamp, marks the file object deleted through the lifecycle service, emits bounded audit, and returns no file bytes or storage internals.
      operationId: removePersonalBillAttachment
      security:
        - SessionBearerAuth: []
      parameters:
        - name: billId
          in: path
          required: true
          description: Personal bill ID whose attachment should be removed.
          schema:
            type: string
            format: uuid
        - name: fileId
          in: path
          required: true
          description: Stable file object ID to detach from the bill.
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: The attachment is no longer active for the bill.
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Bill or attachment file is missing, removed, deleted, wrong-purpose, wrong-owner, unrelated, not actor-owned, inactive, corrupt, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Visible bill state is not attachment-mutable.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Bill attachment removal could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/bills/{billId}/attachments/{fileId}/ocr-review:
    put:
      summary: Upsert personal bill receipt OCR review
      description: Saves provisional or reviewed OCR-derived receipt data for one existing active receipt attachment on a visible personal bill. The authenticated actor is derived from SettleoraSession and must be the bill creator or owner/responsible editor. The attachment must belong to the bill, be active, and have receipt purpose. This endpoint stores review/intake data only and does not mutate bill items, splits, settlements, balances, payments, storage bytes, OCR jobs, or worker state. It never accepts or returns raw OCR full text, file bytes, storage object keys, provider paths, signed URLs, auth/session fields, payment details, or unrelated bill data.
      operationId: upsertPersonalBillAttachmentOcrReview
      security:
        - SessionBearerAuth: []
      parameters:
        - name: billId
          in: path
          required: true
          description: Personal bill ID that owns the receipt attachment.
          schema:
            type: string
            format: uuid
        - name: fileId
          in: path
          required: true
          description: Stable receipt file object ID for the bill attachment.
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReceiptOcrReviewUpsertRequest"
      responses:
        "200":
          description: Existing receipt OCR review updated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReceiptOcrReviewResponse"
        "201":
          description: Receipt OCR review created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReceiptOcrReviewResponse"
        "400":
          description: The submitted review payload is malformed, unsupported, too large, or violates decimal/currency validation.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Bill or receipt attachment is missing, removed, deleted, wrong-purpose, unrelated, group-scoped, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Visible bill state is not OCR-review-mutable.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Receipt OCR review could not be saved.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
    get:
      summary: Get personal bill receipt OCR review
      description: Returns one active provisional/reviewed OCR review for an active personal bill receipt attachment. The authenticated actor must be able to see the bill and attachment. The response is bounded to reviewed candidate fields and excludes raw OCR full text, file bytes, storage/provider internals, auth/session data, payment details, raw audit metadata, and unrelated users.
      operationId: getPersonalBillAttachmentOcrReview
      security:
        - SessionBearerAuth: []
      parameters:
        - name: billId
          in: path
          required: true
          description: Personal bill ID that owns the receipt attachment.
          schema:
            type: string
            format: uuid
        - name: fileId
          in: path
          required: true
          description: Stable receipt file object ID for the bill attachment.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Active receipt OCR review visible to the authenticated actor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReceiptOcrReviewResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Bill, receipt attachment, or OCR review is missing, removed, deleted, wrong-purpose, unrelated, group-scoped, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Receipt OCR review access could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
    delete:
      summary: Remove personal bill receipt OCR review
      description: Soft-removes one active OCR review from a visible OCR-review-mutable personal bill receipt attachment. The authenticated actor is derived from SettleoraSession and must be the bill creator or owner/responsible editor. The endpoint removes only provisional/review intake state; it does not delete receipt bytes or mutate authoritative bill, split, settlement, balance, or payment records.
      operationId: removePersonalBillAttachmentOcrReview
      security:
        - SessionBearerAuth: []
      parameters:
        - name: billId
          in: path
          required: true
          description: Personal bill ID that owns the receipt attachment.
          schema:
            type: string
            format: uuid
        - name: fileId
          in: path
          required: true
          description: Stable receipt file object ID for the bill attachment.
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: The receipt OCR review is no longer active.
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Bill, receipt attachment, or OCR review is missing, removed, deleted, wrong-purpose, unrelated, group-scoped, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Visible bill state is not OCR-review-mutable.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Receipt OCR review removal could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/bills/{billId}/attachments/{fileId}/ocr-review/apply-preview:
    get:
      summary: Preview personal bill receipt OCR review apply result
      description: Converts one existing saved receipt OCR review for an active personal bill receipt attachment into a compact non-mutating bill-draft preview for user confirmation. The authenticated actor is derived from SettleoraSession and must be able to see the bill and receipt attachment; creator/owner mutation rights are not required for this read-only preview. The response includes bounded proposed bill/header fields, proposed line candidates, decimal-string summary totals, canApply, blockedReasons, and warnings only. It does not finalize OCR, create or mutate bill items, splits, settlements, balances, payments, storage bytes, files, OCR jobs, worker state, or audit rows, and it never exposes raw OCR full text, file bytes, storage object keys, provider paths, filenames, signed URLs, auth/session fields, payment details, or unrelated users.
      operationId: getPersonalBillAttachmentOcrReviewApplyPreview
      security:
        - SessionBearerAuth: []
      parameters:
        - name: billId
          in: path
          required: true
          description: Personal bill ID that owns the receipt attachment.
          schema:
            type: string
            format: uuid
        - name: fileId
          in: path
          required: true
          description: Stable receipt file object ID for the bill attachment.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Non-mutating apply preview for the visible receipt OCR review.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReceiptOcrReviewApplyPreviewResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Bill, receipt attachment, or OCR review is missing, removed, deleted, wrong-purpose, unrelated, group-scoped, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/bills/{billId}/attachments/{fileId}/ocr-review/apply:
    post:
      summary: Apply personal bill receipt OCR review to draft item candidates
      description: Explicitly applies one existing reviewed receipt OCR review for an active personal bill receipt attachment into draft OCR-generated bill item candidates. The authenticated actor is derived from SettleoraSession and must be the bill creator or owner/responsible editor. The request carries only the apply mode and expected saved review update timestamp; the server rechecks bill, attachment, file, review, draft state, mutation rights, review status/source, and apply-preview validation before writing. This Day 1 operation supports only replace_draft_ocr_items, never applies automatically, never accepts client-submitted line IDs, totals, split allocations, actor identity, settlement/payment data, file metadata, or OCR truth, and does not mutate settlement, payment, proof, balance, residual, storage, file-object, OCR job, worker, or receipt-byte state.
      operationId: applyPersonalBillAttachmentOcrReview
      security:
        - SessionBearerAuth: []
      parameters:
        - name: billId
          in: path
          required: true
          description: Draft personal bill ID that owns the receipt attachment.
          schema:
            type: string
            format: uuid
        - name: fileId
          in: path
          required: true
          description: Stable receipt file object ID for the bill attachment.
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReceiptOcrReviewApplyRequest"
      responses:
        "200":
          description: Reviewed OCR review applied to safe draft OCR-generated bill item candidates.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReceiptOcrReviewApplyResponse"
        "400":
          description: The submitted apply request is malformed, stale-guard fields are missing, or the apply mode is unsupported.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Bill, receipt attachment, or OCR review is missing, removed, deleted, wrong-purpose, unrelated, group-scoped, not visible, not mutable by the actor, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Visible bill/review state is not safe for draft OCR item candidate apply, the expected review timestamp is stale, preview validation blocks apply, downstream settlement state exists, or the current draft bill shape would require inferring shared split policy.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Receipt OCR review apply could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/receipt-ocr-reviews:
    get:
      summary: List receipt OCR review queue
      description: Lists bounded receipt OCR review summaries visible to the authenticated actor across personal bills and active authorized group bills. The actor is derived from SettleoraSession; personal and group visibility are rechecked against existing bill, group membership, attachment, and file-object constraints. Optional status/source filters use the existing bounded review values and limit is capped. The endpoint is read-only and does not expose file bytes, storage object keys, provider paths, signed URLs, filenames, raw OCR full text, auth/session data, payment details, bill items, split/settlement/balance state, unrelated users, OCR jobs, worker state, or request bodies.
      operationId: listReceiptOcrReviews
      security:
        - SessionBearerAuth: []
      parameters:
        - name: status
          in: query
          required: false
          description: Optional exact review status filter.
          schema:
            $ref: "#/components/schemas/ReceiptOcrReviewStatus"
        - name: source
          in: query
          required: false
          description: Optional exact review source filter.
          schema:
            $ref: "#/components/schemas/ReceiptOcrReviewSource"
        - name: limit
          in: query
          required: false
          description: Maximum number of latest review summaries to return.
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
      responses:
        "200":
          description: Receipt OCR review summaries visible to the authenticated actor. The list is empty when no visible reviews match the optional filters.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReceiptOcrReviewListResponse"
        "400":
          description: The submitted queue filters are unsupported or out of range.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: The current profile is unavailable or not allowed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/bills/{billId}/attachments/{fileId}/content:
    get:
      summary: Read personal bill attachment content
      description: Streams one active bill attachment through API authorization every time. The authenticated actor must be related to the personal bill as creator, owner/responsible editor, participant, or payer. The linked file must be active, owned and created by the attachment creator, have the expected bill attachment purpose, and use an allowed content type. The response uses conservative content headers and never exposes storage object keys, provider paths, provider URLs, original filenames, auth/session fields, OCR text, thumbnails, or unrelated data.
      operationId: getPersonalBillAttachmentContent
      security:
        - SessionBearerAuth: []
      parameters:
        - name: billId
          in: path
          required: true
          description: Personal bill ID whose attachment content should be read.
          schema:
            type: string
            format: uuid
        - name: fileId
          in: path
          required: true
          description: Stable file object ID to stream.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Bill attachment bytes authorized through the personal bill attachment boundary.
          headers:
            X-Content-Type-Options:
              schema:
                type: string
              description: Set to nosniff.
            Content-Disposition:
              schema:
                type: string
              description: Conservative attachment disposition with no submitted filename.
          content:
            image/png:
              schema:
                type: string
                format: binary
            image/jpeg:
              schema:
                type: string
                format: binary
            image/webp:
              schema:
                type: string
                format: binary
            application/pdf:
              schema:
                type: string
                format: binary
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Bill or attachment file is missing, removed, deleted, wrong-purpose, wrong-owner, inactive, corrupt, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Bill attachment read audit could not be completed after content authorization.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/bills/{billId}/revisions:
    get:
      summary: List bill revisions
      description: Lists revision proposals for one visible non-archived bill. The authenticated actor must be related to the bill as creator, owner/responsible editor, participant, payer, or an active group member for a related group bill. Missing, deleted, archived, unrelated, inactive, removed-member, and not-visible cases fail closed without exposing bill existence.
      operationId: listBillRevisions
      security:
        - SessionBearerAuth: []
      parameters:
        - name: billId
          in: path
          required: true
          description: Bill ID whose revision proposals should be listed.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Bill revisions visible to the authenticated actor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillRevisionListResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Bill is missing, archived, deleted, unrelated, inactive, removed from group access, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
    post:
      summary: Create bill revision proposal
      description: Creates a draft revision proposal for one visible confirmed or rejected bill. Actor identity is derived from the authenticated session; clients submit only a bounded candidate money snapshot. The server validates relationship, profile visibility, one-active-pending-revision policy, same-currency Day 1 rules, participant-set stability, payer relationship, participant share totals, and payer contribution totals. Creation does not mutate settlement requests, settlement lines, payments, residuals, proof files, or settlement history.
      operationId: createBillRevision
      security:
        - SessionBearerAuth: []
      parameters:
        - name: billId
          in: path
          required: true
          description: Bill ID receiving a revision proposal.
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateBillRevisionProposalRequest"
      responses:
        "201":
          description: Draft bill revision proposal created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillRevisionResponse"
        "400":
          description: Request body is malformed or contains unsupported fields.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Bill is missing, archived, deleted, unrelated, inactive, removed from group access, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Visible bill state, one-active-pending-revision policy, or revision domain policy blocks creation.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Bill revision write could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/bills/{billId}/revisions/{revisionId}:
    get:
      summary: Get bill revision
      description: Returns one visible bill revision proposal for a related authenticated actor. The response exposes the review calculation hash needed for revision-specific approval, bounded status fields, money strings, affected-participant flags, payer-confirmation flags, and no auth/session, storage, proof, settlement, or provider internals.
      operationId: getBillRevision
      security:
        - SessionBearerAuth: []
      parameters:
        - name: billId
          in: path
          required: true
          description: Bill ID.
          schema:
            type: string
            format: uuid
        - name: revisionId
          in: path
          required: true
          description: Bill revision proposal ID.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Bill revision visible to the authenticated actor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillRevisionResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Bill or revision is missing, archived, deleted, unrelated, inactive, removed from group access, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
    patch:
      summary: Revise and resubmit bill revision
      description: Supersedes one active pending proposal and creates the replacement submitted proposal from a bounded candidate money snapshot. Only the proposer is supported by the current domain policy. Prior approvals on the superseded proposal are invalidated and do not carry to the replacement. This operation does not mutate settlement requests, settlement lines, payments, residuals, proof files, or settlement history.
      operationId: reviseBillRevision
      security:
        - SessionBearerAuth: []
      parameters:
        - name: billId
          in: path
          required: true
          description: Bill ID.
          schema:
            type: string
            format: uuid
        - name: revisionId
          in: path
          required: true
          description: Active pending bill revision proposal ID to supersede.
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateBillRevisionProposalRequest"
      responses:
        "200":
          description: Replacement submitted bill revision proposal created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillRevisionResponse"
        "400":
          description: Request body is malformed or contains unsupported fields.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Bill or revision is missing, archived, deleted, unrelated, inactive, removed from group access, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Revision state, proposer policy, visible bill state, or revision domain policy blocks revise/resubmit.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Bill revision write could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/bills/{billId}/revisions/{revisionId}/submit:
    post:
      summary: Submit bill revision
      description: Submits one draft revision proposal for review. The authenticated actor must be the proposal creator under the current domain policy. This action accepts no request body and does not accept client-submitted actor, participant, payer, bill, group, settlement, status, audit, file, proof, auth, or session identity.
      operationId: submitBillRevision
      security:
        - SessionBearerAuth: []
      parameters:
        - name: billId
          in: path
          required: true
          description: Bill ID.
          schema:
            type: string
            format: uuid
        - name: revisionId
          in: path
          required: true
          description: Draft bill revision proposal ID.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Bill revision submitted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillRevisionResponse"
        "400":
          description: A request body was submitted even though revision submission accepts no body.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Bill or revision is missing, archived, deleted, unrelated, inactive, removed from group access, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Revision state or proposer policy blocks submission.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Bill revision write could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/bills/{billId}/revisions/{revisionId}/withdraw:
    post:
      summary: Withdraw bill revision
      description: Withdraws one active pending revision proposal. The current domain policy restricts withdrawal to the proposal creator; responsible-editor withdrawal is not exposed until a reviewed domain policy supports it. This action accepts no request body and does not mutate settlement records.
      operationId: withdrawBillRevision
      security:
        - SessionBearerAuth: []
      parameters:
        - name: billId
          in: path
          required: true
          description: Bill ID.
          schema:
            type: string
            format: uuid
        - name: revisionId
          in: path
          required: true
          description: Active pending bill revision proposal ID.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Bill revision withdrawn.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillRevisionResponse"
        "400":
          description: A request body was submitted even though revision withdrawal accepts no body.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Bill or revision is missing, archived, deleted, unrelated, inactive, removed from group access, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Revision state or proposer policy blocks withdrawal.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Bill revision write could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/bills/{billId}/revisions/{revisionId}/approve:
    post:
      summary: Approve bill revision
      description: Records the authenticated actor's affected-participant approval for one submitted revision proposal. The route revision ID plus submitted accepted amount, currency, and calculation hash must match the server-stored approval basis exactly. Stale, wrong-hash, wrong-amount, wrong-currency, superseded, rejected, withdrawn, unaffected already-preserved, and unauthorized approvals fail safely.
      operationId: approveBillRevision
      security:
        - SessionBearerAuth: []
      parameters:
        - name: billId
          in: path
          required: true
          description: Bill ID.
          schema:
            type: string
            format: uuid
        - name: revisionId
          in: path
          required: true
          description: Submitted bill revision proposal ID.
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApproveBillRevisionRequest"
      responses:
        "200":
          description: Revision approval recorded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillRevisionResponse"
        "400":
          description: Request body is malformed or contains unsupported fields.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Bill or revision is missing, archived, deleted, unrelated, inactive, removed from group access, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Revision state, actor, accepted amount, currency, or calculation hash does not match the current review basis.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Bill revision write could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/bills/{billId}/revisions/{revisionId}/reject:
    post:
      summary: Reject bill revision
      description: Rejects one submitted bill revision proposal using the authenticated actor as the rejecting participant. The current domain foundation stores bounded status/timestamp state only, so this endpoint accepts no reason body and stores no free text. Rejected approvals do not carry to active bill truth, and settlement records are not mutated.
      operationId: rejectBillRevision
      security:
        - SessionBearerAuth: []
      parameters:
        - name: billId
          in: path
          required: true
          description: Bill ID.
          schema:
            type: string
            format: uuid
        - name: revisionId
          in: path
          required: true
          description: Submitted bill revision proposal ID.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Revision rejection recorded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillRevisionResponse"
        "400":
          description: A request body was submitted even though revision rejection accepts no body.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Bill or revision is missing, archived, deleted, unrelated, inactive, removed from group access, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Revision state or actor policy blocks rejection.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Bill revision write could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/bills/{billId}/revisions/{revisionId}/apply:
    post:
      summary: Apply bill revision
      description: Applies the latest eligible submitted bill revision to the active bill state for an owner/responsible editor. The authenticated actor is derived from SettleoraSession; the request accepts no body. The server requires exact revision-specific approvals, confirmed required payer confirmation, visible bill access, and no existing settlement request, request-line, payment, allocation, residual, proof, or settled-participant state. This first safe apply slice does not mutate settlement rows, proof files, receipt files, storage objects, OCR data, or audit history.
      operationId: applyBillRevision
      security:
        - SessionBearerAuth: []
      parameters:
        - name: billId
          in: path
          required: true
          description: Bill ID.
          schema:
            type: string
            format: uuid
        - name: revisionId
          in: path
          required: true
          description: Submitted bill revision proposal ID to apply.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Revision applied to active bill state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillRevisionResponse"
        "400":
          description: A request body was submitted even though revision apply accepts no body.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Bill or revision is missing, archived, deleted, unrelated, inactive, removed from group access, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Revision state, owner/responsible-editor policy, approval basis, payer confirmation state, latest-submitted policy, or existing settlement state blocks apply. Existing settlement state is a bounded conflict because settlement adjustment/reopen policy is not implemented in this slice.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Bill revision write could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/bills/{billId}/settlement-candidates:
    get:
      summary: List personal bill settlement candidates
      description: Returns read-only settlement candidate previews for one non-archived personal bill visible to the authenticated actor through creator, participant, or payer relationship. Only candidates where the current actor is the debtor or creditor are returned; unrelated candidate pairs, participant net-position lists, payment details, proof metadata, file internals, and settlement runtime records are not exposed or written.
      operationId: listPersonalBillSettlementCandidates
      security:
        - SessionBearerAuth: []
      parameters:
        - name: billId
          in: path
          required: true
          description: Personal bill ID whose settlement candidates should be previewed.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Candidate previews involving the authenticated actor. The list can be empty when the visible confirmed bill has no candidates involving the actor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SettlementCandidateListResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Bill is missing, archived, deleted, group-scoped, unrelated, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Visible bill state or persisted bill money basis is not eligible for candidate derivation.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/bills/{billId}/settlement-requests:
    post:
      summary: Create personal bill settlement request
      description: Creates one settlement request from a freshly re-derived confirmed personal bill settlement candidate. The authenticated actor is resolved server-side and must be the debtor or creditor for the submitted server-derived candidate key. Debtor, creditor, amount, currency, bill, requester, status, payment details, proof/file IDs, and auth/session fields are never accepted from the body.
      operationId: createPersonalBillSettlementRequest
      security:
        - SessionBearerAuth: []
      parameters:
        - name: billId
          in: path
          required: true
          description: Personal bill ID that owns the settlement candidate.
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSettlementRequestRequest"
      responses:
        "201":
          description: Settlement request created from the matched candidate.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SettlementRequestResponse"
        "400":
          description: Request body is malformed or contains unsupported fields.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Bill is missing, archived, deleted, group-scoped, unrelated, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Visible bill state, stale candidate key, non-party actor, duplicate active request, or persisted bill money basis blocks creation.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/bills/{billId}/submit:
    post:
      summary: Submit personal bill
      description: Submits a draft personal bill created by the authenticated actor. Submission moves the bill to pending confirmation, resets draft participant acknowledgement state, and writes bounded safe audit metadata. Rejected bill correction/resubmission must use the bill revision proposal workflow and is rejected here to avoid silently resetting all accepted participants.
      operationId: submitPersonalBill
      security:
        - SessionBearerAuth: []
      parameters:
        - name: billId
          in: path
          required: true
          description: Personal bill ID to submit.
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: Personal bill submitted.
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Bill is missing, archived, a group bill, unrelated, not created by the actor, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Bill state does not allow submission.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Personal bill workflow write could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/bills/{billId}/participants/{userProfileId}/accept:
    post:
      summary: Accept personal bill participant share
      description: Accepts the authenticated actor's own participant row on a visible personal bill in pending confirmation. The route userProfileId must equal the current actor profile ID; final participant acceptance confirms the bill and writes bounded safe audit metadata.
      operationId: acceptPersonalBillParticipant
      security:
        - SessionBearerAuth: []
      parameters:
        - name: billId
          in: path
          required: true
          description: Personal bill ID.
          schema:
            type: string
            format: uuid
        - name: userProfileId
          in: path
          required: true
          description: Participant user profile ID. Must be the authenticated actor's own profile ID.
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: Participant acceptance recorded.
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Bill or participant is missing, archived, group-scoped, unrelated, route-mismatched, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Bill or participant state does not allow acceptance.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Personal bill workflow write could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/bills/{billId}/participants/{userProfileId}/reject:
    post:
      summary: Reject personal bill participant share
      description: Rejects the authenticated actor's own participant row on a visible personal bill in pending confirmation using a bounded reason code. The route userProfileId must equal the current actor profile ID; rejection moves the bill to rejected and writes bounded safe audit metadata.
      operationId: rejectPersonalBillParticipant
      security:
        - SessionBearerAuth: []
      parameters:
        - name: billId
          in: path
          required: true
          description: Personal bill ID.
          schema:
            type: string
            format: uuid
        - name: userProfileId
          in: path
          required: true
          description: Participant user profile ID. Must be the authenticated actor's own profile ID.
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RejectBillParticipantRequest"
      responses:
        "204":
          description: Participant rejection recorded.
        "400":
          description: The submitted rejection request is invalid.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Bill or participant is missing, archived, group-scoped, unrelated, route-mismatched, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Bill or participant state does not allow rejection.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Personal bill workflow write could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/groups/{groupId}/bills:
    post:
      summary: Create group bill
      description: Creates a draft group bill for a non-deleted group where the authenticated actor is an active member. The route groupId is the only group identity input; the API derives creator/current actor server-side, validates split and payer profiles as active group members, calculates totals and resolved shares server-side, and rejects client-submitted ownership, creator, auth, session, file, or unrelated identity fields.
      operationId: createGroupBill
      security:
        - SessionBearerAuth: []
      parameters:
        - name: groupId
          in: path
          required: true
          description: Group ID that owns the bill.
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateGroupBillRequest"
      responses:
        "201":
          description: Group draft bill created for the authenticated active group member.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GroupBillResponse"
        "400":
          description: The submitted group bill request is invalid.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Group, bill, profile, membership, or submitted member identity is missing, deleted, unrelated, inactive, or unavailable.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Group bill creation could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
    get:
      summary: List group bills
      description: Lists non-archived bills in the route group only when the authenticated actor is an active member of that non-deleted group. Personal, archived, unrelated, and other-group bills are excluded. This first foundation response is intentionally unpaginated.
      operationId: listGroupBills
      security:
        - SessionBearerAuth: []
      parameters:
        - name: groupId
          in: path
          required: true
          description: Group ID whose bills should be listed.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Group bills visible to the authenticated active group member.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GroupBillListResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Group is missing, deleted, unrelated, inactive, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/groups/{groupId}/bills/{billId}:
    get:
      summary: Get group bill
      description: Returns one non-archived bill in the route group only when the authenticated actor is an active member of that non-deleted group. Missing, archived, personal, unrelated, and other-group bills return the same safe 404.
      operationId: getGroupBill
      security:
        - SessionBearerAuth: []
      parameters:
        - name: groupId
          in: path
          required: true
          description: Group ID that owns the bill.
          schema:
            type: string
            format: uuid
        - name: billId
          in: path
          required: true
          description: Group bill ID to read.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Group bill visible to the authenticated active group member.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GroupBillResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Group or bill is missing, deleted, archived, personal, unrelated, in another group, inactive, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/groups/{groupId}/bills/{billId}/attachments:
    post:
      summary: Attach group bill file
      description: Uploads one purpose-specific receipt or supporting attachment for a visible group bill. The authenticated actor is derived from SettleoraSession, must have active route-group access, must be related to the bill as creator, owner/responsible editor, participant, or payer, and must be the bill creator or owner/responsible editor for upload. The API validates a bounded multipart upload, writes bytes through the storage lifecycle foundation, creates a receipt_image or supporting_attachment file object plus an expense_bill_attachments association, emits bounded audit, and returns safe metadata only. This is not a generic file API and never accepts or returns storage object keys, provider paths, provider URLs, original filenames, auth/session fields, OCR text, raw request bodies, or unrelated bill data.
      operationId: attachGroupBillAttachment
      security:
        - SessionBearerAuth: []
      parameters:
        - name: groupId
          in: path
          required: true
          description: Group ID that owns the bill.
          schema:
            type: string
            format: uuid
        - name: billId
          in: path
          required: true
          description: Group bill ID that should receive the attachment.
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/AttachBillAttachmentRequest"
      responses:
        "201":
          description: Bill attachment created for the visible group bill.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillAttachmentResponse"
        "400":
          description: The submitted multipart upload is malformed, unsupported, empty, too large, or has mismatched declared content type and bytes.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Group, bill, or actor relationship is missing, deleted, archived, unrelated, removed, inactive, non-writable by the actor, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Visible bill state is not attachment-mutable.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Bill attachment upload could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
    get:
      summary: List group bill attachment metadata
      description: Lists safe active receipt and supporting attachment metadata for one visible group bill. The authenticated actor must have active route-group access and be related to the bill as creator, owner/responsible editor, participant, or payer. Responses expose stable file IDs, bill ID, purpose, content type, size, and timestamps only, and exclude file bytes, storage object keys, provider paths, provider URLs, original filenames, auth/session fields, bill merchant/item details, OCR text, audit internals, and unrelated users.
      operationId: listGroupBillAttachments
      security:
        - SessionBearerAuth: []
      parameters:
        - name: groupId
          in: path
          required: true
          description: Group ID that owns the bill.
          schema:
            type: string
            format: uuid
        - name: billId
          in: path
          required: true
          description: Group bill ID whose active attachment metadata should be listed.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Safe bill attachment metadata visible to the authenticated actor. The list is empty when the bill is visible and has no active readable attachments.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillAttachmentListResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Group, bill, or actor relationship is missing, deleted, archived, unrelated, inactive, removed, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/groups/{groupId}/bills/{billId}/attachments/{fileId}:
    delete:
      summary: Remove group bill attachment
      description: Soft-removes one actor-owned active attachment from a visible attachment-mutable group bill. The authenticated actor is derived from SettleoraSession, must have active route-group access, must be the bill creator or owner/responsible editor, and must be the owner/creator of the linked file object. The API sets the association removal timestamp, marks the file object deleted through the lifecycle service, emits bounded audit, and returns no file bytes or storage internals.
      operationId: removeGroupBillAttachment
      security:
        - SessionBearerAuth: []
      parameters:
        - name: groupId
          in: path
          required: true
          description: Group ID that owns the bill.
          schema:
            type: string
            format: uuid
        - name: billId
          in: path
          required: true
          description: Group bill ID whose attachment should be removed.
          schema:
            type: string
            format: uuid
        - name: fileId
          in: path
          required: true
          description: Stable file object ID to detach from the bill.
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: The attachment is no longer active for the bill.
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Group, bill, or attachment file is missing, removed, deleted, wrong-purpose, wrong-owner, unrelated, not actor-owned, inactive, corrupt, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Visible bill state is not attachment-mutable.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Bill attachment removal could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/groups/{groupId}/bills/{billId}/attachments/{fileId}/ocr-review:
    put:
      summary: Upsert group bill receipt OCR review
      description: Saves provisional or reviewed OCR-derived receipt data for one existing active receipt attachment on a visible group bill. The authenticated actor is derived from SettleoraSession, must have active route-group access, and must be the bill creator or owner/responsible editor. The attachment must belong to the route bill and group, be active, and have receipt purpose. This endpoint stores review/intake data only and does not mutate bill items, splits, settlements, balances, payments, storage bytes, OCR jobs, or worker state. It never accepts or returns raw OCR full text, file bytes, storage object keys, provider paths, signed URLs, auth/session fields, payment details, or unrelated bill data.
      operationId: upsertGroupBillAttachmentOcrReview
      security:
        - SessionBearerAuth: []
      parameters:
        - name: groupId
          in: path
          required: true
          description: Group ID that owns the bill.
          schema:
            type: string
            format: uuid
        - name: billId
          in: path
          required: true
          description: Group bill ID that owns the receipt attachment.
          schema:
            type: string
            format: uuid
        - name: fileId
          in: path
          required: true
          description: Stable receipt file object ID for the bill attachment.
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReceiptOcrReviewUpsertRequest"
      responses:
        "200":
          description: Existing receipt OCR review updated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReceiptOcrReviewResponse"
        "201":
          description: Receipt OCR review created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReceiptOcrReviewResponse"
        "400":
          description: The submitted review payload is malformed, unsupported, too large, or violates decimal/currency validation.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Group, bill, or receipt attachment is missing, removed, deleted, wrong-purpose, unrelated, removed-member, inactive, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Visible bill state is not OCR-review-mutable.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Receipt OCR review could not be saved.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
    get:
      summary: Get group bill receipt OCR review
      description: Returns one active provisional/reviewed OCR review for an active group bill receipt attachment. The authenticated actor must have active route-group access and be able to see the bill and attachment. The response is bounded to reviewed candidate fields and excludes raw OCR full text, file bytes, storage/provider internals, auth/session data, payment details, raw audit metadata, and unrelated users.
      operationId: getGroupBillAttachmentOcrReview
      security:
        - SessionBearerAuth: []
      parameters:
        - name: groupId
          in: path
          required: true
          description: Group ID that owns the bill.
          schema:
            type: string
            format: uuid
        - name: billId
          in: path
          required: true
          description: Group bill ID that owns the receipt attachment.
          schema:
            type: string
            format: uuid
        - name: fileId
          in: path
          required: true
          description: Stable receipt file object ID for the bill attachment.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Active receipt OCR review visible to the authenticated actor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReceiptOcrReviewResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Group, bill, receipt attachment, or OCR review is missing, removed, deleted, wrong-purpose, unrelated, inactive, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Receipt OCR review access could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
    delete:
      summary: Remove group bill receipt OCR review
      description: Soft-removes one active OCR review from a visible OCR-review-mutable group bill receipt attachment. The authenticated actor is derived from SettleoraSession, must have active route-group access, and must be the bill creator or owner/responsible editor. The endpoint removes only provisional/review intake state; it does not delete receipt bytes or mutate authoritative bill, split, settlement, balance, or payment records.
      operationId: removeGroupBillAttachmentOcrReview
      security:
        - SessionBearerAuth: []
      parameters:
        - name: groupId
          in: path
          required: true
          description: Group ID that owns the bill.
          schema:
            type: string
            format: uuid
        - name: billId
          in: path
          required: true
          description: Group bill ID that owns the receipt attachment.
          schema:
            type: string
            format: uuid
        - name: fileId
          in: path
          required: true
          description: Stable receipt file object ID for the bill attachment.
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: The receipt OCR review is no longer active.
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Group, bill, receipt attachment, or OCR review is missing, removed, deleted, wrong-purpose, unrelated, inactive, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Visible bill state is not OCR-review-mutable.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Receipt OCR review removal could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/groups/{groupId}/bills/{billId}/attachments/{fileId}/ocr-review/apply-preview:
    get:
      summary: Preview group bill receipt OCR review apply result
      description: Converts one existing saved receipt OCR review for an active group bill receipt attachment into a compact non-mutating bill-draft preview for user confirmation. The authenticated actor is derived from SettleoraSession, must have active route-group access, and must be able to see the bill and receipt attachment; creator/owner mutation rights are not required for this read-only preview. The response includes bounded proposed bill/header fields, proposed line candidates, decimal-string summary totals, canApply, blockedReasons, and warnings only. It does not finalize OCR, create or mutate bill items, splits, settlements, balances, payments, storage bytes, files, OCR jobs, worker state, or audit rows, and it never exposes raw OCR full text, file bytes, storage object keys, provider paths, filenames, signed URLs, auth/session fields, payment details, or unrelated users.
      operationId: getGroupBillAttachmentOcrReviewApplyPreview
      security:
        - SessionBearerAuth: []
      parameters:
        - name: groupId
          in: path
          required: true
          description: Group ID that owns the bill.
          schema:
            type: string
            format: uuid
        - name: billId
          in: path
          required: true
          description: Group bill ID that owns the receipt attachment.
          schema:
            type: string
            format: uuid
        - name: fileId
          in: path
          required: true
          description: Stable receipt file object ID for the bill attachment.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Non-mutating apply preview for the visible receipt OCR review.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReceiptOcrReviewApplyPreviewResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Group, bill, receipt attachment, or OCR review is missing, removed, deleted, wrong-purpose, unrelated, inactive, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/groups/{groupId}/bills/{billId}/attachments/{fileId}/ocr-review/apply:
    post:
      summary: Apply group bill receipt OCR review to draft item candidates
      description: Explicitly applies one existing reviewed receipt OCR review for an active group bill receipt attachment into draft OCR-generated bill item candidates when the group bill is still safely draft-only and does not require inferring shared split policy. The authenticated actor is derived from SettleoraSession, must have active route-group access, and must be the bill creator or owner/responsible editor. The request carries only the apply mode and expected saved review update timestamp; the server rechecks group, bill, attachment, file, review, draft state, mutation rights, review status/source, and apply-preview validation before writing. This Day 1 operation supports only replace_draft_ocr_items, never applies automatically, never accepts client-submitted line IDs, totals, split allocations, actor identity, settlement/payment data, file metadata, or OCR truth, and does not mutate settlement, payment, proof, balance, residual, storage, file-object, OCR job, worker, or receipt-byte state.
      operationId: applyGroupBillAttachmentOcrReview
      security:
        - SessionBearerAuth: []
      parameters:
        - name: groupId
          in: path
          required: true
          description: Group ID that owns the bill.
          schema:
            type: string
            format: uuid
        - name: billId
          in: path
          required: true
          description: Draft group bill ID that owns the receipt attachment.
          schema:
            type: string
            format: uuid
        - name: fileId
          in: path
          required: true
          description: Stable receipt file object ID for the bill attachment.
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReceiptOcrReviewApplyRequest"
      responses:
        "200":
          description: Reviewed OCR review applied to safe draft OCR-generated bill item candidates.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReceiptOcrReviewApplyResponse"
        "400":
          description: The submitted apply request is malformed, stale-guard fields are missing, or the apply mode is unsupported.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Group, bill, receipt attachment, or OCR review is missing, removed, deleted, wrong-purpose, unrelated, inactive, not visible, not mutable by the actor, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Visible bill/review state is not safe for draft OCR item candidate apply, the expected review timestamp is stale, preview validation blocks apply, downstream settlement state exists, or the current draft bill shape would require inferring shared split policy.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Receipt OCR review apply could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/groups/{groupId}/receipt-ocr-reviews:
    get:
      summary: List group receipt OCR review queue
      description: Lists bounded receipt OCR review summaries visible to the authenticated actor within one active route group. The actor is derived from SettleoraSession and must have active route-group access; each review must still be tied to a visible bill relationship, an active receipt attachment, and an active receipt_image file object. Optional status/source filters use the existing bounded review values and limit is capped. The endpoint is read-only and does not expose file bytes, storage object keys, provider paths, signed URLs, filenames, raw OCR full text, auth/session data, payment details, bill items, split/settlement/balance state, unrelated users, OCR jobs, worker state, or request bodies.
      operationId: listGroupReceiptOcrReviews
      security:
        - SessionBearerAuth: []
      parameters:
        - name: groupId
          in: path
          required: true
          description: Route group ID whose visible receipt OCR review queue should be listed.
          schema:
            type: string
            format: uuid
        - name: status
          in: query
          required: false
          description: Optional exact review status filter.
          schema:
            $ref: "#/components/schemas/ReceiptOcrReviewStatus"
        - name: source
          in: query
          required: false
          description: Optional exact review source filter.
          schema:
            $ref: "#/components/schemas/ReceiptOcrReviewSource"
        - name: limit
          in: query
          required: false
          description: Maximum number of latest review summaries to return.
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
      responses:
        "200":
          description: Group receipt OCR review summaries visible to the authenticated actor. The list is empty when no visible reviews match the optional filters.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReceiptOcrReviewListResponse"
        "400":
          description: The submitted queue filters are unsupported or out of range.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Group, membership, bill visibility, or actor relationship is missing, deleted, archived, unrelated, inactive, removed, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/groups/{groupId}/bills/{billId}/attachments/{fileId}/content:
    get:
      summary: Read group bill attachment content
      description: Streams one active group bill attachment through API authorization every time. The authenticated actor must have active route-group access and be related to the bill as creator, owner/responsible editor, participant, or payer. The linked file must be active, owned and created by the attachment creator, have the expected bill attachment purpose, and use an allowed content type. The response uses conservative content headers and never exposes storage object keys, provider paths, provider URLs, original filenames, auth/session fields, OCR text, thumbnails, or unrelated data.
      operationId: getGroupBillAttachmentContent
      security:
        - SessionBearerAuth: []
      parameters:
        - name: groupId
          in: path
          required: true
          description: Group ID that owns the bill.
          schema:
            type: string
            format: uuid
        - name: billId
          in: path
          required: true
          description: Group bill ID whose attachment content should be read.
          schema:
            type: string
            format: uuid
        - name: fileId
          in: path
          required: true
          description: Stable file object ID to stream.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Bill attachment bytes authorized through the group bill attachment boundary.
          headers:
            X-Content-Type-Options:
              schema:
                type: string
              description: Set to nosniff.
            Content-Disposition:
              schema:
                type: string
              description: Conservative attachment disposition with no submitted filename.
          content:
            image/png:
              schema:
                type: string
                format: binary
            image/jpeg:
              schema:
                type: string
                format: binary
            image/webp:
              schema:
                type: string
                format: binary
            application/pdf:
              schema:
                type: string
                format: binary
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Group, bill, or attachment file is missing, removed, deleted, wrong-purpose, wrong-owner, inactive, corrupt, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Bill attachment read audit could not be completed after content authorization.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/groups/{groupId}/bills/{billId}/settlement-candidates:
    get:
      summary: List group bill settlement candidates
      description: Returns read-only settlement candidate previews for one non-archived bill in the route group. The actor must have active group access and must be related to the bill as creator, participant, or payer; group membership alone does not expose candidate details. Only candidates where the current actor is the debtor or creditor are returned; unrelated candidate pairs, participant net-position lists, payment details, proof metadata, file internals, and settlement runtime records are not exposed or written.
      operationId: listGroupBillSettlementCandidates
      security:
        - SessionBearerAuth: []
      parameters:
        - name: groupId
          in: path
          required: true
          description: Group ID that owns the bill.
          schema:
            type: string
            format: uuid
        - name: billId
          in: path
          required: true
          description: Group bill ID whose settlement candidates should be previewed.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Candidate previews involving the authenticated actor. The list can be empty when the visible confirmed bill has no candidates involving the actor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SettlementCandidateListResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Group or bill is missing, deleted, archived, personal, unrelated, in another group, inactive, removed, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Visible bill state or persisted bill money basis is not eligible for candidate derivation.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/groups/{groupId}/bills/{billId}/settlement-requests:
    post:
      summary: Create group bill settlement request
      description: Creates one settlement request from a freshly re-derived confirmed group bill settlement candidate. The authenticated actor must have active group access and must be the debtor or creditor for the submitted server-derived candidate key; group membership alone does not authorize unrelated candidate creation. Debtor, creditor, amount, currency, group, bill, requester, status, payment details, proof/file IDs, and auth/session fields are never accepted from the body.
      operationId: createGroupBillSettlementRequest
      security:
        - SessionBearerAuth: []
      parameters:
        - name: groupId
          in: path
          required: true
          description: Group ID that owns the bill.
          schema:
            type: string
            format: uuid
        - name: billId
          in: path
          required: true
          description: Group bill ID that owns the settlement candidate.
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSettlementRequestRequest"
      responses:
        "201":
          description: Settlement request created from the matched candidate.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SettlementRequestResponse"
        "400":
          description: Request body is malformed or contains unsupported fields.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Group or bill is missing, deleted, archived, personal, unrelated, in another group, inactive, removed, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Visible bill state, stale candidate key, non-party actor, duplicate active request, or persisted bill money basis blocks creation.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/settlements/baskets:
    post:
      summary: Create settlement basket request
      description: Creates one same-currency settlement request from all eligible outstanding confirmed bill candidate lines between the authenticated actor and one counterparty for the supported pay-all-outstanding selection mode. The current actor, debtor/creditor direction, selected source bills, candidate keys, amounts, active accepted bill revision trace, duplicate active request exclusion, group visibility, selected total, request lines, request status, and bounded audit metadata are derived server-side at write time. The client does not submit line IDs, debtor/creditor/requester IDs, money totals, status, payment data, proof/file data, audit metadata, auth/session data, bill merchant/item details, receipt/OCR contents, or unrelated users.
      operationId: createSettlementBasketRequest
      security:
        - SessionBearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSettlementBasketRequest"
      responses:
        "201":
          description: Settlement request created from the server-derived concrete basket lines.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SettlementRequestResponse"
        "400":
          description: Request body is malformed, contains unsupported fields, unsupported direction or selection mode values, invalid UUIDs, self-counterparty, or invalid currency.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Actor profile, counterparty profile, group, or required active group membership visibility is missing, deleted, inactive, removed, unrelated, or unavailable.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: No eligible outstanding same-currency lines remain, selected lines are stale, duplicate active requests already cover the selected candidates, or current server state blocks basket creation.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Settlement basket write could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/settlements/baskets/preview:
    post:
      summary: Preview settlement basket
      description: Returns a read-only same-currency basket preview for all eligible outstanding confirmed bill settlement candidate lines between the authenticated actor and one counterparty. The current actor, debtor/creditor direction, eligible source bills, candidate keys, amounts, active accepted bill revision trace, duplicate active request exclusion, and group visibility are derived server-side. The endpoint does not create settlement requests, request lines, payments, allocations, residuals, proof files, notifications, jobs, or projection rows, and it excludes merchant/item details, payment details, proof/file/storage internals, auth/session data, raw audit metadata, raw request bodies, unrelated users, and receipt/OCR contents.
      operationId: previewSettlementBasket
      security:
        - SessionBearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SettlementBasketPreviewRequest"
      responses:
        "200":
          description: Read-only concrete line preview for the selected current-actor/counterparty scope. The line list can be empty when no eligible outstanding same-currency candidates exist.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SettlementBasketPreviewResponse"
        "400":
          description: Request body is malformed, contains unsupported fields, unsupported direction or selection mode values, invalid UUIDs, or invalid currency.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Actor profile, counterparty profile, group, or required active group membership visibility is missing, deleted, inactive, removed, unrelated, or unavailable.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/settlement-balances:
    get:
      summary: List settlement balance projections
      description: Lists read-only current-actor settlement balance projection rows derived from durable settlement requests, selected request lines, settlement payments, payment allocations, and bounded receiver-confirmed residual effects. Only debtor/creditor balances involving the authenticated actor are returned; requester-only records, archived requests, cancelled requests, disputed requests, hidden groups, removed memberships, deleted profiles, unsafe allocation coverage, unsafe residual rows, pending residual effects, cancelled/disputed residual effects, bill merchant/item details, payment details, proof/file/storage internals, auth/session data, raw audit metadata, raw request bodies, and unrelated users are excluded.
      operationId: listSettlementBalanceProjections
      security:
        - SessionBearerAuth: []
      responses:
        "200":
          description: Settlement balance projections involving the authenticated actor as debtor or creditor. The list can be empty when no safe visible active balances involve the actor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SettlementBalanceProjectionListResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Actor profile is missing, deleted, inactive, or otherwise unavailable.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/settlements:
    get:
      summary: List settlement requests
      description: Lists read-only settlement requests involving the authenticated actor as debtor, creditor, or requester. Group-scoped requests additionally require active group visibility for the current actor and active group visibility for the request parties; group membership alone does not expose unrelated settlements. Responses include request-level fields and selected request-line summaries only, and exclude bill merchant/item details, payment details, proof/file internals, auth/session data, raw audit metadata, request bodies, and unrelated users.
      operationId: listSettlementRequests
      security:
        - SessionBearerAuth: []
      responses:
        "200":
          description: Settlement requests involving the authenticated actor. The list can be empty when no visible settlement requests involve the actor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SettlementRequestListResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Actor profile is missing, deleted, inactive, or otherwise unavailable.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/settlements/{settlementId}:
    get:
      summary: Get settlement request
      description: Returns one read-only settlement request with selected request-line summaries only when it involves the authenticated actor as debtor, creditor, or requester. Group-scoped requests additionally require active group visibility for the current actor and active group visibility for the request parties. Missing, archived, deleted, unrelated, inactive, removed, not-visible, or unavailable settlement requests return the same safe 404.
      operationId: getSettlementRequest
      security:
        - SessionBearerAuth: []
      parameters:
        - name: settlementId
          in: path
          required: true
          description: Settlement request ID to read.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Settlement request visible to the authenticated actor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SettlementRequestResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Settlement request is missing, archived, deleted, unrelated, inactive, removed, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/settlements/{settlementId}/counterparties/{userProfileId}/payment-details:
    get:
      summary: Get settlement counterparty payment details
      description: Returns a bounded counterparty-shaped payment-details response for the target profile only when the authenticated actor is the debtor or creditor counterparty on a visible, non-archived settlement request. Group-scoped settlements additionally require active group visibility for the current actor and required settlement parties. The response applies the target payment profile visibility server-side, returns not-configured fields for private, missing, deleted, or not-visible payment profiles, and never exposes auth account IDs, session IDs, tokens, raw audit metadata, storage object keys, storage paths, provider URLs, vault internals, QR bytes, request bodies, or unrelated users.
      operationId: getSettlementCounterpartyPaymentDetails
      security:
        - SessionBearerAuth: []
      parameters:
        - name: settlementId
          in: path
          required: true
          description: Settlement request ID anchoring the counterparty relationship.
          schema:
            type: string
            format: uuid
        - name: userProfileId
          in: path
          required: true
          description: Target counterparty user profile ID. The target must be the authenticated actor's debtor or creditor counterparty on the settlement request.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Counterparty payment-details response with visibility-scoped fields and safe QR metadata only.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SettlementCounterpartyPaymentDetailsResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Settlement request, actor profile, target profile, relationship, group visibility, or target auth account is missing, deleted, archived, unrelated, inactive, removed, not visible, or unavailable.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Counterparty payment-details access audit could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/settlements/{settlementId}/counterparties/{userProfileId}/payment-details/qr/content:
    get:
      summary: Get settlement counterparty payment QR content
      description: Streams the active linked payment QR bytes for the target counterparty only through the settlement/counterparty payment-details boundary. Authorization is evaluated against the settlement request and target profile every time, the target payment profile must be visible to this relationship, and the linked file object must be active, owned and created by the target profile, purpose payment_qr, and an allowed image type. This endpoint is not a generic file API and never exposes filesystem paths, storage object keys, provider URLs, bucket names, vault internals, filenames, thumbnails, or unrelated file metadata.
      operationId: getSettlementCounterpartyPaymentDetailsQrContent
      security:
        - SessionBearerAuth: []
      parameters:
        - name: settlementId
          in: path
          required: true
          description: Settlement request ID anchoring the counterparty relationship.
          schema:
            type: string
            format: uuid
        - name: userProfileId
          in: path
          required: true
          description: Target counterparty user profile ID whose active linked payment QR content is requested.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Payment QR image bytes for the authorized settlement counterparty relationship.
          content:
            image/png:
              schema:
                type: string
                format: binary
            image/jpeg:
              schema:
                type: string
                format: binary
            image/webp:
              schema:
                type: string
                format: binary
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Settlement request, actor profile, target profile, relationship, group visibility, payment profile, QR linkage, file lifecycle, file ownership, file purpose, file content type, or storage object is missing, deleted, archived, unrelated, inactive, removed, not visible, or unavailable.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Counterparty QR content read audit could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/settlements/{settlementId}/cancel:
    post:
      summary: Cancel settlement request
      description: Cancels one visible settlement request without accepting a request body. The authenticated actor must be the settlement requester, and the server derives actor identity, request visibility, cancellation timestamp, request status transition, and audit metadata. This operation can move only requested settlement requests with no payment rows to cancelled. It does not delete rows or expose payment handles, payment details, proof metadata, file/storage internals, bill merchant/item details, auth/session data, raw request bodies, or raw audit metadata.
      operationId: cancelSettlementRequest
      security:
        - SessionBearerAuth: []
      parameters:
        - name: settlementId
          in: path
          required: true
          description: Settlement request ID to cancel.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Settlement request moved to cancelled.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SettlementRequestResponse"
        "400":
          description: A request body was submitted even though request cancellation accepts no client-submitted identity, payment, file, proof, status, or audit fields.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Settlement request is missing, archived, deleted, unrelated, not requester-owned by the actor, inactive, removed, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Visible settlement request state, existing payment row, unsupported status transition, or corrupt settlement basis blocks cancellation.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Settlement cancellation write could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/settlements/{settlementId}/dispute:
    post:
      summary: Dispute settlement request
      description: Disputes one visible settlement request without accepting a request body. The authenticated actor must be the request debtor or creditor, and the server derives actor identity, request visibility, dispute timestamp, request status transition, and audit metadata. This operation can move ordinary pre-final request states requested, partially_paid, or marked_paid to disputed. It does not delete or rewrite existing payment claims, proof attachment rows, bill rows, payment details, file metadata, proof bytes, QR contents, storage internals, auth/session data, raw request bodies, or raw audit metadata.
      operationId: disputeSettlementRequest
      security:
        - SessionBearerAuth: []
      parameters:
        - name: settlementId
          in: path
          required: true
          description: Settlement request ID to dispute.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Settlement request moved to disputed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SettlementRequestResponse"
        "400":
          description: A request body was submitted even though request dispute accepts no client-submitted fields.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Settlement request is missing, archived, deleted, unrelated, not debtor-or-creditor-owned by the actor, inactive, removed, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Visible settlement request state, unsupported status transition, or corrupt settlement basis blocks dispute.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Settlement dispute write could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/settlements/{settlementId}/payments:
    get:
      summary: List settlement payments
      description: Lists read-only payment claims for one visible settlement request. The authenticated actor must be related to the parent settlement request as debtor, creditor, or requester; group-scoped settlements additionally require active group visibility for the current actor and active required settlement parties. Responses expose bounded payment state only and exclude payment handles, payment notes, payment QR data, proof/file/storage internals, bill merchant/item details, auth/session data, raw audit metadata, request bodies, and unrelated users.
      operationId: listSettlementPayments
      security:
        - SessionBearerAuth: []
      parameters:
        - name: settlementId
          in: path
          required: true
          description: Settlement request ID whose visible payment claims should be listed.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Settlement payment claims visible to the authenticated actor. The list can be empty when the settlement is visible and has no visible payment claims.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SettlementPaymentListResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Settlement request is missing, archived, deleted, unrelated, inactive, removed, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
    post:
      summary: Create settlement payment claim
      description: Creates one debtor-authored payment claim for an existing settlement request. The authenticated actor must be the request debtor, the server derives payer, receiver, created-by profile, payment status, request status transition, remaining amount, residual decision state, and partial-vs-covered state. The request body intentionally accepts only amount, currency, paymentDate, and an optional bounded proposedResidualPolicy; payer, receiver, debtor, creditor, status, settlement request ID, group ID, bill ID, proof file IDs, payment details, auth/session/account IDs, payment handles, notes, and arbitrary identity fields are not accepted. This endpoint supports same-currency exact and ordinary partial payments without residual rows, supports explicit same-currency underpayment/overpayment residual proposals, still rejects overpayment without a supported residual policy, and does not perform receiver residual confirmation, dispute, cancellation, proof upload/download, proof attachment linkage, payment-detail reads, QR reads, balance projection writes, or file byte behavior.
      operationId: createSettlementPaymentClaim
      security:
        - SessionBearerAuth: []
      parameters:
        - name: settlementId
          in: path
          required: true
          description: Settlement request ID to mark paid or partially paid.
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSettlementPaymentRequest"
      responses:
        "201":
          description: Settlement payment claim created and the request status moved to partially_paid or marked_paid.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SettlementPaymentResponse"
        "400":
          description: Request body is malformed, contains unsupported fields, or contains an invalid decimal amount, currency, or payment date.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Settlement request is missing, archived, deleted, unrelated, not debtor-owned by the actor, inactive, removed, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Visible settlement state, unsupported status transition, currency mismatch, existing payment coverage, or overpayment blocks the claim.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Settlement payment write could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/settlement-payments/{paymentId}:
    get:
      summary: Get settlement payment
      description: Returns one read-only settlement payment claim only when the parent settlement request is visible to the authenticated actor as debtor, creditor, or requester. Group-scoped payments additionally require active group visibility for the current actor and active required settlement parties. Missing, archived, deleted, unrelated, inactive, removed, corrupt, not-visible, or unavailable settlement payments return the same safe 404. The response excludes payment handles, payment notes, QR data, proof/file/storage internals, bill merchant/item details, auth/session data, raw audit metadata, request bodies, and unrelated users.
      operationId: getSettlementPayment
      security:
        - SessionBearerAuth: []
      parameters:
        - name: paymentId
          in: path
          required: true
          description: Settlement payment claim ID to read.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Settlement payment claim visible to the authenticated actor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SettlementPaymentResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Settlement payment is missing, archived through its parent request, deleted, unrelated, inactive, removed, not visible, corrupt, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/settlement-payments/{paymentId}/proof:
    post:
      summary: Attach settlement payment proof
      description: Uploads one purpose-specific proof file for an existing visible settlement payment claim. The authenticated actor is derived from the current session and must be the debtor/payer/created-by profile for the marked_paid payment. The parent settlement request must be partially_paid or marked_paid. The API validates a narrow proof content policy, writes bytes through the internal storage lifecycle foundation, creates a settlement_proof file object and active proof association, emits bounded audit, and returns safe proof metadata only. This is not a generic file API and never accepts or returns storage object keys, provider paths, provider URLs, original filenames, auth/session fields, payment handles, payment notes, request bodies, OCR text, thumbnails, or unrelated financial data.
      operationId: attachSettlementPaymentProof
      security:
        - SessionBearerAuth: []
      parameters:
        - name: paymentId
          in: path
          required: true
          description: Settlement payment claim ID that should receive a proof attachment.
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/AttachSettlementPaymentProofRequest"
      responses:
        "201":
          description: Settlement proof attachment created for the visible payment claim.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SettlementPaymentProofResponse"
        "400":
          description: The submitted multipart proof upload is malformed, unsupported, empty, too large, or has mismatched declared content type and bytes.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Settlement payment is missing, archived through its parent request, unrelated, not debtor/creator-owned by the actor, inactive, removed, corrupt, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Visible settlement payment or parent request state is not proof-mutable.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Settlement proof upload could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
    get:
      summary: List settlement payment proof metadata
      description: Lists safe active proof attachment metadata for one visible settlement payment claim. Debtor and creditor can read proof metadata when the payment is visible through the existing settlement runtime policy; group-scoped payments additionally require active group visibility for the current actor and active required settlement parties. Responses expose stable file IDs, payment IDs, content type, size, and timestamps only, and exclude proof bytes, storage object keys, provider paths, provider URLs, original filenames, auth/session fields, payment details, bill merchant/item details, OCR text, thumbnails, audit internals, and unrelated users.
      operationId: listSettlementPaymentProofs
      security:
        - SessionBearerAuth: []
      parameters:
        - name: paymentId
          in: path
          required: true
          description: Settlement payment claim ID whose active proof metadata should be listed.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Safe proof attachment metadata visible to the authenticated actor. The list is empty when the payment is visible and has no active readable proof attachments.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SettlementPaymentProofListResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Settlement payment is missing, archived through its parent request, unrelated, inactive, removed, corrupt, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/settlement-payments/{paymentId}/proof/{fileId}:
    delete:
      summary: Remove settlement payment proof
      description: Soft-removes one actor-owned proof attachment from a visible proof-mutable settlement payment claim. The authenticated actor is derived from the current session and must be the debtor/payer/created-by profile that owns and created the linked settlement_proof file. The API sets the attachment removal timestamp, marks the file object deleted through the lifecycle service, emits bounded audit, and returns no proof bytes or storage internals. This endpoint is purpose-specific and does not expose or mutate generic files.
      operationId: removeSettlementPaymentProof
      security:
        - SessionBearerAuth: []
      parameters:
        - name: paymentId
          in: path
          required: true
          description: Settlement payment claim ID whose proof attachment should be removed.
          schema:
            type: string
            format: uuid
        - name: fileId
          in: path
          required: true
          description: Stable proof file object ID to detach from the settlement payment.
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: The proof attachment is no longer active for the settlement payment.
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Settlement payment or proof file is missing, removed, deleted, wrong-purpose, wrong-owner, unrelated, not debtor/creator-owned by the actor, inactive, corrupt, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Visible settlement payment or parent request state is not proof-mutable.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Settlement proof removal could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/settlement-payments/{paymentId}/proof/{fileId}/content:
    get:
      summary: Read settlement payment proof content
      description: Streams one active settlement proof attachment through API authorization every time. The authenticated actor must be able to read the settlement payment through the existing settlement runtime policy; group-scoped payments additionally require active group visibility for the actor and required parties. The linked file must be active, owned and created by the debtor/payer, purpose settlement_proof, and one of the allowed proof content types. The response uses conservative content headers and never exposes storage object keys, provider paths, provider URLs, original filenames, auth/session fields, payment details, OCR text, thumbnails, or unrelated data.
      operationId: getSettlementPaymentProofContent
      security:
        - SessionBearerAuth: []
      parameters:
        - name: paymentId
          in: path
          required: true
          description: Settlement payment claim ID whose proof content should be read.
          schema:
            type: string
            format: uuid
        - name: fileId
          in: path
          required: true
          description: Stable proof file object ID to stream.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Settlement proof bytes authorized through the settlement payment proof boundary.
          headers:
            X-Content-Type-Options:
              schema:
                type: string
              description: Set to nosniff.
            Content-Disposition:
              schema:
                type: string
              description: Conservative attachment disposition with no submitted filename.
          content:
            image/png:
              schema:
                type: string
                format: binary
            image/jpeg:
              schema:
                type: string
                format: binary
            image/webp:
              schema:
                type: string
                format: binary
            application/pdf:
              schema:
                type: string
                format: binary
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Settlement payment or proof file is missing, removed, deleted, wrong-purpose, wrong-owner, inactive, corrupt, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Settlement proof read audit could not be completed after content authorization.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/settlement-payments/{paymentId}/cancel:
    post:
      summary: Cancel settlement payment
      description: Cancels one visible debtor-authored marked_paid settlement payment claim without accepting a request body. The authenticated actor must be the debtor who paid and created the claim, and the server derives actor identity, payment visibility, cancellation timestamp, payment status transition, pending residual cancellation, parent request status recomputation, and audit metadata. This operation accepts no client-submitted actor, debtor, creditor, requester, request, group, amount, currency, status, auth, session, file, proof, payment-detail, QR, or audit fields; it does not delete rows, rewrite proof attachments, expose payment handles, expose storage internals, or perform proof bytes, residual confirmation, balance projection writes, OCR, recurring, forecasting, reconciliation, FX, refund, governance, notification, or UI behavior.
      operationId: cancelSettlementPayment
      security:
        - SessionBearerAuth: []
      parameters:
        - name: paymentId
          in: path
          required: true
          description: Settlement payment claim ID to cancel.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Settlement payment moved to cancelled and the parent settlement request status recomputed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SettlementPaymentResponse"
        "400":
          description: A request body was submitted even though payment cancellation accepts no client-submitted fields.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Settlement payment is missing, archived through its parent request, unrelated, not debtor/creator-owned by the actor, inactive, removed, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Visible settlement payment state, parent request state, unsupported transition, currency mismatch, wrong party relationship, corrupt amount, or corrupt coverage data blocks cancellation.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Settlement payment cancellation write could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/settlement-payments/{paymentId}/confirm:
    post:
      summary: Confirm settlement payment
      description: Confirms one existing debtor-authored settlement payment claim. The authenticated actor must be the receiver/creditor for the payment's settlement request, and the server derives actor identity, settlement request visibility, payment transition, confirmation timestamp, request status recomputation, and audit metadata. The operation accepts no request body and does not accept client-submitted actor, debtor, creditor, request, group, amount, currency, status, auth, session, file, proof, payment details, or QR identity fields. It confirms only marked_paid payments without pending residuals, rejects corrupt coverage or currency state, blocks pending residual proposals until the receiver confirms or neutralizes the residual through a supported settlement residual flow, and does not perform dispute, cancellation, proof upload/download, proof attachment linkage, payment-detail reads, QR reads, balance projection writes, or file byte behavior.
      operationId: confirmSettlementPayment
      security:
        - SessionBearerAuth: []
      parameters:
        - name: paymentId
          in: path
          required: true
          description: Settlement payment claim ID to confirm.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Settlement payment confirmed and the parent settlement request status recomputed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SettlementPaymentResponse"
        "400":
          description: A request body was submitted even though confirmation accepts no client-submitted fields.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Settlement payment is missing, archived through its parent request, unrelated, not receiver-owned by the actor, inactive, removed, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Visible settlement payment state, parent request state, unsupported transition, currency mismatch, corrupt coverage data, or over-coverage blocks confirmation.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Settlement payment write could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/settlement-payments/{paymentId}/residuals/{residualId}/confirm:
    post:
      summary: Confirm settlement payment residual
      description: Confirms one pending receiver-confirmation residual proposal on one visible debtor-authored settlement payment claim. The authenticated actor must be the receiver/creditor for the payment's settlement request, and the server derives actor identity, settlement request visibility, residual status mapping, resolved timestamp, allocation safety, and audit metadata. The operation accepts no request body and does not accept client-submitted actor, debtor, creditor, request, group, payment, residual, amount, currency, status, policy, auth, session, file, proof, payment-detail, QR, or audit fields. It updates only the matching pending residual row to its policy-derived receiver-confirmed status, keeps overpayment credit/waiver behavior bounded to the payment residual summary, does not create a broad credit ledger, does not silently clear remaining-balance or carried-forward underpayment debt, does not reopen or simplify settlements, and does not perform payment confirmation, dispute, cancellation, proof upload/download, proof attachment linkage, payment-detail reads, QR reads, balance projection writes, or file byte behavior.
      operationId: confirmSettlementPaymentResidual
      security:
        - SessionBearerAuth: []
      parameters:
        - name: paymentId
          in: path
          required: true
          description: Settlement payment claim ID whose pending residual is being confirmed.
          schema:
            type: string
            format: uuid
        - name: residualId
          in: path
          required: true
          description: Settlement residual ID to confirm.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Settlement residual moved from pending receiver confirmation to its policy-derived receiver-confirmed status.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SettlementPaymentResponse"
        "400":
          description: A request body was submitted even though residual confirmation accepts no client-submitted fields.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Settlement payment or residual is missing, archived through its parent request, unrelated, not receiver-owned by the actor, inactive, removed, not visible, not linked to a visible actor relationship, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Visible settlement payment state, parent request state, residual state, unsupported policy/status mapping, wrong party relationship, currency mismatch, corrupt amount, corrupt linkage, corrupt coverage data, or unsupported reopen/simplification behavior blocks residual confirmation.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Settlement residual confirmation write could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/settlement-payments/{paymentId}/dispute:
    post:
      summary: Dispute settlement payment
      description: Disputes one visible debtor-authored settlement payment claim without accepting a request body. The authenticated actor must be the receiver/creditor for the payment's settlement request, and the server derives actor identity, payment visibility, payment dispute timestamp, pending residual dispute state, parent request dispute transition, and audit metadata. This operation can move only marked_paid payments to disputed and moves the parent request to disputed. It does not delete or rewrite other payment claims, proof attachment rows, bill rows, payment details, file metadata, proof bytes, QR contents, storage internals, auth/session data, raw request bodies, residual confirmation state, or raw audit metadata.
      operationId: disputeSettlementPayment
      security:
        - SessionBearerAuth: []
      parameters:
        - name: paymentId
          in: path
          required: true
          description: Settlement payment claim ID to dispute.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Settlement payment and parent request moved to disputed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SettlementPaymentResponse"
        "400":
          description: A request body was submitted even though payment dispute accepts no client-submitted fields.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Settlement payment is missing, archived through its parent request, unrelated, not receiver-owned by the actor, inactive, removed, not visible, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Visible settlement payment state, parent request state, unsupported transition, currency mismatch, or corrupt settlement/payment basis blocks dispute.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Settlement payment dispute write could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/groups/{groupId}/bills/{billId}/submit:
    post:
      summary: Submit group bill
      description: Submits a draft group bill created by the authenticated actor. The actor must still have active access to the route group. Submission moves the bill to pending confirmation, resets draft participant acknowledgement state, and writes bounded safe audit metadata. Rejected bill correction/resubmission must use the bill revision proposal workflow and is rejected here to avoid silently resetting all accepted participants.
      operationId: submitGroupBill
      security:
        - SessionBearerAuth: []
      parameters:
        - name: groupId
          in: path
          required: true
          description: Group ID that owns the bill.
          schema:
            type: string
            format: uuid
        - name: billId
          in: path
          required: true
          description: Group bill ID to submit.
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: Group bill submitted.
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Group or bill is missing, deleted, archived, personal, unrelated, in another group, not created by the actor, inactive, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Bill state does not allow submission.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Group bill workflow write could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/groups/{groupId}/bills/{billId}/participants/{userProfileId}/accept:
    post:
      summary: Accept group bill participant share
      description: Accepts the authenticated actor's own participant row on a visible group bill in pending confirmation. The actor must have active access to the route group, and the route userProfileId must equal the current actor profile ID; final participant acceptance confirms the bill and writes bounded safe audit metadata.
      operationId: acceptGroupBillParticipant
      security:
        - SessionBearerAuth: []
      parameters:
        - name: groupId
          in: path
          required: true
          description: Group ID that owns the bill.
          schema:
            type: string
            format: uuid
        - name: billId
          in: path
          required: true
          description: Group bill ID.
          schema:
            type: string
            format: uuid
        - name: userProfileId
          in: path
          required: true
          description: Participant user profile ID. Must be the authenticated actor's own profile ID.
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: Participant acceptance recorded.
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Group, bill, or participant is missing, deleted, archived, personal, unrelated, in another group, route-mismatched, inactive, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Bill or participant state does not allow acceptance.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Group bill workflow write could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/groups/{groupId}/bills/{billId}/participants/{userProfileId}/reject:
    post:
      summary: Reject group bill participant share
      description: Rejects the authenticated actor's own participant row on a visible group bill in pending confirmation using a bounded reason code. The actor must have active access to the route group, and the route userProfileId must equal the current actor profile ID; rejection moves the bill to rejected and writes bounded safe audit metadata.
      operationId: rejectGroupBillParticipant
      security:
        - SessionBearerAuth: []
      parameters:
        - name: groupId
          in: path
          required: true
          description: Group ID that owns the bill.
          schema:
            type: string
            format: uuid
        - name: billId
          in: path
          required: true
          description: Group bill ID.
          schema:
            type: string
            format: uuid
        - name: userProfileId
          in: path
          required: true
          description: Participant user profile ID. Must be the authenticated actor's own profile ID.
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RejectBillParticipantRequest"
      responses:
        "204":
          description: Participant rejection recorded.
        "400":
          description: The submitted rejection request is invalid.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Group, bill, or participant is missing, deleted, archived, personal, unrelated, in another group, route-mismatched, inactive, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Bill or participant state does not allow rejection.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Group bill workflow write could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/recurring-bills:
    post:
      summary: Create recurring bill template
      description: Creates an API-owned recurring bill template for the authenticated actor. The server derives owner and creator identity from the current session, validates schedule and bounded template payload, calculates forecast amount server-side, and does not create any expense bill during template creation.
      operationId: createRecurringBillTemplate
      security:
        - SessionBearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateRecurringBillTemplateRequest"
      responses:
        "201":
          description: Recurring bill template created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecurringBillTemplateResponse"
        "400":
          description: The submitted recurring bill template request is invalid.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Current profile, group, or submitted participant context is unavailable or not visible to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Recurring bill template creation could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
    get:
      summary: List recurring bill templates
      description: Lists recurring bill templates visible to the authenticated actor. Personal templates are owner-only; group templates require active group membership. This read does not create bills or forecast occurrence rows.
      operationId: listRecurringBillTemplates
      security:
        - SessionBearerAuth: []
      parameters:
        - name: status
          in: query
          required: false
          description: Optional template status filter.
          schema:
            $ref: "#/components/schemas/RecurringBillTemplateStatus"
        - name: groupId
          in: query
          required: false
          description: Optional group filter.
          schema:
            type: string
            format: uuid
        - name: fromDate
          in: query
          required: false
          description: Optional inclusive schedule overlap start date.
          schema:
            type: string
            format: date
        - name: toDate
          in: query
          required: false
          description: Optional inclusive schedule overlap end date.
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Recurring bill templates visible to the authenticated actor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecurringBillTemplateListResponse"
        "400":
          description: The submitted recurring bill list filter is invalid.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Current profile is unavailable or not allowed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/recurring-bills/forecast:
    get:
      summary: List recurring bill forecast occurrences
      description: Lists bounded upcoming forecast occurrences from active visible recurring templates. Forecast rows are estimates only and this read does not create expense bills or occurrence rows.
      operationId: listRecurringBillForecast
      security:
        - SessionBearerAuth: []
      parameters:
        - name: fromDate
          in: query
          required: false
          description: Inclusive forecast range start date. Defaults to the server's current UTC date.
          schema:
            type: string
            format: date
        - name: toDate
          in: query
          required: false
          description: Inclusive forecast range end date. Defaults to 60 days after fromDate.
          schema:
            type: string
            format: date
        - name: limit
          in: query
          required: false
          description: Maximum forecast rows to return.
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
        - name: groupId
          in: query
          required: false
          description: Optional group filter.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Bounded recurring bill forecast occurrences visible to the authenticated actor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecurringBillForecastListResponse"
        "400":
          description: The submitted forecast filter is invalid.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Current profile is unavailable or not allowed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/recurring-bills/{templateId}:
    get:
      summary: Get recurring bill template
      description: Returns one recurring bill template visible to the authenticated actor. Missing, personal cross-user, group non-member, removed-member, deleted-profile, or deleted-group cases fail closed with the same safe 404.
      operationId: getRecurringBillTemplate
      security:
        - SessionBearerAuth: []
      parameters:
        - name: templateId
          in: path
          required: true
          description: Recurring bill template ID.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Recurring bill template visible to the authenticated actor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecurringBillTemplateResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Recurring template is missing, deleted, unrelated, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
    patch:
      summary: Update recurring bill template
      description: Updates safe recurring template fields without mutating historical bills or generated drafts. The server revalidates schedule, payload, money, membership, and forecast amount before persisting the update.
      operationId: updateRecurringBillTemplate
      security:
        - SessionBearerAuth: []
      parameters:
        - name: templateId
          in: path
          required: true
          description: Recurring bill template ID.
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateRecurringBillTemplateRequest"
      responses:
        "200":
          description: Recurring bill template updated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecurringBillTemplateResponse"
        "400":
          description: The submitted recurring template update is invalid.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Recurring template is missing, deleted, unrelated, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Recurring template state does not allow update.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Recurring template update could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/recurring-bills/{templateId}/pause:
    post:
      summary: Pause recurring bill template
      description: Pauses a visible non-archived recurring bill template. Paused templates remain readable but cannot generate new draft bills.
      operationId: pauseRecurringBillTemplate
      security:
        - SessionBearerAuth: []
      parameters:
        - name: templateId
          in: path
          required: true
          description: Recurring bill template ID.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Recurring bill template paused.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecurringBillTemplateResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Recurring template is missing, deleted, unrelated, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Recurring template state does not allow pause.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Recurring template pause could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/recurring-bills/{templateId}/resume:
    post:
      summary: Resume recurring bill template
      description: Resumes a visible non-archived recurring bill template and recomputes its next occurrence date from the server's current UTC date.
      operationId: resumeRecurringBillTemplate
      security:
        - SessionBearerAuth: []
      parameters:
        - name: templateId
          in: path
          required: true
          description: Recurring bill template ID.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Recurring bill template resumed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecurringBillTemplateResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Recurring template is missing, deleted, unrelated, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Recurring template state does not allow resume.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Recurring template resume could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/recurring-bills/{templateId}/archive:
    post:
      summary: Archive recurring bill template
      description: Archives a visible recurring bill template. Archived templates remain readable but cannot generate new draft bills.
      operationId: archiveRecurringBillTemplate
      security:
        - SessionBearerAuth: []
      parameters:
        - name: templateId
          in: path
          required: true
          description: Recurring bill template ID.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Recurring bill template archived.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecurringBillTemplateResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Recurring template is missing, deleted, unrelated, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Recurring template archive could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/recurring-bills/{templateId}/occurrences/{occurrenceDate}/generate-draft:
    post:
      summary: Generate recurring bill draft
      description: Generates one draft expense bill for a visible active recurring template occurrence after explicit user action. The server revalidates current actor access, group membership, schedule membership, template payload, money, currency, participants, and payer policy at write time. Repeating the same template and occurrence date returns the existing generated draft instead of creating duplicate money records.
      operationId: generateRecurringBillDraft
      security:
        - SessionBearerAuth: []
      parameters:
        - name: templateId
          in: path
          required: true
          description: Recurring bill template ID.
          schema:
            type: string
            format: uuid
        - name: occurrenceDate
          in: path
          required: true
          description: Occurrence date to generate as a draft bill.
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Draft had already been generated for this template occurrence and is returned idempotently.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecurringBillGenerateDraftResponse"
        "201":
          description: Draft bill generated for this template occurrence.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecurringBillGenerateDraftResponse"
        "400":
          description: The occurrence date is invalid or not generated by the template schedule.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Recurring template is missing, deleted, unrelated, group-inactive, or otherwise unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Template or occurrence state does not allow draft generation.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Draft generation could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/groups:
    post:
      summary: Create group
      description: Creates a group for the authenticated actor and creates an active owner membership for that actor. The creator profile is derived server-side; clients cannot submit owner or creator IDs.
      operationId: createGroup
      security:
        - SessionBearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateGroupRequest"
      responses:
        "201":
          description: Group created with an active owner membership for the authenticated actor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GroupResponse"
        "400":
          description: The submitted group creation request is invalid.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: The current profile is unavailable or not allowed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Group creation could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
    get:
      summary: List current user's groups
      description: Lists non-deleted groups where the authenticated actor has active membership. This is a foundation endpoint without pagination; future group list slices are expected to add pagination or filtering when needed.
      operationId: listGroups
      security:
        - SessionBearerAuth: []
      responses:
        "200":
          description: Groups where the authenticated actor has active membership.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GroupListResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: The current profile is unavailable or not allowed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/groups/{groupId}:
    get:
      summary: Get group
      description: Returns one non-deleted group only when the authenticated actor has active membership.
      operationId: getGroup
      security:
        - SessionBearerAuth: []
      parameters:
        - name: groupId
          in: path
          required: true
          description: Group ID to read.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Group visible to the authenticated actor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GroupResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Group is missing, deleted, unrelated, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
    patch:
      summary: Update group
      description: Updates the group name only when the authenticated actor is an active owner of the group.
      operationId: updateGroup
      security:
        - SessionBearerAuth: []
      parameters:
        - name: groupId
          in: path
          required: true
          description: Group ID to update.
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateGroupRequest"
      responses:
        "200":
          description: Updated group visible to the authenticated actor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GroupResponse"
        "400":
          description: The submitted group update request is invalid.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "403":
          description: The authenticated actor has active group membership but is not allowed to update group settings.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Group is missing, deleted, unrelated, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Group update could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/groups/{groupId}/members:
    get:
      summary: List group members
      description: Lists active registered members for a non-deleted group only when the authenticated actor has active group membership. Removed memberships, auth account IDs, identifiers, credentials, sessions, token material, provider payloads, audit metadata, storage paths, and unrelated users are excluded.
      operationId: listGroupMembers
      security:
        - SessionBearerAuth: []
      parameters:
        - name: groupId
          in: path
          required: true
          description: Group ID whose active members should be listed.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Active group members visible to the authenticated actor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GroupMemberListResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Group is missing, deleted, unrelated, or unavailable to the authenticated actor.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
    post:
      summary: Add group member
      description: Adds an existing active registered user profile to a group only when the authenticated actor is an active group owner. This foundation does not create invitations, guest placeholders, auth accounts, or billing participation defaults.
      operationId: addGroupMember
      security:
        - SessionBearerAuth: []
      parameters:
        - name: groupId
          in: path
          required: true
          description: Group ID whose membership should be changed.
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddGroupMemberRequest"
      responses:
        "201":
          description: Existing registered user was added as an active group member.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GroupMemberResponse"
        "400":
          description: The submitted group member request is invalid.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "403":
          description: The authenticated actor has active group membership but is not allowed to manage group members.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Group or target registered user is missing, deleted, unrelated, or unavailable.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: Active or removed membership already exists for the target user.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/groups/{groupId}/members/{userProfileId}:
    patch:
      summary: Update group member role
      description: Updates an active group member's group role only when the authenticated actor is an active group owner. The API rejects last-owner demotion with 409 Conflict.
      operationId: updateGroupMember
      security:
        - SessionBearerAuth: []
      parameters:
        - name: groupId
          in: path
          required: true
          description: Group ID whose membership should be changed.
          schema:
            type: string
            format: uuid
        - name: userProfileId
          in: path
          required: true
          description: User profile ID for the active group membership to update.
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateGroupMemberRequest"
      responses:
        "200":
          description: Updated active group member.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GroupMemberResponse"
        "400":
          description: The submitted group member request is invalid.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "403":
          description: The authenticated actor has active group membership but is not allowed to manage group members.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Group or target active membership is missing, deleted, unrelated, or unavailable.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: The requested role change would remove the last active owner.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Group member update could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
    delete:
      summary: Remove group member
      description: Marks an active group member as removed only when the authenticated actor is an active group owner. Membership rows are not hard-deleted. The API rejects last-owner removal with 409 Conflict.
      operationId: removeGroupMember
      security:
        - SessionBearerAuth: []
      parameters:
        - name: groupId
          in: path
          required: true
          description: Group ID whose membership should be changed.
          schema:
            type: string
            format: uuid
        - name: userProfileId
          in: path
          required: true
          description: User profile ID for the active group membership to remove.
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: Active group member was marked removed.
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "403":
          description: The authenticated actor has active group membership but is not allowed to manage group members.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: Group or target active membership is missing, deleted, unrelated, or unavailable.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "409":
          description: The requested removal would remove the last active owner.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Group member removal could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/users/me/payment-details:
    get:
      summary: Self payment details
      description: Returns the authenticated actor's own payment details through the server-side business authorization boundary. Counterparty, admin, storage path, provider, object key, filename, and vault fields are not exposed.
      operationId: getSelfPaymentDetails
      security:
        - SessionBearerAuth: []
      responses:
        "200":
          description: Authenticated user's payment details, or a stable unconfigured object when no payment profile exists.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SelfPaymentDetailsResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: The current profile is unavailable or not allowed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
    patch:
      summary: Update self payment details
      description: Creates or updates the authenticated actor's own default payment profile through the server-side business authorization boundary. Profile identity is derived from the current session; QR image bytes are managed only through the dedicated self QR endpoints.
      operationId: updateSelfPaymentDetails
      security:
        - SessionBearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateSelfPaymentDetailsRequest"
      responses:
        "200":
          description: Configured authenticated user's payment details after create or update.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SelfPaymentDetailsResponse"
        "400":
          description: The submitted payment details update is invalid.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: The current profile is unavailable or not allowed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Payment details update could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/users/me/payment-details/qr:
    post:
      summary: Attach or replace self payment QR
      description: Uploads one QR image for the authenticated actor's own active payment profile. The API derives the actor from the current session, creates the payment profile if needed, validates a narrow image policy, writes bytes through the internal storage lifecycle foundation, and returns only safe payment-details metadata.
      operationId: attachSelfPaymentQr
      security:
        - SessionBearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/AttachSelfPaymentQrRequest"
      responses:
        "200":
          description: Configured authenticated user's payment details after QR attach or replacement.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SelfPaymentDetailsResponse"
        "400":
          description: The submitted QR upload is invalid.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: The current profile is unavailable or not allowed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Payment QR upload could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
    delete:
      summary: Remove self payment QR
      description: Clears the authenticated actor's own payment QR link and marks the previously linked file deleted through the internal lifecycle foundation. Missing QR state is idempotent and does not expose unrelated state.
      operationId: removeSelfPaymentQr
      security:
        - SessionBearerAuth: []
      responses:
        "204":
          description: The self payment QR link is absent after the request.
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: The current profile is unavailable or not allowed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "500":
          description: Payment QR removal could not be completed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/users/me/payment-details/qr/content:
    get:
      summary: Read self payment QR content
      description: Returns bytes for the authenticated actor's own active linked payment QR only. The endpoint validates the current actor, payment-details authorization, file purpose, owner, creator, lifecycle state, and content type before opening storage.
      operationId: getSelfPaymentQrContent
      security:
        - SessionBearerAuth: []
      responses:
        "200":
          description: Self payment QR image bytes.
          headers:
            X-Content-Type-Options:
              schema:
                type: string
              description: Set to nosniff.
            Content-Disposition:
              schema:
                type: string
              description: Conservative attachment disposition with no submitted filename.
          content:
            image/png:
              schema:
                type: string
                format: binary
            image/jpeg:
              schema:
                type: string
                format: binary
            image/webp:
              schema:
                type: string
                format: binary
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: The current profile or payment QR is unavailable or not allowed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
  /api/v1/users/me/profile:
    get:
      summary: Self user profile
      description: Returns the authenticated actor's own active profile through the server-side business authorization boundary.
      operationId: getSelfUserProfile
      security:
        - SessionBearerAuth: []
      responses:
        "200":
          description: Authenticated user's profile.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SelfUserProfileResponse"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: The current profile is unavailable or not allowed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
    patch:
      summary: Update self user profile
      description: Updates safe fields on the authenticated actor's own active profile through the server-side business authorization boundary.
      operationId: updateSelfUserProfile
      security:
        - SessionBearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateSelfUserProfileRequest"
      responses:
        "200":
          description: Updated authenticated user's profile.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SelfUserProfileResponse"
        "400":
          description: The submitted profile update is invalid.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "401":
          description: Authentication is required to access this resource.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        "404":
          description: The current profile is unavailable or not allowed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
        default:
          description: Problem response.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetails"
components:
  securitySchemes:
    SessionBearerAuth:
      type: http
      scheme: bearer
      bearerFormat: opaque-session-token
  schemas:
    BootstrapStatusResponse:
      type: object
      description: Bounded setup status for first-owner local bootstrap.
      required:
        - bootstrapRequired
      properties:
        bootstrapRequired:
          type: boolean
          description: True only when no auth account exists.
    BootstrapLocalOwnerRequest:
      type: object
      description: First-owner local bootstrap request for a fresh deployment. This is not general public registration.
      additionalProperties: false
      required:
        - identifier
        - password
        - displayName
      properties:
        identifier:
          type: string
          minLength: 1
          maxLength: 320
          description: Submitted local account identifier, normalized server-side consistently with local sign-in.
        password:
          type: string
          format: password
          minLength: 12
          maxLength: 4096
          description: Submitted local account password. The API never stores this plaintext value.
        displayName:
          type: string
          minLength: 1
          maxLength: 160
          description: Initial user profile display name after server-side trimming.
        defaultCurrency:
          oneOf:
            - $ref: "#/components/schemas/CurrencyCode"
            - type: "null"
          description: Optional initial default currency. Explicit null is accepted.
    BootstrapLocalOwnerResponse:
      type: object
      description: Safe first-owner bootstrap response. It excludes session tokens, password material, credential metadata, provider payloads, audit metadata, storage paths, and unrelated records.
      required:
        - userProfile
        - roles
      properties:
        userProfile:
          $ref: "#/components/schemas/SelfUserProfileResponse"
        roles:
          type: array
          items:
            type: string
            enum:
              - owner
              - admin
              - user
    LocalSignInRequest:
      type: object
      description: Local-account sign-in request. Source, network, and policy bucket details are derived server-side.
      additionalProperties: false
      required:
        - identifier
        - password
      properties:
        identifier:
          type: string
          minLength: 1
          maxLength: 320
          description: Submitted local account identifier.
        password:
          type: string
          format: password
          minLength: 1
          maxLength: 4096
          description: Submitted local account password. The API never stores this plaintext value.
        deviceLabel:
          type: string
          maxLength: 120
          description: Optional display label for the newly created session.
    LocalSignInResponse:
      type: object
      description: Minimal refresh-capable local sign-in success response. Raw credential material is returned only once.
      required:
        - session
        - refreshCredential
      properties:
        session:
          $ref: "#/components/schemas/RefreshSessionAccessSession"
        refreshCredential:
          $ref: "#/components/schemas/RefreshSessionCredential"
    RefreshSessionRequest:
      type: object
      description: Public refresh request. Credential lookup, account, session-family, status, expiry, and revocation policy are resolved server-side.
      additionalProperties: false
      required:
        - refreshCredential
      properties:
        refreshCredential:
          type: string
          minLength: 1
          description: Submitted raw refresh-like credential. The API never stores this raw value.
        deviceLabel:
          type: string
          maxLength: 120
          description: Optional display label for the replacement access session.
    RefreshSessionResponse:
      type: object
      description: Minimal refresh success response. Raw credential material is returned only once.
      required:
        - session
        - refreshCredential
      properties:
        session:
          $ref: "#/components/schemas/RefreshSessionAccessSession"
        refreshCredential:
          $ref: "#/components/schemas/RefreshSessionCredential"
    RefreshSessionAccessSession:
      type: object
      description: Replacement opaque access session returned from refresh.
      required:
        - id
        - token
        - expiresAtUtc
      properties:
        id:
          type: string
          format: uuid
        token:
          type: string
          minLength: 1
          description: Opaque raw access-session token returned once on refresh success.
        expiresAtUtc:
          type: string
          format: date-time
    RefreshSessionCredential:
      type: object
      description: Replacement refresh-like credential returned from refresh.
      required:
        - token
        - idleExpiresAtUtc
        - absoluteExpiresAtUtc
      properties:
        token:
          type: string
          minLength: 1
          description: Raw refresh-like credential returned once on refresh success.
        idleExpiresAtUtc:
          type: string
          format: date-time
        absoluteExpiresAtUtc:
          type: string
          format: date-time
    CurrentUserResponse:
      type: object
      description: Minimal current authenticated actor, profile, session, and system-role summary.
      required:
        - authAccountId
        - userProfile
        - session
        - roles
      properties:
        authAccountId:
          type: string
          format: uuid
        userProfile:
          $ref: "#/components/schemas/CurrentUserProfile"
        session:
          $ref: "#/components/schemas/CurrentUserSession"
        roles:
          type: array
          items:
            type: string
            enum:
              - owner
              - admin
              - user
          uniqueItems: true
    CurrentUserProfile:
      type: object
      description: Linked active user profile summary for the authenticated account.
      required:
        - id
        - displayName
        - defaultCurrency
      properties:
        id:
          type: string
          format: uuid
        displayName:
          type: string
        defaultCurrency:
          oneOf:
            - $ref: "#/components/schemas/CurrencyCode"
            - type: "null"
    CurrentUserSession:
      type: object
      description: Validated current session summary.
      required:
        - id
        - expiresAtUtc
      properties:
        id:
          type: string
          format: uuid
        expiresAtUtc:
          type: string
          format: date-time
    CreateLocalUserRequest:
      type: object
      description: Authenticated owner/admin local-user creation request. The server derives the actor and assigns only the default system user role.
      additionalProperties: false
      required:
        - identifier
        - password
        - displayName
      properties:
        identifier:
          type: string
          minLength: 1
          maxLength: 320
          description: Submitted local account identifier, normalized server-side consistently with local sign-in and first-owner bootstrap.
        password:
          type: string
          format: password
          minLength: 12
          maxLength: 4096
          description: Submitted local account password. The API never stores this plaintext value.
        displayName:
          type: string
          minLength: 1
          maxLength: 160
          description: Initial user profile display name after server-side trimming.
        defaultCurrency:
          oneOf:
            - $ref: "#/components/schemas/CurrencyCode"
            - type: "null"
          description: Optional initial default currency. Explicit null is accepted.
    AdminUserListResponse:
      type: object
      description: Safe admin user list response for owner/admin user-management foundations.
      required:
        - users
      properties:
        users:
          type: array
          items:
            $ref: "#/components/schemas/AdminUserSummaryResponse"
    AdminUserSummaryResponse:
      type: object
      description: Safe admin user summary. It excludes identifiers, password material, credential metadata, session/token material, refresh credential material, provider payloads, audit metadata, storage paths, and unrelated records.
      required:
        - userProfileId
        - authAccountId
        - displayName
        - defaultCurrency
        - accountStatus
        - roles
        - createdAtUtc
        - updatedAtUtc
      properties:
        userProfileId:
          type: string
          format: uuid
        authAccountId:
          type: string
          format: uuid
        displayName:
          type: string
          minLength: 1
          maxLength: 160
        defaultCurrency:
          oneOf:
            - $ref: "#/components/schemas/CurrencyCode"
            - type: "null"
        accountStatus:
          type: string
          enum:
            - active
            - disabled
        roles:
          type: array
          items:
            type: string
            enum:
              - owner
              - admin
              - user
          uniqueItems: true
        createdAtUtc:
          type: string
          format: date-time
        updatedAtUtc:
          type: string
          format: date-time
    CreateGroupRequest:
      type: object
      description: Group creation request. Creator and owner membership are derived from the authenticated actor.
      additionalProperties: false
      required:
        - name
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 160
          description: Group name after server-side trimming. Blank or whitespace-only values are rejected.
    UpdateGroupRequest:
      type: object
      description: Group settings update. Only the name can be updated in this foundation endpoint.
      additionalProperties: false
      minProperties: 1
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 160
          description: Group name after server-side trimming. Blank or whitespace-only values are rejected.
    GroupListResponse:
      type: object
      description: Groups where the authenticated actor has active membership. This foundation response is intentionally unpaginated for now.
      required:
        - groups
      properties:
        groups:
          type: array
          items:
            $ref: "#/components/schemas/GroupResponse"
    GroupResponse:
      type: object
      description: Safe group summary for the authenticated actor. It excludes creator profile IDs, unrelated members, auth/session/credential data, provider payloads, audit metadata, storage paths, and group policy internals.
      required:
        - id
        - name
        - currentUserRole
        - currentUserStatus
        - createdAtUtc
        - updatedAtUtc
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          minLength: 1
          maxLength: 160
        currentUserRole:
          $ref: "#/components/schemas/GroupRole"
        currentUserStatus:
          $ref: "#/components/schemas/GroupMembershipStatus"
        createdAtUtc:
          type: string
          format: date-time
        updatedAtUtc:
          type: string
          format: date-time
    AddGroupMemberRequest:
      type: object
      description: Adds an existing active registered user profile to a group. Role defaults to member when omitted; clients cannot submit actor IDs, auth account IDs, owner IDs, or authorization context.
      additionalProperties: false
      required:
        - userProfileId
      properties:
        userProfileId:
          type: string
          format: uuid
          description: Existing active user profile with an active auth account.
        role:
          $ref: "#/components/schemas/GroupRole"
    UpdateGroupMemberRequest:
      type: object
      description: Updates the group role for an existing active group membership. The route userProfileId identifies the member; body userProfileId fields are rejected.
      additionalProperties: false
      required:
        - role
      properties:
        role:
          $ref: "#/components/schemas/GroupRole"
    GroupMemberListResponse:
      type: object
      description: Active registered group members visible to the authenticated actor.
      required:
        - members
      properties:
        members:
          type: array
          items:
            $ref: "#/components/schemas/GroupMemberResponse"
    GroupMemberResponse:
      type: object
      description: Safe group member summary. It excludes auth account IDs, identifiers, credentials, sessions, token material, provider payloads, audit metadata, storage paths, removed memberships, and unrelated users.
      required:
        - userProfileId
        - displayName
        - role
        - status
        - joinedAtUtc
        - updatedAtUtc
      properties:
        userProfileId:
          type: string
          format: uuid
        displayName:
          type: string
          minLength: 1
          maxLength: 160
        role:
          $ref: "#/components/schemas/GroupRole"
        status:
          $ref: "#/components/schemas/GroupMembershipStatus"
        joinedAtUtc:
          type: string
          format: date-time
        updatedAtUtc:
          type: string
          format: date-time
    GroupRole:
      type: string
      description: Group-scoped role.
      enum:
        - owner
        - member
    GroupMembershipStatus:
      type: string
      description: Group membership status.
      enum:
        - active
        - removed
    CreateRecurringBillTemplateRequest:
      type: object
      description: Recurring bill template creation request. Owner, creator, actor, and authorization identity are derived server-side.
      additionalProperties: false
      required:
        - schedule
        - billPayload
      properties:
        groupId:
          oneOf:
            - type: string
              format: uuid
            - type: "null"
          description: Optional group ID for a group recurring bill. The authenticated actor must be an active group member.
        merchantName:
          oneOf:
            - type: string
              maxLength: 200
            - type: "null"
          description: Optional merchant or template display name after server-side trimming.
        description:
          oneOf:
            - type: string
              maxLength: 1000
            - type: "null"
          description: Optional bounded template description. Audit metadata does not log this text.
        schedule:
          $ref: "#/components/schemas/RecurringBillScheduleRequest"
        billPayload:
          $ref: "#/components/schemas/RecurringBillTemplatePayload"
    UpdateRecurringBillTemplateRequest:
      type: object
      description: Safe recurring bill template update request. Historical generated bills are not mutated.
      additionalProperties: false
      properties:
        merchantName:
          oneOf:
            - type: string
              maxLength: 200
            - type: "null"
          description: Optional merchant or template display name after server-side trimming.
        description:
          oneOf:
            - type: string
              maxLength: 1000
            - type: "null"
          description: Optional bounded template description. Audit metadata does not log this text.
        schedule:
          $ref: "#/components/schemas/RecurringBillScheduleRequest"
        billPayload:
          $ref: "#/components/schemas/RecurringBillTemplatePayload"
    RecurringBillScheduleRequest:
      type: object
      description: Date-based recurrence schedule. The first foundation intentionally avoids time-zone-heavy behavior.
      additionalProperties: false
      required:
        - type
        - startDate
      properties:
        type:
          $ref: "#/components/schemas/RecurringBillScheduleType"
        intervalCount:
          oneOf:
            - type: integer
              minimum: 1
              maximum: 120
            - type: "null"
          description: Positive interval count for weekly, monthly, and yearly schedules.
        intervalDays:
          oneOf:
            - type: integer
              minimum: 1
              maximum: 3660
            - type: "null"
          description: Positive day interval for custom interval day schedules.
        startDate:
          type: string
          format: date
        endDate:
          oneOf:
            - type: string
              format: date
            - type: "null"
        dueOffsetDays:
          oneOf:
            - type: integer
              minimum: -365
              maximum: 365
            - type: "null"
    RecurringBillTemplatePayload:
      type: object
      description: Versioned template bill payload. It is validated at write time and again at draft generation time; generated expense bill rows remain financial truth.
      additionalProperties: false
      required:
        - currency
        - items
      properties:
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        items:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/RecurringBillTemplatePayloadItem"
        adjustments:
          type: array
          items:
            $ref: "#/components/schemas/RecurringBillTemplatePayloadAdjustment"
        payers:
          type: array
          items:
            $ref: "#/components/schemas/RecurringBillTemplatePayloadPayer"
    RecurringBillTemplatePayloadItem:
      type: object
      additionalProperties: false
      required:
        - name
        - amount
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 240
        note:
          oneOf:
            - type: string
              maxLength: 1000
            - type: "null"
        amount:
          type: string
          description: Decimal-safe amount represented as a string.
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        splits:
          type: array
          items:
            $ref: "#/components/schemas/RecurringBillTemplatePayloadItemSplit"
    RecurringBillTemplatePayloadItemSplit:
      type: object
      additionalProperties: false
      required:
        - userProfileId
        - splitMethod
      properties:
        userProfileId:
          type: string
          format: uuid
        splitMethod:
          $ref: "#/components/schemas/ExpenseBillItemSplitMethod"
        basisValue:
          oneOf:
            - type: string
              description: Decimal-safe split basis value represented as a string.
            - type: "null"
        allocationOrder:
          type: integer
          minimum: 0
          default: 0
    RecurringBillTemplatePayloadAdjustment:
      type: object
      additionalProperties: false
      required:
        - type
        - direction
        - allocationMethod
        - amount
      properties:
        type:
          $ref: "#/components/schemas/ExpenseBillAdjustmentType"
        direction:
          $ref: "#/components/schemas/ExpenseBillAdjustmentDirection"
        allocationMethod:
          $ref: "#/components/schemas/PersonalBillAdjustmentAllocationMethod"
        amount:
          type: string
          description: Decimal-safe amount represented as a string.
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        reasonNote:
          oneOf:
            - type: string
              maxLength: 1000
            - type: "null"
    RecurringBillTemplatePayloadPayer:
      type: object
      additionalProperties: false
      required:
        - userProfileId
        - amount
      properties:
        userProfileId:
          type: string
          format: uuid
        amount:
          type: string
          description: Decimal-safe amount represented as a string.
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        paymentMethodLabelSnapshot:
          oneOf:
            - type: string
              maxLength: 120
            - type: "null"
    RecurringBillTemplateListResponse:
      type: object
      required:
        - templates
      properties:
        templates:
          type: array
          items:
            $ref: "#/components/schemas/RecurringBillTemplateResponse"
    RecurringBillTemplateResponse:
      type: object
      description: Safe recurring bill template response. It excludes raw template payload JSON, auth account IDs, session material, storage internals, and unrelated profile details.
      required:
        - id
        - ownerUserProfileId
        - groupId
        - merchantName
        - description
        - status
        - schedule
        - forecastAmount
        - forecastCurrency
        - nextOccurrenceDate
        - payloadVersion
        - createdAtUtc
        - updatedAtUtc
        - archivedAtUtc
      properties:
        id:
          type: string
          format: uuid
        ownerUserProfileId:
          type: string
          format: uuid
        groupId:
          oneOf:
            - type: string
              format: uuid
            - type: "null"
        merchantName:
          oneOf:
            - type: string
              maxLength: 200
            - type: "null"
        description:
          oneOf:
            - type: string
              maxLength: 1000
            - type: "null"
        status:
          $ref: "#/components/schemas/RecurringBillTemplateStatus"
        schedule:
          $ref: "#/components/schemas/RecurringBillScheduleResponse"
        forecastAmount:
          type: string
          description: Decimal-safe forecast estimate represented as a string.
        forecastCurrency:
          $ref: "#/components/schemas/CurrencyCode"
        nextOccurrenceDate:
          oneOf:
            - type: string
              format: date
            - type: "null"
        payloadVersion:
          type: integer
          minimum: 1
        createdAtUtc:
          type: string
          format: date-time
        updatedAtUtc:
          type: string
          format: date-time
        archivedAtUtc:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
    RecurringBillScheduleResponse:
      type: object
      required:
        - type
        - intervalCount
        - intervalDays
        - startDate
        - endDate
        - dueOffsetDays
      properties:
        type:
          $ref: "#/components/schemas/RecurringBillScheduleType"
        intervalCount:
          oneOf:
            - type: integer
            - type: "null"
        intervalDays:
          oneOf:
            - type: integer
            - type: "null"
        startDate:
          type: string
          format: date
        endDate:
          oneOf:
            - type: string
              format: date
            - type: "null"
        dueOffsetDays:
          oneOf:
            - type: integer
            - type: "null"
    RecurringBillForecastListResponse:
      type: object
      required:
        - occurrences
      properties:
        occurrences:
          type: array
          items:
            $ref: "#/components/schemas/RecurringBillForecastOccurrenceResponse"
    RecurringBillForecastOccurrenceResponse:
      type: object
      description: Forecast occurrence response. Forecast-only rows are estimates and do not imply bill creation.
      required:
        - templateId
        - occurrenceId
        - groupId
        - occurrenceDate
        - dueDate
        - status
        - draftGenerated
        - generatedBillId
        - forecastAmount
        - forecastCurrency
        - merchantName
      properties:
        templateId:
          type: string
          format: uuid
        occurrenceId:
          oneOf:
            - type: string
              format: uuid
            - type: "null"
        groupId:
          oneOf:
            - type: string
              format: uuid
            - type: "null"
        occurrenceDate:
          type: string
          format: date
        dueDate:
          oneOf:
            - type: string
              format: date
            - type: "null"
        status:
          $ref: "#/components/schemas/RecurringBillOccurrenceStatus"
        draftGenerated:
          type: boolean
        generatedBillId:
          oneOf:
            - type: string
              format: uuid
            - type: "null"
        forecastAmount:
          type: string
        forecastCurrency:
          $ref: "#/components/schemas/CurrencyCode"
        merchantName:
          oneOf:
            - type: string
              maxLength: 200
            - type: "null"
    RecurringBillGenerateDraftResponse:
      type: object
      required:
        - templateId
        - occurrenceId
        - occurrenceDate
        - dueDate
        - occurrenceStatus
        - generatedBillId
        - billStatus
        - totalAmount
        - totalCurrency
      properties:
        templateId:
          type: string
          format: uuid
        occurrenceId:
          type: string
          format: uuid
        occurrenceDate:
          type: string
          format: date
        dueDate:
          oneOf:
            - type: string
              format: date
            - type: "null"
        occurrenceStatus:
          $ref: "#/components/schemas/RecurringBillOccurrenceStatus"
        generatedBillId:
          type: string
          format: uuid
        billStatus:
          $ref: "#/components/schemas/ExpenseBillStatus"
        totalAmount:
          type: string
        totalCurrency:
          $ref: "#/components/schemas/CurrencyCode"
    RecurringBillScheduleType:
      type: string
      enum:
        - weekly
        - monthly
        - yearly
        - custom_interval_days
    RecurringBillTemplateStatus:
      type: string
      enum:
        - active
        - paused
        - archived
    RecurringBillOccurrenceStatus:
      type: string
      enum:
        - forecasted
        - draft_generated
        - skipped
        - cancelled
    CreatePersonalBillRequest:
      type: object
      description: Minimal personal bill creation request. Creator, group, participant, payer, profile, account, file, and authorization identity are derived server-side and cannot be submitted by clients.
      additionalProperties: false
      required:
        - billDate
        - currency
        - items
      properties:
        merchantName:
          oneOf:
            - type: string
              maxLength: 200
            - type: "null"
          description: Optional merchant name after server-side trimming.
        billDate:
          type: string
          format: date
          description: Bill date as yyyy-MM-dd.
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        items:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/CreatePersonalBillItemRequest"
        adjustments:
          type: array
          items:
            $ref: "#/components/schemas/CreatePersonalBillAdjustmentRequest"
        payerPaymentMethodLabelSnapshot:
          oneOf:
            - type: string
              maxLength: 120
            - type: "null"
          description: Optional payment method label snapshot for the actor payer row after server-side trimming.
    CreatePersonalBillItemRequest:
      type: object
      description: Personal bill item. The service creates exactly one exact-amount split for the authenticated actor.
      additionalProperties: false
      required:
        - name
        - amount
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 240
          description: Item name after server-side trimming.
        note:
          oneOf:
            - type: string
              maxLength: 1000
            - type: "null"
          description: Optional item note after server-side trimming.
        amount:
          type: string
          description: Decimal-safe item amount represented as a string.
          examples:
            - "12.34"
        currency:
          $ref: "#/components/schemas/CurrencyCode"
    CreatePersonalBillAdjustmentRequest:
      type: object
      description: Optional personal bill adjustment. Manual allocation is intentionally excluded from this first endpoint slice.
      additionalProperties: false
      required:
        - type
        - direction
        - allocationMethod
        - amount
      properties:
        type:
          $ref: "#/components/schemas/ExpenseBillAdjustmentType"
        direction:
          $ref: "#/components/schemas/ExpenseBillAdjustmentDirection"
        allocationMethod:
          $ref: "#/components/schemas/PersonalBillAdjustmentAllocationMethod"
        amount:
          type: string
          description: Decimal-safe adjustment amount represented as a string.
          examples:
            - "1.23"
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        reasonNote:
          oneOf:
            - type: string
              maxLength: 1000
            - type: "null"
          description: Optional adjustment reason note after server-side trimming.
    RejectBillParticipantRequest:
      type: object
      description: Bounded participant rejection request. Free-text rejection notes are intentionally excluded from this slice.
      additionalProperties: false
      required:
        - reasonCode
      properties:
        reasonCode:
          $ref: "#/components/schemas/ExpenseBillParticipantRejectionReasonCode"
    CreateBillRevisionProposalRequest:
      type: object
      description: Bounded candidate money snapshot for a bill revision proposal. The server derives actor identity, bill visibility, active revision policy, timestamps, approval rows, affected participants, payer confirmation flags, audit metadata, and settlement safety behavior. This snapshot does not accept auth/session IDs, storage fields, settlement fields, proof fields, free-text reasons, or internal database-only fields.
      additionalProperties: false
      required:
        - totalAmount
        - totalCurrency
        - participants
        - payers
      properties:
        totalAmount:
          type: string
          description: Decimal-safe proposed bill total amount represented as a string. Must be positive and match participant and payer totals.
          examples:
            - "100.00"
        totalCurrency:
          $ref: "#/components/schemas/CurrencyCode"
        participants:
          type: array
          minItems: 1
          description: Proposed participant share rows. This first endpoint slice requires the participant set to match the current bill participant set.
          items:
            $ref: "#/components/schemas/BillRevisionProposalParticipantRequest"
        payers:
          type: array
          minItems: 1
          description: Proposed payer contribution rows. Payer profile IDs must already be related to the bill.
          items:
            $ref: "#/components/schemas/BillRevisionProposalPayerRequest"
    BillRevisionProposalParticipantRequest:
      type: object
      additionalProperties: false
      required:
        - userProfileId
        - resolvedShareAmount
        - resolvedShareCurrency
      properties:
        userProfileId:
          type: string
          format: uuid
        resolvedShareAmount:
          type: string
          description: Decimal-safe participant share amount represented as a string.
          examples:
            - "50.00"
        resolvedShareCurrency:
          $ref: "#/components/schemas/CurrencyCode"
    BillRevisionProposalPayerRequest:
      type: object
      additionalProperties: false
      required:
        - userProfileId
        - amount
        - currency
      properties:
        userProfileId:
          type: string
          format: uuid
        amount:
          type: string
          description: Decimal-safe proposed payer contribution represented as a string. Must be positive.
          examples:
            - "100.00"
        currency:
          $ref: "#/components/schemas/CurrencyCode"
    ApproveBillRevisionRequest:
      type: object
      description: Revision-specific approval basis. The route revision ID, accepted amount, currency, and calculation hash must match the server-stored pending approval exactly.
      additionalProperties: false
      required:
        - acceptedAmount
        - currency
        - calculationHash
      properties:
        acceptedAmount:
          type: string
          description: Decimal-safe amount the participant is accepting for this exact revision.
          examples:
            - "50.00"
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        calculationHash:
          type: string
          minLength: 64
          maxLength: 64
          pattern: "^[0-9a-f]{64}$"
          description: Deterministic revision review hash returned by the API for this calculation state. It is not an auth/session token or storage secret.
    BillRevisionListResponse:
      type: object
      description: Bill revisions visible to the authenticated actor. The response is intentionally unpaginated for this first lifecycle slice.
      required:
        - revisions
      properties:
        revisions:
          type: array
          items:
            $ref: "#/components/schemas/BillRevisionResponse"
    BillRevisionResponse:
      type: object
      description: Safe bill revision response. It exposes bounded revision lifecycle, money, affected-participant, payer-confirmation, and approval review fields while excluding auth/session/credential data, storage internals, proof data, settlement rows, raw audit metadata, and unrelated user data.
      required:
        - id
        - billId
        - groupId
        - proposalCreatorUserProfileId
        - supersedesExpenseBillRevisionId
        - supersededByExpenseBillRevisionId
        - status
        - totalAmount
        - totalCurrency
        - calculationHash
        - submittedAtUtc
        - withdrawnAtUtc
        - supersededAtUtc
        - rejectedAtUtc
        - appliedAtUtc
        - cancelledAtUtc
        - createdAtUtc
        - updatedAtUtc
        - participants
        - payers
        - approvals
      properties:
        id:
          type: string
          format: uuid
        billId:
          type: string
          format: uuid
        groupId:
          oneOf:
            - type: string
              format: uuid
            - type: "null"
        proposalCreatorUserProfileId:
          type: string
          format: uuid
        supersedesExpenseBillRevisionId:
          oneOf:
            - type: string
              format: uuid
            - type: "null"
        supersededByExpenseBillRevisionId:
          oneOf:
            - type: string
              format: uuid
            - type: "null"
        status:
          $ref: "#/components/schemas/ExpenseBillRevisionStatus"
        totalAmount:
          type: string
          description: Decimal-safe proposed revision total amount represented as a string.
        totalCurrency:
          $ref: "#/components/schemas/CurrencyCode"
        calculationHash:
          type: string
          minLength: 64
          maxLength: 64
          pattern: "^[0-9a-f]{64}$"
          description: Deterministic revision review hash for client display/approval binding. It is not an auth/session token or storage secret.
        submittedAtUtc:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        withdrawnAtUtc:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        supersededAtUtc:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        rejectedAtUtc:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        appliedAtUtc:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        cancelledAtUtc:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        createdAtUtc:
          type: string
          format: date-time
        updatedAtUtc:
          type: string
          format: date-time
        participants:
          type: array
          items:
            $ref: "#/components/schemas/BillRevisionParticipantResponse"
        payers:
          type: array
          items:
            $ref: "#/components/schemas/BillRevisionPayerResponse"
        approvals:
          type: array
          items:
            $ref: "#/components/schemas/BillRevisionApprovalResponse"
    BillRevisionParticipantResponse:
      type: object
      required:
        - userProfileId
        - resolvedShareAmount
        - resolvedShareCurrency
        - affectedByRevision
      properties:
        userProfileId:
          type: string
          format: uuid
        resolvedShareAmount:
          type: string
        resolvedShareCurrency:
          $ref: "#/components/schemas/CurrencyCode"
        affectedByRevision:
          type: boolean
    BillRevisionPayerResponse:
      type: object
      required:
        - userProfileId
        - amount
        - currency
        - requiresPayerConfirmation
        - payerConfirmationStatus
      properties:
        userProfileId:
          type: string
          format: uuid
        amount:
          type: string
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        requiresPayerConfirmation:
          type: boolean
        payerConfirmationStatus:
          $ref: "#/components/schemas/ExpenseBillPayerConfirmationStatus"
    BillRevisionApprovalResponse:
      type: object
      required:
        - participantUserProfileId
        - acceptedAmount
        - currency
        - status
        - approvedAtUtc
        - rejectedAtUtc
        - invalidatedAtUtc
      properties:
        participantUserProfileId:
          type: string
          format: uuid
        acceptedAmount:
          type: string
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        status:
          $ref: "#/components/schemas/ExpenseBillRevisionApprovalStatus"
        approvedAtUtc:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        rejectedAtUtc:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        invalidatedAtUtc:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
    PersonalBillListResponse:
      type: object
      description: Personal bills visible to the authenticated actor. This first foundation response is intentionally unpaginated.
      required:
        - bills
      properties:
        bills:
          type: array
          items:
            $ref: "#/components/schemas/PersonalBillResponse"
    PersonalBillResponse:
      type: object
      description: Safe personal bill response for the authenticated actor. It excludes auth/session/credential data, group membership internals, storage paths, object keys, provider internals, audit metadata, OCR data, and unrelated user data. Adjustment allocations are calculated response data because no adjustment-allocation persistence table exists in the current schema.
      required:
        - id
        - merchantName
        - billDate
        - status
        - totalAmount
        - totalCurrency
        - createdAtUtc
        - updatedAtUtc
        - items
        - participants
        - payers
        - adjustments
        - calculatedAdjustmentAllocations
      properties:
        id:
          type: string
          format: uuid
        merchantName:
          oneOf:
            - type: string
              maxLength: 200
            - type: "null"
        billDate:
          type: string
          format: date
        status:
          $ref: "#/components/schemas/ExpenseBillStatus"
        totalAmount:
          type: string
          description: Decimal-safe total amount represented as a string.
        totalCurrency:
          $ref: "#/components/schemas/CurrencyCode"
        createdAtUtc:
          type: string
          format: date-time
        updatedAtUtc:
          type: string
          format: date-time
        items:
          type: array
          items:
            $ref: "#/components/schemas/PersonalBillItemResponse"
        participants:
          type: array
          items:
            $ref: "#/components/schemas/PersonalBillParticipantResponse"
        payers:
          type: array
          items:
            $ref: "#/components/schemas/PersonalBillPayerResponse"
        adjustments:
          type: array
          items:
            $ref: "#/components/schemas/PersonalBillAdjustmentResponse"
        calculatedAdjustmentAllocations:
          type: array
          description: Calculated adjustment allocation data returned for display/auditability. It is not backed by a dedicated persistence table in this schema slice.
          items:
            $ref: "#/components/schemas/PersonalBillCalculatedAdjustmentAllocationResponse"
    PersonalBillItemResponse:
      type: object
      required:
        - id
        - name
        - note
        - amount
        - currency
        - sortOrder
        - splits
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          minLength: 1
          maxLength: 240
        note:
          oneOf:
            - type: string
              maxLength: 1000
            - type: "null"
        amount:
          type: string
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        sortOrder:
          type: integer
          minimum: 0
        splits:
          type: array
          items:
            $ref: "#/components/schemas/PersonalBillItemSplitResponse"
    PersonalBillItemSplitResponse:
      type: object
      required:
        - userProfileId
        - splitMethod
        - basisValue
        - resolvedAmount
        - resolvedCurrency
        - allocationOrder
        - receivedResidualMinorUnit
      properties:
        userProfileId:
          type: string
          format: uuid
        splitMethod:
          $ref: "#/components/schemas/ExpenseBillItemSplitMethod"
        basisValue:
          oneOf:
            - type: string
            - type: "null"
        resolvedAmount:
          type: string
        resolvedCurrency:
          $ref: "#/components/schemas/CurrencyCode"
        allocationOrder:
          type: integer
          minimum: 0
        receivedResidualMinorUnit:
          type: boolean
    PersonalBillParticipantResponse:
      type: object
      required:
        - userProfileId
        - status
        - resolvedShareAmount
        - resolvedShareCurrency
        - rejectionReasonCode
      properties:
        userProfileId:
          type: string
          format: uuid
        status:
          $ref: "#/components/schemas/ExpenseBillParticipantStatus"
        resolvedShareAmount:
          type: string
        resolvedShareCurrency:
          $ref: "#/components/schemas/CurrencyCode"
        rejectionReasonCode:
          oneOf:
            - $ref: "#/components/schemas/ExpenseBillParticipantRejectionReasonCode"
            - type: "null"
          description: Bounded rejection reason code when the participant rejected the bill; null otherwise.
    PersonalBillPayerResponse:
      type: object
      required:
        - userProfileId
        - amount
        - currency
        - paymentMethodLabelSnapshot
      properties:
        userProfileId:
          type: string
          format: uuid
        amount:
          type: string
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        paymentMethodLabelSnapshot:
          oneOf:
            - type: string
              maxLength: 120
            - type: "null"
    PersonalBillAdjustmentResponse:
      type: object
      required:
        - id
        - type
        - direction
        - allocationMethod
        - amount
        - currency
        - reasonNote
        - sortOrder
      properties:
        id:
          type: string
          format: uuid
        type:
          $ref: "#/components/schemas/ExpenseBillAdjustmentType"
        direction:
          $ref: "#/components/schemas/ExpenseBillAdjustmentDirection"
        allocationMethod:
          $ref: "#/components/schemas/PersonalBillAdjustmentAllocationMethod"
        amount:
          type: string
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        reasonNote:
          oneOf:
            - type: string
              maxLength: 1000
            - type: "null"
        sortOrder:
          type: integer
          minimum: 0
    PersonalBillCalculatedAdjustmentAllocationResponse:
      type: object
      required:
        - expenseBillAdjustmentId
        - userProfileId
        - direction
        - allocationMethod
        - allocatedAmount
        - currency
        - allocationOrder
        - receivedResidualMinorUnit
      properties:
        expenseBillAdjustmentId:
          type: string
          format: uuid
        userProfileId:
          type: string
          format: uuid
        direction:
          $ref: "#/components/schemas/ExpenseBillAdjustmentDirection"
        allocationMethod:
          $ref: "#/components/schemas/PersonalBillAdjustmentAllocationMethod"
        allocatedAmount:
          type: string
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        allocationOrder:
          type: integer
          minimum: 0
        receivedResidualMinorUnit:
          type: boolean
    SettlementCandidateListResponse:
      type: object
      description: Bounded read-only settlement candidate previews involving the authenticated actor only. The response excludes full net-position lists, payment details, proof/file internals, auth/session data, request bodies, and unrelated candidate pairs.
      required:
        - candidates
      properties:
        candidates:
          type: array
          items:
            $ref: "#/components/schemas/SettlementCandidateResponse"
    SettlementCandidateResponse:
      type: object
      description: One deterministic settlement candidate derived from a visible confirmed bill where the authenticated actor is the debtor or creditor.
      required:
        - candidateKey
        - sourceExpenseBillId
        - groupId
        - debtorUserProfileId
        - creditorUserProfileId
        - amount
        - currency
        - basis
        - allocationOrder
      properties:
        candidateKey:
          type: string
          minLength: 1
          maxLength: 240
          description: Deterministic server-derived candidate key for this bill/counterparty/amount/currency basis.
        sourceExpenseBillId:
          type: string
          format: uuid
        groupId:
          oneOf:
            - type: string
              format: uuid
            - type: "null"
          description: Route group ID for group bills; null for personal bills.
        debtorUserProfileId:
          type: string
          format: uuid
        creditorUserProfileId:
          type: string
          format: uuid
        amount:
          type: string
          description: Decimal-safe candidate amount represented as a string.
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        basis:
          type: string
          enum:
            - confirmed_bill_net_position_v1
          description: Server policy basis used to derive the candidate.
        allocationOrder:
          type: integer
          minimum: 0
    CreateSettlementRequestRequest:
      type: object
      description: Minimal settlement request creation body. The candidate key must come from a server-derived candidate preview; all settlement parties, amounts, currency, source bill, group, requester, status, payment details, proof/file IDs, auth account IDs, and session IDs are derived server-side.
      additionalProperties: false
      required:
        - candidateKey
      properties:
        candidateKey:
          type: string
          minLength: 1
          maxLength: 240
          description: Server-derived candidate key from the current confirmed bill candidate basis.
    CreateSettlementBasketRequest:
      type: object
      description: Settlement basket write request for the first same-currency pay-all-outstanding selection mode. The client submits only the counterparty, current-actor direction, optional group scope, currency, and selection mode; source lines, candidate keys, amounts, request parties, requester, status, and audit metadata are always derived server-side at write time.
      additionalProperties: false
      required:
        - counterpartyUserProfileId
        - direction
        - currency
        - selectionMode
      properties:
        counterpartyUserProfileId:
          type: string
          format: uuid
          description: Counterparty profile ID for the current actor's basket creation scope.
        direction:
          $ref: "#/components/schemas/SettlementBalanceDirection"
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        groupId:
          oneOf:
            - type: string
              format: uuid
            - type: "null"
          description: Optional group scope. Null or omission creates a personal/non-group basket only.
        selectionMode:
          $ref: "#/components/schemas/SettlementBasketSelectionMode"
    SettlementBasketPreviewRequest:
      type: object
      description: Read-only basket preview request for the first same-currency pay-all-outstanding selection mode. The client submits only the counterparty, current-actor direction, optional group scope, currency, and selection mode; candidate lines and amounts are always derived server-side.
      additionalProperties: false
      required:
        - counterpartyUserProfileId
        - direction
        - currency
        - selectionMode
      properties:
        counterpartyUserProfileId:
          type: string
          format: uuid
          description: Counterparty profile ID for the current actor's preview scope.
        direction:
          $ref: "#/components/schemas/SettlementBalanceDirection"
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        groupId:
          oneOf:
            - type: string
              format: uuid
            - type: "null"
          description: Optional group scope. Null or omission previews personal/non-group candidates only.
        selectionMode:
          $ref: "#/components/schemas/SettlementBasketSelectionMode"
    SettlementBasketPreviewResponse:
      type: object
      description: Bounded read-only settlement basket preview for one current-actor/counterparty direction, optional group, and currency. Amounts are decimal-safe strings; line keys and amounts are server-derived from eligible confirmed bill candidates. The response excludes merchant/item details, payment details, proof/file/storage internals, auth/session data, raw audit metadata, raw request bodies, unrelated users, and receipt/OCR contents.
      required:
        - generatedAtUtc
        - selectionMode
        - direction
        - debtorUserProfileId
        - creditorUserProfileId
        - counterpartyUserProfileId
        - groupId
        - currency
        - exactSelectedTotal
        - lineCount
        - lines
      properties:
        generatedAtUtc:
          type: string
          format: date-time
          description: UTC timestamp when the read-only preview was generated.
        selectionMode:
          $ref: "#/components/schemas/SettlementBasketSelectionMode"
        direction:
          $ref: "#/components/schemas/SettlementBalanceDirection"
        debtorUserProfileId:
          type: string
          format: uuid
        creditorUserProfileId:
          type: string
          format: uuid
        counterpartyUserProfileId:
          type: string
          format: uuid
        groupId:
          oneOf:
            - type: string
              format: uuid
            - type: "null"
          description: Group scope for group basket previews; null for personal previews.
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        exactSelectedTotal:
          type: string
          description: Decimal-safe sum of all selected preview line exact amounts represented as a string.
        lineCount:
          type: integer
          minimum: 0
        lines:
          type: array
          items:
            $ref: "#/components/schemas/SettlementBasketPreviewLineResponse"
    SettlementBasketPreviewLineResponse:
      type: object
      description: One bounded server-derived outstanding source line included in a settlement basket preview. It exposes source bill ID, active accepted bill revision trace where available, deterministic candidate key, exact amount/currency, candidate basis, and source bill creation timestamp only.
      required:
        - sourceExpenseBillId
        - sourceBillRevisionId
        - sourceCandidateKey
        - exactAmount
        - currency
        - candidateBasis
        - createdAtUtc
      properties:
        sourceExpenseBillId:
          type: string
          format: uuid
        sourceBillRevisionId:
          oneOf:
            - type: string
              format: uuid
            - type: "null"
          description: Active accepted bill revision ID used by the current bill state when available; otherwise null.
        sourceCandidateKey:
          type: string
          minLength: 1
          maxLength: 240
          description: Deterministic server-derived candidate key for this bill/counterparty/amount/currency basis.
        exactAmount:
          type: string
          description: Decimal-safe exact outstanding amount represented as a string.
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        candidateBasis:
          type: string
          enum:
            - confirmed_bill_net_position_v1
          description: Server policy basis used to derive the candidate.
        createdAtUtc:
          type: string
          format: date-time
          description: Source bill creation timestamp used for deterministic preview ordering.
    SettlementRequestListResponse:
      type: object
      description: Bounded read-only settlement request list for the authenticated actor. It includes only requests where the actor is debtor, creditor, or requester, includes selected request-line summaries, and excludes payment details, proof/file internals, bill merchant/item details, auth/session data, raw audit metadata, request bodies, and unrelated users.
      required:
        - settlements
      properties:
        settlements:
          type: array
          items:
            $ref: "#/components/schemas/SettlementRequestResponse"
    SettlementRequestResponse:
      type: object
      description: Bounded settlement request response. It exposes request-level fields and selected request-line summaries only, and excludes bill merchant/item details, payment details, proof/file internals, auth/session data, raw audit metadata, unrelated candidates, unrelated users, and raw request bodies.
      required:
        - id
        - sourceExpenseBillId
        - groupId
        - debtorUserProfileId
        - creditorUserProfileId
        - amount
        - currency
        - status
        - requestedByUserProfileId
        - requestedAtUtc
        - createdAtUtc
        - updatedAtUtc
        - lines
      properties:
        id:
          type: string
          format: uuid
        sourceExpenseBillId:
          type: string
          format: uuid
        groupId:
          oneOf:
            - type: string
              format: uuid
            - type: "null"
          description: Route group ID for group bill settlement requests; null for personal bill settlement requests.
        debtorUserProfileId:
          type: string
          format: uuid
        creditorUserProfileId:
          type: string
          format: uuid
        amount:
          type: string
          description: Decimal-safe requested settlement amount represented as a string.
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        status:
          $ref: "#/components/schemas/SettlementRequestStatus"
        requestedByUserProfileId:
          type: string
          format: uuid
        requestedAtUtc:
          type: string
          format: date-time
        createdAtUtc:
          type: string
          format: date-time
        updatedAtUtc:
          type: string
          format: date-time
        lines:
          type: array
          description: Concrete selected request lines included in this settlement request. Single-bill creation returns one line; basket creation can return multiple server-derived lines ordered by allocation order.
          items:
            $ref: "#/components/schemas/SettlementRequestLineResponse"
    SettlementRequestLineResponse:
      type: object
      description: Bounded settlement request-line summary for the selected concrete bill candidate. It exposes stable source IDs, the already-public candidate key, exact amount/currency, order, status, and timestamps; it excludes bill merchant/item details, payment details, proof/file internals, auth/session data, raw audit metadata, and unrelated users.
      required:
        - id
        - sourceExpenseBillId
        - sourceBillRevisionId
        - sourceCandidateKey
        - exactAmount
        - currency
        - allocationOrder
        - status
        - createdAtUtc
        - updatedAtUtc
      properties:
        id:
          type: string
          format: uuid
        sourceExpenseBillId:
          type: string
          format: uuid
        sourceBillRevisionId:
          oneOf:
            - type: string
              format: uuid
            - type: "null"
          description: Active accepted bill revision ID used for the selected line when the source bill has one; otherwise null.
        sourceCandidateKey:
          oneOf:
            - type: string
              minLength: 1
              maxLength: 240
            - type: "null"
          description: Server-derived settlement candidate key selected into this request line.
        exactAmount:
          type: string
          description: Decimal-safe exact selected line amount represented as a string.
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        allocationOrder:
          type: integer
          minimum: 0
        status:
          $ref: "#/components/schemas/SettlementRequestLineStatus"
        createdAtUtc:
          type: string
          format: date-time
        updatedAtUtc:
          type: string
          format: date-time
    SettlementBalanceProjectionListResponse:
      type: object
      description: Bounded read-only settlement balance projection list for the authenticated actor. It contains only debtor/creditor balance rows that can be safely rebuilt from settlement request lines and payment allocations, and excludes requester-only records, bill merchant/item details, payment details, proof/file/storage internals, auth/session data, raw audit metadata, raw request bodies, and unrelated users.
      required:
        - generatedAtUtc
        - balances
      properties:
        generatedAtUtc:
          type: string
          format: date-time
          description: UTC timestamp when the projection was generated.
        balances:
          type: array
          items:
            $ref: "#/components/schemas/SettlementBalanceProjectionResponse"
    SettlementBalanceProjectionResponse:
      type: object
      description: One bounded current-actor settlement balance projection row grouped by actor, counterparty, direction, optional group, and currency. Amount fields are decimal-safe strings, active allocation coverage is separated into pending marked-paid claims and confirmed cleared payments, and receiver-confirmed residual effects are exposed without creating or netting a broad credit ledger.
      required:
        - counterpartyUserProfileId
        - groupId
        - direction
        - currency
        - selectedLineAmount
        - pendingClaimedAmount
        - confirmedClearedAmount
        - remainingUnclaimedAmount
        - confirmedRemainingResidualAmount
        - waivedResidualAmount
        - creditResidualAmount
        - requestCount
        - lineCount
        - pendingPaymentCount
        - confirmedPaymentCount
      properties:
        counterpartyUserProfileId:
          type: string
          format: uuid
          description: Debtor or creditor counterparty profile ID for this current-actor balance row.
        groupId:
          oneOf:
            - type: string
              format: uuid
            - type: "null"
          description: Route group ID for group-scoped settlement balances; null for personal settlement balances.
        direction:
          $ref: "#/components/schemas/SettlementBalanceDirection"
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        selectedLineAmount:
          type: string
          description: Decimal-safe selected request-line total amount represented as a string.
        pendingClaimedAmount:
          type: string
          description: Decimal-safe amount represented as a string for allocations from marked_paid payments.
        confirmedClearedAmount:
          type: string
          description: Decimal-safe amount represented as a string for allocations from confirmed payments.
        remainingUnclaimedAmount:
          type: string
          description: Decimal-safe selected line amount minus pending claimed, confirmed cleared, and receiver-confirmed underpayment-waiver amounts, never below zero. Confirmed credit-forward and waived-by-payer overpayment residuals are not netted against unrelated remaining balances.
        confirmedRemainingResidualAmount:
          type: string
          description: Decimal-safe receiver-confirmed underpayment residual amount that remains debt through remaining_balance or carried_forward policy.
        waivedResidualAmount:
          type: string
          description: Decimal-safe receiver-confirmed residual amount waived through underpayment waived or overpayment waived_by_payer policy. Only underpayment waiver reduces remainingUnclaimedAmount for the selected request scope.
        creditResidualAmount:
          type: string
          description: Decimal-safe receiver-confirmed overpayment credit_forward residual amount exposed as bounded residual information without a broad credit ledger or automatic netting against unrelated balances.
        requestCount:
          type: integer
          minimum: 0
          description: Count of settlement requests included in this grouped projection row.
        lineCount:
          type: integer
          minimum: 0
          description: Count of selected settlement request lines included in this grouped projection row.
        pendingPaymentCount:
          type: integer
          minimum: 0
          description: Count of marked_paid payment claims contributing active pending allocation coverage.
        confirmedPaymentCount:
          type: integer
          minimum: 0
          description: Count of confirmed payment claims contributing active cleared allocation coverage.
    CreateSettlementPaymentRequest:
      type: object
      description: Minimal settlement payment claim body. Amount is a decimal-safe string, currency must match the settlement request currency, paymentDate is a yyyy-MM-dd calendar date, and proposedResidualPolicy may be supplied only for explicit non-exact residual proposals. The server derives settlement request, payer/debtor, receiver/creditor, created-by profile, payment status, request status transition, remaining amount, residual direction/status, and partial-vs-covered state; proof files, payment details, payment handles, notes, and auth/session/account identity fields are not accepted.
      additionalProperties: false
      required:
        - amount
        - currency
        - paymentDate
      properties:
        amount:
          type: string
          description: Positive decimal-safe payment amount represented as a string. The API rejects zero, negative values, malformed decimals, unsupported precision, and overpayment unless an explicit supported residual policy is supplied.
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        paymentDate:
          type: string
          format: date
          description: Claimed payment date as yyyy-MM-dd.
        proposedResidualPolicy:
          $ref: "#/components/schemas/SettlementResidualPolicy"
    SettlementPaymentListResponse:
      type: object
      description: Bounded read-only settlement payment list for one visible settlement request. It includes only visible payment claims, bounded allocation summaries, and bounded residual summaries, and excludes payment details, payment handles, payment notes, QR data, proof/file/storage internals, bill merchant/item details, auth/session data, raw audit metadata, request bodies, and unrelated users.
      required:
        - payments
      properties:
        payments:
          type: array
          items:
            $ref: "#/components/schemas/SettlementPaymentResponse"
    SettlementPaymentAllocationResponse:
      type: object
      description: Bounded settlement payment allocation summary showing how a payment claim maps to selected settlement request lines. It excludes bill merchant/item details, proof/file/storage internals, payment details, auth/session data, raw audit metadata, private notes, and unrelated users.
      required:
        - id
        - settlementRequestLineId
        - clearedAmount
        - currency
        - allocationOrder
        - createdAtUtc
      properties:
        id:
          type: string
          format: uuid
        settlementRequestLineId:
          type: string
          format: uuid
        clearedAmount:
          type: string
          description: Positive decimal-safe cleared amount represented as a string.
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        allocationOrder:
          type: integer
          minimum: 0
        createdAtUtc:
          type: string
          format: date-time
    SettlementPaymentResidualResponse:
      type: object
      description: Bounded settlement residual summary tied to one payment claim. It exposes only stable IDs, direction, amount, currency, policy, status, and lifecycle timestamps, and excludes payment details, proof/file/storage internals, auth/session data, raw request bodies, audit metadata, bill merchant/item details, OCR text, and unrelated users.
      required:
        - id
        - settlementPaymentId
        - settlementRequestId
        - direction
        - amount
        - currency
        - policy
        - status
        - createdAtUtc
      properties:
        id:
          type: string
          format: uuid
        settlementPaymentId:
          type: string
          format: uuid
        settlementRequestId:
          type: string
          format: uuid
        direction:
          $ref: "#/components/schemas/SettlementResidualDirection"
        amount:
          type: string
          description: Positive decimal-safe residual amount represented as a string.
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        policy:
          $ref: "#/components/schemas/SettlementResidualPolicy"
        status:
          $ref: "#/components/schemas/SettlementResidualStatus"
        createdAtUtc:
          type: string
          format: date-time
        resolvedAtUtc:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
    SettlementPaymentResponse:
      type: object
      description: Bounded settlement payment response for payment read, claim, confirmation, dispute, and cancellation surfaces. It exposes payment fields, bounded allocation summaries, bounded residual summaries, and resulting settlement request status only, and excludes payment details, payment handles, payment notes, QR data, proof/file/storage internals, auth/session/credential/token data, raw request bodies, bill merchant/item details, OCR text, and unrelated users.
      required:
        - paymentId
        - settlementRequestId
        - paidByUserProfileId
        - receivedByUserProfileId
        - amount
        - currency
        - status
        - paymentDate
        - claimedAtUtc
        - createdAtUtc
        - updatedAtUtc
        - allocations
        - residuals
        - settlementRequestStatus
      properties:
        paymentId:
          type: string
          format: uuid
        settlementRequestId:
          type: string
          format: uuid
        paidByUserProfileId:
          type: string
          format: uuid
        receivedByUserProfileId:
          type: string
          format: uuid
        amount:
          type: string
          description: Decimal-safe payment amount represented as a string.
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        status:
          $ref: "#/components/schemas/SettlementPaymentStatus"
        paymentDate:
          type: string
          format: date
        claimedAtUtc:
          type: string
          format: date-time
        createdAtUtc:
          type: string
          format: date-time
        updatedAtUtc:
          type: string
          format: date-time
        allocations:
          type: array
          items:
            $ref: "#/components/schemas/SettlementPaymentAllocationResponse"
        residuals:
          type: array
          items:
            $ref: "#/components/schemas/SettlementPaymentResidualResponse"
        settlementRequestStatus:
          $ref: "#/components/schemas/SettlementRequestStatus"
    BillAttachmentListResponse:
      type: object
      description: Safe bill attachment metadata list. It excludes file bytes, storage object keys, storage paths, provider URLs, direct URLs, vault references, original filenames, auth/session data, bill merchant/item details, raw audit metadata, OCR text, and unrelated users.
      required:
        - attachments
      properties:
        attachments:
          type: array
          items:
            $ref: "#/components/schemas/BillAttachmentResponse"
    BillAttachmentResponse:
      type: object
      description: Safe bill attachment file metadata. It exposes stable file and bill IDs plus bounded purpose and content metadata only, and never contains storage object keys, storage paths, provider URLs, direct URLs, vault references, original filenames, bytes, thumbnails, OCR text, raw audit metadata, or unrelated user data.
      required:
        - fileId
        - billId
        - purpose
        - contentType
        - sizeBytes
        - uploadedAtUtc
        - updatedAtUtc
      properties:
        fileId:
          type: string
          format: uuid
          description: Stable file metadata identifier for the attachment.
        billId:
          type: string
          format: uuid
          description: Expense bill ID that owns this attachment association.
        purpose:
          type: string
          enum:
            - receipt
            - supporting_attachment
          description: Bounded bill attachment purpose stored on the bill association.
        contentType:
          type: string
          enum:
            - image/png
            - image/jpeg
            - image/webp
            - application/pdf
        sizeBytes:
          type: integer
          format: int64
          minimum: 1
          maximum: 5242880
        uploadedAtUtc:
          type: string
          format: date-time
          description: Timestamp when the bill attachment association was created.
        updatedAtUtc:
          type: string
          format: date-time
          description: Timestamp when the underlying file metadata was last updated.
    AttachBillAttachmentRequest:
      type: object
      description: Multipart form used to upload one personal or group bill attachment file. The purpose must be receipt or supporting_attachment. Receipt uploads accept PNG, JPEG, and WEBP. Supporting attachments accept PNG, JPEG, WEBP, and PDF. The filename is treated as submitted transport metadata only and is not persisted into attachment responses or audit metadata.
      additionalProperties: false
      required:
        - file
        - purpose
      properties:
        file:
          type: string
          format: binary
          maxLength: 5242880
          description: Bill attachment file. The maximum accepted file size is 5 MB.
        purpose:
          type: string
          enum:
            - receipt
            - supporting_attachment
          description: Bounded bill attachment purpose for the file association.
    ReceiptOcrReviewStatus:
      type: string
      description: Minimal provisional review state for client/on-device or user-reviewed receipt OCR data.
      enum:
        - provisional
        - reviewed
    ReceiptOcrReviewSource:
      type: string
      description: Bounded source category for submitted receipt OCR review data.
      enum:
        - on_device
        - manual_entry
        - imported_reviewed_data
    ReceiptOcrCandidateAmount:
      type: string
      pattern: "^(0|[1-9][0-9]*)(\\.[0-9]{1,4})?$"
      maxLength: 22
      description: Decimal-safe non-negative candidate amount represented as a string. Exponent notation, locale formatting, symbols, and floating-point JSON numbers are not accepted.
      examples:
        - "12.34"
    ReceiptOcrReviewLineRequest:
      type: object
      description: One bounded OCR review line. Line text is reviewed/candidate text, not raw OCR full text. Amounts use the review-level currency and remain provisional.
      additionalProperties: false
      required:
        - text
      properties:
        text:
          type: string
          minLength: 1
          maxLength: 240
          description: Reviewed or candidate receipt line text.
        quantity:
          oneOf:
            - type: string
              pattern: "^(?=.*[1-9])(?:0|[0-9]+)(?:\\.[0-9]{1,4})?$"
              maxLength: 22
            - type: "null"
          description: Optional positive decimal quantity candidate represented as a string.
        unitPriceAmount:
          oneOf:
            - $ref: "#/components/schemas/ReceiptOcrCandidateAmount"
            - type: "null"
          description: Optional non-negative unit price candidate represented as a string.
        lineTotalAmount:
          oneOf:
            - $ref: "#/components/schemas/ReceiptOcrCandidateAmount"
            - type: "null"
          description: Optional non-negative line total candidate represented as a string.
    ReceiptOcrReviewUpsertRequest:
      type: object
      description: Purpose-specific review/intake payload for one existing bill receipt attachment. It stores provisional OCR-derived or manually reviewed candidate fields only and must not include raw OCR full text, file bytes, storage/provider fields, auth/session fields, bill item mutation instructions, split allocation inputs, settlement data, payment details, or unrelated user data.
      additionalProperties: false
      required:
        - status
        - source
      properties:
        status:
          $ref: "#/components/schemas/ReceiptOcrReviewStatus"
        source:
          $ref: "#/components/schemas/ReceiptOcrReviewSource"
        merchantText:
          oneOf:
            - type: string
              minLength: 1
              maxLength: 200
            - type: "null"
          description: Optional reviewed or candidate merchant text. It is bounded review data, not raw OCR full text.
        receiptIssuedAtUtc:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
          description: Optional candidate receipt date/time.
        currency:
          oneOf:
            - $ref: "#/components/schemas/CurrencyCode"
            - type: "null"
          description: Optional review-level currency. Required when any amount candidate is supplied.
        subtotalAmount:
          oneOf:
            - $ref: "#/components/schemas/ReceiptOcrCandidateAmount"
            - type: "null"
        taxAmount:
          oneOf:
            - $ref: "#/components/schemas/ReceiptOcrCandidateAmount"
            - type: "null"
        serviceChargeAmount:
          oneOf:
            - $ref: "#/components/schemas/ReceiptOcrCandidateAmount"
            - type: "null"
        discountAmount:
          oneOf:
            - $ref: "#/components/schemas/ReceiptOcrCandidateAmount"
            - type: "null"
          description: Optional non-negative discount magnitude candidate. It is not a signed authoritative adjustment.
        grandTotalAmount:
          oneOf:
            - $ref: "#/components/schemas/ReceiptOcrCandidateAmount"
            - type: "null"
        lines:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/ReceiptOcrReviewLineRequest"
          description: Optional bounded review lines. The server derives stable line order from array order.
    ReceiptOcrReviewLineResponse:
      type: object
      description: Safe receipt OCR review line response. It exposes bounded reviewed/candidate fields only and excludes raw OCR full text, file bytes, storage/provider internals, bill item mutation state, payment details, and unrelated users.
      required:
        - id
        - sortOrder
        - text
        - quantity
        - unitPriceAmount
        - lineTotalAmount
        - createdAtUtc
        - updatedAtUtc
      properties:
        id:
          type: string
          format: uuid
        sortOrder:
          type: integer
          minimum: 0
        text:
          type: string
          maxLength: 240
        quantity:
          oneOf:
            - type: string
            - type: "null"
        unitPriceAmount:
          oneOf:
            - type: string
            - type: "null"
        lineTotalAmount:
          oneOf:
            - type: string
            - type: "null"
        createdAtUtc:
          type: string
          format: date-time
        updatedAtUtc:
          type: string
          format: date-time
    ReceiptOcrReviewListResponse:
      type: object
      description: Safe receipt OCR review queue list. It contains compact navigation summaries only and excludes raw OCR full text, receipt bytes, storage object keys, provider paths, signed URLs, filenames, auth/session data, payment details, raw audit metadata, bill item/split/settlement/balance state, and unrelated users.
      required:
        - reviews
      properties:
        reviews:
          type: array
          items:
            $ref: "#/components/schemas/ReceiptOcrReviewSummaryResponse"
    ReceiptOcrReviewSummaryResponse:
      type: object
      description: Compact safe receipt OCR review summary for review queue navigation. It exposes stable review, bill, group, and file IDs plus bounded review status/source, optional reviewed merchant text/currency, line count, and timestamps only.
      required:
        - reviewId
        - billId
        - groupId
        - fileId
        - status
        - source
        - merchantText
        - currency
        - lineCount
        - createdAtUtc
        - updatedAtUtc
      properties:
        reviewId:
          type: string
          format: uuid
          description: Stable receipt OCR review ID.
        billId:
          type: string
          format: uuid
          description: Expense bill ID that owns the receipt attachment.
        groupId:
          oneOf:
            - type: string
              format: uuid
            - type: "null"
          description: Owning group ID for group bills, or null for personal bills.
        fileId:
          type: string
          format: uuid
          description: Stable file metadata ID of the reviewed receipt attachment.
        status:
          $ref: "#/components/schemas/ReceiptOcrReviewStatus"
        source:
          $ref: "#/components/schemas/ReceiptOcrReviewSource"
        merchantText:
          oneOf:
            - type: string
              maxLength: 200
            - type: "null"
          description: Optional reviewed or candidate merchant text. It is bounded review data, not raw OCR full text.
        currency:
          oneOf:
            - $ref: "#/components/schemas/CurrencyCode"
            - type: "null"
        lineCount:
          type: integer
          minimum: 0
          maximum: 100
          description: Number of bounded reviewed/candidate line rows linked to the review.
        createdAtUtc:
          type: string
          format: date-time
        updatedAtUtc:
          type: string
          format: date-time
    ReceiptOcrReviewResponse:
      type: object
      description: Safe receipt OCR review response for one bill receipt attachment. It exposes stable bill/file/review IDs and bounded reviewed candidate fields only. It excludes raw OCR full text, receipt bytes, storage object keys, provider paths, signed URLs, auth/session data, payment details, raw audit metadata, bill split/settlement/balance state, and unrelated users.
      required:
        - id
        - billId
        - fileId
        - groupId
        - status
        - source
        - merchantText
        - receiptIssuedAtUtc
        - currency
        - subtotalAmount
        - taxAmount
        - serviceChargeAmount
        - discountAmount
        - grandTotalAmount
        - lines
        - createdAtUtc
        - updatedAtUtc
      properties:
        id:
          type: string
          format: uuid
          description: Stable receipt OCR review ID.
        billId:
          type: string
          format: uuid
          description: Expense bill ID that owns the receipt attachment.
        fileId:
          type: string
          format: uuid
          description: Stable file metadata ID of the reviewed receipt attachment.
        groupId:
          oneOf:
            - type: string
              format: uuid
            - type: "null"
          description: Owning group ID for group bills, or null for personal bills.
        status:
          $ref: "#/components/schemas/ReceiptOcrReviewStatus"
        source:
          $ref: "#/components/schemas/ReceiptOcrReviewSource"
        merchantText:
          oneOf:
            - type: string
              maxLength: 200
            - type: "null"
        receiptIssuedAtUtc:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        currency:
          oneOf:
            - $ref: "#/components/schemas/CurrencyCode"
            - type: "null"
        subtotalAmount:
          oneOf:
            - type: string
            - type: "null"
        taxAmount:
          oneOf:
            - type: string
            - type: "null"
        serviceChargeAmount:
          oneOf:
            - type: string
            - type: "null"
        discountAmount:
          oneOf:
            - type: string
            - type: "null"
        grandTotalAmount:
          oneOf:
            - type: string
            - type: "null"
        lines:
          type: array
          items:
            $ref: "#/components/schemas/ReceiptOcrReviewLineResponse"
        createdAtUtc:
          type: string
          format: date-time
        updatedAtUtc:
          type: string
          format: date-time
    ReceiptOcrReviewApplyPreviewIssueCode:
      type: string
      description: Bounded receipt OCR apply-preview validation issue code. Codes in blockedReasons make canApply false; codes in warnings may be informational or blocking.
      enum:
        - unsupported_review_status
        - unsupported_review_source
        - missing_currency
        - unsupported_currency
        - currency_mismatch
        - missing_grand_total
        - empty_line_set
        - line_total_missing
        - unsupported_line_state
        - line_total_mismatch
        - line_sum_mismatch
        - header_total_mismatch
    ReceiptOcrReviewApplyPreviewLineCandidateResponse:
      type: object
      description: Safe proposed bill-item candidate derived from one bounded receipt OCR review line. It excludes bill-item IDs, split allocation input, raw OCR text, file bytes, storage/provider internals, payment details, and unrelated users.
      required:
        - reviewLineId
        - sortOrder
        - text
        - quantity
        - unitPriceAmount
        - lineTotalAmount
        - proposedLineTotalAmount
      properties:
        reviewLineId:
          type: string
          format: uuid
          description: Stable OCR review line ID used only for UI correlation within this preview.
        sortOrder:
          type: integer
          minimum: 0
        text:
          type: string
          maxLength: 240
        quantity:
          oneOf:
            - type: string
            - type: "null"
        unitPriceAmount:
          oneOf:
            - type: string
            - type: "null"
        lineTotalAmount:
          oneOf:
            - type: string
            - type: "null"
          description: Stored OCR review line total candidate when present.
        proposedLineTotalAmount:
          oneOf:
            - type: string
            - type: "null"
          description: Decimal-string line total the preview can safely propose, using the stored line total when present or a quantity times unit-price derivation when possible.
    ReceiptOcrReviewApplyPreviewSummaryResponse:
      type: object
      description: Decimal-string and count summary derived from the saved OCR review. The summary is preview data only and is not authoritative bill, split, settlement, payment, or balance state.
      required:
        - lineCount
        - linesWithProposedTotalCount
        - linesMissingProposedTotalCount
        - proposedLineTotalSumAmount
        - expectedHeaderTotalAmount
      properties:
        lineCount:
          type: integer
          minimum: 0
          maximum: 100
        linesWithProposedTotalCount:
          type: integer
          minimum: 0
          maximum: 100
        linesMissingProposedTotalCount:
          type: integer
          minimum: 0
          maximum: 100
        proposedLineTotalSumAmount:
          oneOf:
            - type: string
            - type: "null"
          description: Sum of safely proposed line totals when at least one line total can be proposed.
        expectedHeaderTotalAmount:
          oneOf:
            - type: string
            - type: "null"
          description: Header total derived as subtotal plus tax plus service charge minus discount when subtotal is available.
    ReceiptOcrReviewApplyPreviewResponse:
      type: object
      description: Safe non-mutating bill-draft preview derived from one saved receipt OCR review. It exposes only proposed header fields, proposed line candidates, decimal-string summary totals, canApply, blockedReasons, warnings, and stable review/bill/file IDs. It excludes raw OCR full text, receipt bytes, storage object keys, provider paths, signed URLs, filenames, auth/session data, payment details, raw audit metadata, bill split/settlement/payment/balance internals, worker state, and unrelated users.
      required:
        - reviewId
        - billId
        - groupId
        - fileId
        - status
        - source
        - proposedMerchantText
        - proposedReceiptIssuedAtUtc
        - proposedCurrency
        - proposedSubtotalAmount
        - proposedTaxAmount
        - proposedServiceChargeAmount
        - proposedDiscountAmount
        - proposedGrandTotalAmount
        - proposedLines
        - summary
        - canApply
        - blockedReasons
        - warnings
        - createdAtUtc
        - updatedAtUtc
      properties:
        reviewId:
          type: string
          format: uuid
          description: Stable receipt OCR review ID.
        billId:
          type: string
          format: uuid
          description: Expense bill ID that owns the receipt attachment.
        groupId:
          oneOf:
            - type: string
              format: uuid
            - type: "null"
          description: Owning group ID for group bills, or null for personal bills.
        fileId:
          type: string
          format: uuid
          description: Stable file metadata ID of the reviewed receipt attachment.
        status:
          $ref: "#/components/schemas/ReceiptOcrReviewStatus"
        source:
          $ref: "#/components/schemas/ReceiptOcrReviewSource"
        proposedMerchantText:
          oneOf:
            - type: string
              maxLength: 200
            - type: "null"
        proposedReceiptIssuedAtUtc:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        proposedCurrency:
          oneOf:
            - $ref: "#/components/schemas/CurrencyCode"
            - type: "null"
        proposedSubtotalAmount:
          oneOf:
            - type: string
            - type: "null"
        proposedTaxAmount:
          oneOf:
            - type: string
            - type: "null"
        proposedServiceChargeAmount:
          oneOf:
            - type: string
            - type: "null"
        proposedDiscountAmount:
          oneOf:
            - type: string
            - type: "null"
        proposedGrandTotalAmount:
          oneOf:
            - type: string
            - type: "null"
        proposedLines:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/ReceiptOcrReviewApplyPreviewLineCandidateResponse"
        summary:
          $ref: "#/components/schemas/ReceiptOcrReviewApplyPreviewSummaryResponse"
        canApply:
          type: boolean
          description: True only when no blocking preview validation issue was derived.
        blockedReasons:
          type: array
          items:
            $ref: "#/components/schemas/ReceiptOcrReviewApplyPreviewIssueCode"
          description: Blocking validation issue codes that make canApply false.
        warnings:
          type: array
          items:
            $ref: "#/components/schemas/ReceiptOcrReviewApplyPreviewIssueCode"
          description: Bounded validation issue codes for UI confirmation.
        createdAtUtc:
          type: string
          format: date-time
        updatedAtUtc:
          type: string
          format: date-time
    ReceiptOcrReviewApplyMode:
      type: string
      description: Day 1 receipt OCR review apply mode. Only replacement of prior OCR-generated draft item candidates from the same review source is supported.
      enum:
        - replace_draft_ocr_items
    ReceiptOcrReviewApplyRequest:
      type: object
      description: Explicit receipt OCR review apply request. The server derives actor identity, bill/review truth, line candidates, money totals, split safety, attachment/file visibility, and audit metadata; clients submit only the mode and expected saved-review timestamp for stale-apply prevention.
      additionalProperties: false
      required:
        - applyMode
        - expectedReviewUpdatedAtUtc
      properties:
        applyMode:
          $ref: "#/components/schemas/ReceiptOcrReviewApplyMode"
        expectedReviewUpdatedAtUtc:
          type: string
          format: date-time
          description: The saved review updatedAtUtc value the user previewed or intended to apply.
    ReceiptOcrReviewApplyResponse:
      type: object
      description: Safe receipt OCR review apply result. It exposes bounded IDs, mode, applied item count, currency, reviewed subtotal/grand-total summary, validation issue code arrays, and apply timestamp only. It excludes raw OCR full text, receipt bytes, storage object keys, provider paths, signed URLs, filenames, auth/session data, payment details, raw audit metadata, bill split internals, settlement/payment/balance/residual/proof state, worker state, and unrelated users.
      required:
        - reviewId
        - billId
        - groupId
        - fileId
        - applyMode
        - appliedItemCount
        - currency
        - subtotalAmount
        - grandTotalAmount
        - summary
        - blockedReasons
        - warnings
        - appliedAtUtc
      properties:
        reviewId:
          type: string
          format: uuid
        billId:
          type: string
          format: uuid
        groupId:
          oneOf:
            - type: string
              format: uuid
            - type: "null"
        fileId:
          type: string
          format: uuid
        applyMode:
          $ref: "#/components/schemas/ReceiptOcrReviewApplyMode"
        appliedItemCount:
          type: integer
          minimum: 0
          maximum: 100
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        subtotalAmount:
          oneOf:
            - type: string
            - type: "null"
        grandTotalAmount:
          oneOf:
            - type: string
            - type: "null"
        summary:
          $ref: "#/components/schemas/ReceiptOcrReviewApplyPreviewSummaryResponse"
        blockedReasons:
          type: array
          items:
            $ref: "#/components/schemas/ReceiptOcrReviewApplyPreviewIssueCode"
        warnings:
          type: array
          items:
            $ref: "#/components/schemas/ReceiptOcrReviewApplyPreviewIssueCode"
        appliedAtUtc:
          type: string
          format: date-time
    SettlementPaymentProofListResponse:
      type: object
      description: Safe settlement payment proof metadata list. It excludes proof bytes, storage object keys, storage paths, provider URLs, direct URLs, vault references, original filenames, auth/session data, payment handles, payment notes, bill merchant/item details, raw audit metadata, and unrelated users.
      required:
        - proofs
      properties:
        proofs:
          type: array
          items:
            $ref: "#/components/schemas/SettlementPaymentProofResponse"
    SettlementPaymentProofResponse:
      type: object
      description: Safe settlement payment proof file metadata. It exposes stable file and payment IDs plus content metadata only, and never contains storage object keys, storage paths, provider URLs, direct URLs, vault references, original filenames, bytes, thumbnails, OCR text, or raw audit metadata.
      required:
        - fileId
        - settlementPaymentId
        - contentType
        - sizeBytes
        - uploadedAtUtc
        - updatedAtUtc
      properties:
        fileId:
          type: string
          format: uuid
          description: Stable file metadata identifier for the proof attachment.
        settlementPaymentId:
          type: string
          format: uuid
          description: Settlement payment claim ID that owns this proof association.
        contentType:
          type: string
          enum:
            - image/png
            - image/jpeg
            - image/webp
            - application/pdf
        sizeBytes:
          type: integer
          format: int64
          minimum: 1
          maximum: 5242880
        uploadedAtUtc:
          type: string
          format: date-time
          description: Timestamp when the proof association was created.
        updatedAtUtc:
          type: string
          format: date-time
          description: Timestamp when the underlying file metadata was last updated.
    AttachSettlementPaymentProofRequest:
      type: object
      description: Multipart form used to upload one settlement payment proof file. The filename is treated as submitted transport metadata only and is not persisted into proof responses or audit metadata.
      additionalProperties: false
      required:
        - file
      properties:
        file:
          type: string
          format: binary
          maxLength: 5242880
          description: Settlement proof file. Allowed content types are image/png, image/jpeg, image/webp, and application/pdf; the maximum accepted file size is 5 MB.
    CreateGroupBillRequest:
      type: object
      description: Minimal group bill creation request. Creator/current actor are derived server-side; the route groupId is authoritative and clients cannot submit creator, owner, auth account, session, group override, file, or authorization identity fields.
      additionalProperties: false
      required:
        - billDate
        - currency
        - items
      properties:
        merchantName:
          oneOf:
            - type: string
              maxLength: 200
            - type: "null"
          description: Optional merchant name after server-side trimming.
        billDate:
          type: string
          format: date
          description: Bill date as yyyy-MM-dd.
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        items:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/CreateGroupBillItemRequest"
        adjustments:
          oneOf:
            - type: array
              items:
                $ref: "#/components/schemas/CreateGroupBillAdjustmentRequest"
            - type: "null"
          description: Optional adjustments. Manual allocation is intentionally excluded from this first group bill endpoint slice.
        payers:
          oneOf:
            - type: array
              items:
                $ref: "#/components/schemas/CreateGroupBillPayerRequest"
            - type: "null"
          description: Optional payer rows. When omitted, null, or empty, the authenticated actor is the sole payer for the calculated bill total.
    CreateGroupBillItemRequest:
      type: object
      description: Group bill item. Splits are required and every split userProfileId must belong to an active member of the route group.
      additionalProperties: false
      required:
        - name
        - amount
        - splits
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 240
          description: Item name after server-side trimming.
        note:
          oneOf:
            - type: string
              maxLength: 1000
            - type: "null"
          description: Optional item note after server-side trimming.
        amount:
          type: string
          description: Decimal-safe item amount represented as a string.
          examples:
            - "12.34"
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        splits:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/CreateGroupBillItemSplitRequest"
    CreateGroupBillItemSplitRequest:
      type: object
      description: Group bill item split. exact_amount, percentage, ratio, and share_weight require basisValue; equal split does not.
      additionalProperties: false
      required:
        - userProfileId
        - splitMethod
      properties:
        userProfileId:
          type: string
          format: uuid
          description: User profile ID for an active member of the route group.
        splitMethod:
          $ref: "#/components/schemas/ExpenseBillItemSplitMethod"
        basisValue:
          oneOf:
            - type: string
            - type: "null"
          description: Decimal-safe split basis represented as a string where required by splitMethod.
        allocationOrder:
          type: integer
          minimum: 0
          description: Optional allocation order. When omitted, input order is used.
    CreateGroupBillAdjustmentRequest:
      type: object
      description: Optional group bill adjustment. Manual allocation is intentionally excluded from this first endpoint slice.
      additionalProperties: false
      required:
        - type
        - direction
        - allocationMethod
        - amount
      properties:
        type:
          $ref: "#/components/schemas/ExpenseBillAdjustmentType"
        direction:
          $ref: "#/components/schemas/ExpenseBillAdjustmentDirection"
        allocationMethod:
          $ref: "#/components/schemas/GroupBillAdjustmentAllocationMethod"
        amount:
          type: string
          description: Decimal-safe adjustment amount represented as a string.
          examples:
            - "1.23"
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        reasonNote:
          oneOf:
            - type: string
              maxLength: 1000
            - type: "null"
          description: Optional adjustment reason note after server-side trimming.
    CreateGroupBillPayerRequest:
      type: object
      description: Optional group bill payer row. Every userProfileId must belong to an active member of the route group, and supplied payer totals must match the calculated bill total.
      additionalProperties: false
      required:
        - userProfileId
        - amount
      properties:
        userProfileId:
          type: string
          format: uuid
          description: User profile ID for an active member of the route group.
        amount:
          type: string
          description: Decimal-safe payer amount represented as a string.
          examples:
            - "12.34"
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        paymentMethodLabelSnapshot:
          oneOf:
            - type: string
              maxLength: 120
            - type: "null"
          description: Optional payment method label snapshot after server-side trimming.
    GroupBillListResponse:
      type: object
      description: Group bills visible to the authenticated active group member. This first foundation response is intentionally unpaginated.
      required:
        - bills
      properties:
        bills:
          type: array
          items:
            $ref: "#/components/schemas/GroupBillResponse"
    GroupBillResponse:
      type: object
      description: Safe group bill response for the authenticated active group member. It excludes auth/session/credential data, group membership internals beyond submitted bill participants, storage paths, object keys, provider internals, audit metadata, OCR data, and unrelated user data. Adjustment allocations are calculated response data because no adjustment-allocation persistence table exists in the current schema.
      required:
        - id
        - groupId
        - merchantName
        - billDate
        - status
        - totalAmount
        - totalCurrency
        - createdAtUtc
        - updatedAtUtc
        - items
        - participants
        - payers
        - adjustments
        - calculatedAdjustmentAllocations
      properties:
        id:
          type: string
          format: uuid
        groupId:
          type: string
          format: uuid
        merchantName:
          oneOf:
            - type: string
              maxLength: 200
            - type: "null"
        billDate:
          type: string
          format: date
        status:
          $ref: "#/components/schemas/ExpenseBillStatus"
        totalAmount:
          type: string
          description: Decimal-safe total amount represented as a string.
        totalCurrency:
          $ref: "#/components/schemas/CurrencyCode"
        createdAtUtc:
          type: string
          format: date-time
        updatedAtUtc:
          type: string
          format: date-time
        items:
          type: array
          items:
            $ref: "#/components/schemas/GroupBillItemResponse"
        participants:
          type: array
          items:
            $ref: "#/components/schemas/GroupBillParticipantResponse"
        payers:
          type: array
          items:
            $ref: "#/components/schemas/GroupBillPayerResponse"
        adjustments:
          type: array
          items:
            $ref: "#/components/schemas/GroupBillAdjustmentResponse"
        calculatedAdjustmentAllocations:
          type: array
          description: Calculated adjustment allocation data returned for display/auditability. It is not backed by a dedicated persistence table in this schema slice.
          items:
            $ref: "#/components/schemas/GroupBillCalculatedAdjustmentAllocationResponse"
    GroupBillItemResponse:
      type: object
      required:
        - id
        - name
        - note
        - amount
        - currency
        - sortOrder
        - splits
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          minLength: 1
          maxLength: 240
        note:
          oneOf:
            - type: string
              maxLength: 1000
            - type: "null"
        amount:
          type: string
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        sortOrder:
          type: integer
          minimum: 0
        splits:
          type: array
          items:
            $ref: "#/components/schemas/GroupBillItemSplitResponse"
    GroupBillItemSplitResponse:
      type: object
      required:
        - userProfileId
        - splitMethod
        - basisValue
        - resolvedAmount
        - resolvedCurrency
        - allocationOrder
        - receivedResidualMinorUnit
      properties:
        userProfileId:
          type: string
          format: uuid
        splitMethod:
          $ref: "#/components/schemas/ExpenseBillItemSplitMethod"
        basisValue:
          oneOf:
            - type: string
            - type: "null"
        resolvedAmount:
          type: string
        resolvedCurrency:
          $ref: "#/components/schemas/CurrencyCode"
        allocationOrder:
          type: integer
          minimum: 0
        receivedResidualMinorUnit:
          type: boolean
    GroupBillParticipantResponse:
      type: object
      required:
        - userProfileId
        - status
        - resolvedShareAmount
        - resolvedShareCurrency
        - rejectionReasonCode
      properties:
        userProfileId:
          type: string
          format: uuid
        status:
          $ref: "#/components/schemas/ExpenseBillParticipantStatus"
        resolvedShareAmount:
          type: string
        resolvedShareCurrency:
          $ref: "#/components/schemas/CurrencyCode"
        rejectionReasonCode:
          oneOf:
            - $ref: "#/components/schemas/ExpenseBillParticipantRejectionReasonCode"
            - type: "null"
          description: Bounded rejection reason code when the participant rejected the bill; null otherwise.
    GroupBillPayerResponse:
      type: object
      required:
        - userProfileId
        - amount
        - currency
        - paymentMethodLabelSnapshot
      properties:
        userProfileId:
          type: string
          format: uuid
        amount:
          type: string
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        paymentMethodLabelSnapshot:
          oneOf:
            - type: string
              maxLength: 120
            - type: "null"
    GroupBillAdjustmentResponse:
      type: object
      required:
        - id
        - type
        - direction
        - allocationMethod
        - amount
        - currency
        - reasonNote
        - sortOrder
      properties:
        id:
          type: string
          format: uuid
        type:
          $ref: "#/components/schemas/ExpenseBillAdjustmentType"
        direction:
          $ref: "#/components/schemas/ExpenseBillAdjustmentDirection"
        allocationMethod:
          $ref: "#/components/schemas/GroupBillAdjustmentAllocationMethod"
        amount:
          type: string
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        reasonNote:
          oneOf:
            - type: string
              maxLength: 1000
            - type: "null"
        sortOrder:
          type: integer
          minimum: 0
    GroupBillCalculatedAdjustmentAllocationResponse:
      type: object
      required:
        - expenseBillAdjustmentId
        - userProfileId
        - direction
        - allocationMethod
        - allocatedAmount
        - currency
        - allocationOrder
        - receivedResidualMinorUnit
      properties:
        expenseBillAdjustmentId:
          type: string
          format: uuid
        userProfileId:
          type: string
          format: uuid
        direction:
          $ref: "#/components/schemas/ExpenseBillAdjustmentDirection"
        allocationMethod:
          $ref: "#/components/schemas/GroupBillAdjustmentAllocationMethod"
        allocatedAmount:
          type: string
        currency:
          $ref: "#/components/schemas/CurrencyCode"
        allocationOrder:
          type: integer
          minimum: 0
        receivedResidualMinorUnit:
          type: boolean
    ExpenseBillStatus:
      type: string
      description: Expense bill root status.
      enum:
        - draft
        - pending_confirmation
        - confirmed
        - rejected
        - cancelled
        - finalized
        - archived
    ExpenseBillParticipantStatus:
      type: string
      description: Expense bill participant status.
      enum:
        - pending_acceptance
        - accepted
        - rejected
        - partially_settled
        - settled
        - waived
        - claimed_paid
        - confirmed_paid
    ExpenseBillRevisionStatus:
      type: string
      description: Bill revision proposal lifecycle status.
      enum:
        - draft_revision
        - submitted_for_review
        - withdrawn_by_proposer
        - superseded_by_resubmission
        - rejected
        - accepted_applied
        - cancelled_by_authorized_editor
    ExpenseBillRevisionApprovalStatus:
      type: string
      description: Bill revision approval status.
      enum:
        - pending_review
        - approved
        - rejected
        - invalidated_by_supersession
    ExpenseBillPayerConfirmationStatus:
      type: string
      description: Payer confirmation status for original bill payer facts and proposed revision payer facts.
      enum:
        - pending_confirmation
        - confirmed
        - rejected
    ExpenseBillParticipantRejectionReasonCode:
      type: string
      description: Bounded participant rejection reason code.
      enum:
        - wrong_amount
        - wrong_items
        - wrong_split
        - duplicate
        - not_mine
        - other
    ExpenseBillItemSplitMethod:
      type: string
      description: Expense bill item split method.
      enum:
        - equal
        - exact_amount
        - percentage
        - ratio
        - share_weight
    ExpenseBillAdjustmentType:
      type: string
      description: Expense bill adjustment type.
      enum:
        - tax
        - service_charge
        - discount
        - manual_adjustment
        - credit
    ExpenseBillAdjustmentDirection:
      type: string
      description: Expense bill adjustment direction.
      enum:
        - charge
        - credit
    PersonalBillAdjustmentAllocationMethod:
      type: string
      description: Supported adjustment allocation methods for the first personal bill endpoint slice.
      enum:
        - equal
        - proportional_by_item_subtotal
    GroupBillAdjustmentAllocationMethod:
      type: string
      description: Supported non-manual adjustment allocation methods for the first group bill endpoint slice.
      enum:
        - equal
        - proportional_by_item_subtotal
    SettlementRequestStatus:
      type: string
      description: Settlement request status returned by settlement request surfaces.
      enum:
        - requested
        - partially_paid
        - marked_paid
        - confirmed
        - disputed
        - cancelled
    SettlementRequestLineStatus:
      type: string
      description: Settlement request-line status returned by settlement request surfaces.
      enum:
        - open
        - partially_cleared
        - cleared
        - waived
        - disputed
        - cancelled
    SettlementBalanceDirection:
      type: string
      description: Direction of a settlement balance projection from the authenticated actor's perspective.
      enum:
        - incoming
        - outgoing
    SettlementBasketSelectionMode:
      type: string
      description: Supported settlement basket selection mode for the first preview and create slices.
      enum:
        - pay_all_outstanding_for_counterparty
    SettlementPaymentStatus:
      type: string
      description: Settlement payment status returned by settlement payment surfaces. Day 1 payment claim creates marked_paid payments, receiver confirmation moves them to confirmed, receiver dispute moves eligible marked_paid claims to disputed, and debtor cancellation moves eligible own marked_paid claims to cancelled.
      enum:
        - marked_paid
        - confirmed
        - disputed
        - cancelled
    SettlementResidualDirection:
      type: string
      description: Direction of a proposed settlement residual relative to the exact selected outstanding total.
      enum:
        - underpayment
        - overpayment
    SettlementResidualPolicy:
      type: string
      description: Supported residual policy values for explicit payment-claim-time residual proposals.
      enum:
        - remaining_balance
        - carried_forward
        - waived
        - credit_forward
        - waived_by_payer
        - applied_to_other_line
    SettlementResidualStatus:
      type: string
      description: Settlement residual status returned by payment response summaries. Payment claims create pending residuals, receiver residual confirmation moves supported pending residuals to policy-derived confirmed, carried_forward, waived, or credited statuses, and dispute/cancellation neutralizes pending residuals to disputed or cancelled.
      enum:
        - pending_receiver_confirmation
        - confirmed
        - carried_forward
        - waived
        - credited
        - disputed
        - cancelled
    PaymentDetailsVisibility:
      type: string
      description: Payment details visibility policy value. The default app behavior is settlement_counterparties_only.
      enum:
        - private
        - settlement_counterparties_only
        - group_members_when_shared
    SelfPaymentDetailsResponse:
      type: object
      description: Safe self payment-details response for the authenticated actor. It excludes auth account IDs, session IDs, credential state, audit metadata, counterparty data, storage object keys, storage paths, provider URLs, vault internals, and submitted filenames.
      required:
        - isConfigured
        - id
        - preferredMethodLabel
        - paymentHandle
        - paymentNote
        - visibility
        - qrFile
        - createdAtUtc
        - updatedAtUtc
      properties:
        isConfigured:
          type: boolean
          description: True only when an active payment profile row exists for the authenticated actor.
        id:
          oneOf:
            - type: string
              format: uuid
            - type: "null"
        preferredMethodLabel:
          oneOf:
            - type: string
              minLength: 1
              maxLength: 120
            - type: "null"
        paymentHandle:
          oneOf:
            - type: string
              minLength: 1
              maxLength: 320
            - type: "null"
        paymentNote:
          oneOf:
            - type: string
              minLength: 1
              maxLength: 1000
            - type: "null"
        visibility:
          $ref: "#/components/schemas/PaymentDetailsVisibility"
        qrFile:
          oneOf:
            - $ref: "#/components/schemas/SelfPaymentDetailsQrFileResponse"
            - type: "null"
          description: Safe metadata for the active linked payment QR file, or null when no active QR is linked.
        createdAtUtc:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        updatedAtUtc:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
    SelfPaymentDetailsQrFileResponse:
      type: object
      description: Safe self payment QR file metadata. It never contains storage object keys, storage paths, provider URLs, vault references, bytes, thumbnails, direct URLs, or submitted filenames.
      required:
        - id
        - contentType
        - sizeBytes
        - updatedAtUtc
      properties:
        id:
          type: string
          format: uuid
          description: Stable file metadata identifier for the linked QR file.
        contentType:
          type: string
          enum:
            - image/png
            - image/jpeg
            - image/webp
        sizeBytes:
          type: integer
          format: int64
          minimum: 1
          maximum: 2097152
        updatedAtUtc:
          type: string
          format: date-time
    SettlementCounterpartyPaymentDetailsResponse:
      type: object
      description: Safe settlement-scoped counterparty payment-details response. It is not the self owner response and excludes payment profile IDs, owner-only timestamps, auth/session/credential data, audit internals, storage object keys, storage paths, provider URLs, vault internals, QR bytes, direct URLs, filenames, and unrelated users.
      required:
        - userProfileId
        - isConfigured
        - preferredMethodLabel
        - paymentHandle
        - paymentNote
        - visibilityApplied
        - qrFile
      properties:
        userProfileId:
          type: string
          format: uuid
          description: Target counterparty user profile ID proven through the settlement request relationship.
        isConfigured:
          type: boolean
          description: True only when an active target payment profile is visible through the settlement counterparty relationship.
        preferredMethodLabel:
          oneOf:
            - type: string
              minLength: 1
              maxLength: 120
            - type: "null"
          description: Visibility-scoped payment method label, or null when not configured or not visible.
        paymentHandle:
          oneOf:
            - type: string
              minLength: 1
              maxLength: 320
            - type: "null"
          description: Visibility-scoped payment handle, or null when not configured or not visible.
        paymentNote:
          oneOf:
            - type: string
              minLength: 1
              maxLength: 1000
            - type: "null"
          description: Visibility-scoped payment note, or null when not configured or not visible.
        visibilityApplied:
          $ref: "#/components/schemas/PaymentDetailsVisibility"
        qrFile:
          oneOf:
            - $ref: "#/components/schemas/SettlementCounterpartyPaymentDetailsQrFileResponse"
            - type: "null"
          description: Safe metadata for the active linked payment QR file when visible through the settlement counterparty relationship, or null.
    SettlementCounterpartyPaymentDetailsQrFileResponse:
      type: object
      description: Safe counterparty payment QR file metadata for a settlement-scoped relationship. It never contains storage object keys, storage paths, provider URLs, vault references, bytes, thumbnails, direct URLs, or submitted filenames.
      required:
        - id
        - contentType
        - sizeBytes
        - updatedAtUtc
      properties:
        id:
          type: string
          format: uuid
          description: Stable file metadata identifier for the linked QR file.
        contentType:
          type: string
          enum:
            - image/png
            - image/jpeg
            - image/webp
        sizeBytes:
          type: integer
          format: int64
          minimum: 1
          maximum: 2097152
        updatedAtUtc:
          type: string
          format: date-time
    AttachSelfPaymentQrRequest:
      type: object
      description: Multipart form used to upload a single self payment QR image. The filename is treated as submitted transport metadata only and is not returned by the API.
      additionalProperties: false
      required:
        - file
      properties:
        file:
          type: string
          format: binary
          description: Payment QR image. Allowed content types are image/png, image/jpeg, and image/webp; the maximum accepted file size is 2 MB.
    UpdateSelfPaymentDetailsRequest:
      type: object
      description: Safe self payment-details update. Missing text fields are preserved on update, explicit null clears them, whitespace-only text normalizes to null, and omitted visibility defaults only when creating a new payment profile.
      additionalProperties: false
      minProperties: 1
      properties:
        preferredMethodLabel:
          oneOf:
            - type: string
              maxLength: 120
            - type: "null"
          description: Optional payment method label after server-side trimming.
        paymentHandle:
          oneOf:
            - type: string
              maxLength: 320
            - type: "null"
          description: Optional user-entered payment handle after server-side trimming.
        paymentNote:
          oneOf:
            - type: string
              maxLength: 1000
            - type: "null"
          description: Optional user-entered payment note after server-side trimming.
        visibility:
          $ref: "#/components/schemas/PaymentDetailsVisibility"
    SelfUserProfileResponse:
      type: object
      description: Safe self-profile response for the authenticated actor.
      required:
        - id
        - displayName
        - defaultCurrency
        - createdAtUtc
        - updatedAtUtc
      properties:
        id:
          type: string
          format: uuid
        displayName:
          type: string
          minLength: 1
          maxLength: 160
        defaultCurrency:
          oneOf:
            - $ref: "#/components/schemas/CurrencyCode"
            - type: "null"
        createdAtUtc:
          type: string
          format: date-time
        updatedAtUtc:
          type: string
          format: date-time
    UpdateSelfUserProfileRequest:
      type: object
      description: Safe self-profile update. Profile identity is derived from the authenticated actor, not from client input.
      additionalProperties: false
      minProperties: 1
      properties:
        displayName:
          type: string
          minLength: 1
          maxLength: 160
          description: Display name after server-side trimming. Blank or whitespace-only values are rejected.
        defaultCurrency:
          oneOf:
            - $ref: "#/components/schemas/CurrencyCode"
            - type: "null"
          description: Optional default currency. Explicit null clears the value.
    SessionListResponse:
      type: object
      description: Safe active session metadata for the authenticated account. The list is capped by the API.
      required:
        - sessions
      properties:
        sessions:
          type: array
          maxItems: 50
          items:
            $ref: "#/components/schemas/SessionSummary"
    SessionSummary:
      type: object
      description: Bounded session metadata that excludes token material, hashes, network data, user-agent data, and revocation details.
      required:
        - id
        - isCurrent
        - status
        - issuedAtUtc
        - expiresAtUtc
        - lastSeenAtUtc
        - deviceLabel
      properties:
        id:
          type: string
          format: uuid
        isCurrent:
          type: boolean
        status:
          type: string
          enum:
            - active
        issuedAtUtc:
          type: string
          format: date-time
        expiresAtUtc:
          type: string
          format: date-time
        lastSeenAtUtc:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        deviceLabel:
          oneOf:
            - type: string
              maxLength: 120
            - type: "null"
    CurrencyCode:
      type: string
      pattern: "^[A-Z]{3}$"
      description: ISO-4217 style currency code.
      examples:
        - USD
    Money:
      type: object
      description: Decimal-safe money value with an attached currency.
      required:
        - amount
        - currency
      properties:
        amount:
          type: string
          description: Decimal-safe amount represented as a string to avoid floating point loss.
          examples:
            - "12.34"
        currency:
          $ref: "#/components/schemas/CurrencyCode"
    RoundingMode:
      type: string
      description: Placeholder rounding modes for future policy-driven money handling.
      enum:
        - up
        - down
        - nearest
        - bankers
    SyncState:
      type: string
      description: Placeholder sync state values shared by local and server-mode clients.
      enum:
        - queued
        - synced
        - conflict
        - failed
    StorageObjectRef:
      type: object
      description: Stable storage object reference that does not expose physical filesystem paths.
      required:
        - id
      properties:
        id:
          type: string
          description: Opaque storage object identifier.
        contentType:
          type: string
        sizeBytes:
          type: integer
          format: int64
          minimum: 0
    ProblemDetails:
      type: object
      description: RFC 7807 / RFC 9457 style problem response.
      properties:
        type:
          type: string
          format: uri-reference
        title:
          type: string
        status:
          type: integer
          minimum: 100
          maximum: 599
        detail:
          type: string
        instance:
          type: string
          format: uri-reference
    OcrJobStatus:
      type: string
      description: Placeholder OCR job status values for future worker contract design.
      enum:
        - queued
        - processing
        - completed
        - failed
