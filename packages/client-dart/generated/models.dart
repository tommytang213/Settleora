// <auto-generated>
// Generated by tools/generate-clients.mjs from packages/contracts/openapi/settleora.v1.yaml.
// Do not edit this file by hand.
// </auto-generated>

typedef JsonObject = Map<String, dynamic>;

/// Local-account sign-in request. Source, network, and policy bucket details are derived server-side.
class LocalSignInRequest {
  const LocalSignInRequest({
    required this.identifier,
    required this.password,
    this.deviceLabel,
  });

  /// Submitted local account identifier.
  final String identifier;
  /// Submitted local account password. The API never stores this plaintext value.
  final String password;
  /// Optional display label for the newly created session.
  final String? deviceLabel;

  factory LocalSignInRequest.fromJson(JsonObject json) {
    return LocalSignInRequest(
      identifier: json["identifier"] as String,
      password: json["password"] as String,
      deviceLabel: json["deviceLabel"] == null ? null : json["deviceLabel"] as String,
    );
  }

  JsonObject toJson() {
    return {
      "identifier": identifier,
      "password": password,
      if (deviceLabel != null) "deviceLabel": deviceLabel,
    };
  }
}

/// Minimal refresh-capable local sign-in success response. Raw credential material is returned only once.
class LocalSignInResponse {
  const LocalSignInResponse({
    required this.session,
    required this.refreshCredential,
  });

  final RefreshSessionAccessSession session;
  final RefreshSessionCredential refreshCredential;

  factory LocalSignInResponse.fromJson(JsonObject json) {
    return LocalSignInResponse(
      session: RefreshSessionAccessSession.fromJson(JsonObject.from(json["session"] as Map)),
      refreshCredential: RefreshSessionCredential.fromJson(JsonObject.from(json["refreshCredential"] as Map)),
    );
  }

  JsonObject toJson() {
    return {
      "session": session.toJson(),
      "refreshCredential": refreshCredential.toJson(),
    };
  }
}

/// Public refresh request. Credential lookup, account, session-family, status, expiry, and revocation policy are resolved server-side.
class RefreshSessionRequest {
  const RefreshSessionRequest({
    required this.refreshCredential,
    this.deviceLabel,
  });

  /// Submitted raw refresh-like credential. The API never stores this raw value.
  final String refreshCredential;
  /// Optional display label for the replacement access session.
  final String? deviceLabel;

  factory RefreshSessionRequest.fromJson(JsonObject json) {
    return RefreshSessionRequest(
      refreshCredential: json["refreshCredential"] as String,
      deviceLabel: json["deviceLabel"] == null ? null : json["deviceLabel"] as String,
    );
  }

  JsonObject toJson() {
    return {
      "refreshCredential": refreshCredential,
      if (deviceLabel != null) "deviceLabel": deviceLabel,
    };
  }
}

/// Minimal refresh success response. Raw credential material is returned only once.
class RefreshSessionResponse {
  const RefreshSessionResponse({
    required this.session,
    required this.refreshCredential,
  });

  final RefreshSessionAccessSession session;
  final RefreshSessionCredential refreshCredential;

  factory RefreshSessionResponse.fromJson(JsonObject json) {
    return RefreshSessionResponse(
      session: RefreshSessionAccessSession.fromJson(JsonObject.from(json["session"] as Map)),
      refreshCredential: RefreshSessionCredential.fromJson(JsonObject.from(json["refreshCredential"] as Map)),
    );
  }

  JsonObject toJson() {
    return {
      "session": session.toJson(),
      "refreshCredential": refreshCredential.toJson(),
    };
  }
}

/// Replacement opaque access session returned from refresh.
class RefreshSessionAccessSession {
  const RefreshSessionAccessSession({
    required this.id,
    required this.token,
    required this.expiresAtUtc,
  });

  final String id;
  /// Opaque raw access-session token returned once on refresh success.
  final String token;
  final DateTime expiresAtUtc;

  factory RefreshSessionAccessSession.fromJson(JsonObject json) {
    return RefreshSessionAccessSession(
      id: json["id"] as String,
      token: json["token"] as String,
      expiresAtUtc: DateTime.parse(json["expiresAtUtc"] as String),
    );
  }

  JsonObject toJson() {
    return {
      "id": id,
      "token": token,
      "expiresAtUtc": expiresAtUtc.toUtc().toIso8601String(),
    };
  }
}

/// Replacement refresh-like credential returned from refresh.
class RefreshSessionCredential {
  const RefreshSessionCredential({
    required this.token,
    required this.idleExpiresAtUtc,
    required this.absoluteExpiresAtUtc,
  });

  /// Raw refresh-like credential returned once on refresh success.
  final String token;
  final DateTime idleExpiresAtUtc;
  final DateTime absoluteExpiresAtUtc;

  factory RefreshSessionCredential.fromJson(JsonObject json) {
    return RefreshSessionCredential(
      token: json["token"] as String,
      idleExpiresAtUtc: DateTime.parse(json["idleExpiresAtUtc"] as String),
      absoluteExpiresAtUtc: DateTime.parse(json["absoluteExpiresAtUtc"] as String),
    );
  }

  JsonObject toJson() {
    return {
      "token": token,
      "idleExpiresAtUtc": idleExpiresAtUtc.toUtc().toIso8601String(),
      "absoluteExpiresAtUtc": absoluteExpiresAtUtc.toUtc().toIso8601String(),
    };
  }
}

/// Minimal current authenticated actor, profile, session, and system-role summary.
class CurrentUserResponse {
  const CurrentUserResponse({
    required this.authAccountId,
    required this.userProfile,
    required this.session,
    required this.roles,
  });

  final String authAccountId;
  final CurrentUserProfile userProfile;
  final CurrentUserSession session;
  final List<String> roles;

  factory CurrentUserResponse.fromJson(JsonObject json) {
    return CurrentUserResponse(
      authAccountId: json["authAccountId"] as String,
      userProfile: CurrentUserProfile.fromJson(JsonObject.from(json["userProfile"] as Map)),
      session: CurrentUserSession.fromJson(JsonObject.from(json["session"] as Map)),
      roles: (json["roles"] as List<dynamic>).map((item) => item as String).toList(growable: false),
    );
  }

  JsonObject toJson() {
    return {
      "authAccountId": authAccountId,
      "userProfile": userProfile.toJson(),
      "session": session.toJson(),
      "roles": roles,
    };
  }
}

/// Linked active user profile summary for the authenticated account.
class CurrentUserProfile {
  const CurrentUserProfile({
    required this.id,
    required this.displayName,
    required this.defaultCurrency,
  });

  final String id;
  final String displayName;
  final CurrencyCode? defaultCurrency;

  factory CurrentUserProfile.fromJson(JsonObject json) {
    return CurrentUserProfile(
      id: json["id"] as String,
      displayName: json["displayName"] as String,
      defaultCurrency: json["defaultCurrency"] == null ? null : json["defaultCurrency"] as String,
    );
  }

  JsonObject toJson() {
    return {
      "id": id,
      "displayName": displayName,
      "defaultCurrency": defaultCurrency,
    };
  }
}

/// Validated current session summary.
class CurrentUserSession {
  const CurrentUserSession({
    required this.id,
    required this.expiresAtUtc,
  });

  final String id;
  final DateTime expiresAtUtc;

  factory CurrentUserSession.fromJson(JsonObject json) {
    return CurrentUserSession(
      id: json["id"] as String,
      expiresAtUtc: DateTime.parse(json["expiresAtUtc"] as String),
    );
  }

  JsonObject toJson() {
    return {
      "id": id,
      "expiresAtUtc": expiresAtUtc.toUtc().toIso8601String(),
    };
  }
}

/// Safe active session metadata for the authenticated account. The list is capped by the API.
class SessionListResponse {
  const SessionListResponse({
    required this.sessions,
  });

  final List<SessionSummary> sessions;

  factory SessionListResponse.fromJson(JsonObject json) {
    return SessionListResponse(
      sessions: (json["sessions"] as List<dynamic>).map((item) => SessionSummary.fromJson(JsonObject.from(item as Map))).toList(growable: false),
    );
  }

  JsonObject toJson() {
    return {
      "sessions": sessions.map((item) => item.toJson()).toList(growable: false),
    };
  }
}

/// Bounded session metadata that excludes token material, hashes, network data, user-agent data, and revocation details.
class SessionSummary {
  const SessionSummary({
    required this.id,
    required this.isCurrent,
    required this.status,
    required this.issuedAtUtc,
    required this.expiresAtUtc,
    required this.lastSeenAtUtc,
    required this.deviceLabel,
  });

  final String id;
  final bool isCurrent;
  final String status;
  final DateTime issuedAtUtc;
  final DateTime expiresAtUtc;
  final DateTime? lastSeenAtUtc;
  final String? deviceLabel;

  factory SessionSummary.fromJson(JsonObject json) {
    return SessionSummary(
      id: json["id"] as String,
      isCurrent: json["isCurrent"] as bool,
      status: json["status"] as String,
      issuedAtUtc: DateTime.parse(json["issuedAtUtc"] as String),
      expiresAtUtc: DateTime.parse(json["expiresAtUtc"] as String),
      lastSeenAtUtc: json["lastSeenAtUtc"] == null ? null : DateTime.parse(json["lastSeenAtUtc"] as String),
      deviceLabel: json["deviceLabel"] == null ? null : json["deviceLabel"] as String,
    );
  }

  JsonObject toJson() {
    return {
      "id": id,
      "isCurrent": isCurrent,
      "status": status,
      "issuedAtUtc": issuedAtUtc.toUtc().toIso8601String(),
      "expiresAtUtc": expiresAtUtc.toUtc().toIso8601String(),
      "lastSeenAtUtc": lastSeenAtUtc == null ? null : lastSeenAtUtc.toUtc().toIso8601String(),
      "deviceLabel": deviceLabel,
    };
  }
}

/// ISO-4217 style currency code.
typedef CurrencyCode = String;

/// Decimal-safe money value with an attached currency.
class Money {
  const Money({
    required this.amount,
    required this.currency,
  });

  /// Decimal-safe amount represented as a string to avoid floating point loss.
  final String amount;
  final CurrencyCode currency;

  factory Money.fromJson(JsonObject json) {
    return Money(
      amount: json["amount"] as String,
      currency: json["currency"] as String,
    );
  }

  JsonObject toJson() {
    return {
      "amount": amount,
      "currency": currency,
    };
  }
}

/// Placeholder rounding modes for future policy-driven money handling.
typedef RoundingMode = String;
class RoundingModeValues {
  const RoundingModeValues._();
  static const RoundingMode up = "up";
  static const RoundingMode down = "down";
  static const RoundingMode nearest = "nearest";
  static const RoundingMode bankers = "bankers";
  static const Set<RoundingMode> values = {up, down, nearest, bankers};
}

/// Placeholder sync state values shared by local and server-mode clients.
typedef SyncState = String;
class SyncStateValues {
  const SyncStateValues._();
  static const SyncState queued = "queued";
  static const SyncState synced = "synced";
  static const SyncState conflict = "conflict";
  static const SyncState failed = "failed";
  static const Set<SyncState> values = {queued, synced, conflict, failed};
}

/// Stable storage object reference that does not expose physical filesystem paths.
class StorageObjectRef {
  const StorageObjectRef({
    required this.id,
    this.contentType,
    this.sizeBytes,
  });

  /// Opaque storage object identifier.
  final String id;
  final String? contentType;
  final int? sizeBytes;

  factory StorageObjectRef.fromJson(JsonObject json) {
    return StorageObjectRef(
      id: json["id"] as String,
      contentType: json["contentType"] == null ? null : json["contentType"] as String,
      sizeBytes: json["sizeBytes"] == null ? null : (json["sizeBytes"] as num).toInt(),
    );
  }

  JsonObject toJson() {
    return {
      "id": id,
      if (contentType != null) "contentType": contentType,
      if (sizeBytes != null) "sizeBytes": sizeBytes,
    };
  }
}

/// RFC 7807 / RFC 9457 style problem response.
class ProblemDetails {
  const ProblemDetails({
    this.type,
    this.title,
    this.status,
    this.detail,
    this.instance,
  });

  final String? type;
  final String? title;
  final int? status;
  final String? detail;
  final String? instance;

  factory ProblemDetails.fromJson(JsonObject json) {
    return ProblemDetails(
      type: json["type"] == null ? null : json["type"] as String,
      title: json["title"] == null ? null : json["title"] as String,
      status: json["status"] == null ? null : (json["status"] as num).toInt(),
      detail: json["detail"] == null ? null : json["detail"] as String,
      instance: json["instance"] == null ? null : json["instance"] as String,
    );
  }

  JsonObject toJson() {
    return {
      if (type != null) "type": type,
      if (title != null) "title": title,
      if (status != null) "status": status,
      if (detail != null) "detail": detail,
      if (instance != null) "instance": instance,
    };
  }
}

/// Placeholder OCR job status values for future worker contract design.
typedef OcrJobStatus = String;
class OcrJobStatusValues {
  const OcrJobStatusValues._();
  static const OcrJobStatus queued = "queued";
  static const OcrJobStatus processing = "processing";
  static const OcrJobStatus completed = "completed";
  static const OcrJobStatus failed = "failed";
  static const Set<OcrJobStatus> values = {queued, processing, completed, failed};
}

class GetHealthResponse {
  const GetHealthResponse({
    required this.status,
  });

  final String status;

  factory GetHealthResponse.fromJson(JsonObject json) {
    return GetHealthResponse(
      status: json["status"] as String,
    );
  }

  JsonObject toJson() {
    return {
      "status": status,
    };
  }
}

class GetHealthReadyResponse {
  const GetHealthReadyResponse({
    required this.status,
    required this.service,
    required this.checks,
  });

  final String status;
  final String service;
  final JsonObject checks;

  factory GetHealthReadyResponse.fromJson(JsonObject json) {
    return GetHealthReadyResponse(
      status: json["status"] as String,
      service: json["service"] as String,
      checks: JsonObject.from(json["checks"] as Map),
    );
  }

  JsonObject toJson() {
    return {
      "status": status,
      "service": service,
      "checks": checks,
    };
  }
}
