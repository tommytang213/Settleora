# Settleora User Portal

This directory will contain the React + Vite user portal.

No implementation exists here yet. Future generated API client code must come from `packages/contracts/openapi/settleora.v1.yaml`, and generated files must not be manually edited.

The user portal must not directly access the database. User-facing operations go through the backend API.
