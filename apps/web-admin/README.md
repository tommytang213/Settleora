# Settleora Admin Portal

This directory will contain the React + Vite admin portal.

No implementation exists here yet. Admin functionality must go through the backend API, not direct database access.

Admin actions requiring audit coverage must be handled server-side.
