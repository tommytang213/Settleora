# Settleora - Outstanding Questions and Suggestions V4

Most major business requirements are locked. Remaining items are mainly implementation and refinement topics.

1. Final public name clearance:
   - Settleora is the current working name.
   - Final legal/trademark/domain/store clearance still recommended.

2. Exact file upload policy:
   - broad format support is required
   - final allowed list, size limits, OCR-supported subset, and security scanning policy should be finalized later

3. OCR local mode implementation detail:
   - local-only OCR is required
   - exact implementation method (on-device OCR, bundled engine, hybrid packaging) still needs architectural design

4. Shared visibility defaults:
   - exact defaults for other participants’ split details and proof visibility may still be fine-tuned later

5. Trash retention behavior:
   - default trash retention periods and purge policies should be finalized later

6. Dashboard widget catalog:
   - customizable dashboard is required
   - exact widget set and layout rules still need UX design

7. Sync conflict UX:
   - compare/reload/overwrite is required
   - final mobile/web conflict dialog behavior still needs product design

8. Reporting depth:
   - basic analytics/reporting/dashboard are required
   - advanced analytics scope can be refined later

9. Attachment scope:
   - Word/PDF/common files are allowed conceptually for optional proofs/supporting files
   - final security restrictions and processing rules still need design

10. Release operations detail:
   - exact CI provider and pipeline tooling still need final technical selection

11. Mobile release automation depth:
   - final level of automation vs manual approval for app-store submission still needs implementation planning
