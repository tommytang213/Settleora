# Day 1 localization-readiness audit

- Issue: [#409](https://github.com/tommytang213/Settleora/issues/409)
- Task key: `20260909-1312`
- Evidence base: `3fa603828c141fcbd9f91fb700c573d0448df3e1` (`origin/main`, 2026-09-09)
- Lane / validation: `docs-planning` / `docs-only`

## 1. Executive status

Day 1 may ship in English only, but [the Day 1 language policy](../prd/MVP_DAY1_SCOPE.md#day-1-language-scope) requires bounded string ownership, locale-aware presentation formatting, stable errors separate from visible copy, and translation-ready notifications. Actual Traditional Chinese resources, a language preference, and translated notifications/errors remain [Day 2](../prd/DAY2_SCOPE.md#19-localization-foundation-and-traditional-chinese-ui).

Overall classification: **partial**. Current clients safely preserve server authority for money and authorization, many mobile failures are reduced to typed categories and bounded copy, notification records carry stable title/message keys, and recent shared mobile components have strong 320px/2x evidence. PR #1165 completed the user-web English catalog/bootstrap/document-language foundation (`L10N-D1-005`), but mobile still lacks its locale/catalog bootstrap, formatting is mostly manual, and user-web notification UI can expose transport template keys as copy. Admin web is a reference-only dependency rather than a localization implementation.

Classifications in this document:

- `ready`: the current boundary has a stable, locale-safe seam and no Day 1 remediation is evidenced.
- `partial`: useful structure exists, but a bounded Day 1 readiness change is still required.
- `missing`: a required Day 1 structural seam is absent from the current runtime.
- `blocked/reference`: readiness cannot be implemented independently because the product runtime or a higher-consequence gate is not yet admitted.
- `Day 2 translation`: actual translated resources, language selection, and language-specific UX; absence is not a Day 1 failure.

Highest-risk remaining Day 1 gaps are `L10N-D1-014` then `001` (mobile build/native-resource and catalog-bootstrap foundations), `L10N-D1-023`/`024`/`026` (ordinary mobile/web and shared-control copy adoption is not yet completely owned), `L10N-D1-002`/`006` (manual date/time/number formatting), `L10N-D1-011`/`012` (money display is not locale-aware), `L10N-D1-008`/`015`/`017`/`019` (notification display/template seams), `L10N-D1-016`/`018`/`025` (security-sensitive web/mobile auth copy), and `L10N-D1-020` (server-authored non-problem presentation strings). Completed `L10N-D1-005` is now a credible foundation alongside decimal strings with currency attached, server-authoritative financial truth, the existing optional `ProblemDetails.code` contract/generated-client field, generated-client isolation, typed mobile failure kinds, safe raw-detail suppression tests, server notification template keys, responsive web CSS, and shared mobile components tested at high text scale.

## 2. Cross-platform infrastructure inventory

| Boundary | Status | Current evidence | Fallback / interpretation |
| --- | --- | --- | --- |
| Mobile locale/bootstrap | `missing` | `apps/mobile/lib/main.dart` constructs `MaterialApp` without `supportedLocales`, `localizationsDelegates`, locale selection, or generated localizations; `apps/mobile/pubspec.yaml` has no localization package/resource configuration and no ARB files exist. | Framework/device defaults may affect built-in widgets, but application copy is English literals. That is not an application localization seam. |
| User-web locale/bootstrap | `ready` foundation | PR #1165 adds a typed English catalog, deterministic `en` resolver, `LocaleProvider` around `App`, typed lookup, missing-key developer failure, and runtime `document.documentElement.lang` synchronization while retaining static `<html lang="en">`. | Day 1 remains English-only. This foundation does not complete ordinary feature-copy, auth/session copy, notification resolution, formatting, money, expansion, or server-message ownership. |
| Admin-web locale/bootstrap | `blocked/reference` | `apps/web-admin/README.md` says no implementation exists. `WEB_ADMIN_REFERENCE_V1.md` is an approved textual reference, not runtime. | #964 must split the runtime first; localization cannot be “added” to a nonexistent shell. |
| Server-facing/template locale | `partial` | `InAppNotificationEvents.TitleKey`/`MessageKey` produce stable keys, but invitation/password-reset composers and `PushNotificationPayloadBuilder` contain fixed English. No request/user language is resolved; the separate server-announcement contract/runtime is blocked under #1094. | In-app keys are useful machine seams. Email/push currently fall back only to English; announcements need their own future authority/locale boundary and must not reuse event notifications. |
| Shared resource/catalog layer | `partial` | User web now has its repository-local typed English catalog through PR #1165; mobile and server/template catalog seams remain incomplete. | A single cross-platform runtime is not required; platform-native catalogs with stable shared message identifiers are safer. |
| Stable errors vs display copy | `partial` | Mobile repositories expose typed `*FailureKind` values and bounded messages; user web maps `SettleoraApiError.status` to local copy. OpenAPI `ProblemDetails.code` and both generated clients already expose an optional safe machine-readable seam, with `BillCsvImportEndpoints` as a populated precedent, but most ordinary `Results.Problem` calls do not populate it consistently. | Clients usually suppress raw details. Reuse the existing field; do not invent or hand-edit a second generated-client contract. |
| Generated-client boundary | `ready` | `PROGRAM_ARCHITECTURE.md` and generated packages under `packages/client-{dart,web}` keep transport generation separate from handwritten presentation. | Do not hand-edit generated clients or put translated copy in transport models. Contract changes, if later approved, originate in OpenAPI/API work. |

## 3. Mobile Day 1 presentation inventory

All rows inherit the `L10N-D1-014` build-input and `L10N-D1-001` catalog/bootstrap prerequisites. Ordinary feature-copy extraction is tracked by non-runnable `L10N-D1-023`; reusable component/form defaults and gated labels are classified/split by `026`; auth is `018`, What's New `021`, contextual help `022`, and notification-key retention remains bounded in `001`. Where ordinary formatting appears, rows inherit non-runnable `L10N-D1-002`; money display uses separately gated `011`. “Expansion” records current evidence, not an RTL Day 1 promise.

| Surface family | Current source evidence and readiness | Interpolation / formatting / layout / accessibility | Smallest Day 1 remediation and owner |
| --- | --- | --- | --- |
| Setup | `app/setup_screen.dart`, `app/app_bootstrap.dart`; English literals, `partial`. | Scrollable/shared controls; #1096 proved 390px/1x and 320px/2x truthful connection states. Semantics are explicit. | Extract setup through a bounded child of ordinary-copy parent `L10N-D1-023`; preserve #1096 behavior. |
| Sign-in, sessions, recovery | `app/sign_in_screen.dart`, `app/password_reset_repository.dart`, `app/auth_session_repository.dart`; typed failure kinds and bounded copy are `partial`. | Timestamps use `server_mode_shell.dart::_formatTimestamp`; recovery/security wording is high consequence. Back-arrow icons rely on Material directionality, while some explicit arrows require later review. | Exclude auth/session/recovery strings from catalog-foundation `001` and ordinary-copy `023`; catalog extraction belongs gated `L10N-D1-018` after #965. Formatter adoption remains `002`; no policy rewrite. |
| Home/dashboard and shortcuts | `app/server_mode_shell.dart`; #299 actionable metrics and #295 lightweight shortcuts are implemented. | Money semantics attach amount and currency; card title/caption use ellipsis and shortcut width constraints (`_DashboardSummaryCard`, `_DashboardShortcut`). Recent 320px/2x evidence is credited. | Ordinary copy (`023`), money display (`011`) and focused expansion review (`004`); do not reopen #299/#295 Day 1 work. |
| Bills and revisions | `bills/bill_list_screen.dart`, `bill_revision_*`; broad English copy and local label maps are `partial`. | `_decimalPreviewMoney` uses binary `double` plus `toStringAsFixed(2)` for a non-authoritative preview; `_formatCurrencyAmount` preserves decimal digits but is not locale-aware; `_formatBillDate` is ISO-like. Multiple `maxLines: 1`, ellipses, fixed `118`-wide amounts, explicit left/right alignment and arrows exist. Accessibility tests cover many state/action labels and raw-detail suppression. | Ordinary copy (`023`), date/number adoption (`002`), gated money adoption (`011`), and expansion/direction parent (`004`). |
| Groups/participants | `groups/group_list_screen.dart` and bill participant controls; `partial`. | Search uses lowercased strings; timestamps use raw local `DateTime.toString`; explicit chevrons/arrows and ellipsis occur. Shared selectors/search/dialog work under #301 children is credited. | `023`/`002`/`003`/`004`; do not recreate completed #1097/#1106/#1128 work. |
| Settlements | `settlements/settlement_list_screen.dart`; `partial`. | `_money` concatenates decimal string and code; timestamps and dates are manual. Explicit right padding/alignment and chevrons occur. Typed failures and server-authority copy are strengths. | `023`/`002`/`004`/`011`; money/payment behavior stays server-authoritative and manual-gated. |
| OCR capture/review | `receipt_ocr_capture/*`, `receipt_ocr_review/*`; `partial`. | OCR parser normalization is data parsing, not display localization. Review dates are manual ISO UTC; money/quantity suggestions remain strings. Extensive semantics tests suppress IDs, paths, tokens, raw payloads; recent state/button shared-component work is credited. | Presentation copy/format/layout only (`023`/`002`/`004`). #959 parser and #970 workflow audit remain independent. |
| Recurring/future bills | `recurring_bills/recurring_bill_screen.dart`, `future_bills/*`; `partial`. | `_money` concatenates amount/code and timestamps use raw local strings; singular/plural and schedule labels are ad hoc. One-line/ellipsis and directional layout occur. | `023`/`002`/`003`/`004`; recurrence and draft truth remain server-owned. |
| Notifications | `notifications/generated_notification_repository.dart`, `notification_repository.dart`, `notification_screen.dart`; `partial`. | Generated responses contain `titleKey`/`messageKey`, but `_mapRow` drops them from `SettleoraNotificationRow`; the UI instead maps `eventType`, `_plural` is English-only, timestamps are raw local strings, and `safeSummary` can override mapped copy. Semantics and redaction tests are strong. | `001` must retain/test the generated keys through the handwritten row and consume them in the catalog; `003` owns plural/interpolation and `002` locale time. Do not hand-edit generated clients. |
| Reports/search/reconciliation | `reports/monthly_report_screen.dart`, `manual_finance/manual_finance_screen.dart`; `partial`. | Report month is `yyyy-MM`, timestamps are raw, totals use `toStringAsFixed(2)` or amount/code concatenation. Search normalizes only with trim/lowercase. | `002`/`003`/`011`, with money gate for financial display and no client recalculation. |
| Profile/payment details | `profile/profile_screen.dart`; `partial`. | Central field selectors and semantic labels exist; timestamps and file sizes are manual and English units. High-consequence visibility copy is locally bounded. | `023`/`002`; payment/privacy meaning stays with existing policy owners. |
| More/App settings | `app/server_mode_shell.dart`; `partial`. | Shared `SettingsRow` improves wrapping/focus. Static copy remains local; explicit `Alignment.centerLeft` appears in settings/readout rows. | `023`/`004`; preserve #295 navigation and #301 shared-component behavior. |
| Data/import/export/backup preview | `app/local_data_backup.dart`, backup UI in `server_mode_shell.dart`; `partial`. | Stored payload timestamps correctly remain ISO/UTC machine data. Visible preview uses `_formatBackupUtcMinute`; copy clearly states non-mutating preview/disabled restore. | Only visible copy/formatting (`023`/`002`); restore/apply/storage/privacy stays separately gated. |
| What's New | `app/version_notes.dart` and bootstrap/settings launchers; `partial` for localization, behavior complete under #1092. | Caller-owned bundled copy, shared scrolling sheet, focus return, and 320px/2x long-copy evidence are strong extraction seams. | Extract the bounded version-note bundle through `L10N-D1-021`; no seen-state or behavior replay. Translation itself is Day 2. |
| Contextual help | `help/contextual_help.dart`; `partial` for localization, behavior complete under #1093. | One keyed registry and shared flexible bottom sheet are ready for extraction; 10 topics have 320px/2x and focus evidence. | Extract registry messages through `L10N-D1-022`; do not recreate #1093 mechanics. Translation is Day 2. |

## 4. User-web Day 1 inventory

`apps/web-user/src/App.tsx` is an actual React runtime, not a design mock. It exposes authenticated/read-only and several import/export/backup workflows through generated-client seams. `dist/` is build output and `docs/design/web/WEB_USER_REFERENCE_V1.md` is reference-only; neither owns source copy.

| Surface family | Status and evidence | Smallest Day 1 boundary |
| --- | --- | --- |
| Shell/dashboard/account | `partial`: PR #1165 completed `L10N-D1-005` with an English catalog/provider, static/runtime document language, and catalog-owned ordinary navigation labels/descriptions/actions/compact labels and navigation semantics. Dashboard/shared states and broader feature copy remain distributed. | `L10N-D1-024` owns later ordinary feature-copy/visible-enum adoption; auth/session states remain excluded to gated `025`. |
| Sign-in/session/authorization states | `partial`: `authSession.ts`, `App.tsx`, and bills/groups/profile/reports/settlements/notifications/import-export readout adapters own session-verification, sign-in-required, session-expired, authorization-denied and protected-information copy. Typed state and raw-detail suppression are strengths. | Inventory and split all approved visible auth/session/authz states through non-runnable `L10N-D1-025` after #965; every runtime child is manual-gated and preserves behavior. |
| Public password-reset completion | `partial`: `App.tsx` dispatches `/auth/password-reset` to `PasswordResetCompletePage.tsx`; labels, validation failures and session-ending success copy are hardcoded. Existing tests prove fragment scrubbing, safe generic states and completion behavior, not localization. | Consume the web catalog only through the manual-gated auth copy slice (`L10N-D1-016`); preserve the completed #339 behavior and #965 reconciliation authority. |
| Bills/groups/friends | `partial`: typed generated responses and safe error reductions exist in `billsReadout.ts`/`groupsFriendsReadout.ts`; dates and money are manual. Some friend/direct-share surfaces explicitly report missing methods rather than pretending runtime. | Ordinary copy (`024`), formatter/collation adoption (`006`) and separately gated money adoption (`012`) under #963 reconciliation; runtime completeness stays #963-owned. |
| Settlements/profile/payment | `partial`: `settlementsReadout.ts` and `profileReadout.ts` keep server authority and bounded errors; `App.tsx` uses shared `formatMoney`/`formatDate`, but those return raw amount/code and sliced ISO date. | `024`/`006`/`012`; money/payment/authz behavior is out of scope. |
| Notifications | `partial`: stable generated fields exist, but `App.tsx` renders `safeSummary ?? messageKey` and also exposes `titleKey`/`messageKey`; feature-local filters, metrics, empty states, preference/readout headings and visible status/priority/subject labels remain English; count pluralization is an English suffix in `notificationsReadout.ts`. | Message-key resolution belongs `L10N-D1-008` after #973; ordinary notification chrome/visible enum labels belong a bounded child of `024`; pluralization belongs `006`. |
| Reports/search | `partial`: server totals are rendered without recalculation; `reportsReadout.ts` concatenates amount/currency, month defaults from UTC ISO, and filtering/sorting uses lowercase/`localeCompare` without an explicit product locale/collator. | Ordinary copy (`024`), formatter/collation (`006`) and money (`012`); retain server-authoritative totals. |
| Import/export/backup/sync | `partial`: substantial current runtime exists with bounded stable-code handling and safe error tests. Visible times use the same sliced-date helper. `App.tsx` directly renders `normalizedCandidate.splitMethod` and `splitBasisValue`; the latter can be percentage basis. Local copy and server-authored `safeMessage`/`privacyBoundary`/`dataEgressBoundary` are distinct; CSV/JSON transport formats and filenames remain invariant machine contracts. | Ordinary copy and visible enum labels use `024`; percentage/date formatting uses `006`; server-authored display-field normalization belongs `020`. No restore/apply or storage/privacy change. |
| Layout/accessibility | `partial`: `styles.css` includes `min-width: 320px`, responsive breakpoints at 1060/760px, `minmax(0,1fr)`, `overflow-wrap`, and narrow single-column fallbacks. It also has physical `left/right`, `text-align: left/right`, fixed 132/340px columns and `white-space: nowrap`. | Focused web expansion/logical-direction implementation and visual QA (`013`); this audit does not promise RTL support. |

## 5. Admin-web Day 1 inventory

`apps/web-admin/README.md` is the entire current app surface and states that no implementation exists. The approved [admin reference](../design/web/WEB_ADMIN_REFERENCE_V1.md) specifies product-grade desktop/tablet layout, bounded/redacted copy, accessibility, responsive behavior, and manual gates, but it is not runnable UI. Therefore admin localization readiness is `blocked/reference`, not `missing translation`: #964 must first reconcile and split #463-#467. `L10N-D1-007` requires the first admitted shell child to establish catalog/locale/document-language ownership and keeps #964 open until later runtime children either meet bounded formatting/expansion acceptance or have focused follow-on owners. Admin exposure, auth/security, provider secrets, storage/privacy, backup/restore, and deployment gates remain unchanged.

## 6. Server-facing presentation inventory

| Boundary | Status | Evidence and interpretation |
| --- | --- | --- |
| In-app notification records | `partial` | `InAppNotification` persists `TitleKey`/`MessageKey`; `InAppNotificationEvents` derives stable `notifications.<event>.{title,message}` keys. This is a good resource seam. Some event producers persist dynamic English `SafeSummary` (for example recurring due dates), clients prefer it over keys, and the response lacks typed template arguments. |
| Push payload | `partial` | `PushNotificationPayloadBuilder` deliberately sends privacy-safe generic English title/body and stable event/subject/reference data. Provider separation is sound; locale selection/catalog resolution is absent. |
| Generic SMTP notification email | `partial` | `SmtpEmailNotificationTemplate` emits fixed English event-family subjects/body and directly interpolates `request.EventType` into a visible `Notification type` line. `SmtpEmailNotificationSenderTests::CompleteConfigurationBuildsBoundedPrivacySafeMessageThroughTransportBoundary` proves safe transport composition, not locale resolution. |
| Invitation email | `partial` | `InvitationEmailTemplateComposer.TemplateSubject` and `BuildTextBody` are fixed English with safe link interpolation and a redacted preview. Provider/readiness separation is sound; locale/fallback input is absent. |
| Password-reset email | `partial` | `PasswordResetEmailTemplateComposer` has the same safe composition/provider split and fixed English template. Because it is auth/security copy, implementation is manual-gated. |
| API problems | `partial` | OpenAPI `ProblemDetails.code` and generated clients already expose an optional safe machine-readable value; `BillCsvImportEndpoints` populates it as a precedent. Most endpoints return bounded English problems without consistently populating that existing field, and clients mostly map status into local typed copy. Details should remain diagnostic/fallback data, not primary translated UI. |
| Non-problem server display fields | `partial` | Import/export/backup/sync success and readiness DTOs expose fixed-English `safeMessage`, `privacyBoundary` and `dataEgressBoundary`; user web renders many directly and mobile renders server sync `safeMessage`. These are visible presentation data, not `ProblemDetails`, logs, or internal diagnostics. |
| Server-managed announcements | `blocked/reference` | Day 1 PRD requires admin/API-authoritative announcements targeted to mobile, user web, admin web or all, but the current OpenAPI/runtime/surfaces are absent; the mobile checklist assigns authority/contract planning to #1094 and distinguishes announcements from event notifications. #1094 must define stable message/content ownership, locale targeting/fallback, active-window and safe-content seams before separately gated platform children; security/admin/policy-impact audit gates remain intact. |
| Exported headers/labels | `ready` for current purpose | `ExpenseBillExportEndpoints.CsvHeaders` and invariant date/number serialization form a machine-readable export/import contract. Translating these identifiers would reduce interoperability. User-facing export confirmation copy is a separate catalog concern. |
| Logs/audit/internal diagnostics | not localization UI | They remain redacted operational evidence under existing privacy/audit policy. This audit does not turn diagnostics into translated product copy or authorize broader exposure. |

## 7. Formatting readiness

| Concern | Status | Evidence / rule |
| --- | --- | --- |
| Dates | `partial` | Mobile has several private ISO/manual helpers and raw `toLocal().toString().split('.')`; web `formatDate` slices the first 10 characters. Machine/storage dates may stay ISO; visible dates need platform locale formatters. |
| Times and zones | `partial` | Mobile sometimes labels explicit UTC (backup/sync) and elsewhere silently calls `toLocal`; web shows sliced dates and manual `HH:00` quiet hours. Preserve instants/zones as data and localize only presentation. |
| Decimal numbers | `partial` | Counts are raw strings and file sizes use fixed English units. Financial decimal strings are safely transported; display grouping/digits are not locale-aware. |
| Money/currency | `partial` | Currency is consistently attached, a major strength. Mobile/web generally concatenate amount then code; one mobile preview uses `double.toStringAsFixed(2)`. A locale formatter must accept decimal-safe text/minor-unit truth and currency metadata without recalculating, rounding, converting, or changing server authority. |
| Percentages | `partial` | User web directly renders `App.tsx` CSV-review `normalizedCandidate.splitBasisValue`, whose `splitMethod` may be percentage. `L10N-D1-006` must inventory the representation and assign locale-safe display adoption without changing the invariant import value or split calculations. |
| Pluralization | `missing` structurally | Mobile `_plural` and web `${count === 1 ? "" : "s"}` implement English suffix rules only. Use catalog plural/select forms; do not build sentences from translated fragments. |
| Interpolation | `partial` | Dart/TS string interpolation is widespread and safe for values, but placeholder names/order are not catalog-controlled. Notification/email links are already bounded variables. |
| Sorting/comparison/search | `partial` | Mobile search commonly uses trim/lowercase; web uses lowercase and `localeCompare` without an explicit resolved locale/collator. Identifiers/status codes remain ordinal machine values; user-facing names need locale-aware comparison/normalization after locale bootstrap. |
| Financial truth | `ready` boundary | API/domain remains authoritative and generated decimal strings/currency remain unmodified. Localization work is presentation-only and must not infer settlement, split, tax, OCR acceptance, FX, or rounding truth. |

## 8. Text expansion and RTL readiness

RTL is not a Day 1 support promise. The readiness goal is to avoid choices that force a later domain/API rewrite.

- Mobile shared sheets, `SettingsRow`, state panels, scrollable scaffolds, and recent #1092/#1093/#1096/#299/#295 320px/2x evidence materially reduce expansion risk. These are credited, not reopened.
- Mobile still contains one-line ellipsis and fixed-width presentation in dashboard shortcuts/metrics, notification rows, bill/OCR rows, and recurring rows; physical `Alignment.centerLeft/centerRight`, `EdgeInsets.only(left/right)`, and explicit forward/back arrows appear in core flows. Review only meaning-bearing cases; decorative spacing and Material direction-aware icons are not automatically defects.
- User web has responsive breakpoints, wrapping and `minmax(0,1fr)` safeguards. Physical left/right borders/alignment, `white-space: nowrap`, and fixed table/detail columns require a catalog-expansion and logical-property pass.
- Admin reference explicitly requires tablet accessibility and responsive fallbacks. Actual expansion/RTL evidence cannot exist before runtime.

`L10N-D1-004` and `013` own these bounded implementation checks. #975 retains final native/device/screen-reader/platform acceptance; it is not a substitute for implementation.

## 9. Error/copy architecture

Mobile domain repositories generally define stable local `FailureKind` enums and suppress generated/transport details; focused tests in bill attachments, OCR review, notifications, auth, and other domains assert that paths, tokens, IDs, stack traces, raw payloads, or provider details do not enter visible/semantic text. User-web readout modules similarly convert status categories to bounded copy, and import/export tests exercise unsafe problem payloads.

The existing OpenAPI `ProblemDetails.code` property and generated-client fields are the correct stable top-level seam; `BillCsvImportEndpoints` demonstrates safe population through the problem `extensions` map. The remaining structural gap is broader than top-level adoption: most ordinary problems leave the optional code absent, client mappings are repeated by domain/status, and widespread `Results.ValidationProblem` returns field-keyed English message arrays while OpenAPI describes coded `ProblemDetailsError` entries. `L10N-D1-010` therefore recommends a non-runnable parent that inventories both top-level and per-field validation errors, resolves API/OpenAPI compatibility before runtime children, and remains open while it assigns bounded `api-domain-runtime` children. Consequential domains remain separately manual-gated; client catalogs consume only safe populated codes, and generated clients are never hand-edited.

## 10. Notifications and email templates

Notification state, provider readiness, and language preference are separate concerns. Current notification records have excellent stable key seams and safe reference fields; user preferences cover delivery/timing/category behavior, not language. No code should infer language from notification channel, quiet hours, currency, region, or server locale.

`L10N-D1-008` owns the evidenced user-web key-resolution defect. Mobile key retention/adoption stays inside `L10N-D1-001`. Existing #973 owns `L10N-D1-019`: decide and split the server `SafeSummary`/typed-template-argument boundary so clients can prefer catalog keys without losing required dynamic meaning. `L10N-D1-015` and `017` separately own server push and generic SMTP notification template seams. All preserve privacy-safe content, delivery-state truth and provider separation.

`L10N-D1-009` owns auth email composition: accept an explicit resolved locale/fallback at the template boundary, keep invitation/reset variables and redacted previews, and leave SMTP/provider readiness separate. It is manual-gated auth/security work. Actual Traditional Chinese templates remain Day 2.

## 11. Finding registry

Every non-ready Day 1 finding below has exactly one owner recommendation. “New focused issue” means recommendation only; #409's close rule does not authorize issue creation. After merge, #409 remains the durable audit/registry reference, but owns no runtime mutation: a recommendation is not runnable until a separately authorized planning action reconciles it into a numbered focused issue.

| ID | Platform / surface | Severity / status | Exact evidence | Owner | Smallest remediation boundary | Lane / validation | Scope / gate / duplicate prevention |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `L10N-D1-001` | Mobile application catalog/bootstrap and notification-key retention | high / `missing` | `main.dart::SettleoraMobileApp.build`; no locale/catalog context; notification `_mapRow` drops generated `titleKey`/`messageKey` | **New focused issue after `014`: Mobile English catalog/bootstrap and notification-key retention** | Wire the admitted catalog into locale bootstrap/shared chrome; retain/test notification keys through the handwritten row and use Notifications as the representative feature. Ordinary feature families (`023`), auth (`018`), What's New (`021`) and help (`022`) are excluded. | `mobile-application` / `mobile` | Day 1 readiness. No translation or generated-client edit. #301 is component adoption, not copy ownership. |
| `L10N-D1-002` | Mobile visible date/time/non-money-number adoption | high / `partial` | `_formatTimestamp` duplicates across shell, notification, group, profile, settlement, recurring, revision; manual file sizes and count rendering | **New focused planning issue: Mobile locale-safe date, time, and ordinary-number adoption inventory** | Define shared presentation helpers, inventory every current visible call-site family, and assign bounded `mobile-application` children until each is adopted or explicitly invariant; exclude money. | `docs-planning` / `docs-only` | Non-runnable Day 1 parent that stays open through all bounded mobile children. No current focused owner found. |
| `L10N-D1-003` | Mobile plural/interpolation/search adoption | medium / `partial` | `notification_screen.dart::_plural`; lowercased discovery in reports and feature screens; interpolated sentence fragments | **New focused planning issue after `014`/`001`: Mobile plural, placeholder, and collation inventory/split** | Inventory catalog plural/select placeholders and user-visible-name comparison call sites, then assign bounded mobile children; keep machine codes ordinal. | `docs-planning` / `docs-only` | Non-runnable Day 1 parent that stays open through all bounded children; no matching focused owner found. |
| `L10N-D1-004` | Mobile expansion/direction adoption | medium / `partial` | dashboard fixed widths/ellipsis; bill/notification/recurring one-line rows; explicit physical alignment/padding/arrows | **New focused planning issue: Mobile localization expansion/direction inventory and split** | Inventory representative core-flow risks under long-English fixtures/high scale and assign bounded surface children for meaning-bearing physical assumptions. | `docs-planning` / `docs-only` | Non-runnable Day 1 parent. #975 owns final platform acceptance, not remediation; completed #301/#1092/#1093 work is not replayed. |
| `L10N-D1-005` | User-web catalog/bootstrap and document language | high / `ready` foundation | #1164 / PR #1165 (`284a580ee160d4f39f59bf9099398df57de810b9` → `a0ae6d58e5207c5137703c3ac54db108a6144585`): typed English catalog/context, deterministic unsupported-input fallback to `en`, bootstrap provider, static/runtime `lang`, and ordinary navigation/shared-chrome migration with desktop/narrow proof | **Completed #1164** | Foundation complete. Keep the current supported locale `en`; later bounded owners consume this seam without replacing it. | `web-user-ui` / `web-ui` | Auth/session/password reset (`016`/`025`), notification resolution (`008`/`019`), ordinary feature copy (`024`), formatting (`006`), money (`012`), expansion (`013`), server strings (`020`) and announcements (`027`) remain incomplete and separately gated. |
| `L10N-D1-006` | User-web date/time/non-money number/percentage/plural/collation adoption | high / `partial` | `billsReadout.formatDate`; report UTC month default; notification suffix plural; lowercase/`localeCompare`; CSV review `normalizedCandidate.splitBasisValue` can represent percentage | **New focused planning issue after #963 reconciliation: User-web locale-safe ordinary presentation-helper inventory** | Define resolved-locale helpers, inventory every visible date/time/ordinary-number/percentage/plural/collation call-site family, and assign bounded `web-user-ui` children; exclude money/layout and preserve invariant transport values. | `docs-planning` / `docs-only` | Non-runnable Day 1 parent that remains open through all bounded web children. No matching focused owner found; #963 remains dependency/anti-duplication audit. |
| `L10N-D1-007` | Admin-web catalog, formatting, and expansion readiness | high / `blocked/reference` | `apps/web-admin/README.md` says no implementation; admin reference is docs only, while planned audit/health/storage/backup/provider readouts imply future timestamps, counts, sizes and responsive copy | **Existing #964** | Reconcile #463, establish catalog/locale/document-language ownership in the first shell child, and keep #964 open until every admitted admin child has bounded locale-safe formatting and expansion acceptance or a focused follow-on owner. | `docs-planning` / `docs-only` | Day 1 readiness blocked on runtime/reference/manual gates. Runtime children use `web-admin-ui` and retain their security/privacy/provider gates; do not create a parallel admin-localization parent before #964. |
| `L10N-D1-008` | User-web notification display resolution | high / `partial` | server `TitleKey`/`MessageKey`; web `safeSummary ?? messageKey` and visible key fields | **New focused issue after #973 reconciliation: User-web notification message-key resolver** | Resolve current stable keys/placeholders through the web English catalog with safe bounded fallback; never render raw keys. | `web-user-ui` / `web-ui` | Day 1 readiness. #973 first confirms current event ownership; mobile adoption belongs `001`, and push/provider behavior is unchanged. |
| `L10N-D1-009` | Invitation/password-reset email | high / `partial` | `InvitationEmailTemplateComposer`; `PasswordResetEmailTemplateComposer`; fixed English subject/body with safe variables | **New focused issue: Auth email template locale seam and English fallback** | Explicit locale/fallback input at composers, keyed subject/body, safe interpolation/redacted preview; no provider change. | `auth-session-security` / `api-security` | Day 1 readiness; manual auth/security gate. Searches found no email-localization issue; provider #403 does not own template language. |
| `L10N-D1-010` | API top-level and validation problem-code adoption across Day 1 domains | medium / `partial` | OpenAPI/generated `ProblemDetails.code`; `BillCsvImportEndpoints` populated precedent; most `Results.Problem` calls omit it; widespread `Results.ValidationProblem` returns field-keyed English arrays while OpenAPI expects coded `ProblemDetailsError` entries | **New focused planning issue: Problem-code and validation-error compatibility inventory/domain split** | Inventory top-level and per-field validation errors, reconcile actual API/OpenAPI shape compatibility, credit populated codes, and assign one bounded API child per unresolved domain/gate until all are owned. | `docs-planning` / `docs-only` | Non-runnable Day 1 parent. Any contract/client change requires the API/OpenAPI compatibility gate and regeneration—never generated-client hand edits; consequential domains remain separate. |
| `L10N-D1-011` | Mobile money-display adoption | high / `partial` | `_money`, `_decimalPreviewMoney`, `_formatCurrencyAmount`, report totals and currency-code concatenation across multiple surfaces | **New focused planning issue: Mobile locale-aware money-display inventory and gated split** | Specify a decimal-safe presentation adapter, inventory every current money call-site family, and assign bounded `mobile-application` children until adopted; no calculation/rounding/FX change. | `docs-planning` / `docs-only` | Non-runnable Day 1 parent; every runtime child requires the manual money gate. Closed #852 covers semantics, not locale display. |
| `L10N-D1-012` | User-web money-display adoption | high / `partial` | `billsReadout.formatMoney`, `reportsReadout` and settlement/profile readouts concatenate amount/currency across multiple surfaces | **New focused planning issue after #963 reconciliation: User-web locale-aware money-display inventory and gated split** | Specify a decimal-safe presentation adapter, inventory every current money call-site family, and assign bounded `web-user-ui` children until adopted; no recalculation. | `docs-planning` / `docs-only` | Non-runnable Day 1 parent; every runtime child requires the manual money gate. No matching focused owner found. |
| `L10N-D1-013` | User-web expansion/direction adoption | medium / `partial` | `styles.css` physical alignment/borders, fixed columns and `white-space: nowrap` alongside responsive safeguards | **New focused planning issue after #963 reconciliation: User-web expansion/logical-CSS inventory and split** | Inventory long-English desktop/narrow risks and meaning-bearing physical properties, then assign bounded `web-user-ui` surface children. | `docs-planning` / `docs-only` | Non-runnable Day 1 parent; no RTL support promise. Separate from formatting/money; no matching focused owner found. |
| `L10N-D1-014` | Mobile localization build/native inputs | high / `missing` | `apps/mobile/pubspec.yaml` has no localization dependency/ARB config; Android manifest uses literal `android:label="mobile"`; iOS `Info.plist` uses literal `CFBundleDisplayName`/`CFBundleName` and fixed-English permission descriptions with no `InfoPlist.strings` | **New focused issue: Mobile English localization build and native-resource foundation** | Admit required localization dependency/configuration, minimal English catalog skeleton, Android app-label resource, and iOS display-name/permission resource seams; no Dart application migration. | `mobile-build-config` / `mobile-build-config` | Day 1 readiness; strong review plus manual privacy wording gate. Must precede `001`; no translated content, permission behavior, signing/release or generated-output change. No matching focused owner found. |
| `L10N-D1-015` | Server push-template locale seam | high / `partial` | `PushNotificationPayloadBuilder` emits fixed generic English title/body; no explicit locale/catalog input | **New focused issue after #973 reconciliation: Privacy-safe push locale seam and English fallback** | Resolve an explicit locale at the server payload-template boundary with a bounded English catalog/fallback; keep generic content, delivery state and provider integration unchanged. | `api-domain-runtime` / `api-domain` | Day 1 readiness; notification privacy/content review and strong independent review. #634 owns device/provider delivery runtime, not template-language resolution; no provider/secret/mobile-release change. |
| `L10N-D1-016` | User-web password-reset completion copy | high / `partial` | `App.tsx` public route; `PasswordResetCompletePage.tsx` hardcoded labels, validation/failure and session-ending success copy; focused safety tests | **New focused issue after #965 reconciliation: Password-reset-complete catalog adoption** | Extract only the current approved generic reset-completion UI states into the web catalog; preserve fragment scrubbing, anti-enumeration posture, validation and session revocation behavior. | `web-user-ui` / `web-ui` | Day 1 readiness with manual auth/security review. #339's current behavior stays complete; no auth/API/session logic, reset material or public-exposure change. No matching localization owner found. |
| `L10N-D1-017` | Generic SMTP notification email template | high / `partial` | `SmtpEmailNotificationTemplate.BuildSubject/BuildTextBody`; fixed English family copy; visible `Notification type: ${request.EventType}` exposes a transport identifier; focused sender test | **New focused issue after #973 reconciliation: Generic notification-email locale seam and English fallback** | Resolve explicit locale/catalog subject/body at the template boundary with typed placeholders; map event type to a locale-owned safe label or omit that visible line while retaining the raw value only as transport/diagnostic data. | `api-domain-runtime` / `api-domain` | Day 1 readiness with notification privacy review. Preserve safe references, delivery/provider separation and #632/PR #645 runtime; #403/#449 do not own template language. No SMTP config, secret, provider or delivery-state change. |
| `L10N-D1-018` | Mobile auth/session/recovery and domain-failure copy | high / `partial` | `sign_in_screen.dart`, password-reset/session repositories and shell UI plus handwritten/generated domain repositories under bills, groups, profile, reports, settlements, notifications, recurring, OCR, sync and other features emit rendered sign-in/session/authz messages | **New focused planning issue after #965: Mobile auth/session/domain-failure copy inventory and split** | Inventory every repository-to-screen visible auth/session/authz state, then assign bounded `mobile-application` children by cohesive domain; preserve validation, anti-enumeration, recovery, credential, session and authorization behavior. | `docs-planning` / `docs-only` | Non-runnable Day 1 parent; every runtime child requires manual auth/security review. Separate from `001`/`023`; no auth/API/session/deep-link/public-exposure change. |
| `L10N-D1-019` | In-app notification dynamic summary/template arguments | high / `partial` | recurring `DueSoonSummaryTemplate`; persisted `SafeSummary`; web/mobile prefer `safeSummary`; no typed due-date/template argument in response | **Existing #973** | Reconcile whether clients prefer a generic keyed message with safe summary fallback or a separately split typed-template-argument contract; assign each required runtime slice before #973 closes. | `docs-planning` / `docs-only` | Day 1 readiness planning owner; notification privacy and any OpenAPI/client changes remain separate gated children. Do not infer arguments from mutable English or expose raw keys. |
| `L10N-D1-020` | Non-problem server-authored presentation fields | high / `partial` | OpenAPI/API `safeMessage`, `privacyBoundary`, `dataEgressBoundary`; direct web rendering in import/export/backup/sync and mobile sync display | **New focused planning issue after #963/#970 and #406/#971/#966 reconciliation: Server presentation-message/code inventory and domain split** | Inventory visible non-problem DTO copy, credit machine-only fields, choose stable code/client-catalog versus explicit server locale per field, and place each bounded portability/storage/sync child under the existing canonical owner before any new allocation. | `docs-planning` / `docs-only` | Non-runnable Day 1 parent; #406 is the portability implementation-split owner and #971/#966 are required reconciliation inputs. Storage/privacy/restore/sync/API/OpenAPI/client children stay separate/manual-gated; remain open until all fields are owned. |
| `L10N-D1-021` | Mobile What's New resource extraction | medium / `partial` | `app/version_notes.dart` owns bundled English release content; #1092 completed behavior/high-scale evidence | **New focused issue after `001`: What's New English-catalog extraction** | Move only the bounded version-note title/body family into the admitted mobile catalog; preserve keys, seen state, sheet mechanics and focus behavior. | `mobile-application` / `mobile` | Day 1 readiness. #1092 is complete and is not reused as an owner; actual translation is Day 2. No matching focused owner found. |
| `L10N-D1-022` | Mobile contextual-help resource extraction | medium / `partial` | `help/contextual_help.dart` owns one versioned keyed registry with 10 English topic bodies; #1093 completed mechanics/high-scale evidence | **New focused issue after `001`: Contextual-help English-catalog extraction** | Move only the current topic title/body family into the admitted mobile catalog; preserve registry keys, authority copy, sheet mechanics and focus behavior. | `mobile-application` / `mobile` | Day 1 readiness. #1093 is complete and is not reused as an owner; actual translation is Day 2. No matching focused owner found. |
| `L10N-D1-023` | Mobile ordinary feature-copy adoption | high / `partial` | setup, dashboard, bills, groups, settlements, OCR, recurring, reports, profile, settings and backup-preview sources retain distributed English literals after the representative bootstrap slice | **New focused planning issue after `001`: Mobile ordinary feature-copy inventory and split** | Inventory every ordinary Day 1 feature-copy family and assign one bounded `mobile-application` child per cohesive surface until each is catalog-owned. Exclude auth (`018`), What's New (`021`), help (`022`) and server-authored fields (`020`). | `docs-planning` / `docs-only` | Non-runnable Day 1 parent that remains open through all bounded children. #301 owns components, not copy; no matching catalog-adoption parent found. |
| `L10N-D1-024` | User-web ordinary feature copy and visible enum labels | high / `partial` | authenticated bill/group/settlement/profile/report/import/export/backup/sync JSX and notification chrome remain distributed English; CSV review renders raw `normalizedCandidate.splitMethod` such as `exact_amount`/`share_weight` | **New focused planning issue after `005`/#963: User-web ordinary feature-copy and visible-enum inventory/split** | Inventory every ordinary feature-copy family and visible machine-enum label, including notification filters/metrics/states/headings/labels, then assign bounded `web-user-ui` children that map display values through the catalog while preserving transport values. Exclude password reset (`016`), other auth/session states (`025`), notification message-key resolution (`008`) and server-authored fields (`020`). | `docs-planning` / `docs-only` | Non-runnable Day 1 parent that remains open through all bounded children. #963 prevents runtime-completeness duplication; no matching catalog-adoption parent found. |
| `L10N-D1-025` | User-web sign-in/session/authorization copy | high / `partial` | `authSession.ts`, `App.tsx`, `billsReadout.ts`, `groupsFriendsReadout.ts`, `profileReadout.ts`, `reportsReadout.ts`, `settlementsReadout.ts`, `notificationsReadout.ts`, and `importExportReadout.ts` contain visible sign-in/session/authz states | **New focused planning issue after #965: User-web auth/session copy inventory and split** | Inventory every shell/domain-adapter visible auth/session/authz state and assign bounded `web-user-ui` children by cohesive surface; preserve verification, expiry, denial, sign-in and protected-data behavior. | `docs-planning` / `docs-only` | Non-runnable Day 1 parent; every runtime child requires manual auth/security review. Excluded from `005`/`024`; no auth/API/session/authz/public-exposure change. |
| `L10N-D1-026` | Mobile shared component/form copy | high / `partial` | `ui/settleora_components.dart` owns selector defaults/loading/retry labels; `ui/settleora_form_fields.dart::settleoraPaymentMethodOptions` uses the same strings as displayed and persisted values, alongside other defaults/helpers/validation copy | **New focused planning issue after `001`: Mobile shared-control catalog inventory and gated split** | Classify every shared visible/semantic default; assign bounded ordinary children. For payment methods, preserve stable persisted/custom values through an explicit value-to-localized-label mapping; if the free-text API cannot represent that safely, assign a separately gated API/OpenAPI compatibility child before UI adoption. | `docs-planning` / `docs-only` | Non-runnable Day 1 parent. #301 owns component behavior, not copy; money/payment/security children retain manual gates. No generic component replay, persisted-value migration or matching catalog owner. |
| `L10N-D1-027` | Cross-platform server-managed announcements | high / `blocked/reference` | PRD “User guidance, release notes, and announcements”; mobile checklist M48; no current announcement OpenAPI/runtime/platform surface | **Existing #1094** | Define authority/contract and mobile handoff with stable message/content ownership, locale targeting/fallback, safe interpolation and platform-target semantics; assign separately gated mobile/web/admin children before #1094 closes. | `docs-planning` / `docs-only` | Day 1 readiness blocked on authority/API/admin/reference gates. Announcements remain distinct from event notifications and bundled What's New; security/admin/policy-impact content retains manual audit gates. |

Finding totals: **27** — mobile 11, user web 8, admin web 1, server/cross-boundary 7; severity high 21 / medium 6; readiness `ready foundation` 1 / `missing` 2 / `partial` 22 / `blocked/reference` 2. All twenty-seven are Day 1 structural readiness; actual Traditional Chinese content remains Day 2.

## 12. Dependency-safe remediation wave

1. Reconcile #963, #964, #965, #970 and #973 first where their live audits govern user-web, admin, auth-recovery, OCR/import presentation, or notification child creation. #1094 separately defines server-announcement authority/localization seams before any platform child. This prevents parallel owners and does not block the independent mobile build foundation.
2. Implement the focused mobile build/native-resource foundation (`014`) before mobile catalog/bootstrap and notification-key retention (`001`). User-web `005` is complete through #1164/PR #1165; use that seam for later bounded children. Create the non-runnable ordinary/shared-copy parents (`023`/`024`/`026`) and split their bounded feature children, including the separately focused What's New (`021`) and contextual-help (`022`) mobile slices and separately gated shared-form labels. Admit admin catalog, formatting and expansion only through children governed to completion by #964 (`007`).
3. Create the non-runnable ordinary-formatting adoption parents (`002` and `006`) and gated money-adoption parents (`011` and `012`), then assign bounded surface children until every inventoried call-site family is owned. Start with low-consequence dates/counts/visible percentage display; money children require the manual gate and must not change decimal truth, currency, rounding, FX, split, or settlement behavior.
4. After #973 fixes exact event ownership and splits `019`, implement the user-web notification resolver (`008`), mobile key retention/catalog adoption through `001`, and separate server push (`015`) and generic SMTP notification-email (`017`) locale seams. The email child maps or omits visible raw event identifiers; provider delivery stays separate. Implement auth email seam (`009`) separately under manual auth/security review.
5. Create the non-runnable mobile plural/collation (`003`) and mobile/web expansion/direction (`004`/`013`) parents; use long-English fixtures, 320px/2x mobile evidence, and desktop/narrow web evidence to assign bounded surface children rather than one cross-feature runtime task.
6. Create the non-runnable `L10N-D1-010` parent to inventory and split all in-scope top-level and field-validation problem families. First reconcile `Results.ValidationProblem` output against the coded OpenAPI error-entry contract; then stage bounded API children under the appropriate compatibility and consequential-domain gates. The parent stays open until every family has a bounded owner; generated clients are regenerated only from an authorized contract change and are never hand-edited.
7. After #965 confirms current auth boundaries, extract only `PasswordResetCompletePage` copy through `016`, other user-web auth/session states through `025`, and mobile auth/session/recovery copy through `018` under manual auth/security review; do not replay completed behavior or alter reset/session/authz authority.
8. Create the non-runnable `020` inventory only after #963/#970 and canonical portability/storage reconciliation through #406/#971/#966; place server-authored success/readiness display-field children under those existing owners by domain/high-consequence gate. Do not create competing portability children or bundle storage/privacy, restore, sync and client adoption into one runtime issue.
9. Day 2 may add Traditional Chinese catalogs, language preference, translated notifications/errors, locale-specific QA, and broader RTL policy if separately required. Day 2 must consume rather than replace the Day 1 seams.

## 13. Explicit non-gaps and credited work

- #1092 What's New is behavior-complete: bundled keyed release content, safe seen state, reusable scrolling sheet, focus return, and 320px/2x evidence. Only resource extraction remains; the feature is not reimplemented.
- #1093 contextual help is behavior-complete with one versioned keyed registry, 10 topics, shared sheet mechanics, high-scale evidence, and bounded authority copy. Its registry is a strong extraction seam.
- #1096 setup truthfulness and connection/persistence retry behavior are complete and must not be altered by copy extraction.
- #299 dashboard actionability and #295 Day 1 lightweight shortcuts are complete. #295 remains open only for retained Day 2 drag/drop/custom-layout scope.
- #301 and its completed focused children provide shared fields, buttons, dialogs, state panels, guidance sheets, semantics and focus seams. Localization owns message resources; #301 remains the component-adoption owner and should not be used as a generic copy bucket.
- User web is not a mock: current generated-client-backed bill/group/settlement/profile/notification/report/import/export/backup/sync readouts are real evidence. Its missing/partial classifications concern localization seams, not a claim that all runtime is absent.
- Admin web is the opposite: an approved reference exists, but runtime does not. Translation absence is not the defect; catalog readiness must enter with the first real shell.
- Currency-attached decimal transport, server-authoritative financial calculations, invariant machine timestamps/CSV identifiers, stable notification keys, typed local failure kinds, and generated-client isolation are credited foundations.
- Logs, audit records, machine-only enum values, route identifiers, storage keys, API paths, generated models, OCR parser tokens, and CSV contract headers are not translated UI resources. Any enum rendered as user-visible copy—such as CSV-review `splitMethod`—requires catalog label mapping under `L10N-D1-024` while its transport value remains invariant.
- No Day 1 Traditional Chinese resources are required or claimed by this audit.
- #409 remains the canonical merged audit/registry reference after its close rule is met. Its unnumbered recommendations grant no runtime authority; each must be created/reconciled as a focused issue before implementation.
