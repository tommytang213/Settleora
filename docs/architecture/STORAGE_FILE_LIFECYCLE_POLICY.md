# Storage File Lifecycle Policy

Issue: [#719](https://github.com/tommytang213/Settleora/issues/719)

Parent chain: [#716](https://github.com/tommytang213/Settleora/issues/716) →
[#717](https://github.com/tommytang213/Settleora/issues/717) → #719; shared
template: [#960](https://github.com/tommytang213/Settleora/issues/960).

## 1. Decision And Authority

This document defines Day 1 provider-neutral lifecycle policy for file objects
and their subject links. It applies the
[shared lifecycle taxonomy](../planning/DAY1_LIFECYCLE_POLICY_TEMPLATE.md) to
the repository at `6981236c786a7914ac1a17c25d9c3d9de22c8b74` (tree
`40aad85c0b7ee7dcf2a4fc4b982b5ad18cb91ca9`). It is planning and factual
reconciliation only. It does not change bytes, runtime, schema, migrations,
API or OpenAPI, generated clients, providers, configuration, secrets,
deployment, exposure, cleanup, or disposal.

In server mode, API/domain/storage services own file-object and subject-link
mutation acceptance, authorization, current policy, dependency checks,
retention classification, and audit. PostgreSQL owns provider-neutral metadata;
bytes remain behind `IFileObjectStorageProvider`. Stable opaque file IDs are
references, never access grants. Clients, routes, object URLs, browser caches,
device caches, generated methods, and worker job payloads are not authority.
Workers may perform bounded provider work from API-approved jobs, but they must
return structured results and never mutate core business, file-object, or link
tables directly.

Local-only files and caches remain inside the local application authority
boundary described by the
[local/server authority audit](LOCAL_SERVER_MODE_AUTHORITY_BOUNDARY_AUDIT.md).
They do not become server `file_objects`, server links, or accepted server
bytes until an explicit upload/import action is revalidated and accepted by
the API. This policy does not invent a local persistence model that the current
mobile source does not have.

## 2. Reconciled Current Facts

Current source proves the following, and no more:

- `FileObjectPurposes` contains exactly `receipt_image`, `ocr_source`,
  `settlement_proof`, `payment_qr`, `statement_upload`, `export_file`, and
  `supporting_attachment`.
- `FileObjectStatuses` contains exactly `pending`, `active`, `quarantined`,
  `deleted`, `purged`, and `upload_failed`. The schema has no persisted
  `orphan`, `expired`, `cleanup_eligible`, or provider-missing state.
- `EfFileObjectLifecycleService` implements metadata transitions
  `pending → active`, `upload_failed → active`, `pending → upload_failed`,
  `active|quarantined → deleted`, and `deleted|upload_failed → purged`.
  It has no quarantine, restore, expiry, reference-proof, or cleanup method.
- The current `purged` transition only changes PostgreSQL metadata and writes
  audit. No production source calls `IFileObjectStorageProvider.DeleteAsync`.
  A current `purged` row therefore does **not** prove physical deletion.
- Purpose-specific bill attachment, settlement proof, and payment QR endpoints
  reserve metadata, write through the provider, activate the object, then link
  it. Bill-attachment and settlement-proof authorization, membership, and
  mutable-status checks occur before the upload and are not reloaded
  immediately before link commit, so a concurrent bill, payment, request, or
  membership change can make those observations stale. There is no generic
  public file API.
- Bill and proof ordinary list/content queries require `RemovedAtUtc == null`,
  an `active` file object, an unset file `DeletedAtUtc`, matching purpose and
  ownership, current subject visibility, and current actor authorization.
  QR metadata/content reads likewise require a current QR link, matching
  ownership/purpose, and an active non-deleted object. Thus ordinary current
  reads stop for later requests when either the implemented link or object
  condition is inactive. Those checks are not serialized with provider open or
  response delivery: a removal, deletion, or authorization revocation can race
  an already-authorized in-flight read, which may still return bytes.
- Bill/proof removal sets link `RemovedAtUtc` and changes the file object to
  `deleted` in one `SaveChanges` consistency boundary used by the lifecycle
  service. Its subject authorization/status observations are not reloaded at
  acceptance, so revocation or immutability can race the removal. Cancellation
  or another non-database exception can also escape from `MarkDeletedAsync`
  with the combined link/object/audit commit unknown and no success response;
  retry then sees no readable active link rather than a prior-result record.
  QR removal clears `QrFileObjectId`, saves that change, then attempts
  `active → deleted`. An unsuccessful lifecycle result is ignored, but
  cancellation or another exception thrown by that later transition propagates
  after the profile-link/payment-audit commit: the client receives no success,
  the old-object outcome is unknown, and retry can observe an already-removed
  link or create another replacement object.
- Bill and settlement link keys do not make `FileObjectId` globally unique;
  payment QR linkage is also not unique. Receipt OCR reviews, OCR assignments,
  notification history, normalized bill-item source-review/source-line fields,
  and baseline/proposed bill-revision snapshot JSON can retain dependencies.
  Bill-item source fields have restrictive FKs to OCR reviews and review lines.
  OCR-sourced revisions serialize the receipt attachment ID
  inside `attachmentFileIds` and the source review ID inside
  `receiptOcrReviewIds`; those historical references have no direct foreign
  keys to `FileObject` or `ReceiptOcrReview`. Restrictive FKs preserve the
  normalized rows, but do not inventory the JSON references. Consequently, one
  observed unlink or zero normalized links is not proof that an object or OCR
  review is unreferenced.
- A non-cancellation storage-write failure triggers a best-effort attempt to
  mark the reserved object `upload_failed`. Each upload handler ignores a
  returned unsuccessful lifecycle result, so metadata can remain `pending`; the
  compensation call itself is unguarded, however, so cancellation or another
  thrown exception can propagate with its status/audit commit unknown and no
  bounded upload-failure response. The bill handler has the same unguarded call
  after a returned activation failure. Request cancellation during provider write bypasses
  that failure handler, so metadata remains `pending` while the local provider
  may already have created partial bytes. The endpoint does not remove them.
  Each bill, proof, and QR upload also buffers the multipart file into a managed
  `byte[]` before metadata reservation and retains that copy through validation
  and provider write; ordinary garbage collection, not explicit zeroization or
  a deterministic release time, controls its disposal. The bill handler parses
  and copies the full upload after route-scope authorization but before it loads
  the bill or checks subject visibility, mutation authority, and mutable status,
  so an unavailable or immutable bill can still incur that sensitive buffer.
  Each handler calls
  `ReadFormAsync` first, so framework multipart buffering may also stage an
  above-threshold body in a server temporary file; no repository `FormOptions`
  override or application crash-cleanup evidence defines that copy's lifecycle.
  Cancellation or another non-database exception during the initial
  `CreatePendingAsync` save can also leave its commit unknown: a `pending` row
  and upload-start audit may exist without the handler receiving the file ID,
  and an unkeyed retry creates another reservation. The same unreachable
  reservation occurs when `CreatePendingAsync` succeeds but the handler's
  immediate `SingleAsync` metadata reload is cancelled or fails: the committed
  ID is not returned. Cancellation or another
  thrown exception during `MarkActiveAsync` after the
  provider write bypasses the unsuccessful-result branch in every handler, so
  activation commit/state can be unknown with complete bytes and no link. A caught
  `DbUpdateException` during association save triggers a best-effort attempt to
  change the now-active object to `deleted`, but each handler ignores a returned
  unsuccessful result. That compensation call is itself unguarded:
  cancellation or another thrown exception can propagate with its delete/audit
  commit unknown and no bounded upload-failure response. Cancellation and other
  exception types from audit/notification staging before the guarded save, or
  from the original association save, bypass that catch and compensation
  entirely, so the object can remain active and unlinked while staged
  link/audit/notification state or a later save outcome is unknown. After a
  successful association commit, bill/proof handlers reload the saved link and
  QR reloads the profile after old-object cleanup is attempted. Cancellation or
  query failure during those reloads returns no success although the new link is
  already live; an unkeyed retry can create another upload/replacement. No
  cleanup runner or prior-result record reconciles these cases.
- QR upload has another post-activation gap before audit staging: its active
  payment-profile query can be cancelled or fail, leaving the newly active file
  unlinked with no compensation or bounded response.
- The current local provider opens a new destination with `FileMode.CreateNew`
  before copying content. Cancellation or write failure can therefore leave a
  zero-length, partial, or complete file at the reserved key, and another write
  to that same key fails because the destination already exists. A failed row
  cannot be retried in place: reconciliation must either validate complete
  bytes before activation or authorize a new target and separately dispose of
  the old copy.
- Initial QR attachment and QR replacement have no expected-version/null-at-
  commit guard. Two initial uploads can each activate a new object after
  observing a null link; last write wins and leaves the losing new object active
  and unlinked because neither request observed an old object to delete.
  Concurrent replacements can
  each activate a new object and save the profile; last write wins, while both
  requests target only the commonly observed previous object for logical
  deletion. The losing new object can therefore remain active and unlinked.
- Current file lifecycle audit proves success events only for upload start,
  completion, failure, logical deletion, and metadata `purged`. It records a
  bounded file ID, purpose, prior/new status, provider category, timestamp, and
  actor account through `auth_audit_events`; it excludes filenames, hashes,
  retention text, vault references, object keys, paths, and contents. It does
  not prove denial, cleanup, dependency, restore, or physical-delete audit.
  Subject access audit is asymmetric: bill attachment, settlement-proof, and
  settlement-scoped counterparty QR content reads open the provider stream and
  then attempt to save bounded read events, but the self QR content endpoint
  streams bytes without an audit writer/event. Successful metadata-only reads
  are also unaudited: bill attachment lists, settlement-proof lists, and self
  payment-details reads return active file IDs plus bounded type, size, time,
  or current QR metadata without an access event. The audited handlers catch only
  `DbUpdateException` around that save. Cancellation or another exception can
  leave the audit commit unknown and the already-open stream without explicit
  disposal; a committed success event still precedes response delivery and is
  not proof that the client received content.
- Current OpenAPI and generated clients expose subject-specific bill
  attachments, settlement proofs, payment QR flows, OCR review flows over a
  receipt attachment, direct streamed bill export/import operations, and local
  backup package operations. Direct CSV/JSON export creates a request response,
  not a server artifact; its browser download copies are inventoried separately
  under `FILE-LC-LOCAL-011`. Direct CSV
  preflight returns a request-scoped review without mutation; direct CSV import
  creates draft bills and audits without a session or confirmation. Persisted
  CSV import sessions retain their review/candidate JSON after confirmation,
  discard, or lazy expiry; no cleanup removes those fields or rows. Direct and
  session-confirmed imports do not recheck authorization at database acceptance,
  so revocation after their initial check may race the commit. Direct import
  catches only `DbUpdateException`; cancellation/other thrown save failures have
  unknown commit outcome, and an unkeyed retry can duplicate a committed import.
  Either persisted import-session creation save can likewise be interrupted by
  cancellation or another uncaught exception with unknown commit outcome; retry
  can create a second session while an unreachable first row retains private
  review/candidate JSON. Discard and lazy-expiry saves likewise leave commit
  unknown on cancellation/other uncaught exceptions; retry may observe the
  terminal row without proving the prior response. Session GET and discard load
  only by stored account/profile/auth-session binding: after profile soft
  deletion or group-access revocation they can still return private review data,
  trigger persisted expiry, or discard. Confirmation alone rechecks current
  profile/group authorization. Parallel confirmations can both pass ready/challenge checks; after one commits
  the shared candidate IDs, the other may return generic database-write failure
  rather than the successful prior result. Cancellation or another non-
  `DbUpdateException` during the confirmation save can also leave the session,
  draft bills, and audits committed even though no success response returned;
  a retry then sees a non-confirmable session rather than the prior result. The backup
  runtime holds sessions and data-only
  package artifacts in process memory, permits authenticated content downloads
  through separately issued actions whose consumed flags block sequential
  reuse but are not atomically single-use under concurrent requests, and parses submitted
  package content into a restore preview and metadata-only confirmation
  session. Restore-preview creation rejects a missing, zero, or oversized
  `Content-Length` before reading the body, so current chunked requests do not
  reach full-body materialization. For an accepted bounded request it
  materializes private submitted content as the request string, parsed package text and
  UTF-8 bytes; those full-content copies
  are GC-controlled and not explicitly cleared or zeroized. Its advertised TTLs are enforced lazily on later access; there is no
  background sweep or removal of package-session entries, so an unaccessed
  artifact can remain in process memory until process restart. The three
  process-static package-session, restore-preview, and restore-confirmation
  dictionaries also have no per-actor/global entry count, aggregate memory
  quota, or top-level removal sweep. Each ready package session also accepts
  repeated download-action issuance into its nested dictionary without a per-
  session count/memory quota or issuance-rate limit; lazy pruning removes only
  expired or consumed actions. Repeated creation or action issuance can therefore retain an
  unbounded aggregate of entries, artifact/summary references, and private
  metadata until process restart even when each individual item is bounded and
  later becomes unavailable. Future acceptance requires policy-approved
  bounded capacity plus eager/scheduled sweep and removal behavior; this
  document does not invent numeric limits. Later accesses
  can clear artifact references and prune actions. Preparation can enter
  `blocked`; the authorization-failure branch preserves a prior ready artifact
  and actions, while generation-validation failure clears them. Cancellation,
  query failure, or a profile-deletion race inside the builder is uncaught:
  `created` remains created, while ready reprepare preserves the prior artifact/
  actions without a bounded response. Restore-preview
  responses hard-code confirmation unavailable/unsupported and omit the mapped
  confirmation-session action even though a ready preview is accepted by that
  handler. A confirmation creation can also race preview discard or lazy expiry:
  each can observe `ready`, leaving a live confirmation that references a now-
  terminal preview. Restore-preview
  and confirmation entries retain package-summary references after discard or
  lazy expiry. Submitted `sections[].name` values have no category allowlist or
  safe-value validation, so arbitrary caller-supplied private strings can enter
  the included/omitted/unsupported/blocked lists, persist in both entry types,
  and be echoed by responses until process restart. Their dictionaries have no
  removal sweep. Confirmation entries also retain the trimmed caller
  idempotency key without length/safe-token validation through discard/expiry;
  responses expose only key-accepted boolean and request digest, not the key.
  Preview record summaries clamp each candidate's nested counts but sum as many
  as 250 candidate counts without an aggregate cap, so returned aggregate item,
  participant, payer, or adjustment counts can exceed the OpenAPI field maximum.
  Except for preparation's `CanAccessProfileAsync` check,
  package-session and restore-preview/confirmation surfaces rely on active
  account/session authentication plus stored actor/session binding without
  rejecting a soft-deleted profile: such a profile can create/read/discard
  package metadata, submit private preview content, and create/read/discard
  confirmation metadata while its account/session remains active. It does not
  persist a backup as a `FileObject` or through the storage provider, include
  file bytes, or apply a restore. No file-object restore, quarantine, retention,
  cleanup, or physical-purge operation is exposed.
- Package preparation checks current profile accessibility, but later download-
  action creation and byte delivery recheck only the stored actor/auth-session
  binding. If the user profile becomes soft-deleted while the account/session
  remains active, a prepared artifact can still receive actions and download;
  the preparation-time profile decision is not an acceptance-time content gate.
- Package sessions live in a concurrent top-level dictionary, but each
  session's download actions use an ordinary unsynchronized dictionary.
  Concurrent issue/download/cancel/prune operations can race the consumed flag,
  mutate during enumeration, throw, or leave inconsistent process state.
- The OpenAPI package-session create description says generation/download are
  unsupported, and its prepare description says preparation never creates an
  artifact or bytes and can only be unavailable/blocked. Current prepare code
  instead builds and retains an artifact and returns `ready_to_download`.
  The separate package-readiness GET also returns `Available: false`, an
  unsupported stable code, and copy saying creation/download/preview/
  confirmation are unsupported even though those handlers are mapped in the
  same endpoint class and generated clients expose them. Package-session
  creation, preparation, artifact-status, and download-action responses also
  reuse capability fields that label restore preview and confirmation as
  unsupported. These contract/runtime
  contradictions are current source truth, not authority to infer either side
  away.
- Current cancellation copy is also contradictory: cancelling a ready session
  clears its existing artifact/actions, but the returned artifact/session copy
  says no artifact or package bytes were created, deleted, or mutated.
- Mobile camera/gallery and file-picker intake retains a trimmed raw local path
  reference alongside copied bytes in `SettleoraPickedBillAttachmentFile`, and
  receipt OCR may pass that path to ML Kit. The source path can identify
  picker-managed temporary material or a user-owned import; current source does
  not prove ownership, cleanup, or a common disposal clock. Path values remain
  private local state and must not enter logs, audit, server metadata, or
  evidence. Receipt processing also copies the compressed source, decodes a
  full pixel-buffer image, creates a second resized pixel buffer, then encodes
  normalized and thumbnail arrays. These sensitive decoded buffers can exceed
  source size and have only GC-controlled lifetime; no wipe/disposal evidence
  exists.
- Mobile App settings also implements a separate client-local backup JSON
  lifecycle. `SecureStorageLocalDataBackupService.buildExport` materializes
  mobile-local configuration and the entire device-wide sync queue as an object
  graph and full `encodedJson`. The queue uses one unscoped secure-storage key,
  logout clears only the server session, and export merely stamps the newly
  authenticated profile ID; queued resource IDs, states, or error text from a
  prior account can therefore be disclosed under the next account's export.
  The settings state retains the latest export and a
  `SelectableText` reveals the JSON for manual copying. Generating again
  replaces the state reference without explicit zeroization or a disposal
  record. Import preview retains pasted full JSON in a `TextEditingController`,
  parses it into another object graph and bounded preview, and disposes the
  controller on dialog close without proving the underlying string was wiped.
  `buildExport` separately calls `_previewBackupMap` directly and returns a valid
  bounded preview that the export card displays. If that same `encodedJson` is
  pasted into the distinct `previewImport` path, it is always rejected before
  schema preview because its own `includesSessionMaterial` key matches the broad
  sensitive fragment `session`; queue error strings can add other false-positive
  rejection paths.
  No file, server `FileObject`, sharing action, restore apply, retention clock,
  copy inventory, or audit is created by these mobile surfaces.
- Other implemented server-mode clients create independent copies. The generated
  Dart client first accumulates a mobile bill attachment response in a mutable
  full `List<int>`; the repository then creates a second full unmodifiable list,
  so two file-sized buffers coexist during wrapping before the UI shows only
  the length. Mobile bill
  attachment download materializes full content as `List<int>` before the UI
  displays only its length. Web package download/restore creates a `Blob`, an
  object URL, a user-saved file, a selected browser `File`, and a full
  `File.text()` string; restore selection reads that string before the server
  package limit applies, then request construction and the generated client
  retain the package-content string and create another full `JSON.stringify`
  request string whose escaping can increase its size. Web bill CSV import
  likewise reads the selected file fully without first checking `File.size`
  against the API limit. Concurrent selections have no generation/current-file
  guard, so an older read can resolve last and overwrite the text while newer
  filename/size state remains displayed, allowing the wrong selected content to
  be submitted. Direct CSV
  export separately creates a response `Blob`, object URL, and potentially a
  user-saved file. Mobile existing-bill receipt/supporting selection requests
  full bytes and upload mapping makes another full list copy without routing
  through the receipt processor's size gate. Server unlink/expiry/purge cannot recall or prove
  disposal of any of these client/device/browser copies; their release,
  revocation, saved-file ownership and wipe clocks are unrecorded.

Architecture direction is not runtime proof. The
[metadata architecture](STORAGE_FILE_METADATA_ARCHITECTURE.md),
[file policy](STORAGE_FILE_POLICY_ARCHITECTURE.md),
[privacy vault architecture](PRIVACY_VAULT_ARCHITECTURE.md), and
[package security policy](LOCAL_BACKUP_RESTORE_PACKAGE_SECURITY.md) define
required future behavior, including normalized receipt retention, derivatives,
Trash, restore checks, vault protection, and consistency-set backup/restore.
Where those documents do not define an exact duration or transition mechanic,
this policy leaves it unresolved.

## 3. Vocabulary And Separate State Models

- **Archive** removes a record from ordinary active use while retaining it and
  its history. No current file endpoint implements an object archive distinct
  from `deleted`.
- **Trash** is user-visible holding for logically removed content. It is not a
  current state name and does not authorize a retention timer or purge.
- **Soft/logical removal** changes a file object or link so defined ordinary
  use stops while bytes and required evidence remain.
- **Unlink** makes one subject relationship inactive or clears it. It is not
  deletion of bytes and need not change the file-object state.
- **Restore** requests re-entry from an eligible inactive condition after all
  current checks. **Reactivate** is the accepted transition to one named usable
  state. Neither means silent reattachment.
- **Quarantine** blocks ordinary access or processing while validation,
  integrity, privacy, or safety is unresolved. It is not archive or disposal.
- **Expire** means a retention or validity condition ended. Expiry is evidence,
  not a state transition and not purge.
- **Purge** is the separately authorized terminal disposition workflow. The
  current metadata status named `purged` is only a marker transition and must
  not be presented as byte deletion without provider outcome evidence.
- **Hard/physical delete** removes eligible bytes immediately rather than
  placing authoritative content in Trash. It is permitted only for positively
  proven non-authoritative temporary/failed material or dependency-free drafts
  under a separately approved policy and every destructive gate.
- **Temporary cleanup** concerns positively identified scratch, staging, cache,
  or failed temporary material. **Orphan cleanup** additionally requires proof
  that no authoritative relationship, history, package, replica, or hold needs
  the material. An orphan candidate is not a runtime state and is never proof
  of disposability.

### 3.1 File-object state model

| Persisted state | Provider-neutral policy meaning | Current support |
| --- | --- | --- |
| `pending` | Metadata reservation for an incomplete write/finalization. Never ordinarily readable. | Created by current uploads. No age/cleanup policy. |
| `active` | Eligible for purpose- and subject-authorized use if every link, privacy, and content check also passes. | Current upload target and only ordinarily readable object state. |
| `quarantined` | Intended isolation state; ordinary metadata/content/preview use is blocked except bounded authorized maintenance/security inspection. | Schema/enum only; no transition or inspection API. #1062 owns validation/quarantine runtime reconciliation. |
| `deleted` | Retained logical object removal. Bytes may remain. Ordinary reads stop. | Current object transition; no restore or retention clock. |
| `upload_failed` | Metadata says upload completion failed. It says nothing conclusive about whether zero, partial, or complete bytes exist. | Current transition from `pending`; no cleanup runner. |
| `purged` | Current bounded tombstone marker. It is not proof of provider deletion, copy disposition, or terminal workflow completion. | Metadata-only transition exists; no production physical-delete orchestration. |
| unknown provider state | Provider outcome is missing, unreadable, timed out, or inconsistent with metadata. This is a derived maintenance condition, not a new persisted status. | No reconciliation runtime; fail closed and investigate. |

No second state taxonomy is created. Proposed concepts such as staging,
available, rejected, orphan candidate, cleanup eligible, or disposed map to the
existing states or remain derived/documented-only conditions until a focused
schema/API decision is approved.

### 3.2 Subject-link state model

| Link family | Active | Inactive | Current support and distinction |
| --- | --- | --- | --- |
| Bill receipt attachment | `RemovedAtUtc == null` with `receipt_image` purpose | `RemovedAtUtc != null` | Current attach/remove. The row persists; restrictive OCR review/assignment and notification dependencies can persist. |
| Bill supporting attachment | `RemovedAtUtc == null` with `supporting_attachment` purpose | `RemovedAtUtc != null` | Current attach/remove. The row persists and bill/history dependencies can persist; receipt OCR review/assignment flows do not apply. |
| Settlement proof attachment | `RemovedAtUtc == null` | `RemovedAtUtc != null` | Current attach/remove. The payment and proof history persist. |
| Payment-profile QR current scalar pointer | `QrFileObjectId` equals this object's ID, independently of readability | Relationship is absent when scalar is null or points to replacement; no inactive-link row. | Current attach/replace/remove. Removal audit records the removed old ID. Replacement payment audit records only the new ID; if old-object deletion produces no lifecycle audit, no retained row/audit identifies the displaced old ID. Restore/orphan logic must not infer it. |
| OCR review/assignment, bill-item source, and notification reference | Current domain status plus a stable file/review/line reference | Domain-specific terminal/removed state where defined | Dependency/history record, not a byte-ownership link. Bill items retain restrictive `SourceReceiptOcrReviewId` and `SourceReceiptOcrReviewLineId` relationships; every applicable normalized reference blocks orphan/disposition proof until policy classifies its authoritative target or sufficient tombstone. |
| Bill-revision snapshot JSON references | `BaselineSnapshotJson` or `ProposedSnapshotJson` contains the receipt file ID in `attachmentFileIds` and the source OCR review ID in `receiptOcrReviewIds` for an OCR-sourced revision | No current lifecycle removes either embedded ID when the attachment, review, or proposal changes state. Authorized ordinary revision list/detail mapping parses and returns the first proposed attachment ID as the OCR source file ID without current link/object-state validation. | Retained historical references without direct file/review FKs. A deleted object's stable ID can remain visible to bill viewers although content stays blocked. Current policy cannot choose retained targets versus tombstones/redaction, so each ID blocks disposition until history/file/OCR owners decide audience, audit, minimization and retention. |
| Future package/import/derivative link | Not defined | Not defined | Documented-only; must use a reviewed relationship rather than provider paths or an inferred filename/hash. |

## 4. Day 1 Purpose Mapping

This table uses the existing file-purpose enum. Qualifiers such as canonical,
derivative, temporary, failed, and orphan are lifecycle/relationship facts,
not new purposes.

| Required class | Existing purpose mapping | Current runtime | Retention classification |
| --- | --- | --- | --- |
| Receipt source/original | The normalized user-accepted canonical image maps to `receipt_image`. A raw camera/gallery input is temporary intake material and is not retained by default after successful normalization. | Bill upload stores accepted JPEG/PNG/WebP bytes as `receipt_image`; normalization and raw-source cleanup are not proved. Mobile retains a private path/copy for OCR. OCR-sourced bill revisions can persist the receipt file ID inside baseline/proposed snapshot JSON without a direct file FK. | Canonical image: dependency-gated to parent/history, including unresolved bill-revision snapshot semantics; duration unresolved. An embedded historical ID blocks disposition until the domain owner proves required byte retention or sufficient tombstone treatment. Raw/picker cleanup also requires ownership, integrity, accepted linkage and no OCR/retry need. |
| OCR source where distinct | `ocr_source`. A receipt used directly by current OCR review remains its `receipt_image`; do not relabel it. | Enum/schema reserved; no standalone OCR-source upload or worker-file runtime. | Required but duration unresolved; dependency-gated to job/review/reprocessing and privacy policy. |
| Receipt thumbnail/preview | A derivative; current enum has no thumbnail purpose. Future persistence needs parent/derivative markers. | Mobile processor copies compressed input, decodes a full pixel buffer, creates a resized pixel buffer, then encodes normalized/thumbnail arrays for JPEG/PNG/WebP. No server object/link; all intermediate/full arrays are GC-controlled and may exceed source size. | Bytes/pixel buffers are transient but have no wipe/disposal proof. Future derivative retention follows parent and reproducibility/dependency proof. `FILE-LC-CHOICE-005`. |
| Supporting attachment | `supporting_attachment`; current bill-link purpose uses the same value. Screenshots or documents remain this purpose when supporting a bill. | Current bill upload/list/content/remove. | Policy-defined dependency-gated to the attachment subject/history; duration unresolved. |
| Settlement/payment proof | `settlement_proof`. | Current settlement-payment upload/list/content/remove. | Policy-defined dependency-gated to proof/payment/settlement history; duration unresolved. |
| Payment QR/payment image | `payment_qr`. | Current self attach/replace/remove/content and settlement-scoped counterparty content read. | Policy-defined dependency-gated while the payment setting/link exists; removed/replaced copies enter retained logical removal pending policy. |
| Generated export package | `export_file` when persisted. Current CSV/JSON responses are not server file objects. | Server response is transient. Web download creates a browser `Blob`, object URL and user-saved file; server has no inventory/disposal authority over them. | Future expiry cannot prove browser URL/blob/saved-copy cleanup. Server response is request-scoped; client retention/disposal unresolved. |
| Generated backup package | `export_file` for a persisted bundle; manifests distinguish backup without a second purpose. | API creates process artifacts/actions. Web download creates Blob/object URL/saved file; restore selection creates browser `File` plus full `File.text()` string. Mobile settings creates cross-account-capable JSON copies. None creates a server `FileObject` or applies restore. | API TTL is availability, not browser/device copy disposal. Blob/URL/File/text/mobile graphs have no common inventory, revoke, wipe or saved-file ownership clock. Future persistence is dependency-gated/destructive-manual. `FILE-LC-CHOICE-006/007`. |
| Imported-source file | `statement_upload` for a file-backed statement/import source. Package-local blobs remain untrusted package input until API acceptance; current CSV request bodies are not `file_objects`. | Enum reserved. Direct preflight/import and session creation accumulate request bytes in a `MemoryStream`, decode a separate full CSV string, then parse request-scoped data; direct import creates draft bills/audits. Session-backed import persists `ReviewJson` and `CandidateJson`; confirm, discard, and lazy expiry change status/timestamps without clearing those fields or deleting the row. | Required but duration unresolved. The byte buffer and decoded string can coexist and are transient with independently GC-controlled release and no zeroization proof. Persisted candidate/review data remains after confirmation, discard, or expiry until a separate authorized retention operation exists; those statuses are not disposal proof. |
| Temporary upload/staging | Keep the intended final purpose and use `pending`; local scratch before reservation is outside server metadata. | Current uploads use `pending`; no separate staging purpose or cleanup timer. | Transient cleanup eligible only with positive non-authoritative/dependency proof. |
| Failed upload | Keep intended purpose and use `upload_failed`. | Current metadata transition; byte presence is unknown. | Transient cleanup candidate, never automatically disposable; duration unresolved. |
| Orphan/unlinked object | Keep original purpose and derive “orphan candidate” from a complete reference inventory. | No state or proof service. | Dependency-gated; physical disposal blocked until positive proof. |
| Statement upload (additional current enum) | `statement_upload`. | Reserved; no storage-backed statement upload. | Required but duration unresolved; private to importer by default. |
| Export file (additional current enum) | `export_file`. | Reserved; current exports are streamed. | Explicit expiry policy required; duration unresolved. |

## 5. Common Policy-Row Contract

The matrices in sections 6.1 through 6.6 together complete every reusable-row
field from #960. These common values apply to every row unless a cell narrows
them:

- **Authority mode/workspace:** server mode in one self-hosted workspace.
  `FILE-LC-LOCAL-*` rows describe client-local copies/processing and cannot
  mutate server records; specifically, `FILE-LC-LOCAL-003..006` run inside the
  authenticated server-mode shell, but the exported device-wide sync queue is
  not account-scoped and can retain prior-account entries, so these rows are
  neither local-only-profile nor safely current-user-scoped authority.
  `FILE-LC-PKG-001..010` and `012..017` are API-owned process memory;
  `PKG-018..021` are PostgreSQL import-session/business state; `PKG-011`,
  `022`, and `023` are request/response scoped; and `PKG-024` is a read-only
  readiness response. None is storage-provider or `FileObject` state.
  The API/domain/storage boundary remains authoritative in server mode.
- **Actor/authorization:** an authenticated owner/uploader may request only a
  purpose-specific subject action current policy permits. Domain owners and
  narrowly approved maintenance actors may execute only their reviewed scope.
  Admin is never blanket metadata or content access. A worker is an executor,
  not an approver.
  Current `PKG-001..010` and `PKG-012..017` do not share one complete current-
  profile authorization barrier: only preparation (`PKG-002/010`) calls
  `CanAccessProfileAsync`; creation, metadata/status/cancel/discard/action/content
  paths and every restore-preview/confirmation path otherwise use active
  account/session authentication and stored actor/session binding. A soft-
  deleted profile can therefore use each applicable non-prepare surface while
  its account/session remains active; the affected rows repeat that consequence.
- **Preconditions:** current actor/account, ownership, purpose compatibility,
  current object and link state, subject visibility/status, privacy/vault and
  quarantine policy, dependency/hold evidence, byte integrity/readability when
  relevant, conflict/duplicate detection, and authoritative concurrency state.
- **Persisted lifecycle metadata:** stable file ID, purpose, bounded prior/new
  object state, applicable link state, actor, timestamp, safe reason/category,
  correlation/idempotency value, and transition/provider outcome are required.
  Current rows lack reason, restoration, disposition outcome, and concurrency
  metadata; that is a gap, not permission to infer success. For
  `FILE-LC-LOCAL-001..012` and `FILE-LC-PKG-*`, server file-object/link fields
  are not applicable. Required client-local identity/acknowledgment/disposal,
  process-package state, persisted import-session state, or request/response
  evidence is row-specific and unresolved rather than inferred from file metadata.
- **Idempotency/replay/concurrency:** retries must return the prior bounded
  result or resume from a recorded step. The same idempotency identity with
  different actor, subject, purpose, or content fingerprint is a conflict.
  Races with unlink, relink, replace, quarantine, subject status, hold, or
  cleanup revalidate before commit. Current lifecycle methods reject repeated
  terminal transitions rather than proving idempotent endpoint outcomes.
- **Dependency evidence:** inspect every registered subject link and historical
  reference, OCR review/assignment, normalized bill-item source-review/source-
  line reference, notification reference, bill-revision snapshot JSON
  `attachmentFileIds` and `receiptOcrReviewIds`, import/export or
  backup manifest, retained audit/tombstone, active job/lease, vault/recovery
  dependency, legal/operational hold, and backup/snapshot/replica copy. Current
  source has no complete dependency registry or copy-disposition proof.
- **Hard delete and purge:** ordinary user-visible remove is ineligible for
  both. A future physical action is blocked unless its row positively proves
  target class, retention eligibility, all dependencies/copies/holds,
  authority, audit, retry/concurrency safety, required tombstone/history,
  consequence warning, explicit confirmation when user-initiated, and every
  destructive/storage/privacy/operational manual gate. Retention expiry alone
  is insufficient.
- **Audit/privacy:** audit applicable success and security-relevant denied,
  blocked, conflict, retry, cleanup, and disposition outcomes using bounded
  metadata only. Exclude bytes, thumbnails/previews, raw OCR text, document or
  QR contents, original filenames unless separately approved and bounded,
  hashes unless explicitly needed, physical/mounted/local paths, object keys,
  buckets/containers, signed/direct URLs, provider diagnostics, vault/key
  material, secrets/credentials, request bodies, hidden metadata, and
  unnecessary private data.
- **Client implication:** refresh authoritative state after every mutation;
  show unavailable/blocked states without existence leaks; never infer access,
  restoration, cleanup, or disposal from cache, IDs, routes, object URLs, or
  generated methods. User-initiated irreversible action requires accessible
  warning and explicit confirmation in the future #723 reference.
- **Runtime acceptance:** future changes require focused authorization and
  purpose tests, object-versus-link tests, hostile/concurrent/replay tests,
  provider fault injection, reference/hold/copy proof, redaction scans,
  OpenAPI/generated parity when applicable, and manual destructive/provider/
  privacy/production evidence. This document supplies none of that runtime
  acceptance.

## 6. Lifecycle Transition Matrix

### 6.1 Transition, State Delta, And Read Effect

| Row ID | Record/action | Internal transition and authority | Object vs link delta | Ordinary read and mutation effect |
| --- | --- | --- | --- | --- |
| `FILE-LC-OBJ-001` | File reservation — not user-visible separately | Service checks actor/account availability before constructing the reservation, then attempts no row → `pending` without predicating the save on continued eligibility. Disablement or profile soft deletion can race that check and still commit the object/audit. Initial save may commit without returning ID. Even after returned success, each handler immediately reloads metadata by ID; cancellation/query failure there loses the committed ID. | Object only; no link. Either failure path can strand `pending` plus start audit. | No ordinary reads. Reconciliation only; unkeyed retry creates another reservation. Future acceptance needs an acceptance-time or atomically bound actor-eligibility predicate. |
| `FILE-LC-OBJ-002` | Upload reports **Added** only after linkage succeeds | Current service accepts `pending|upload_failed → active`; current endpoints use `pending → active` after provider write. | Object only at this step; link is a later separate write. | Object becomes potentially usable, but no ordinary subject read until an active authorized link exists. |
| `FILE-LC-OBJ-003` | Upload reports bounded failure only if compensation returns | Current service accepts `pending → upload_failed`. Upload handlers ignore a returned unsuccessful compensation result, but a cancellation/other exception from the unguarded compensation propagates with transition commit unknown and no bounded failure response. The local provider may already have created zero-length, partial, or complete bytes at a key that `CreateNew` will not overwrite. | Object only if the transition commits; link remains absent. | All ordinary reads blocked. Missing response is not proof the object stayed `pending` or became `upload_failed`. Do not retry a provider write in place: reconcile complete readable bytes for activation or use a separately authorized new target plus old-copy disposition. Failure is not disposal proof. |
| `FILE-LC-OBJ-004` | **Quarantine file** / not yet user-visible | Proposed API/storage-policy transition from an intake or active state to existing `quarantined`; exact sources allowed are unresolved. | Object only; links/history stay. | Ordinary metadata/content/preview reads stop. Bounded maintenance/security inspection only under a separate role/policy; mutations limited to review/reject/release. |
| `FILE-LC-OBJ-005` | **Release from quarantine** | Proposed authorized `quarantined → active` after fresh validation, privacy, integrity, subject, and policy checks. | Object only; every link remains independently revalidated. | Does not itself grant a read or reactivate a link. Ordinary reads resume only through an active authorized compatible link. |
| `FILE-LC-OBJ-006` | **Move active file to Trash** / logical object removal | Current service accepts `active → deleted`; current subject removes invoke it. The service can technically accept `quarantined → deleted`, but that is not approved here as a rejection policy. | Object changes; link may or may not change in a separate row. | Ordinary metadata/content/preview reads stop for every link. Retained evidence remains maintenance/audit-only. No physical delete. |
| `FILE-LC-OBJ-010` | **Reject quarantined file** / bounded unavailable outcome | Documented decision row from `quarantined` to a target state that remains unresolved under `FILE-LC-CHOICE-003`. The current generic service's ability to mark `quarantined → deleted` is not proof of accepted rejection authority or semantics. | Object decision only; links/history remain blocked and unchanged unless separately authorized. | Ordinary reads remain blocked. Show only an approved safe rejected/unavailable result; do not call it Trash, deletion, or disposal while the target is unresolved. |
| `FILE-LC-OBJ-007` | **Restore file** | Proposed `deleted → active` plus clearing `DeletedAtUtc`, only after the checks in section 7 and explicit acceptance of every link already marked active. | Object only. Inactive links remain inactive; any already-active link must be enumerated and independently accepted or the object restore blocks. | Restore preview may expose bounded safe metadata only after requester authorization. Content resumes only through links explicitly accepted as active; a chosen inactive receipt/supporting/proof/QR link still requires `LINK-004`, `LINK-013`, `LINK-008`, or `LINK-009`. |
| `FILE-LC-OBJ-008` | Internal metadata tombstone transition — never claim **Permanently deleted** | Current service accepts `deleted|upload_failed → purged`, writes metadata/audit only, and does not call provider delete or minimize the row. Original filename, content type, byte size, SHA-256, provider object key, vault-key reference, retention text, owner, purpose, and timestamps remain. | Object status changes; links, bytes, and every other metadata field are not proved changed. | Ordinary reads remain blocked. Further mutation must treat provider and dependency outcome as unknown. Field-by-field tombstone retention/redaction/removal is unresolved; partial implementation must not be presented as completed disposal or minimization. |
| `FILE-LC-OBJ-009` | **Purge retained logically removed file** | Future API-owned orchestration accepts only an eligible `deleted` authoritative object, authorizes provider deletion after retention/dependency/hold proof, records the bounded provider result, and accepts the terminal metadata/tombstone state. | Retained bytes/provider outcome plus object marker; links/history remain retained or separately transitioned by policy. | Irreversible for the deleted bytes. Metadata/content restore is unavailable; bounded tombstone/history remains where required. This is not immediate failed/staging cleanup. |
| `FILE-LC-LINK-001` | **Attach receipt image** | Current bill API creates an active `ExpenseBillAttachment` after earlier bill mutation authorization/status checks, object activation, `receipt_image` purpose/image-media validation, and duplicate-pair checks. It does not reload authorization/status at link acceptance or enforce an attachment-count capacity. Audit staging can throw before the guarded save and strand an active unlinked object; response reload can throw after a committed link. | Receipt bill link only; object remains `active`. | A concurrent bill finalization/archive or group-access loss can make the earlier check stale and still allow link commit. Missing response can mean pre-save staging failure or committed success, so retry can duplicate. Authorized bill viewers may later read; OCR review/assignment and revision JSON can retain receipt-specific dependencies. |
| `FILE-LC-LINK-011` | **Attach supporting file** | Current bill API creates an active `ExpenseBillAttachment` after earlier bill mutation authorization/status checks, object activation, `supporting_attachment` purpose/image-or-PDF validation, and duplicate-pair checks. It does not reload authorization/status at link acceptance or enforce an attachment-count capacity. Audit staging can throw before the guarded save and strand an active unlinked object; response reload can throw after a committed link. | Supporting bill link only; object remains `active`. | A concurrent bill finalization/archive or group-access loss can make the earlier check stale and still allow link commit. Missing response can mean pre-save staging failure or committed success, so retry can duplicate. Current authorized bill viewers may later list/read; no receipt OCR relationship is created. |
| `FILE-LC-LINK-006` | **Add settlement proof** | Current settlement-payment API creates an active `SettlementProofAttachment` after object activation and earlier profile/group authorization, proof-mutation, and payment/request status checks. It does not reload those predicates at link acceptance or enforce a per-payment/actor storage quota or upload-rate limit. Audit or notification staging can throw before the guarded save and strand an active unlinked object; response reload can throw after a committed link. | Proof link only; object remains `active`. | A concurrent membership, payment, or request-status change can make the earlier check stale and still allow link commit. Missing response can mean pre-save staging failure or committed success, so retry can duplicate. Repeated uploads can grow retained provider storage; current authorized proof/payment viewers may later list/read, and link creation never broadens object ownership. |
| `FILE-LC-LINK-005` | **Add QR image** | Current self payment-details API checks profile access before upload, activates an owned `payment_qr` object, then loads an active payment profile without an acceptance-time authorization barrier. If the user profile is deleted in that window and no payment-profile row existed, the filtered lookup returns null and the handler may create a new payment profile linked to the QR after deletion. It otherwise changes an observed `QrFileObjectId == null` without a null-at-commit/version guard. No per-actor storage quota or upload-rate limit exists. Audit staging can throw before the guarded save and strand an active unlinked object; profile reload can throw after committed association/cleanup attempts. | Link only at this step; object remains `active`. The deletion race can create a profile/link after eligibility was revoked. Concurrent initial attaches can each activate an object; last link write wins and the losing object remains active/unlinked because neither observed an old object. | Ordinary reads still require current profile/access predicates, but committed deleted-profile association requires reconciliation. Missing response can mean pre-save staging failure or committed success, so retry can create another object. Repeated attempts can grow retained provider storage; initial attachment grants no generic access, and every operation-created ID requires reconciliation. |
| `FILE-LC-LINK-002` | **Remove receipt image** | Current bill API uses earlier authorization/status observations, sets receipt-link `RemovedAtUtc`, and calls object `active → deleted` in one save boundary without checking for another active subject link to the same file ID. The non-unique file-object index permits that shared reference. Revocation/status change can race acceptance; a cancellation/other thrown save failure can leave link/object/audits committed without success. | Receipt bill link and shared object currently change together if committed, but unlink and object removal remain conceptually distinct. Another active link row can remain unchanged while the shared object becomes `deleted`. | All links to that object become unreadable if committed, including collateral active links whose `RemovedAtUtc` remains null; retry returns unavailable rather than prior success. Future acceptance must enumerate current references and unlink the selected relationship without logically deleting the object until reference-aware policy proves no active link still requires it. OCR review/assignment, notification, and bill-revision snapshot JSON references also prevent physical-disposability inference; whether snapshots require retained bytes or a tombstone is unresolved. |
| `FILE-LC-LINK-012` | **Remove supporting file** | Current bill API uses earlier authorization/status observations, sets supporting-link `RemovedAtUtc`, and calls object `active → deleted` in one save boundary without checking for another active subject link to the same file ID. The non-unique file-object index permits that shared reference. Revocation/status change can race acceptance; a cancellation/other thrown save failure can leave link/object/audits committed without success. | Supporting bill link and shared object currently change together if committed, but unlink and object removal remain conceptually distinct. Another active link row can remain unchanged while the shared object becomes `deleted`. | All links to that object become unreadable if committed, including collateral active links whose `RemovedAtUtc` remains null; retry returns unavailable rather than prior success. Future acceptance must enumerate current references and unlink the selected relationship without logically deleting the object until reference-aware policy proves no active link still requires it. Bill/history and other references prevent physical-disposability inference. |
| `FILE-LC-LINK-007` | **Remove settlement proof** | Current settlement API uses earlier authorization/status observations, sets proof-link `RemovedAtUtc`, and calls object `active → deleted` in one save boundary without checking for another active subject link to the same file ID. The non-unique file-object index permits that shared reference. Revocation/status change can race acceptance; a cancellation/other thrown save failure can leave link/object/audits committed without success. | Proof link and shared object currently change together if committed, but unlink and object removal remain conceptually distinct. Another active link row can remain unchanged while the shared object becomes `deleted`. | All links to that object become unreadable if committed, including collateral active links whose `RemovedAtUtc` remains null; retry returns unavailable rather than prior success. Future acceptance must enumerate current references and unlink the selected relationship without logically deleting the object until reference-aware policy proves no active link still requires it. Payment/settlement history and other references prevent physical-disposability inference. |
| `FILE-LC-LINK-003` | **Remove QR image** | Current DELETE clears `QrFileObjectId` and saves the profile/audit, then attempts old object `active → deleted`. An unsuccessful result is ignored; a thrown cancellation/other exception propagates after that commit. | Link changes from old ID → null first; old-object transition is separate and may fail, succeed, or have an unknown commit outcome without a success response. | Old QR is no longer ordinarily reachable through the profile. An active/unknown unlinked object may remain and becomes a reconciliation candidate, not disposable proof; retry sees the already-null link rather than a prior-result record. |
| `FILE-LC-LINK-010` | **Replace QR image** | Current POST reserves, writes, and activates a new object, stages audit, changes `QrFileObjectId` old → new and saves, attempts old object `active → deleted`, then reloads the profile. Audit staging can throw before the guarded save; cleanup or response reload can throw after the committed swap. No expected-version guard, storage quota, or rate limit protects it. | New object plus QR link change first; old-object transition is separate and may fail, succeed, or have an unknown commit outcome without a success response; logical deletion never proves old bytes removed. | Failed staging/link save can strand the new object; failed/thrown cleanup can leave a committed replacement plus active/unknown old object; reload failure hides committed success. Any missing response followed by unkeyed retry can create another replacement. Repeated or concurrent replacements can grow retained provider storage and last-write-win can strand the losing newly active object because both delete only the shared old object. |
| `FILE-LC-LINK-004` | **Restore receipt image** | Proposed explicit removed → active receipt bill relationship after object readiness, current bill authorization/status, `receipt_image`/image-media checks, and fresh OCR/notification dependency validation. | Receipt bill link only; object restore is separately accepted first. | Bill access resumes only for authorized current bill viewers. OCR review/assignment and notification state are not silently reopened or changed. |
| `FILE-LC-LINK-013` | **Restore supporting file** | Proposed explicit removed → active supporting bill relationship after object readiness, current bill authorization/status, and `supporting_attachment`/image-or-PDF checks. | Supporting bill link only; object restore is separately accepted first. | Bill access resumes only for authorized current bill viewers. No receipt OCR relationship is created, and other links/history remain unchanged. |
| `FILE-LC-LINK-008` | **Restore settlement proof** | Proposed explicit removed → active proof relationship after object readiness and current settlement/payment authorization/status/purpose checks. | Proof link only; object restore is separately accepted first. | Proof access resumes only for the authorized payment/settlement audience. Other links and history remain unchanged. |
| `FILE-LC-LINK-009` | **Reattach QR** | Proposed explicit null/replaced → linked QR relationship after object readiness and current self-profile ownership, visibility, and purpose checks. | QR link only; object restore is separately accepted first. | QR access resumes only under self and settlement-scoped counterparty policies. Cached routes or prior profile state grant nothing. |
| `FILE-LC-PKG-001` | Create process-local backup session | Current API appends actor/session-bound `created` metadata to a process-static dictionary, with no artifact bytes, no `CanAccessProfileAsync` check at this step, and no per-actor/global entry quota or top-level removal sweep. Its OpenAPI description nevertheless says generation/download remain unsupported although mapped follow-up operations implement them. Preview and confirmation creation likewise append to separate static dictionaries without aggregate capacity/removal controls. | Neither file object nor subject link; process-local session metadata only. Repeated creation can accumulate entries and retained references until process restart. | A soft-deleted profile with an active account/session can create, read, cancel, discard, and inspect metadata; actor/session binding is not current-profile authorization. Lazy expiry changes availability but does not remove top-level entries. Future acceptance requires bounded quota/sweep/removal behavior without inferring a numeric limit here. Contract prose is not capability authority. |
| `FILE-LC-PKG-002` | Prepare/reprepare data-only backup artifact | Current API accepts `created|ready_to_download`, checks profile access, then builds data-only JSON bytes and unconditionally publishes/replaces the artifact as `ready_to_download`. The builder's first query filters a deleted profile, but later queries/serialization have no final or atomically bound eligibility check: deletion after that first query can still produce a newly downloadable artifact for the now-deleted profile. Earlier deletion, cancellation, or another query failure can instead escape without a bounded response; `created` remains created, while reprepare preserves the prior ready artifact/actions. Only bounded generation-validation failure is caught. Its OpenAPI description falsely says no artifacts/bytes are created. | Neither; on successful stale or current build, process-local artifact bytes and session state change. On thrown build failure neither changes, including preservation of any prior ready artifact/actions. | Missing response is not a blocked result or proof of no downloadable prior artifact. A successful response is not proof eligibility remained current through publication. Reconcile current authorization, source state, and existing artifact/actions before retry/download. No generic provider read; contract/API/client owners must reconcile the false description. |
| `FILE-LC-PKG-010` | Package preparation is blocked | Current prepare changes `created|ready_to_download → blocked` only for denied profile authorization or caught generation-validation failure. Authorization failure preserves prior artifact/actions; validation failure clears them. Uncaught builder exceptions are not this row and leave the source state/prior artifact under `PKG-002`. | Neither; process-local session state changes and references may remain/clear by handled branch. | Blocked prevents new actions/content. Preserved artifacts can clear only by later lazy artifact expiry; cancel/discard/session expiry do not transition blocked. Hints falsely advertise prepare/cancel/discard and omit create-new-session. Never map a thrown builder to blocked. |
| `FILE-LC-PKG-011` | Direct bill CSV/JSON export response | Current personal/group export authorizes before `LoadRowsAsync`, then builds and returns a one-request CSV or JSON response without a server file object, artifact, session, or expiry mechanism. Personal row visibility predicates creator deletion but not deletion of the requesting participant profile; group rows are not actor-membership-predicated. Personal profile deletion or group revocation can therefore race generation/delivery. Web group readiness/download handlers capture the selected group, but the selector stays enabled and their eventual results are written to shared group-export state without current-group binding. Readiness claims export audit, but the handlers emit no audit event. | Neither; transient response content egresses directly to the client. Builder working graphs are separate `CLN-008` copies; browser results/downloads are separate `LOCAL-010/011` copies. | Personal or group authorization can become stale in flight. A result/download for group A can complete after selecting group B and appear or save under the B display; generic group/date filenames do not expose the mismatch. No export audit proves execution or delivery, and the server has no downloaded-copy inventory or authority after response delivery. |
| `FILE-LC-PKG-012` | Create backup restore preview | Current API accepts any authenticated active account/session without current-profile revalidation. It requires a positive bounded `Content-Length` before reading; missing/chunked and declared-oversized requests are rejected before full-body materialization. It then reads an accepted request string, validates content, and creates actor/session-bound `ready` preview metadata plus a package summary. Unallowlisted `sections[].name` strings enter retained/echoed category lists. Per-candidate nested counts are clamped, but up to 250 candidates are summed without an aggregate cap, allowing response counts above the OpenAPI maximum. Its response falsely says confirmation is unavailable. | Neither; process-local preview/summary entry only. No file object, link, provider write, or restore mutation. | A soft-deleted profile can submit private content and create/read the preview. Accepted request/package string and byte copies have GC-controlled lifetimes; an accepted preview can violate response bounds. Same actor/session may read it after discard/lazy expiry until restart; accepted preview never grants restore authority. |
| `FILE-LC-PKG-013` | Discard backup restore preview | Current API checks only authentication and stored actor/session binding, accepts `ready → discarded`, and retains the entry/summary, including unvalidated caller section-name strings. | Neither; process-local status only. | A soft-deleted profile can read/discard it. A racing confirmation can retain the same summary; terminal responses can echo caller text until restart. |
| `FILE-LC-PKG-014` | Lazy backup restore-preview expiry | Later same-actor/session read/discard/confirmation access changes eligible `ready → expired`, but retains the entry/summary, including unvalidated caller section-name strings. | Neither; process-local status only. | Soft-deleted-profile access remains possible while account/session is active. A racing confirmation can reference the expired preview; expiry is not cleanup or redaction. |
| `FILE-LC-PKG-015` | Create metadata-only restore confirmation session | From an actor/session-bound ready preview, current API creates `metadata_only` confirmation metadata referencing the same summary without current-profile revalidation. It retains a trimmed caller idempotency key without length/safe-token validation; the response exposes only `IdempotencyKeyAccepted`, not the key. Scan-then-insert is non-atomic, so concurrent same-key requests can create different IDs. | Neither; process-local confirmation/summary entry only; it can coexist with a terminal preview and internally retain the private key plus caller section names. | A soft-deleted profile can create/read confirmation responses, but the key itself is not response-visible. `CanApplyRestore` remains false; section names can be echoed until restart, and the retained key is not concurrent retry protection. |
| `FILE-LC-PKG-016` | Discard restore confirmation session | Current API checks only authentication/stored binding, changes `metadata_only → discarded`, and retains the entry/summary plus private caller idempotency key. Responses retain/echo section names and expose only key-accepted boolean/digest, not the key. | Neither; process-local status only. | A soft-deleted profile can read/discard the response; section names remain response-visible and the key remains internal until restart. Discard is neither redaction nor disposal. |
| `FILE-LC-PKG-017` | Lazy restore-confirmation expiry | Later same-actor/session read/discard/idempotency scan changes eligible `metadata_only → expired`, but internally retains the entry, private caller key, digest, summary, and caller section names. Responses expose only key-accepted boolean/digest, not the key itself. | Neither; process-local status only. | Soft-deleted-profile response access remains possible while account/session is active. Expiry stops confirmation use but is not internal key/summary redaction or disposal. |
| `FILE-LC-PKG-018` | Create persisted CSV import review session | Current API authorizes the profile/group (and loads active group members) before reading/planning the CSV, then unconditionally inserts actor/session/scope-bound `ReviewJson` and `CandidateJson` without rechecking or predicating current authorization at save; save can commit without response. GET/discard later match stored account/profile/auth-session only and do not revalidate current profile/group access. Repeated valid requests have no per-actor/global row count, retained-byte quota, rate limit, or sweep; confirmation/discard/lazy expiry retain both JSON fields. | Neither file object nor link; row may exist without returned ID. Repeated individually bounded requests can grow persistent private JSON storage indefinitely. | Profile deletion, group revocation, or membership removal can race creation and still allow a new private row to commit. With active account/session, a soft-deleted profile or revoked group actor can then read private review-derived data, trigger expiry, or discard a known session. Retry/replay creates another retained row. Future acceptance requires approved quota/rate/sweep/removal behavior without inventing a numeric limit. Confirmation separately rechecks current authorization. |
| `FILE-LC-PKG-019` | Confirm persisted CSV import session | Current API sequentially accepts `ready_for_confirmation → confirmed` after challenge and an earlier authorization/group-membership check, then deserializes the stored candidate plan and creates draft bills plus bill audit rows in the same save boundary. For group sessions it reloads active membership but checks only that the actor remains a member; stored split-participant and payer IDs are not revalidated, so removed non-actor members can enter new drafts. It does not rebuild candidates or recheck authorization/reference eligibility at database acceptance. | Neither file object nor subject link; import-session state and authoritative bill-domain rows change. | Actor revocation can race commit, and removed participant/payer references can be accepted from the stale plan. Confirm versus discard/expiry can create bills after terminal observation or leave terminal status after creation. Parallel confirms can conflict. Cancellation/other uncaught save failures can leave confirmation and bills committed without a success response; retry returns non-confirmable, not the prior result. Stored JSON remains. |
| `FILE-LC-PKG-020` | Discard persisted CSV import session | GET/discard use stored binding without current profile/group revalidation. Eligible row becomes discarded after lazy expiry; save can commit without response. | Neither file object nor link; persisted session only. | Soft-deleted/revoked actor with active account/session can discard. Retry may observe terminal without prior-result proof; racing confirm can create bills. JSON remains; GET exposes `ReviewJson`-derived data. |
| `FILE-LC-PKG-021` | Lazy persisted CSV import-session expiry | GET/discard can trigger/save expiry using stored binding without current profile/group revalidation; confirm rechecks authorization later but may save expiry before that check. Save can commit without response. | Neither file object nor link; persisted session only. | Soft-deleted/revoked actor can read private review data and trigger expiry. Retry lacks prior-result proof; racing confirm can create bills. JSON remains; no sweep/disposal. |
| `FILE-LC-PKG-022` | Direct CSV import preflight | Current personal/group preflight incrementally copies a bounded request into a `MemoryStream`, decodes a separate full CSV string, parses it, authorizes the scope, and returns validation/review data without persisting a session or mutating bills. | Neither file object nor subject link; request-memory byte buffer, decoder/text, plan, and response only. | Response data egresses to the client. Byte and character copies can coexist and have no durable ID, retention clock, explicit zeroization, or deterministic disposal beyond GC. Replay reparses current input and authorization state. |
| `FILE-LC-PKG-023` | Direct CSV import | Current personal/group direct import accumulates a bounded request byte buffer and separate decoded CSV string, then parses it and, after earlier authorization plus group-member snapshot validation, creates draft bills plus per-bill import audits in one save boundary without a session or confirmation step. It does not recheck actor authorization or every split-participant/payer reference at database acceptance. | Neither file object nor subject link; authoritative bill-domain/audit rows change while request-memory byte/string/plan copies remain transient. | Actor revocation or removal of a referenced non-actor member can race commit, allowing stale participant/payer IDs into drafts. `DbUpdateException` returns a bounded failure, but cancellation/other thrown save failures have unknown commit outcome. With no idempotency key or source relationship, retry after ambiguity can duplicate a committed import; the coexisting byte and character copies have unproved GC/zeroization timing. |
| `FILE-LC-PKG-024` | Read package readiness and package capability fields | Current authenticated readiness GET returns `Available: false`, `backup_package_unsupported`, and says package creation/download/restore preview/confirmation are unsupported while the same class maps those implemented process-local operations. Create/prepare OpenAPI prose repeats the broader contradiction, and package-session creation, preparation, artifact-status, and download-action responses reuse `UnsupportedPackageArtifactFeatures`, which labels restore preview and confirmation unsupported. | Neither file object nor subject link; bounded readiness/capability response only. | Clients following readiness, runtime capability fields, or descriptive contract prose may suppress implemented operations. These contradictions are not lifecycle authorization grants and must be reconciled across every response surface before clients treat either contract as authoritative availability. |
| `FILE-LC-PKG-003` | Cancel package generation/artifact | Current API uses authentication/stored binding, accepts `created|ready_to_download → cancelled`, and clears artifact/actions without current-profile revalidation. Ready-artifact response copy falsely says no bytes were created/deleted/mutated. The current web `canCancel` condition makes `creating_session|preparing` impossible because they are busy, excludes `created|ready_to_download`, and enables only `blocked`, which the API leaves unchanged; the adapter then unconditionally overwrites mapped status to `cancelled`. | Neither; only API-accepted source states change process-local session/artifact/action state. From the sole web-enabled source, server state remains `blocked` while the browser displays false cancellation. | A soft-deleted profile can cancel through an API client. The web action cannot invoke the API transition and must not be treated as cancellation evidence. Refresh/reconciliation must use returned server state. Concurrent prepare can resurrect API state; reference clearing is not zeroization/copy disposal. |
| `FILE-LC-PKG-004` | Discard unprepared package session | Current API uses authentication/stored binding and accepts only `created → discarded` without current-profile revalidation; no artifact exists at that return boundary. The web card's broad `canDiscard` enables every nonbusy state except `idle|discarded` when session/artifact/action data exists, including `ready_to_download`, `blocked`, `cancelled`, and `expired`. The API leaves all those non-created states unchanged, but the adapter unconditionally relabels each result `discarded`. | Neither; only an accepted `created` source changes process-local session metadata. For every falsely enabled non-created source, server state and any applicable artifact/actions remain unchanged while the browser drops the readout and displays a false terminal state. | A soft-deleted profile can discard an unprepared session. Concurrent prepare can resurrect. A client `discarded` label is neither server disposal nor revocation; refresh/reconciliation must use returned server state, and future UI/API acceptance must align source states before reporting success. |
| `FILE-LC-PKG-005` | Lazy package-session expiry | On later stored-binding access, current API changes expired `created|ready_to_download → expired` and clears artifact/actions without current-profile revalidation. | Neither; process-local session/artifact/action state only. | A soft-deleted profile can trigger/read expiry. Concurrent prepare can resurrect; no eager disposal proof exists. |
| `FILE-LC-PKG-006` | Lazy artifact expiry | On later stored-binding artifact/action/content access after TTL, current API clears the artifact and changes `ready_to_download → expired`; action pruning is separate and no current-profile check precedes it. | Neither; process-local artifact/session state only. | A soft-deleted profile can trigger/read this transition. Content then stops, but TTL is not eager cleanup. |
| `FILE-LC-PKG-007` | Issue package download action | For a current ready artifact, the API creates a new actor/session-scoped, expiring, unconsumed action on every call. The nested dictionary has no per-session count/memory quota or issuance-rate limit; lazy pruning removes only expired or consumed entries. It does not repeat preparation's profile-access check. The action stores no immutable artifact/version identity, survives successful reprepare, and lives in an unsynchronized dictionary. | Neither; process-local action metadata only. Repeated issuance can grow one ready session's memory until entries become prune-eligible, independent of top-level session bounds. | A soft-deleted profile can issue repeatedly. Future acceptance requires a policy-approved nested capacity/rate bound without inventing a number here. An action for artifact A can resolve replacement B; concurrent issue/download/reprepare/cancel/prune can race, throw, or leave inconsistent state. |
| `FILE-LC-PKG-008` | Consume package download action | Current API checks stored actor/session/action plus the session's **current** artifact, then marks one action consumed before returning bytes, without rechecking current profile accessibility, immutable action-to-artifact binding, an atomic compare-and-set, or a lock. A soft-deleted profile can download, and an action created for a prior artifact can return a replacement. | Neither; process-local action flag changes and current artifact bytes egress to the client. | Sequential replay cannot read again, but profile deletion does not stop delivery; cross-version capability drift is possible; concurrent requests can both return bytes or fail during mutation; another action may also read; server lifecycle cannot prove downloaded-copy disposition. |
| `FILE-LC-PKG-009` | Lazy download-action pruning | On later authentication/stored-binding artifact/action/content access, current API enumerates and removes consumed or expired entries without current-profile revalidation from an unsynchronized dictionary. | Neither; process-local action metadata only. | A soft-deleted profile can trigger pruning. Concurrent issue/download/clear/prune can fail; pruning does not affect artifact/downloaded copies. |
| `FILE-LC-CLN-001` | Failed/incomplete upload reconciliation | API-owned maintenance compares metadata step, provider outcome, link commit, retry identity, and holds. It may validate complete bytes for activation/linking or mark cleanup eligible; it cannot assume failed means byte-free. The current local provider cannot overwrite an existing partial target. | Neither until evidence authorizes a specific object/link transition; provider cleanup or a separately authorized new target may follow. | Ordinary reads remain blocked. Never retry an in-place write at the same occupied key or attach different bytes under the same intent unnoticed. |
| `FILE-LC-CLN-002` | Orphan candidate reconciliation | API/domain registry proves all link/history/job/package/copy categories; missing/unreadable evidence yields blocked/unknown, not orphan. | Neither; “candidate” is eligibility only. | No read grant and no deletion. Positively proven disposable material may proceed to a separately gated cleanup/disposal row. |
| `FILE-LC-CLN-003` | Temporary/derivative cleanup | API-approved cleanup targets positively identified non-authoritative scratch or reproducible derivative material after final artifact integrity/link proof. | Temporary bytes may change; authoritative object/link state remains unchanged unless separately accepted. | Must not remove canonical receipt/proof/QR/supporting/package bytes, OCR/history evidence, or the sole readable derivative required by policy. |
| `FILE-LC-CLN-004` | Immediate hard delete of proven non-authoritative bytes | Future API-owned provider operation accepts only positively proven operation-owned scratch, staging, or failed-upload bytes associated with `pending|upload_failed` metadata (or no authoritative object), never a retained `deleted` authoritative object. | Provider bytes change; any object marker is a separate accepted reconciliation transition; links/history stay unchanged. | Irreversible for that temporary/failed copy. It is hard delete/temporary cleanup, not retention purge, and every destructive gate still applies. |
| `FILE-LC-CLN-005` | Server upload request-memory buffers | Current bill, proof, and QR handlers copy multipart input into a file-sized `MemoryStream` backing buffer and then call `ToArray()`, creating a second full managed `byte[]`. The backing buffer and clone coexist until stream disposal; disposal does not zero either array, and the clone remains through validation/provider write. Bill upload allocates them after route-scope authorization but before bill visibility/mutation/status checks, so rejected subjects can still be fully double-buffered. | Neither file object nor subject link; two process-memory copies only. Creating/activating an object—or rejecting before reservation—does not prove immediate buffer disposal. | Not independently readable through an API, but both sensitive copies may remain until GC. No deterministic release, zeroization, audit, or retry identity exists; provider cleanup cannot dispose of either copy. |
| `FILE-LC-CLN-006` | Restore-preview request-memory copies | Current restore-preview creation rejects missing/chunked, zero, and declared-oversized requests before reading. For an accepted bounded request it materializes the request string, extracted package text, and UTF-8 bytes for validation/parsing, then stores preview/summary metadata that includes unallowlisted caller section names. It does not explicitly clear the full-content copies. | Neither file object nor subject link; multiple process-memory copies only. Preview creation, rejection after accepted-body parsing, discard, or expiry does not prove immediate memory disposal or summary redaction. | Raw submitted content is not retained in the preview entry, but caller section names remain private response-visible metadata and accepted full package copies may remain until GC. No deterministic release, zeroization, audit, or cleanup identity exists. |
| `FILE-LC-CLN-007` | Multipart framework temporary staging | Current bill, proof, and QR handlers call `ReadFormAsync` before their later `IFormFile.Length` application checks, and no repository `FormOptions` or equivalent pre-read body bound was found. Framework/host buffering can therefore stage an oversized body or temporary-file copy before Settleora rejects it. | Neither file object nor subject link; framework request staging only. The application per-file limits are not ingress/temp-staging bounds. Object activation, handler rejection/return, or application buffer release does not itself prove immediate temp-file removal after abnormal termination. | Not an application content-read route. Normal request disposal is framework-owned; pre-read capacity, crash/cancellation cleanup, retention, path/configuration and cleanup evidence are not established here. Future acceptance requires an enforced pre-read bound and verified cleanup lifecycle. Treat the location as provider/server-internal and never log or expose it. |
| `FILE-LC-CLN-008` | Direct-export and backup-builder server working copies | Current direct bill export and backup preparation materialize full `ExpenseBill` graphs with every included item, item split, participant, payer, and adjustment before projecting narrower output. Export row and backup bill-count limits do not bound related-row counts, and no aggregate per-bill child/query-result bound was found; sibling includes can multiply database result rows. Direct CSV then builds the complete output in `StringBuilder` storage and a separate immutable `ToString()` result. | Neither file object nor subject link; potentially unbounded query graphs/result rows, projections, CSV builder storage and response string are process working copies only. Response completion or output-field omission does not prove disposal. | A small number of relation-heavy bills can consume excessive database/API memory before projection. Future acceptance requires policy-approved aggregate relation/query-result bounds without inventing numbers here. Private graphs/text remain GC-controlled with no stable ID, clock, zeroization, audit, or cleanup identity. |
| `FILE-LC-CLN-009` | CSV-import request-memory copies | `ReadCsvBodyAsync` incrementally accumulates every accepted preflight, direct-import, or session-creation request in a `MemoryStream` backing array, then decodes it into a separate full `csvText` string before parsing. Session creation additionally re-encodes that string into a full UTF-8 byte array for `ComputePayloadDigest`. The bounded request buffer, character copy, and session-digest byte copy have independent GC lifetimes. | Neither file object nor subject link; request byte buffer, decoder/text, session digest bytes, and later parse/plan copies only. Import completion, rejection, session creation, confirmation, discard, or expiry does not prove immediate memory disposal. | No stable copy ID, retention clock, explicit clear/zeroization, audit, or cleanup identity exists. Each retry allocates another copy set; persisted session JSON and client-selected copies have separate lifecycles. |
| `FILE-LC-LOCAL-001` | Local capture/import scratch **Discard temporary copy** | Mobile camera/gallery/file-picker intake retains a trimmed raw private local path reference plus copied bytes. Local app authority may remove only positively identified picker-owned session scratch/cache after accepted local save or server acknowledgment; a user-owned import is not app-disposable merely because its bytes were copied. | Local-only source material/reference; neither server object nor server link changes. | A browser cache, local path, or queued copy never proves server cleanup or local ownership. OCR may still depend on the path. Failed/offline queues remain until acknowledged or explicitly discarded under reviewed local policy; path values never enter evidence. |
| `FILE-LC-LOCAL-002` | Generate in-memory normalized receipt and thumbnail | File picking requests full bytes, picked-file construction and validation each make an unmodifiable full-byte copy, and the processor makes another mutable full-byte copy before enforcing its compressed-byte limit. Accepted JPEG/PNG/WebP is decoded without a width, height, pixel-count, decoded-memory, or decompression budget, then resized and encoded into normalized/thumbnail arrays; wrapper may retain raw path for OCR. | Multiple sensitive compressed/decoded/resized local buffers plus path-backed source; no server state. Oversized input creates pre-limit copies, while an individually accepted compressed image can allocate an excessive full-pixel buffer before thumbnailing. | Future acceptance requires both an approved pre-allocation byte guard and decoded-dimension/pixel/decompression-memory bounds without inventing numbers here. Accepted/rejected copies remain GC-only; no wipe, identity, ownership proof or deterministic release exists. |
| `FILE-LC-LOCAL-003` | Generate mobile-local backup JSON | Current authenticated server-mode App settings reads client-local configuration plus the entire device-wide sync queue from one unscoped storage key, stamps the current profile ID, builds a backup graph/full `encodedJson`, and retains both. Logout does not clear that queue, so a new account can export prior-account resource IDs, states, or error text. | Client-local process/widget memory within the server-mode boundary; neither server object nor link changes, and no file is written. `buildExport` directly returns a valid bounded preview for the export card. | Current settings may disclose generated data, including stale cross-account queue entries. Pasting the encoded JSON into separate `previewImport` is incompatible because `includesSessionMaterial` is rejected. No clock, stable ID, encryption, audit, wipe, or external-copy inventory exists. |
| `FILE-LC-LOCAL-004` | Read/copy mobile-local backup JSON | Current authenticated server-mode settings renders retained full `encodedJson` in `SelectableText`; because generation reads an unscoped device queue, the view can expose prior-account queue metadata to the current account. File sharing is not wired. | Neither server object nor link; full current/prior-account cached data is disclosed locally and any clipboard/file/share copy leaves app authority. | Visibility ends when the widget/export reference is replaced or disposed, but this does not recall or prove disposal of rendered, clipboard, screenshot, or manually saved copies. |
| `FILE-LC-LOCAL-005` | Replace/release mobile-local backup export | Generating again in authenticated server-mode settings replaces `_latestBackupExport`; leaving/disposal releases the state reference through ordinary Dart lifecycle. There is no explicit clear action, zeroization, or disposition record. | Client-local references may change; neither server object nor link changes and no server record is erased. | The old object graph/string becomes GC-eligible only when no other reference exists. Replacement/disposal is not verified erasure and cannot dispose of external copies. |
| `FILE-LC-LOCAL-006` | Preview pasted mobile-local backup JSON | Current authenticated server-mode dialog retains unrestricted pasted JSON in a `TextEditingController`, calls `jsonDecode`, then recursively traverses the decoded graph for sensitive keys/strings before schema preview. No character, decoded-node, collection-size, structural-depth, or traversal-depth limit was found. Every current `buildExport` document is later rejected because `includesSessionMaterial` matches `session`; exported queue error text can also reject. | Client-local raw string and a potentially large/deep decoded graph only; neither server object/link nor authoritative records change. Allocation or traversal failure can occur before bounded schema preview or sensitive-field rejection. | Future acceptance requires UI/pre-decode size and decoded structure/depth limits without inventing values here. Controller/graph release is GC-only with no stable identity, audit, clock, deterministic wipe, or mutation authority; generation-to-preview interoperability remains broken. |
| `FILE-LC-LOCAL-007` | Download mobile bill attachment content | The generated Dart client first accumulates the full authorized response in a mutable `List<int>`; the server-mode mobile repository then constructs a separate full unmodifiable list, so both file-sized copies coexist during wrapping before the current UI displays only its length. | Two client/device process-memory byte copies; server object/link unchanged. | Server inactivity cannot recall either copy. No stable local copy IDs, retention clock, wipe/disposal audit, cache inventory or proof of release after display. |
| `FILE-LC-LOCAL-008` | Download/select/read web backup package | Current web download creates `Blob`, object URL and user-saved file; restore selection creates browser `File` and calls `File.text()` before any matching client-side size gate, materializing the full string before the server package limit applies. Request construction retains `packageContent`, and the generated client creates another full `JSON.stringify(body)` string that can grow through JSON escaping before `fetch`. The file input ignores confirmation busy state, and preview discard marks itself `discarded` before awaiting, which is not preview-busy. Reselecting backup B can therefore be followed by unconditional results from A's pending confirmation create/refresh/discard or preview discard, reinstalling or accompanying stale A metadata beside B. | Browser memory/object URL and user-controlled saved/selected file copies plus the original package-content and independently GC-controlled serialized-request strings; server package/object/link unchanged. A large selection can allocate both sensitive strings before server rejection. All listed browser result handlers lack current-selection/request-generation binding. | Server TTL/discard cannot revoke these copies or bind a result to the currently displayed file. Future acceptance requires a pre-read client size gate while server validation remains authoritative, bounded request-serialization/copy lifetime, and cancellation or a current-selection/request-generation check before committing every preview/confirmation result. URL revocation, memory/string release, saved-file ownership/deletion, retention clock and copy inventory are unproved. |
| `FILE-LC-LOCAL-009` | Select/read web bill-import CSV | Current web bill-import selection retains browser-selected `File` metadata and calls `File.text()` without first checking `File.size` against the API application limit, materializing the complete contents as a React-state string before preflight or import-session creation. A new selection clears state but does not cancel or generation-guard an older in-flight read; an older read can resolve last and overwrite CSV text while the displayed filename/size remain from the newer file. File, import-scope and group selectors ignore `sessionBusy`; scope/group changes do not clear or rebind the existing session. A pending create/confirm/discard result can therefore reinstall A after any selection change, and even after creation finishes the still-enabled A session can be confirmed while controls display another scope/group. | Browser `File`, full client string, filename/size/scope/group state, review-session/action result and any user-controlled source copy; server import session/business state is separate and neither file object nor subject link changes. A large selection can allocate its complete sensitive JavaScript string before server rejection, and overlapping reads/actions or later scope/group changes can leave content/session identity mismatched with visible controls. | Future acceptance requires a pre-read client size gate plus binding/invalidation for file, scope and group identity before enabling a session action or committing every text/session result (or cancellation) while server validation remains authoritative. Server completion or browser state replacement cannot prove memory wipe or delete the source. Error, navigation, overlapping request, and component-lifetime disposal are GC/browser-owned; no stable copy ID, clock, inventory, explicit clear audit, or deterministic erasure exists. |
| `FILE-LC-LOCAL-010` | Parse/reserialize/download web JSON bill export | The generated web client first materializes the full response text, then `JSON.parse` creates an object graph; the web export surface passes that graph through `JSON.stringify`, producing another full string before constructing a Blob, object URL, and user-saved file. For group readiness/download, the selector remains enabled; handlers capture group A and unconditionally write results into shared state after group B may be selected, and the generic group/date filename does not identify A. | Browser response string, parsed object graph, reserialized string, Blob/object URL, saved file, selected-group display and async result copies; server file object/link state is unchanged. | A readiness result or saved JSON for A can complete under the B display. Require group-selection/request-generation binding or disabling selection until settlement. Request completion and URL revocation cannot prove strings/graph/Blob were wiped or delete the user-saved file. Repeat export creates another copy set; no shared ID, retention clock, inventory, explicit release audit, or deterministic erasure exists. |
| `FILE-LC-LOCAL-011` | Download direct web bill CSV export | The generated client returns a response `Blob`; the browser adapter creates then revokes an object URL and can create a user-saved file. For group readiness/download, the selector remains enabled; handlers capture group A and unconditionally write results into shared state after group B may be selected, and the generic group/date filename does not identify A. | Browser response Blob/object URL, user-controlled saved file, selected-group display and async result; no server `FileObject`, package session, or subject link is created. | A readiness result or saved CSV for A can complete under the B display. Require group-selection/request-generation binding or disabling selection until settlement. Request/download completion and URL revocation cannot prove Blob memory was wiped or delete the saved file. Repeat export creates another copy set; retention/disposal remains bounded future client policy without server recall authority. |
| `FILE-LC-LOCAL-012` | Select/copy mobile existing-bill attachment | The existing-bill attachment picker accepts both `receipt` and `supporting` kinds with `withData: true`; picked-file validation retains complete bytes and generated upload mapping creates another full unmodifiable list. This route does not pass either kind through `ReceiptImageArtifactProcessor` or its size gate before those allocations. | Mobile-memory/source copies only; no server object/link exists until separately authorized upload succeeds. This receipt route is distinct from processed receipt capture/intake under `LOCAL-002`. | A later server rejection is not a mobile allocation bound or disposal result. No approved pre-allocation/pre-upload guard, stable copy ID, clock, audit, wipe, or deterministic release exists; retry can allocate another copy set. |

### 6.2 Preconditions, Re-entry, Dependencies, And Evidence

| Row ID | Preconditions and concurrency | Reversibility/reactivation | Required dependency evidence and blockers | Implementation status / evidence |
| --- | --- | --- | --- | --- |
| `FILE-LC-OBJ-001` | Service observes an active actor/account before constructing the object and requires owner=actor, supported purpose/category, and bounded metadata. Continued eligibility is not predicated at save, so account disablement or profile soft deletion can race and still commit `pending` plus audit. No replay semantics. | Creation is not reactivation; initial save interruption or post-success handler metadata reload failure can leave committed reservation unreachable. | Acceptance-time actor eligibility, subject authorization before reservation except bill buffering, and operation identity. No subject/idempotency/recoverable operation identity exists; `pending`/audit can strand when save result or immediate reload fails. | Partial: service plus handlers/tests. Eligibility check-to-save race, initial non-database/cancellation save, and post-reservation reload outcomes are unreconciled. |
| `FILE-LC-OBJ-002` | Same owner/creator; expected state; provider write intended complete; size/hash/type and subject intent current. Current service does not read bytes before activation. | `upload_failed → active` is source-supported internally but no current endpoint resumes that row; fresh integrity validation is required. | Provider readability/integrity, no conflicting upload, current subject capacity/status, privacy/quarantine policy. | Partial: metadata transition implemented; end-to-end normalization/quarantine/resume absent. Owners #341/#1062/#966. |
| `FILE-LC-OBJ-003` | Expected `pending`; safe bounded failure result. A returned compensation failure may leave `pending`; a thrown compensation failure can leave status/audit commit unknown and no response. Current attempt after a failed `active` transition is invalid and may leave an active object. The local provider may retain an occupied target after a failed/cancelled copy. | Conditional activation only after validating complete readable bytes; otherwise a separately authorized new target plus old-copy disposition or retained cleanup candidate. Never infer state from the response or retry a write in place at the same local-provider key. | Prove compensation commit/state, byte outcome/integrity, target occupancy, link absence, no accepted job/history, retry identity, new-target authority and old-copy holds. | Partial: metadata transition exists; unguarded compensation can throw; provider uses `FileMode.CreateNew`; reconciliation/cleanup/new-target workflow absent. #966/#724. |
| `FILE-LC-OBJ-004` | Validation/scanner/policy reason; authorized API decision; atomic read blocking; safe reason; race with content reads resolved. | Conditional release or logical rejection; not archive. | Links remain; scan/provider unavailable is not “clean”; vault and purpose policy. | Documented-only; state exists. #1062. `FILE-LC-CHOICE-003`. |
| `FILE-LC-OBJ-005` | Current `quarantined`; fresh content validation and scanner status; subject/link/owner still valid; no privacy downgrade. | Conditional to existing `active`; never revives links. | Current bytes/hash/readability, validation policy version, holds, all intended links. | Unimplemented. #1062/#966. |
| `FILE-LC-OBJ-006` | Authorized owner/domain action; expected `active`; current subject dependencies and concurrency. Current service checks owner/creator but not all links. | Conditionally reversible only through `OBJ-007`; bytes retained. | Every link/history reference must inform whether object-wide deletion is safe; current service lacks that proof. | Metadata transition implemented; dependency-safe policy incomplete. #724/#966. |
| `FILE-LC-OBJ-010` | Current `quarantined`; authorized security/privacy decision; validated bounded rejection reason; target, restore posture, notification and retention outcome explicitly approved. Until `CHOICE-003` resolves, no target transition is authorized. | Re-entry depends on the chosen rejection target and fresh security/privacy validation; never inherit ordinary Trash restore automatically. | Scanner/validation evidence, all links/subjects, content risk, owner communication, holds, vault/recovery and chosen retention outcome. | Documented-only. Generic lifecycle service can accept `quarantined → deleted`, but no caller/rejection policy proves that target. #1062/#966. `FILE-LC-CHOICE-003`. |
| `FILE-LC-OBJ-007` | Requester authenticated; object owned/visible; deleted not purged; bytes readable and integral; purpose/privacy/quarantine/retention valid; no duplicate/conflict; optimistic concurrency/idempotency. Enumerate every link already marked active and explicitly revalidate/accept each; any unaccepted link blocks activation. | Conditional `deleted → active` with `DeletedAtUtc → null`; inactive links remain inactive. Blocked by missing bytes, terminal evidence, hold/policy, invalid subject, unsafe content, ownership change, or conflict. | All former/current links, subject statuses, OCR/review history, backups/copies, vault recovery, replacement object, and quota. | Unimplemented; no API/schema/client method. #722 after #961; #723 UX; #724 retention. `FILE-LC-CHOICE-002`. |
| `FILE-LC-OBJ-008` | Current service requires owner/creator and `deleted|upload_failed`; it does not require retention, links, holds, provider outcome, warning, confirmation, or field minimization. | Metadata transition has no restore path and does not prove irreversibility of bytes; it retains original filename, content type, byte size, hash, provider key, vault-key reference, retention text, owner, purpose, and timestamps. | All disposal dependencies and field-specific tombstone rules are missing. Treat current marker as incomplete/unsafe for terminal or minimization claims. | Partial metadata-only implementation; not used by production endpoint. Row-field retention/redaction/removal remains unresolved under #966/#724. |
| `FILE-LC-OBJ-009` | Exact `deleted` authoritative object; positive retention eligibility; complete dependency/copy/hold proof; expected version; separate purge action, audit, retry record and manual destructive approval; explicit confirmation if user-initiated. | Irreversible retained bytes; no reactivation. | Subject/history, OCR, packages, backup/snapshot/replica, vault recovery, provider lease/job, required tombstone and audit. Any unknown blocks. | Unimplemented and manual-gated. #724 must split focused owners after #961/#966. |
| `FILE-LC-LINK-001` | Active compatible `receipt_image` object; uploader and bill mutation authorization/mutable status observed before upload; image media and no duplicate pair. No acceptance-time authorization/status reload, expected version, per-bill/account retained-byte quota, or upload-rate limit. | Link may later be removed; receipt attachment creation is not object creation, and logical removal does not reclaim provider bytes. | Bill ownership/visibility/status at acceptance, retained-byte usage/quota/rate policy, audit-staging/reload, group membership, receipt purpose, links, OCR/notification and revision JSON. | Implemented with stale authorization/status, staging/compensation/unknown response, capacity and history gaps. Repeated uploads can retain active/deleted/failed/stranded objects. #341/#966. |
| `FILE-LC-LINK-011` | Active compatible `supporting_attachment` object; uploader and bill mutation authorization/mutable status observed before upload; accepted image/PDF media and no duplicate pair. No acceptance-time authorization/status reload, expected version, per-bill/account retained-byte quota, or upload-rate limit. | Link may later be removed; supporting attachment creation is not object creation, and logical removal does not reclaim provider bytes. | Bill ownership/visibility/status at acceptance, retained-byte usage/quota/rate policy, audit-staging/reload, group membership, supporting purpose and bill/history links. Receipt OCR is inapplicable. | Implemented with stale authorization/status, staging/compensation/unknown response and capacity gaps. Repeated uploads can retain active/deleted/failed/stranded objects. #341/#966. |
| `FILE-LC-LINK-006` | Active compatible proof object; uploader and earlier settlement/payment proof mutation authorization; eligible status and no duplicate. No acceptance-time reload/predicate, per-payment/actor storage quota, or upload-rate limit exists. | Link may later be removed; proof creation is not object creation, and logical removal is not byte reclamation. | Fresh profile/group/payment/request state, audit/notification-staging and response-reload outcome, proof purpose/ownership, existing links, retained-byte usage, quota/rate policy, and concurrent mutation. Staging can fail before save; reload can fail after commit. | Implemented with stale authorization/status, association-after-activation staging, compensation, committed-without-response, and storage-growth control gaps. #341/#966. |
| `FILE-LC-LINK-005` | Self authorization is observed before upload; newly active owned object has `payment_qr`. The post-activation active-profile lookup is not an authorization recheck: deletion can make it return null, after which a new payment profile/link may be inserted. If a profile is loaded, its QR is observed null. No acceptance-time eligibility, expected-version/null-at-commit guard, per-actor storage quota, or upload-rate limit exists. | Initial link can later be removed/replaced; it is not object creation or restore, and replacement/removal does not prove bytes reclaimed. A deletion race can commit a profile/link after eligibility loss; a racing attach or pre-save audit-staging failure can leave an active unlinked object; response reload can fail after commit. | Current profile eligibility at acceptance, profile/object/purpose/readiness, observed null, retained-byte usage, quota/rate policy, audit-staging/link/reload outcome, visibility, every operation-created ID and concurrent attach. Deletion and concurrent requests can race; missing response can hide committed success. | Implemented with stale authorization plus association-after-activation, staging, concurrency, compensation, committed-without-response, and storage-growth control gaps. #341/#966. |
| `FILE-LC-LINK-002` | Initially observed active readable receipt link, uploader/owner bill mutation rights and mutable bill; those predicates are not reloaded at acceptance. Current object transition lacks multi-reference proof. | Receipt-link re-entry conditional under `LINK-004`; object needs independent `OBJ-007`. Missing response may hide a committed removal. | Current membership/bill status, other bill/file links, OCR review/assignment, notifications, and baseline/proposed revision snapshot JSON `attachmentFileIds` plus `receiptOcrReviewIds`; cancellation/other thrown save outcome. File-byte and OCR-review target-versus-tombstone needs are unresolved. | Implemented with stale-acceptance, unknown-commit/replay, non-FK history-reference and unsafe object-coupling gaps. #966/#724. |
| `FILE-LC-LINK-012` | Initially observed active readable supporting link, uploader/owner bill mutation rights and mutable bill; those predicates are not reloaded at acceptance. Current object transition lacks multi-reference proof. | Supporting-link re-entry conditional under `LINK-013`; object needs independent `OBJ-007`. Missing response may hide a committed removal. | Current membership/bill status, other bill/file/history dependencies and cancellation/other thrown save outcome; receipt OCR is inapplicable. | Implemented with stale-acceptance and unknown-commit/replay gaps; object coupling unsafe for shared references. #966/#724. |
| `FILE-LC-LINK-007` | Initially observed active readable proof link, authorized participant/removal rights and eligible payment/request; those predicates are not reloaded at acceptance. Current object transition lacks multi-reference proof. | Proof-link re-entry conditional under `LINK-008`; object needs independent `OBJ-007`. Missing response may hide a committed removal. | Current membership/payment/request status, other proof/file/history dependencies and cancellation/other thrown save outcome. | Implemented with stale-acceptance and unknown-commit/replay gaps; object coupling unsafe for shared references. #966/#724. |
| `FILE-LC-LINK-003` | Current self profile authorization and a scalar pointer equal to the old QR ID; expected profile/link state. Current old-object deletion result may be ignored, or a thrown failure may follow the committed null/audit. | Later reattach is a separate `LINK-009` action; removal leaves no inactive-link row. Missing success after cleanup is not rollback proof. | Profile state, settlement-scoped visibility, audit-only evidence, other QR references and old-object/operation outcome. | Implemented scalar-link removal; displaced relationship is absent rather than retained inactive, while object reconciliation is partial. #966. |
| `FILE-LC-LINK-010` | Current self authorization and old scalar; new object active before profile load/swap; no expected version, per-actor storage quota, or upload-rate limit. Profile load/audit staging may fail before save; cleanup/reload may fail after commit. | Replacement overwrites old ID without inactive-link row. Missing success is not rollback proof, and logical old-object deletion is not byte reclamation. | New/old IDs from operation context, retained-byte usage, quota/rate policy, staging/link/cleanup/reload outcomes and concurrency. Replacement payment audit records only new ID; failed old deletion may leave no durable old-ID evidence. | Implemented scalar replacement with incomplete displaced-ID evidence and storage-growth controls; missing response can hide active-unlinked staging failure or committed replacement/unknown old-object outcome. #966. |
| `FILE-LC-LINK-004` | Authorized bill requester; exact removed receipt link; object independently active; `receipt_image`/image media compatible; bill mutable/current; privacy/integrity/duplicate checks; idempotency/version. | Conditional explicit receipt re-link. Cached association is irrelevant. | Historical bill link, current bill, replacement links, OCR review/assignment and notification dependencies, copies/holds. | Unimplemented. #722/#723 after #961 plus bill/OCR owners. `FILE-LC-CHOICE-002`. |
| `FILE-LC-LINK-013` | Authorized bill requester; exact removed supporting link; object independently active; `supporting_attachment`/image-or-PDF compatible; bill mutable/current; privacy/integrity/duplicate checks; idempotency/version. | Conditional explicit supporting re-link. Cached association is irrelevant. | Historical bill link, current bill, replacement links, bill/history dependencies and copies/holds; no receipt OCR re-entry. | Unimplemented. #722/#723 after #961 plus bill owner. `FILE-LC-CHOICE-002`. |
| `FILE-LC-LINK-008` | Authorized settlement/payment requester; exact removed proof link; object independently active; proof purpose compatible; payment/settlement state current; privacy/integrity/duplicate checks; idempotency/version. | Conditional explicit proof re-link. Cached association is irrelevant. | Historical proof link, payment/settlement evidence and state, replacement proof, copies/holds. | Unimplemented. #722/#723 after #961 plus settlement owner. `FILE-LC-CHOICE-002`. |
| `FILE-LC-LINK-009` | Authorized self requester; exact profile; object independently active and owned with QR purpose; profile/visibility current; privacy/integrity/replacement checks; idempotency/version. | Conditional explicit QR re-link. Cached route/profile state is irrelevant. | Current/replaced QR, profile visibility, settlement counterparty dependencies, copies/holds. | Unimplemented. #722/#723 after #961 plus payment-details owner. `FILE-LC-CHOICE-002`. |
| `FILE-LC-PKG-001` | Authenticated active account/session; no current-profile check; new opaque session ID appended to a static dictionary. OpenAPI accurately says no bytes at creation but falsely says later generation/download are unsupported. | Creation can later enter discard/cancel/expiry; lazy terminal state does not remove the top-level entry, and restart loses it. | Actor profile/session IDs, current profile eligibility, aggregate package/preview/confirmation entries/references, capacity and sweep/removal. A soft-deleted profile can create/read/manage metadata while account/session stays active. | Implemented in `CreatePackageSessionAsync`; process-local, non-durable, missing current-profile authorization, per-actor/global quota and removal sweep. Repeated sessions can accumulate until restart. #406/#971/#724. `FILE-LC-CHOICE-006`. |
| `FILE-LC-PKG-002` | Same actor/session; current `created|ready_to_download`; profile access checked before build; size-bounded generation. The builder filters profile deletion only in its first query and separate later queries/serialization have no final/atomic eligibility check, so later deletion can still publish ready bytes. Earlier deletion/cancellation/query failure can throw: created stays created; ready reprepare preserves prior artifact/actions. Only validation failure is caught. | Successful artifact reference can clear on cancel/lazy expiry; downloaded copies remain outside authority. A stale successful build publishes ready content; a thrown build has no bounded transition/result. | Current authorization through publication, database reads, artifact/version, source and prior artifact/actions, thrown outcome, actor/session, races, private-data bounds and contract alignment. Separate queries lack a transaction. | Implemented success/validation branches with stale-publication risk; final eligibility/predicate, thrown-build reconciliation, consistent snapshot, durable step/idempotency/audit and eager cleanup absent. OpenAPI is false. #406/#971/#724. `FILE-LC-CHOICE-006`. |
| `FILE-LC-PKG-010` | Same actor/session/source; profile authorization denied or caught generation validation only. Exact handled branch controls prior artifact/actions. Uncaught builder failure stays in `PKG-002`, not blocked. | Blocked has no re-entry; create new session. Preserved artifact may age out only on later artifact/action/content access. | Authorization/validation branch, source/prior artifact/actions and races. Hints falsely advertise prepare/cancel/discard and omit create-new-session. | Implemented handled branches; blocked excludes cancel/discard/session expiry. No durable audit. Never infer blocked from missing response. #406/#971/#724. `CHOICE-006`. |
| `FILE-LC-PKG-011` | Actor authorization is observed before personal/group export row loading. Personal visibility predicates creator deletion but not deletion of the requesting participant profile, while group rows are not actor-membership-predicated; either profile deletion or membership revocation can race generation/delivery. Web group handlers also capture a group without binding the eventual result to the still-selected group. Response generation must succeed. | Server response is request-scoped; client copy cannot be reactivated or disposed by server lifecycle. A server-authorized response for A is not a browser binding to the later B selection. | Current personal-profile/group authorization at query and delivery, bill snapshot/visibility, selected-group/request generation, export format, client/download copy and execution audit. No server artifact ID, action, TTL or inventory. Readiness says audit is written, but execution has no audit writer/event. | Implemented server export plus web group result handling; stale personal/group authorization, group-selection/result mismatch, and readiness/runtime audit contradiction remain. No server retention/disposition record. #406/#724. `FILE-LC-CHOICE-006`. |
| `FILE-LC-PKG-012` | Authenticated active account/session, but no current-profile check. A positive declared length within the application bound is required before body read. Whole-package hash is optional; section hashes are unverified, section names are not allowlisted or safely bounded by category, inventory-to-`data` correspondence is unproved, and summed nested record counts are not aggregate-capped to the OpenAPI maximum. | Preview may be discarded/expire; recreation reparses input. | Soft-deleted-profile access, accepted raw request copies, package validity, aggregate count/contract bounds, arbitrary caller section-name strings retained/echoed in summary lists, duplicate requests, and false capability copy. | Partial in `CreateRestorePreviewAsync`; missing profile authorization, aggregate response validation, safe category allowlist/redaction, integrity checks, durable audit/idempotency, and truthful copy. #406/#971/#724. `FILE-LC-CHOICE-006`. |
| `FILE-LC-PKG-013` | Same active account/session and stored binding, but no current-profile check; preview observed `ready`; no lock/version protects discard. | Sequentially terminal, but a racing creator can insert a confirmation; GET remains readable until restart. | Soft-deleted-profile access, racing confirmations, and retained/echoed caller section-name strings; discard removes neither entry nor summary. | Implemented in `DiscardRestorePreviewAsync`; no current-profile barrier, redaction, eager cleanup, or concurrency barrier. #406/#971/#724. `FILE-LC-CHOICE-006`. |
| `FILE-LC-PKG-014` | Later active account/session stored-binding access at/after expiry, but no current-profile check; mutable state has no lock/version. | Sequentially terminal, but a racing creator can insert; GET remains readable until restart. | Soft-deleted-profile access, racing confirmations, retained/echoed caller section names, and no sweep. | Implemented lazily; no current-profile barrier, redaction, or concurrency barrier. #406/#971/#724. `FILE-LC-CHOICE-006`. |
| `FILE-LC-PKG-015` | Same active account/session/stored binding, but no current-profile check; ready preview; optional expectations/key. Omitted expectations do not compare; preview check and key scan/insert are non-atomic. Caller key is trimmed but has no length/safe-token validation. | Confirmation may discard/expire; it never applies restore and may coexist with terminal preview or duplicate same-key confirmation. The key remains internal; response exposes only accepted boolean. | Soft-deleted-profile access, caller section names, internally retained private key, optional expectations, key/request match, expiry and races. | Implemented with missing profile authorization, safe key/summary validation/redaction, atomic idempotency/ready predicate and durable audit. #406/#971/#724. `FILE-LC-CHOICE-006`. |
| `FILE-LC-PKG-016` | Same active account/session/stored binding, but no current-profile check; exact confirmation; lazy expiry first. | Terminal response remains available until restart; internally retained key is not returned, only accepted boolean/digest. | Soft-deleted-profile response access, preview/summary, caller section names, private internal key, duplicate keyed sessions and races. | Implemented without profile barrier, safe key/summary redaction or eager cleanup. #406/#971/#724. `FILE-LC-CHOICE-006`. |
| `FILE-LC-PKG-017` | Later active account/session stored-binding read/discard or matching-key scan at/after expiry, but no current-profile check. | Terminal response remains available until restart; the internally retained key is not returned, only key-accepted boolean/digest. | Soft-deleted-profile response access, preview/summary, caller section names, private internal key/digest records and races; no sweep. | Implemented status-only without profile barrier, internal key/summary redaction or cleanup. #406/#971/#724. `FILE-LC-CHOICE-006`. |
| `FILE-LC-PKG-018` | Profile/group authorization and group membership are observed before request read/planning; fresh IDs. No acceptance-time authorization reload or save predicate exists, so deletion/revocation can race persistence and save may be unknown. Later GET/discard load only stored binding without current authz. Every valid creation inserts retained private JSON without per-actor/global row or byte quota, rate limit, or sweep. | Creation is not import/restore. Known session can later confirm/discard/expire, but none removes the row or JSON; only confirm rechecks current authz. | Actor/session/scope, current authorization at acceptance, creation/save outcome, aggregate row/byte capacity, read/discard authz gap, private JSON/digest/challenge/expiry. | Persisted with stale-creation authorization, no request idempotency, bounded quota/rate/sweep, unknown-save reconciliation, cleanup, read/discard reauthorization or lifecycle audit. Repeated bounded inputs can grow the database indefinitely. #406/#971/#724. `CHOICE-001/006`. |
| `FILE-LC-PKG-019` | Same actor/auth session; exact unexpired `ready_for_confirmation`; matching scope/group/digest/version/challenge; actor authorization/membership observed before acceptance; nonempty stored candidates. Candidates are deserialized, not rebuilt. Group confirmation reloads active members but validates only the actor, not every stored split participant/payer. | Terminal session status plus authoritative draft bills; not file activation/restore. | Stored graph, actor and every money-bearing participant/payer eligibility at acceptance, challenge, bill audit, transaction and duplicate IDs. Actor/non-actor revocation, competing confirm/discard/expiry, or an interrupted save can race the outcome. | Implemented in `ConfirmBillImportSessionAsync`; removed non-actor members can remain in imported candidates. No acceptance predicate, candidate-reference rebuild, concurrency token, or idempotent prior result. Parallel confirms can conflict; cancellation/other non-`DbUpdateException` save failures can be unknown. #406/#971/#724. `FILE-LC-CHOICE-001/006`. |
| `FILE-LC-PKG-020` | Stored actor/session binding only; no current profile/group authz. Lazy expiry then discard without concurrency predicate; save may be unknown. | Terminal sequentially; missing response/retry terminal does not prove outcome; racing confirm can create bills. | Current authorization, retained JSON, save certainty and races. | Implemented with stale authorization, unknown commit, retained payload, no prior-result/audit and commit-order races. #406/#971/#724. `CHOICE-001/006`. |
| `FILE-LC-PKG-021` | GET/discard/confirm can assign expiry without concurrency predicate; GET/discard lack current authz, and save may be unknown. | Terminal sequentially; disposal absent; missing response not proof; racing confirm can create bills. | Current authorization, deadline/status, JSON, save certainty and races. | Implemented lazily with stale read/discard authorization, unknown commit, no clearing/cleanup/prior-result/audit/barrier. #406/#971/#724. `CHOICE-001/006`. |
| `FILE-LC-PKG-022` | Authenticated actor; personal-profile or current group authorization/membership; bounded CSV request and current calculation rules. | No server session or persisted source exists to reactivate; replay is a fresh preflight. | Request-memory CSV/plan, current actor/group membership and returned review data. GC timing and client-copy disposition are unproved. | Implemented in personal/group `import-preflight.csv`; request-scoped, no durable lifecycle audit/retention record. #406/#971/#724. `FILE-LC-CHOICE-006`. |
| `FILE-LC-PKG-023` | Authenticated actor; personal-profile or group authorization/membership and every group split-participant/payer ID are observed before planning; bounded valid CSV; import plan succeeds. No acceptance-time authorization or candidate-reference predicate exists. | Created drafts follow bill lifecycle; the transient source cannot be restored through file lifecycle. | Actor/group authorization and every money-bearing participant/payer at acceptance, request CSV/plan, draft graph/audit, transaction outcome and replay risk. Actor or referenced-member revocation can race save. | Implemented in personal/group `import.csv`; removed non-actor references can survive the earlier member snapshot. Only `DbUpdateException` maps to bounded failure. Cancellation/other thrown save failures are uncaught/unknown; no idempotency key means ambiguous retry can duplicate. #406/#971/#724 and bill-domain owners. `FILE-LC-CHOICE-006`. |
| `FILE-LC-PKG-024` | Authenticated actor and no request body; no package/session identity is consulted. | Read-only response; reconciliation of advertised availability and create/prepare descriptions versus implemented handlers is required before client enablement. | OpenAPI/generated-client readiness and operation contracts, mapped operations, client feature gating and current server authority. | Implemented in `GetPackageReadinessAsync`, but its unavailable/unsupported response and create/prepare descriptions contradict mapped artifact/session/download/preview/confirmation behavior. #406/#971. `FILE-LC-CHOICE-006`. |
| `FILE-LC-PKG-003` | Same active account/session/stored binding, but no current-profile check; current API state `created|ready_to_download`; state unsynchronized across prepare await. Current web enablement cannot reach either accepted source state and enables only `blocked`, which the API leaves unchanged. | Sequential API transition is terminal; racing prepare can resurrect. The web surface must not claim a usable cancellation transition. | Soft-deleted-profile access, artifact/actions, prior artifact, API-versus-web source state, response semantics and concurrent prepare/download. | API implemented without profile barrier; current web control cannot invoke its transition, cancellation is not a reliable prepare barrier, and copy contradicts lifecycle. #406/#971/#724. `FILE-LC-CHOICE-006`. |
| `FILE-LC-PKG-004` | Same active account/session/stored binding, but no current-profile check; current `created`; state unsynchronized across prepare await. | Sequentially terminal; racing prepare can resurrect. | Soft-deleted-profile access, unprepared-state proof and concurrent preparation; ready hints contradict handler. | Implemented without profile barrier or reliable prepare barrier. #406/#971. `FILE-LC-CHOICE-006`. |
| `FILE-LC-PKG-005` | Later active account/session stored-binding access at/after expiry, but no current-profile check; state unsynchronized across prepare await. | Sequentially terminal; racing prepare can resurrect. | Soft-deleted-profile access, clock/status and concurrent prepare/cancel/download; no scheduler. | Implemented lazily without profile barrier or reliable prepare barrier. #406/#971/#724. `FILE-LC-CHOICE-006`. |
| `FILE-LC-PKG-006` | Later active account/session stored-binding artifact/action/content access at/after artifact expiry, but no current-profile check. | Terminal availability for current artifact. | Soft-deleted-profile access, artifact/session clock, actions and concurrent content/reprepare. | Implemented lazily without profile barrier or sweep. #406/#971/#724. `FILE-LC-CHOICE-006`. |
| `FILE-LC-PKG-007` | Same account/session; current `ready_to_download`; artifact not expired. Current profile accessibility is not rechecked. Each call adds to an unsynchronized action dictionary with no per-session count/memory quota or issuance-rate limit, and actions store no artifact/version identity or reprepare invalidation. | Action remains usable until consumed, expiry, cancellation/session expiry, or process loss; profile deletion and artifact replacement are not barriers. Lazy pruning does not bound repeated unconsumed/unexpired issuance. | Current profile/account/session, immutable artifact identity/version, expiry, nested count/memory/rate capacity, and concurrent reprepare/cancel/expiry/content. | Implemented in `CreatePackageDownloadActionAsync`; missing profile revalidation, artifact binding/invalidation, nested capacity/rate control, durable audit/idempotency, atomic single-use, lock and safe collection. #406/#971/#724. `FILE-LC-CHOICE-006`. |
| `FILE-LC-PKG-008` | Same account/session; exact unconsumed/unexpired action and a ready **current session** artifact. Current profile accessibility and action-to-artifact identity are not rechecked, so soft-deleted profiles and cross-version actions can receive bytes. Observation/update is non-atomic and dictionary unsynchronized. | Stored flag is one-way only when mutation succeeds; profile deletion or reprepare does not revoke, an A-issued action can return B, racing reads can both return content, and overlapping mutation may fail. | Current profile/account/session, action-bound artifact/version/expiry and concurrent issue/cancel/reprepare/prune/content; downloaded copy becomes client-controlled. | Implemented in `DownloadPackageContentAsync`; missing profile revalidation and immutable artifact binding; consumed flag precedes response without lock/CAS. #406/#971/#724. `FILE-LC-CHOICE-006`. |
| `FILE-LC-PKG-009` | Later active account/session stored-binding access, but no current-profile check; action consumed/expired; enumeration/removal is unsynchronized. | Successfully pruned action cannot reactivate; concurrent mutation can fail. | Soft-deleted-profile access, clocks and concurrent issue/content/cancel/clear. | Implemented lazily without profile barrier; not byte cleanup or concurrency-safe. #406/#971. `FILE-LC-CHOICE-006`. |
| `FILE-LC-CLN-001` | Stable upload intent/key; provider probe; lease; exact state; safe timeout. Probe occupied local target. | Validate bytes before activation or authorize new target/old-copy disposition. Cleanup only proved non-authoritative. | Reservation save/immediate reload, provider outcome, QR post-activation profile load, audit/notification staging, link save, response reload, compensation/commit certainty, retry/holds/copies. | Unimplemented. Failures at any enumerated step can strand pending/active-unlinked or hide committed links; compensation can throw. #966/#724. `CHOICE-004`. |
| `FILE-LC-CLN-002` | Complete registered-reference query in one consistent snapshot; provider check; no lease/job; no hold/copy; repeat proof immediately before action. | Candidate classification is reversible and non-mutating; later disposition is separate. | Bill/proof/QR; OCR review/assignment/notification; normalized bill-item `SourceReceiptOcrReviewId` and `SourceReceiptOcrReviewLineId`; baseline/proposed bill-revision JSON `attachmentFileIds` and `receiptOcrReviewIds`; import/export/backup manifests, audit/tombstone, vault, jobs, backups/snapshots/replicas. | Unimplemented. Both restrictive normalized bill-item OCR FKs and both non-FK JSON reference classes require inventory, with separate retained-target-versus-tombstone decisions for file, OCR-review and OCR-line history. #966/#724. `FILE-LC-CHOICE-004`. |
| `FILE-LC-CLN-003` | Explicit temporary/derivative classification; parent accepted and integral; reproducible or no longer required; bounded age policy; no lease/hold. | Scratch/derived deletion conditional and irreversible for that copy; canonical object remains. | Parent/source, current preview requirement, OCR/reprocessing jobs, offline queues, packages/copies. | Mostly documented-only; raw source not retained by default is policy direction, not cleanup runtime. #358/#1062/#966. |
| `FILE-LC-CLN-004` | Exact non-authoritative provider target; source is operation-owned scratch/staging or proved failed bytes with `pending|upload_failed` metadata (or no authoritative object); complete no-link/history/hold/copy proof; lease/version/manual gates. | Irreversible only for the proved temporary/failed copy; never used for retained `deleted` authoritative bytes. | Upload intent, provider outcome, all references/jobs/holds/copies and any required metadata tombstone. Unknown or active/deleted authority blocks. | Unimplemented. #724/#966 must split executor/acceptance ownership. `FILE-LC-CHOICE-001`, `FILE-LC-CHOICE-004`, `FILE-LC-CHOICE-008`. |
| `FILE-LC-CLN-005` | Current multipart upload and handler process lifetime; no independent buffer identity, lease, or wipe control exists. Bill upload performs a file-sized `MemoryStream` copy plus `ToArray()` clone before bill subject visibility/mutation/status checks. | Stream disposal drops one owner but does not zero the backing buffer or clone; rejecting an unavailable/immutable subject is not an explicit lifecycle transition. Retry allocates another two-copy set. | Route-scope versus subject authorization boundary, request/form/file stream, both managed buffers, validation, reserved object if any, provider write, cancellation and handler completion. Provider cleanup does not reach either managed copy. | Implemented double-buffering in bill/proof/QR upload handlers; bill pre-subject-check allocation plus deterministic release/zeroization and memory-copy audit are gaps. #341/#1062/#966/#724. `FILE-LC-CHOICE-004`. |
| `FILE-LC-CLN-006` | Authenticated restore-preview request with a positive declared length within the application bound; missing/chunked and declared-oversized requests are rejected before body read. Accepted requests create request/package strings, UTF-8 conversion and parsing copies without independent identities. | Dropping handler-local references is not a verified lifecycle transition; parsing rejection and preview discard/expiry affect neither GC timing nor retained-summary redaction. | Declared/request byte relationship, request body, package text/bytes, private section-name metadata, parser/validation lifetime, preview creation/rejection outcome and GC behavior. | Implemented in `CreateRestorePreviewAsync`; safe summary validation/redaction, deterministic release/zeroization and memory-copy audit are absent. #406/#971/#724/#966. `FILE-LC-CHOICE-006`. |
| `FILE-LC-CLN-007` | Multipart request reaches bill/proof/QR `ReadFormAsync` before the later per-file length check; framework/host form parsing can stage an oversized body or temporary-file copy before application rejection, with no repository-proved pre-read bound. | Framework request disposal or application rejection is not an application lifecycle transition; crashes/cancellation make exact cleanup timing and evidence unknown. | HTTP ingress/request/form lifetime, framework temp-file behavior/configuration, later application limit, application buffer, reservation/provider write, cancellation and process termination. Never expose the temp path. | Current framework staging is possible because handlers use `ReadFormAsync` first and no repository `FormOptions`/equivalent override was found; future work needs enforced pre-read bounds and verified cleanup. Deterministic crash cleanup/audit is unproved. #341/#1062/#966/#724 plus deployment/config owner. `FILE-LC-CHOICE-004`. |
| `FILE-LC-CLN-008` | Direct export and backup preparation load `WithBillDetails` graphs before projection; bill/row-count limits do not bound related rows, no aggregate child/query-result limit was found, and sibling includes can multiply database result rows. Direct CSV also holds complete `StringBuilder` character storage and the separate immutable `ToString()` result. | No user-visible cleanup claim; response completion or output minimization is not working-copy erasure, and a bounded bill count is not a bounded graph. | Full queried bill/item/split/participant/payer/adjustment graphs, aggregate relation/query-result capacity, projections, CSV builder/string coexistence, query/build cancellation and GC. Never log private graph or CSV contents. | Both paths can create excessive relation-heavy process/database working sets and have no stable ID, clock, audit, zeroization or deterministic release. Future acceptance requires aggregate relation/query bounds. #406/#971/#724. `FILE-LC-CHOICE-006`. |
| `FILE-LC-CLN-009` | Every accepted CSV preflight, direct import, and session creation is copied incrementally into a bounded `MemoryStream`, then decoded to a separate full string before parsing. Session creation also re-encodes the string into a full byte array for its digest. | No user-visible cleanup claim; request/session completion or rejection is not verified erasure of the request buffer, decoded characters, digest bytes, or parse plan. | Request byte buffer, decoder/text string and session-digest byte coexistence, parse/plan lifetime, cancellation, GC, persisted session JSON and client source-copy relationship. Never log private CSV content or digest input. | Implemented in `ReadCsvBodyAsync` and `ComputePayloadDigest`; copy identities, deterministic release/zeroization and memory-copy audit are absent. #406/#971/#724/#966. `FILE-LC-CHOICE-006`. |
| `FILE-LC-LOCAL-001` | Local policy, exact scratch identity and ownership class, accepted destination or explicit discard, no queued/OCR/retry dependency. Current picker wrappers retain trimmed raw local path references alongside copied bytes. | Local discard is irreversible for an app-owned scratch copy; a user-owned source is ineligible. Never server restore. | Picker ownership/lifetime, original source, OCR path use, offline queue, local accepted record, server acknowledgment, user intent and device backup/cache policy. Never record the path value. | Local path retention is implemented; `_safeLocalPath` only trims and does not redact path components; ownership/cleanup classification is incomplete. #971/#358/#966. `FILE-LC-CHOICE-007`. |
| `FILE-LC-LOCAL-002` | Picker requests complete bytes; construction, validation, and processor entry copy them before the compressed-byte check. Accepted JPEG/PNG/WebP is decoded without width/height/pixel-count/decoded-memory/decompression bounds; wrapper path may feed OCR. | Rejected oversized input leaves pre-limit copies GC-only; accepted compressed input can exhaust memory during full-pixel decode before resize. Compressed, decoded, resized and encoded arrays have nondeterministic GC/no wipe. | Source ownership, every buffer class, decoded dimensions/pixels/memory budget, draft/OCR/upload references and caller lifetime; no server metadata/derivative ID. Never record path/content. | Implemented with pre-limit copies and unbounded decoded dimensions. Future acceptance needs pre-allocation and decoded-dimension/decompression bounds plus disposal evidence. #358/#966/#971. `CHOICE-005/007`. |
| `FILE-LC-LOCAL-003` | Authenticated server-mode settings context and available service, but the device-wide sync queue uses one unscoped key and survives session logout; no ownership filter or consistent snapshot/version spans configuration/queue reads. | Regeneration is a client-local export, not server restore/authority. It can expose prior-account queue metadata. Its direct `buildExport` preview is valid, while pasting the encoded JSON into separate `previewImport` is guaranteed to fail on `includesSessionMaterial`. | Current/prior account ownership, device queue/configuration, excluded sensitive/session/server URI/file-byte fields, consistency, direct-versus-import preview paths, memory and external-copy policy. | Implemented with cross-account cache and import-preview incompatibility; no file write/share, stable ID, encryption, clock, audit or wipe. #406/#971. `FILE-LC-CHOICE-006/007`. |
| `FILE-LC-LOCAL-004` | Authenticated server-mode settings retains an export stamped for the current user, but its device-wide queue content may belong to prior accounts; the viewer expands/selects full JSON. | Closing/replacing UI releases references but cannot revoke clipboard, screenshot or saved copies; it changes no server authority or queue ownership. | Current/prior account scope, device/viewer privacy, capture/clipboard policy, data classification and manual copies. | Implemented as `SelectableText`; cross-account disclosure is possible, manual copies are outside inventory, and no read audit exists. #406/#971. `FILE-LC-CHOICE-006/007`. |
| `FILE-LC-LOCAL-005` | Authenticated server-mode settings state receives a newly generated export or the widget is disposed; other references/copies are not inventoried. | Old references become GC-eligible only; there is no verified erasure, restoration, external-copy recall, or server-record change. | Current server-mode user/scope, old/new object graphs and strings, rendered/controller/clipboard/manual copies, local holds and user intent. | Implemented reference replacement/widget lifecycle only; no clear command, zeroization, disposition audit or deterministic GC. #406/#971/#724. `FILE-LC-CHOICE-001/006/007`. |
| `FILE-LC-LOCAL-006` | Authenticated server-mode `previewImport` and pasted JSON; controller retains unrestricted raw text, `jsonDecode` allocates a graph, and recursive sensitive scans have no character/node/collection/depth/traversal bound before schema preview. Generated exports are later rejected via `includesSessionMaterial`; queue error strings can also reject. | Closing releases references only; allocation or recursive traversal can fail before bounded preview. Preview-only creates no restore/apply authority. | Raw/decoded copies, character/node/collection/depth budgets, traversal outcome, clipboard/input method, filter/schema compatibility and future restore gates. | Implemented without pre-decode or structural-depth limits; controller/graph disposal is GC-only, no stable ID/audit/clock exists, restore disabled. #406/#971/#724. `FILE-LC-CHOICE-006/007`. |
| `FILE-LC-LOCAL-007` | Authorized server-mode mobile attachment content request; generated client materializes a mutable full list and repository wrapping creates a second full unmodifiable list before UI display. | View may release references; not server unlink/delete and not verified wipe of either buffer. | Server authorization at request, both local byte-copy identities/lifetimes, generated-client/repository/widget/cache references and device-copy policy. | Implemented two-buffer download/display; no local IDs, audit, clock, zeroization or disposal proof. #971/#966/#724. `CHOICE-007`. |
| `FILE-LC-LOCAL-008` | Authorized web package content/selected file; browser APIs create Blob/object URL/saved file/File/text, restore calls `File.text()` before any matching client-side size gate, request construction retains `packageContent`, and the generated client creates another full `JSON.stringify(body)` request string. The enabled input permits reselection during confirmation creation, whose handler has no current-selection/request-generation guard. | URL revocation/reference release cannot delete user-saved files or prove either full string/memory copy was wiped; later server rejection is not a browser allocation bound and never grants server disposal/restore authority. A confirmation is not proven to belong to the displayed selection. | Actor/package authorization, Blob/URL/File/original-text/serialized-request identities and amplification, confirmation-result selection/request binding, pre-read size evidence, download/selection time, browser/device ownership and external copies. | Implemented web package download/restore readout; future acceptance needs a pre-read client gate, bounded serialization/copy lifetime, and cancellation or current-selection/request-generation validation before applying confirmation results while preserving server authority. No common inventory, retention clock, revoke/wipe/saved-file disposition audit. #406/#971/#724. `CHOICE-006/007`. |
| `FILE-LC-LOCAL-009` | Authenticated web bill-import selection reads a browser `File` completely into React state before preflight/session creation and without checking `File.size` against the API application limit. Selection/read handlers have no generation/current-file guard. File, scope and group selectors ignore `sessionBusy`; scope/group callbacks neither clear nor rebind an existing session. Every session action applies its result without validating the current file/scope/group identity. | **Select CSV for review** only; later server rejection, clearing/replacing client state, or completing a request cannot be described as an intake bound, source-file deletion, verified wipe, or server-session disposal. Text/session identity can differ from filename, scope or group controls, including confirmation after a post-create selector change. | Browser File/full strings/filename/size/scope/group, review-session/action result, selection/request generation, overlapping ordering and user source ownership; no shared identity binding, retention clock, copy inventory, release result, audit, or deterministic wipe. Server records remain separately authoritative. | Future acceptance needs a pre-read client gate plus session invalidation or immutable file/scope/group binding before enabling actions or committing every text/session result. Failures/navigation/concurrency and browser GC remain unproved; user source survives. #406/#971/#724. `CHOICE-006/007`. |
| `FILE-LC-LOCAL-010` | Authorized web JSON bill export; generated client uses full `response.text()` then `JSON.parse`, and `createJsonExportBlob` uses `JSON.stringify` before Blob/object-URL download. | **Export JSON** only; request completion or download completion must not imply browser copies were wiped or a saved file was deleted. | Response text, parsed object graph, reserialized text, Blob/object URL and saved file have no shared identity, retention clock, copy inventory, release result, audit, or deterministic wipe. Server response state is separate. | Implemented web JSON export/download. URL revocation can release a browser handle but cannot prove memory erasure or saved-file deletion; retry creates another copy set. #406/#971/#724. `CHOICE-006/007`. |
| `FILE-LC-LOCAL-011` | Authorized direct web CSV bill export returns a response Blob; the browser adapter creates/revokes an object URL and can leave a user-saved file. | **Export CSV** only; never call it a server `FileObject` or treat request/download completion or URL revocation as browser-memory wipe or saved-file deletion. | Response Blob/object URL/saved file and user/device ownership; no shared ID, retention clock, copy inventory, release result, audit, deterministic wipe, or server disposal authority. | Implemented direct CSV download. Future client retention/disposal must be bounded without claiming recall of user-saved copies; repeat creates another copy set. #406/#971/#724. `CHOICE-006/007`. |
| `FILE-LC-LOCAL-012` | Mobile existing-bill attachment picker accepts receipt or supporting kind with `withData: true`; validation retains complete bytes and upload mapping creates another full unmodifiable list without routing through the receipt processor's size gate. | **Select receipt/supporting attachment** only; later server rejection is neither a pre-allocation/pre-upload bound nor local disposal proof. This does not replace the separately processed receipt-intake row. | Source ownership/path, selected kind, each full-byte copy and picker/repository/upload lifetime; no stable ID, approved capacity policy, clock, audit, zeroization, or deterministic release. | Implemented mobile existing-bill receipt/supporting intake. Future acceptance needs an approved pre-allocation/pre-upload guard without inventing a numeric limit here; server authority cannot dispose client copies. #358/#971/#966/#724. `CHOICE-007`. |

### 6.3 Retention, Disposition, Audit, Follow-Up, And Gates

| Row ID(s) | Retention / hard delete / purge | Audit and privacy | Follow-up lane and manual gates |
| --- | --- | --- | --- |
| `OBJ-001..003`, `CLN-001` | `pending`/`upload_failed`: transient candidates with unresolved clock. Upload/activation failure, audit/notification staging, association save, compensation, or post-commit response reload can throw. Outcomes include active-unlinked, unknown state/commit, or a committed live link without success response. Hard delete remains blocked pending positive classification; purge is separate. | Record exact step, staged side effects, link/compensation/reload outcome, actor, file ID, purpose, states, reason, correlation/idempotency and response/commit certainty; no content/provider internals. | #341/#1062/#966/#724. Storage/privacy, schema, API/client, provider/config and destructive gates pending. |
| `OBJ-004..005`, `OBJ-010` | Quarantine retention and rejection target/outcome required but duration unresolved. Neither quarantine nor rejection authorizes Trash or disposal. | Record enter/release/reject/blocked/scan-unavailable categories without scanner/provider details or contents. No current audit proof. | #1062 canonical upload-security owner; #966 reconciliation. Security/storage/privacy, retention, UX and provider gates pending. |
| `OBJ-006..007`, `LINK-002..004`, `LINK-007..010`, `LINK-012..013` | Dependency-gated Trash/replacement; clocks unresolved. Bill/proof removal can be stale/unknown and can mark a shared object `deleted` while another active link row remains, making the collateral link unreadable. Revision JSON blocks file/OCR disposition. Replaced QR relation is absent, not inactive; unlike removal, replacement payment audit records only new ID, so failed old deletion may leave no durable displaced-old-ID evidence. Hard delete/purge ineligible. | Record each transition, authz/result certainty, safe IDs, complete active-reference decision and blocked reason. Removal audit identifies old QR; replacement audit does not. Neither is enumerable inactive-link history. | Existing owners/gates remain separate; future unlink/object-state acceptance must be reference-aware. |
| `OBJ-008` | Current metadata marker is not disposal proof; it changes no provider bytes and does not minimize the row. Tombstone/marker and individual retained-field durations are unresolved, but this metadata-only transition does not itself require physical-delete or provider-operation approval. | Current success audit records bounded state metadata; retained filename, type, size, hash, provider key, vault-key reference, retention text and ownership data require independent minimization/redaction decisions and must not enter audit evidence. Missing dependency/reason/version evidence prevents terminal wording. | #724/#966 own marker semantics, per-field tombstone retention/minimization, and reconciliation; storage/privacy, schema/API and retention/audit gates remain pending. Destructive/provider gates apply only if later byte work is added, not to this current marker row. |
| `CLN-002` | Orphan-candidate classification is non-mutating and never disposal proof. It may proceed only with a complete fresh reference/provider evidence set; no physical deletion occurs in this row. | Require proof-set version, bounded reference/outcome categories, blocked reason, actor, time and correlation. Exclude provider identifiers/diagnostics. | #724/#966 own proof-registry/completeness reconciliation; #467/#964 may own redacted maintenance presentation. Destructive approval applies only to a later cleanup/disposal row. |
| `OBJ-009` | Physical deletion is dependency-gated and destructive/manual; retained tombstone/history duration remains unresolved. | Require attempted/outcome audit, prior/new bounded state, byte outcome category, proof-set version, retry result, blocked reason, actor, time and correlation. Exclude provider identifiers/diagnostics. | #724 owns policy split; #966 owns completeness/duplicate reconciliation; #467/#964 may own redacted maintenance presentation. Destructive, storage/privacy, admin exposure, provider/config, schema/API and production-operation gates remain pending. |
| `LINK-001`, `LINK-005`, `LINK-006`, `LINK-011` | Retain with active subject/history. No hard delete/purge from attach. Audit/notification staging can fail after activation but before save; response reload can fail after commit. | Current attach audits use bounded metadata, but staging failure can strand active-unlinked objects and reload failure can hide a live link. Public DTO tests exclude internals. Self QR content reads lack access audit; bill attachment lists, proof lists, and self payment-details/QR metadata reads are also unaudited. Audited content reads do not prove delivery. | #341/#966 plus bill/OCR/settlement/payment owners. Runtime storage/privacy, retry/result and metadata/content-read audit gates remain. |
| `PKG-001..017` | TTLs are availability checks; terminal entries retain summaries. Static package/preview/confirmation dictionaries have no per-actor/global entry or aggregate-memory quota and no top-level removal sweep, so repeated creation can accumulate entries/references until restart. Only preparation revalidates profile access. Thrown package builds preserve source state/prior ready artifact/actions without a bounded result. Summaries retain/echo caller section names. Confirmation sessions internally retain unvalidated private idempotency keys (responses expose only accepted boolean), and same-key calls can duplicate. Direct export can use stale personal-profile or group-membership authorization and has no execution audit despite readiness claiming one. Download actions can drift to a replacement artifact. None proves disposal. | Actor/session binding is not current-profile authorization. Distinguish internally retained key text from response-visible metadata. Require policy-approved bounded capacity, sweep/removal, key length/safe-token validation, allowlisted/redacted summary categories, atomic idempotency, immutable action/artifact binding and truthful export-audit status; never log private keys/names/content. | #406/#971/#724/#966 plus API/OpenAPI/client owners for authorization, capacity/lifetime, build failure/retry, idempotency, validation/redaction, audit parity and capability drift. |
| `PKG-018..024` | Review/candidate JSON remains after terminal states. Every valid creation adds another retained row with no per-actor/global count, byte quota, rate limit, or sweep, so repeated bounded inputs can grow persistent storage indefinitely. Creation/confirmation/discard/expiry may commit without response. GET/discard use stored binding without current profile/group revalidation, so private review reads/status mutation survive deletion/revocation while account/session active; confirmation separately rechecks. Expiry is availability, not disposal. | Missing aggregate capacity/rate/removal controls, current authorization, lifecycle audit/correlation/prior-result identity. Never log CSV/JSON/private rows/digests/challenges/content. | #406/#971/#724 plus bill/money owners. API/privacy/authz, persistence/capacity, money, audit/retention and ingress gates pending. |
| `CLN-003..009`, `LOCAL-001..012` | Generation/buffering/download is non-destructive. Temporary hard delete needs positive proof. Memory/staging/package-builder/raw-path/decoded-pixel/mobile-backup/mobile-download/browser Blob/URL/File/text/object-graph copies, including upload double buffers, private builder graphs, JSON export parse/reserialize copies, direct CSV export copies, web pre-limit file-text reads, and mobile existing-bill receipt/supporting picker copies, lack disposal proof; device queue can cross accounts. Server lifecycle never cleans authoritative/history or proves client-copy disposal. | Local/request/framework/browser/mobile audit, ownership, bounded intake/capacity and disposal are unresolved; exclude paths, keys, content/private package data. | #358/#971/#406/#1062/#724/#966 plus platform owners; privacy/destructive/local-data gates pending. |

For every row, hard-delete status is `ineligible` for ordinary actions and
`unresolved` for the narrowly described future cleanup/disposition case.
Purge/disposal status is `unresolved` and blocked, except that it is
`ineligible` where the row is merely attach, activate, quarantine, release,
restore, re-link, or candidate classification. A user-initiated future terminal
action requires a separate consequence warning and explicit confirmation;
automated temporary cleanup must be non-user-initiated and policy-bounded.

### 6.4 Per-row Unresolved-choice References

Every transition has an explicit choice set. `none` would mean current
authority answers all row questions; no row qualifies for `none` at this
planning checkpoint.

| Row ID | Unresolved choice refs |
| --- | --- |
| `FILE-LC-OBJ-001` | `FILE-LC-CHOICE-004` |
| `FILE-LC-OBJ-002` | `FILE-LC-CHOICE-003`, `FILE-LC-CHOICE-004` |
| `FILE-LC-OBJ-003` | `FILE-LC-CHOICE-004` |
| `FILE-LC-OBJ-004` | `FILE-LC-CHOICE-003` |
| `FILE-LC-OBJ-005` | `FILE-LC-CHOICE-003` |
| `FILE-LC-OBJ-006` | `FILE-LC-CHOICE-001`, `FILE-LC-CHOICE-002`, `FILE-LC-CHOICE-004`, `FILE-LC-CHOICE-008` |
| `FILE-LC-OBJ-007` | `FILE-LC-CHOICE-001`, `FILE-LC-CHOICE-002`, `FILE-LC-CHOICE-003`, `FILE-LC-CHOICE-008` |
| `FILE-LC-OBJ-008` | `FILE-LC-CHOICE-001`, `FILE-LC-CHOICE-004`, `FILE-LC-CHOICE-008` |
| `FILE-LC-OBJ-009` | `FILE-LC-CHOICE-001`, `FILE-LC-CHOICE-004`, `FILE-LC-CHOICE-008` |
| `FILE-LC-OBJ-010` | `FILE-LC-CHOICE-003` |
| `FILE-LC-LINK-001` | `FILE-LC-CHOICE-004` |
| `FILE-LC-LINK-002` | `FILE-LC-CHOICE-002`, `FILE-LC-CHOICE-004` |
| `FILE-LC-LINK-003` | `FILE-LC-CHOICE-002`, `FILE-LC-CHOICE-004` |
| `FILE-LC-LINK-004` | `FILE-LC-CHOICE-002` |
| `FILE-LC-LINK-005` | `FILE-LC-CHOICE-004` |
| `FILE-LC-LINK-006` | `FILE-LC-CHOICE-004` |
| `FILE-LC-LINK-007` | `FILE-LC-CHOICE-002`, `FILE-LC-CHOICE-004` |
| `FILE-LC-LINK-008` | `FILE-LC-CHOICE-002` |
| `FILE-LC-LINK-009` | `FILE-LC-CHOICE-002` |
| `FILE-LC-LINK-010` | `FILE-LC-CHOICE-004` |
| `FILE-LC-LINK-011` | `FILE-LC-CHOICE-004` |
| `FILE-LC-LINK-012` | `FILE-LC-CHOICE-002`, `FILE-LC-CHOICE-004` |
| `FILE-LC-LINK-013` | `FILE-LC-CHOICE-002` |
| `FILE-LC-PKG-001` | `FILE-LC-CHOICE-006` |
| `FILE-LC-PKG-002` | `FILE-LC-CHOICE-006` |
| `FILE-LC-PKG-003` | `FILE-LC-CHOICE-006` |
| `FILE-LC-PKG-004` | `FILE-LC-CHOICE-006` |
| `FILE-LC-PKG-005` | `FILE-LC-CHOICE-006` |
| `FILE-LC-PKG-006` | `FILE-LC-CHOICE-006` |
| `FILE-LC-PKG-007` | `FILE-LC-CHOICE-006` |
| `FILE-LC-PKG-008` | `FILE-LC-CHOICE-006` |
| `FILE-LC-PKG-009` | `FILE-LC-CHOICE-006` |
| `FILE-LC-PKG-010` | `FILE-LC-CHOICE-006` |
| `FILE-LC-PKG-011` | `FILE-LC-CHOICE-006` |
| `FILE-LC-PKG-012` | `FILE-LC-CHOICE-006` |
| `FILE-LC-PKG-013` | `FILE-LC-CHOICE-006` |
| `FILE-LC-PKG-014` | `FILE-LC-CHOICE-006` |
| `FILE-LC-PKG-015` | `FILE-LC-CHOICE-006` |
| `FILE-LC-PKG-016` | `FILE-LC-CHOICE-006` |
| `FILE-LC-PKG-017` | `FILE-LC-CHOICE-006` |
| `FILE-LC-PKG-018` | `FILE-LC-CHOICE-001`, `FILE-LC-CHOICE-006` |
| `FILE-LC-PKG-019` | `FILE-LC-CHOICE-001`, `FILE-LC-CHOICE-006` |
| `FILE-LC-PKG-020` | `FILE-LC-CHOICE-001`, `FILE-LC-CHOICE-006` |
| `FILE-LC-PKG-021` | `FILE-LC-CHOICE-001`, `FILE-LC-CHOICE-006` |
| `FILE-LC-PKG-022` | `FILE-LC-CHOICE-006` |
| `FILE-LC-PKG-023` | `FILE-LC-CHOICE-006` |
| `FILE-LC-PKG-024` | `FILE-LC-CHOICE-006` |
| `FILE-LC-CLN-001` | `FILE-LC-CHOICE-001`, `FILE-LC-CHOICE-004` |
| `FILE-LC-CLN-002` | `FILE-LC-CHOICE-001`, `FILE-LC-CHOICE-004` |
| `FILE-LC-CLN-003` | `FILE-LC-CHOICE-001`, `FILE-LC-CHOICE-005`, `FILE-LC-CHOICE-007` |
| `FILE-LC-CLN-004` | `FILE-LC-CHOICE-001`, `FILE-LC-CHOICE-004`, `FILE-LC-CHOICE-008` |
| `FILE-LC-CLN-005` | `FILE-LC-CHOICE-004` |
| `FILE-LC-CLN-006` | `FILE-LC-CHOICE-006` |
| `FILE-LC-CLN-007` | `FILE-LC-CHOICE-004` |
| `FILE-LC-CLN-008` | `FILE-LC-CHOICE-006` |
| `FILE-LC-CLN-009` | `FILE-LC-CHOICE-006` |
| `FILE-LC-LOCAL-001` | `FILE-LC-CHOICE-007` |
| `FILE-LC-LOCAL-002` | `FILE-LC-CHOICE-005`, `FILE-LC-CHOICE-007` |
| `FILE-LC-LOCAL-003` | `FILE-LC-CHOICE-006`, `FILE-LC-CHOICE-007` |
| `FILE-LC-LOCAL-004` | `FILE-LC-CHOICE-006`, `FILE-LC-CHOICE-007` |
| `FILE-LC-LOCAL-005` | `FILE-LC-CHOICE-001`, `FILE-LC-CHOICE-006`, `FILE-LC-CHOICE-007` |
| `FILE-LC-LOCAL-006` | `FILE-LC-CHOICE-006`, `FILE-LC-CHOICE-007` |
| `FILE-LC-LOCAL-007` | `FILE-LC-CHOICE-007` |
| `FILE-LC-LOCAL-008` | `FILE-LC-CHOICE-006`, `FILE-LC-CHOICE-007` |
| `FILE-LC-LOCAL-009` | `FILE-LC-CHOICE-006`, `FILE-LC-CHOICE-007` |
| `FILE-LC-LOCAL-010` | `FILE-LC-CHOICE-006`, `FILE-LC-CHOICE-007` |
| `FILE-LC-LOCAL-011` | `FILE-LC-CHOICE-006`, `FILE-LC-CHOICE-007` |
| `FILE-LC-LOCAL-012` | `FILE-LC-CHOICE-007` |

### 6.5 Per-row Manual-gate Mapping And Registry

Each gate is independent. `pending` means this policy supplies no approval;
satisfying one gate never satisfies another. Approval evidence is `none` for
every pending gate.

| Row ID(s) | Applicable gate IDs |
| --- | --- |
| `FILE-LC-OBJ-001..003`, `FILE-LC-LINK-001`, `FILE-LC-LINK-005..006`, `FILE-LC-LINK-011` | `FILE-LC-GATE-001`, `002`, `003`, `006`, `007` |
| `FILE-LC-CLN-001` | `FILE-LC-GATE-001`, `002`, `003`, `005`, `006`, `007`, `010` |
| `FILE-LC-OBJ-004..005` | `FILE-LC-GATE-001`, `002`, `003`, `004`, `006`, `007`, `008` |
| `FILE-LC-OBJ-010` | `FILE-LC-GATE-001`, `002`, `003`, `004`, `006`, `007`, `008` |
| `FILE-LC-OBJ-006`, `FILE-LC-LINK-002..004`, `FILE-LC-LINK-007..010`, `FILE-LC-LINK-012..013` | `FILE-LC-GATE-001`, `002`, `003`, `004`, `006`, `008` |
| `FILE-LC-OBJ-007` | `FILE-LC-GATE-001`, `002`, `003`, `004`, `006`, `007`, `008` |
| `FILE-LC-OBJ-008` | `FILE-LC-GATE-001`, `002`, `003`, `006`, `008` |
| `FILE-LC-OBJ-009` | `FILE-LC-GATE-001`, `002`, `003`, `004`, `005`, `006`, `007`, `008`, `009`, `010` |
| `FILE-LC-CLN-002` | `FILE-LC-GATE-001`, `002`, `003`, `006`, `007`, `008`, `009`, `010` |
| `FILE-LC-CLN-003` | `FILE-LC-GATE-001`, `002`, `003`, `005`, `006`, `007`, `008`, `010` |
| `FILE-LC-CLN-004` | `FILE-LC-GATE-001`, `002`, `003`, `005`, `006`, `007`, `008`, `010` |
| `FILE-LC-CLN-005`, `FILE-LC-CLN-007` | `FILE-LC-GATE-001`, `003`, `006`, `007`, `008`, `010` |
| `FILE-LC-CLN-006`, `FILE-LC-CLN-008`, `FILE-LC-CLN-009` | `FILE-LC-GATE-001`, `003`, `006`, `008`, `010` |
| `FILE-LC-LOCAL-001` | `FILE-LC-GATE-005`, `006`, `011` |
| `FILE-LC-LOCAL-002` | `FILE-LC-GATE-006`, `011` |
| `FILE-LC-LOCAL-003..004` | `FILE-LC-GATE-006`, `008`, `011` |
| `FILE-LC-LOCAL-006` | `FILE-LC-GATE-004`, `006`, `008`, `011` |
| `FILE-LC-LOCAL-005` | `FILE-LC-GATE-005`, `006`, `008`, `011` |
| `FILE-LC-LOCAL-007..012` | `FILE-LC-GATE-005`, `006`, `008`, `011` |
| `FILE-LC-PKG-001`, `FILE-LC-PKG-004`, `FILE-LC-PKG-007..008` | `FILE-LC-GATE-001`, `003`, `004`, `006`, `008` |
| `FILE-LC-PKG-002` | `FILE-LC-GATE-001`, `003`, `004`, `005`, `006`, `008` |
| `FILE-LC-PKG-010` | `FILE-LC-GATE-001`, `003`, `004`, `005`, `006`, `008` |
| `FILE-LC-PKG-003`, `FILE-LC-PKG-005..006` | `FILE-LC-GATE-001`, `003`, `004`, `005`, `006`, `008` |
| `FILE-LC-PKG-009` | `FILE-LC-GATE-001`, `003`, `005`, `006`, `008` |
| `FILE-LC-PKG-011..017` | `FILE-LC-GATE-001`, `003`, `004`, `006`, `008` |
| `FILE-LC-PKG-018..019` | `FILE-LC-GATE-001`, `002`, `003`, `004`, `006`, `008`, `012` |
| `FILE-LC-PKG-020..021` | `FILE-LC-GATE-001`, `002`, `003`, `004`, `006`, `008`, `012` |
| `FILE-LC-PKG-022..023` | `FILE-LC-GATE-001`, `003`, `006`, `008`, `012` |
| `FILE-LC-PKG-024` | `FILE-LC-GATE-001`, `003`, `004` |

| Gate ID | Required scope; owner | Status / approval evidence | Downstream work blocked |
| --- | --- | --- | --- |
| `FILE-LC-GATE-001` | Storage/file privacy and authorization policy/runtime; #966/#341/#1062 with human privacy authority. Required for any affected server behavior. | `pending`; none. | Object/link/read/upload/quarantine/cleanup runtime. |
| `FILE-LC-GATE-002` | Persistence/schema/migration; #722 schema lane after #961. Required when lifecycle fields, constraints, reference registry, or state meaning changes. | `pending`; none. | New restore, disposition, idempotency, reference-proof, and provider-outcome persistence. |
| `FILE-LC-GATE-003` | API/OpenAPI/generated clients; #722 contract/API lanes after #961. Required for any new/changed endpoint or generated surface. | `pending`; none. | Restore, lifecycle status, quarantine, maintenance, cleanup, and disposition APIs/clients. |
| `FILE-LC-GATE-004` | Product Trash/restore wording and Figma; #723 with human product/UX approval. Required for user-visible inactive/restore/terminal action. | `pending`; none. | Client Trash, restore, blocked-state, warning, and confirmation UI. |
| `FILE-LC-GATE-005` | Destructive cleanup/disposal decision; human product/data owner and operator, with #724 technical evidence. Required before physical byte, process-artifact reference, or local authoritative-copy deletion. | `pending`; none. | `OBJ-009`, `CLN-004`, and any destructive `CLN-001..003`/`PKG-002/003/005/006/009/010`/`LOCAL-001/005` outcome. |
| `FILE-LC-GATE-006` | Retention, dependency, hold, tombstone, and audit policy; #724 with human trust/privacy decisions. Required for clocks, eligibility, or evidence disposal. | `pending`; none. | Expiry, orphan proof, cleanup scheduling, restore eligibility, and terminal disposition. |
| `FILE-LC-GATE-007` | Provider/configuration and operational behavior; deployment/operator owner after storage policy. Required for provider probes, deletion, leases, or scanner integration. | `pending`; none. | Provider reconciliation, cleanup executor, scanner, and physical outcome proof. |
| `FILE-LC-GATE-008` | Privacy-vault/security/key/recovery behavior; #343/#966 and human security/privacy authority. Required when protected content or recovery is affected. | `pending`; none. | Vault-file restore, quarantine inspection, cleanup, copy disposition, and no-downgrade acceptance. |
| `FILE-LC-GATE-009` | Admin/public exposure; #466/#467/#964 with human exposure approval. Required for maintenance/admin read or action surfaces. | `pending`; none. | Admin lifecycle metadata, blocked-attempt, cleanup, and disposition controls. |
| `FILE-LC-GATE-010` | Production maintenance/deployment operation; operator/human release authority. Required before live cleanup or purge execution. | `pending`; none. | Scheduled/manual production cleanup, byte deletion, and provider reconciliation. |
| `FILE-LC-GATE-011` | Local platform storage/privacy and user-discard behavior; #971/#358 with human platform/privacy authority. Required for local scratch/cache/offline deletion. | `pending`; none. | Local-only cleanup, offline acknowledgment, secure-cache retention, and device acceptance. |
| `FILE-LC-GATE-012` | Bill calculation and money-domain mutation authority; bill/money owners plus human money-policy approval. Required for any change to import candidate calculation, validation, or authoritative draft-bill creation. | `pending`; none. | `PKG-018`, `019`, `022`, `023` calculation/review/import mutation behavior. |

### 6.6 Per-row Authority, Presentation, Metadata, And Operation Mechanics

This matrix makes the remaining #960 fields explicit per transition. A stated
missing mechanism is an unresolved blocker, not a proposed runtime default.

| Row ID | Decision authority and source / implementation evidence | User-visible wording and surface | Persisted metadata applicability and evidence | Idempotency key; replay; retry result; concurrency conflict |
| --- | --- | --- | --- | --- |
| `FILE-LC-OBJ-001` | Authority: storage rules/metadata architecture. Evidence: lifecycle service actor/account check before object construction plus upload-handler immediate reloads/tests. The save is not predicated on continued eligibility. | Not separately visible; upload remains in progress. An earlier availability check is not acceptance proof. | If committed: object/timestamps/start audit. Missing acceptance-time actor eligibility and operation identity. Disablement/deletion can race commit; initial save may commit without ID; subsequent handler `SingleAsync` can fail after returned success and likewise lose ID. | No key; retry creates another ID. Each path can strand pending/audit; no version/serialization. `CHOICE-004`. |
| `FILE-LC-OBJ-002` | Authority: metadata architecture upload pattern. Current evidence: `MarkActiveAsync` and bill/proof/QR upload handlers/tests. | **Added** only after the subject link commits, on the invoking bill/proof/payment-details surface; activation alone has no success label. | Applicable/current: status/updated time and completion audit. Missing: provider-write outcome, validation policy/version, link result, concurrency version. | No key; replay after `active` is invalid; retry result is not prior success; racing fail/delete is selected by database save/order, not an explicit conflict contract. `CHOICE-003/004`. |
| `FILE-LC-OBJ-003` | Authority: metadata architecture failure pattern. Current evidence: `MarkUploadFailedAsync`; handlers ignore returned failure but do not guard a thrown compensation exception. | Bounded **Upload failed** only when the endpoint returns it; missing response is an unknown compensation outcome, never **Deleted** or **Safe to retry**. | Applicable/conditional: status/audit may commit even when cancellation/another exception propagates. Missing: failure reason, byte outcome, response/commit certainty, retry identity/version. | No key; replay after a committed failure mark is invalid, while uncommitted/unknown outcome differs. Reconcile rather than infer state or replay provider write. `CHOICE-004`. |
| `FILE-LC-OBJ-004` | Authority: file policy plus #1062; current enum/schema only, no transition implementation. | Proposed **Unavailable while file safety review is pending** on a future subject/status surface; no preview/download. | Applicable/required: entered time/actor or system source, safe reason, policy/scanner category, prior state and version. No current fields/event prove these. | Required key/lease unresolved; replay/retry must return current quarantine result; concurrent read/release/delete must fail closed. `CHOICE-003`. |
| `FILE-LC-OBJ-005` | Authority: file policy/#1062 release policy; no runtime evidence. | Proposed **File is available again** only on the authorized subject after link revalidation; no standalone generic file surface. | Applicable/required: release actor/result, policy/validation version, integrity outcome, prior/new state and version. Absent. | Required key unresolved; same-result replay expected but not implemented; retry must not rescan/activate different bytes silently; racing delete/requarantine conflicts. `CHOICE-003`. |
| `FILE-LC-OBJ-006` | Authority: shared taxonomy and metadata architecture. Current evidence: `MarkDeletedAsync` and subject removal handlers/tests for `active → deleted`; this row does not approve quarantine rejection. | Proposed object-wide **Move active file to Trash** only on a future approved lifecycle surface; current subject surfaces say remove. | Applicable/current: `deleted`, `DeletedAtUtc`, update time and lifecycle audit. Missing: safe reason, dependency proof, link set, version. | No key; replay after `deleted` is invalid; current retry is not prior success; concurrent link/replace/delete has no explicit conflict/version rule. `CHOICE-001/002/004/008`. |
| `FILE-LC-OBJ-010` | Authority: upload security/privacy choice #1062/#966; current evidence proves only that the generic service can accept `quarantined → deleted`, not that this is an approved rejection outcome. | Future bounded **File rejected** or **Unavailable** on an approved subject/status surface; never **Moved to Trash** or **Deleted** until target/wording is decided. | Applicable/required: security decision actor/source, bounded reason/category, validation policy/version, source state, approved target, retention/restore treatment and audit. Absent. | Required decision key/version unresolved; identical replay returns prior rejection result; changed target/reason conflicts; release/delete/hold races block. `CHOICE-003`. |
| `FILE-LC-OBJ-007` | Authority: shared restore taxonomy and metadata architecture; no runtime/API/client evidence. | **Restore file** on future authorized Trash/subject surface, with blocker text; never silent. | Applicable/required: requester, reason, restored time, `Status=active`, cleared `DeletedAtUtc`, byte/integrity/privacy/purpose checks, complete active-link acceptance set, selected inactive-link intent, version. Absent. | Required operation key and expected version unresolved; identical replay returns prior result; changed payload or link set conflicts; purge/replacement/link races block. `CHOICE-001/002/003/008`. |
| `FILE-LC-OBJ-008` | Authority for current fact: `MarkPurgedAsync`/lifecycle test; policy authority: shared taxonomy says marker is not disposal. | Not user-visible; maintenance must say **Disposition not verified**, never **Permanently deleted** or **Metadata minimized**. | Applicable/current: status/update time and success audit; all other row fields remain, including original filename, type, size, hash, provider key, vault-key reference, retention text, owner, purpose and timestamps. Missing: provider call/outcome, dependency/retention proof, per-field tombstone/minimization rules, confirmation, reason, version. | No key; replay after marker is invalid; retry does not verify bytes or minimize metadata; concurrent links/holds/provider state are unchecked. `CHOICE-001/004/008`. |
| `FILE-LC-OBJ-009` | Authority: shared purge taxonomy, metadata/file policy and #724; `DeleteAsync` exists but has no production caller. | Future **Permanently delete retained file** on an approved Trash/maintenance surface, only from `deleted`, with consequence warning and explicit confirmation when user-initiated. | Applicable/required: exact deleted object, durable operation/lease, retention/proof-set/policy versions, actor/approval, reason, provider outcome category, attempts, disposed time, tombstone. Absent. | Required key/expected deleted-object version; exact replay resumes/returns prior outcome; changed target/proof conflicts; link/hold/restore races revalidate immediately before provider work. `CHOICE-001/004/008`. |
| `FILE-LC-LINK-001` | Authority: bill/OCR domain and metadata architecture. Evidence: receipt handler/entity/OCR tests and revision snapshots; per-file validation has no per-bill/account retained-byte quota or upload-rate limit. | **Attach receipt** only after response; missing response is unknown, and accepted status does not mean retained storage is bounded. | Current if committed: receipt link/audit. Missing acceptance-time authz, quota/rate decision, operation/result identity, version/capacity and reference registry; logical removal retains bytes. | No key; retry/repeated uploads can retain active, deleted, failed, stranded or duplicate objects. Revocation/finalization races. `CHOICE-004`. |
| `FILE-LC-LINK-011` | Authority: bill domain/metadata architecture. Evidence: supporting handler/entity/tests; per-file validation has no per-bill/account retained-byte quota or upload-rate limit. | **Add supporting file** only after response; missing response is unknown, and accepted status does not mean retained storage is bounded. | Current if committed: link creator/time/audit. Missing acceptance-time authz, quota/rate decision, operation/result identity, version/capacity and atomic step; logical removal retains bytes. | No key; retry/repeated uploads can retain active, deleted, failed, stranded or duplicate objects. Revocation/finalization races; receipt OCR inapplicable. `CHOICE-004`. |
| `FILE-LC-LINK-005` | Authority: self payment-details. Evidence: QR handler, active-profile helper, FK/audit/tests. Initial authorization precedes upload; the post-activation profile lookup filters deleted users but is not a recheck and may lead to new profile creation. No actor storage quota/rate limit exists. | **Add QR image** only after response; missing response is unknown. Never imply profile eligibility was current at association acceptance or storage growth is bounded by lifecycle policy. | Current if committed: winning pointer/update/audit. Profile deletion can race into a new linked payment profile. Audit staging can throw before save; profile reload can throw after commit. Missing acceptance predicate, quota/rate decision, link version/idempotency, atomic result and losing-object reconciliation. | No key; retry can create another object after staging failure or committed-without-response. Deletion and concurrent null observations can race; concurrent attaches last-write-win and strand losing object; repeated attempts retain more bytes. `CHOICE-004`. |
| `FILE-LC-LINK-006` | Authority: settlement/payment/metadata architecture. Evidence: proof handler/entity/tests; no per-payment/actor storage quota or upload-rate limit. | **Add proof** only after response; missing response is unknown, pre-upload predicates are not acceptance checks, and accepted status does not mean retained storage is bounded. | Current if committed: link/time/audit/notification. Audit/notification staging can throw before save; reload can fail after commit. Missing accepted authz/status, quota/rate decision, operation/result identity, version/idempotency and atomic step. | No key; retry can duplicate after active-unlinked staging failure or committed-without-response; composite key covers only same pair. State can race and repeated distinct proofs retain more bytes. `CHOICE-004`. |
| `FILE-LC-LINK-002` | Authority: bill/OCR domain plus shared unlink rule. Current evidence: receipt removal and bill-revision snapshot materialization/tests; authorization/status are not reloaded before final save. | **Remove receipt** on the exact bill surface; must not say bytes are deleted or missing response means unchanged. | Applicable/current if committed: receipt-link `RemovedAtUtc`, removal audit, object deletion metadata/audit. Baseline/proposed snapshot JSON may retain `attachmentFileIds` and `receiptOcrReviewIds` without direct file or review FKs. Missing acceptance-time authorization, response record, link version and complete proof. | No key; revocation/status change can race; thrown save may commit without success; replay is unavailable. File-byte and OCR-review targets each require an explicit retained-target versus sufficient-tombstone decision before disposition. `CHOICE-002/004`. |
| `FILE-LC-LINK-012` | Authority: bill domain plus shared unlink rule. Current evidence: supporting removal handler/tests; authorization/status are observed before the final lifecycle save and not reloaded. | **Remove supporting file** on the exact bill surface; must not say bytes are deleted or missing response means unchanged. | Applicable/current if committed: supporting-link `RemovedAtUtc`, removal audit, object deletion metadata/audit in one save. Missing: acceptance-time authorization, response record, link actor/reason/version and complete reference proof. | No key; revocation/status change can race commit. Cancellation/other thrown save can commit then return no success; replay is unavailable. Receipt OCR does not apply. `CHOICE-002/004`. |
| `FILE-LC-LINK-003` | Authority: payment-details/visibility policy. Current evidence: self QR DELETE handler/tests. | **Remove QR image** on self payment details. | Applicable/current: scalar pointer becomes null, so the old relationship is absent rather than an inactive link row; profile update time and removal audit commit before old-object transition. Audit is the only retained displaced-link evidence. An unsuccessful result is ignored; a thrown failure yields no success and unknown object outcome. Missing: enumerable link lifecycle timestamp/version and reconciliation operation identity/outcome. | No key; ordinary replay sees null and returns no-content, not the prior result. A thrown post-commit cleanup cannot safely be inferred failed or retried as the original operation; concurrent replace/remove is last accepted save without explicit version conflict. `CHOICE-002/004`. |
| `FILE-LC-LINK-010` | Authority: payment-details/visibility. Evidence: QR POST/tests; no actor storage quota or upload-rate limit. | **Replace QR image** only after response; missing response unknown, and logical replacement does not mean old bytes were reclaimed. | If committed: pointer/update and payment audit containing only new QR ID; old relation absent. Profile load/audit staging can throw before save; cleanup/reload after. Failed old deletion may leave no durable old-ID evidence. Missing quota/rate decision, operation/version/outcomes. | No key; retry or repeated replacement can create and retain more objects. Concurrent replaces strand losing new object. Preserve operation old ID for reconciliation; audit cannot reconstruct it. `CHOICE-004`. |
| `FILE-LC-LINK-007` | Authority: settlement/payment domain plus shared unlink rule. Current evidence: proof removal handler/tests; membership/payment/request status are observed before final lifecycle save and not reloaded. | **Remove proof** on the exact settlement-payment surface; must not say bytes are deleted or missing response means unchanged. | Applicable/current if committed: proof-link `RemovedAtUtc`, `settlement.proof_removed` audit, object deletion metadata/audit in one save. No removal notification. Missing acceptance-time authorization, response record, link actor/reason/version and complete reference proof. | No key; revocation/status change can race commit. Cancellation/other thrown save can commit then return no success; replay is unavailable rather than prior result. `CHOICE-002/004`. |
| `FILE-LC-LINK-004` | Authority: shared restore/unlink taxonomy plus bill/OCR policy; no runtime evidence. | **Restore receipt** on a future bill/Trash surface after file restore. | Applicable/required: bill/file/receipt purpose, requester/reason, restored time, source/target bill-link state, expected version, fresh OCR dependency result and separate audit. Absent. | Required key/version; identical replay returns same receipt link; changed bill/file conflicts; OCR/replacement/object/bill-state races block. `CHOICE-002`. |
| `FILE-LC-LINK-013` | Authority: shared restore/unlink taxonomy plus bill policy; no runtime evidence. | **Restore supporting file** on a future bill/Trash surface after file restore. | Applicable/required: bill/file/supporting purpose, requester/reason, restored time, source/target bill-link state, expected version and separate audit. Absent. | Required key/version; identical replay returns same supporting link; changed bill/file conflicts; replacement/object/bill-state races block. Receipt OCR re-entry is inapplicable. `CHOICE-002`. |
| `FILE-LC-LINK-008` | Authority: shared restore/unlink taxonomy plus settlement/payment policy; no runtime evidence. | **Restore proof** on a future payment/Trash surface after file restore. | Applicable/required: payment/file/proof purpose, requester/reason, restored time, source/target proof-link state, expected version and separate audit. Absent. | Required key/version; identical replay returns same proof link; changed payment/file conflicts; replacement/object/payment-state races block. `CHOICE-002`. |
| `FILE-LC-LINK-009` | Authority: shared restore/unlink taxonomy plus payment-details policy; no runtime evidence. | **Reattach QR** on a future self payment-details/Trash surface after file restore. | Applicable/required: profile/file/QR purpose, requester/reason, restored time, source/target profile-link state, expected version and separate audit. Absent. | Required key/version; identical replay returns same QR link; changed profile/file conflicts; replacement/object/profile-state races block. `CHOICE-002`. |
| `FILE-LC-PKG-001` | Authority/current evidence: API local-backup package contract and `CreatePackageSessionAsync`; it authenticates account/session but does not call `CanAccessProfileAsync`, and inserts into a process-static dictionary without entry quota/removal. Preview and confirmation creation do the same in separate dictionaries. | **Create backup session** on the authenticated backup surface; do not repeat the OpenAPI claim that generation/download are unsupported, imply profile soft deletion revokes current access, or call lazy expiry aggregate cleanup. | Process-local/current: opaque session ID, actor profile/session IDs, `created`, created/expiry times, plus aggregate entry/reference capacity. No durable audit or `FileObject`; no per-actor/global quota or top-level sweep. Contract prose omits implemented follow-up capability. | No key; a soft-deleted profile can create/read/manage metadata. Replay creates another session and aggregate entries can grow until restart; concurrent sessions coexist. Future policy must define bounded quota/sweep/removal without a fabricated number. API/OpenAPI/client owners must reconcile authorization, capacity and descriptive capability. `CHOICE-006`. |
| `FILE-LC-PKG-002` | Authority/evidence: `PreparePackageSessionAsync`, builder/tests and contradictory OpenAPI. Initial profile authorization plus the builder's first profile query are not bound to final publication. | **Ready to download** only after response; never imply eligibility remained current through publication. A thrown build is unknown, not blocked/success. | On success, including a stale successful build: status/artifact/version/times/counts. On earlier deletion/cancellation/query race: no bounded result; created remains, or prior ready artifact/actions remain. Missing final eligibility evidence, durable audit and consistent snapshot. | No key; retry may rebuild while prior artifact/actions remain downloadable. Preserve source/prior-artifact state and thrown category; races unversioned. Reconcile current authorization before download and OpenAPI. `CHOICE-006`. |
| `FILE-LC-PKG-010` | Authority/evidence: handled authorization/validation branches plus action hints. Uncaught builder failure is `PKG-002`, not blocked. | **Preparation blocked** only with returned bounded code; never infer it from missing response. Hints are not authority. | Blocked/code; authorization failure preserves prior artifact/actions, validation clears. No audit/branch-neutral disposition. Hints contradict handlers. | Repeated calls return current blocked without transition; no re-entry. Preserved artifact identity/races unversioned; lazy artifact expiry alone can clear. `CHOICE-006`. |
| `FILE-LC-PKG-011` | Authority/current evidence: `ExpenseBillExportEndpoints` personal/group CSV/JSON responses, personal visibility query, and readiness metadata. | **Export CSV/JSON** only after current personal-profile or group authorization at row load/delivery; do not promise server expiry or claim readiness's advertised audit occurred. | No persisted lifecycle metadata or server artifact. Response rows/content are built per request; requesting participant-profile deletion or group-membership revocation can become stale; execution audit, client-copy inventory and disposal outcome are absent. | No key/artifact ID; replay creates another response/copy; profile deletion, membership revocation, or source data can change in flight or between requests; delivery/client retention outcome is unknown. `CHOICE-006`. |
| `FILE-LC-PKG-012` | Authority/current evidence: `CreateRestorePreviewAsync`, response mapping and preview dictionary; it authenticates account/session but does not call `CanAccessProfileAsync`. Missing/chunked, zero and declared-oversized requests return before body read; accepted requests create full-content working copies, and per-candidate clamping is followed by uncapped aggregate summation. | **Preview backup restore**; explicitly metadata-only and not restore. Do not repeat its false confirmation unavailable/unsupported fields or omitted create-confirmation hint, imply profile soft deletion revokes access, or call returned aggregate counts OpenAPI-bounded. | Process-local/current: preview/actor/session IDs, `ready`, codes/times and package summary. Submitted raw package content is not stored in the entry, but accepted request/package copies are GC-controlled, arbitrary unallowlisted `sections[].name` strings are retained/echoed, and aggregate nested counts can exceed contract maxima; no durable audit/removal record. Whole-package comparison is optional; declared section hashes are not verified, and section names/inventory are not reconciled to `data`. | No key; a soft-deleted profile with active account/session can submit private content and create/read previews. Accepted aggregate counts can violate the contract. Replay creates another preview; duplicate/concurrent previews coexist. API/OpenAPI/client owners must reconcile aggregate validation, authorization and confirmation/capability copy. Mandatory trusted digest plus safe-category validation/redaction is required before acceptance. `CHOICE-006`. |
| `FILE-LC-PKG-013` | Authority/current evidence: `DiscardRestorePreviewAsync`, confirmation creation and preview GET; these use stored actor/session binding without current-profile revalidation. | **Discard restore preview**; never claim summary data was erased, unreadable, profile-deletion-inaccessible, or a racing confirmation was prevented. | Process-local/current: `discarded`, code/time, and retained summary including arbitrary caller section names. Entry/summary remain readable and a racing confirmation can retain the same summary; no durable audit. | A soft-deleted profile with active account/session can read/discard. Sequential confirmation after discard is unavailable, but a creator that observed ready can insert afterward. GET returns terminal mapping until restart; no lock/version makes discard a barrier. `CHOICE-006`. |
| `FILE-LC-PKG-014` | Authority/current evidence: `ExpireRestorePreviewIfNeeded`, confirmation creation and preview GET; these use stored actor/session binding without current-profile revalidation. | **Restore preview expired** only after later access detects the deadline; never claim summary read ended, caller text was redacted, profile deletion revoked access, or a racing confirmation was prevented. | Process-local/current: status/code change; entry/summary, including arbitrary caller section names, remain readable and a racing confirmation can retain the same summary. No expiry event time, sweep, removal or durable audit. | A soft-deleted profile with active account/session can trigger/read expiry. Sequential creation after expiry is unavailable, but a creator that observed ready can insert afterward. GET returns terminal mapping until restart; no lock/version makes expiry a barrier. `CHOICE-006`. |
| `FILE-LC-PKG-015` | Authority/evidence: `CreateRestoreConfirmationSessionAsync`; stored binding, no profile revalidation. | **Confirm restore intent** / metadata-only; never imply restore, atomic ready state, safe caller metadata, or key disclosure. | Process-local: IDs/scope/label/expectations, internally retained trimmed caller key, digest/status/times and summary. Response returns only `IdempotencyKeyAccepted`, not key. No key length/safe-token validation, applied mutation or durable audit. | Soft-deleted profile can use response. Omitted guards do not compare; scan/insert and preview-ready are non-atomic, allowing distinct same-key IDs or terminal preview coexistence. `CHOICE-006`. |
| `FILE-LC-PKG-016` | Authority/evidence: confirmation discard/GET; stored binding, no profile revalidation. | **Discard restore confirmation**; never claim summary/internal key erased, caller names redacted, or profile deletion revoked response access. | Discarded/code/time plus retained summary and private internal key. Response returns section names and key-accepted boolean/digest, not key. No audit. | Soft-deleted profile can read/discard response. Repeat/GET terminal until restart; same-key create/read/expiry uses mutable state. `CHOICE-006`. |
| `FILE-LC-PKG-017` | Authority/evidence: confirmation expiry/GET/key scan; stored binding, no profile revalidation. | **Expired** only after access detects deadline; never claim summary/internal key redacted or profile deletion revoked response access. | Status/code changes; entry, internal private key/digest and summary remain. Response exposes accepted boolean/digest and summary, not the key. No sweep/removal/audit. | Soft-deleted profile can access response. Matching create returns terminal state; duplicate same-key records may exist; ordering unversioned. `CHOICE-006`. |
| `FILE-LC-PKG-018` | Authority/evidence: import-session create/load methods/entities/tests. Profile/group authorization and membership are checked before CSV read/planning, not revalidated or predicated at save. | **Review CSV import** after response only; never imply missing response did not persist, creation authority was current at persistence, or stored binding equals current authorization. | If committed: actor/account/session/scope/status/digest/challenge/counts/expiry/private JSON. GET exposes review-derived data using stored binding without current profile/group check. No lifecycle audit/recoverable identity or acceptance-time authorization evidence. | No key; deletion/revocation can race creation, and an unknown save can strand a row. A known row remains readable/actionable after profile deletion/group revocation while account/session active. `CHOICE-001/006`. |
| `FILE-LC-PKG-019` | Authority/current evidence: `ConfirmBillImportSessionAsync`. Group confirmation reloads active members but checks only that the actor remains present before deserializing unchanged candidates. | **Confirm import** and report created draft-bill summaries only after the response; never claim retained data was erased, freshly recalculated, every participant/payer was revalidated, authorization was checked at commit, or interruption did not commit. | Persisted/current if committed: `confirmed`, times and unchanged JSON; draft bills/audits share one save boundary. No session version, durable request identity or recoverable response record. Stored candidates follow earlier validation; removed non-actor split participant/payer IDs can survive. | Actor or candidate-member revocation can race/stale-import. Confirm/discard/expiry races remain; parallel confirms can conflict. Uncaught save failure can commit without result; retry sees non-confirmable, not prior success. `CHOICE-001/006`. |
| `FILE-LC-PKG-020` | Authority/evidence: discard/load/GET mapping. | **Discard import** after response only; never claim current authz, data deletion, race prevention or missing-response rollback. | Stored binding only; current profile/group not checked. If committed: discarded/times/JSON; GET review-derived. Save may be unknown; no audit/prior-result. | Soft-deleted/revoked actor can discard; retry may see terminal; racing confirm can apply candidates. `CHOICE-001/006`. |
| `FILE-LC-PKG-021` | Authority/evidence: expiry on get/discard/confirm plus load/GET mapping. | **Expired** after response only; never claim current authz, race prevention or missing-response rollback. | GET/discard use stored binding; if committed expired/update/JSON; GET review-derived. Save may be unknown; no sweep/audit/prior-result. | Soft-deleted/revoked actor can read/trigger expiry; retry may see terminal; racing confirm can create bills. `CHOICE-001/006`. |
| `FILE-LC-PKG-022` | Authority/current evidence: personal/group `Preflight*BillsCsvAsync`. | **Review CSV import** for this request only; never imply a session/source file was retained. | Request-scoped/current: parsed CSV, plan and review response; no persisted source/session or lifecycle audit. GC release/zeroization and client-copy disposition are not recorded. | No key; replay reparses the submitted body under current authorization/domain state and returns a fresh response. `CHOICE-006`. |
| `FILE-LC-PKG-023` | Authority/current evidence: personal/group `Import*BillsCsvAsync`; group members are snapshotted before planning. | **Import draft bills** only after current actor and every split-participant/payer reference are revalidated at acceptance; never imply a staged session or deterministic result after interruption. | Draft bills/audits commit together; raw CSV/plan stay request-scoped. Missing source relationship, lifecycle audit, actor/candidate acceptance predicate, idempotency and request-buffer disposal proof. | Actor or referenced-member removal can race commit. `DbUpdateException` returns bounded failure; cancellation/other thrown save failures leave commit unknown. With no key, ambiguous retry can create another draft set. `CHOICE-006`. |
| `FILE-LC-PKG-024` | Authority/current evidence: `GetPackageReadinessAsync`, mapped package operations and generated readiness/operation contracts. | **Backup package unavailable** is the current readiness response, but neither it nor false create/prepare descriptions may be treated as proof that implemented artifact/download handlers are absent. | Current bounded response: unavailable/unsupported code, safe message, server-authority posture and freshness time. Missing internally consistent capability/readiness and operation-description contract. | Replay returns fresh unavailable metadata while implemented handlers remain mapped and prepare can create ready bytes; clients may suppress those operations. Contract/API/client owners must reconcile before enablement. `CHOICE-006`. |
| `FILE-LC-PKG-003` | Authority/current evidence: `CancelPackageGenerationAsync`, response mappers, web `canCancel`, and `cancelLocalBackupPackage`; API cancellation uses stored actor/session binding without current-profile revalidation. Web `creating_session|preparing` are simultaneously busy, `created|ready_to_download` are excluded, and only `blocked` enables cancel although the API will not transition it; the adapter overwrites the mapped result to `cancelled`. | **Cancel backup generation** describes the API capability only; the current web action is unusable for accepted API source states and falsely reports cancellation from blocked. Never claim downloaded copies were removed, a concurrent prepare stopped, profile deletion revoked access, or no artifact was mutated. | Process-local/current at successful API cancel: `cancelled`, cancel time, null artifact and cleared actions. A web attempt from `blocked` leaves the server blocked while browser state says cancelled. Prior-artifact/action identity, truthful returned-state validation, durable audit and zeroization proof are absent. | A soft-deleted profile with active account/session can cancel through an API client. Web cannot currently invoke the transition. Reconcile from returned server state. A prepare that passed its check can restore `ready_to_download`; other races are mutable ordering. `CHOICE-006`. |
| `FILE-LC-PKG-004` | Authority/current evidence: `DiscardPackageSessionAsync`, `PreparePackageSessionAsync`, `NextAllowedArtifactActions`, the web card's broad `canDiscard`, and `discardLocalBackupPackage`; discard uses stored actor/session binding without current-profile revalidation. | **Discard unprepared backup session** only after returned server state proves `discarded`; never claim bytes were deleted or another state was transitioned. Current web wording is false for ready, blocked, cancelled and expired sessions, and can also be offered for any other nonbusy non-idle/non-discarded local state retaining session/artifact/action data. | Process-local/current at discard return: `discarded` and discard time only when source is `created`; no artifact exists at that boundary. For every non-created source the handler returns current server state, but the adapter overwrites local status to discarded and drops the readout; applicable ready artifact/actions remain live. | A soft-deleted profile with active account/session can discard an unprepared session. A prepare can resurrect it. Non-created calls produce false client success, not disposal/revocation. Reconcile from server state; align UI/API source states. `CHOICE-006`. |
| `FILE-LC-PKG-005` | Authority/current evidence: `ExpirePackageSessionIfNeeded`; later access uses stored actor/session binding without current-profile revalidation. | **Backup session expired** after later access detects the deadline; never claim profile deletion revoked access or a concurrently preparing artifact was stopped. | Process-local/current at expiry return: `expired`, null artifact, cleared actions. No expiry event time, durable audit, background sweep, or memory-zeroization proof. | A soft-deleted profile with active account/session can trigger/read expiry. A prepare that already passed its state check can finish after expiry and unconditionally restore `ready_to_download`; other races use mutable process state. `CHOICE-006`. |
| `FILE-LC-PKG-006` | Authority/current evidence: `ExpireArtifactAndDownloadActionsIfNeeded`; later artifact/action/content access uses stored actor/session binding without current-profile revalidation. | **Backup artifact expired** after later artifact/action/content access; never claim profile deletion revoked access. | Process-local/current: artifact reference cleared and ready session becomes `expired`; no durable audit/background sweep/zeroization proof. | A soft-deleted profile with active account/session can trigger/read this transition. Later access repeats unavailable outcome; expiry/reprepare/content ordering is not bound to an artifact version. `CHOICE-006`. |
| `FILE-LC-PKG-007` | Authority/current evidence: `CreatePackageDownloadActionAsync`; it authenticates account/session and stored binding but does not repeat `CanAccessProfileAsync`, store artifact/version identity, or bound the nested action dictionary/rate. | **Create download action** only for an immutable identified artifact under an approved capacity/rate policy; current runtime cannot promise profile revocation, bounded issuance, or reprepare invalidation. | Process-local/current: opaque action ID, times, unconsumed flag. Missing current-profile authorization, immutable artifact binding/invalidation, per-session count/memory/rate controls, durable audit, idempotency and synchronization. | No key; soft-deleted profile or ordinary caller can issue repeatedly; replay creates another action and grows memory until lazy prune eligibility. Reprepare can retarget actions; concurrent mutation can fail. `CHOICE-006`. |
| `FILE-LC-PKG-008` | Authority/current evidence: `DownloadPackageContentAsync`; it authenticates account/session and stored binding but resolves the session's current artifact without action-bound identity. | **Download backup** only for the exact action-bound artifact; never promise current-profile revalidation, version binding, atomic single-use or safe concurrency. | Process-local/current: action marked consumed before response; current bytes egress. Missing current-profile authorization, artifact/version binding, CAS/lock, synchronized collection, durable outcome and copy inventory. | Soft-deleted profile can download; an action for A can return replacement B. Races can both return bytes; mutation/network failure is unknown; another action may download. `CHOICE-006`. |
| `FILE-LC-PKG-009` | Authority/current evidence: unsynchronized lazy action loop in `ExpireArtifactAndDownloadActionsIfNeeded`; later access uses stored actor/session binding without current-profile revalidation. | No separate success surface; a successfully expired/used action becomes unavailable, but profile soft deletion is not a current barrier. | Process-local/current: consumed/expired entry enumerated and removed on later access. No lock/durable audit; artifact/client copies unchanged. | A soft-deleted profile with active account/session can trigger pruning. Concurrent issue/content/cancel/clear/prune can throw or leave inconsistent state; a successful prune is not byte cleanup. `CHOICE-006`. |
| `FILE-LC-CLN-001` | Authority: metadata architecture/#724; evidence: provider/upload handlers; no reconciler. | Bounded failure only when returned; reservation/profile/staging/save/compensation/reload exceptions are unknown. | Required: intent/step, reservation save/reload, bytes/target, QR profile load, audit/notification staging, link save/reload, compensation certainty, attempts/lease/proof. | Required key. Failures strand pending/active-unlinked or hide committed links; compensation can throw. Same-key write invalid; validate bytes or new target/old-copy disposition. `CHOICE-001/004`. |
| `FILE-LC-CLN-002` | Authority: shared orphan taxonomy, metadata architecture and #724; no proof registry/runtime. Current evidence includes restrictive bill-item `SourceReceiptOcrReviewId`/`SourceReceiptOcrReviewLineId` FKs and non-FK `attachmentFileIds`/`receiptOcrReviewIds` in bill-revision JSON. | Not user-visible as “orphan”; future maintenance may say **Reference review required** or **Eligible for cleanup** only after proof. | Applicable/required: consistent proof set/version, normalized file/review/line and both JSON reference classes/counts, leases/jobs/holds/copies, provider check, actor/time and separately accepted file-byte/OCR-review/OCR-line target-versus-tombstone policies. Absent. | Required key/target version; replay reruns freshness. Prior eligibility is not reusable; any normalized/embedded reference, hold or job blocks until explicitly classified. `CHOICE-001/004`. |
| `FILE-LC-CLN-003` | Authority: file policy raw/derivative rules and #358/#1062; no server cleanup runtime. | Not user-visible for automatic scratch cleanup; an explicit user discard must name only the temporary copy. | Applicable/required for server temporary material: parent/result identity, non-authoritative class, integrity/reproducibility, lease/job/hold checks and outcome. Absent. | Required operation key; already-absent succeeds only for exact proved target; retry resumes; parent/job/hold change conflicts. `CHOICE-001/005/007`. |
| `FILE-LC-CLN-004` | Authority: shared hard-delete/temporary-cleanup taxonomy plus #724/#966; provider `DeleteAsync` has no production caller or acceptance orchestration. | Not ordinarily user-visible; maintenance says **Temporary cleanup completed/blocked** without provider identity. Never label it purge or Trash. | Applicable/required: exact temporary/failed target and source state, non-authoritative proof set/version, actor/approval, operation lease, bounded provider outcome, attempts, time and any tombstone. Absent. | Required key/target version; exact replay resumes/returns bounded result; changed proof conflicts; activation/link/job/hold race revalidates immediately before deletion. `CHOICE-001/004/008`. |
| `FILE-LC-CLN-005` | Authority/current evidence: multipart readers and upload handlers for bill attachments, settlement proofs, and payment QR. Bill parsing creates a file-sized `MemoryStream` backing buffer plus a full `ToArray()` clone before bill subject visibility/mutation/status checks. | No user-visible cleanup claim; upload completion, stream disposal, or subject rejection must not imply request-memory zeroization. | Current: two full managed buffers coexist during parsing; the clone remains through validation/provider write or until an unavailable/immutable bill is rejected. No stable buffer IDs, release times, zeroization result or audit; evidence never logs content. | Each request allocates a new two-copy set; early subject rejection, cancellation or provider failure leaves GC timing nondeterministic. Provider cleanup cannot address either copy. `CHOICE-004`. |
| `FILE-LC-CLN-006` | Authority/current evidence: `CreateRestorePreviewAsync` declared-length guards, accepted request read, package extraction, UTF-8 conversion and parsing. Missing/chunked and declared-oversized requests return before body read. | No user-visible cleanup claim; accepted-body rejection or preview creation/discard/expiry must not imply submitted-package memory was zeroized or retained caller section names were redacted. | Current accepted requests create request/package strings and package bytes containing private full content during handling. Preview entry excludes raw package content but retains unallowlisted caller section names; no stable buffer ID, release time, zeroization/redaction result or audit exists. | Each accepted request can allocate multiple full-content copies; validation failure/success ends handler control while GC timing remains nondeterministic. Preview cleanup cannot address those copies or retained private section names. `CHOICE-006`. |
| `FILE-LC-CLN-007` | Authority/current evidence: bill/proof/QR handlers call `ReadFormAsync` before later `IFormFile.Length` checks; no repository `FormOptions`, equivalent pre-read body bound, or cleanup override was found. Framework/host buffering may stage an oversized body or temporary-file copy before application rejection. | No user-visible cleanup claim; an application-level size rejection is not proof of a pre-read ingress/temp-staging bound or disposal result. Handler completion must not imply deterministic abnormal-termination cleanup, and evidence must never name the temp path. | Framework-managed/current: rejected or accepted staging has no application-stable ID, repository-proved capacity bound, retention clock, cleanup result or audit. Exact host path/configuration is intentionally excluded. | Normal request disposal is framework-owned; cancellation/crash/process termination timing is not an application disposal contract. Future acceptance requires an enforced pre-read ingress/staging bound and verified cleanup lifecycle. Provider or managed-buffer cleanup cannot prove this staging copy's outcome. `CHOICE-004`. |
| `FILE-LC-CLN-008` | Authority/current evidence: direct export, backup builder and `ExpenseBillSearchQueries.WithBillDetails`; no aggregate related-row/query-result bound is proved by the source. | No user-visible cleanup claim; private-field omission or a bounded bill count must not be presented as absence, boundedness, or erasure of builder working copies. | Process/database bill graphs can include all item/split/participant/payer/adjustment rows before projection, with sibling-include row multiplication. No stable copy ID, capacity proof, clock, release/zeroization result or audit; evidence excludes private values. | A relation-heavy bill set can consume excessive memory before projection. Future aggregate relation/query bounds are required; response/session cleanup cannot address GC-only working copies. `CHOICE-006`. |
| `FILE-LC-CLN-009` | Authority/current evidence: `BillCsvImportEndpoints.ReadCsvBodyAsync`, session creation's `ComputePayloadDigest`, and their preflight/direct/session callers. | No user-visible cleanup claim; request completion, import result, or session terminal state must not imply the request byte buffer, decoded text, or digest-input bytes were zeroized. | The bounded `MemoryStream` backing bytes and separate full decoded string can coexist before parsing; session creation adds a full UTF-8 re-encoding for hashing. No stable copy ID, retention clock, release/zeroization result or audit exists. Evidence excludes CSV values. | Every retry allocates a new copy set; GC timing is nondeterministic, and persisted session JSON/client source copies are separate. `CHOICE-006`. |
| `FILE-LC-LOCAL-001` | Authority: local/server boundary and local intake policy; current picker wrappers retain private trimmed raw local path references plus copied bytes. Server `FileObject` fields are not applicable. | **Discard temporary copy** only on the exact app-owned local capture/import/offline-queue surface, with unsynced/OCR warning; never offer app deletion for an unproved user-owned source. | Server metadata: not applicable. Local applicability required but unresolved: source ownership class, scratch ID, OCR/queue/subject links, accepted/acknowledged state, user intent, time, reason and result. Full path value and its private components are excluded. | Local key/version unresolved; replay targets the same proved app-owned copy; retry preserves unsynced/OCR data; concurrent upload/save/OCR/ack blocks discard. `CHOICE-007`. |
| `FILE-LC-LOCAL-002` | Authority/current evidence: mobile picker `withData`, construction/validation, `ReceiptImageArtifactProcessor`, image decoder, and OCR path handoff. Complete copies precede the compressed-byte check, and decode has no dimension/pixel/decoded-memory budget. | No separate lifecycle surface; a byte-limit rejection is not an intake or decompression bound. Accepted normalized/thumbnail previews and path-backed source remain within receipt draft/OCR flow. | Multiple source arrays plus decoded/resized/encoded arrays and a raw path can coexist. Missing width/height/pixel/memory budgets, source ownership, release audit and wipe. Full path/content excluded. | Accepted compressed images can exhaust memory during decode before thumbnailing. Future decoded-dimension/decompression bounds and disposal evidence are required. `CHOICE-005/007`. |
| `FILE-LC-LOCAL-003` | Authority/current evidence: authenticated server-mode `buildExport`, one device-wide `_syncQueueKey`, and logout clearing only server session. | **Generate backup JSON** only as client-local data safety; warn that unscoped queued metadata may belong to a prior account and never claim restore readiness. | Client-local/current: graph/full JSON plus a valid direct `_previewBackupMap` preview retained/displayed. Current profile ID is stamped without ownership filtering; pasting encoded JSON into `previewImport` fails on `includesSessionMaterial`. No stable ID, clock, audit or wipe. | No key; replay replaces reference but not queue. Reads are non-atomic; account switch can change stamp without clearing prior entries. `CHOICE-006/007`. |
| `FILE-LC-LOCAL-004` | Authority/current evidence: authenticated server-mode settings and `SelectableText`; displayed unscoped queue entries may belong to prior accounts. | **Generated backup JSON** may be expanded/selected; warn that it can contain prior-account queue metadata and copied/saved data leaves app control. | Client-local/current full JSON display; no account-ownership proof, read audit, clipboard/file identity, expiry, encryption or disposal evidence. | Repeated display reads retained string. Closing/replacing UI cannot recall screenshot, clipboard, saved or shared copies. `CHOICE-006/007`. |
| `FILE-LC-LOCAL-005` | Authority/current evidence: authenticated server-mode `_latestBackupExport` state assignment and widget lifecycle. | No current **Delete backup** surface. Replacement/release must never be presented as secure erasure or server-record deletion. | Client-local/current reference replacement or widget disposal only; no old-copy identity, zeroization, GC completion, external-copy inventory, timestamp, reason or audit. | Regeneration overwrites the state reference with a new export; old graph/string disposal is nondeterministic and any other/manual copy survives. `CHOICE-001/006/007`. |
| `FILE-LC-LOCAL-006` | Authority/current evidence: pasted-preview dialog/`previewImport`, unrestricted controller text, `jsonDecode`, and recursive sensitive scans before schema preview. This is distinct from valid direct export preview. | **Preview backup import** only after approved pre-decode/structural limits; report current export unavailable/blocked, never compatible or restore-ready. | Full pasted text/decoded graph with no character/node/collection/depth/traversal bound. `includesSessionMaterial` later rejects generated exports. No stable ID, clock, audit or wipe; authoritative records unchanged. | Large/deep paste can allocate or fail recursively before rejection. Close cannot prove wipe; require UI/pre-decode and structural-depth bounds. `CHOICE-006/007`. |
| `FILE-LC-LOCAL-007` | Authority/evidence: generated Dart attachment client, mobile repository wrapper, and bill attachment UI. | **Downloaded attachment** only for current authorized view; never claim display completion or server unlink disposed local bytes. | Mutable full response list plus separate full unmodifiable repository list coexist before UI displays length; no local IDs/clocks/audit/wipe/cache inventory. | Re-download creates another two-copy set; widget/reference release is GC-only and server state cannot recall either buffer. `CHOICE-007`. |
| `FILE-LC-LOCAL-008` | Authority/evidence: web import/export readout package download/restore selection, request construction, preview-discard and confirmation create/refresh/discard handlers, input disabled-state conditions, and generated JSON request serialization. Restore calls `File.text()` before any matching client-side size gate, then retains `packageContent` and creates another full serialized string. The input remains enabled during confirmation work and after preview discard sets a non-busy status; every listed handler commits its result without checking current selection. | **Download backup** / **Select backup file**; never claim server expiry/discard deletes browser/saved copies, later server rejection bounded browser allocation, or request completion wiped either string. Never present any stale preview/confirmation result for backup A as belonging to newly selected backup B. | Browser Blob, object URL, saved file, selected File, full package-content string, full serialized-request string, preview/confirmation responses and selection/request generation; escaping can amplify size before server validation. No common identity binding/clock/audit/revocation/wipe/saved-file inventory. | Future acceptance requires a pre-read `File.size` gate, bounded serialization/copy lifetime, and cancellation or current-selection/request-generation validation before applying every preview/confirmation result while preserving server authority. Repeat creates copies; URL/reference release cannot prove memory wipe or delete saved files. Restore remains separately gated. `CHOICE-006/007`. |
| `FILE-LC-LOCAL-009` | Authority/evidence: web bill-import selection plus preflight and session create/confirm/discard handlers and input disabled-state conditions. Selection calls `File.text()` without checking `File.size` against the API application limit and has no generation/current-file guard across overlapping reads. The input ignores `sessionBusy`, and each session handler commits its response without checking current selection. | **Select CSV for review**; never claim later server rejection, request completion, session expiry/discard/confirmation, state replacement, or navigation bounded intake, deleted the source, or securely wiped copies. An older read can overwrite text after newer filename/size state was set; any session action for A can reinstall A state after B selection, and confirmation can commit A bills under B's display. | Browser-selected Files/full strings/filename/size, every review-session action response, selection/request generation and user source ownership; a large selection can allocate the full sensitive string before server validation, and overlapping strings/results can outlive or mismatch their metadata. No shared identity binding/clock/audit/revocation/wipe/source-file inventory. | Future acceptance requires a pre-read `File.size` gate plus current-file/request-generation validation before committing text or every session-action result (or cancellation) while preserving server authority. Successful creation clears the string, but failures/navigation/concurrency/GC are not deterministic erasure; user source survives. `CHOICE-006/007`. |
| `FILE-LC-LOCAL-010` | Authority/evidence: generated web client JSON response parser, web `createJsonExportBlob`, group selector enablement, and group readiness/download handlers. Those handlers capture the old group and write results after the selector can change. | **Export JSON**; never present readiness/download for A as bound to currently displayed group B, or claim completion/object-URL revocation securely wipes browser copies or deletes the saved file. | Full response string, parsed object graph, reserialized string, Blob/object URL, saved file, selected-group/request generation and displayed result; no shared identity binding/clock/audit/revocation/wipe/saved-file inventory. | Require group-selection/request-generation binding or disable selection until settlement. Repeat creates another copy set; reference/URL release is GC/browser-controlled and cannot prove memory wipe or delete saved files. `CHOICE-006/007`. |
| `FILE-LC-LOCAL-011` | Authority/evidence: generated direct web CSV export response, browser download adapter, group selector enablement, and group readiness/download handlers. Those handlers capture the old group and write results after the selector can change. | **Export CSV** only; never present readiness/download for A as bound to currently displayed group B, treat it as a server `FileObject`, or claim completion/URL revocation wiped browser memory or deleted a saved file. | Response Blob, object URL, saved file, selected-group/request generation and displayed result have no shared identity binding, clock, inventory, release result, audit, deterministic wipe or server disposal authority. | Require group-selection/request-generation binding or disable selection until settlement. Repeat creates another copy set; user-saved copies remain user/device controlled and not server-recallable. `CHOICE-006/007`. |
| `FILE-LC-LOCAL-012` | Authority/evidence: existing-bill receipt/supporting selector, mobile `withData: true` picker, picked-file validation, and generated upload mapping. Neither kind on this route uses the receipt processor first. | **Select receipt/supporting attachment** only; later server rejection is not a mobile intake bound or disposal result. Keep processed receipt capture under `LOCAL-002` distinct. | Selected full bytes plus another full unmodifiable upload list can coexist before upload; no approved pre-allocation limit, stable ID, clock, audit, wipe or deterministic release. | Future acceptance requires an approved pre-allocation/pre-upload guard without inventing a numeric limit; retry creates copies and server validation cannot dispose mobile memory/source material. `CHOICE-007`. |

## 7. Inactive Read And Restore Contract

### 7.1 Audience-specific inactive reads

| Condition | Ordinary metadata/list | Content | Thumbnail/preview | Owner/admin | Maintenance/audit and retained history |
| --- | --- | --- | --- | --- | --- |
| Active object + active compatible link | Allowed only after current purpose/subject authorization. | Same; authorization is checked on every request. | Same; a derivative is not a bypass. | Owner follows subject policy; admin has no blanket access. | Bounded safe metadata only under an approved role/purpose. |
| Active object + inactive/missing link | Not readable through that subject. | Blocked. | Blocked. | Owner does not gain a generic-file bypass; use an explicit restore/reconcile surface when implemented. | Bounded link/object inspection only if separately authorized. Historical reference may remain. |
| `pending` or `upload_failed` | Blocked outside bounded upload status/reconciliation. | Blocked. | Blocked. | No generic owner read. | Bounded state/outcome inspection; byte preview is not proof and is blocked by default. |
| `quarantined` | Blocked from ordinary surfaces. | Blocked. | Blocked; do not render untrusted content. | Owner receives bounded status/reason only where policy permits; admin still lacks blanket content. | Separately authorized security inspection may use sandboxed tooling; no ordinary download. Runtime absent. |
| `deleted` | Blocked from ordinary **current attachment/file** views; however current authorized bill-revision list/detail mapping can return a retained receipt file ID parsed from `ProposedSnapshotJson` without checking current link/object state. | Blocked. | Blocked unless a future restore-preview policy explicitly authorizes a safe generated representation. | Bill viewers may receive that historical stable ID through revision history; owner/admin otherwise gain no generic inactive-file bypass. | Required history and bounded lifecycle evidence may remain visible to approved audit/maintenance roles. Historical ID visibility is not content access or disposal evidence. |
| `purged` marker or unknown provider state | Tombstone/status only where authorized; never claim byte outcome from marker alone. | Blocked. | Blocked. | No bypass. | Reconciliation sees bounded outcome categories, not paths, keys, URLs, credentials, contents, or provider diagnostics. |

Current ordinary bill/proof/QR reads implement the active-object/active-link
posture for the point-in-time checks made by each request. They do not provide
an atomic inactive barrier: after authorization/link/object state is loaded, a
concurrent unlink, object deletion, subject change, or authorization revocation
can occur while provider open or response delivery continues, and the in-flight
request may still return bytes. Future acceptance must revalidate or serialize
the delivery boundary where policy requires immediate revocation. For bill,
proof and settlement-counterparty QR reads, provider open precedes the audit
save; cancellation/other non-database save failures can leave audit outcome
unknown and the open stream without explicit disposal, while a committed audit
still precedes delivery and cannot prove bytes reached the client. Current
self-QR content reads omit that audit entirely. Current source does not implement Trash
metadata, maintenance, admin, quarantine, or restore-preview reads; those
entries are policy requirements.

Bill-revision history is a current metadata exception to the ordinary inactive-
file posture. Authorized bill viewers can receive the first retained
`attachmentFileIds` value as the OCR source file ID from proposed snapshot JSON,
even after its live attachment is removed and the object becomes `deleted`;
the mapper does not check current link/object state. This exposes a stable
historical ID, not bytes or provider details. Future acceptance must explicitly
authorize/minimize this audience and audit posture, or return an approved
tombstone/redaction, without treating history visibility as a current-file read.
Current revision list/detail mapping records no dedicated storage/file access
event for returning that historical ID; future bounded evidence must exclude
snapshot contents and unrelated bill data.

Successful ordinary metadata reads are not lifecycle-audited either. Current
bill attachment lists, settlement-proof lists, and self payment-details reads
can return active stable file IDs with bounded content type, size, timestamps,
or current QR metadata after authorization without recording a storage-access
event. This is a current audit gap, not permission to expose inactive metadata;
future audit must remain bounded and must not include filenames, contents,
payment-detail values, or provider internals.

### 7.2 Restore/reactivation decision

A restore request must name the authenticated requester, exact authoritative
API/domain/storage owner, stable file ID, optional exact subject/link ID, source
state, and target state. The authoritative owner must revalidate at acceptance
time:

1. account and requester ownership/authorization, without treating admin as a
   content role;
2. exact `deleted` object and inactive-link state, retained bytes, byte
   readability/integrity, and any supported content validation; restoring the
   object sets `Status = active` and clears `DeletedAtUtc` in one accepted
   object transition;
3. purpose compatibility with the target subject and the active upload policy;
4. target subject existence, visibility, status, capacity, historical
   dependencies, and current mutation eligibility;
5. privacy/vault mode, quarantine/rejection, encryption/recovery availability,
   and no downgrade;
6. replacements, duplicates, conflicting active links, changed owner, quotas,
   retention holds, and terminal-disposition evidence; every link already
   marked active must be enumerated and explicitly accepted under its current
   subject policy before object activation, or the object restore blocks;
7. idempotency identity, expected version, racing unlink/replace/purge, and a
   deterministic prior-result or conflict response; and
8. separate bounded object-restore and link-reactivation audit events.

Missing/unreadable bytes, a `purged` marker without trustworthy provider
outcome, failed integrity/validation, incompatible purpose, unavailable or
immutable subject, unauthorized actor, changed privacy policy, unresolved
dependency/hold, active replacement, duplicate ambiguity, and concurrent
transition block restore. Restore does not silently reattach any subject,
reopen an OCR review, restore a notification, change a settlement/payment/bill,
or revive a browser/device copy. An inactive link remains inactive. A link
already marked active is not implicitly trusted merely because the deleted
object had blocked its reads; it must be included in the accepted active-link
set or the object transition cannot commit.

## 8. Failure And Cleanup Decision Table

| Scenario | Required posture |
| --- | --- |
| Pending reservation becomes unreachable | Cancellation/another exception can make initial metadata/audit commit unknown before the handler receives an ID. Separately, after successful `CreatePendingAsync`, cancellation/query failure in the immediate `SingleAsync` metadata reload loses the already committed ID. Do not assume no row. Reconcile through durable operation identity; current unkeyed retry creates another `pending` reservation and cannot target the stranded row. |
| Byte write fails before metadata reservation | Current server pattern reserves metadata first. For any future alternate pattern, no accepted row/link exists; bounded temporary cleanup may target only the exact operation-owned bytes after proving they are non-authoritative. |
| Metadata exists but byte write fails | Keep/mark `upload_failed`; ordinary reads stop. A returned unsuccessful compensation is ignored, while cancellation/another exception from the unguarded compensation can leave its status/audit commit unknown and return no bounded failure; the bill activation-failure branch has the same risk. Reconcile metadata plus zero/partial/complete bytes. `FileMode.CreateNew` blocks in-place rewrite after any target creation; validate complete bytes or authorize a new target and old-copy disposition. |
| Request cancellation interrupts byte write | Current handlers skip their non-cancellation failure catch, so no `MarkUploadFailedAsync` attempt runs; metadata can remain `pending` and the provider may hold partial bytes. Treat as a stranded upload requiring the same positive reconciliation as any unknown write outcome; cancellation is never cleanup proof. |
| Bytes exist but activation/link completion fails | Do not serve. Returned/thrown activation failures can leave `pending`, `upload_failed`, active, or unknown state. After activation, audit staging in all three handlers and proof-notification staging occur before the guarded association save; cancellation/query/other failure there bypasses compensation and leaves an active unlinked object. A caught association `DbUpdateException` invokes unguarded `MarkDeletedAsync`; returned failure is ignored and a thrown compensation can leave delete/audit commit unknown. Other association exceptions also bypass compensation. Record exact staging/save/compensation step and certainty; reconcile before read/deletion. |
| QR active object cannot load payment profile | After activation and before link/audit staging, QR upload queries the active payment profile. Cancellation/query failure leaves the new object active and unlinked with no compensation or bounded response. Reconcile the operation-created ID and profile state; retry may create another QR object. |
| User profile is deleted after QR activation | The active-payment-profile lookup filters the deleted user and returns null; when no prior payment-profile row existed, the handler can create and commit a new payment profile linked to the active QR after eligibility was revoked. The filtered lookup is not an acceptance-time authorization barrier. Reconcile the new profile/link/object; future acceptance must revalidate or atomically predicate current profile eligibility. |
| Actor eligibility changes during file-object reservation or status transition | `CreatePendingAsync` and every generic status transition call `IsActorAvailableAsync` before constructing/loading and saving the object. The later save is not predicated on continued account/profile eligibility, so concurrent account disablement or profile soft deletion can still commit `pending`, activation, logical deletion, quarantine, purge-marker state, and its audit after eligibility was revoked. Treat the check as an observation, not an accepted precondition; future acceptance must revalidate or atomically predicate actor eligibility and reconcile ambiguous committed outcomes without exposing object metadata. |
| Upload association commits but response reload fails | Bill/proof handlers reload the saved link after link/audit commit; QR reloads the profile after committed association and old-object cleanup attempt. Cancellation/query failure returns no success although the new link is live. An unkeyed retry can create another object/link/replacement. Reconcile the committed link, every operation-created object and QR old-object outcome; missing response is never association-failure proof. |
| Multipart upload buffer outlives handler logic | Current managed `byte[]` copies have no deterministic release/zeroization contract. Do not equate successful provider write, metadata activation, request cancellation, or provider cleanup with disposal of process-memory copies; define memory-handling acceptance separately without logging contents. |
| Bill upload is rejected after body buffering | Current bill upload authorizes only the route scope before full multipart parse/copy; bill visibility, mutation authority, and mutable status are checked afterward. Treat the managed/framework copies as sensitive even when no reservation occurs. Future resource/privacy controls must decide whether subject checks can precede buffering without trusting client claims. |
| Framework multipart staging outlives normal request handling | `ReadFormAsync` can use framework-managed temporary files above the in-memory threshold before the handler copies `IFormFile`; no repository override or application cleanup evidence defines their path, crash behavior, or cleanup timing. Treat normal request disposal as framework behavior, not proof for cancellation/process failure, and never expose or log the temp path. |
| Oversized multipart input is rejected only after form parsing | Bill/proof/QR handlers call `ReadFormAsync` before their per-file `IFormFile.Length` checks. The application size limits therefore do not prove a pre-read ingress or temporary-staging bound, and a rejected body may already have consumed framework/host staging capacity. Require a verified pre-read bound and cleanup lifecycle; do not infer them from the later rejection. |
| Authorization/status changes during a long upload or import | An authorization, group-membership, or mutable-subject check made before provider work or plan construction is not an acceptance-time predicate. Current bill-attachment, settlement-proof, and bill-import paths can commit after that observation becomes stale; proof creation specifically does not reload profile/group membership or payment/request eligibility after upload. Record the observed-versus-accepted boundary; future acceptance must revalidate or atomically predicate the write. |
| Personal or group authorization changes during direct export | Current personal/group export authorizes before `LoadRowsAsync`. Personal row visibility predicates creator deletion but not deletion of the requesting participant profile; the group query has no actor-membership predicate. Profile deletion or membership revocation can therefore race row materialization and response delivery. A completed early check is not an acceptance/delivery barrier; future export must revalidate or atomically bind current profile/group eligibility without logging response data. |
| Export readiness promises audit that execution omits | Readiness advertises `WritesAuditOnExport: true`, but direct CSV/JSON handlers have no audit writer/event. Treat current export as unaudited domain execution; readiness metadata is not evidence. Contract/runtime owners must reconcile the promise, and future audit must remain bounded and exclude bill/export contents. |
| Authorization/status changes during bill/proof removal | Bill and proof handlers load subject/membership/status context before the final lifecycle save; `MarkDeletedAsync` rechecks actor/file ownership, not current subject eligibility. Revocation, finalization or payment/request status change can therefore race a committed removal. Future acceptance must revalidate/atomically predicate the subject mutation and preserve the stale-observation outcome. |
| Bill/proof removal save is interrupted | Link `RemovedAtUtc`, domain removal audit, object `deleted` state and lifecycle audit share the service context/save. Cancellation/another non-database exception can escape with that commit unknown and no success response; retry sees no readable active link rather than prior success. Reconcile link/object/audits as one uncertain commit, while retaining their conceptual lifecycle separation. |
| One shared file object has another active subject link when removal commits | Bill/proof file-object indexes are non-unique, while current removal marks the selected link removed and the shared object `deleted` without enumerating other active links. A collateral link can keep `RemovedAtUtc == null` yet become unreadable through object status. Future acceptance must make unlink and object-state decisions reference-aware and must not logically delete the object while another accepted active link requires it. |
| Authorization/link/object state changes during content delivery | Current reads validate before provider open, but do not atomically couple that decision to unlink, deletion, revocation, or response completion. A later request is blocked; an already-authorized in-flight request may still deliver bytes. Treat immediate revocation semantics as unresolved and never claim link/object inactivity recalls an issued stream or client copy. |
| Audited content read fails after provider open | Bill, proof and settlement-counterparty QR handlers open the stream before saving the bounded access event. They dispose it only for the caught `DbUpdateException`; cancellation/another exception can leave audit commit unknown and no explicit stream disposal, while a successful audit commit occurs before response delivery. Reconcile event and stream outcomes separately: the event is neither proof of delivery nor deterministic cleanup. |
| Successful file metadata read has no access event | Bill attachment lists, settlement-proof lists, and self payment-details return active file IDs with bounded type, size, timestamps, or current QR metadata after authorization, but write no storage/file read audit. Success is therefore not traceable through the current lifecycle audit. A future bounded event must record actor/account, stable object and safe subject/link IDs, action, time, correlation and result category without filenames, contents, provider internals, or unrelated private profile/payment fields. |
| Initial QR attachments race | Two requests can activate different new objects after both observe a null profile QR. Last profile write wins; neither request has an old ID to delete, so the losing new object can remain active/unlinked. Reconcile every operation-created ID, not only the final profile link. |
| QR replacements race | Two requests can activate different new objects after observing the same old QR. Last profile write wins, both compensate only the old object, and the losing new object can remain active/unlinked. Reconcile all operation-created IDs; do not infer success or disposability from the final profile link alone. |
| QR post-link object transition throws | Removal/replacement has already committed the profile link and payment audit when `MarkDeletedAsync` runs. An unsuccessful result is ignored, but cancellation/another thrown exception returns no success with old-object state/commit unknown; replacement retry can create yet another object. Reconcile the committed link/audit and every involved object before retry; never use the missing response as rollback or cleanup proof. |
| Import save or confirmation outcome is ambiguous | Direct import catches only database-update exceptions; cancellation/other thrown failures can leave commit unknown. The same is true of the confirmation save: session status, draft bills and audits may commit before interruption, then retry returns non-confirmable instead of the prior success. Parallel confirmations can also yield one committed import and one generic duplicate-ID write failure. A failure/no-response is not proof that no bills exist; retry requires reconciliation and cannot safely replay as a fresh unkeyed import. |
| A reviewed group participant or payer is removed before confirmation | Group confirmation reloads membership but verifies only the actor, then deserializes unchanged candidates. Removed non-actor split participants and payers can enter new draft bills. Treat candidate references as stale until every money-bearing member is revalidated or the commit is atomically predicated; actor membership alone is insufficient. |
| A direct-import group participant or payer is removed before save | Direct group import snapshots active member IDs before reading/planning the CSV, validates candidate references against that snapshot, and later saves without revalidation. A removed non-actor split participant or payer can enter a new draft. Revalidate every money-bearing reference at acceptance or atomically predicate the save; current actor authorization alone is insufficient. |
| Import-session creation outcome is ambiguous | Personal/group session creation does not catch cancellation or other save exceptions. A private review/candidate row may commit without returning its ID; unkeyed retry creates another session and cannot address the first. Preserve both as unknown retention/reconciliation candidates, never as safe cleanup proof. |
| Import-session discard or lazy-expiry outcome is ambiguous | Discard and expiry saves do not catch cancellation or other exceptions. Terminal status may commit without a response; retry then observes `discarded` or `expired` rather than the prior result. Reconcile the status save separately from concurrent confirmation/bill creation and retained JSON. Missing response is not proof the terminal transition failed. |
| Restore confirmation races preview discard/expiry | Mutable process state has no lock/version across the ready check and confirmation insertion. A live confirmation may reference a preview that concurrently became `discarded` or `expired`. Terminal preview status is only a sequential availability result, not proof that no confirmation exists. |
| Concurrent restore confirmations reuse one idempotency key | Current lookup and insertion are separate unsynchronized operations. Two matching requests can both miss the scan and create different confirmation IDs under the same key. A key therefore provides sequential replay behavior only; require atomic key ownership plus request-digest matching before treating it as retry protection. |
| Restore confirmation retains a caller idempotency key | Current normalization trims nonblank text but applies no length/safe-token validation. The private key remains internally retained through discard/expiry until restart, while responses disclose only `IdempotencyKeyAccepted` and the request digest—not the key. Validate/bound the key, never log/echo it, and define internal redaction/disposal separately from response visibility. |
| Package session or restore-preview access follows profile soft deletion | Only backup preparation calls `CanAccessProfileAsync`. Creation, metadata reads, cancel/discard, lazy expiry, action creation/content, restore-preview creation/read/discard/expiry, and confirmation create/read/discard/expiry otherwise rely on active account/session plus stored binding where applicable. A soft-deleted profile can still use those surfaces. Actor/session binding is not current-profile authorization; every applicable operation needs an explicit fresh or atomically bound profile-eligibility rule. |
| Restore-preview section names carry private caller text | `sections[].name` is not restricted to an allowlisted safe category before it enters retained preview/confirmation summary lists and terminal responses. Treat the strings as private content, not bounded lifecycle metadata. Discard/expiry does not redact them; future validation must allowlist safe categories or store/return redacted values and never log the originals. |
| Backup cancellation is invoked from the web card | The web card enables cancel only from `blocked`, which the API does not transition, while excluding accepted `created|ready_to_download` sources. `cancelLocalBackupPackage` then overwrites the unchanged mapped server result to local `cancelled`. Never treat that browser status as cancellation evidence; require returned-state validation and UI/API transition parity. |
| Backup discard is invoked from a non-created web state | The broad web predicate enables discard for `ready_to_download`, `blocked`, `cancelled`, and `expired` (and any other nonbusy non-idle/non-discarded local state retaining session/artifact/action data). The API accepts only `created → discarded`, so those calls return unchanged server state, but the adapter reports local `discarded` and drops the readout; ready artifact/actions remain live. Never treat that client state as disposal or revocation; require returned-state validation and aligned source states before showing terminal success. |
| Browser-selected CSV or backup package exceeds the server application limit | Bill import and backup restore call `File.text()` without a matching pre-read `File.size` gate, so the tab can allocate a complete sensitive JavaScript string before later server/application rejection. Require a pre-read client guard while keeping server limits authoritative; client rejection or reference release is not verified wipe of the source or string. |
| Backup restore request serialization duplicates full package content | After `File.text()`, restore request construction retains the full `packageContent` string and the generated client creates a second full `JSON.stringify(body)` request string whose escaping can increase size. Bound both working-copy lifetimes and serialization capacity; request completion or server rejection is not deterministic GC/zeroization evidence. |
| Overlapping CSV selections resolve out of order | Selection handlers do not cancel an older `File.text()` call or check a generation/current-file identity before committing its result. An older read can resolve after a newer one and overwrite CSV text while filename/size still describe the newer file, so preflight/session creation can submit the wrong selected content. Require a current-file/generation check before committing text; clearing references at selection start is not concurrency control or disposal proof. |
| CSV selection changes while any import-session action is pending | The file input ignores `sessionBusy`. Selecting B resets the session; A's pending create, confirm, or discard handler can then install A's result while B's filename/size remain displayed. Creation also clears B's text, and confirmation can commit A's bills while the UI identifies B. Require cancellation or selected-file/request-generation binding before applying every session-action result; clearing state on reselection is not result binding. |
| Import scope or group changes around an existing/pending session | Scope and group selectors also ignore `sessionBusy`, and their callbacks do not clear or rebind the session. A pending result can install old scope/group state after the change; even without a pending request, a session created for personal/group A remains confirmable while controls display group B. Require immutable session-to-file/scope/group binding plus invalidation or display of that bound identity before any action. |
| Backup selection changes while preview/confirmation work is pending | The input ignores confirmation busy state, while preview discard first sets a non-busy `discarded` status. Selecting B can reset state before A's pending preview-discard or confirmation create/refresh/discard handler unconditionally installs stale A metadata. Require cancellation or preview/selection-generation binding before applying every result; server actor/session binding does not bind the browser result to its current selection. |
| Group selection changes while readiness/export is pending | The group selector remains enabled while CSV/JSON readiness and download handlers capture group A. Their eventual result is written into shared group-export state, or saved with a generic group/date filename, after B can be displayed. Require group-selection/request-generation binding or disable the selector until settlement; server authorization for A does not bind the browser result to B. |
| Web backup cancellation is offered | Current enablement excludes API-accepted `created|ready_to_download`, treats `creating_session|preparing` as both eligible and busy, and enables only `blocked`; the API leaves blocked unchanged. Do not present the web control as a usable transition until its enabled source states match the API contract. API-client cancellation remains separately subject to authorization, concurrency and copy-disposition gaps. |
| Restore-preview aggregate counts exceed contract bounds | Up to 250 candidate summaries can each contribute nested counts clamped to 1,000,000, but current aggregation has no final cap or rejection. Returned totals can exceed the OpenAPI maximum. Validate aggregate counts before accepting the preview and return a bounded failure; per-candidate clamping is not aggregate contract proof. |
| Process-local backup entries exhaust aggregate memory | Package-session, restore-preview, and restore-confirmation static dictionaries accept new top-level entries without per-actor/global count, aggregate-memory quota, or removal sweep. Lazy expiry changes availability and may clear some references but does not remove every top-level entry. Future acceptance requires policy-approved bounded quota plus sweep/removal behavior; no numeric limit is approved here. |
| One ready package session accumulates download actions | Every action request inserts a new unconsumed/unexpired entry into the session's nested dictionary. Pruning is lazy and removes only consumed/expired entries; no per-session count/memory quota or issuance-rate limit exists. Require approved nested capacity/rate controls and safe pruning without assuming top-level session quotas bound this collection. |
| Persisted CSV import sessions exhaust aggregate storage | Every valid creation inserts a new row containing `ReviewJson` and `CandidateJson`; confirmation, discard and lazy expiry retain both fields. There is no per-actor/global row count, retained-byte quota, rate limit, sweep or removal path. Require policy-approved quota/rate/sweep/removal behavior and reconciliation of ambiguous creations; no numeric limit or retention duration is approved here. |
| Direct CSV response completes | Server construction holds complete `StringBuilder` character storage and a second immutable `ToString()` result before `Results.Text`; browser handling later creates its own Blob/URL/file copies. Response completion is not deterministic disposal of either server copy. Bound working-copy lifetime/capacity and exclude contents from evidence without treating browser cleanup as server-memory cleanup. |
| Relation-heavy bills are exported or packaged | `WithBillDetails` eagerly includes every related item, split, participant, payer and adjustment. Bill-count/row limits do not bound those child collections or sibling-include result multiplication, so a small bill set can exhaust database/API memory before projection. Require approved aggregate relation/query-result bounds; output minimization is not input working-set control. |
| Mobile existing-bill receipt/supporting attachment exceeds practical device capacity before upload | The common picker route accepts receipt and supporting kinds, requests full bytes, and upload mapping makes another full list copy; neither kind passes through the receipt processor's size gate first on this route. A later server rejection cannot prevent those allocations. Require an approved pre-allocation/pre-upload guard and local-copy lifecycle; do not invent a numeric limit or treat server validation as client-memory disposal. |
| Compressed receipt decodes to excessive pixels | The receipt processor checks compressed bytes only after several copies, then decodes without width, height, pixel-count, decoded-memory, or decompression bounds. A compressed image within the byte limit can exhaust memory before resize. Require approved decoded-dimension/pixel/memory bounds in addition to pre-read byte controls; failure/reference release is not wipe proof. |
| Pasted backup JSON is oversized or deeply nested | The dialog retains unrestricted text, `jsonDecode` creates a graph, and recursive sensitive scans run before bounded schema preview with no character/node/collection/depth/traversal limits. Large or deep input can exhaust memory or recursion before rejection. Require UI/pre-decode size and structural-depth bounds; do not log the input or treat close as wipe. |
| Deleted receipt ID remains in bill-revision history | Authorized bill-revision list/detail mapping can return the retained proposed snapshot's first attachment ID as OCR source file ID without checking current attachment/object state. Content stays blocked, but the stable inactive ID is ordinary bill-viewer metadata. Define/minimize the audience, tombstone/redaction, retention and bounded audit posture; do not claim the ID is hidden or that visibility authorizes file access. |
| Direct CSV export completes in the browser | The generated client returns a response Blob and the download adapter creates/revokes an object URL, potentially leaving a user-saved file. These copies are not server `FileObject` records. Bound future app-controlled handle retention/disposal, but do not claim URL revocation wipes memory or that the server can recall/delete a user-saved copy. |
| File uploads exhaust retained provider storage | Current bill receipt/supporting, settlement-proof, and QR initial/replacement uploads enforce per-file size but no applicable per-bill, per-payment, or per-account retained-byte quota or upload-rate limit. Their logical removal, and QR replacement of the old object, do not physically reclaim bytes. Future acceptance must atomically authorize bounded quota/rate usage and account for pending, active, deleted, failed, stranded, and concurrently created objects; per-file limits are not aggregate storage controls. |
| Restore-preview request completes or fails | Request text, extracted package text and UTF-8 package bytes are full-content process-memory copies with no deterministic release/zeroization contract. Stored preview metadata excludes raw content, but success, validation failure, discard, expiry and preview-entry cleanup do not prove these handler copies were cleared. |
| Prepared package outlives profile eligibility | Preparation checks current profile access, but action issuance/content delivery later check only active account/session plus stored actor/session binding. A soft-deleted profile can still download while those remain active. Treat preparation authorization as stale; future delivery must revalidate or atomically bind current profile eligibility without treating session binding as sufficient. |
| Profile is deleted during a successful package build | The builder filters profile deletion only in its first query, then performs more queries and serialization before unconditional ready-artifact publication. Deletion after that query may not throw and can expose a newly generated artifact for an ineligible profile. Require a final or atomically bound eligibility predicate; success is not proof authorization stayed current. |
| Package artifact build throws | `PreparePackageSessionAsync` catches only its bounded generation-validation exception. Cancellation, database/query failure, or profile deletion between authorization and a filtered builder query escapes. A created session remains created; reprepare of a ready session preserves its prior artifact/actions, which can remain downloadable. Preserve source/prior-artifact state and thrown category; missing response is not blocked status, cleanup, or safe fresh-retry proof. |
| Reprepare replaces an artifact after actions exist | Successful reprepare replaces `session.Artifact` but does not clear issued actions; actions store no artifact/version identity and download resolves the session's current artifact. An action issued for A may return B. Require immutable action-to-artifact binding or action invalidation on replacement; do not describe the current action as capability for one stable artifact. |
| Direct export or package build omits private fields from output | Both direct export and backup preparation call `WithBillDetails`, materializing all included item, split, participant, payer and adjustment rows before narrower projection. Bill-count limits do not bound child counts or sibling-include row multiplication. Private omitted fields remain in a potentially excessive working set. Require aggregate relation/query-result bounds; output minimization is neither capacity control nor working-copy disposal. Never log contents. |
| Mobile-local backup is generated, displayed, replaced, or previewed | Full JSON/object/rendered/controller copies live inside authenticated server-mode UI, but the device-wide queue is not account-scoped and survives logout; a new account can export prior-account metadata under its stamped profile ID. The direct `buildExport` preview remains valid, while pasting the generated `encodedJson` into the separate `previewImport` path is guaranteed to fail because `includesSessionMaterial` matches `session`; error text may also reject. These copies are not server authority. Replacement/disposal is not zeroization or external-copy recall. Keep restore disabled and never log the JSON. |
| Mobile/web download creates a client copy | Mobile attachment content becomes a full `List<int>`. Web package download/selection creates Blob, object URL, saved file, browser File and full text. Later server unlink, object deletion, package expiry/discard or purge cannot revoke those copies. Client reference release is not wipe; object-URL revocation is not saved-file deletion. Inventory and dispose only under explicit platform/user ownership policy. |
| One link is removed | Make that link inactive. Do not infer byte deletion. Recalculate reference/hold evidence across all link and history categories. |
| Zero normalized references are observed | Mark only a derived orphan candidate after querying the complete registered reference set, including bill-item source-review/source-line FKs and both bill-revision snapshot JSON arrays `attachmentFileIds` and `receiptOcrReviewIds`, in a consistent snapshot. Repeat immediately before disposal. Absence in one table or absence of one foreign-key class is insufficient. |
| Multiple or embedded references exist | Object-wide logical removal or disposal is blocked unless every authoritative link/history owner accepts its own transition. One unlink affects only that link. Each embedded file or OCR-review ID remains a historical reference until the relevant owners decide and prove retained-target versus sufficient-tombstone treatment. |
| Temporary/generated intermediate | Cleanup only after the accepted canonical result and its dependency/integrity evidence exist, no retry/job needs the intermediate, and policy classifies it non-authoritative. |
| Process-local backup artifact reaches advertised TTL | Current TTL is checked only on a later session request. Treat availability as expired when checked, but do not claim memory cleanup: without later access, artifact bytes remain referenced until process restart. A focused owner must define eager removal, restart/crash behavior, and bounded cleanup evidence before acceptance. |
| Provider says missing/unreadable | Fail closed as unknown/inconsistent. Do not call it orphaned, purged, or restored. Preserve metadata/history and route bounded reconciliation. |
| Cleanup partially fails or times out | Retry the same idempotent cleanup intent. “Already absent” is success only when exact target identity and prior authorization are proved; otherwise return unknown/blocked. API accepts authoritative metadata outcome after provider result. |

Cleanup jobs must be lease/concurrency safe, bounded in batch size, restartable,
and auditable. They receive stable opaque IDs and approved operation tokens, not
client paths or provider internals. They never delete authoritative files,
history, or core rows and never write those tables directly.

## 9. Retention And Terminal Disposal

No numeric duration is chosen here.

| Purpose/state | Classification and trigger |
| --- | --- |
| Active normalized receipt | Policy-defined, dependency-gated while parent receipt/bill and required history exist. Current per-file validation has no per-bill/account retained-byte quota or upload-rate limit, and logical attachment removal is not capacity reclamation. OCR-sourced bill revisions can retain both `attachmentFileIds` and `receiptOcrReviewIds` in baseline/proposed snapshot JSON without direct file/review FKs. Whether history requires the authoritative file/OCR-review targets or sufficient tombstones is unresolved for each ID class, so embedded references block their applicable disposition until the bill-revision/history plus file/OCR owners decide and prove the accepted treatment. |
| Raw receipt input | Source-defined transient intent: discard only an app-owned picker/capture scratch copy after successful normalization, integrity/readability, accepted canonical persistence/link, and no OCR/retry need. Current mobile retains a private local path reference alongside bytes; picker ownership and cleanup are unproved, and user-owned imports are not app-disposable by default. |
| OCR source/derivative | Required but duration unresolved; gated by review, review-line, normalized bill-item source references, reprocessing, privacy, accepted-record, revision JSON, and audit needs. Restrictive bill-item review/line FKs require an explicit retained-target-versus-tombstone decision before applicable OCR disposition. |
| Thumbnail/preview | Mobile processing holds compressed copies, decoded full pixels, resized pixels and encoded normalized/thumbnail arrays with no wipe/clock; wrapper may retain separate OCR path source. Future persistence follows parent; cleanup requires reproducibility/dependency proof. |
| Settlement proof | Policy-defined dependency-gated while proof/payment/settlement record and history require it. Current per-file validation has no per-payment/actor retained-byte quota or upload-rate limit; logical removal is not capacity reclamation. |
| Payment QR | Policy-defined while payment-profile setting/link exists; replacement/removal enters logical-removal policy. Current per-file validation has no actor retained-byte quota or upload-rate limit, and replacement marks old metadata deleted without physical byte reclamation. |
| Supporting attachment | Policy-defined dependency-gated while parent attachment/history exists. Current per-file validation has no per-bill/account retained-byte quota or upload-rate limit, and logical attachment removal is not capacity reclamation. |
| Statement/import source | Required but duration unresolved. Direct CSV preflight/import holds request-scoped copies with no zeroization proof; direct import creates draft bills/audits. Persisted `ReviewJson`/`CandidateJson` survive confirmation, discard and lazy expiry, and repeated valid creation has no aggregate row/byte quota, rate limit or sweep. Creation, confirmation, discard or expiry may commit before an uncaught interruption returns no success; retry cannot prove the prior result, and terminal GET exposes review-derived data rather than `CandidateJson`. No status removes row/fields; aggregate capacity, retention/disposal and reconciliation are absent. |
| Export package | Direct response is request-scoped, but generation materializes private full bill graphs with GC-only lifetime, personal-profile or group-membership authorization can become stale before delivery, and readiness's promised execution audit is absent. Web JSON export additionally materializes response text, a parsed graph, reserialized text, Blob/object URL and user-saved file. Future server expiry cannot govern process/browser/device/saved copies; inventory/disposal unresolved. |
| Backup package | API TTLs are lazy; preparation checks profile only before build, so mid-build deletion can still publish a new ready artifact. Build failure can preserve a prior artifact. Builder-only full bill/item relationship graphs may hold private fields excluded from the artifact and have GC-only lifetime. Summaries/keys have private retention gaps. Mobile retains JSON/cross-account queue. Web creates Blob/object URL/saved file and restore File/full text. Server expiry/discard cannot revoke these. Future persistence is dependency-gated; authorization, safe metadata, retry, encryption, copy/replica/hold and client-disposal rules unresolved. Destructive/manual disposal. |
| Downloaded attachment client copy | The generated Dart client mutable full response list and repository's separate unmodifiable full list are client-controlled process memory after authorized download; server link/object inactivity cannot recall either. No local IDs, clocks, cache inventory, wipe or disposition audit. |
| `pending`/`upload_failed` | Transient cleanup candidate after positive proof; clock unresolved. |
| `deleted`/Trash | Configurable Trash retention is architecture direction; duration and clock trigger unresolved. Expiry does not purge. |
| `quarantined` | Required but duration/review/rejection outcome unresolved; no automatic disposal. |
| `purged` tombstone | Required tombstone/history retention unresolved; marker does not establish byte outcome or minimize metadata. Current original filename, type, size, hash, provider key, vault-key reference, retention text, owner, purpose and timestamps remain, and each field's retained/redacted/removed treatment requires an independent approved rule. |

Physical purge and immediate hard delete are separate operations from each
other and from user-visible removal, retention expiry, unlink, and metadata
marking. `OBJ-009` starts only from retained `deleted` authoritative content;
`CLN-004` starts only from positively proven non-authoritative temporary or
failed material. Before either provider operation,
the API-owned decision must positively establish retention eligibility,
dependency and hold clearance, retained-copy disposition, exact target and
version, approved authority, consequence warning/confirmation where applicable,
and a durable retry/audit record. After provider work, the API accepts only a
bounded success/already-absent/failed/unknown result and preserves required
tombstone/history. This policy executes none of those actions.

## 10. Explicit Open Choices

| Choice ID / exact question | Affected authority domain | Why current sources cannot answer | Safe default or blocked posture | Decision owner / exact manual gate | Downstream work blocked |
| --- | --- | --- | --- | --- | --- |
| `FILE-LC-CHOICE-001` — What are each purpose/state retention duration, clock trigger, hold, and tombstone rule? | Server file objects, links, packages, audit and local copies. | The current field accepts arbitrary nonblank text; architecture defines classes but no numbers, clocks, holds, or tombstone duration. | No automatic expiry or purge. | #724 technical policy plus human trust/privacy decision; `FILE-LC-GATE-006` (and `005` for disposal). | Cleanup scheduling, disposal, restore eligibility, admin retention policy and runtime acceptance. |
| `FILE-LC-CHOICE-002` — Which actors and exact mechanics restore an object and separately reactivate each link, including replacement/multi-link conflicts? | Server file-object lifecycle and bill/proof/payment-profile/domain-link authority. | No restore service, endpoint, schema metadata, client method, actor matrix, or conflict contract exists. | No restore and no silent reattachment. | #722 after #961 plus subject-domain owners; `FILE-LC-GATE-002`, `003`, and #723 `FILE-LC-GATE-004`. | Object restore, link reactivation, Trash/client surfaces and domain acceptance. |
| `FILE-LC-CHOICE-003` — Which source states enter quarantine, who may inspect/release/reject, and what reasons/outcomes persist? | Upload security, storage authorization, privacy/vault and maintenance inspection. | The state exists, but no transitions, scanner result model, inspection authority, reason metadata, or API exists. | Ordinary access blocked; unavailable/failed scanner is not clean. | #1062/#966 and human security/privacy authority; `FILE-LC-GATE-001`, `006`, `007`, `008`. | Quarantine/release/rejection runtime, safe preview, maintenance read and acceptance. |
| `FILE-LC-CHOICE-004` — What reference registry, copy inventory, leases, consistent-snapshot proof, and idempotency record establish orphan/failed-upload cleanup eligibility? | File objects, every domain/history link, OCR history, jobs, packages, copies and provider execution. | No complete registry/proof transaction/cleanup record/lease/inventory/runner. Bill items retain restrictive OCR review/line references, while revision JSON retains both ID classes without FKs; all lack accepted target-versus-tombstone decisions. Reservation/activation, audit/notification staging, association save, compensation, post-commit response reload and removal can have unknown outcomes. Unguarded compensation can throw. Local `CreateNew` can leave occupied targets; concurrent/missing-response retries can leave active unlinked objects or duplicate live links. | Unknown evidence blocks cleanup; failed/canceled/exception-stranded is not disposable. Normalized and embedded IDs block applicable disposition. Missing response never proves link absence. Do not retry occupied key; validate bytes or authorize new target/old-copy disposition. | #724/#966 plus bill-revision/file/OCR owners; gates `001`, `002`, `003`, `005`, `006`, `007`, `010`. | Complete reference inventory, target/tombstone policy, staging/association/response reconciliation, orphan classification, cleanup executor/outcome. |
| `FILE-LC-CHOICE-005` — How are thumbnail/preview derivatives identified and linked without a second purpose enum, and when are they reproducible? | Receipt/OCR intake, storage metadata/links, preview security and clients. | Mobile allocates multiple complete source copies before its compressed-byte check, then decodes accepted input without dimension/pixel/decoded-memory/decompression bounds and creates resized/encoded arrays; server has no derivative relation/persistence/safe preview/reproducibility record, and local buffers have no wipe evidence. | No independent server cleanup or inactive preview; require approved pre-allocation plus decoded-dimension/decompression bounds. Dropping references is not buffer disposal proof. | #1062/#966/#358/#971; gates `001`, `002`, `003`, `006`, `008`. | Derivative schema/API, generation/read, decode capacity, local buffer/source and persisted retention/cleanup. |
| `FILE-LC-CHOICE-006` — How are direct/session-backed imports and persisted export versus backup packages distinguished, authorization/integrity-validated, capacity-bounded, encrypted, expired, downloaded, and copy-disposed? | Sync/import/export/restore, storage, privacy/vault, bill/money import and recovery. | Direct exports can use stale authorization, advertise an audit they do not emit, hold complete CSV copies, materialize unbounded related-row graphs, and apply group A results after selecting B. Persisted imports can have stale/unknown commits and references, retain JSON after every terminal state, and grow without aggregate row/byte quota, rate limit or sweep. API backup has authorization/concurrency/contract/private-metadata, unbounded top-level static entries, and an independently unbounded nested download-action dictionary. Its web cancel/discard adapters falsely report terminal states. Mobile has cross-account JSON. Web export/restore/import creates multiple copies; pre-read gates are absent, and asynchronous results lack identity binding. | Binding is not current authz. Require safe metadata, pre-read bounds, bounded serialization/copy lifetime, immutable request binding, returned-state validation, aggregate response/process/persistent-storage and nested-action capacity/rate/sweep controls, aggregate related-row/query-result bounds, truthful audit, immutable action/artifact binding, atomic idempotency, commit reconciliation, final eligibility, candidate-reference revalidation and per-platform copy inventory. Server expiry/discard/session completion never proves copy cleanup. | #406/#971/#724/#966 plus API/web/mobile/money owners; gates `001`, `002`, `003`, `005`, `006`, `008`, `011`, `012`. | Authorization/capability/audit parity, request/response/capacity bounds, selection/request-copy concurrency, client/API transition parity, build retry, safe metadata, idempotency, process/browser/device copy inventory, import ambiguity, retention/integrity/recovery/disposition. |
| `FILE-LC-CHOICE-007` — What client-local scratch/cache/offline/download/browser-file model, account ownership, clock, secure storage, source ownership, intake/decode/structure bound, selection identity, and acknowledgment proof apply per platform? | Mobile/web client copies, queue, capture/OCR, receipt/supporting attachments, pasted backup JSON, package/file downloads, bill-import CSV selection, and server handoff. | Mobile retains paths, processed-receipt pre-limit copies and unbounded decoded pixels, existing-bill full-byte picker copies, downloaded lists, cross-account backup JSON, and unrestricted pasted text/decoded graph with recursive traversal. Web retains strings, graphs, Blob/URL/saved file/selected File; bill-import and restore read before a client size gate. Async import/backup/export results lack complete identity binding. Sources define no common ownership, clock, inventory, capacity/depth, revocation, erasure or disposition. | Client material is not server authority. Require approved pre-read/pre-allocation, decoded-dimension/decompression, and pasted-text/node/collection/depth bounds plus immutable request-generation validation. Server lifecycle cannot recall copies. Treat unscoped queue as prior-account data; never log/disclose paths or contents. | #971/#358/#406/#966/#724 plus mobile/web owners; gates `005`, `006`, `008`, `011`. | Account-safe queue, bounded intake/decode/structure, selection concurrency, source/cache/download/browser-copy cleanup, OCR dependency, backup interoperability, offline acknowledgment, secure retention/device/browser acceptance. |
| `FILE-LC-CHOICE-008` — Should current metadata `purged` be redefined/renamed or paired with explicit provider-disposition state, and which tombstone fields remain? | Server lifecycle metadata, provider orchestration, API/contracts, audit and client wording. | Current transition records only status/audit while no production code deletes bytes or stores provider outcome; it also retains the full metadata row rather than applying field-specific minimization. | Treat as incomplete, non-minimized tombstone marker; never say permanently deleted or metadata minimized from it. | #724 then #722; `FILE-LC-GATE-002`, `003`, `005`, `006`, `007`. | State/schema contract, per-field retention/redaction/removal, physical orchestration, audit, restore blockers and terminal wording. |

## 11. Follow-Up Ownership And Duplicate Prevention

Live GitHub was checked on 2026-09-15 before editing. Merged work is credited
even when umbrellas remain open:

| Gap | Existing owner / canonical lane | Recommendation |
| --- | --- | --- |
| Purpose limits, normalization, retention hooks | #341, storage-file-privacy-authz | Reconcile rather than duplicate; #966 determines remaining runtime slices. |
| Malicious-file validation, safe previews, quarantine | #1062, storage-file-privacy-authz/security | Reuse. Do not absorb into #719. |
| Storage/file completeness and runtime split | #966, storage-file-privacy-authz | Consume this policy after merge; it remains inactive here. |
| Retention, dependency proof, orphan/failed cleanup and terminal disposition | #724 after #961, retention/audit/maintenance | Reuse umbrella and require it to split focused runtime/maintenance owners. Recommend a new orphan-cleanup child only if its fresh search still finds none. |
| API/OpenAPI/generated clients and persistence/schema | #722 after #961, separate API/contract/schema lanes | Keep separate and manual-gated; generated clients are never hand-edited. |
| Trash/restore/purge presentation | #723, mobile/web/admin UI with Figma/manual gate | Reuse; no UI decision here. |
| Admin policy/maintenance/readout | #466/#467 and #964, web-admin/maintenance | Reuse; admin remains redacted and has no blanket content access. |
| Privacy vault sensitive file integration | #343 with merged #418–#422 policy packets; #966 runtime reconciliation | Credit the merged policies; do not claim vault runtime or create a replacement umbrella. |
| Receipt intake/derivatives/OCR | #358/#357 and #1062/#966, mobile/OCR/storage lanes | Keep normalization, OCR processing, file authz, and lifecycle ownership separate. |
| Settlement proof/payment QR domain behavior | Existing settlement/payment endpoints and #966; domain changes stay settlement/payment-details lanes | Do not move money, payment, visibility, or settlement authority into cleanup. |
| Import/export/backup/restore packages | #406/#971, sync-import-export-restore | Reuse; browser/package/local paths never become server storage authority. |
| Future provider tiering/multi-target | #1076, later-day provider/deployment lane | Preserve abstraction only; no Day 2 runtime enters Day 1. |

#719 creates no runnable issue. #723, #724, #966, and later #961 retain their
separate close rules and are not silently completed by this document.

## 12. Evidence, Manual Gates, And Stop Conditions

Primary implementation evidence includes:

- `Domain/Files/FileObject*`, `Persistence/SettleoraDbContext`, and the file
  metadata migration/model tests for enum, field, index, and restrictive-FK
  facts;
- `Storage/EfFileObjectLifecycleService`, its `IsActorAvailableAsync` call sites,
  audit writer, provider interface,
  local provider, and lifecycle/provider tests for transition and byte-operation
  facts, including the actor-eligibility check-to-save race for reservation and
  every status transition, `FileMode.CreateNew` partial-write/retry behavior and the
  metadata-only, non-minimizing `MarkPurgedAsync` transition;
- bill attachment, settlement proof, self payment QR, and counterparty payment
  detail endpoints/tests for file-sized `MemoryStream` plus `ToArray()` upload
  double-buffering, cancellation, link, authz,
  reservation metadata reload, QR post-activation profile load, read, remove,
  audit/notification staging, association compensation, post-commit response
  reload, successful unaudited attachment/proof/self-payment-details metadata
  reads, removal's unconditional shared-object logical deletion, and failure
  facts; `SettleoraDbContext`/schema tests show the bill/proof file-object
  indexes are non-unique, so another active link can remain when that object is
  marked `deleted`;
- OCR review/assignment and notification persistence for retained file-ID
  dependencies; `ExpenseBillItem`/model configuration for restrictive
  `SourceReceiptOcrReviewId` and `SourceReceiptOcrReviewLineId` relationships;
  `BillRevisionSnapshotFoundation`, revision persistence and revision endpoint/
  service tests for `attachmentFileIds` and `receiptOcrReviewIds` embedded in
  baseline/proposed JSON without direct `FileObject` or `ReceiptOcrReview` FKs;
- current OpenAPI plus generated Dart/web methods and serialization privacy
  tests for exposed surfaces and provider-internal exclusion;
- `ExpenseBillExportEndpoints`, personal visibility queries, and
  `ExpenseBillSearchQueries.WithBillDetails` for pre-query personal/group
  authorization, personal requesting-participant deletion and non-membership-
  predicated group row-loading gaps,
  direct-export full-graph working copies without aggregate related-row/query-
  result bounds and with possible sibling-include row multiplication, projected output, complete CSV
  `StringBuilder` character storage plus its separate `ToString()` result, and
  the readiness audit claim without an execution audit writer/event; and
- `LocalBackupPackageReadinessEndpoints` and tests for process-local package
  sessions, artifacts, download actions, lazy expiry and reference clearing;
  its three static dictionaries, creation assignments, absence of per-actor or
  global aggregate capacity controls, absence of top-level removal sweep, and
  repeated nested action issuance without per-session count/memory or rate bounds;
  package/preview/confirmation authorization branches for the preparation-only
  `CanAccessProfileAsync` check; builder first-profile-query/final-publication
  race, `WithBillDetails` private working graphs, exception boundary and prior-
  artifact preservation plus download actions without immutable artifact/version
  binding across reprepare; restore parsing/mapping for retained caller `sections[].name`
  strings; and confirmation storage/mapping for the private internal key,
  response-visible accepted boolean, non-atomic same-key race, and
  `UnsupportedPackageArtifactFeatures` reuse by session creation, preparation,
  artifact-status, and download-action response mapping;
  restore declared-length guards, accepted full-content working copies, and preview-record aggregate
  counts that can exceed the OpenAPI field maximum;
  `BillCsvImportEndpoints`, its entity/configuration, and endpoint tests for
  direct preflight/import plus persisted-session storage/read/terminal facts,
  `ReadCsvBodyAsync`'s bounded `MemoryStream` request-byte accumulation and
  separate decoded full-string copy, plus session creation's
  `ComputePayloadDigest` full UTF-8 re-encoding,
  including uncaught discard/lazy-expiry saves, stored-binding-only GET/discard
  authorization, both session-confirm plus direct-import stale participant/
  payer reference windows, unconditional retained JSON row insertion, and the
  absence of aggregate row/byte quota, rate limit, sweep, or removal;
  mobile picker/picked-file validation, `ReceiptImageArtifactProcessor`, and
  tests for pre-limit full-byte copies, in-memory normalized/thumbnail bytes,
  decoding without dimension/pixel/decoded-memory/decompression bounds,
  decoded/resized pixel buffers and deferred secure-cache posture;
  generated Dart attachment client plus mobile repository/UI for the mutable
  response list and separate unmodifiable full downloaded list;
  generated web-client JSON response parsing and web `importExportReadout` for
  response-text/parsed-graph/reserialized-text/Blob/object-URL/saved-file/File/
  full-text copies, direct CSV response Blob/object-URL/saved-file copies,
  broad non-created-state discard enablement and the adapter's unconditional
  local `discarded` result despite unchanged ready/blocked/cancelled/expired
  server sessions, plus cancellation's unconditional local `cancelled` result
  despite the unchanged blocked server session,
  backup restore's `File.text()` before any matching client size gate, retained
  `packageContent`, generated-client `JSON.stringify(body)` request copy, and
  preview-discard and confirmation create/refresh/discard results committing
  after enabled-input reselection without a current-selection/request-generation
  check;
  web bill-import selection for its separate browser `File`/full-string copies
  and its `File.text()` before an API-limit-matching client size gate, including
  the absence of selection/request generation guards that permits an older
  read to overwrite text under newer filename/size state and permits an
  in-flight session create/confirm/discard responses to reinstall the old
  session (and creation to clear the new selection's text), plus enabled scope/
  group changes that do not clear or rebind the existing session;
  web group-export selector/readiness/download handlers for unbound group A
  results or saved files completing after group B is selected; and
  web backup package `canCancel` versus API accepted source states and the
  adapter's false local cancellation result;
  mobile `local_data_backup.dart`, App
  settings, and their tests for generated/rendered/replaced/pasted backup JSON
  copies, unrestricted pasted text/`jsonDecode`, recursive traversal without
  character/node/collection/depth bounds, and preview-only behavior; and
- bill/proof/QR upload handlers for `ReadFormAsync` preceding their later
  `IFormFile.Length` checks and the absence of a repository `FormOptions` or
  equivalent pre-read bound/cleanup contract;
- mobile existing-bill attachment kind selector plus picker/validation/upload
  mapping for receipt and supporting kinds, `withData: true`, retained complete
  bytes, the additional unmodifiable upload list, and absence of the receipt
  processor's pre-upload size gate on this route; and
- bill-revision list/detail mapping and snapshot tests for ordinary authorized
  bill-viewer exposure of the proposed snapshot's retained receipt file ID
  without a current attachment-link or object-state check; and
- bill receipt/supporting, proof, and QR upload handlers/tests for per-file
  limits, logical removal/replacement without physical byte reclamation, and
  the absence of applicable per-bill, per-payment, or per-account retained-byte
  quotas or upload-rate limits; and
- the architecture sources linked above for documented-only policy direction.

Live issue/PR reconciliation at that revision found #960 closed through merged
PR #1237; #721 closed through PR #1238; #719/#716/#717/#723/#724/#966/#975
open; #418–#422 closed through their reviewed planning/UX PRs; and no existing
#719 branch, PR, merged equivalent, or focused orphan-cleanup issue. Open state
alone was not treated as proof of missing implementation.

The following gates remain separate and pending whenever a future change
touches them: storage/file privacy and authorization; schema/migration;
API/OpenAPI/generated clients; bill calculation/money mutation authority;
product retention/trust choice; Figma/client UX;
admin/public exposure; vault/security/key management; provider/configuration;
destructive cleanup or purge; production operations/deployment; and local
platform storage. This documentation review satisfies none of them.

Stop future work rather than infer authority when object and link state are
conflated, a read bypasses either inactive condition, restore lacks current
revalidation, failed/inaccessible is treated as disposable, dependency or copy
proof is incomplete, a worker would mutate business tables, provider internals
would escape, retention expiry is treated as purge, current `purged` is treated
as physical proof, or any manual gate remains unsatisfied.

## 13. Validation And Acceptance Contract

This policy is complete only as a planning artifact. Runtime acceptance later
must prove, for every applicable purpose and transition:

- deterministic inventory coverage using the row IDs above and all existing
  purpose/status symbols;
- object/link independence, multi-reference preservation, and restrictive
  history behavior;
- ordinary metadata/content/preview denial for every inactive combination;
- owner, participant, counterparty, maintenance, and hostile unrelated-actor
  authorization without admin blanket access or enumeration leakage;
- restore revalidation, duplicate/conflict/idempotency/concurrency behavior,
  and no silent reattachment;
- storage-write, metadata-write, association-write, provider-read/delete,
  timeout, retry, crash, and partial-failure reconciliation;
- complete dependency/hold/copy proof before cleanup and terminal disposition;
- bounded lifecycle/denial/cleanup/disposal audit with sensitive/provider-
  internal exclusion; and
- exact OpenAPI/generated-client parity, client unavailable/blocked states,
  approved UX evidence, provider-operational proof, and destructive/manual
  approval where those lanes apply.

Planning text, enum presence, a metadata marker, a worker result, an available
generated method, or a passing ordinary-read test is never proof that restore,
retention, cleanup, physical deletion, or terminal disposal is implemented.
