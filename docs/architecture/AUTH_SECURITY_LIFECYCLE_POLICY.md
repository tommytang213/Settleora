# Auth And Security Lifecycle Policy

Issue: [#721](https://github.com/tommytang213/Settleora/issues/721)

Parent chain: [#716](https://github.com/tommytang213/Settleora/issues/716) →
[#717](https://github.com/tommytang213/Settleora/issues/717) → #721; shared
template: [#960](https://github.com/tommytang213/Settleora/issues/960).

## 1. Decision And Authority

This document defines Day 1 lifecycle wording and retention posture for
server-mode authentication and security records. It applies the
[shared lifecycle taxonomy](../planning/DAY1_LIFECYCLE_POLICY_TEMPLATE.md) to
the current repository at `a93409c306133ef7b9c4d1861f36f69f724b011e` (tree
`956339fed075713be12f3d74c0742eb2cc4414ac`). It is policy and factual
reconciliation only: it does not change runtime behavior, persistence,
contracts, clients, configuration, providers, exposure, or data.

API/domain auth services remain authoritative for server-mode account,
identity, role, credential, factor, challenge, session, transient abuse state, policy, and audit
writes. A client label or generated method never proves authorization or an
accepted transition. Local-only mode has no server auth account/session
records: device biometric, app-PIN, and local secure-storage lifecycle belongs
to a separate local-device security owner. Nothing here turns local profile
state into a server identity.

The original #721 wording is stale where it calls passkeys, TOTP/MFA, recovery
codes, and challenges “future” families. The merged
[auth completeness audit](../qa/AUTH_DAY1_COMPLETENESS_AUDIT.md) and current
source prove persisted records and substantial API runtime for each. Older
architecture text is retained as historical design evidence only where it
conflicts with current source. `REC-AUTH-DOC-DRIFT-001` remains the owner for
reconciling those canonical documents and the unresolved immediate one-time
response rules.

## 2. Vocabulary And User-Visible Wording

- **Revoke** means make a grant, credential, factor, session, invitation, or
  lineage unusable. It does not mean erase the row or its evidence.
- **Disable** means block eligibility under a reversible administrative or
  policy condition. It is not automatically revoke and never implies that
  existing sessions were invalidated.
- **Expire** means validity ended at a server-controlled time or policy limit.
  It is not an actor-initiated revocation and is not a purge trigger.
- **Rotate / replace** means create new authority or verifier state and make
  the predecessor unusable. It does not erase predecessor evidence.
- **Unlink** means end an identity-provider relationship. It is not account
  disablement, profile deletion, or historical-reference removal.
- **Retain** means preserve the minimum justified record or safe metadata for
  enforcement, replay detection, investigation, or audit. A stored hash is not
  automatically safe or necessary to retain indefinitely.
- **Reactivate / re-enable** is a new authorized transition after current
  policy, recovery, dependency, and lockout checks. It is not silent restore.
- **Retention-expired** means a retention condition ended. It does not execute
  hard deletion or prove disposal eligibility.
- **Purge / hard delete** is a separate irreversible disposition action. No
  ordinary account-security control should be labelled “Delete” when the
  actual effect is revoke, disable, replace, or unlink.

Approved presentation verbs when a client later exposes the corresponding
currently implemented API actions are
“Sign out”, “Sign out all sessions”, “Revoke session”, “Change password”,
“Reset password”, “Revoke passkey”, “Cancel authenticator setup”, “Revoke
authenticator”, “Generate new recovery codes”, and “Revoke recovery codes”.
“Remove passkey/factor” is acceptable only when adjacent copy says the
credential is revoked and security history may remain. Account administration
must use “Disable account” and “Re-enable account”; identity administration
must use “Unlink identity”; role administration must name the exact role
assignment or removal.

## 3. Secret-Material Boundary

Lifecycle history must never preserve reusable authentication material merely
to prove that an event happened. Documentation, logs, audit, notifications,
metrics, traces, reports, examples, and later metadata/read APIs must never
contain plaintext or reversible passwords; raw access or refresh credentials;
raw password-reset or invitation material; raw recovery codes; submitted OTPs;
TOTP seeds, provisioning URIs, or QR payloads; passkey private material; raw
WebAuthn assertions or reusable challenges; provider tokens/payloads; or
private signing, encryption, pepper, or data-protection keys.

Current persistence uses password verifiers, purpose-bound token/material
hashes, challenge verifiers, recovery-code salted verifiers, protected TOTP
payloads, and passkey public material. Those values remain security-sensitive.
Their existence does not authorize indefinite retention. In particular:

- a revoked password verifier may not be kept merely as lifecycle evidence;
- a revoked/disabled TOTP factor must not retain decryptable reusable secret
  material merely for history;
- terminal reset/invitation hashes and recovery-code verifiers need an explicit
  enforcement or replay-detection purpose and bounded retention decision;
- passkey public keys and credential lookup hashes are not private keys, but
  their terminal retention still needs a bounded policy; and
- access/refresh hashes may be retained only as long as a defined lookup,
  revocation, replay, or investigation purpose requires them.

Immediate successful issuance/display responses are transport boundaries, not
lifecycle evidence. Current source returns access/refresh credentials once,
TOTP setup material during enrollment start, and recovery codes once at batch
generation. Canonical documents conflict over those response rules. This
policy neither expands nor resolves that behavior; `AUTH-LC-CHOICE-012` keeps
the manual security decision open.

## 4. Row Contract And Common Rules

The three synchronized matrices in sections 5.1, 5.2, and 5.5 together form
each policy row. These common
values apply to every row unless its cell states otherwise, satisfying the
shared-template fields without repeating unsafe boilerplate:

- **Authority mode/workspace:** server mode, within one self-hosted workspace.
- **Authoritative boundary:** the API/domain auth service named by current
  source; clients and workers cannot accept transitions.
- **Actor/authorization:** current authenticated account for self-service and
  owner/admin or system only where explicitly designed. Anonymous protocol
  callers are separately bounded: local sign-in presents an identifier and
  password proof; password-reset initiation presents an identifier but must
  receive an enumeration-resistant no-authority result; refresh presents a
  refresh credential; invitation/reset completion presents its purpose-bound
  material; passkey sign-in and MFA challenge completion present a
  server-issued challenge plus the required authenticator proof. Current MFA
  challenge issuance/completion does not prove the intended anonymous
  pending-flow binding described in `AUTH-LC-CHL-008`; therefore the bounded
  scope is a requirement, not current credit. Protocol proofs authorize only their exact
  protocol transition; they are not a current-account actor, and the server
  must enforce the persisted account/session/purpose/credential/factor binding.
  Possession of an ID, route, or generated method is not authorization.
- **Preconditions:** authoritative current state, account availability, target
  ownership, policy eligibility, fresh step-up where the current factor runtime
  requires it, and a persistence transaction/concurrency guard where the
  transition can race. Unimplemented administration also requires lockout and
  last-owner proof.
- **Ordinary read/mutation effect:** terminal security authority is excluded
  from authentication use. Retained rows may remain available only to an
  authorized bounded security/audit read; ordinary clients must not infer
  authority from cached status.
- **Persisted lifecycle metadata:** stable subject ID, bounded prior/new status,
  applicable created/issued/updated/expiry/consumed/revoked/disabled/replaced
  timestamps, safe actor/reason/correlation fields, and a concurrency/version
  mechanism where a future mutation is added. Absence of one of these fields
  in current source is an implementation gap, not permission to invent it.
- **Idempotency/replay/concurrency:** current endpoint behavior is credited only
  where source/tests prove it. Otherwise the retry result and conflicting
  transition outcome are unresolved and the future mutation is blocked.
  Replaying one-time material never restores usability.
- **Dependency checks:** account state, role/last-owner safety, remaining sign-in
  methods, current and family-linked sessions, factors/recovery, policy version,
  audit, notifications, and backup/export/replica disposition where relevant.
  No current source proves complete backup/export/replica disposition.
- **Hard-delete/purge:** `ineligible` for ordinary user/admin actions.
  `unresolved` for a separate retention cleanup; it requires explicit target
  classification, retention and hold proof, all dependency/copy checks,
  authority, audit, retry/concurrency safety, consequence warning and explicit
  confirmation when user-initiated, plus every destructive/privacy/security
  manual gate. Retention expiry alone is insufficient.
- **Audit:** where applicable, record success and security-relevant denied,
  blocked, expired, replay, and conflict outcomes with actor/account ID when
  safely resolved, subject type/ID, action, bounded prior/new status, reason
  category, UTC timestamp, correlation/request/idempotency metadata, and no
  secret or private payload. The matrices do not claim an event exists unless
  implementation evidence says so.
- **Client presentation:** refresh server state after mutation; show precise
  lost-usability and session impact; preserve generic public failure shapes;
  never claim deletion, restoration, delivery, or notification that the server
  did not prove; expose a blocked/unavailable state rather than deciding policy
  locally.
- **Manual gates:** policy/runtime, schema/migration, OpenAPI/generated clients,
  platform UI/Figma, provider/configuration, public/admin exposure, destructive
  disposal, and privacy gates remain separate. A satisfied docs review does not
  satisfy any implementation gate.
- **Runtime acceptance:** future work needs focused service/endpoint tests,
  transaction and concurrent/replay tests, authorization/lockout tests,
  redaction/no-secret scans, OpenAPI/generated parity when applicable, client
  state tests, and manual hostile/provider/device/exposure evidence where the
  lane requires it. This policy is never runtime proof.

## 5. Lifecycle Transition Matrix

### 5.1 Transition, Authority, And Usability

| Row ID | Record family and user-visible action | Internal transition and actor | Authentication / authorization / session effect | Reversibility, dependency checks, implementation evidence |
| --- | --- | --- | --- | --- |
| `AUTH-LC-SES-001` | Current access session — **Sign out** | Current account requests the selected session `active → revoked`; `AuthSessionRuntimeService` executes with `user_sign_out`. | The selected access credential stops. Linked sibling sessions, family, and refresh credentials are separate roots below. If refresh rotation overlaps selection, replacement authority can escape; family status is not consulted during access validation. Independent families remain usable. | Irreversible for this row; sign in creates new authority. Implemented and idempotent after validation, but provider rollback and rotation-versus-revoke authority are partial under `AUTH-LC-CHOICE-010`. Evidence: `SignOutEndpoints`, `AuthSessionRuntimeService`, `SessionRevocationEndpointTests`. Owner #338. |
| `AUTH-LC-SES-002` | Selected owned access session — **Revoke session** | Current account requests an owned session `active → revoked`; not-owned/missing/already-terminal targets use a non-disclosing unavailable result. | Target access credential and observed linked family authority stop working; an overlapping refresh rotation can insert a replacement session outside the selected revoke sets, and that access credential can remain usable. Unrelated families remain usable. Authorization records are unchanged. | Irreversible for revoked rows; reauthentication creates new authority. Implemented sequentially; target ownership and current-session self-revoke are tested, while rotation-versus-revoke is partial under `AUTH-LC-CHOICE-010`. Owner #338/#774. |
| `AUTH-LC-SES-003` | Account sessions — **Sign out all sessions** | Current account requests each unexpired active access session selected by `RevokeActiveSessionsForAccountAsync` `→ revoked`; the service revokes refresh families discovered through credentials linked to those selected sessions. | Selected bearer sessions and discovered family continuity stop. A valid refresh credential whose only linked access session is already expired is not discovered; overlapping rotation can also insert a replacement session after selection, and session validation does not consult the revoked family. Either access authority can remain usable. Account, identities, password/factors, and roles remain. | Irreversible for revoked rows; later sign-in is possible. Implemented but cascade/concurrency completeness is partial under `AUTH-LC-CHOICE-010/013`; owner #338. |
| `AUTH-LC-SES-004` | Access session — not user-initiated: **Expired** | Server validation observes `ExpiresAtUtc` reached; the row can remain stored even if its status is still `active` until another path materializes status. | Credential cannot authenticate or authorize; expiry does not revoke unrelated sessions or change roles. | Irreversible for that session. Source-defined validity from `Settleora:Auth:Sessions`; no expiry cleanup exists. Owner #338/#1059. |
| `AUTH-LC-SES-005` | Access session — **Sign in** with local password | After local identity/account/password verification, `AuthRefreshSessionRuntimeService` creates this active access-session root in the same save as the separately inventoried family, refresh credential, and creation audit. Raw access/refresh values are generated and held transiently in process before that save, then returned to `LocalSignInService`; external handoff occurs only after a later `sign_in.succeeded` audit save. Current local sign-in does not require the MFA challenge flow. | If the whole workflow returns success, the access credential authenticates until expiry/revocation; roles are read from current assignments. If the final audit save fails, the endpoint hands no material to the caller although transient raw values existed and active authority remains persisted and inaccessible. Retry creates independent authority. Old-password proof can also escape concurrent change/reset selection. | Issuance is not reversible; revoke/expire are separate. Implemented sequentially; final-audit handoff/failed-path in-memory material disposal, credential-replacement serialization, and MFA binding are partial under `AUTH-LC-CHOICE-002/010/012` and `REC-AUTH-MFA-SIGNIN-001`. Owner #338/#339. |
| `AUTH-LC-SES-006` | Access-only session — passkey sign-in reports **Signed in** | Successful passkey completion calls `AuthSessionRuntimeService.CreateSessionAsync`, which persists an active access session, but the completion response omits `RawSessionToken` and the endpoint sets no cookie/header. | The new record exists, but the caller receives no bearer credential and therefore cannot use that session for a later authenticated request. No refresh family is created. | The record can expire/revoke but issuance is unusable to its caller. Current source behavior is incomplete under `REC-AUTH-PASSKEY-SESSION-001`; do not present successful authenticated handoff. Owner #394/#338. |
| `AUTH-LC-SES-007` | Active access session — not separately user-visible: **Validate/use session** | A successful authenticated request validates the bearer, account availability, and session time/status, then updates `LastSeenAtUtc`/`UpdatedAtUtc`, writes validation audit, and returns the current actor/roles. Family status is not consulted. | The request may proceed with current roles; validation does not extend expiry or create refresh authority. Concurrent validation can load `active` before revocation and later overwrite timestamps/audit after revoke; out-of-order validations can regress last-seen metadata. | Use is repeatable, not restore/reactivation. Implemented sequentially; validation-versus-revoke ordering, monotonic last-seen, and family-aware denial are partial under `AUTH-LC-CHOICE-010`. Owner #338/#774. |
| `AUTH-LC-SES-008` | Access session — refresh protocol reports **Session refreshed** | A valid active refresh credential authorizes creation of a new active access session in the same family while `AUTH-LC-REF-001` separately rotates the credential. Raw access material crosses only the immediate refresh response boundary. | The new access credential can authenticate until expiry/revocation; current roles are read when requests authenticate. The predecessor access session is not thereby revoked, family absolute expiry is unchanged, and overlapping revoke/replay can miss the new row. | Issuance is not rotation or reactivation. Implemented transactionally with credential rotation on the relational path, while retry is replay rather than success replay and rotation-versus-revoke/replay remains partial under `AUTH-LC-CHOICE-010/012`. Owner #338. |
| `AUTH-LC-SES-009` | Current-family sibling access sessions — **Sign out** | Current account's selected session authorizes observed active sibling sessions in the same refresh family `active → revoked` within `RevokeSessionAsync`. | Observed sibling bearer authority stops; unrelated families remain. Overlapping rotation can insert a sibling after selection, and an already-revoked selected-session retry can return before repairing a prior partial provider save. | Irreversible for transitioned rows. Relational `SaveChanges` has provider transaction semantics; the non-relational branch has one save without rollback proof. Selection/race/retry/failure atomicity is partial under `AUTH-LC-CHOICE-010`; owner #338. |
| `AUTH-LC-SES-010` | Selected owned session's family sibling access sessions — **Revoke session** | Current account ownership of the target session authorizes observed active sibling sessions in its refresh family `active → revoked`. | Observed sibling bearer authority stops; unrelated families remain, while overlapping rotation can escape. | Irreversible for transitioned rows. Relational versus non-relational rollback, target retry after partial save, selection, and rotation concurrency are partial under `AUTH-LC-CHOICE-010`; owner #338/#774. |
| `AUTH-LC-SES-011` | Other access sessions after **Change password** | Authenticated current-password proof invokes account-session revocation excluding the current session; selected unexpired active non-current sessions change `→ revoked`. | Observed other bearer sessions stop. The current session remains; expired-session-linked refresh lineage, a family shared with the current session, and overlapping replacement authority can remain usable. | Irreversible for transitioned rows. Relational outer-workflow coupling exists, but non-relational rollback, old-proof ordering, selection, and concurrency are partial under `AUTH-LC-CHOICE-002/010/013`; owner #339/#338/#774. |
| `AUTH-LC-SES-012` | Account access sessions after **Reset password** | Valid anonymous reset-material proof and successful password replacement invoke account-session revocation without an exclusion; selected unexpired active sessions change `→ revoked`. | Observed bearer sessions stop, while lineage linked only through expired sessions and overlapping replacement sessions can remain usable. | Irreversible for transitioned rows. Relational outer-workflow coupling exists; non-relational failure can follow committed credential work. Provider rollback, selection, and concurrency are partial under `AUTH-LC-CHOICE-002/007/010/013`; owner #339/#338/#773. |
| `AUTH-LC-SES-013` | Family-linked access sessions after detected refresh **Reuse** | Linkable sequential replay authorizes observed active access sessions in the replayed family `active → revoked`. | Observed bearer sessions stop; overlapping rotation can escape and access validation ignores family status. The broken relational two-rotation loser can throw before this root changes. | Irreversible only when committed. Relational race handling, selection, provider rollback, and audit are partial/broken under `AUTH-LC-CHOICE-010`; owner #338. |
| `AUTH-LC-SES-014` | Family-linked access sessions after refresh **Expiry** | A matched expired refresh credential/family causes observed active linked access sessions `→ revoked` during expiry handling. | Observed bearer sessions stop; unrelated families remain. | Irreversible for transitioned rows. Implemented on the matched path; provider rollback, selection concurrency, and retained evidence remain under `AUTH-LC-CHOICE-010`; owner #338/#1059. |
| `AUTH-LC-SES-015` | Family-linked access sessions after account becomes unavailable — generic **Sign in again** | Refresh processing that resolves a disabled/deleted account changes observed active access sessions in that one family `→ revoked`. | Observed family-linked bearer sessions stop; other families are untouched and can become usable if account eligibility returns because access validation independently checks account state. | Irreversible for transitioned rows. Relational provider uses a transaction; non-relational provider uses one save without rollback proof. Account-wide completeness and audit are partial under `AUTH-LC-CHOICE-001/009/010`; owner #338/#788/#724. |
| `AUTH-LC-REF-001` | Predecessor refresh credential — not user-visible: **Rotate** | A valid `active` credential is status-qualified and consumed as `rotated`, with its consumed time and replacement-credential link recorded. Replacement credential/session issuance and family rotation metadata are the separate `AUTH-LC-REF-017`/`AUTH-LC-SES-008`/`AUTH-LC-REF-021` roots. | Old refresh material cannot refresh after commit. Rotation does not extend family absolute expiry or change roles. | Irreversible for the committed predecessor; retry with it is replay, not success replay. Relational status-qualified consume exists, but focused tests do not prove the complete transaction/race boundary. Owner #338. |
| `AUTH-LC-REF-021` | Active refresh family — not user-visible: **Record rotation** | Successful rotation keeps the family `active` while replacing `LastRotatedAtUtc` and `UpdatedAtUtc`; predecessor, replacement credential, and replacement session are separate roots. | Family continuity remains usable through the replacement credential; absolute expiry and roles are unchanged. Concurrent successful rotations can race this last-writer metadata, and the current audit identifies the account rather than the family root. | Repeatable metadata mutation, not reactivation. The relational path intends one transaction across roots; the non-relational path uses one save without rollback proof. Monotonic time, per-family audit correlation, provider failure atomicity, and race behavior are partial under `AUTH-LC-CHOICE-009/010`; owner #338/#724. |
| `AUTH-LC-REF-002` | Active refresh family — user sees generic **Sign in again** after detected reuse | A credential classified first as replayed, rotated/consumed, revoked, or otherwise inactive drives the matched family `active → replayed`; terminal-source rewriting is the separate `AUTH-LC-REF-022` root. Active credential and access-session roots are `AUTH-LC-REF-018` and `AUTH-LC-SES-013`. Unknown material stays generic. In the relational two-rotation race, the losing branch rolls back its still-current transaction and calls the family-revoke helper before disposal; the helper's nested `BeginTransactionAsync` throws, so the transitions/audits do not run. | The active family loses rotation continuity when committed. The broken relational loser can instead return an unhandled failure with the family unchanged; overlapping rotation and family-blind access validation remain separate authority risks. Unrelated families remain. | Irreversible only when committed. Sequential active-family replay is implemented, while the relational race branch is broken under `AUTH-LC-CHOICE-010`; owner #338. |
| `AUTH-LC-REF-022` | Terminal refresh family — generic **Sign in again** after reuse-classified material | Only a credential classified first as replayed, rotated/consumed, revoked, or otherwise inactive drives an already `revoked`, `expired`, or `replayed` family `→ replayed`, replacing reason and terminal-time metadata. An expired credential instead drives `AUTH-LC-REF-023`; an active credential in any revoked, replayed, or normally materialized expired family writes failure audit without family mutation because current terminal families have `RevokedAtUtc`. Credential/session root mutations, if any, are separately inventoried; unknown material stays generic. | The family remains unusable, so this transition removes no new live continuity, but it mutates retained terminal evidence. Repeated reuse-classified requests can replace the reason/time again without restoring authority. | Non-idempotent terminal evidence mutation, not active-family replay or reactivation. Implemented sequentially; exact credential/family source combinations, evidence integrity, per-family audit correlation, retry semantics, provider rollback, and concurrency are partial under `AUTH-LC-CHOICE-009/010`; owner #338/#724. |
| `AUTH-LC-REF-003` | Active refresh family — user sees generic **Sign in again** after expiry | Server time observes credential idle/absolute expiry or family absolute expiry and materializes the matched family `active → expired`; terminal expired-family evidence rewriting is the separate `AUTH-LC-REF-023` root. Credential and linked-session roots are `AUTH-LC-REF-019` and `AUTH-LC-SES-014`. | Identified active family continuity stops; unrelated families and role assignments remain. | Irreversible for that family. Implemented; no cleanup/retention period follows from expiry. Owner #338/#1059. |
| `AUTH-LC-REF-023` | Terminal refresh family — generic **Sign in again** after expired-credential use | An expired credential is classified before family state and drives an already `revoked`, `replayed`, or `expired` family `→ expired`, replacing terminal reason/time metadata through the expiry cascade. An active credential in a normally materialized expired family is classified as family-revoked first because current expiry writes `RevokedAtUtc`, so it produces failure audit without this rewrite. The separate `AUTH-LC-REF-003` row owns every `active → expired` family transition; resulting credential/session root mutations are separately inventoried. | The terminal family remains unusable and no new live continuity is removed, but retained terminal evidence can change from revoked/replayed to expired or have its expiry metadata refreshed. Active refresh credentials selected by the cascade become expired under `AUTH-LC-REF-019`; linked active access sessions become revoked under `AUTH-LC-SES-014`. | Non-idempotent terminal evidence mutation, not active-family expiry or reactivation. Implemented sequentially; exact expired-credential/family source combinations, timestamp/evidence integrity, per-family audit, retry, provider rollback, and concurrency are partial under `AUTH-LC-CHOICE-009/010`; owner #338/#1059/#724. |
| `AUTH-LC-REF-004` | Refresh family — local-password **Sign in** creates continuity authority | Successful local proof creates one active family with absolute expiry in the same `CreateRefreshSessionAsync` save as its initial access session, refresh credential, and creation audit. | The family bounds rotation lineage and later family-scoped revoke/replay/expiry. It grants no authorization role itself. A final `sign_in.succeeded` audit failure can leave it active although no raw material reached the caller. | Creation is not reactivation. Retry creates an independent family; final-audit handoff, old-proof issuance ordering, MFA prerequisite, and retention are partial under `AUTH-LC-CHOICE-002/010/012`. Owner #338/#339. |
| `AUTH-LC-REF-005` | Refresh credential — local-password **Sign in** issues initial refresh material | Successful local proof creates one active hashed refresh-credential root linked to the new family/session. The raw value is generated and held transiently in process before persistence, returned between services after the creation save, and crosses the external response boundary only after the later sign-in audit succeeds. | On returned success it can authorize rotation until idle/absolute expiry, consumption, family revocation, or replay response. If final sign-in audit persistence fails, the hash remains active while the caller receives no raw value; the async state-machine/task lifetime of the immutable in-process strings and their eventual disposal/zeroization are unproved and must remain unresolved. | Issuance is not rotation or restore. Retry creates independent material; final-audit handoff/failed-path material lifetime, old-proof ordering, MFA prerequisite, and no-redisplay policy are partial under `AUTH-LC-CHOICE-002/010/012`. Owner #338/#339. |
| `AUTH-LC-REF-006` | Refresh family after account becomes unavailable — generic **Sign in again** | When rotation resolves an otherwise active credential whose account is disabled or deleted, `RevokeLinkedFamilyAndReturnFailureAsync` marks that one family `active → revoked`; active credential and linked-session roots are `AUTH-LC-REF-020` and `AUTH-LC-SES-015`. | That family's continuity stops; other families are untouched. Re-enabling an account does not reactivate this family, but untouched families can become usable again unless a separate account lifecycle policy revokes them. | Irreversible for the transitioned family. Implemented, but provider atomicity, account-wide coordination, and family-identifying audit evidence remain partial under `AUTH-LC-CHOICE-001/009/010`. Owner #338/#788/#724. |
| `AUTH-LC-REF-007` | Current refresh family — **Sign out** | The current account's selected session authorizes its observed active family `active → revoked` inside `RevokeSessionAsync`; this is the family root of the `AUTH-LC-SES-001/009` cascade. | Observed family rotation continuity stops, and linked access/refresh rows are separately transitioned; overlapping rotation can insert authority after selection, while family-blind access validation can still accept that replacement. Independent families remain usable. | Irreversible for the transitioned family; later sign-in creates a new family. Relational save has provider transaction semantics; non-relational uses one save without rollback proof, and already-revoked retry can skip repair after partial failure. Selection/race and family-subject audit remain partial under `AUTH-LC-CHOICE-010`. Owner #338. |
| `AUTH-LC-REF-008` | Current-family refresh credentials — **Sign out** | The current account's selected session authorizes each observed active credential in its family `active → revoked` inside the same cascade. | Observed refresh material stops authorizing rotation; no raw value is retained or audited. A replacement credential inserted by overlapping rotation can escape the selected set. | Irreversible for transitioned credentials. Relational save has provider transaction semantics; non-relational uses one save without rollback proof, and terminal-session retry can skip partial-failure repair. Race/retry behavior is partial under `AUTH-LC-CHOICE-010`; owner #338. |
| `AUTH-LC-REF-009` | Selected owned session's refresh family — **Revoke session** | Current account ownership of the selected access session authorizes its observed active family `active → revoked`; this is a distinct family root in the `AUTH-LC-SES-002/010` cascade. | Observed family continuity stops; unrelated families remain. Overlapping rotation and family-blind access validation can leave replacement access authority usable. | Irreversible for the transitioned family. Relational versus non-relational rollback, already-terminal retry repair, target disclosure, selection/concurrency, and family-subject audit are partial under `AUTH-LC-CHOICE-010`; owner #338/#774. |
| `AUTH-LC-REF-010` | Selected owned session's family refresh credentials — **Revoke session** | Current account ownership of the selected access session authorizes observed active credentials in that linked family `active → revoked`. | Observed refresh material stops rotating; credentials in unrelated families remain, and an overlapping replacement can escape the selected set. | Irreversible for transitioned credentials. Relational versus non-relational rollback, terminal-target retry repair, and rotation concurrency are partial under `AUTH-LC-CHOICE-010`; owner #338/#774. |
| `AUTH-LC-REF-011` | Account refresh families — **Sign out all sessions** | Current account requests revocation of families discovered through refresh credentials linked to selected unexpired active access sessions; each observed active family changes `→ revoked`. | Discovered continuity stops. A family linked only through an already-expired access session is not discovered, and overlapping rotation can escape selection; unrelated-account authority is unchanged. | Irreversible for transitioned families. Relational save has provider transaction semantics; non-relational uses one save without rollback proof. Discovery/concurrency and per-family audit correlation are partial under `AUTH-LC-CHOICE-010/013`; owner #338. |
| `AUTH-LC-REF-012` | Account refresh credentials — **Sign out all sessions** | Current account requests observed active credentials in the discovered families `active → revoked` as a separate root of the `AUTH-LC-SES-003` cascade. | Observed refresh material stops rotating. Undiscovered lineage and concurrently inserted replacement credentials can remain usable. | Irreversible for transitioned credentials. Relational versus non-relational rollback and discovery/concurrency completeness are partial under `AUTH-LC-CHOICE-010/013`; owner #338. |
| `AUTH-LC-REF-013` | Other refresh families after **Change password** | Authenticated current-password proof invokes account-session revocation while excluding the current access session; observed families discovered through selected unexpired sessions change `active → revoked`, except a family is removed from the candidate set when it is also linked to the excluded current session. | Discovered non-excluded continuity stops. A shared family linked to the current session, lineage linked only through expired sessions, and overlapping replacement authority can remain usable; the password verifier transition is separate. | Irreversible for transitioned families. Relational coupling to password replacement exists only when the outer workflow opens a transaction; non-relational rollback, shared-family/discovery completeness, concurrency, and family-subject audit are partial under `AUTH-LC-CHOICE-002/010/013`. Owner #339/#338/#774. |
| `AUTH-LC-REF-014` | Other refresh credentials after **Change password** | The same authenticated workflow changes observed active credentials in the selected non-excluded families `active → revoked`; current-session exclusion can remove a shared family and all its credentials from selection. | Observed refresh material stops rotating, but shared-family, expired-session-only, or concurrently inserted credentials can remain usable. No old/new password or raw refresh value becomes lifecycle evidence. | Irreversible for transitioned credentials. Provider rollback, discovery/exclusion, and rotation ordering are partial under `AUTH-LC-CHOICE-002/010/013`; owner #339/#338/#774. |
| `AUTH-LC-REF-015` | Refresh families after **Reset password** | Valid anonymous purpose-bound reset material and successful password replacement invoke account-session revocation without a current-session exclusion; families discovered through selected unexpired active sessions change `active → revoked`. | Discovered continuity stops. Lineage linked only through already-expired sessions and overlapping replacement authority can remain usable; reset-request consumption and password replacement are separate roots. | Irreversible for transitioned families. Relational outer-transaction coupling exists, while non-relational failure can follow committed credential work; discovery/concurrency and family-subject audit are partial under `AUTH-LC-CHOICE-002/007/010/013`. Owner #339/#338/#773/#724. |
| `AUTH-LC-REF-016` | Refresh credentials after **Reset password** | The anonymous reset workflow changes observed active credentials in discovered families `active → revoked` without a session exclusion. | Observed refresh material stops rotating, but expired-session-only lineage or concurrently inserted credentials can remain usable. No reset/password/raw refresh material is retained in audit. | Irreversible for transitioned credentials. Reset consume/replacement/cascade rollback, discovery, and rotation ordering are partial under `AUTH-LC-CHOICE-002/007/010/013`; owner #339/#338/#773. |
| `AUTH-LC-REF-017` | Replacement refresh credential — refresh protocol reports **Session refreshed** | After valid proof, the service creates a new `active` hashed credential linked to the same family and the separately inventoried replacement access session. Raw refresh material is generated before the relational transaction and crosses the response boundary only after commit. | On returned success the new material can rotate until idle/absolute expiry or a terminal transition. On failure it is not returned, but in-process async reference lifetime and disposal/zeroization are unproved. | Issuance is not predecessor rotation or reactivation. Transaction/race and failed-path material lifetime remain partial under `AUTH-LC-CHOICE-010/012`; owner #338/#1059. |
| `AUTH-LC-REF-018` | Active refresh credentials after detected **Reuse** | A committed linkable replay cascade changes the family's observed active credentials `→ replayed`; the matched predecessor may already be rotated/revoked and is not rewritten by this active-row loop. | Observed active refresh material stops rotating, while overlapping insertion can escape selection. No raw value or exposed hash becomes evidence. | Irreversible for transitioned credentials. The relational two-rotation loser can fail before this root; selection/concurrency remains partial under `AUTH-LC-CHOICE-010`. Owner #338. |
| `AUTH-LC-REF-019` | Active refresh credentials after refresh **Expiry** | Matched credential or family expiry changes observed active credentials in that family `→ expired`, including the matched credential when it is active. | Observed refresh material stops rotating; unrelated families remain. | Irreversible for transitioned credentials. Implemented on the matched path; clock, selection, and retention remain under `AUTH-LC-CHOICE-010`. Owner #338/#1059. |
| `AUTH-LC-REF-020` | Active refresh credentials after account becomes unavailable — generic **Sign in again** | The one-family unavailable-account cascade changes observed active credentials `→ revoked`. | Observed refresh material in that family stops rotating; other families are untouched and no raw value/hash is retained as evidence. | Irreversible for transitioned credentials. Provider failure atomicity, account-wide completeness, and concurrency remain partial under `AUTH-LC-CHOICE-001/009/010`; owner #338/#788/#724. |
| `AUTH-LC-PWD-001` | Local password credential — **Change password** | Current account proves current password; API replaces verifier fields in the same active credential row. This is replacement, not credential revocation. Two concurrent requests can both verify the same predecessor and report success while the last committed verifier determines which new password works. | New password from the last writer works; the other concurrently acknowledged password may fail. Current flow preserves the current session and invokes account-session revocation excluding it. Only selected unexpired sessions and families discovered through them are candidates; moreover, if the excluded current session shares a discovered family with another selected session, that whole family is removed from the revocation candidates and its active refresh credential can remain usable. The limitations in `AUTH-LC-CHOICE-002/013` apply. Roles/identity link remain. | Not reversible; user may change again. Implemented sequentially, with replacement concurrency, provider-specific rollback, and cascade/client surface partial: on the non-relational branch there is no outer transaction, so a failure while revoking sessions can return failure after the credential workflow has already committed the new verifier, leaving other sessions active. Evidence: `AuthCredentialWorkflowService`, `CurrentAccountPasswordChangeService`, `CurrentAccountPasswordChangeEndpointTests`. Owner #339/#338/#774. |
| `AUTH-LC-PWD-002` | Local password credential — **Reset password** | Valid purpose-bound reset proof authorizes replacement of the active password verifier in its existing credential row. Reset-request consumption is separately inventoried at `AUTH-LC-RST-002`; outstanding-request and session/refresh cascades are separate roots. Concurrent completions can both replace the same credential and report success while the last committed verifier wins. | After an uncontested commit, the new password works and old password fails. Under the race, an earlier acknowledged new password can fail. Identity and role assignments remain; session effects are defined only by the separate `SES`/`REF` rows. | Not reversible; a later reset creates new material. Implemented sequentially with replacement concurrency and provider/workflow coupling partial under `AUTH-LC-CHOICE-002/007/012/013`; no password, verifier, or reset material is lifecycle evidence. Owner #339/#338/#773. |
| `AUTH-LC-PWD-003` | Local password credential — **Disable credential** | Proposed reversible administrative `active → disabled`; schema supports the target, but no current endpoint/service performs it. | Verification refuses disabled credentials. Current reset issuance, reset account resolution, and `ResetLocalPasswordAsync` also require an active credential, so password reset cannot recover a disabled password. Existing-session and alternate-method effects remain unresolved. | Conditionally re-enableable only through a separately authorized recovery/re-enable path; current reset is not that path. Unimplemented and blocked by `AUTH-LC-CHOICE-002`/last-method lockout proof. Owner #788/#339. |
| `AUTH-LC-PWD-011` | Disabled local password credential — **Re-enable credential** | Proposed separately authorized recovery/administrative `disabled → active`; current reset cannot perform it and no endpoint/service exists. The retained verifier must not become eligible until actor, recovery proof, account state, compromise disposition, and last-method policy pass. | May restore future password verification only; it must not revive revoked/expired sessions, refresh families, reset requests, challenges, or other credentials, and exact existing-session treatment remains unresolved. | Conditional re-entry, not restore. Retry/concurrency, verifier replacement versus reuse, session cascade, and audit are blocked by `AUTH-LC-CHOICE-002`; owner #788/#339. |
| `AUTH-LC-PWD-004` | Local password credential — **Revoke credential** | Proposed terminal `active/disabled → revoked`; schema supports the target, but no current endpoint/service performs it. | Verifier stops future password sign-in; exact existing-session and fallback effects are unresolved. | Irreversible for that verifier; replacement requires a distinct proofed workflow. Blocked by `AUTH-LC-CHOICE-002`. Owner #788/#339. |
| `AUTH-LC-PWD-005` | Local password credential — bootstrap **Create password** | Bootstrap invokes credential creation after saving the other account roots; successful creation persists only a one-way verifier and safe parameters. | Password sign-in becomes available only if this credential save succeeds; no session is issued. | Relational source rolls the bundle back on credential failure. A non-relational provider has already committed the other roots and has no compensation, so failure can leave bootstrap unavailable with no password credential. `AUTH-LC-CHOICE-001/002`; owner #339/#785. |
| `AUTH-LC-PWD-006` | Local password credential — invitation **Create password** | A valid invitation accept supplies plaintext to the credential workflow, which persists only a one-way verifier and safe parameters for the newly created invited account. | Password sign-in becomes available; invitation acceptance does not issue a session. | Credential/account/identity creation is intended to roll back together on failure; relational acceptance behavior lacks focused tests. Implemented sequentially under `AUTH-LC-CHOICE-008`. Owner #339/#784. |
| `AUTH-LC-PWD-007` | Local password credential — admin **Create local user password** | An authenticated owner/admin passes plaintext to the credential workflow during guarded local-user creation; only a one-way verifier and safe parameters are persisted. | Password sign-in becomes available for the created account; provisioning issues no session. | Creation is intended to roll back with the account bundle. The relational transaction is source-defined; non-relational compensation physically removes only rows created by the failed workflow. Implemented and endpoint-tested; relational rollback/race proof remains partial under `AUTH-LC-CHOICE-001/002`. Owner #339/#785. |
| `AUTH-LC-PWD-008` | Active local password credential — not separately user-visible: **Upgrade password protection** | During successful password verification, when current hashing policy requires rehash, `VerifyLocalPasswordAsync` replaces the verifier plus algorithm/version/parameters and clears `RequiresRehash`; the submitted password and predecessor verifier are not retained as history. | The same password remains usable under upgraded protection; no session, role, identity, or account state changes solely because of rehash. | Replacement is not rollback or credential revocation. Implemented sequentially; concurrent verify/change/reset/rehash last-writer behavior and missing dedicated lifecycle audit are partial under `AUTH-LC-CHOICE-002`. Owner #339. |
| `AUTH-LC-PWD-009` | Active local password credential — not separately user-visible: **Verify password** | Successful `VerifyLocalPasswordAsync` updates `LastVerifiedAtUtc`/`UpdatedAtUtc` and emits credential-verification audit even when rehash is unnecessary; the submitted password is not retained. | Verification is proof for the exact calling workflow only; it creates no session itself and does not change roles, identity, or account status. A later sign-in issuance is a separate transition. | Repeatable proof, not replacement/reactivation. Implemented sequentially; verify-versus-change/reset/disable/rehash ordering and out-of-order last-verified metadata are partial under `AUTH-LC-CHOICE-002`. Owner #339. |
| `AUTH-LC-PWD-010` | Newly created password credential — failed invitation acceptance reports generic failure | `InvitationAcceptanceService` non-relational compensation physically removes password-credential rows selected by the positively identified invited account after a later acceptance-bundle save fails; invitation restoration is separate. | The failed invitation workflow leaves no usable local-password verifier if compensation completes; unrelated credentials must not be selected. | Irreversible temporary failed-workflow cleanup only, not credential revoke or general purge authority. Implemented; invitation-specific ordering, retry, compensation failure, and provider equivalence remain partial under `AUTH-LC-CHOICE-002/008`. Owners #784/#339. |
| `AUTH-LC-PWD-012` | Newly created password credential — failed admin provisioning reports failure | `AdminLocalUserService` non-relational compensation physically removes password-credential rows selected by the positively identified admin-created account after a later provisioning-bundle save fails. | The failed authenticated admin workflow leaves no usable local-password verifier if compensation completes; unrelated credentials must not be selected. | Irreversible temporary failed-workflow cleanup only. Implemented; admin actor context, ordering, retry, compensation failure, and provider equivalence remain partial under `AUTH-LC-CHOICE-001/002`. Owners #785/#339. |
| `AUTH-LC-ACC-001` | Auth account — **Disable account** | Proposed administrative `active → disabled`; schema supports status/`DisabledAtUtc`, but no mutation runtime exists. | Session validation, primary sign-in/refresh, reset completion, and mapped passkey paths reject unavailable accounts, but anonymous TOTP/recovery challenge verification does not reload account availability after challenge issuance. Whether disable must transactionally revoke sessions/material and invalidate outstanding challenges is unresolved; roles remain assignments but are ineligible while account is disabled. | Conditionally reversible only through explicit re-enable. Requires last-owner and alternate-auth recovery proof. Blocked by `AUTH-LC-CHOICE-001`; owner #788/#785. |
| `AUTH-LC-ACC-002` | Auth account — **Re-enable account** | Proposed authorized `disabled → active` after current policy and recovery checks; no runtime exists. It must not clear audit or silently restore credentials/sessions/factors. | Account eligibility may return, but each identity, credential, factor, role and session must be independently revalidated; old revoked/expired authority stays unusable. | Conditional, not restore. Blocked by `AUTH-LC-CHOICE-001`; owner #788/#785. |
| `AUTH-LC-ACC-003` | Auth account — **Delete account** | Proposed account-level deletion distinct from disablement, identity unlinking, and profile deletion. Persistence has `DeletedAtUtc` and current auth consumers treat that marker as unavailable, but no authorized mutation/cascade exists. | Future sign-in/refresh/account use must stop. Exact effects on sessions, credentials/factors, identities, roles, invitations/reset material, profile/domain data, evidence, and copies are unresolved; no physical deletion is authorized. | Re-entry/restore is undefined and must not be promised. Blocked by `AUTH-LC-CHOICE-001`; manual auth, owner-lockout, privacy, schema, and destructive gates. Owner #788/#785/#724. |
| `AUTH-LC-ACC-004` | Auth account — setup-only **Create first owner** | Bootstrap saves the first active account with profile, identity, roles, and audit before password creation; only relational source encloses both saves in a serializable transaction/table lock. | Success establishes first-owner eligibility without a session. Non-relational credential failure can leave the active account without a password and make later bootstrap unavailable. | Creation is not reactivation. Relational rollback is intended but race coverage is incomplete; non-relational partial commit has no compensation. `AUTH-LC-CHOICE-001`; owner #785/#339. |
| `AUTH-LC-ACC-005` | Auth account — invitation **Create invited user** | A valid pending invitation authorizes creation of an active account plus linked profile, local identity, local password credential, and bounded `user` role within the acceptance transaction. | Establishes later local-password eligibility but issues no session and grants no owner/admin role. | Creation is not reactivation. Source has a serializable relational branch/compensation, but focused tests are in-memory; concurrency/rollback evidence is partial under `AUTH-LC-CHOICE-008`. Owner #784/#339. |
| `AUTH-LC-ACC-006` | Auth account — admin **Create local user** | An authenticated owner/admin creates one active account plus profile, local identity, password credential, bounded `user` role, and creation audit. Relational source uses a serializable transaction; the in-memory/non-relational fallback compensates a failed bundle by deleting the positively identified rows it just created. | Establishes future local-password and `user`-role eligibility but issues no session and grants no owner/admin role. | Creation is not reactivation. Compensation is temporary failed-workflow cleanup, not general account deletion authority. Endpoint tests do not prove the relational race/rollback branch; `AUTH-LC-CHOICE-001` remains open. Owner #785/#339. |
| `AUTH-LC-ACC-007` | Newly created auth account — failed invitation acceptance reports generic failure | `InvitationAcceptanceService` non-relational compensation physically removes the exact invited account after a later acceptance-bundle save fails and separately attempts to restore the invitation. | Account eligibility ends only for that failed invitation workflow if compensation completes; this is not disablement or a general deletion cascade. | Irreversible temporary failed-workflow cleanup only. Invitation restoration can still fail independently; ordering, retry, compensation failure, and relational equivalence remain partial under `AUTH-LC-CHOICE-008`. Owner #784. |
| `AUTH-LC-ACC-008` | Newly created auth account — failed admin provisioning reports failure | `AdminLocalUserService` non-relational compensation physically removes the exact admin-created account after a later provisioning-bundle save fails; no invitation restoration exists in this authority. | Account eligibility ends only for that failed authenticated admin workflow if compensation completes; this is not disablement or general account deletion. | Irreversible temporary failed-workflow cleanup only. Admin actor context, ordering, retry, compensation failure, and relational equivalence remain partial under `AUTH-LC-CHOICE-001`. Owner #785. |
| `AUTH-LC-ID-001` | Provider identity link — **Unlink identity** | Proposed relationship disable/unlink; schema has `DisabledAtUtc` but no unlink runtime. Local and OIDC links must not be conflated. | Stops future resolution through that identity only. It must not disable/delete account/profile, erase historical subject linkage, remove roles, or silently revoke other sign-in methods. Session effect is unresolved. | Re-link is a distinct proofed transition, not restore. Must preserve at least one safe owner/admin sign-in path. Blocked by `AUTH-LC-CHOICE-003`; owner #787/#773/#785. |
| `AUTH-LC-ID-002` | Local identity link — bootstrap **Create first-owner identity** | Bootstrap saves the local link with other pre-credential roots; only relational source encloses the later password step in that transaction. | Success enables local identity resolution. Non-relational credential failure can leave the link bound to a stranded active account with no password. | Relational rollback is intended; non-relational partial commit has no compensation. Collision/concurrency evidence remains partial under `AUTH-LC-CHOICE-001/003`; owner #785/#787. |
| `AUTH-LC-ID-003` | Local identity link — invitation **Create invited identity** | Valid invitation acceptance creates a local identity bound to the new account using the invitation's normalized contact identifier. | Enables later local identity resolution; it neither signs in the caller nor links an OIDC provider. | Intended rollback is coupled to acceptance; focused relational collision/concurrency evidence is absent. `AUTH-LC-CHOICE-003/008`; owner #784/#787. |
| `AUTH-LC-ID-004` | Local identity link — admin **Create local-user identity** | Guarded owner/admin provisioning creates a local identity bound to the new account after a preflight and in-transaction normalized-identifier check. | Enables later local identity resolution; it is not a session or provider credential. | Relational creation/rollback is source-defined, while endpoint tests do not prove the serializable collision branch; non-relational failed-workflow compensation removes only the just-created link. `AUTH-LC-CHOICE-001/003`; owner #785/#787. |
| `AUTH-LC-ID-005` | Provider identity link — **Link external identity** | Proposed default-disabled OIDC/provider-proof transition creates a link from stable issuer/provider subject to an existing or newly authorized account; no current OIDC runtime implements it. | May enable future provider identity resolution only after current account, collision, takeover, and remaining-method checks; it must not silently merge profiles, grant roles, or create a session outside the approved sign-in ceremony. | Link is a new relationship, not reactivation; unlink/relink are distinct. Unimplemented and blocked by `AUTH-LC-CHOICE-003`; owner #787/#773/#785. |
| `AUTH-LC-ID-006` | Newly created local identity link — failed invitation acceptance reports generic failure | `InvitationAcceptanceService` non-relational compensation removes local identity rows selected by the invited account after a later acceptance-bundle save fails. | The failed invitation workflow's local identifier no longer resolves if compensation completes; unrelated links remain. This is not unlinking, and invitation restoration is separate. | Irreversible temporary failed-workflow cleanup only. Invitation ordering, retry, selection proof, and compensation failure remain partial under `AUTH-LC-CHOICE-003/008`. Owners #784/#787. |
| `AUTH-LC-ID-009` | Newly created local identity link — failed admin provisioning reports failure | `AdminLocalUserService` non-relational compensation removes local identity rows selected by the admin-created account after a later provisioning-bundle save fails. | The failed authenticated admin workflow's local identifier no longer resolves if compensation completes; unrelated links remain. This is not unlinking. | Irreversible temporary failed-workflow cleanup only. Admin actor context, ordering, retry, selection proof, and compensation failure remain partial under `AUTH-LC-CHOICE-001/003`. Owners #785/#787. |
| `AUTH-LC-ID-007` | Disabled local identity link — **Re-link local identity** | Proposed authorized `DisabledAtUtc set → cleared` or new-link replacement after account ownership/recovery proof and normalized-identifier collision checks; no runtime exists. | May restore local identity resolution only after the linked account and credential remain eligible. It must not restore sessions, roles, factors, or provider links, and cannot bypass last-method/takeover checks. | Conditional re-entry, not restore. Exact self/admin authority, reuse-versus-new-row rule, collision/concurrency, session impact, and audit are unresolved under `AUTH-LC-CHOICE-003`; owner #787/#773/#785. |
| `AUTH-LC-ID-008` | Disabled provider identity link — **Re-link external identity** | Proposed provider-proof transition after stable issuer/subject proof, account binding, collision/takeover, and current policy checks; it may clear the tombstone or create a new relationship only as separately decided. No OIDC runtime exists. | May restore provider identity resolution only; it must not merge accounts/profiles, grant roles, revive sessions, or reactivate other credentials. | Conditional re-entry, not restore. Provider-specific proof, self/admin authority, retained tombstone matching, concurrency, session effect, and audit are unresolved under `AUTH-LC-CHOICE-003`; owner #787/#773/#785. |
| `AUTH-LC-ROL-001` | Product role assignment — bootstrap **Assign first-owner roles** | Bootstrap saves exact `owner`, `admin`, and `user` assignments with the pre-credential roots; only relational source makes the later password step atomic with them. | Success establishes first-owner authorization after revalidation. Non-relational credential failure can leave assignments on a stranded account that cannot sign in. | Relational rollback is intended; non-relational partial commit has no compensation. Later role mutation is distinct; `AUTH-LC-CHOICE-001/004`. Owners #785/#464/#465. |
| `AUTH-LC-ROL-002` | Product role assignment — **Change [old role] to [new role]** | Proposed atomic remove-and-assign or replacement semantics; current source defines neither the transition nor history shape. | Once committed, the next authenticated request reloads assignments and builds fresh role claims, so it observes the new role without session/token refresh. An already-authorized in-flight request is not recalled; mutation ordering and last-owner effects remain unresolved. | Not a silent restore; blocked by `AUTH-LC-CHOICE-004`. Owner #785/#464/#465. |
| `AUTH-LC-ROL-003` | Product role assignment — **Remove [role]** | Proposed authorized removal/logical terminal transition; current row has no removal metadata and no mutation API. | Once committed, the next authenticated request reloads assignments and excludes the removed role without session/token refresh; credentials remain valid. An already-authorized in-flight request is not recalled, and mutation ordering remains unresolved. | New assignment later is a new event. Last-owner/self-lockout and in-flight behavior block implementation under `AUTH-LC-CHOICE-004`. |
| `AUTH-LC-ROL-004` | Product role assignment — invitation **Assign user role** | Successful invitation acceptance creates exactly the bounded `user` assignment for the new account under valid invitation-material authority. | Establishes later `user` authorization; it grants no owner/admin role and issues no session. | Creation is coupled to invitation acceptance and intended rollback; later removal is distinct. Implemented sequentially, with relational rollback/collision partial under `AUTH-LC-CHOICE-004/008`; owner #784/#785. |
| `AUTH-LC-ROL-005` | Product role assignment — admin **Assign created-user role** | Guarded owner/admin local-user provisioning creates exactly the bounded `user` assignment and records the provisioning actor. | Establishes later `user` authorization; it grants no owner/admin role and issues no session. | Creation rolls back transactionally or through failed-bundle compensation; later removal is distinct. Implemented and endpoint-tested, with relational rollback/race proof partial under `AUTH-LC-CHOICE-001/004`; owner #785/#464/#465. |
| `AUTH-LC-ROL-006` | Newly created product-role assignment — failed invitation acceptance reports generic failure | `InvitationAcceptanceService` non-relational compensation removes assignments selected by the invited account as subject or assigner after a later acceptance-bundle save fails. | The failed invitation workflow grants no product role if compensation completes; unrelated assignments remain. This is not authorized role removal, and invitation restoration is separate. | Irreversible temporary cleanup only. Selection breadth, invitation ordering, retry, concurrency, and compensation failure remain partial under `AUTH-LC-CHOICE-004/008`. Owners #784/#464/#465. |
| `AUTH-LC-ROL-008` | Newly created product-role assignment — failed admin provisioning reports failure | `AdminLocalUserService` non-relational compensation removes assignments selected by the admin-created account as subject or assigner after a later provisioning-bundle save fails. | The failed authenticated admin workflow grants no product role if compensation completes; unrelated assignments remain. This is not authorized role removal. | Irreversible temporary cleanup only. Admin actor context, selection breadth, ordering, retry, concurrency, and compensation failure remain partial under `AUTH-LC-CHOICE-001/004`. Owners #785/#464/#465. |
| `AUTH-LC-ROL-007` | Existing account — **Assign additional role** | Proposed authorized creation of one new assignment while retaining the account's other assignments; no general role-mutation endpoint/service exists. Duplicate insertion, concurrent assign/remove, and client-supplied authority must be rejected or safely serialized. | Once committed, the next authenticated request reloads roles and can gain the added authorization without session/token refresh; already-running requests are unchanged. Adding `owner`/`admin` requires distinct elevation and last-owner/self-action policy. | New relationship, not replacement or restore. Authority, idempotency, uniqueness/concurrency, elevation rules, audit, and client surface are blocked by `AUTH-LC-CHOICE-004`; owner #785/#464/#465. |
| `AUTH-LC-PKY-001` | Passkey credential — **Revoke passkey** (or **Remove passkey** with explanatory copy) | Current owner passes the active policy/actor-dependent step-up check and, when that check requires it, supplies fresh proof; service changes retained credential `enrolled → revoked` with actor/reason/correlation metadata. | Credential stops future enrollment lookup/sign-in/step-up use. Current code does not revoke existing sessions or other factors/roles. | Irreversible for that credential; enroll a new passkey. Implemented and idempotent for already revoked. Anti-lockout/session review remains `AUTH-LC-CHOICE-005`. Owner #394/#776; session handoff recommendation remains separate. |
| `AUTH-LC-PKY-002` | Passkey credential — **Disable passkey** | Proposed `enrolled → disabled`; schema supports the status, but current self-service runtime exposes revoke only. | Disabled credential must be excluded from authentication; session and remaining-factor effects are unresolved. | Conditional re-entry only by a separately approved transition. Unimplemented; `AUTH-LC-CHOICE-005`. Owner #394/#776. |
| `AUTH-LC-PKY-003` | Passkey credential — **Re-enable passkey** | Proposed `disabled → enrolled` after current account, policy, ownership, compromise, and recovery checks; no runtime exists. | May restore future credential use but never revives expired/revoked sessions or challenges. | Conditional, not restore of earlier authority. Unimplemented; `AUTH-LC-CHOICE-005`. Owner #394/#776. |
| `AUTH-LC-PKY-004` | Owned non-revoked passkey record — generic **Revoke passkey** endpoint | Current revoke accepts `pending`, `enrolled`, or `disabled` and changes it `→ revoked` after the policy/actor-dependent step-up check. | Target record stops future use; existing sessions, other credentials/factors, and roles remain. | Irreversible for that row. Implemented broad source-state behavior; whether the endpoint should be narrowed and its concurrent outcome are unresolved in `AUTH-LC-CHOICE-005`. Owner #394/#776. |
| `AUTH-LC-PKY-005` | Passkey credential — **Finish passkey setup** | Authenticated completion verifies the ceremony, creates an `enrolled` credential, consumes the challenge, writes audit, and commits before response construction awaits policy readout; policy is not revalidated. | Credential authority survives a later readout fault/cancellation even though the caller receives no success. Retry conflicts because the challenge is consumed; existing sessions/roles remain. | Enrollment is irreversible. Post-save handoff/reconciliation, deterministic retry, completion policy, concurrent completion, and uniqueness remain partial under `AUTH-LC-CHOICE-005/006/012`; owners #394/#776. |
| `AUTH-LC-PKY-006` | Owned passkey record — **Rename passkey** | Current owner passes the policy/actor-dependent step-up check and may replace `DisplayLabel`/`UpdatedAtUtc` on any owned record; loading does not restrict `pending`, `enrolled`, `disabled`, or `revoked` status. Audit records `passkey.renamed` with bounded metadata. | Label changes do not change authentication usability, credential status, sessions, factors, account, or roles. A terminal record remains terminal but its retained evidence metadata is mutable. | Repeat is last-writer metadata replacement, not reactivation. Implemented sequentially; allowed terminal source states, concurrent rename/revoke ordering, and evidence immutability expectations are unresolved under `AUTH-LC-CHOICE-005`. Owner #394/#776. |
| `AUTH-LC-PKY-007` | Enrolled passkey credential — anonymous **Use passkey to sign in** | Successful assertion independently resolves the asserted credential/account, updates signature counter, backup state, `LastUsedAtUtc`/`UpdatedAtUtc`, consumes the sign-in challenge, and creates the access-only session in `AUTH-LC-SES-006`. The nested session creation saves all tracked changes before the later challenge-success audit is added. | The credential remains enrolled and records use, but the created session is unusable to the caller because no bearer is handed off. An identifier-bound challenge does not enforce that account at completion: a credential for another account can consume it. If the later audit save fails, the caller receives persistence failure although credential use, challenge consumption, and the inaccessible active session already committed. | Repeatable credential use, not reactivation. Implemented with provider-independent partial commit; challenge/account binding, unusable handoff, audit-failure rollback, and concurrent out-of-order counter commits are security-partial under `AUTH-LC-CHOICE-005/006`; require cross-account, failure-injection, rollback, and monotonic-counter tests. Owner #394/#338. |
| `AUTH-LC-PKY-008` | Enrolled passkey credential — authenticated **Use passkey for step-up** | Successful assertion independently updates signature counter, backup state, `LastUsedAtUtc`/`UpdatedAtUtc`, consumes the account/session/operation-bound challenge, and satisfies that scoped step-up outcome. The save commits before response construction awaits the database-backed policy readout. | It grants only the bounded operation proof; it creates no session or refresh authority and does not alter roles. A later readout fault/cancellation returns no success although the credential/challenge/audit commit remains authoritative; retry conflicts on the consumed challenge. The response freshness deadline uses the configurable passkey runtime window (300 seconds by default), while authorization uses a hard-coded 900 seconds but considers only the ten newest consumed step-up challenges before operation matching, so newer proofs for other operations can evict this one even inside the response window. | Repeatable use through a new challenge, not reactivation. Implemented sequentially, but post-save handoff/reconciliation, time-and-operation freshness parity/eviction, verify-versus-revoke/rename ordering, completion-time policy, and concurrent out-of-order counter commits are partial under `AUTH-LC-CHOICE-005/006/012`. Owner #394/#776/#724. |
| `AUTH-LC-TOTP-001` | Pending TOTP factor — **Cancel authenticator setup** | Current owner cancels `pending → revoked`. | Pending factor never authenticates; existing enrolled factors/sessions remain. | Terminal for that setup. Implemented; terminal secret disposition remains `AUTH-LC-CHOICE-005`. Owner #394/#776. |
| `AUTH-LC-TOTP-002` | Earlier pending TOTP factors — start replacement setup | Starting a new enrollment sequentially marks observed earlier pending rows `→ revoked`; creation of the new pending factor is the separate `AUTH-LC-TOTP-012` root. The only success audit names the new factor and does not identify or count these predecessors, so their per-root replacement evidence is absent. | Observed earlier setup material stops working; enrolled factors/sessions remain. Concurrent starts can each miss the other's new row. | Irreversible for observed prior pending factors; audit correlation, concurrency, provider-specific rollback, and terminal secret disposition are partial under `AUTH-LC-CHOICE-005/009`. Owner #394/#776/#724. |
| `AUTH-LC-TOTP-011` | TOTP factor — **Start authenticator setup** with no pending predecessor | Authenticated owner under current factor policy creates one pending factor with protected secret and source-defined setup expiry, then returns provisioning URI/manual-entry material only at the immediate enrollment boundary. No predecessor is revoked. Raw secret bytes and the immutable manual-entry key are created before persistence; a failed save returns no setup response but their async reference lifetime and disposal/zeroization are unproved. After a successful save, response construction awaits policy readout while still holding that material; fault/cancellation can strand the committed setup without handing material to the caller. | Pending setup cannot authenticate until verified; existing sessions/factors/roles remain. Concurrent first starts can each create pending factors. A concurrent account disable/delete can make the pre-save account load return `Denied` without a bounded audit event even though the account ID is known. | Issuance is not replacement or reactivation. Implemented sequentially, but retry/concurrent uniqueness, failed-save and post-save-handoff material lifetime, stranded-setup retry/disposition, unavailable-account denial audit, setup-material delivery, no-redisplay, cancellation/expiry disposition, and policy change remain partial under `AUTH-LC-CHOICE-005/009/012`. Owner #394/#776/#724. |
| `AUTH-LC-TOTP-012` | Replacement TOTP factor — **Start new authenticator setup** | After observed prior pending factors are staged as revoked, the service creates a distinct pending factor with protected secret and source-defined setup expiry, then returns provisioning URI/manual-entry material only at the immediate enrollment boundary. Raw secret bytes/manual-entry key exist before the save; failed-save lifetime/disposal are unproved. After commit, response construction awaits policy readout while retaining the material; fault/cancellation can leave the replacement pending and predecessors revoked without handing setup material to the caller. | The new setup cannot authenticate until verified; enrolled factors/sessions/roles remain. Concurrent starts can create multiple pending factors. A non-relational provider has only one save and no proof that partial predecessor/new-factor/audit persistence rolls back. A concurrent account disable/delete can also produce an unaudited pre-save `Denied` result for the known account. | Issuance is not revocation or reactivation. Relational ordinary save atomicity differs from unproved non-relational failure behavior; retry/concurrent uniqueness, failed-save and post-save-handoff material lifetime, stranded replacement recovery, unavailable-account denial audit, setup delivery/no-redisplay, policy change, and terminal secret disposition remain partial under `AUTH-LC-CHOICE-005/009/012`. Owner #394/#776/#724. |
| `AUTH-LC-TOTP-003` | Pending TOTP factor — **Setup expired** | Verification observes the deadline and attempts `pending → expired` through `TrySaveAsync`, but ignores its false result and still returns conflict. | If save commits, setup is excluded; if it fails, persisted factor can remain pending although the caller receives the same conflict. Other authority remains. | Terminal only when committed. Retry, provider failure, misleading result, and concurrent verification lack proof under `AUTH-LC-CHOICE-005/009`; owners #394/#776/#724. |
| `AUTH-LC-TOTP-008` | Pending TOTP factor — **Finish authenticator setup** | Valid OTP changes `pending → enrolled`, clears setup expiry, retains protected secret, writes audit, and commits before response construction awaits policy readout. Verification decrypts the protected seed into a mutable byte array and does not prove disposal/zeroization on success or failure. | The committed factor becomes eligible even if later policy-readout fault/cancellation returns no success. Retrying verification then conflicts because status is enrolled; no setup result is redisplayed. | Enrollment is irreversible; disable/revoke separate. Decrypted-seed lifetime, post-save handoff, retry UX, policy readout, and verify-versus-cancel/revoke concurrency remain partial under `AUTH-LC-CHOICE-005/012`; owners #394/#776/#724. |
| `AUTH-LC-TOTP-013` | Pending TOTP factor — **Finish authenticator setup** encounters seed-unprotect failure | If the protected enrollment payload is corrupt or its Data Protection key is unavailable, `VerifyTotpCode` throws before `totp.enrollment_failed` audit/failure handling. | No bounded result is returned; the factor stays pending, no failure audit is written, and the attempt is not durably counted. A retry can repeat the exception; a later replacement setup may revoke the pending row separately. | Nonterminal failed attempt, not enrollment/revocation. Current failure behavior is partial under `AUTH-LC-CHOICE-005/009/012`; require key-loss/corrupt-ciphertext result, audit, retry, replacement, and redaction tests. Owners #394/#776/#724. |
| `AUTH-LC-TOTP-004` | Enrolled TOTP factor — **Disable authenticator** | Proposed `enrolled → disabled`; schema supports the status, but current self-service runtime exposes revoke only. | Disabled factor must not authenticate. Exact session, recovery, and policy-compliance effects are unresolved. | Conditional re-entry only after approved checks. Unimplemented; `AUTH-LC-CHOICE-005`. Owner #394/#465/#776. |
| `AUTH-LC-TOTP-005` | Disabled TOTP factor — **Re-enable authenticator** | Proposed `disabled → enrolled`; no runtime exists. | May restore future TOTP use; must not revive sessions/challenges or bypass current policy. | Conditional, not silent restore. Unimplemented; `AUTH-LC-CHOICE-005`. Owner #394/#465/#776. |
| `AUTH-LC-TOTP-006` | Enrolled TOTP factor — **Revoke authenticator** | Current owner passes the policy/actor-dependent step-up check and, when required, supplies fresh proof; service changes retained factor `enrolled → revoked`. | Factor stops future TOTP authentication; current code does not revoke existing sessions, challenges, recovery batches, or other factors. | Irreversible for that factor; re-enroll creates new protected material. Implemented, but protected-secret disposition and anti-lockout/session response are unresolved under `AUTH-LC-CHOICE-005`. |
| `AUTH-LC-TOTP-007` | Owned non-revoked TOTP factor — generic **Revoke authenticator** endpoint | Current generic factor revoke accepts `pending`, `enrolled`, `disabled`, or `expired` and changes it `→ revoked` after the policy/actor-dependent step-up check; this overlaps pending cancellation with a different audit reason. | Target factor/setup stops any future use; sessions, challenges, recovery batches, and other factors remain. | Irreversible for that row. Implemented broad source-state behavior; whether the endpoint should be narrowed is unresolved in `AUTH-LC-CHOICE-005`. Owner #394/#776. |
| `AUTH-LC-TOTP-009` | Owned MFA factor record — **Rename authenticator** | Current owner passes the policy/actor-dependent step-up check and may replace `DisplayLabel`/`UpdatedAtUtc` on any owned factor; loading does not restrict `pending`, `enrolled`, `disabled`, `expired`, or `revoked` status. Audit records `mfa.factor_metadata_updated` with bounded factor type/outcome metadata. | Label changes do not change authentication usability, factor status, sessions, challenges, recovery batches, account, or roles. A terminal record remains terminal but its retained evidence metadata is mutable. | Repeat is last-writer metadata replacement, not re-enable/reactivation. Implemented sequentially; allowed terminal source states, concurrent rename/revoke ordering, and evidence immutability expectations are unresolved under `AUTH-LC-CHOICE-005`. Owner #394/#776. |
| `AUTH-LC-TOTP-010` | Enrolled TOTP factor — not separately user-visible: **Use authenticator code** | Successful TOTP challenge verification updates the factor's `LastUsedAtUtc`/`UpdatedAtUtc` and separately verifies the bound challenge. Each submitted-code check decrypts the protected seed into a mutable byte array and returns without proved disposal/zeroization, including failed-code paths. If unprotect throws because key material is unavailable or ciphertext is corrupt, verification exits before challenge-failure handling, so the incremented attempt is not saved and no failure audit is written. | The factor remains enrolled; the calling challenge's scoped outcome may proceed only after successful decrypt/verification. A decrypt failure can repeatedly strand the challenge attempt and prevent trying a healthy sibling through that request; it creates no session by itself and does not alter roles or other factors. | Repeatable use, not re-enable/reactivation. Implemented sequentially; decrypt/key-rotation failure result and sibling behavior, active-use decrypted-seed lifetime, verify-versus-revoke/rename ordering, out-of-order last-used metadata, cross-challenge timestep replay, and anonymous binding remain partial under `AUTH-LC-CHOICE-005/006/009/012`. Owner #394/#776/#724. |
| `AUTH-LC-RCV-001` | Active recovery-code batch — **Replace recovery codes** | With enrolled TOTP, an active batch, explicit `ReplaceExisting`, and the policy/actor-dependent step-up result satisfied, the old batch changes `active → replaced`. Old verifiers and each new persisted root are separately inventoried below. | The old batch stops governing availability; unused old-code authority ends only with `AUTH-LC-RCV-008`. Sessions, factors, and roles remain. | Irreversible for this batch. Implemented sequentially; eligibility, retry, rollback, and concurrent generation are unresolved under `AUTH-LC-CHOICE-005/012`. Owner #775/#776. |
| `AUTH-LC-RCV-002` | One recovery-code verifier — not a management action: **Used** | Successful bound challenge changes one observed `unused → consumed` and decrements batch count through ordinary tracked updates. Two concurrent challenges can each read the same unused verifier and both commit success. | After an uncontested commit the code cannot satisfy another challenge; concurrent submissions can currently authorize two scoped outcomes. It does not re-enable/rotate a factor or alter session lifetime. | Irreversible after commit, but single-use is proven only sequentially. Atomic consume is partial under `AUTH-LC-CHOICE-005/006`; owner #775. Password-primary MFA binding remains `REC-AUTH-MFA-SIGNIN-001`. |
| `AUTH-LC-RCV-003` | Recovery-code batch — **Revoke recovery codes** | Current owner passes the policy/actor-dependent step-up result and changes an active batch `active → revoked`; unused-verifier revocation is `AUTH-LC-RCV-012`. | Batch becomes terminal; individual unused-code authority stops only when its separate verifier roots commit. Existing sessions/factors remain. | Irreversible for the batch; generate a new batch if policy permits. Implemented sequentially; rollback/concurrency and verifier disposition remain `AUTH-LC-CHOICE-005`. |
| `AUTH-LC-RCV-004` | Recovery-code batch — **Expired** | Schema defines batch expiry state, but current management runtime does not materialize this transition. | An expired batch must govern no authentication authority; verifier expiry is separate and exact factor/session effects are unresolved. | Terminal. Unimplemented; `AUTH-LC-CHOICE-005`. Owner #775/#776. |
| `AUTH-LC-RCV-005` | Owned replaced, expired, or already-revoked recovery batch — generic **Revoke recovery codes** endpoint | Current service accepts any owned batch and rewrites it `→ revoked`, refreshing revoke/update/reason fields and audit; broad unused-verifier mutations are separately inventoried. | Batch remains terminal and existing sessions/factors remain. | No state restoration, but repeated revoke is not state/audit idempotent. Implemented broad source-state behavior; desired guard is unresolved in `AUTH-LC-CHOICE-005`. Owner #775/#776. |
| `AUTH-LC-RCV-006` | Recovery-code batch — first **Generate recovery codes** | With enrolled TOTP, no active batch, and the policy/actor-dependent step-up result satisfied, service creates one active batch. Individual verifier issuance and raw display are `AUTH-LC-RCV-011`; no predecessor is replaced. | Batch becomes the aggregate for newly issued code verifiers; it grants no authentication authority without those verifier roots. | Initial creation is not replacement/reactivation. Implemented sequentially; concurrent first generation, rollback, and aggregate uniqueness are partial under `AUTH-LC-CHOICE-005/012`. Owner #775/#776. |
| `AUTH-LC-RCV-007` | Active recovery-code batch — not user-visible: **Record code use** | Successful consumption of a verifier separately increments `UsedCount`, decrements `RemainingUnusedCount`, and updates `LastUsedAtUtc`/`UpdatedAtUtc` on the batch. | Aggregate availability/readout changes; the verifier transition in `AUTH-LC-RCV-002` is the authority-bearing single-use change. Concurrent distinct-code uses can both succeed yet overwrite the same loaded counters and regress last-used time. | Repeat belongs to another unused code, not restore. Implemented sequentially, but distinct-code aggregate counters/monotonic time lack a concurrency guard under `AUTH-LC-CHOICE-005/006`. Owner #775. |
| `AUTH-LC-RCV-008` | Unused old recovery-code verifiers — **Replace recovery codes** | The authorized replacement workflow changes each observed old verifier `unused → replaced` separately from the old batch and new roots. | All transitioned old codes stop satisfying recovery challenges; consumed verifiers remain consumed. | Irreversible for transitioned verifiers. Implemented sequentially; observed-set concurrency, rollback, and verifier retention/disposition are partial under `AUTH-LC-CHOICE-005/012`; owner #775/#776. |
| `AUTH-LC-RCV-009` | Replacement recovery-code batch — **Generate new recovery codes** | After authorized old-batch replacement, the service creates a distinct new active batch. New verifier rows and display are `AUTH-LC-RCV-010`. | The new aggregate becomes readable, but grants no recovery authority without its separate verifier roots. | New creation, not reactivation. Implemented sequentially; multiple-active-batch race, rollback, and retry are partial under `AUTH-LC-CHOICE-005/012`; owner #775/#776. |
| `AUTH-LC-RCV-010` | Replacement recovery-code verifiers — **Generate new recovery codes** | The replacement workflow creates multiple salted `unused` verifier rows linked to the new batch; matching raw codes exist transiently and cross only the successful immediate display-once response. After the batch/verifiers/audit and predecessor replacements commit, response construction awaits policy readout while retaining the raw-code collection; fault/cancellation can strand usable replacement authority, invalidate old codes, and return no display-once values. Retrying with `ReplaceExisting=true` replaces that stranded active batch/verifiers again and can return a different newly issued set; the earlier stranded raw codes are not recoverable. | Each committed verifier can authorize one scoped recovery challenge until a successful replacement marks it replaced, subject to the concurrent-consume gap. A retry may remove the stranded authority but is new issuance, never redisplay. | Issuance is not redisplay. Implemented sequentially; batch/verifier rollback, failed-save and post-save raw-code lifetime, repeated replacement, failure injection, and concurrency are partial under `AUTH-LC-CHOICE-005/012`; owner #775/#776. |
| `AUTH-LC-RCV-011` | Initial recovery-code verifiers — first **Generate recovery codes** | The first-generation workflow creates multiple salted `unused` verifier rows linked to `AUTH-LC-RCV-006`; matching raw codes exist transiently and cross only the successful immediate display-once response. After batch/verifiers/audit commit, response construction awaits policy readout while retaining the raw-code collection; fault/cancellation can strand usable verifier authority and return no display-once values. Retrying with `ReplaceExisting=false` deterministically returns conflict because that committed active batch now exists; it cannot recover the stranded codes. | Each committed verifier can authorize one scoped recovery challenge even when the caller received no raw codes; no predecessor code is replaced and an ordinary retry does not redisplay them. | Initial issuance, not replacement/reactivation. Implemented sequentially; batch/verifier rollback, failed-save and post-save raw-code lifetime, recovery UX after conflict, failure injection, and concurrent first generation are partial under `AUTH-LC-CHOICE-005/012`; owner #775/#776. |
| `AUTH-LC-RCV-012` | Unused recovery-code verifiers — **Revoke recovery codes** | Authorized revocation of an active batch changes each observed unused verifier `unused → revoked` as a separate persisted root. | Transitioned codes stop authenticating; consumed evidence remains consumed and sessions/factors remain. | Irreversible for transitioned verifiers. Implemented sequentially; rollback/revoke-versus-use concurrency and sensitive verifier disposition are partial under `AUTH-LC-CHOICE-005`; owner #775/#776. |
| `AUTH-LC-RCV-013` | Unused verifiers under a replaced, expired, or already-revoked batch — generic **Revoke recovery codes** endpoint | The broad current endpoint again changes any observed unused verifier `→ revoked` while rewriting the terminal batch and audit. | Transitioned codes stop authenticating; other authority remains. | No re-entry. Implemented broad behavior; repeated/broad source-state selection, rollback, and concurrent use are partial under `AUTH-LC-CHOICE-005`; owner #775/#776. |
| `AUTH-LC-RCV-014` | Recovery-code verifier — **Expired** | Schema defines verifier expiry state, but current management runtime does not materialize it. | Expired verifier must not satisfy recovery proof; batch aggregate reconciliation and other authority are unresolved. | Terminal. Unimplemented under `AUTH-LC-CHOICE-005`; owner #775/#776. |
| `AUTH-LC-CHL-001` | Anonymous TOTP/recovery challenge — not user-visible: **Verified** | Successful anonymous proof changes a pending challenge `→ verified` and records consumption time; load by challenge ID/type does not enforce persisted account/session predicates. TOTP completion also does not persist/reject the accepted timestep. | Intended scoped result may proceed, but pending-flow/account/session binding is unproved and one current TOTP code can satisfy separate pending challenges. Success never extends refresh lifetime. | Irreversible per challenge. Implemented sequentially; anonymous binding, proof replay, policy revalidation, and completion concurrency are partial under `AUTH-LC-CHOICE-005/006`; owner #394/#775 and `REC-AUTH-MFA-SIGNIN-001`. |
| `AUTH-LC-CHL-002` | Anonymous passkey sign-in challenge — not user-visible: **Consumed** | Successful anonymous passkey sign-in changes `pending → consumed`; completion does not revalidate policy or enforce an options-time account hint, then creates the separately inventoried unusable-to-caller session. | Challenge cannot be reused after commit, but a cross-account credential can consume it and the caller receives no usable session credential. | Irreversible. Sequential behavior is implemented; binding, concurrency, policy revalidation, audit ordering, and handoff are partial under `AUTH-LC-CHOICE-005/006` and `REC-AUTH-PASSKEY-SESSION-001`. |
| `AUTH-LC-CHL-003` | MFA/passkey challenge — **Expired** | Server time blocks verification after `ExpiresAtUtc`; preparation helpers set tracked `pending → expired`, but the verification callers return without saving that change, so the persisted row can remain `pending`. | No auth result; existing sessions/factors remain. | Terminal by validity clock, but durable status materialization is partial/unproven. Retention/status reconciliation is `AUTH-LC-CHOICE-006`. |
| `AUTH-LC-CHL-004` | MFA/passkey challenge — **Blocked** | Preparation changes a pending challenge already at `MaxAttemptCount` to tracked `blocked` but callers return without saving. Threshold failure paths attempt a save but ignore `TrySaveAsync=false`; concurrent invalid attempts can also collapse increments. | Each request reports conflict/denial, yet persistence can remain `pending`, allowing repeated misleading results and retaining live challenge material. Only a successful threshold-path save makes blocking durable. | Terminal only when committed. Missing-save and failed-save boundaries, retry/reconciliation, concurrency, audit and material retention remain partial under `AUTH-LC-CHOICE-006/009`. |
| `AUTH-LC-CHL-005` | MFA/passkey challenge — **Failed** | Schema supports `failed`; current mapped services generally increment attempts and may block rather than consistently materialize this status. | No auth result. | Terminal when used; partial/unresolved by flow under `AUTH-LC-CHOICE-006`. |
| `AUTH-LC-CHL-006` | MFA/passkey challenge — **Cancelled** | Schema supports `cancelled`; no complete current cancellation transition was found. | No auth result; other authority remains. | Terminal; unimplemented and blocked by `AUTH-LC-CHOICE-006`. |
| `AUTH-LC-CHL-007` | MFA/passkey challenge — **Replay detected** | Schema supports `replay_detected`; current challenge services reject terminal reuse but do not consistently materialize this status. | No auth result; suspicion-driven family/session response is unresolved. | Terminal; partial/unresolved under `AUTH-LC-CHOICE-006`. |
| `AUTH-LC-CHL-008` | MFA challenge — **Start verification** | Anonymous route ignores `PendingAuthFlowId` and rejects missing actor. Authenticated issuance commits an account/session-bound pending challenge, then awaits policy readout for the response. | Post-save readout fault strands live challenge authority without options/success; retry creates another pending challenge. Anonymous verification separately omits persisted bindings. | Intended anonymous flow is broken. Authenticated post-save handoff/material lifetime, duplicates, cleanup, pending-flow authority, and races remain `AUTH-LC-CHOICE-006/012`; owner #394/`REC-AUTH-MFA-SIGNIN-001`. |
| `AUTH-LC-CHL-009` | Passkey enrollment challenge — **Start passkey setup** | Authenticated actor commits a pending account/session-bound enrollment challenge and start audit, then awaits policy readout while constructing WebAuthn options. | A post-save fault/cancellation returns no options while challenge material remains live; retry creates another pending challenge, and no credential exists yet. | Expires by clock. Post-save handoff, reusable material lifetime, stranded cleanup/retry, duplicates, completion policy, and concurrency remain `AUTH-LC-CHOICE-005/006/012`; owner #394. |
| `AUTH-LC-CHL-010` | Passkey sign-in challenge — **Start passkey sign-in** | Anonymous caller under enabled passkey policy creates a pending sign-in challenge without a challenge-issuance audit; account binding is populated only when candidate credentials resolve to exactly one account. Completion does not enforce that binding and resolves the asserted credential independently. | Creates bounded ceremony state only and grants no session authority. An identifier hint for account A therefore does not prevent a credential for account B from consuming the challenge and reaching B's sign-in outcome. | Expires by source clock; repeated calls create separate challenges. Implemented but issuance audit/denial evidence is absent; cross-account assertion, identifier disclosure, duplicate state, handoff, and policy change remain `AUTH-LC-CHOICE-005/006/009` and `REC-AUTH-PASSKEY-SESSION-001`. |
| `AUTH-LC-CHL-011` | Passkey step-up challenge — **Start verification** | Authenticated actor commits a pending account/session/operation-bound challenge, then awaits policy readout for options; no issuance audit exists. Stored credential hashes cannot reconstruct IDs, so `allowCredentials` is empty despite permitted non-discoverable credentials. | Post-save fault returns no options while challenge remains live; retry creates another. Non-discoverable credentials may be unusable; issuance alone grants no step-up/session extension. | Expires by clock. Handoff/material lifetime, stranded cleanup/retry, missing audit, options usability, duplicates, policy and concurrency remain `AUTH-LC-CHOICE-005/006/009/012`; owners #394/#724. |
| `AUTH-LC-CHL-012` | Anonymous passkey sign-in challenge — generic retry after **Failed assertion attempt** | Anonymous sign-in proof reaches failure handling before the limit; service increments `AttemptCount`, updates bounded failure/timestamps, writes failure audit with no actor, and deliberately keeps status `pending`. | No credential/session authority is granted; anonymous caller may retry the same challenge while time and remaining attempts allow. | Nonterminal repeat, not terminal `failed`. Implemented sequentially; account binding is unenforced and concurrent attempts can collapse increments/delay blocking under `AUTH-LC-CHOICE-006`. Owner #394. |
| `AUTH-LC-CHL-013` | Anonymous TOTP/recovery challenge — generic retry after **Failed verification attempt** | An anonymous caller submits an invalid TOTP or recovery proof before the limit; service loads by challenge ID/type without enforcing the persisted account/session predicates, increments `AttemptCount`, updates bounded failure/timestamps, writes failure audit with no actor, and keeps status `pending`. | No scoped outcome is granted and no factor/code is consumed; anonymous retry remains possible while time and attempts allow, but the intended pending-flow/account/session binding is unproved. | Nonterminal repeat, not terminal `failed`. Implemented sequentially; authority binding and concurrent attempt accounting are partial under `AUTH-LC-CHOICE-006`. Owner #394/#775 and `REC-AUTH-MFA-SIGNIN-001`. |
| `AUTH-LC-CHL-014` | Authenticated passkey enrollment/step-up challenge — generic retry after **Failed assertion attempt** | Authenticated account/session authority submits invalid enrollment or step-up proof before the limit; service mutates failure metadata and keeps `pending`, but hardcodes null audit actor despite the available caller. | No credential or step-up result is granted; the authenticated caller may retry while challenge scope, time, and attempts allow. | Nonterminal repeat. Implemented sequentially; missing actor, concurrent attempt collapse, and audit/persistence ordering remain partial under `AUTH-LC-CHOICE-006/009`. Owner #394/#724. |
| `AUTH-LC-CHL-015` | Authenticated TOTP/recovery challenge — generic retry after **Failed verification attempt** | An authenticated account/session actor submits an invalid TOTP or recovery proof before the limit; challenge ownership/session predicates are checked, then failure metadata is updated and status remains `pending`, but the failure writer hardcodes null audit actor. | No scoped outcome is granted and no factor/code is consumed; the bound caller may retry while scope, time, and attempts allow. | Nonterminal repeat. Implemented sequentially; authenticated actor attribution, concurrent attempt collapse, and audit/persistence ordering remain partial under `AUTH-LC-CHOICE-006/009`. Owner #394/#775/#724. |
| `AUTH-LC-CHL-016` | Bound authenticated TOTP/recovery `step_up` challenge — not user-visible: **Verified** | Authenticated account/session authority submits successful proof for an explicitly operation-bound step-up; loader enforces account/session predicates, challenge changes `pending → verified`, and success audit can identify the actor. | The bound operation may proceed. The response reports freshness from the configurable MFA runtime window (300 seconds by default); authorization uses a hard-coded 900 seconds but considers only the ten newest consumed step-up challenges before operation matching, so ten newer proofs for other operations can evict this proof even inside its response window. TOTP accepted-timestep reuse across separate challenges and recovery-verifier concurrent consume remain distinct gaps. | Irreversible per challenge. Implemented sequentially; response-versus-authorization time/operation parity and eviction, completion concurrency, proof replay, intervening policy/factor lifecycle, and audit ordering are partial under `AUTH-LC-CHOICE-005/006`; owner #394/#775/#724. |
| `AUTH-LC-CHL-019` | Authenticated TOTP/recovery `sign_in` challenge — not user-visible: **Verified** | Authenticated account/session authority submits successful proof; challenge changes `pending → verified` and the response advertises a configurable 300-second-default `FreshUntilUtc`. | `EvaluateFreshnessAsync` accepts only `step_up` and `passkey_step_up`, so this advertised freshness authorizes no sensitive step-up operation. Factor/recovery roots still record their separate use/consume effects. | Irreversible per challenge. Current response-versus-authorization contradiction is partial under `AUTH-LC-CHOICE-006`; require purpose-specific response/authorization and caller-meaning tests. Owner #394/#775/#724. |
| `AUTH-LC-CHL-020` | Authenticated TOTP/recovery challenge — omitted purpose/operation defaults to unbound `step_up`, then **Verified** | Creation defaults purpose to `step_up` but persists null `RequestContextHash`; successful proof changes `pending → verified` and returns a configurable 300-second-default freshness deadline. | Authorization normalizes a requested operation and cannot match the null binding, so the verified result cannot authorize any scoped sensitive operation despite its success/freshness response. | Irreversible per challenge. Current default is unusable and security-partial under `AUTH-LC-CHOICE-006`; require missing-operation create/complete/authorize tests and an explicit contract decision. Owner #394/#724. |
| `AUTH-LC-CHL-017` | Authenticated passkey enrollment challenge — not user-visible: **Consumed** | Completion consumes the bound challenge in the same save that creates credential/audit, then awaits policy readout for the response without revalidating policy. | After commit the challenge cannot be reused and credential remains active even if readout fails; retry conflicts rather than reconciling/redisplaying success. | Irreversible. Post-save handoff, deterministic retry/reconciliation, atomic completion, policy, uniqueness, and audit rollback are partial under `AUTH-LC-CHOICE-005/006/012`; owners #394/#776. |
| `AUTH-LC-CHL-018` | Authenticated passkey step-up challenge — not user-visible: **Consumed** | Bound account/session/operation actor successfully completes step-up; challenge changes `pending → consumed` with credential/audit updates, then response construction awaits the database-backed policy readout. Completion does not revalidate passkey policy. | Only the bound operation may proceed; no session lifetime extension, credential creation, or role change follows from the challenge alone. A post-save readout fault/cancellation returns no scoped success although authorization can accept the committed challenge; retry conflicts because it is consumed. The response reports the configurable passkey runtime freshness window (300 seconds by default); authorization uses a hard-coded 900 seconds but searches only the ten newest consumed step-up challenges before operation matching, allowing newer other-operation proofs to evict this one even inside the response window. | Irreversible. Implemented sequentially; stranded-success reconciliation/retry, response-versus-authorization time/operation parity and eviction, atomic completion, operation binding, intervening policy, audit ordering, and non-discoverable credential usability remain partial under `AUTH-LC-CHOICE-005/006/012`; owner #394/#724. |
| `AUTH-LC-RST-001` | Password-reset request — not user-visible: **Replace/reset link** | Sequential issuance of newer material changes the pending requests it observed `→ revoked`, sets replaced metadata and links them to the replacement. Concurrent issuances can each miss the other's insertion and leave multiple pending requests. | Observed old reset material cannot reset the password; concurrent newly issued links can both remain usable. Account credential/sessions remain until successful completion. | Irreversible for replaced material. Implemented sequentially; issuance concurrency is partial under `AUTH-LC-CHOICE-007`. Raw material only crosses the immediate delivery boundary. Owner #339. |
| `AUTH-LC-RST-002` | Password-reset request — **Reset password** | Valid pending request is intended `→ consumed` after password replacement; outstanding-request and session cascades plus final audit/save are later boundaries. A relational outer transaction can roll back the workflow; a non-relational password replacement can commit before those later steps fail. | After complete commit, material is consumed and later reuse becomes suspicious. On non-relational partial failure, the new password may work while this request remains pending/retryable and sessions may remain, or a later final save may partially persist provider-dependent roots. | Irreversible only when committed. Provider failure/repair, retry outcome, audit-save boundary and concurrent completion remain partial under `AUTH-LC-CHOICE-002/007/009/013`; owners #339/#338/#724. |
| `AUTH-LC-RST-003` | Password-reset request — **Expired reset link** | Server observes `ExpiresAtUtc` and changes request `pending → expired` during a matched completion attempt. | Material cannot reset a password; account, credential, roles, identities and existing sessions are otherwise unchanged. | Terminal. Implemented lazily; no cleanup runtime. Owner #339. |
| `AUTH-LC-RST-010` | Already-expired password-reset request — generic **Link unavailable** | A later matched completion attempt against an `expired` request rewrites `LastCheckedAtUtc` and `UpdatedAtUtc` while status remains `expired`, then emits another bounded denial audit. | Material remains unusable and no credential, account, role, identity, or session authority changes; the public result remains generic. | Repeatable terminal evidence mutation, not active expiry or reactivation. Implemented sequentially; idempotency semantics, monotonic timestamps, concurrent checks, audit volume/correlation, and retention remain partial under `AUTH-LC-CHOICE-007/009`; owners #339/#724. |
| `AUTH-LC-RST-004` | Consumed/revoked/replaced `email_link` reset request — generic **Link unavailable** after replay | After a matched request passes the scope check, terminal reuse changes it `→ suspicious_replay`; unknown and wrong-scope material follow separate roots. | No credential/session authority is granted and the public response stays generic. The matched request remains terminal with no re-entry. | Irreversible. Implemented sequentially; concurrent reuse, guessing/rate limiting, audit, and disposition remain unresolved under `AUTH-LC-CHOICE-007`. Owners #339 and `REC-AUTH-ABUSE-001`. |
| `AUTH-LC-RST-011` | Already-suspicious password-reset request — generic **Link unavailable** after repeated replay | Reuse of already matched `suspicious_replay` material keeps that status and its first `SuspiciousReplayAtUtc` (`??=`), advances `LastCheckedAtUtc`/`UpdatedAtUtc`, and emits another bounded replay audit; unknown material remains non-disclosing. | No credential/session authority is granted, the request stays terminal, and the public result remains generic. | Repeatable terminal evidence mutation, not a new active-to-replay transition. Implemented sequentially; timestamp monotonicity, concurrent retries, audit volume/correlation, rate limiting, and retention remain partial under `AUTH-LC-CHOICE-007/009`; owners #339/#724 and `REC-AUTH-ABUSE-001`. |
| `AUTH-LC-RST-012` | Matched wrong-scope password-reset material — generic **Link unavailable** | A request whose persisted scope is not `email_link` is denied before terminal-status classification; its pending/consumed/revoked/replaced/expired/suspicious status stays unchanged while `LastCheckedAtUtc`/`UpdatedAtUtc` advance and a bounded `wrong_scope` denial audit is written. | No password/session authority is granted. Terminal material is not reclassified as suspicious replay, and the public result remains generic. | Repeatable evidence mutation, not replay, expiry, or reactivation. Schema permits `typed_code`, but completion accepts only `email_link`; ordering, retry/concurrency, audit volume, retention, and future scope ownership remain partial under `AUTH-LC-CHOICE-007/009`; owners #339/#724. |
| `AUTH-LC-RST-005` | Pending password-reset requests — not user-visible: **Revoke outstanding reset links** | Successful reset completion changes other observed pending requests `→ revoked`; sequential newer issuance uses the distinct replacement transition in `AUTH-LC-RST-001`. Account/policy denial does not revoke them. | Revoked material cannot reset a password; it does not itself replace the credential. | Terminal for each revoked request. Implemented on successful completion; concurrency, trigger completeness, and retention remain `AUTH-LC-CHOICE-007`. Owner #339. |
| `AUTH-LC-RST-006` | Password-reset workflow — **Request password reset** | For every syntactically valid request, endpoint first invokes `RequestResetAsync`, which resolves the submitted identifier and attempts to persist bounded `password_reset.requested` audit before delivery readiness, abuse, or provider checks. An unhandled failure of that save prevents the orchestrator from reaching those gates and can bypass the intended generic public response. Only after a successful save may the orchestrator issue material. | Default-disabled, throttled, unknown, and ineligible requests grant no authority but can still perform account lookup and attempt a durable pre-gate audit. A failed write records no throttle attempt and reaches no provider/material boundary; raw material otherwise crosses only an approved immediate provider boundary. | Repeat is a new attempt. Partially implemented: durable-write failure/result truth, admission/audit-flood protection, ordering, enumeration timing, provider truth, and issuance concurrency remain `AUTH-LC-CHOICE-007/009`; owners #339/#724 and `REC-AUTH-ABUSE-001`. |
| `AUTH-LC-RST-007` | Password-reset request — first **Issue reset link** | With no observed pending predecessor, the eligible anonymous workflow creates one pending request with lookup hash, purpose, scope, issue/expiry and delivery metadata, writes material-issued audit, and holds raw material only for the immediate provider boundary. It does not assign `CleanupEligibleAtUtc`. | New material can attempt one password reset until consume/revoke/replace/expiry/replay; no password/session authority changes at issuance. | Initial issuance, not replacement. Implemented sequentially; concurrent first issuance, provider/final-audit failure, raw-material lifetime/disposal, retry/no-redisplay, and retention are partial under `AUTH-LC-CHOICE-007/012`; owner #339/#724. |
| `AUTH-LC-RST-008` | Pending password-reset request — generic **Link unavailable** after dependency denial | When account, identity, credential, or policy eligibility fails, the request remains `pending`; only `LastCheckedAtUtc`/`UpdatedAtUtc` and bounded denial audit change. | No authority is granted for that attempt. Material can become usable again if dependencies recover before expiry, so the result is not revocation or replay terminalization. | Nonterminal repeat, not restore. Implemented; dependency-recovery policy, retry/race, audit, and whether denial should revoke remain unresolved under `AUTH-LC-CHOICE-002/007`; owner #339/#724. |
| `AUTH-LC-RST-009` | Password-reset request — **Issue replacement reset link** | After observed pending predecessors are changed by `AUTH-LC-RST-001`, the same issuance workflow creates a distinct new pending request with lookup hash and expiry, writes material-issued audit, and returns raw material only to the immediate provider boundary. | New material becomes eligible; observed predecessors are unusable, but concurrent issuances can each create a pending usable link. | New issuance, not restore. Implemented sequentially; atomic replacement linkage, concurrent multi-link creation, provider/audit failure, raw-material lifetime, retry/no-redisplay, and retention are partial under `AUTH-LC-CHOICE-007/012`; owner #339/#724. |
| `AUTH-LC-INV-001` | Invitation — **Accept invitation** | Valid pending invitation is transactionally changed `→ accepted` after policy, secret, account, and credential checks. The acceptance service requires capability `enabled`; it ignores `PendingInviteGraceWhenDisabled`, even though the policy readout can report public acceptance enabled from that grace flag. Acceptance audit incorrectly writes the inviter as `ActorAuthAccountId` even though the actor is the anonymous bearer-material caller and the created account is already the subject. | Invitation becomes single-use; accepted account gets only the approved `user` role. The invitation is not a session and does not sign the user in. A disabled capability currently rejects existing pending invitations despite the grace readout. | Terminal for invitation; rollback logic restores pending only if account-creation persistence fails. Runtime is sequentially implemented, but grace, relational behavior, and actor attribution are partial under `AUTH-LC-CHOICE-008`; capability/provider default-off. Owner #784. |
| `AUTH-LC-INV-002` | Pending invitation — **Resend invitation** with ready requested delivery | When delivery is requested, readiness passes, and composition succeeds, the invitation remains `pending`; server commits a rotated secret hash before handing new raw material to delivery. | Prior link stops redeeming; only the newly generated material can redeem until expiry, even if its send later fails or is ambiguous. No account/session/role changes occur. | Old material is irreversibly replaced. Implemented sequentially and non-idempotent; send outcome remains separately truthful. Owner #784. |
| `AUTH-LC-INV-003` | Pending invitation — **Revoke invitation** | Authorized owner/admin changes `pending → revoked`, records actor/time, and sets current cleanup eligibility. The API accepts a reason, but service, persistence, and audit ignore it. | Link stops redeeming immediately. No accepted account, credential, session, identity, or role is removed. | Terminal. Implemented sequentially for status/usability but reason evidence is absent and revoke-versus-accept remains partial under `AUTH-LC-CHOICE-008`. Owner #784. |
| `AUTH-LC-INV-004` | Pending invitation selected in the 50-row batch — authenticated management/read discovers **Expired invitation** | Before authenticated list/get/create/revoke/resend primary work, lifecycle cleanup changes up to 50 eligible `pending → expired`; the caller's requested operation then proceeds or fails on the updated state. Cleanup audit always has null actor and the service does not receive the resolved management caller/request correlation. | Selected link cannot redeem; no account/session/role change. Expired rows beyond the batch follow `INV-013/014/015`, not this persisted transition. | Terminal. Implemented for selected rows; authenticated trigger attribution/correlation, batch selection, and failure ordering remain under `AUTH-LC-CHOICE-008/009`. Owner #784/#724. |
| `AUTH-LC-INV-011` | Pending invitation — anonymous acceptance reports generic **Invitation unavailable** after expiry | Before anonymous acceptance validates abuse policy or submitted material, lifecycle cleanup changes eligible `pending → expired`; later matching remains generic. | Link cannot redeem; no account/session/role change. The anonymous caller can trigger this state mutation without proving invitation possession. | Terminal state, but the public mutation authority/exposure boundary is unresolved under `AUTH-LC-CHOICE-008`; implemented. Owner #784/#724. |
| `AUTH-LC-INV-012` | Matched pending invitation — anonymous acceptance reports generic **Invitation unavailable** after target-specific expiry | After bounded global cleanup and secret lookup, acceptance observes its target's `ExpiresAtUtc`, changes that still-pending target `→ expired`, sets cleanup eligibility, saves it, then writes denial audit in a separate save. The target can reach this branch when it falls outside the 50-row cleanup batch. | The matched link cannot redeem; no account/session/role is created. Unlike `INV-011`, submitted material has resolved one target before mutation. | Terminal. Implemented; anonymous proof boundary, batch selection, two-save failure/atomicity, retry/concurrency, audit and retention remain under `AUTH-LC-CHOICE-008/009`; owners #784/#724. |
| `AUTH-LC-INV-013` | Expired-by-clock pending invitation beyond the cleanup batch — authenticated **List/Get projection** | After cleanup persists at most 50 other expired rows, list/get can load a still-`pending` row and `MapSummary` presents it as `expired` without changing `Status`, `ExpiredAtUtc`, `CleanupEligibleAtUtc`, or audit evidence. A pending-status list filter can therefore return an item displayed as expired. | The response says expired and the clock prevents legitimate acceptance, but the authoritative row remains pending and is not cleanup-eligible. No account/session/role changes. | Repeatable projection, not a durable lifecycle transition. Current behavior is partial under `AUTH-LC-CHOICE-008/009`; require over-batch list/get/filter, status-truth, reconciliation, actor/correlation, and retention tests. Owner #784/#724. |
| `AUTH-LC-INV-014` | Expired-by-clock pending invitation beyond the cleanup batch — authenticated **Revoke/Resend denial** | After cleanup persists at most 50 other expired rows, revoke/resend can load the still-`pending` target, reject it by clock, and return without setting expiry/cleanup metadata or writing target-expiry evidence. | The requested mutation/delivery does not proceed and the clock prevents legitimate acceptance, but the authoritative row remains pending and not cleanup-eligible. | Repeatable terminal denial without state reconciliation. Current behavior is partial under `AUTH-LC-CHOICE-008/009`; require over-batch revoke/resend, response/status truth, actor/correlation, retry, and retention tests. Owner #784/#724. |
| `AUTH-LC-INV-015` | Expired-by-clock pending invitation beyond the cleanup batch — authenticated **Create invitation duplicate denial** | After cleanup persists at most 50 other expired rows, create checks the still-`pending` row for the same normalized contact without considering `ExpiresAtUtc` and returns `DuplicatePendingInvitation`; it does not create replacement material, persist expiry/cleanup metadata, or write target-expiry evidence. | The obsolete link is unusable by clock, but it remains pending/not cleanup-eligible and blocks a new invitation for that contact. No account/session/role or delivery authority is created. | Repeatable duplicate denial without state reconciliation. Current behavior is partial under `AUTH-LC-CHOICE-008/009`; require over-batch create, actor/audit, retry, status reconciliation, concurrency, and retention tests. Owner #784/#724. |
| `AUTH-LC-INV-005` | Terminal invitation — authenticated management/read triggers **retention cleanup** | Before authenticated `list`, `get`, `create`, `revoke`, or `resend` primary work, current runtime physically deletes up to 50 eligible accepted/revoked/expired rows and writes aggregate cleanup audit. | Deletion can remove unrelated row-level invitation evidence without altering accepted accounts; the authenticated caller did not request or confirm this destructive side effect. | Irreversible current behavior, not endorsed policy. Management/read trigger authority, failure ordering, holds/copies, warning/confirmation, and evidence sufficiency are blocked by `AUTH-LC-CHOICE-008`; owner #784/#724. |
| `AUTH-LC-INV-006` | Pending invitation — **Resend invitation** without ready requested delivery | When delivery is not requested, readiness fails, or composition is unavailable, the invitation stays `pending`, `UpdatedAtUtc` changes, delivery state is recorded, and the existing secret hash is not rotated. | Existing link remains redeemable until its original expiry; no new material is emitted and no account/session/role change occurs. | No credential lifecycle replacement occurs. Implemented and tested; client must show the returned non-sent delivery state and must not claim the old link was revoked. Owner #784. |
| `AUTH-LC-INV-007` | Invitation — **Create invitation** | Authorized owner/admin under enabled capability and bounded abuse/contact/role rules creates a pending invitation with lookup hash, seven-day expiry, delivery state, and creation audit; raw material crosses only the immediate optional delivery boundary after persistence. | The link may later be accepted, resent/replaced, revoked, or expired; creation grants no account/session/role authority. | Retry is a new request, while the pending-recipient uniqueness constraint can reject overlap; delivery-after-commit can be ambiguous. Implemented sequentially; relational uniqueness/race/provider truth remains `AUTH-LC-CHOICE-008`. Owner #784. |
| `AUTH-LC-INV-008` | Invitation — failed non-relational acceptance returns invitation to **Pending** | If acceptance saved `accepted` before a later account-bundle save failed, compensation restores that exact invitation to `pending`, clears acceptance/cleanup timestamps, and retains its existing material and contact fields. | The invitation can be attempted again; no created account authority should remain if every separate compensation transition succeeds. This restoration is not invitation reissue or secret rotation. | Conditionally reusable rather than terminal. Implemented; compensation failure/race, accepted-to-pending audit evidence, and safe retry remain partial under `AUTH-LC-CHOICE-008/009`. Owners #784/#724. |
| `AUTH-LC-INV-009` | Pending invitation — failed first non-relational acceptance save reports generic failure | The acceptance path assigns the compensation target before its first bundle save. If that save fails while the invitation is still `pending`, cleanup reloads it, keeps `pending`, clears terminal timestamps, and advances `UpdatedAtUtc`; existing material and contact fields remain. | Invitation authority remains retryable if compensation succeeds; this pending-to-pending metadata mutation creates no account/session/role authority and is not reissue, rotation, expiry, or accepted-state restoration. | Repeatable compensation rather than restore. Implemented; first-save provider behavior, redundant mutation, compensation failure/race, audit evidence, and safe retry remain partial under `AUTH-LC-CHOICE-008/009`. Owners #784/#724. |
| `AUTH-LC-INV-010` | Terminal invitation — anonymous acceptance request triggers **retention cleanup** | Any well-formed anonymous acceptance request invokes global cleanup before abuse, capability, or submitted-secret validation, so even invalid material can physically delete up to 50 unrelated eligible terminal rows and cause aggregate audit. | No invitation/account authority is granted by the trigger, but unrelated row-level evidence is irreversibly removed before caller proof. Public failure remains generic. | Irreversible current behavior, not endorsed policy. Anonymous trigger authority, hostile-input isolation, failure ordering, exposure, holds/copies, and evidence sufficiency are blocked by `AUTH-LC-CHOICE-008`; owner #784/#724. |
| `AUTH-LC-PRO-001` | Newly created user profile — failed invitation acceptance reports generic failure | `InvitationAcceptanceService` non-relational compensation physically removes the exact invited profile after a later acceptance-bundle save fails; invitation restoration is separate. | The failed invitation workflow profile becomes unreadable if compensation completes; it confers no authentication authority and unrelated domain/profile data remain. | Irreversible temporary cleanup only. Invitation ordering, retry, compensation failure, and domain-data boundaries remain partial under `AUTH-LC-CHOICE-008`. Owners #784/#724. |
| `AUTH-LC-PRO-005` | Newly created user profile — failed admin provisioning reports failure | `AdminLocalUserService` non-relational compensation physically removes the exact admin-created profile after a later provisioning-bundle save fails; no invitation restoration exists in this authority. | The failed authenticated admin workflow profile becomes unreadable if compensation completes; it confers no authentication authority and unrelated data remain. | Irreversible temporary cleanup only. Admin actor context, dependency ordering, retry, compensation failure, and domain-data boundaries remain partial under `AUTH-LC-CHOICE-001`. Owners #785/#724. |
| `AUTH-LC-PRO-002` | User profile — setup-only **Create first owner profile** | Bootstrap saves one profile with the other pre-credential roots; only relational source encloses the later password step in the same serializable transaction. | Success makes the profile readable but grants no auth authority. Non-relational credential failure can leave it linked to a stranded active account. | Relational rollback is intended; non-relational partial commit has no compensation. Race/retention remain partial under `AUTH-LC-CHOICE-001`; owner #785/#724. |
| `AUTH-LC-PRO-003` | User profile — invitation **Create invited profile** | A valid invitation acceptance creates one profile root linked to the new invited auth account within the acceptance transaction/compensation workflow. | Profile becomes readable after authorized account use but grants no authentication/role/session authority by itself. | Creation is not reactivation. Relational rollback/collision, compensation, privacy and retention are partial under `AUTH-LC-CHOICE-001/008`; owners #784/#724. |
| `AUTH-LC-PRO-004` | User profile — admin **Create local user profile** | Guarded owner/admin provisioning creates one profile root linked to the new auth account within the relational transaction or non-relational compensation workflow. | Profile becomes admin/account-readable under its separate authorization, but grants no credential, role, or session authority itself. | Creation is not reactivation. Relational rollback/collision, compensation, actor audit, privacy and retention remain partial under `AUTH-LC-CHOICE-001`; owners #785/#724. |
| `AUTH-LC-ABU-001` | Sign-in abuse state — generic request outcome after **Count failed/throttled attempt** | Local sign-in records source, normalized-identifier, combined, and global in-memory bucket timestamps; limits can set bounded throttle-until state. | Subsequent matching sign-in prechecks can be denied until the configured window/throttle permits them; no credential/session/account state changes. | Repeat updates transient process-local state. Implemented and locked per process; restart/multi-node consistency and trusted source-key policy remain `REC-AUTH-ABUSE-001`. |
| `AUTH-LC-ABU-002` | Sign-in abuse state — not user-visible: **Clear identifier scopes before final sign-in success boundary** | After session credentials are created, local sign-in records success and removes identifier/combined in-memory buckets before the final sign-in audit save and before returning credentials; source/global buckets remain. | Later identifier checks lose that history even if final audit persistence fails and the caller receives failure while created session authority is stranded. Remaining source/global scopes can still deny. | Conditional process-memory clear, not evidence/account restore. Final-audit failure, stranded authority, retry, and multi-node semantics remain under `AUTH-LC-CHOICE-009/010/012` and `REC-AUTH-ABUSE-001`. |
| `AUTH-LC-ABU-003` | Sign-in abuse state — not user-visible: **Prune/evict bucket state** | Every check/record drops timestamps at `EntryRetention` (default 30 minutes), clears elapsed throttles, removes empty keyed buckets, and capacity-trims oldest source/identifier/combined buckets. Process restart also loses all buckets. | Removed state no longer influences admission; this is transient non-authoritative cleanup, not deletion of auth/audit records. | Source-defined process-memory behavior; distributed durability, privacy, restart and capacity-eviction acceptance remain `REC-AUTH-ABUSE-001`. |
| `AUTH-LC-ABU-004` | Password-reset abuse state — generic request outcome after **Count reset request** | Anonymous reset initiation records source, normalized-identifier, combined, and global in-memory request timestamps; limits set bounded throttle-until state. | Subsequent matching initiation can be generically denied; no reset credential is issued by the bucket transition itself. | Repeat updates process-local state; there is no success clear. Implemented; restart/multi-node/source-key behavior remains `REC-AUTH-ABUSE-001`. |
| `AUTH-LC-ABU-005` | Password-reset abuse state — generic provider outcome after **Count delivery attempt** | Eligible reset delivery records a distinct source-plus-identifier provider-send bucket and may throttle later sends. | Later provider sends can be suppressed without exposing account existence; request-admission buckets are separate. | Repeat updates process-local state; no explicit clear. Implemented; provider/restart/multi-node semantics remain `REC-AUTH-ABUSE-001` and provider gates. |
| `AUTH-LC-ABU-006` | Password-reset abuse state — not user-visible: **Prune bucket state** | Every check/record removes timestamps at `EntryRetention` (default one hour), clears elapsed throttles, removes empty keyed buckets; restart loses all buckets. | Removed state no longer affects request/provider admission; no reset request or audit row is deleted. | Source-defined transient cleanup, not security-record purge. Distributed durability, privacy, restart and retention configuration remain `REC-AUTH-ABUSE-001`. |
| `AUTH-LC-ABU-007` | Authenticated invitation-management abuse state — operation outcome after **Count attempt** | Create/resend management records operation-scoped source, actor, subject, actor-subject, and global timestamps for throttled, failed, and successful outcomes; unlike successful acceptance, management success is counted. | Later matching authenticated management operations can be denied; invitation/account/session records are unchanged by this state transition. | Repeat process-local updates. Implemented; per-operation outcomes, restart/multi-node/source derivation remain `AUTH-LC-CHOICE-008`/`REC-AUTH-ABUSE-001`. |
| `AUTH-LC-ABU-010` | Anonymous invitation-accept abuse state — generic outcome after **Count failed/throttled attempt** | Acceptance records operation-scoped source/subject/combined/global timestamps for throttled, policy-blocked, and failed outcomes. Successful acceptance instead clears selected scopes under `AUTH-LC-ABU-008`; it is not counted here. | Later anonymous acceptance attempts can be denied; no invitation/account/session root changes solely from this transition. | Repeat process-local updates. Implemented; anonymous source/subject derivation, outcome classification, restart/multi-node behavior remain `AUTH-LC-CHOICE-008`/`REC-AUTH-ABUSE-001`. |
| `AUTH-LC-ABU-008` | Invitation-accept abuse state — not user-visible: **Clear successful subject scopes** | Successful invitation acceptance removes only subject and actor-subject buckets for that operation; source, actor, and global buckets remain. | Cleared subject history stops influencing later checks, while remaining scopes can still deny; accepted invitation/account authority is separate. | Conditional clear, not invitation/evidence deletion. Implemented process-locally; multi-node semantics remain `AUTH-LC-CHOICE-008`/`REC-AUTH-ABUSE-001`. |
| `AUTH-LC-ABU-009` | Invitation abuse state — not user-visible: **Prune bucket state** | Every check/record removes timestamps at `EntryRetention` (default one hour), clears elapsed throttles, removes empty keyed buckets; restart loses all buckets. | Removed state no longer affects invitation admission; no invitation or audit row is deleted. | Source-defined transient cleanup, not invitation hard-delete. Distributed durability, privacy, restart and retention configuration remain `AUTH-LC-CHOICE-008`/`REC-AUTH-ABUSE-001`. |
| `AUTH-LC-POL-001` | General auth security policy — **Save draft security policy** | Proposed create/edit of a `draft` version with no effective authority; schema supports draft state, but current service is read/default focused and no mutation API exists. | Draft does not affect authentication eligibility, factors, sessions, roles, or owner access. | Draft replacement/version retry is undefined. Unimplemented and blocked by `AUTH-LC-CHOICE-011`; owners #394/#465/#785. |
| `AUTH-LC-POL-003` | General auth security policy — **Activate security policy** | Proposed guarded draft/new version `→ active` plus atomic retirement of any predecessor and effective-time/version evidence; no mutation runtime exists. | Only after activation may the version govern later credential/factor/challenge eligibility. Exact existing-session/factor and owner-lockout effects are unresolved; it must not silently delete material. | Activation is not draft save or reactivation of revoked authority. Unimplemented and blocked by `AUTH-LC-CHOICE-011`; owners #394/#465/#785. |
| `AUTH-LC-POL-004` | General auth security policy — **Retire security policy** | Proposed active `→ retired` with actor/reason/retired time and a separately selected safe successor/default; no mutation runtime exists. | Retired version stops governing future eligibility only after successor/fallback and owner-safety rules are approved; it does not revive or delete credentials/sessions/factors. | Terminal for that version; re-entry requires a new version, not restore. Unimplemented and blocked by `AUTH-LC-CHOICE-011`; owners #394/#465/#785. |
| `AUTH-LC-POL-002` | Existing invitation policy — **Change invitation policy** | With an active predecessor, authorized mutation intends to retire it and add a new active version. `LoadActivePolicyAsync` uses `AsNoTracking`, so retirement is not persisted; the relational filtered unique index rejects the new row, while in-memory can retain multiple active rows. | A change cannot be credited as successful replacement. Readout treats disabled-plus-grace as publicly acceptable while acceptance requires `enabled`; existing accounts/sessions are unchanged. | Replacement is incomplete, not implemented. Blocked by `AUTH-LC-CHOICE-008/011`; owner #784/#785. Requires tracked/conditional retirement and relational replacement/version-race tests. |
| `AUTH-LC-POL-005` | No invitation policy — **Create initial invitation policy** | Authorized owner/admin mutation with no active row creates the first active policy/version and bounded policy-change audit in one save; there is no predecessor retirement. | The new policy governs later invitation operations; it does not change accounts/sessions or itself issue/revoke an invitation. | Initial creation, not replacement. Implemented; first-create retry/concurrency, audit rollback, version uniqueness, grace consistency, and weakening gates remain partial under `AUTH-LC-CHOICE-008/011`. Owners #784/#785. |
| `AUTH-LC-AUD-001` | Auth audit events — not user-removable: **Retain security evidence** | Append bounded event evidence; later retention expiry is a condition, not deletion. The bootstrap audit shares the pre-credential save and can remain after non-relational credential failure. | Audit rows never authenticate or authorize. Ordinary revocation must not erase evidence; a stranded bootstrap audit does not repair the unusable account. | Intended additive evidence except bounded invitation/admin compensation. Bootstrap has relational rollback but no non-relational compensation. General disposition is blocked by `AUTH-LC-CHOICE-001/009`; owners #721/#724/#774/#973/#785. |
| `AUTH-LC-AUD-002` | Newly created invitation-acceptance audit — not user-visible: **Compensate failed acceptance** | `InvitationAcceptanceService` compensation queries audit events by the invited account ID and physically removes them with the failed account bundle; invitation restoration is separate. | Removed rows no longer evidence the attempted/partially persisted invitation workflow. Account-wide selection can include concurrent events. | Irreversible failed-workflow cleanup only. Invitation ordering, selection breadth, retry/race, compensation failure, and evidence loss remain partial under `AUTH-LC-CHOICE-008/009`. Owners #784/#724. |
| `AUTH-LC-AUD-003` | Newly created admin-provisioning audit — not user-visible: **Compensate failed provisioning** | `AdminLocalUserService` compensation queries audit events by the admin-created account ID and physically removes them with the failed bundle; no invitation restoration exists in this authority. | Removed rows no longer evidence the attempted/partially persisted authenticated admin workflow. Account-wide selection can include concurrent events. | Irreversible failed-workflow cleanup only. Admin actor context, selection breadth, retry/race, compensation failure, and evidence loss remain partial under `AUTH-LC-CHOICE-001/009`. Owners #785/#724. |

### 5.2 Per-Row Template Completion

The following evidence keys keep citations concise without conflating policy
authority with implementation. Each mapped key is part of that exact row; the
text before **Implementation** is decision authority, while the source/test
list after it is evidence only:

- `E-SESSION`: [shared template](../planning/DAY1_LIFECYCLE_POLICY_TEMPLATE.md) §§ **Purpose And Authority**, **Canonical Shared
  Vocabulary**, **Reusable Child Row Schema**, and **#721 — Authentication And
  Security**, plus [credential/session design](AUTH_CREDENTIALS_SESSIONS_AUDIT_DESIGN.md) §§ **Credential Storage
  Boundaries**, **Session Model Schema Direction**, **Auth Audit Schema
  Direction**, **Retention And Privacy**, and **Runtime Boundaries**.
  **Implementation:** `AuthSessionRuntimeService`,
  `AuthRefreshSessionRuntimeService`, and session,
  refresh-rotation, password-change, and password-reset endpoint tests.
- `E-IDENTITY`: [shared template](../planning/DAY1_LIFECYCLE_POLICY_TEMPLATE.md) §§ **Purpose And Authority**, **Canonical Shared
  Vocabulary**, **Reusable Child Row Schema**, and **#721 — Authentication And
  Security**, plus [identity foundation](AUTH_IDENTITY_FOUNDATION.md) §§ **Identity Concepts**, **Account To
  Profile Mapping**, **Session Model**, **Authorization Model**, **Role
  Boundaries**, **Audit Requirements**, and **Local Mode And Server Mode**.
  **Implementation:** `AuthAccount`, `AuthIdentity`, `SystemRoleAssignment`, current account/identity
  resolution, bootstrap/invitation source, and authorization tests.
- `E-FACTOR`: [shared template](../planning/DAY1_LIFECYCLE_POLICY_TEMPLATE.md) §§ **Purpose And Authority**, **Canonical Shared
  Vocabulary**, **Reusable Child Row Schema**, and **#721 — Authentication And
  Security**; [MFA/passkey architecture](AUTH_MFA_PASSKEY_ARCHITECTURE.md) §§ **Authority Boundaries**, **Factor
  Model**, **Schema-Design Categories**, **Audit Hooks**, **Sensitive Data
  Handling**, and **Enrollment And Challenge Flows**; and [MFA/passkey policy
  audit](AUTH_MFA_PASSKEY_POLICY_AUDIT.md)
  §§ **Authority Boundaries**, **Policy Model**, **Enrollment And Recovery
  Guardrails**, **Audit Coverage**, and **Policy Change Approval And Visibility**,
  with unresolved choices in this policy controlling conflicts.
  **Implementation:** `PasskeyRuntimeService`, `MfaRuntimeService`,
  `AuthSecurityPolicyService`, and
  `AuthMfaPasskeySecurityRegressionTests`.
- `E-RESET`: [shared template](../planning/DAY1_LIFECYCLE_POLICY_TEMPLATE.md) §§ **Canonical Shared Vocabulary**, **Reusable
  Child Row Schema**, and **#721 — Authentication And Security**; [identity
  foundation](AUTH_IDENTITY_FOUNDATION.md) §§ **Identity Concepts**, **Audit Requirements**, and **Local Mode
  And Server Mode**; and [credential/session design](AUTH_CREDENTIALS_SESSIONS_AUDIT_DESIGN.md) §§ **Credential Storage
  Boundaries**, **Auth Audit Schema Direction**, **Retention And Privacy**, and
  **Runtime Boundaries**.
  **Implementation:**
  `LocalPasswordResetService`, its audit writers, and reset/redaction tests.
- `E-INVITE`: [shared template](../planning/DAY1_LIFECYCLE_POLICY_TEMPLATE.md) §§ **Canonical Shared Vocabulary**, **Reusable
  Child Row Schema**, and **#721 — Authentication And Security**, plus
  [invitation policy audit](AUTH_INVITATION_POLICY_AUDIT_READINESS.md) §§ **Authorization Boundaries**, **Contact Identifier
  Policy**, **Lifecycle, Idempotency, And Cleanup**, **Raw Secret And Link
  Handling**, **Audit Event Families**, and **Email And Provider Boundary**.
  **Implementation:**
  invitation management/acceptance/policy/cleanup services and focused tests.
- `E-ABUSE`: [shared template](../planning/DAY1_LIFECYCLE_POLICY_TEMPLATE.md) §§ **Canonical Shared Vocabulary**, **Reusable
  Child Row Schema**, and **#721 — Authentication And Security**, plus [sign-in
  abuse policy](AUTH_SIGN_IN_ABUSE_POLICY.md) §§ **Threat Model**, **Public Response Policy**, **Rate Limiting
  Policy**, **Lockout And Throttling Behavior**, **Audit Boundaries**, and
  **Storage And Schema Considerations**.
  **Implementation:** the three in-memory sign-in, password-reset, and
  invitation abuse-policy services, their options, callers, and focused tests.
- `E-AUDIT`: [shared template](../planning/DAY1_LIFECYCLE_POLICY_TEMPLATE.md) §§ **Canonical Shared Vocabulary**, **Reusable
  Child Row Schema**, and **#721 — Authentication And Security**; [Day 1 scope](../prd/MVP_DAY1_SCOPE.md)
  §§ **Accounts, identity, sessions, and security**, **Storage and privacy**,
  and **Soft delete and archive**; and `AUTH-LC-CHOICE-009`.
  **Implementation:** current audit model/writers and redaction tests.

Notation in this table is normative: `HD` is hard-delete eligibility and `P`
is purge/disposal eligibility. `HD=no; P=unresolved` means the lifecycle action
cannot delete the authoritative row and a later separate disposition remains
blocked. `retry=current` credits only the result described in section 5.1 and
current tests; otherwise retry and concurrent conflict are explicitly
`unresolved`. All retained evidence uses section 3's redaction boundary and
section 4's bounded audit fields. “Refresh” means refresh authoritative server
state and show the precise usability effect; it does not mean refresh-token
rotation. Compact `CHOICE-nnn` references in this table mean the exact stable
`AUTH-LC-CHOICE-nnn` record in section 6; “same gates” means the complete gate
set on the immediately preceding named row, not an omitted gate decision.

| Exact row | Decision authority / implementation evidence | Ordinary read, later mutation, and re-entry | Idempotency, replay, retry, concurrency | Retention, hard delete, purge | Audit, privacy, client | Choice; status; owner/lane; manual gates; runtime acceptance |
| --- | --- | --- | --- | --- | --- | --- |
| `AUTH-LC-SES-001` | Template + `E-SESSION`; current source/tested sequentially | Requested session excluded; linked sibling/family/credential roots separately inventoried; overlapping rotation can remain usable; new sign-in only | Relational save has provider transaction semantics; non-relational uses one save without rollback proof. Already-terminal retry can skip partial-failure repair; rotation can escape and validation ignores family status | Session class §5.3; HD=no; P=unresolved | Success/denial for requested session, no credential; do not promise every family authority ended | `CHOICE-010`; provider/race/retry-partial; #338/auth; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; provider failure, retry repair, rotation/revoke/validation tests |
| `AUTH-LC-SES-002` | Template + `E-SESSION`; current source/tested sequentially | Target session excluded; sibling/family/credential roots separately inventoried; overlapping rotation can remain; unrelated authority remains | Missing/not-owned/terminal stays non-disclosing. Relational save has provider transaction semantics; non-relational has no rollback proof, terminal retry can skip repair, rotation can escape, and validation ignores family status | Session class §5.3; HD=no; P=unresolved | Actor/target/outcome, no credential; refresh list without universal revoke claim | `CHOICE-010`; provider/race/retry-partial; #338/#774 auth/client; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; ownership/provider failure/repair/rotation tests |
| `AUTH-LC-SES-003` | Template + `E-SESSION`; current source/tested sequentially | Selected unexpired sessions excluded; undiscovered refresh lineage or overlapping-rotation session may remain; new sign-in only | Zero-selected is success. Relational save has provider transaction semantics; non-relational has no rollback proof, and concurrent replacement can escape selection while validation ignores family status | Session class §5.3; HD=no; P=unresolved | Actor/account/count/outcome, no credential; do not promise every refresh/access authority is revoked | `CHOICE-010/013`; provider/lineage/race-partial; #338; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; provider failure/expired-linkage/rotation-race regressions |
| `AUTH-LC-SES-004` | Template + `E-SESSION`; current validation source | Exclude when expired even if stored status is active; new sign-in only | Repeated validation denies; status-materialization race unresolved | Session class §5.3; HD=no; P=unresolved | Expiry denial if safely attributable; generic client reauth | `CHOICE-010`; partial lazy state; #338/#1059; `G-RET`,`G-PRIV`,`G-DEST`; time/race/cleanup tests |
| `AUTH-LC-SES-005` | Template + `E-SESSION`; current local-sign-in source/tested sequentially | New access session readable; family/credential separately inventoried; later revoke/expire distinct; old-proof issuance can escape concurrent change/reset cascade | Creation save is atomic with family/credential and creation audit, but raw values already exist in process and the later sign-in-success audit can fail after commit, externally returning no material while leaving inaccessible active authority; retry creates independent authority; MFA prerequisite absent | Session class §5.3; HD=no; P=unresolved | Safe session ID/times/device only; raw values transient between services and externally handed off only after final audit, never audit/persistence; failed-path disposal proof absent | `CHOICE-002/010/012`; partial final-audit handoff/disposal/replacement race/MFA binding; #338/#339/`REC-AUTH-MFA-SIGNIN-001`; `G-AUTH`,`G-RET`,`G-PRIV`; final-audit failure/material-lifetime, old-password race, retry, MFA, redaction tests |
| `AUTH-LC-SES-006` | Template + `E-SESSION`; current passkey source/tested sequentially | Active session row exists but caller cannot use it because bearer is not returned/set; revoke/expire distinct | Create succeeds, but response drops raw token; retry can create more unusable-to-caller rows | Session class §5.3; HD=no; P=unresolved | Audit safe session/challenge IDs only, no raw/hash; do not show signed-in success | `CHOICE-005/010/012`; incomplete handoff; #394/#338/`REC-AUTH-PASSKEY-SESSION-001`; `G-AUTH`,`G-CONTRACT`,`G-CLIENT`,`G-RET`,`G-PRIV`; usable-credential/no-leak/retry tests |
| `AUTH-LC-SES-007` | Template + `E-SESSION`; current validation source/tested sequentially | Valid request receives current actor/roles; last-seen metadata changes; expiry unchanged; no refresh authority created | Status/account/time checked before ordinary tracked timestamp update; validation-versus-revoke can save metadata/audit after revoke, out-of-order use can regress last-seen, and family status is ignored | Session class §5.3; HD=no; P=unresolved | Validation outcome/session/account/time/correlation and bounded device only; never bearer/hash; no user-facing transition claim | `CHOICE-010`; partial concurrency/family validation; #338/#774; `G-AUTH`,`G-RET`,`G-PRIV`; relational validate-revoke/out-of-order last-seen/family-status tests |
| `AUTH-LC-SES-008` | Template + `E-SESSION`; current refresh source, in-memory tests | New same-family access session usable; predecessor access session remains; later revoke/expire distinct | Coupled transactionally to credential rotation on relational path; retry is predecessor replay, and concurrent revoke/replay can miss the inserted session | Session/refresh classes §5.3; HD=no; P=unresolved | Safe session/family/rotation outcome only; raw bearer immediate response, never audit/hash; show refreshed | `CHOICE-010/012`; partial relational/race evidence; #338/#1059; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; relational issue/rotation/revoke/replay/no-secret tests |
| `AUTH-LC-SES-009` | Template + `E-SESSION`; current self-sign-out source/tested sequentially | Observed same-family sibling sessions excluded; unrelated families remain; concurrent replacement can escape | Relational save has provider transaction semantics; non-relational uses one save without rollback proof; terminal selected-session retry can skip partial-failure repair | Session class §5.3; HD=no; P=unresolved | Safe account/current-family/sibling-count/status/reason only; never bearer/hash; avoid universal sign-out claim | `CHOICE-010`; provider/race/retry-partial; #338; `G-AUTH`,`G-RET`,`G-PRIV`; provider failure, sibling selection, rotation, repair tests |
| `AUTH-LC-SES-010` | Template + `E-SESSION`; current selected-session revoke source/tested sequentially | Observed target-family sibling sessions excluded; unrelated families remain; concurrent replacement can escape | Ownership derives from target session; provider rollback, target-terminal retry repair, and selection/rotation serialization are incomplete | Session class §5.3; HD=no; P=unresolved | Safe account/target/family/sibling-count/status/reason only; no bearer/hash; generic unavailable target | `CHOICE-010`; provider/race/retry-partial; #338/#774; `G-AUTH`,`G-RET`,`G-PRIV`; ownership, provider failure, sibling selection, repair tests |
| `AUTH-LC-SES-011` | Template + `E-SESSION`; current password-change/session-revoke source/tested sequentially | Selected unexpired non-current sessions excluded; current, expired-lineage, shared-family, or concurrent authority can remain | Relational outer workflow can couple roots; non-relational failure can follow committed verifier change; exclusion/discovery and rotation are not serialized | Session/password classes §5.3; HD=no; P=unresolved | Safe account/session-count/status/reason/correlation only; never password/bearer/hash; do not promise all others signed out | `CHOICE-002/010/013`; provider/discovery/race-partial; #339/#338/#774; `G-AUTH`,`G-RET`,`G-PRIV`; rollback, shared-family, expired-lineage, rotation tests |
| `AUTH-LC-SES-012` | Template + `E-RESET`/`E-SESSION`; current reset/session-revoke source/tested sequentially | Selected unexpired sessions excluded; expired-lineage or concurrent authority can remain | Relational outer workflow can couple roots; non-relational failure can follow committed credential work; discovery/rotation unguarded | Session/reset classes §5.3; HD=no; P=unresolved | Safe account/session-count/status/reason/correlation only; never reset/password/bearer/hash; generic public result | `CHOICE-002/007/010/013`; provider/discovery/race-partial; #339/#338/#773; `G-AUTH`,`G-PROV`,`G-RET`,`G-PRIV`; rollback, expired-lineage, rotation, generic-result tests |
| `AUTH-LC-SES-013` | Template + `E-SESSION`; current refresh-replay source/tested sequentially | Observed replay-family sessions excluded; overlapping replacement can remain; relational loser can fail before mutation | Sequential cascade exists, but nested-transaction relational race branch is broken; provider rollback/selection/family-blind validation remain partial | Session/refresh classes §5.3; HD=no; P=unresolved | Safe family/session-count/replay outcome only when written; no bearer/hash; generic reauth | `CHOICE-010`; sequentially implemented/broken race/provider-partial; #338; `G-AUTH`,`G-RET`,`G-PRIV`; nested transaction, provider failure, selection/validation tests |
| `AUTH-LC-SES-014` | Template + `E-SESSION`; current refresh-expiry source/tested | Observed expired-family linked sessions excluded; unrelated families remain | Matched expiry cascade saves roots together; provider rollback and concurrent selection remain unproved | Session/refresh classes §5.3; HD=no; P=unresolved | Safe family/session-count/expiry category only; no material/hash; generic reauth | `CHOICE-010`; provider/race-partial; #338/#1059; `G-RET`,`G-PRIV`,`G-DEST`; provider failure, clock, concurrent rotation tests |
| `AUTH-LC-SES-015` | Template + `E-SESSION`; current account-unavailable refresh source/tested | Observed one-family sessions excluded; other families remain and terminal rows do not revive | Relational provider uses a transaction; non-relational uses one save without rollback proof; account-wide completeness and family correlation unresolved | Session/refresh classes §5.3; HD=no; P=unresolved | Safe account/family/session-count/status/reason only; no bearer/hash; generic reauth | `CHOICE-001/009/010`; provider/audit-partial; #338/#788/#724; `G-AUTH`,`G-LOCK`,`G-RET`,`G-PRIV`; provider failure, family audit, account-wide/re-enable tests |
| `AUTH-LC-REF-001` | Template + `E-SESSION`; current source, in-memory tests | Predecessor excluded after commit; replacement credential/session and family metadata are separate `REF-017`/`SES-008`/`REF-021`; no predecessor re-entry | Relational code uses status-qualified consume/transaction, but current focused tests do not execute that provider branch | Refresh class §5.3; HD=no; P=unresolved | Predecessor lineage/status/outcome, never raw/hash; do not attribute other roots to this row | `CHOICE-010`; partial relational evidence; #338; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; relational atomic-race and predecessor tests |
| `AUTH-LC-REF-021` | Template + `E-SESSION`; current rotation source, focused family-root tests absent | Family stays active with replaced last-rotated/update times; predecessor and replacement roots separate; no validity extension | Same intended relational transaction as rotation; non-relational one-save rollback, concurrent last-writer/monotonic time, and exact family-subject audit are unproved | Refresh class §5.3; HD=no; P=unresolved | Safe family/account/rotation-time/outcome only; current audit lacks family subject; never raw/hash | `CHOICE-009/010`; provider/concurrency/audit-partial; #338/#724; `G-AUTH`,`G-RET`,`G-PRIV`; family-root rollback, monotonic-time, exact-subject and concurrent-rotation tests |
| `AUTH-LC-REF-002` | Template + `E-SESSION`; current source/tested sequentially | Reuse-classified credential drives active family to replayed; terminal-source rewrite is separate `REF-022`; active credential/session roots separate; relational race loser can fail before mutation | Sequential active-family replay works; the relational `consumedRows != 1` branch invokes a nested transaction before the rolled-back outer transaction is disposed and throws | Refresh class §5.3; HD=no; P=unresolved | Replay/family safe ID/prior-new outcome required; current audit lacks exact family subject and broken branch emits none; generic reauth | `CHOICE-010`; broken relational race; #338; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; credential-classification, active-family, nested-transaction, authority-cascade and family-state tests |
| `AUTH-LC-REF-022` | Template + `E-SESSION`; current terminal-source behavior, focused tests absent | Reuse-classified credential drives revoked/expired/replayed family to replayed; expired credential uses `REF-023`; active credential in any current terminal family is audit-only because `RevokedAtUtc` wins classification | Replay helper replaces terminal evidence; repeated or concurrent reuse is non-idempotent and provider rollback/exact audit subject are unproved | Refresh class §5.3; HD=no; P=unresolved | Safe family ID/credential classification/prior-new terminal outcome/reason category required; current audit lacks exact family subject; never raw/hash; generic reauth | `CHOICE-009/010`; terminal-evidence integrity partial; #338/#724; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; exact credential/family combinations, repeated/concurrent reuse, rollback, monotonic-time and family-audit tests |
| `AUTH-LC-REF-003` | Template + `E-SESSION`; current source/tested | Active family becomes expired and is excluded; terminal expired rewrite separate `REF-023`; credential/session roots separate; no re-entry | Expiry determined by clock; matched path works sequentially; provider rollback/concurrency unproved | Refresh class §5.3; HD=no; P=unresolved | Expiry/status safe IDs/times only, never raw/hash; generic reauth | `CHOICE-010`; implemented sequentially; #338/#1059; `G-RET`,`G-PRIV`,`G-DEST`; clock/rollback/race tests |
| `AUTH-LC-REF-023` | Template + `E-SESSION`; current terminal-expiry behavior, focused tests absent | Expired credential drives revoked/replayed/expired family to expired; active credential in a normally materialized expired family is audit-only because `RevokedAtUtc` wins classification; active-to-expired belongs only to `REF-003`; selected active refresh credentials expire under `REF-019`, while linked active access sessions are revoked under `SES-014` | Expiry helper replaces terminal evidence only on the expired-credential path; repeated/concurrent requests, exact terminal family sources, provider rollback and exact family audit are unproved | Refresh class §5.3; HD=no; P=unresolved | Safe family ID/credential classification/prior-new terminal outcome/reason category required; never raw/hash; generic reauth | `CHOICE-009/010`; terminal-expiry evidence integrity partial; #338/#1059/#724; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; revoked/replayed/expired family sources with expired credential, active-credential audit-only case, repeated/concurrent expiry, rollback, monotonic-time and family-audit tests |
| `AUTH-LC-REF-004` | Template + `E-SESSION`; current local-sign-in source/tested sequentially | New active family readable; later rotate/replay/revoke/expire distinct; failed final audit can leave inaccessible family authority | Same creation save as initial session/credential/audit; later sign-in audit is outside that save; retry creates another family; old-proof and MFA binding partial | Refresh class §5.3; HD=no; P=unresolved | Safe family/account/status/absolute-expiry only; no raw material/hash | `CHOICE-002/010/012`; partial handoff/issuance binding; #338/#339; `G-AUTH`,`G-RET`,`G-PRIV`; final-audit failure, retry, old-proof/MFA, lineage tests |
| `AUTH-LC-REF-005` | Template + `E-SESSION`; current local-sign-in source/tested sequentially | Initial active credential/hash readable server-side; raw value exists transiently between services and is externally returned only on whole-workflow success; rotate/replay/revoke/expire distinct | Same creation save as session/family/audit; later sign-in audit can fail before response; async task/state-machine reference lifetime plus disposal/zeroization are unproved; retry creates unrelated material | Refresh class §5.3; HD=no; P=unresolved | Safe credential/family/session IDs and expiry only; raw refresh transient immediate-handoff data, never stored/audited/exposed hash | `CHOICE-002/010/012`; partial handoff/material-lifetime/issuance binding; #338/#339; `G-AUTH`,`G-RET`,`G-PRIV`; final-audit material-lifetime, no-redisplay/leak, retry tests |
| `AUTH-LC-REF-006` | Template + `E-SESSION`; current source/tested | Account-unavailable family excluded; credential/session roots separate; other families remain and re-enable cannot restore this family | Relational provider uses a transaction; non-relational provider uses one save without rollback proof. Provider failure and account-wide coordination unresolved | Refresh class §5.3; HD=no; P=unresolved | Current events identify account/status but omit family ID; require family subject/correlation; generic reauth | `CHOICE-001/009/010`; provider/audit partial; #338/#788/#724; `G-AUTH`,`G-LOCK`,`G-RET`,`G-PRIV`,`G-DEST`; provider failure, family audit, account isolation/re-enable tests |
| `AUTH-LC-REF-007` | Template + `E-SESSION`; current sign-out source/tested sequentially | Current linked family excluded; unrelated families remain; later sign-in creates new lineage | Roots share one save; relational provider supplies transaction semantics, non-relational rollback is unproved, and selection/rotation/family-blind validation remain unsafe | Refresh class §5.3; HD=no; P=unresolved | Safe family/session/account/status/reason only; current audit does not prove family-subject correlation; no material/hash | `CHOICE-010`; provider/race/audit/retry-partial; #338/#724; `G-AUTH`,`G-RET`,`G-PRIV`; per-root provider failure/repair, family-subject, rotation tests |
| `AUTH-LC-REF-008` | Template + `E-SESSION`; current sign-out source/tested sequentially | Observed active credentials excluded; concurrent replacement can escape; unrelated families remain | Roots share one save; relational provider supplies transaction semantics, non-relational rollback is unproved, terminal retry can skip repair, and rotation is unguarded | Refresh class §5.3; HD=no; P=unresolved | Safe credential/family/status/reason only; never raw value or exposed hash; show only sign-out result | `CHOICE-010`; provider/race/retry-partial; #338; `G-AUTH`,`G-RET`,`G-PRIV`; per-root provider failure/repair, concurrent rotation, no-secret tests |
| `AUTH-LC-REF-009` | Template + `E-SESSION`; current owned-session revoke source/tested sequentially | Selected linked family excluded; unrelated families remain; later sign-in creates new lineage | Ownership derives from selected session; roots share one save, relational provider supplies transaction semantics, non-relational rollback is unproved, and selection/rotation are unguarded | Refresh class §5.3; HD=no; P=unresolved | Safe family/selected-session/account/status/reason only; avoid target disclosure; family-subject audit partial | `CHOICE-010`; provider/race/audit/retry-partial; #338/#774/#724; `G-AUTH`,`G-RET`,`G-PRIV`; ownership/provider failure/repair, family-subject, rotation tests |
| `AUTH-LC-REF-010` | Template + `E-SESSION`; current owned-session revoke source/tested sequentially | Observed active linked credentials excluded; concurrent replacement can escape; unrelated families remain | Roots share one save; relational provider supplies transaction semantics, non-relational rollback is unproved, and terminal-target retry/rotation ordering are incomplete | Refresh class §5.3; HD=no; P=unresolved | Safe credential/family/selected-session/status/reason only; no raw/hash; generic unavailable result | `CHOICE-010`; provider/race/retry-partial; #338/#774; `G-AUTH`,`G-RET`,`G-PRIV`; ownership/provider failure/repair, concurrent rotation, no-secret tests |
| `AUTH-LC-REF-011` | Template + `E-SESSION`; current account-session revoke source/tested sequentially | Discovered families excluded; expired-session-only lineage and concurrent replacement can remain | Discovery starts from selected unexpired sessions; roots share one save, relational provider supplies transaction semantics, non-relational rollback is unproved, and lineage/concurrent insertion are unguarded | Refresh class §5.3; HD=no; P=unresolved | Safe account/family/count/status/reason only; require per-family correlation; do not promise universal sign-out | `CHOICE-010/013`; provider/discovery/race/audit-partial; #338/#724; `G-AUTH`,`G-RET`,`G-PRIV`; provider failure, expired-linkage, per-root audit, rotation tests |
| `AUTH-LC-REF-012` | Template + `E-SESSION`; current account-session revoke source/tested sequentially | Observed credentials in discovered families excluded; undiscovered or concurrent replacement authority can remain | Credential selection inherits unexpired-session family discovery and is not serialized with rotation | Refresh class §5.3; HD=no; P=unresolved | Safe account/family/credential-count/status/reason only; never raw/hash; do not promise universal sign-out | `CHOICE-010/013`; implemented/discovery/race-partial; #338; `G-AUTH`,`G-RET`,`G-PRIV`; expired-linkage, rollback, concurrent rotation, no-secret tests |
| `AUTH-LC-REF-013` | Template + `E-SESSION`; current password-change/session-revoke source/tested sequentially | Discovered non-excluded families stop; current-session-shared, expired-session-only, or concurrent lineage can remain; password replacement separate | Relational outer transaction can couple roots, but current-session exclusion removes shared families and non-relational failure lacks rollback across the workflow | Refresh class §5.3; HD=no; P=unresolved | Safe account/family/count/status/reason/correlation only; family subject currently partial; never password/material/hash | `CHOICE-002/010/013`; implemented/exclusion/discovery/provider/audit-partial; #339/#338/#774/#724; `G-AUTH`,`G-RET`,`G-PRIV`; shared-family, expired-lineage, per-root rollback/audit, rotation tests |
| `AUTH-LC-REF-014` | Template + `E-SESSION`; current password-change/session-revoke source/tested sequentially | Observed non-excluded credentials stop; shared-family, undiscovered, or concurrent credentials can remain | Selection inherits current-session family exclusion and unexpired-session discovery; provider rollback and rotation serialization incomplete | Refresh class §5.3; HD=no; P=unresolved | Safe account/family/credential-count/status/reason only; never password, raw refresh or exposed hash; avoid universal sign-out claim | `CHOICE-002/010/013`; implemented/exclusion/discovery/provider/race-partial; #339/#338/#774; `G-AUTH`,`G-RET`,`G-PRIV`; shared-family, expired-lineage, rollback, concurrent rotation, no-secret tests |
| `AUTH-LC-REF-015` | Template + `E-RESET`/`E-SESSION`; current reset/session-revoke source/tested sequentially | Discovered families stop without current-session exclusion; expired-session-only or concurrent lineage can remain; reset consume/password replacement separate | Relational outer transaction can couple roots; non-relational later revoke failure can follow committed credential work, and discovery/rotation are not serialized | Refresh/reset classes §5.3; HD=no; P=unresolved | Safe account/family/count/status/reason/correlation only; family subject partial; never reset/password/material/hash; generic public result | `CHOICE-002/007/010/013`; implemented/discovery/provider/audit/race-partial; #339/#338/#773/#724; `G-AUTH`,`G-PROV`,`G-RET`,`G-PRIV`; expired-lineage, per-root rollback/audit, rotation, generic-result tests |
| `AUTH-LC-REF-016` | Template + `E-RESET`/`E-SESSION`; current reset/session-revoke source/tested sequentially | Observed credentials stop; expired-session-only or concurrently inserted credentials can remain | Selection inherits unexpired-session discovery; reset consume/replacement/cascade rollback and rotation serialization incomplete | Refresh/reset classes §5.3; HD=no; P=unresolved | Safe account/family/credential-count/status/reason only; never reset/password/raw refresh/hash; generic public result | `CHOICE-002/007/010/013`; implemented/discovery/provider/race-partial; #339/#338/#773; `G-AUTH`,`G-PROV`,`G-RET`,`G-PRIV`; expired-lineage, rollback, concurrent rotation, no-secret/generic-result tests |
| `AUTH-LC-REF-017` | Template + `E-SESSION`; current rotation source, in-memory tests | New same-family credential eligible; predecessor/session roots separate; failures return no raw material | Relational insertion and predecessor consume share the intended transaction, but focused relational/race proof and failed async material lifetime/disposal are absent | Refresh class §5.3; HD=no; P=unresolved | Safe credential/family/session/expiry only; raw refresh immediate response, never audit/hash | `CHOICE-010/012`; partial relational/material-lifetime evidence; #338/#1059; `G-AUTH`,`G-RET`,`G-PRIV`; issue/rollback/race/no-leak/disposal tests |
| `AUTH-LC-REF-018` | Template + `E-SESSION`; current replay source/tested sequentially | Observed active credentials become replayed; matched terminal predecessor unchanged; concurrent replacement can escape | Active-row selection occurs in cascade; broken relational loser can fail before it and rotation is not serialized | Refresh class §5.3; HD=no; P=unresolved | Safe family/credential-count/replay outcome; never raw/hash; generic reauth | `CHOICE-010`; sequentially implemented/broken-race partial; #338; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; nested transaction, selection/rotation/no-secret tests |
| `AUTH-LC-REF-019` | Template + `E-SESSION`; current expiry source/tested | Observed active credentials become expired; family/session roots separate | Matched cascade saves roots together; provider failure, clock boundary, and concurrent selection remain unproved | Refresh class §5.3; HD=no; P=unresolved | Safe family/credential-count/expiry category only; never raw/hash; generic reauth | `CHOICE-010`; provider/race partial; #338/#1059; `G-RET`,`G-PRIV`,`G-DEST`; clock/provider/selection/no-secret tests |
| `AUTH-LC-REF-020` | Template + `E-SESSION`; current account-unavailable refresh source/tested | Observed one-family active credentials become revoked; other families remain; no re-entry | Relational provider uses a transaction; non-relational uses one save without rollback proof; account-wide and concurrent rotation completeness unresolved | Refresh class §5.3; HD=no; P=unresolved | Safe account/family/credential-count/status/reason; never raw/hash; generic reauth | `CHOICE-001/009/010`; provider/race partial; #338/#788/#724; `G-AUTH`,`G-LOCK`,`G-RET`,`G-PRIV`; provider failure, account isolation/re-enable/rotation tests |
| `AUTH-LC-PWD-001` | Template + `E-SESSION`; current source/tested sequentially | Last committed verifier remains active; concurrent acknowledged replacement can lose; current session remains; undiscovered refresh lineage or a family shared with the excluded current session may remain | Same-password/current-proof results defined, but credential update has no concurrency token/status-qualified guard; selected-session transaction does not serialize two replacement reads, and family discovery/exclusion makes cascade completeness partial. Without a relational outer transaction, session-revoke failure can follow a committed verifier replacement and return failure | Password class §5.3; HD=no; P=unresolved | Credential change/denial audit exists but `EfAuthCredentialAuditWriter` records null actor even for this authenticated request; later session audit cannot supply actor for early denial. Never old/new/verifier; do not promise rollback, both concurrent changes, or every other refresh ended | `CHOICE-002/009/012/013`; partial actor/replacement/rollback/cascade; #339/#338/#724/#774; `G-AUTH`,`G-CLIENT`,`G-RET`,`G-PRIV`; authenticated actor, proof/relational race/non-relational failure-injection/expired-linkage/shared-family/no-secret tests |
| `AUTH-LC-PWD-002` | Template + `E-RESET`; current source/tested sequentially | Last committed verifier active after success; an earlier concurrently acknowledged password can fail; reset-request consume is separate `RST-002` and session effects use separate `SES`/`REF` roots | Credential replacement lacks an atomic version/status guard; concurrent completions can both succeed with last-writer verifier state, and provider/workflow coupling is incomplete | Password class §5.3; HD=no; P=unresolved | Password-credential replacement outcome only; never password/verifier/reset material; do not promise every acknowledged concurrent password | `CHOICE-002/007/012/013`; partial replacement/rollback coupling; #339/#338/#773; `G-AUTH`,`G-PROV`,`G-RET`,`G-PRIV`; concurrent distinct-password replacement, failure-injection and redaction tests |
| `AUTH-LC-PWD-003` | Template + `E-IDENTITY`; schema/consumer evidence, no mutation | Disabled verifier excluded from sign-in and current reset issuance/resolution/replacement; re-enable/recovery path absent | Idempotency/retry/concurrency unresolved; disable would lock out password reset, so block mutation pending separately authorized recovery/last-method proof | Password class §5.3; HD=no; P=unresolved | Required success/denial/actor/reason, no verifier; unavailable UI; never present reset as recovery | `CHOICE-002`; unimplemented/lockout-blocked; #788/#339; `G-AUTH`,`G-LOCK`,`G-SCHEMA`,`G-CONTRACT`,`G-CLIENT`,`G-PRIV`; disabled-password reset denial/recovery/session tests |
| `AUTH-LC-PWD-011` | Template + `E-IDENTITY`; schema/consumer evidence, no mutation | Disabled verifier remains excluded until separately authorized re-entry; terminal sessions/material never revive | Recovery proof, verifier reuse-versus-replacement, retry/concurrency, compromise, last-method and session effects unresolved; block mutation | Password class §5.3; HD=no; P=unresolved | Required actor/recovery-proof category/prior-new/reason and denial; never password/verifier/reset material; blocked UI | `CHOICE-002`; unimplemented/lockout-blocked; #788/#339; `G-AUTH`,`G-LOCK`,`G-SCHEMA`,`G-CONTRACT`,`G-CLIENT`,`G-PRIV`; proof, compromise, concurrency, session, no-secret tests |
| `AUTH-LC-PWD-004` | Template + `E-IDENTITY`; schema only | Revoked verifier excluded; no re-entry; replacement is distinct | Idempotency/retry/concurrency unresolved; block mutation | Password class §5.3; HD=no; P=unresolved | Required revoke/denial, no verifier; unavailable UI | `CHOICE-002`; unimplemented; #788/#339; same gates as PWD-003; terminal/session/material tests |
| `AUTH-LC-PWD-005` | Template + `E-IDENTITY`; current provider-divergent bootstrap source | Verifier usable only after its later save; non-relational failure can follow committed pre-credential roots | Relational outer transaction can roll back; non-relational path has no transaction/compensation and can strand bootstrap roots | Password class §5.3; HD=no; P=unresolved | Safe credential ID/algorithm only, never plaintext/verifier; failure must not claim rollback | `CHOICE-001/002/012`; provider-partial; #339/#785; `G-AUTH`,`G-LOCK`,`G-PRIV`; relational rollback and non-relational credential-failure/stranding tests |
| `AUTH-LC-PWD-006` | Template + `E-IDENTITY`; current invitation source/tested in-memory | Active verifier usable after acceptance; no session issued; change/reset/revoke distinct | Coupled to acceptance transaction/compensation; relational rollback/race coverage absent | Password class §5.3; HD=no; P=unresolved | Credential-created safe ID/algorithm version only, never plaintext/verifier; generic public acceptance | `CHOICE-002/008/012`; source-implemented/test-partial; #339/#784; `G-AUTH`,`G-PROV`,`G-PRIV`; relational rollback/collision/no-secret tests |
| `AUTH-LC-PWD-007` | Template + `E-IDENTITY`; current admin-local-user source/endpoint-tested | Active verifier usable after provisioning; no session issued; change/reset/revoke distinct | Relational transaction and non-relational compensation exist; endpoint suite does not prove relational rollback/race behavior | Password class §5.3; HD=no; P=unresolved | Credential-created safe ID/algorithm version only, never plaintext/verifier; admin sees created summary | `CHOICE-001/002/012`; source-implemented/test-partial; #339/#785; `G-AUTH`,`G-LOCK`,`G-PRIV`; relational rollback/collision/no-secret tests |
| `AUTH-LC-PWD-008` | Template + `E-IDENTITY`; current verify/rehash source, focused outcome tests | Same password remains eligible with upgraded verifier parameters; no session/role/identity change; predecessor verifier has no re-entry | Rehash occurs after successful verification through ordinary tracked replacement; verify-versus-change/reset/rehash serialization and dedicated audit are partial | Password class §5.3; HD=no; P=unresolved | Safe algorithm/version upgrade category only; never submitted password, predecessor/current verifier, salt/pepper detail | `CHOICE-002`; source-implemented/concurrency-partial; #339; `G-AUTH`,`G-PRIV`; rehash success/failure, out-of-order replacement, and no-secret audit tests |
| `AUTH-LC-PWD-009` | Template + `E-IDENTITY`; current verification source/tested sequentially | Exact caller gains bounded proof result only; last-verified/updated metadata changes; no session until separate issuance | Ordinary tracked timestamps/audit occur after proof; verification-versus-change/reset/disable/rehash and out-of-order metadata lack atomic ordering/version binding | Password class §5.3; HD=no; P=unresolved | Verification success/denial with safe credential/account/correlation only; never password/verifier/hash; no proof redisplay | `CHOICE-002/010`; partial ordering/issuance binding; #339/#338; `G-AUTH`,`G-RET`,`G-PRIV`; old-proof issuance, out-of-order verification, replacement, no-secret audit tests |
| `AUTH-LC-PWD-010` | Template + `E-IDENTITY`/`E-INVITE`; current invitation-compensation source | Failed invitation credential removed; unrelated credentials remain; invitation restoration is separate | Selection uses invited account ID; invitation ordering, retry and compensation failure/provider equivalence partial | Failed-workflow HD=yes only; general P=unresolved | Compensation root/count/outcome needed; never password/verifier/hash; generic acceptance failure | `CHOICE-002/008`; implemented invitation compensation/partial failure proof; #784/#339; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; exact-selection/ordering/retry/failure/no-secret tests |
| `AUTH-LC-PWD-012` | Template + `E-IDENTITY`; current admin-compensation source | Failed admin-provisioning credential removed; unrelated credentials remain; no invitation transition | Selection uses admin-created account ID; actor context, retry and compensation failure/provider equivalence partial | Failed-workflow HD=yes only; general P=unresolved | Compensation root/count/outcome needed; never password/verifier/hash; bounded admin failure | `CHOICE-001/002`; implemented admin compensation/partial failure proof; #785/#339; `G-AUTH`,`G-LOCK`,`G-RET`,`G-PRIV`,`G-DEST`; actor/exact-selection/retry/failure/no-secret tests |
| `AUTH-LC-ACC-001` | Template + `E-IDENTITY`; schema/consumer evidence | Disabled account restricted; re-enable only explicit; dependent mutation unresolved | Idempotency/retry/atomic cascade/last-owner race unresolved | Account class §5.3; HD=no; P=unresolved | Required actor/reason/prior-new/denials; blocked admin UI | `CHOICE-001`; unimplemented; #788/#785; `G-AUTH`,`G-LOCK`,`G-SCHEMA`,`G-CONTRACT`,`G-CLIENT`,`G-PRIV`; last-owner/cascade tests |
| `AUTH-LC-ACC-002` | Template + `E-IDENTITY`; schema only | Re-evaluate every credential/factor/role/session; never revive terminal authority | Idempotency/retry/concurrency and recovery proofs unresolved | Account class §5.3; HD=no; P=unresolved | Required actor/reason/revalidation; blocked admin UI | `CHOICE-001`; unimplemented; #788/#785; same gates as ACC-001; revalidation/denial tests |
| `AUTH-LC-ACC-003` | Template + `E-IDENTITY`; deletion marker/consumer evidence, no mutation | Deleted account excluded by current auth consumers; cascade, profile/domain-data relationship, evidence, restore and re-entry unresolved | Authorization, idempotency, concurrency, last-owner, dependency/hold/copy and logical-versus-physical disposition unresolved | Account class §5.3; HD=no here; P=unresolved/manual | Required actor/prior-new/reason/dependency plan, no private data; blocked admin UI | `CHOICE-001`; unimplemented; #788/#785/#724; `G-AUTH`,`G-LOCK`,`G-SCHEMA`,`G-CONTRACT`,`G-CLIENT`,`G-RET`,`G-PRIV`,`G-DEST`; full cascade/hold/restore-denial tests |
| `AUTH-LC-ACC-004` | Template + `E-IDENTITY`; current provider-divergent bootstrap source | Success creates first-owner roots; non-relational credential failure can leave active account without password and close bootstrap | Relational transaction/table lock couples saves; non-relational first save commits with no compensation before credential workflow | Account/identity/password/role classes §5.3; HD=no; P=unresolved | Safe IDs/roles and truthful partial failure; never identifier/password/verifier | `CHOICE-001/002/003/004`; provider-partial; #785/#339; `G-AUTH`,`G-LOCK`,`G-PRIV`; exact-one relational rollback plus non-relational stranded-account tests |
| `AUTH-LC-ACC-005` | Template + `E-INVITE`; current acceptance source/tested in-memory | New active account/profile/local identity/password/user role readable; no session issued | Serializable relational branch exists in source, but focused suites use in-memory; collision/concurrent accept/rollback evidence partial | Account/identity/password/role/invite classes §5.3; HD=no; P=unresolved | Acceptance safe IDs/status/role, never contact/password/verifier/material; generic public result | `CHOICE-001/002/003/004/008`; source-implemented/test-partial; #784/#339; `G-AUTH`,`G-PROV`,`G-PRIV`; relational collision/single-use/rollback tests |
| `AUTH-LC-ACC-006` | Template + `E-IDENTITY`; current admin-local-user source/endpoint-tested | New active account/profile/local identity/password/user role owner/admin-readable; no session issued | Serializable relational transaction exists; non-relational fallback compensates only its created bundle; focused relational collision/rollback proof absent | Account/identity/password/role classes §5.3; failed-bundle cleanup only; general HD=no; P=unresolved | Creation safe account/profile IDs/status/role and bounded workflow outcome; never identifier/password/verifier | `CHOICE-001/002/003/004/012`; source-implemented/test-partial; #785/#339; `G-AUTH`,`G-LOCK`,`G-PRIV`; relational collision/rollback/audit/no-secret tests |
| `AUTH-LC-ACC-007` | Template + `E-IDENTITY`/`E-INVITE`; current invitation-compensation source | Exact failed invitation account removed; unrelated accounts remain; invitation restoration separate | Exact account-ID selection; invitation ordering, retry/partial compensation/provider equivalence unresolved | Failed-workflow HD=yes only; general account HD/P unresolved | Compensation root/count/outcome needed; no private account payload; generic acceptance failure | `CHOICE-008`; implemented invitation compensation/partial failure proof; #784; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; exact-selection/dependencies/ordering/retry/failure tests |
| `AUTH-LC-ACC-008` | Template + `E-IDENTITY`; current admin-compensation source | Exact failed admin account removed; unrelated accounts remain; no invitation transition | Exact account-ID selection; admin actor context, retry/partial compensation/provider equivalence unresolved | Failed-workflow HD=yes only; general account HD/P unresolved | Compensation root/count/outcome needed; no private account payload; bounded admin failure | `CHOICE-001`; implemented admin compensation/partial failure proof; #785; `G-AUTH`,`G-LOCK`,`G-RET`,`G-PRIV`,`G-DEST`; actor/exact-selection/dependencies/retry/failure tests |
| `AUTH-LC-ID-001` | Template + `E-IDENTITY`; disabled field, no runtime | Unlinked identity excluded; other methods unchanged; relink distinct | Retry/concurrency/provider-collision/last-method result unresolved | Identity class §5.3; HD=no; P=unresolved | Required actor/provider-safe subject/reason, no provider payload; blocked UI | `CHOICE-003`; unimplemented; #787/#773/#785; `G-AUTH`,`G-LOCK`,`G-SCHEMA`,`G-CONTRACT`,`G-CLIENT`,`G-PROV`,`G-PRIV`; takeover/lockout tests |
| `AUTH-LC-ID-002` | Template + `E-IDENTITY`; current provider-divergent bootstrap source | Link resolves to first-owner account; non-relational credential failure can leave it on a stranded account | Relational transaction can roll back; non-relational link commits before credential workflow with no compensation | Identity class §5.3; HD=no; P=unresolved | Safe provider/identity/account IDs; never raw identifier; truthful partial failure | `CHOICE-001/003`; provider-partial; #785/#787; `G-AUTH`,`G-LOCK`,`G-PRIV`; relational rollback plus non-relational stranding/redaction tests |
| `AUTH-LC-ID-003` | Template + `E-INVITE`; current acceptance source/tested in-memory | Local identity resolves to invited account; unlink distinct; no session issued | Acceptance relational branch/constraints source-present; focused relational collision/race proof absent | Identity class §5.3; HD=no; P=unresolved | Provider type/name and safe identity/account IDs, never contact identifier; generic public result | `CHOICE-003/008`; source-implemented/test-partial; #784/#787; `G-AUTH`,`G-PROV`,`G-PRIV`; relational uniqueness/rollback/redaction tests |
| `AUTH-LC-ID-004` | Template + `E-IDENTITY`; current admin-local-user source/endpoint-tested | Local identity resolves to admin-created account; unlink distinct; no session issued | Preflight plus in-transaction check/constraint exist; focused relational collision/race proof absent; failed non-relational bundle is compensated | Identity class §5.3; failed-bundle cleanup only; general HD=no; P=unresolved | Provider type/name and safe identity/account IDs, never submitted identifier in audit; admin sees created summary | `CHOICE-001/003`; source-implemented/test-partial; #785/#787; `G-AUTH`,`G-LOCK`,`G-PRIV`; relational uniqueness/rollback/redaction tests |
| `AUTH-LC-ID-005` | Template + `E-IDENTITY`; architecture/default-disabled capability, no OIDC runtime | Provider link would become eligible only after approved provider proof/account binding; unlink/relink distinct; no implicit role/session/profile merge | Actor/proof, stable issuer-subject uniqueness, takeover/relink, retry/rollback, remaining-method and session outcomes unresolved | Identity class §5.3; HD=no; P=unresolved | Required provider-safe stable reference/account/actor/outcome; never provider credential/token/payload | `CHOICE-003`; unimplemented; #787/#773/#785; `G-AUTH`,`G-LOCK`,`G-SCHEMA`,`G-CONTRACT`,`G-CLIENT`,`G-PROV`,`G-PRIV`; collision/takeover/rollback/session/redaction tests |
| `AUTH-LC-ID-006` | Template + `E-IDENTITY`/`E-INVITE`; current invitation-compensation source | Failed invitation local links removed; unrelated links remain; invitation restoration separate | Selection uses invited account ID; invitation ordering, retry/partial compensation/provider equivalence unresolved | Failed-workflow HD=yes only; general identity HD/P unresolved | Compensation root/count/outcome needed; never identifier/provider payload; generic acceptance failure | `CHOICE-003/008`; implemented invitation compensation/partial failure proof; #784/#787; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; exact-selection/ordering/retry/failure/redaction tests |
| `AUTH-LC-ID-009` | Template + `E-IDENTITY`; current admin-compensation source | Failed admin local links removed; unrelated links remain; no invitation transition | Selection uses admin-created account ID; actor context, retry/partial compensation/provider equivalence unresolved | Failed-workflow HD=yes only; general identity HD/P unresolved | Compensation root/count/outcome needed; never identifier/provider payload; bounded admin failure | `CHOICE-001/003`; implemented admin compensation/partial failure proof; #785/#787; `G-AUTH`,`G-LOCK`,`G-RET`,`G-PRIV`,`G-DEST`; actor/exact-selection/retry/failure/redaction tests |
| `AUTH-LC-ID-007` | Template + `E-IDENTITY`; disabled-field/consumer evidence, no mutation | Local resolution may return only after recovery, account/credential eligibility and collision checks; no other authority revives | Self/admin authority, reuse-versus-new-row, normalized collision/takeover, retry/concurrency, last-method and sessions unresolved | Identity class §5.3; HD=no; P=unresolved | Required actor/account/provider-safe identity/prior-new/reason; never raw identifier, password or provider payload; blocked UI | `CHOICE-003`; unimplemented; #787/#773/#785; `G-AUTH`,`G-LOCK`,`G-SCHEMA`,`G-CONTRACT`,`G-CLIENT`,`G-PRIV`; local recovery/collision/takeover/session/redaction tests |
| `AUTH-LC-ID-008` | Template + `E-IDENTITY`; architecture/default-disabled capability, no OIDC runtime | Provider resolution may return only after stable issuer/subject proof, current policy and collision checks; no account/profile/role/session merge | Provider/self/admin authority, tombstone match, reuse-versus-new-row, takeover, retry/concurrency, last-method and sessions unresolved | Identity class §5.3; HD=no; P=unresolved | Required actor/account/provider-safe stable reference/prior-new/reason; never token, provider credential/payload or raw contact | `CHOICE-003`; unimplemented; #787/#773/#785; `G-AUTH`,`G-LOCK`,`G-SCHEMA`,`G-CONTRACT`,`G-CLIENT`,`G-PROV`,`G-PRIV`; proof/takeover/collision/session/redaction tests |
| `AUTH-LC-ROL-001` | Template + `E-IDENTITY`; current provider-divergent bootstrap source | Assignments readable; non-relational credential failure can leave them on a stranded account | Relational transaction/table lock couples saves; non-relational assignments commit before credential workflow with no compensation | Role class §5.3; HD=no; P=unresolved | Bootstrap/system context, roles/account and truthful partial failure; never credential material | `CHOICE-001/004`; provider-partial; #785/#464/#465; `G-AUTH`,`G-LOCK`; relational rollback plus non-relational stranding/authz tests |
| `AUTH-LC-ROL-002` | Template + `E-IDENTITY`; no mutation runtime; current per-request role read | Committed new assignment affects the next authenticated request without token/session refresh; already-authorized in-flight work is not recalled; re-entry unresolved | Atomic replacement/idempotency/retry/concurrency and last-owner behavior unresolved | Role class §5.3; HD=no; P=unresolved | Required prior/new role + actor/reason; blocked mutation UI | `CHOICE-004`; unimplemented mutation with implemented next-request read; same owners/gates; next-request/in-flight/last-owner tests |
| `AUTH-LC-ROL-003` | Template + `E-IDENTITY`; no mutation runtime; current per-request role read | Committed removal is excluded on the next authenticated request without token/session refresh; later assignment is new event; already-authorized in-flight work is not recalled | Idempotency/retry/concurrency/last-owner and in-flight ordering unresolved | Role class §5.3; HD=no; P=unresolved | Required removal/denial + actor/reason; blocked mutation UI | `CHOICE-004`; unimplemented mutation with implemented next-request read; same owners/gates; next-request/in-flight/self-demotion/concurrency tests |
| `AUTH-LC-ROL-004` | Template + `E-INVITE`; current acceptance source/tested in-memory | Exact user assignment readable after acceptance; no owner/admin role or session; later removal distinct | Coupled to invitation single-use/account bundle; focused relational concurrency/rollback proof absent | Role/invite classes §5.3; HD=no; P=unresolved | Invitation/account/role/outcome safe IDs/categories; never invitation/password material or contact | `CHOICE-004/008`; source-implemented/test-partial; #784/#785; `G-AUTH`,`G-PROV`,`G-PRIV`; relational rollback/single-use/authz tests |
| `AUTH-LC-ROL-005` | Template + `E-IDENTITY`; current admin-local-user source/endpoint-tested | Exact user assignment owner/admin-readable; no owner/admin role or session; later removal distinct | Coupled to guarded provisioning transaction/compensation; focused relational collision/rollback proof absent | Role class §5.3; failed-bundle cleanup only; general HD=no; P=unresolved | Provisioning actor/account/exact role/outcome; never identifier/password/verifier | `CHOICE-001/004`; source-implemented/test-partial; #785/#464/#465; `G-AUTH`,`G-LOCK`,`G-PRIV`; relational rollback/authz/audit tests |
| `AUTH-LC-ROL-006` | Template + `E-IDENTITY`/`E-INVITE`; current invitation-compensation source | Selected failed invitation assignments removed; unrelated assignments remain; invitation restoration separate | Selection includes invited account as subject or assigner; breadth, ordering, concurrent assignment, retry and partial compensation unresolved | Failed-workflow HD=yes only; general role HD/P unresolved | Compensation root/count/role/outcome needed; no identifier/private payload; generic acceptance failure | `CHOICE-004/008`; implemented invitation compensation/selection partial; #784/#464/#465; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; selection-breadth/ordering/concurrency/retry/failure tests |
| `AUTH-LC-ROL-008` | Template + `E-IDENTITY`; current admin-compensation source | Selected failed admin-provisioning assignments removed; unrelated assignments remain; no invitation transition | Selection includes admin-created account as subject or assigner; actor context, breadth, concurrent assignment, retry and partial compensation unresolved | Failed-workflow HD=yes only; general role HD/P unresolved | Compensation root/count/role/outcome needed; no identifier/private payload; bounded admin failure | `CHOICE-001/004`; implemented admin compensation/selection partial; #785/#464/#465; `G-AUTH`,`G-LOCK`,`G-RET`,`G-PRIV`,`G-DEST`; actor/selection-breadth/concurrency/retry/failure tests |
| `AUTH-LC-ROL-007` | Template + `E-IDENTITY`; no mutation runtime; current per-request role read | New assignment adds authorization on the next authenticated request while retaining other assignments; in-flight work unchanged | Authority/elevation, uniqueness/idempotency, concurrent assign/remove, last-owner/self-action and retry unresolved | Role class §5.3; HD=no; P=unresolved | Required actor/account/exact added role/reason and denial; never accept client-supplied authority; blocked UI | `CHOICE-004`; unimplemented mutation with implemented next-request read; #785/#464/#465; `G-AUTH`,`G-LOCK`,`G-SCHEMA`,`G-CONTRACT`,`G-CLIENT`; duplicate/elevation/next-request/in-flight/concurrency tests |
| `AUTH-LC-PKY-001` | Template + `E-FACTOR`; current source/tested sequentially | Revoked credential excluded; metadata owner-read may remain; new enrollment only | Already-revoked retry is sequentially idempotent; ownership/policy-dependent step-up checked; ordinary tracked update leaves concurrent revoke/audit unproved | Passkey class §5.3; HD=no; P=unresolved | Revoke/denial safe credential ID, no assertion/private data; refresh factor list | `CHOICE-005`; partial concurrency; #394/#776; `G-AUTH`,`G-LOCK`,`G-RET`,`G-PRIV`,`G-DEST`; post-revoke/concurrent/session/disposition tests |
| `AUTH-LC-PKY-002` | Template + `E-FACTOR`; schema only | Disabled credential excluded; re-enable only explicit | Idempotency/retry/concurrency unresolved; block mutation | Passkey class §5.3; HD=no; P=unresolved | Required disable/denial, no ceremony payload; unavailable UI | `CHOICE-005`; unimplemented; #394/#776; `G-AUTH`,`G-LOCK`,`G-SCHEMA`,`G-CONTRACT`,`G-CLIENT`,`G-PRIV`; factor/session tests |
| `AUTH-LC-PKY-003` | Template + `E-FACTOR`; schema only | Revalidate credential/account/policy; no session revival | Idempotency/retry/concurrency/compromise response unresolved | Passkey class §5.3; HD=no; P=unresolved | Required re-enable/denial; unavailable UI | `CHOICE-005`; unimplemented; same owners/gates; compromise/re-entry tests |
| `AUTH-LC-PKY-004` | Template + `E-FACTOR`; current source/tested sequentially | Any owned non-revoked passkey row becomes revoked after commit; no re-entry | Broad pending/enrolled/disabled acceptance is current; ordinary tracked update leaves concurrent result/audit and desired source guard unresolved | Passkey class §5.3; HD=no; P=unresolved | Revoke/source-state/reason safe metadata, no assertion/private data; refresh list | `CHOICE-005`; partial broad/concurrent behavior; #394/#776; same gates as PKY-001; source-state/concurrency tests |
| `AUTH-LC-PKY-005` | Template + `E-FACTOR`; current post-save handoff source | Credential eligible after commit even if policy-readout response fails; retry conflicts on consumed challenge; policy not revalidated | Credential/challenge/audit save precedes readout await; reconciliation, concurrent insertion and policy change lack guards | Passkey class §5.3; HD=no; P=unresolved | Safe credential/challenge/outcome only; no private material; do not claim failure means no credential | `CHOICE-005/006/012`; handoff/retry/concurrency partial; #394/#776; `G-AUTH`,`G-LOCK`,`G-RET`,`G-PRIV`; post-save fault/reconciliation/duplicate/policy tests |
| `AUTH-LC-PKY-006` | Template + `E-FACTOR`; current self-service source/tested sequentially | Label/updated time changes on any owned source state; usability/status and all other authority unchanged | Ownership and policy-dependent step-up checked; ordinary update is last-writer and can overlap revoke; terminal evidence metadata remains mutable | Passkey class §5.3; HD=no; P=unresolved | Rename success/denial with safe credential ID/reason only; no prior label, assertion, key, or private payload; refresh list | `CHOICE-005`; implemented broad-state/concurrency-partial; #394/#776; `G-AUTH`,`G-RET`,`G-PRIV`; terminal-state, rename/revoke race, retry/audit-redaction tests |
| `AUTH-LC-PKY-007` | Template + `E-FACTOR`; current anonymous sign-in source/tested sequentially | Enrolled credential records assertion use and causes access-only session creation; challenge's populated account hint is not enforced; caller receives no bearer; later audit-save failure can be reported after these changes commit | Credential/challenge resolution is independent; nested session save creates a provider-independent partial-commit boundary before success audit; concurrent higher-then-lower counter commits can regress state/miss replay suspicion | Passkey/session classes §5.3; HD=no; P=unresolved | Safe credential/challenge/account/session IDs and bounded assertion outcome/counter category; no raw assertion/key; generic public outcome without claiming rollback | `CHOICE-005/006/010/012`; security/rollback-partial; #394/#338/`REC-AUTH-PASSKEY-SESSION-001`; `G-AUTH`,`G-EXPOSE`,`G-PRIV`; cross-account, handoff, audit-failure injection/rollback, revoke-race and relational monotonic-counter/replay tests |
| `AUTH-LC-PKY-008` | Template + `E-FACTOR`; current authenticated step-up post-save source/tested sequentially | Credential/challenge/audit commit before readout response; scoped operation proof only; no session/refresh creation; failed handoff leaves authority usable and retry conflicts | Readout fault/cancellation, 300-second default response versus hard-coded 900-second authorization window, pre-operation-filter ten-challenge cap/eviction, completion policy, verify-versus-revoke/rename, and concurrent higher-then-lower counter ordering lack reconciliation/atomic guards | Passkey/challenge classes §5.3; HD=no; P=unresolved | Safe credential/challenge/account/session/operation and bounded assertion outcome/counter category; no raw assertion/key; do not claim failed response rolled back proof | `CHOICE-005/006/012`; partial handoff/freshness/eviction/concurrency/policy/anti-replay; #394/#776/#724; `G-AUTH`,`G-RET`,`G-PRIV`; post-save fault/retry, time/operation/ten-proof freshness, binding/revoke-race/policy and relational monotonic-counter/replay tests |
| `AUTH-LC-TOTP-001` | Template + `E-FACTOR`; current source/tested sequentially | Pending setup excluded after commit; new enrollment allowed | Already-unavailable generic sequentially; ownership checked; no concurrency token/status-qualified update, so concurrent cancel/revoke audit/outcome is unproved | TOTP class §5.3; HD=no; P=unresolved | Cancel/denial safe ID, never protected secret/OTP; refresh setup state | `CHOICE-005`; partial concurrency; #394/#776; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; cancel-race/secret-erasure tests |
| `AUTH-LC-TOTP-002` | Template + `E-FACTOR`; current source/tested sequentially | Observed prior pending factors excluded; replacement issuance separate `TOTP-012` | Unguarded read/update allows concurrent starts to miss each other's new row; multi-root non-relational save rollback is unproved | TOTP class §5.3; HD=no; P=unresolved | Required prior-factor IDs/count/prior-new status; current enrollment-started audit names only the new factor, so predecessor correlation is absent; no setup secret/OTP | `CHOICE-005/009`; partial concurrency/provider rollback/audit/disposition; #394/#776/#724; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; predecessor audit, provider failure, concurrent-begin and secret-erasure tests |
| `AUTH-LC-TOTP-011` | Template + `E-FACTOR`; current first-enrollment source/tested sequentially | New pending setup eligible; no predecessor replaced; cancel/expire/enroll distinct; failures return no setup response, including a post-save policy-readout failure that leaves committed setup authority stranded | Retry creates another pending setup; no unique pending constraint serializes concurrent first starts; raw secret/manual key span both save and post-save policy-readout awaits, disposal is unproved, and concurrent account unavailability can return an unaudited known-account denial | TOTP class §5.3; HD=no; P=unresolved | Safe factor/status/expiry and bounded unavailable-account denial required; provisioning URI/manual key only immediate success response, never audit/redisplay; no failed-path retention | `CHOICE-005/009/012`; partial retry/concurrency/material lifetime/handoff/audit; #394/#776/#724; `G-AUTH`,`G-RET`,`G-PRIV`; first-start/retry/concurrent uniqueness, failed-save/post-save failure injection, stranded-setup recovery, known-account denial-audit, disposal/no-redisplay/redaction tests |
| `AUTH-LC-TOTP-012` | Template + `E-FACTOR`; current replacement-enrollment source/tested sequentially | New pending replacement factor eligible; prior-factor revoke separate; cancel/expire/enroll distinct; failures return no setup response, including a post-save policy-readout failure after replacement roots commit | Unguarded read-then-insert permits races; relational ordinary save behavior differs from non-relational one-save rollback, which is unproved across predecessor/new-factor/audit roots; raw secret/manual key span save and policy-readout awaits; post-save failure can strand the replacement/predecessor state; concurrent account unavailability can return an unaudited known-account denial | TOTP class §5.3; HD=no; P=unresolved | Safe new factor/status/expiry and bounded unavailable-account denial required; current audit names only the new factor, not predecessors; provisioning URI/manual key only immediate success response and never audit/redisplay | `CHOICE-005/009/012`; partial retry/concurrency/provider rollback/material lifetime/handoff/audit; #394/#776/#724; `G-AUTH`,`G-RET`,`G-PRIV`; replacement save/post-save failure injection, rollback/stranded recovery, predecessor/denial audit, concurrent uniqueness, disposal/no-redisplay tests |
| `AUTH-LC-TOTP-003` | Template + `E-FACTOR`; current ignored-save-result source, focused test absent | Commit excludes expired setup; failed save can leave it pending while response is conflict | `TrySaveAsync=false` is ignored; provider failure/retry, clock and concurrent verification unproved | TOTP class §5.3; HD=no; P=unresolved | Expiry attempt/outcome, no OTP/secret; do not claim persisted expiry from conflict | `CHOICE-005/009`; misleading-result/provider-partial; #394/#776/#724; `G-RET`,`G-PRIV`,`G-DEST`; save-failure/retry/clock/race tests |
| `AUTH-LC-TOTP-008` | Template + `E-FACTOR`; current source/tested sequentially | Enrolled factor eligible after commit even if post-save policy readout fails; retry conflicts; completion does not recheck policy | OTP/audit save precedes policy-readout await; every check decrypts the protected seed into a mutable byte array without proved zeroization; handoff failure, retry UX, verify-versus-cancel/revoke and policy change unguarded | TOTP class §5.3; HD=no; P=unresolved | Safe factor/status only, no OTP/decrypted or protected secret/URI; do not promise failure means not enrolled | `CHOICE-005/012`; handoff/secret-lifetime/concurrency/policy partial; #394/#776/#724; `G-AUTH`,`G-LOCK`,`G-RET`,`G-PRIV`,`G-DEST`; decrypted-seed disposal, post-save fault/retry/race/policy/redaction tests |
| `AUTH-LC-TOTP-013` | Template + `E-FACTOR`; current pending-enrollment unprotect-failure source, focused tests absent | Pending factor remains pending; no authentication authority; replacement setup remains a separate possible transition | Unprotect exception escapes before bounded result/failure audit; attempt is not durably counted; retry and later replacement behavior unproved | TOTP class §5.3; HD=no; P=unresolved | Require bounded decrypt-failure category and safe factor/challenge correlation only; never ciphertext, decrypted seed, URI/key or OTP; generic failure | `CHOICE-005/009/012`; decrypt-failure/audit/retry partial; #394/#776/#724; `G-AUTH`,`G-RET`,`G-PRIV`; key-loss/corrupt-ciphertext result, audit, retry, replacement and redaction tests |
| `AUTH-LC-TOTP-004` | Template + `E-FACTOR`; schema only | Disabled factor excluded; re-enable explicit only | Idempotency/retry/concurrency/last-factor unresolved | TOTP class §5.3; HD=no; P=unresolved | Required disable/denial, no protected secret; unavailable UI | `CHOICE-005`; unimplemented; #394/#465/#776; `G-AUTH`,`G-LOCK`,`G-SCHEMA`,`G-CONTRACT`,`G-CLIENT`,`G-PRIV`; lockout/secret tests |
| `AUTH-LC-TOTP-005` | Template + `E-FACTOR`; schema only | Revalidate factor/account/policy; no session revival | Idempotency/retry/concurrency/secret validity unresolved | TOTP class §5.3; HD=no; P=unresolved | Required re-enable/denial; unavailable UI | `CHOICE-005`; unimplemented; same owners/gates; re-entry/compromise tests |
| `AUTH-LC-TOTP-006` | Template + `E-FACTOR`; current source/tested sequentially | Revoked factor excluded after commit; new enrollment only | Already-revoked idempotent sequentially; ownership/policy-dependent step-up checked; no concurrency token/status-qualified transition | TOTP class §5.3; HD=no; P=unresolved | Revoke/denial safe ID, no secret/OTP; refresh factor list | `CHOICE-005`; partial concurrency; #394/#465/#776; `G-AUTH`,`G-LOCK`,`G-RET`,`G-PRIV`,`G-DEST`; revoke-race/last-factor/session/secret-erasure tests |
| `AUTH-LC-TOTP-007` | Template + `E-FACTOR`; current source/tested sequentially | Any owned non-revoked factor row becomes revoked after commit; no re-entry | Broad source-state acceptance and policy-dependent step-up are current; desired source guard and concurrent transition outcome unresolved | TOTP class §5.3; HD=no; P=unresolved | Revoke/source-state/reason safe metadata, no secret/OTP; refresh factor/setup state | `CHOICE-005`; partial broad/concurrent behavior; #394/#776; same gates as TOTP-006; pending/expired/disabled and concurrency tests |
| `AUTH-LC-TOTP-009` | Template + `E-FACTOR`; current self-service source/tested sequentially | Label/updated time changes on any owned source state; usability/status and all other authority unchanged | Ownership and policy-dependent step-up checked; ordinary update is last-writer and can overlap cancel/revoke; terminal evidence metadata remains mutable | TOTP class §5.3; HD=no; P=unresolved | Metadata-update success/denial with safe factor ID/type/reason only; no prior label, protected secret, URI, or OTP; refresh list | `CHOICE-005`; implemented broad-state/concurrency-partial; #394/#776; `G-AUTH`,`G-RET`,`G-PRIV`; terminal-state, rename/revoke race, retry/audit-redaction tests |
| `AUTH-LC-TOTP-010` | Template + `E-FACTOR`; current challenge-verification source/tested sequentially | Enrolled factor records successful use only after successful decrypt/verify; calling challenge's bounded outcome may proceed; no session or role mutation | Every attempt decrypts to a mutable byte array without proved disposal; unprotect exception escapes before failure save/audit, so attempted count can be lost and the request cannot fall through to a healthy sibling; ordinary updates race lifecycle/use and accepted timestep is not persisted | TOTP/challenge classes §5.3; HD=no; P=unresolved | Safe factor/challenge IDs, purpose, status/outcome and bounded decrypt-failure category only; no OTP/decrypted or protected secret/URI; generic failure | `CHOICE-005/006/009/012`; partial decrypt-failure/secret-lifetime/concurrency/replay/binding; #394/#776/#724; `G-AUTH`,`G-RET`,`G-PRIV`; key-loss/corrupt-ciphertext, attempt/audit persistence, sibling-factor, decrypted-seed disposal, revoke/rename, monotonic-use, timestep and anonymous-binding tests |
| `AUTH-LC-RCV-001` | Template + `E-FACTOR`; current replacement source/tested sequentially | Old batch excluded; old verifiers and new batch/verifiers separately inventoried | Explicit replacement required; roots share ordinary tracked writes, while concurrent calls can create multiple active batches and rollback is unproved relationally | Recovery class §5.3; HD=no; P=unresolved | Old-batch ID/status/reason only; no codes/verifiers; do not imply all roots committed | `CHOICE-005/012`; eligibility/concurrency/rollback-partial; #775/#776; `G-AUTH`,`G-LOCK`,`G-RET`,`G-PRIV`,`G-DEST`; per-root rollback, TOTP/passkey eligibility, active-batch race tests |
| `AUTH-LC-RCV-002` | Template + `E-FACTOR`; current source/tested sequentially | Consumed verifier excluded after commit; batch remains as count permits; concurrent challenges can both obtain scoped success | Ordinary tracked update has no conditional consume/concurrency token; replay denies only after committed state, so single-use is sequential-only | Recovery class §5.3; HD=no; P=unresolved | Required safe verifier/batch/challenge outcome; current `recovery_codes.used` names batch/challenge but no verifier ID, so per-verifier audit correlation is absent; no code/verifier value | `CHOICE-005/006/009`; partial atomic consumption/audit; #775/#724; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; same-code/two-challenge exact-one, verifier-audit/depletion tests |
| `AUTH-LC-RCV-003` | Template + `E-FACTOR`; current source/tested sequentially for active batch | Batch excluded after commit; unused-verifier roots separately inventoried; new generation distinct | Ordinary batch update shares save with verifier roots; rollback and concurrent revoke/use unresolved | Recovery class §5.3; HD=no; P=unresolved | Batch revoke/counts only, no codes/verifiers; refresh list without claiming every root | `CHOICE-005`; concurrency/rollback-partial; #775/#776; same gates as RCV-002; per-root rollback/revoke-use tests |
| `AUTH-LC-RCV-004` | Template + `E-FACTOR`; batch schema only | Expired batch excluded; verifier expiry separate; replacement distinct | Trigger/idempotency/retry/concurrency unresolved | Recovery class §5.3; HD=no; P=unresolved | Required batch expiry/counts, no code/verifier; blocked state | `CHOICE-005`; unimplemented; #775/#776; `G-AUTH`,`G-SCHEMA`,`G-RET`,`G-PRIV`,`G-DEST`; batch clock/depletion tests |
| `AUTH-LC-RCV-005` | Template + `E-FACTOR`; current broad batch source/tested sequentially | Any owned batch is rewritten revoked; verifier roots separate; later generation distinct | Replaced/expired/revoked accepted; repeated batch revoke rewrites timestamps/reason/audit; rollback/concurrency unresolved | Recovery class §5.3; HD=no; P=unresolved | Batch revoke/source-state/counts only; no codes/verifiers; refresh list | `CHOICE-005`; broad/non-idempotent/rollback-partial; #775/#776; same gates as RCV-003; source-state/retry/per-root failure tests |
| `AUTH-LC-RCV-006` | Template + `E-FACTOR`; current first-batch source/tested sequentially | New batch active; initial verifier roots separately inventoried; no predecessor; enrolled TOTP required and passkey-only does not qualify | Retry without replacement conflicts; concurrent first calls can both observe no active batch; rollback/aggregate uniqueness partial | Recovery class §5.3; HD=no; P=unresolved | Generate count/safe batch ID only; no raw codes/verifiers | `CHOICE-005/012`; eligibility/concurrency/rollback-partial; #775/#776; `G-AUTH`,`G-LOCK`,`G-RET`,`G-PRIV`,`G-DEST`; first-batch race/rollback tests |
| `AUTH-LC-RCV-007` | Template + `E-FACTOR`; current recovery-use source/tested sequentially | Batch counts/use time change separately from consumed verifier; other unused codes remain eligible | Concurrent different-code successes can overwrite aggregate increments/decrements and regress last-used time; no concurrency token/conditional aggregate update | Recovery class §5.3; HD=no; P=unresolved | Safe batch/challenge/count/outcome/timing only; no code/verifier; no separate UI | `CHOICE-005/006`; partial aggregate concurrency; #775; `G-AUTH`,`G-RET`,`G-PRIV`; distinct-code exact-two count/monotonic-time/depletion tests |
| `AUTH-LC-RCV-008` | Template + `E-FACTOR`; current replacement source/tested sequentially | Observed old unused verifiers excluded; consumed evidence remains; new verifier roots separate | Ordinary observed-set updates share the save; replacement/use concurrency and rollback unresolved | Recovery class §5.3; HD=no; P=unresolved | Required safe affected verifier IDs/count/status/reason; current generated event names only the new batch, so predecessor-verifier correlation is absent; never raw code/verifier | `CHOICE-005/009/012`; concurrency/rollback/audit/disposition-partial; #775/#776/#724; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; per-root rollback/audit, replace-use, retention tests |
| `AUTH-LC-RCV-009` | Template + `E-FACTOR`; current replacement-batch source/tested sequentially | New active batch readable; new verifier authority separately inventoried | Shares save with old/new roots; concurrent replacement can create multiple active batches; retry/rollback unresolved | Recovery class §5.3; HD=no; P=unresolved | Safe batch ID/count/status only; no codes/verifiers; do not claim display | `CHOICE-005/012`; concurrency/rollback-partial; #775/#776; `G-AUTH`,`G-LOCK`,`G-RET`,`G-PRIV`; active-batch uniqueness/per-root rollback tests |
| `AUTH-LC-RCV-010` | Template + `E-FACTOR`; current replacement-verifier source/tested sequentially | New unused verifiers eligible; old roots separate; raw codes immediate display only; post-save failure can strand committed authority; `ReplaceExisting=true` retry replaces it again and issues a different set, never redisplays it | Shares save with batch/replacement roots; raw codes span post-save policy readout; rollback, repeated replacement, material disposal, and concurrency unresolved | Recovery class §5.3; HD=no; P=unresolved | Required safe batch/issued-verifier count; current event names only batch; raw codes display once, never audit/redisplay | `CHOICE-005/009/012`; material-lifetime/handoff/rollback/concurrency/audit-partial; #775/#776/#724; `G-AUTH`,`G-LOCK`,`G-RET`,`G-PRIV`; failed/post-save injection, stranded replacement retry, predecessor states, per-root audit/no-redisplay tests |
| `AUTH-LC-RCV-011` | Template + `E-FACTOR`; current initial-verifier source/tested sequentially | New unused verifiers eligible; raw codes immediate display only; post-save failure can strand committed authority; `ReplaceExisting=false` retry returns conflict because the active batch exists and cannot redisplay codes | Shares save with initial batch; raw codes span post-save policy readout; rollback, recovery UX after deterministic conflict, disposal, and concurrency unresolved | Recovery class §5.3; HD=no; P=unresolved | Required safe batch/issued-verifier count; current event names only batch; raw codes display once, never audit/redisplay | `CHOICE-005/009/012`; material-lifetime/handoff/rollback/concurrency/audit-partial; #775/#776/#724; `G-AUTH`,`G-LOCK`,`G-RET`,`G-PRIV`; failed/post-save injection, stranded first-issuance conflict, race/per-root audit/no-redisplay tests |
| `AUTH-LC-RCV-012` | Template + `E-FACTOR`; current active-batch revoke source/tested sequentially | Observed unused verifiers excluded; batch root separate; consumed evidence remains | Shares save with batch; revoke/use race and rollback unresolved | Recovery class §5.3; HD=no; P=unresolved | Required safe affected-verifier count/status/reason; current revoked event names only the batch, so per-verifier correlation is absent; never code/verifier/hash | `CHOICE-005/009`; concurrency/rollback/audit/disposition-partial; #775/#776/#724; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; per-root failure/audit/revoke-use/retention tests |
| `AUTH-LC-RCV-013` | Template + `E-FACTOR`; current broad revoke source/tested sequentially | Observed unused verifiers excluded under any owned batch source state; batch rewrite separate | Broad/repeated source state, shared-save rollback, and concurrent use unresolved | Recovery class §5.3; HD=no; P=unresolved | Required safe affected-verifier count/source-state/status; current event names only the batch, so per-verifier correlation is absent; never code/verifier/hash | `CHOICE-005/009`; broad/concurrency/rollback/audit-partial; #775/#776/#724; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; source-state/per-root failure/audit/revoke-use tests |
| `AUTH-LC-RCV-014` | Template + `E-FACTOR`; verifier schema only | Expired verifier excluded; batch reconciliation separate | Trigger/idempotency/retry/concurrency unresolved | Recovery class §5.3; HD=no; P=unresolved | Required verifier expiry/counts only; no code/verifier/hash; blocked state | `CHOICE-005`; unimplemented; #775/#776; `G-AUTH`,`G-SCHEMA`,`G-RET`,`G-PRIV`,`G-DEST`; verifier clock/batch reconciliation tests |
| `AUTH-LC-CHL-001` | Template + `E-FACTOR`; current anonymous MFA source/tested sequentially | Verified challenge terminal after commit; intended bound scope unproved; same TOTP can satisfy another pending challenge | Anonymous load omits account/session predicates; no conditional consume/concurrency token or accepted TOTP timestep/proof identity; intervening policy disable does not block completion | Challenge class §5.3; HD=no; P=unresolved | Purpose/factor/outcome/attempts with null actor appropriate to unresolved anonymous subject; no OTP/verifier; do not claim binding/proof uniqueness | `CHOICE-005/006`; anonymous binding/completion/proof/policy-partial; #394/#775/`REC-AUTH-MFA-SIGNIN-001`; `G-AUTH`,`G-RET`,`G-PRIV`,`G-EXPOSE`; binding, atomic completion, replay, policy tests |
| `AUTH-LC-CHL-002` | Template + `E-FACTOR`; current anonymous passkey sign-in source/tested sequentially | Consumed challenge terminal after commit; hinted account binding unenforced; separately created session unusable to caller | No conditional status update/concurrency token; concurrent completions can succeed; policy not revalidated and cross-account credential can complete | Challenge class §5.3; HD=no; P=unresolved | Sign-in purpose/credential/outcome only, no assertion/challenge/session token; null actor appropriate; do not claim signed in | `CHOICE-005/006`; binding/handoff/concurrency/policy-partial; #394/`REC-AUTH-PASSKEY-SESSION-001`; `G-AUTH`,`G-RET`,`G-PRIV`,`G-EXPOSE`; atomic binding, cross-account, handoff, policy tests |
| `AUTH-LC-CHL-003` | Template + `E-FACTOR`; current clock-denial source/tested | Clock-expired challenge is unusable even when persisted status remains pending; new ceremony required | Repeated completion denies by clock; helper mutation is not saved on early return, and concurrency/status reconciliation is unresolved | Challenge class §5.3; HD=no; P=unresolved | Expiry/purpose safe IDs only when persisted/audited, no material; show expired/generic public failure | `CHOICE-006`; partial/unpersisted status; #394; `G-RET`,`G-PRIV`,`G-DEST`; clock/persistence/race tests |
| `AUTH-LC-CHL-004` | Template + `E-FACTOR`; current missing/ignored-save source | Blocked terminal only after commit; already-at-max path never saves and threshold path can ignore failed save, leaving pending while reporting denial | Non-atomic count/status plus missing/failed persistence; retries can repeat misleading result and retain live material | Challenge class §5.3; HD=no; P=unresolved | Block attempt/outcome/count/reason only; no proof; do not claim durable status from response | `CHOICE-006/009`; persistence/concurrency partial; #394/#724; `G-AUTH`,`G-RET`,`G-PRIV`,`G-EXPOSE`; both save boundaries/retry/reconciliation/atomic threshold tests |
| `AUTH-LC-CHL-005` | Template + `E-FACTOR`; schema/partial flow evidence | Failed challenge excluded if materialized; next mutation unresolved | Retry/status materialization/concurrency unresolved | Challenge class §5.3; HD=no; P=unresolved | Required failure category, no submitted factor; generic failure | `CHOICE-006`; partial; #394/`REC-AUTH-ABUSE-001`; same gates as CHL-004; per-flow transition tests |
| `AUTH-LC-CHL-006` | Template + `E-FACTOR`; schema only | Cancelled challenge excluded; new ceremony distinct | Idempotency/retry/concurrency unresolved; no exposed mutation | Challenge class §5.3; HD=no; P=unresolved | Required cancel actor/reason; blocked UI | `CHOICE-006`; unimplemented; #394; `G-AUTH`,`G-SCHEMA`,`G-CONTRACT`,`G-CLIENT`,`G-RET`,`G-PRIV`; authorization/race tests |
| `AUTH-LC-CHL-007` | Template + `E-FACTOR`; schema/terminal-reject evidence | Replay-detected challenge excluded; session response unresolved | Terminal replay rejects; status materialization/concurrent response incomplete | Challenge class §5.3; HD=no; P=unresolved | Replay/purpose safe IDs, no payload; generic reauth/failure | `CHOICE-006`; partial; #394/`REC-AUTH-ABUSE-001`; `G-AUTH`,`G-RET`,`G-PRIV`,`G-EXPOSE`; replay/session tests |
| `AUTH-LC-CHL-008` | Template + `E-FACTOR`; route/service mismatch plus post-save handoff | Anonymous issuance denied; authenticated challenge can commit before readout response fails and retry creates another; anonymous completion binding broken | Save precedes readout await; live material/cleanup/retry, pending-flow binding and duplicates unresolved | Challenge class §5.3; HD=no; P=unresolved | Safe state/times only; no proof; do not show success/options after failed handoff | `CHOICE-005/006/012`; broken flow/handoff partial; #394/`REC-AUTH-MFA-SIGNIN-001`; `G-AUTH`,`G-CONTRACT`,`G-CLIENT`,`G-RET`,`G-PRIV`; post-save fault/retry/cleanup/binding tests |
| `AUTH-LC-CHL-009` | Template + `E-FACTOR`; current passkey post-save options source | Pending enrollment challenge commits before readout/options; failure strands live material and retry creates another | Save/readout boundary, reusable material cleanup, duplicates and completion policy unresolved | Challenge class §5.3; HD=no; P=unresolved | Safe challenge/account/session/purpose/times; no raw challenge/attestation; do not claim options returned on fault | `CHOICE-005/006/012`; handoff/material partial; #394; `G-AUTH`,`G-RET`,`G-PRIV`; post-save fault/retry/cleanup/binding/duplicate tests |
| `AUTH-LC-CHL-010` | Template + `E-FACTOR`; current anonymous passkey source/tested | Pending sign-in challenge may have no account binding; populated hint unenforced at completion; grants no authority until completion | Options-time policy check exists; completion can consume cross-account credential; non-discoverable descriptor cannot be reconstructed; duplicate/concurrency/handoff partial | Challenge class §5.3; HD=no; P=unresolved | No sign-in challenge-issuance audit/denial event; safe state only in row, no raw assertion/private data; generic options | `CHOICE-005/006/009`; partial binding/options/handoff/audit; #394/#724/`REC-AUTH-PASSKEY-SESSION-001`; `G-AUTH`,`G-EXPOSE`,`G-PRIV`; cross-account/non-discoverable/enumeration/duplicate/issuance-audit tests |
| `AUTH-LC-CHL-011` | Template + `E-FACTOR`; current passkey post-save options source | Pending step-up challenge commits before readout/options; failure strands live material and retry creates another; non-discoverable credential unusable | Save/readout boundary, cleanup, duplicates, policy/resident-key mismatch unresolved; issuance audit absent | Challenge class §5.3; HD=no; P=unresolved | Safe state only; no raw assertion/challenge; do not promise options/handoff | `CHOICE-005/006/009/012`; options/handoff/audit partial; #394/#724; `G-AUTH`,`G-RET`,`G-PRIV`; post-save fault/retry/cleanup/non-discoverable/audit tests |
| `AUTH-LC-CHL-012` | Template + `E-FACTOR`; current anonymous passkey-sign-in failure source/tested sequentially | Challenge stays pending before limit; no credential/session result; anonymous retry allowed | Attempt/failure/audit tracked writes can collapse concurrent increments; sign-in account binding remains unenforced | Challenge class §5.3; HD=no; P=unresolved | Safe challenge/purpose/attempt/outcome/failure category with null actor appropriate to unresolved anonymous subject; no assertion/key; generic retry | `CHOICE-006`; implemented nonterminal/binding/concurrency-partial; #394; `G-AUTH`,`G-RET`,`G-PRIV`,`G-EXPOSE`; anonymous authority, retry, concurrent-attempt, audit-failure/redaction tests |
| `AUTH-LC-CHL-013` | Template + `E-FACTOR`; current anonymous TOTP/recovery failure source/tested sequentially | Challenge stays pending before limit; no scoped result/factor/code consumption; anonymous retry allowed, but account/session binding is unenforced | Anonymous load skips persisted account/session predicates; ordinary attempt/audit writes can collapse concurrent failures | Challenge class §5.3; HD=no; P=unresolved | Safe challenge/factor/purpose/attempt/outcome/failure category with null actor appropriate to unresolved anonymous subject; no OTP/code/secret; generic retry | `CHOICE-006`; implemented nonterminal/binding/concurrency-partial; #394/#775/`REC-AUTH-MFA-SIGNIN-001`; `G-AUTH`,`G-RET`,`G-PRIV`,`G-EXPOSE`; anonymous binding/authority, retry, concurrent-attempt, audit-failure/redaction tests |
| `AUTH-LC-CHL-014` | Template + `E-FACTOR`; current authenticated passkey failure source/tested sequentially | Enrollment/step-up challenge stays pending; no credential/step-up result; authenticated retry allowed | Attempt/failure/audit tracked writes can collapse concurrent increments; audit hardcodes null actor | Challenge class §5.3; HD=no; P=unresolved | Safe challenge/purpose/attempt/outcome/failure category only; authenticated actor missing; no assertion/key; generic retry | `CHOICE-006/009`; implemented nonterminal/concurrency/actor-partial; #394/#724; `G-AUTH`,`G-RET`,`G-PRIV`; authenticated scope/actor, retry, concurrent-attempt, audit-failure/redaction tests |
| `AUTH-LC-CHL-015` | Template + `E-FACTOR`; current authenticated TOTP/recovery failure source/tested sequentially | Bound challenge stays pending before limit; no scoped result/factor/code consumption; authenticated retry allowed | Account/session predicates are checked, but ordinary attempt/audit writes can collapse concurrent failures and failure audit hardcodes null actor | Challenge class §5.3; HD=no; P=unresolved | Safe challenge/factor/purpose/attempt/outcome/failure category only; authenticated actor missing; no OTP/code/secret; generic retry | `CHOICE-006/009`; implemented nonterminal/concurrency/actor-partial; #394/#775/#724; `G-AUTH`,`G-RET`,`G-PRIV`; authenticated scope/actor, retry, concurrent-attempt, audit-failure/redaction tests |
| `AUTH-LC-CHL-016` | Template + `E-FACTOR`; current explicitly bound authenticated MFA step-up source/tested sequentially | Bound challenge verified; matching scoped operation may proceed; TOTP/recovery authority roots separate | Account/session predicates and actor audit exist, but no conditional completion; response uses configurable 300-second default, authorization uses hard-coded 900 seconds and only ten newest proofs before operation filtering; proof can be evicted by other-operation challenges | Challenge class §5.3; HD=no; P=unresolved | Safe actor/challenge/factor/purpose/operation/outcome and bounded freshness only; no OTP/code/verifier; do not imply response deadline alone controls authorization | `CHOICE-005/006`; time/operation/eviction/concurrency/proof/policy-partial; #394/#775/#724; `G-AUTH`,`G-RET`,`G-PRIV`; ten-proof mixed-operation freshness, actor/binding, atomic completion, proof replay, audit tests |
| `AUTH-LC-CHL-019` | Template + `E-FACTOR`; current authenticated MFA `sign_in` success source | Challenge verifies and response advertises 300-second-default freshness; factor/recovery roots separate; no step-up authority | Freshness evaluator excludes `sign_in` purpose, so response deadline authorizes nothing; purpose-specific caller meaning/contract unproved | Challenge class §5.3; HD=no; P=unresolved | Safe actor/challenge/factor/sign-in purpose/outcome only; no OTP/code/verifier; do not present as usable step-up freshness | `CHOICE-006`; response/authorization contradiction partial; #394/#775/#724; `G-AUTH`,`G-CONTRACT`,`G-CLIENT`,`G-PRIV`; sign-in create/verify/response/freshness-authorization tests |
| `AUTH-LC-CHL-020` | Template + `E-FACTOR`; current omitted-purpose/operation default source | Null-bound default step-up verifies and response advertises 300-second-default freshness; no operation can consume the result | Null request-context cannot match a normalized operation; default is unusable despite success, and create/complete contract permits the path | Challenge class §5.3; HD=no; P=unresolved | Safe actor/challenge/factor/default-purpose/null-binding/outcome only; no OTP/code/verifier; do not promise scoped success | `CHOICE-006`; unbound-default contradiction partial; #394/#724; `G-AUTH`,`G-CONTRACT`,`G-CLIENT`,`G-PRIV`; missing-operation creation/completion/authorization tests |
| `AUTH-LC-CHL-017` | Template + `E-FACTOR`; current passkey enrollment post-save handoff source | Challenge consumed with credential/audit before readout response; failure leaves active credential and retry conflict | Handoff/reconciliation, conditional completion, concurrency/uniqueness, audit rollback and policy unresolved | Challenge class §5.3; HD=no; P=unresolved | Safe challenge/outcome only; no private payload; do not claim failure rollback | `CHOICE-005/006/012`; handoff/retry/concurrency partial; #394/#776; `G-AUTH`,`G-RET`,`G-PRIV`; post-save fault/retry/reconciliation/atomic credential-challenge tests |
| `AUTH-LC-CHL-018` | Template + `E-FACTOR`; current authenticated passkey step-up post-save source/tested sequentially | Challenge/credential/audit commit precedes policy readout; failed response does not revoke scoped proof and retry conflicts; no session creation | No conditional completion; post-save fault/cancellation, 300-second default response versus hard-coded 900-second authorization window, pre-operation-filter ten-proof cap/eviction, operation binding, audit ordering, intervening policy, and non-discoverable credential usability partial | Challenge class §5.3; HD=no; P=unresolved | Safe actor/challenge/operation-purpose/outcome and bounded freshness only; no assertion/challenge/private payload; do not imply response failure rolled back proof or response deadline controls authorization | `CHOICE-005/006/009/012`; handoff/freshness/eviction/concurrency/binding/policy/usability-partial; #394/#724; `G-AUTH`,`G-RET`,`G-PRIV`; post-save fault/reconciliation/retry, ten-proof mixed-operation freshness, atomic completion, operation/policy/audit/non-discoverable tests |
| `AUTH-LC-RST-001` | Template + `E-RESET`; current source/tested sequentially | Replaced observed request excluded; newest sequential pending may proceed; concurrent new requests can both remain pending | New issue revokes the pending set it observes transactionally, but no unique active-status constraint, concurrency token, or conditional replacement prevents a two-link issuance race | Reset class §5.3; HD=no; P=unresolved | Replacement/requested events, no email/material/hash; generic delivery response | `CHOICE-007`; partial issuance concurrency; #339/#724; `G-AUTH`,`G-PROV`,`G-RET`,`G-PRIV`,`G-DEST`; atomic replacement/relational race/redaction tests |
| `AUTH-LC-RST-002` | Template + `E-RESET`; current provider-divergent completion source/tested sequentially | Fully committed request excluded; non-relational password replacement can commit while request stays pending/retryable and sessions remain | Relational outer transaction couples roots; non-relational credential workflow commits first, cascades/final audit-save are later failure boundaries; no conditional consume | Reset class §5.3; HD=no; P=unresolved | Per-root bounded outcome; no password/material; do not claim failure rollback or consumed state | `CHOICE-002/007/009/013`; provider/rollback/retry/concurrency partial; #339/#338/#724; `G-AUTH`,`G-PROV`,`G-RET`,`G-PRIV`; each failure boundary, repair/retry, race/redaction tests |
| `AUTH-LC-RST-003` | Template + `E-RESET`; current source/tested | Expired request excluded; later request distinct | Sequential repeated completion is generic; concurrent lazy-expiry outcome remains unproved | Reset class §5.3; HD=no; P=unresolved | Expiry/denial safe metadata, no identifier/material; generic failure | `CHOICE-007`; implemented sequentially; #339/#724; `G-RET`,`G-PRIV`,`G-DEST`; concurrent clock/redaction tests required |
| `AUTH-LC-RST-010` | Template + `E-RESET`; current repeated-expiry source, focused tests absent | Already-expired request remains unusable while last-checked/updated evidence changes; active pending-to-expired belongs only to `RST-003` | Ordinary tracked update and denial audit repeat; timestamp monotonicity, concurrent attempts, audit volume/correlation, and retention are unproved | Reset class §5.3; HD=no; P=unresolved | Safe request ID/prior-new expired status/check time/denial category only; no identifier/material/hash; generic failure | `CHOICE-007/009`; terminal-evidence mutation partial; #339/#724; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; repeated/concurrent completion, monotonic-time, audit-volume/correlation and redaction tests |
| `AUTH-LC-RST-004` | Template + `E-RESET`; current `email_link` terminal-replay source/tested | Scope-valid terminal reuse becomes suspicious replay; unknown/wrong-scope material is separate | Scope check precedes terminal classification; concurrent reuse/guess classification and rate limiting incomplete | Reset class §5.3; HD=no; P=unresolved | Replay category/request ID only when safely matched; no identifier/material/hash; generic failure | `CHOICE-007`; replay/concurrency/abuse-partial; #339/`REC-AUTH-ABUSE-001`; `G-AUTH`,`G-RET`,`G-PRIV`,`G-EXPOSE`; scope-valid/unknown/concurrent replay/redaction tests |
| `AUTH-LC-RST-011` | Template + `E-RESET`; current repeated-replay source, focused tests absent | Already-suspicious request remains unusable; first suspicious timestamp is retained while last-checked/updated evidence and replay audit repeat | Ordinary tracked update/audit repeat; timestamp monotonicity, concurrent attempts, audit volume/correlation, and rate limiting unproved | Reset class §5.3; HD=no; P=unresolved | Safe request ID/prior-new suspicious status/check time/replay category only; no identifier/material/hash; generic failure | `CHOICE-007/009`; repeated-replay evidence partial; #339/#724/`REC-AUTH-ABUSE-001`; `G-AUTH`,`G-RET`,`G-PRIV`,`G-EXPOSE`; repeated/concurrent replay, first-time preservation, audit-volume/correlation/redaction tests |
| `AUTH-LC-RST-012` | Template + `E-RESET`; current wrong-scope source, focused tests absent | Any matched non-`email_link` row retains prior status while check/update evidence changes; replay classification is bypassed | Scope check precedes status; ordinary save and denial audit repeat; concurrency/audit volume/future typed-code ownership unproved | Reset class §5.3; HD=no; P=unresolved | Safe request ID/scope category/unchanged status/check time only; no material/hash/identifier; generic failure | `CHOICE-007/009`; scope-denial ordering partial; #339/#724; `G-AUTH`,`G-RET`,`G-PRIV`,`G-EXPOSE`; every source status, repeated/concurrent checks, audit/redaction tests |
| `AUTH-LC-RST-005` | Template + `E-RESET`; current source/tested successful-completion path | Revoked pending requests excluded; new issuance per policy | Successful completion performs an observed-set revoke in the current transaction; denial does not revoke, and trigger/race completeness is unresolved | Reset class §5.3; HD=no; P=unresolved | Revoke reason/count safe metadata, no material; no distinct public disclosure | `CHOICE-007`; implemented bounded success path; #339/#724; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; trigger/dependency/race tests |
| `AUTH-LC-RST-006` | Template + `E-RESET`; current pre-gate audit-attempt source | Generic result grants no authority; every valid request resolves identifier and attempts requested-audit persistence before abuse/readiness/provider gates, but save failure escapes before those gates | Throttling/default-off do not protect lookup/write attempt; failure leaves no durable event or throttle attempt and may bypass generic response; audit-flood, ordering/timing and issuance races unresolved | Reset class §5.3; HD=no; P=unresolved | Requested safe category/correlation only when committed; never identifier/account existence/material/provider payload; require generic failure shaping | `CHOICE-007/009/012`; admission/failure/audit partial; #339/#724/`REC-AUTH-ABUSE-001`; `G-AUTH`,`G-PROV`,`G-EXPOSE`,`G-RET`,`G-PRIV`; provider-save failure/result/retry/admission, audit-flood/order/enumeration/readiness/race tests |
| `AUTH-LC-RST-007` | Template + `E-RESET`; current first-issuance source/tested sequentially | New pending request eligible; cleanup marker remains null; later terminal roots distinct | No unique active-status guard; concurrent issuance, provider/audit failure and raw-material lifetime unresolved | Reset class §5.3; HD=no; P=unresolved | Safe request/account/credential IDs, purpose/scope/times only; never material/hash; do not claim disposal clock | `CHOICE-007/012`; issuance/concurrency/provider/material-partial; #339/#724; `G-AUTH`,`G-PROV`,`G-RET`,`G-PRIV`,`G-EXPOSE`; first-race, failure material, audit/delivery/no-redisplay tests |
| `AUTH-LC-RST-008` | Template + `E-RESET`; current dependency-denial source/tested | Pending request remains eligible if account/identity/credential/policy recovers before expiry; no authority on denied attempt | Only check/update times and audit change; retry/dependency race and denial-disposition policy unresolved | Reset class §5.3; HD=no; P=unresolved | Safe denial dependency category/request/correlation only; no identifier/material/hash; generic failure | `CHOICE-002/007`; nonterminal dependency-policy-partial; #339/#724; `G-AUTH`,`G-RET`,`G-PRIV`,`G-EXPOSE`; dependency recovery/race/audit/redaction tests |
| `AUTH-LC-RST-009` | Template + `E-RESET`; current replacement-issuance source/tested sequentially | New pending replacement eligible; observed predecessors separately replaced; concurrent new links can coexist | Shares workflow with observed-set replacement; no unique active guard; provider/audit failure, atomic linkage, and failed-path raw-material lifetime unresolved | Reset class §5.3; HD=no; P=unresolved | Safe new/predecessor request IDs, purpose/times/hash version only; raw material immediate provider boundary; no exposed hash | `CHOICE-007/012`; linkage/concurrency/provider/material-partial; #339/#724; `G-AUTH`,`G-PROV`,`G-RET`,`G-PRIV`,`G-EXPOSE`; atomic replacement race, failure material, audit/delivery/no-redisplay tests |
| `AUTH-LC-INV-001` | Template + `E-INVITE`; current source/tested in-memory | Accepted invitation excluded; created roots independently readable/usable; disabled-plus-grace readout does not permit actual acceptance | Serializable relational branch exists, but focused suites use in-memory; constraints/rollback/concurrency/grace partial; audit misattributes inviter as actor instead of bearer-material acceptance context | Invite class §5.3; HD=no for action; P=unresolved | Accept/denial safe IDs/categories, no contact/material; inviter only contextual metadata, not accepting actor; generic public result | `CHOICE-008`; partial relational/grace/audit attribution; #784; `G-AUTH`,`G-PROV`,`G-EXPOSE`,`G-RET`,`G-PRIV`,`G-DEST`; relational grace/single-use/rollback and actor-audit tests |
| `AUTH-LC-INV-002` | Template + `E-INVITE`; current source/tested | Same pending row remains; old material excluded only on ready requested-delivery path | Non-idempotent: every ready retry generates/commits another hash before send, invalidating the prior generated link; timeout/send failure leaves delivery ambiguity | Invite class §5.3; HD=no; P=unresolved | Resend/delivery category, no contact/material/hash; show exact known result and never promise which timed-out link arrived | `CHOICE-008`; partial retry semantics; #784; same gates as INV-001; idempotency/delivery-ambiguity/race tests |
| `AUTH-LC-INV-003` | Template + `E-INVITE`; current source/tested status path | Revoked invitation excluded; no re-entry | Sequential status/authorization bounded; supplied reason is discarded by service/persistence/audit; concurrent revoke/accept unproved | Invite class §5.3; HD=no for revoke; P=unresolved | Current actor/subject/time only; required bounded reason is absent, no contact/material; refresh admin list without claiming reason evidence | `CHOICE-008`; partial reason/concurrency; #784/#724; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; reason persistence/audit/redaction and concurrent authz/race tests |
| `AUTH-LC-INV-004` | Template + `E-INVITE`; current authenticated management/read-triggered selected-batch source/tested | Up to 50 selected rows persist expired and are excluded; over-batch rows use `INV-013/014`; no re-entry | Cleanup precedes authenticated list/get/create/revoke/resend; audit actor/correlation are null because caller is not passed; batch/trigger/failure ordering unresolved | Invite class §5.3; HD=no for expiry; P=unresolved | Expiry/denial safe metadata; required known management actor/correlation absent; no material | `CHOICE-008/009`; implemented/attribution-authority partial; #784/#724; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; per-operation actor/correlation, clock/batch/failure-order tests |
| `AUTH-LC-INV-011` | Template + `E-INVITE`; current anonymous acceptance-triggered source/tested | Expired invitation excluded; no re-entry; acceptance remains generic | Cleanup precedes abuse/policy/material validation, so unproved anonymous callers can mutate eligible rows; hostile-input isolation/failure order unresolved | Invite class §5.3; HD=no for expiry; P=unresolved | Expiry/denial safe metadata only; never submitted material/contact; generic public failure | `CHOICE-008`; implemented/anonymous-authority partial; #784/#724; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`,`G-EXPOSE`; invalid-secret isolation, clock/batch/failure-order tests |
| `AUTH-LC-INV-012` | Template + `E-INVITE`; current target-specific anonymous expiry source | Matched target becomes expired after secret lookup; no re-entry; generic result | Global cleanup is capped at 50; target expiry save precedes separate denial-audit save, so proof/failure boundaries differ | Invite class §5.3; HD=no; P=unresolved | Safe invitation/prior-new expiry outcome after proof; never material/hash/contact; generic failure | `CHOICE-008/009`; implemented/two-save partial; #784/#724; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`,`G-EXPOSE`; >50 batch, target selection, save/audit failure, retry/concurrency tests |
| `AUTH-LC-INV-013` | Template + `E-INVITE`; current authenticated over-batch list/get projection source | API presents expired while persisted row remains pending/not cleanup-eligible; pending filter may return projected-expired item | No status/expiry/cleanup mutation or target audit; repeated projection and selection depend on capped cleanup order | Invite class §5.3; HD=no; P=unresolved | Response may show bounded expired status/time; evidence must say projection, not committed transition; known actor/correlation currently absent | `CHOICE-008/009`; status-truth/reconciliation partial; #784/#724; `G-AUTH`,`G-CONTRACT`,`G-RET`,`G-PRIV`,`G-DEST`; >50 list/get/filter, actor/correlation, persistence and retention tests |
| `AUTH-LC-INV-014` | Template + `E-INVITE`; current authenticated over-batch revoke/resend denial source | Clock-denied target remains pending/not cleanup-eligible; requested revoke/delivery does not proceed | No target expiry save/audit; repeated denial, response/status truth, selection and later cleanup unresolved | Invite class §5.3; HD=no; P=unresolved | Safe denial/invitation/clock category only; required actor/correlation and persisted expiry evidence absent; no material | `CHOICE-008/009`; denial/status-truth partial; #784/#724; `G-AUTH`,`G-CONTRACT`,`G-RET`,`G-PRIV`,`G-DEST`; >50 revoke/resend, actor/correlation, retry/persistence/retention tests |
| `AUTH-LC-INV-015` | Template + `E-INVITE`; current authenticated over-batch create-duplicate denial source | Expired row stays pending/not cleanup-eligible and blocks replacement invitation; no material/delivery/account authority created | Duplicate query ignores expiry; no target expiry save/audit; retry, reconciliation, actor evidence and create race unresolved | Invite class §5.3; HD=no; P=unresolved | Safe duplicate/clock category only; required actor/correlation and persisted expiry evidence absent; no contact/material/hash/provider payload | `CHOICE-008/009`; denial/status-truth partial; #784/#724; `G-AUTH`,`G-CONTRACT`,`G-RET`,`G-PRIV`,`G-DEST`; >50 create, actor/audit, retry/reconciliation/concurrency/retention tests |
| `AUTH-LC-INV-005` | Template + `E-INVITE`; current authenticated-trigger destructive source/tested | Eligible terminal rows become unreadable; accounts remain; authenticated management/read invokes cleanup before primary work | Batch 50/order current; retries process survivors; authenticated trigger authority, read/write failure ordering, holds/copies and concurrency unproved | Current HD=yes after 90-day marker; policy HD/P=unresolved, not authorized here | Aggregate counts/categories only; row evidence unresolved; destructive side effect lacks ordinary UI warning/confirmation | `CHOICE-008`; implemented fact/policy-blocked; #784/#724; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; management/read trigger, failure-order, holds/copies/idempotency/audit tests |
| `AUTH-LC-INV-006` | Template + `E-INVITE`; current source/tested | Pending row and existing material remain eligible; later ready resend/accept/revoke allowed | Repeated non-ready resend updates state/audit but does not rotate; concurrency with accept/revoke needs terminal-state proof | Invite class §5.3; HD=no; P=unresolved | Non-sent delivery category, no contact/material/hash; explicitly do not claim replacement | `CHOICE-008`; implemented; #784; `G-AUTH`,`G-PROV`,`G-RET`,`G-PRIV`; no-rotation/provider/race tests |
| `AUTH-LC-INV-007` | Template + `E-INVITE`; current source/tested sequentially | Pending invitation owner-readable; accept/resend/revoke/expire distinct | Capability/actor/contact/role/abuse checks and pending-recipient constraint exist; concurrent create and delivery-after-commit outcome partial | Invite class §5.3; HD=no; P=unresolved | Creation/delivery safe ID/categories, no contact/material/hash/provider payload; show exact sent/not-sent result | `CHOICE-008/012`; partial relational/provider; #784; `G-AUTH`,`G-PROV`,`G-RET`,`G-PRIV`,`G-EXPOSE`; uniqueness/race/delivery/no-secret tests |
| `AUTH-LC-INV-008` | Template + `E-INVITE`; current non-relational acceptance compensation source | Exact accepted row restored to pending with original material/contact retained; retry possible; created roots separately compensated | Ordinary tracked restoration can itself fail; concurrency, repeated compensation, accepted-to-pending audit and safe retry unresolved | Invite class §5.3; HD=no; P=unresolved | Restoration outcome/reason/correlation needed; never contact/material/hash; generic failure without promising rollback | `CHOICE-008/009`; implemented restoration/partial failure proof; #784/#724; `G-AUTH`,`G-RET`,`G-PRIV`; restoration-failure/race/retry/audit tests |
| `AUTH-LC-INV-009` | Template + `E-INVITE`; current non-relational first-save compensation source | Still-pending row remains pending with existing material/contact; terminal timestamps cleared and update time advances; retry possible | Compensation target is assigned before first save; cleanup mutation can fail/repeat/race, and pending-to-pending audit is absent | Invite class §5.3; HD=no; P=unresolved | Compensation outcome/reason/correlation needed; never expose contact/material/hash; generic failure without claiming no mutation | `CHOICE-008/009`; implemented metadata mutation/partial failure proof; #784/#724; `G-AUTH`,`G-RET`,`G-PRIV`; first-save failure/restoration-failure/race/retry/audit tests |
| `AUTH-LC-INV-010` | Template + `E-INVITE`; current anonymous-trigger destructive source/tested | Eligible unrelated terminal rows become unreadable before submitted invitation proof; accepted accounts remain; no authority granted | Any well-formed accept request triggers batch cleanup before abuse/policy/secret validation; hostile-input isolation, failure order, holds/copies and concurrency unproved | Current HD=yes after 90-day marker; policy HD/P=unresolved, not authorized here | Aggregate counts/categories only; row evidence unresolved; generic public response, no cleanup disclosure or confirmation | `CHOICE-008`; implemented fact/policy-blocked; #784/#724; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`,`G-EXPOSE`; anonymous trigger, invalid-secret isolation, failure-order, holds/copies/idempotency/audit tests |
| `AUTH-LC-PRO-001` | Template + `E-IDENTITY`/`E-INVITE`; current invitation-compensation source | Exact failed invitation profile removed; unrelated data remain; invitation restoration separate | Exact profile-ID selection; invitation ordering, dependency, retry, partial compensation/provider equivalence unresolved | Failed-workflow HD=yes only; general profile/domain P=unresolved | Compensation root/count/outcome needed; no private payload; generic acceptance failure | `CHOICE-008`; implemented invitation compensation/partial dependency proof; #784/#724; `G-RET`,`G-PRIV`,`G-DEST`; ordering/dependency/exact-selection/retry/failure tests |
| `AUTH-LC-PRO-005` | Template + `E-IDENTITY`; current admin-compensation source | Exact failed admin-provisioning profile removed; unrelated data remain; no invitation transition | Exact profile-ID selection; admin actor context, dependency, retry, partial compensation/provider equivalence unresolved | Failed-workflow HD=yes only; general profile/domain P=unresolved | Compensation root/count/outcome needed; no private payload; bounded admin failure | `CHOICE-001`; implemented admin compensation/partial dependency proof; #785/#724; `G-AUTH`,`G-LOCK`,`G-RET`,`G-PRIV`,`G-DEST`; actor/dependency/exact-selection/retry/failure tests |
| `AUTH-LC-PRO-002` | Template + `E-IDENTITY`; current provider-divergent bootstrap source | Profile readable; non-relational credential failure can leave it on a stranded active account | Relational transaction can roll back; non-relational profile commits before credential workflow with no compensation | Profile/account classes §5.3; HD=no; P=unresolved | Safe profile/account IDs and truthful partial outcome; no private payload | `CHOICE-001`; provider-partial; #785/#724; `G-AUTH`,`G-LOCK`,`G-RET`,`G-PRIV`,`G-DEST`; relational rollback plus non-relational stranding/privacy tests |
| `AUTH-LC-PRO-003` | Template + `E-IDENTITY`/`E-INVITE`; current acceptance source/tested in-memory | Invited profile linked/readable after authorized account use; no auth authority itself | Relational branch/compensation exists; focused relational collision/rollback proof absent | Profile/account/invite classes §5.3; HD=no; P=unresolved | Safe profile/account/invitation IDs and outcome only; no contact/password/private payload | `CHOICE-001/008`; source-implemented/test-partial; #784/#724; `G-AUTH`,`G-PROV`,`G-RET`,`G-PRIV`,`G-DEST`; collision/rollback/compensation/privacy tests |
| `AUTH-LC-PRO-004` | Template + `E-IDENTITY`; current admin-local-user source/endpoint-tested | Created profile linked/admin-readable; no auth authority itself | Relational transaction/non-relational compensation exists; focused relational rollback/race proof absent | Profile/account classes §5.3; HD=no; P=unresolved | Safe profile/account/actor/outcome only; no identifier/password/private payload | `CHOICE-001`; source-implemented/test-partial; #785/#724; `G-AUTH`,`G-LOCK`,`G-RET`,`G-PRIV`,`G-DEST`; authz/collision/rollback/compensation/privacy tests |
| `AUTH-LC-ABU-001` | Template + `E-ABUSE`; current sign-in source/tested | Process-local source/identifier/combined/global buckets govern later admission; no re-entry beyond window/throttle/prune | Lock serializes one process; restart/multi-node lose/diverge state | Abuse class §5.3; HD=temporary cleanup only; P=n/a | Bounded bucket category/outcome/count/time only; no identifier/source raw value; generic throttle | `REC-AUTH-ABUSE-001`; implemented single-node; `G-AUTH`,`G-CONFIG`,`G-PRIV`,`G-EXPOSE`; failure/throttle/restart/multi-node/redaction tests |
| `AUTH-LC-ABU-002` | Template + `E-ABUSE`; current pre-final-audit sign-in source | Identifier/combined scopes clear after session creation but before final audit/credential response; source/global remain | Final-audit failure can strand session authority and clear throttle history; retry/multi-node behavior unresolved | Abuse class §5.3; HD=temporary cleanup only; P=n/a | Clear/outcome categories only; no identifier/source/raw credential; no success claim on later failure | `CHOICE-009/010/012`/`REC-AUTH-ABUSE-001`; boundary partial; #338/#724; `G-AUTH`,`G-RET`,`G-PRIV`; final-audit failure/stranded-session/selective-clear tests |
| `AUTH-LC-ABU-003` | Template + `E-ABUSE`; current sign-in clock/capacity source/tested | Expired/throttle-elapsed/empty or oldest-capacity state stops governing; restart clears all | Prune occurs on access; capacity eviction can discard still-relevant oldest bucket; multi-node unresolved | Source default 30m/capacity; positively transient HD=yes; P=n/a | Aggregate prune/eviction category/count only; never bucket key; no ordinary UI | `REC-AUTH-ABUSE-001`; current transient cleanup; `G-CONFIG`,`G-PRIV`; boundary/capacity/restart/multi-node tests |
| `AUTH-LC-ABU-004` | Template + `E-ABUSE`; current reset request source/tested | Process-local request buckets govern later generic initiation; no explicit success clear | Lock serializes one process; restart/multi-node lose/diverge state | Abuse class §5.3; HD=temporary cleanup only; P=n/a | Bounded scope/outcome/count/time only; no identifier/source; generic response | `REC-AUTH-ABUSE-001`; implemented single-node; `G-AUTH`,`G-CONFIG`,`G-PRIV`,`G-EXPOSE`; threshold/restart/multi-node/redaction tests |
| `AUTH-LC-ABU-005` | Template + `E-ABUSE`; current reset provider-send source/tested | Distinct provider-send bucket governs later delivery; request admission state remains | Lock serializes one process; no explicit clear; provider ambiguity and multi-node unresolved | Abuse class §5.3; HD=temporary cleanup only; P=n/a | Provider-send category/count/time only; no identifier/source/provider payload; generic response | `REC-AUTH-ABUSE-001`; implemented single-node; `G-PROV`,`G-CONFIG`,`G-PRIV`; send-throttle/failure/restart tests |
| `AUTH-LC-ABU-006` | Template + `E-ABUSE`; current reset clock source/tested | Retention-expired/throttle-elapsed/empty state stops governing; restart clears all | Lazy prune on check/record; boundary and multi-node unresolved | Source default 1h; positively transient HD=yes; P=n/a | Aggregate prune category/count only; never bucket key; no UI | `REC-AUTH-ABUSE-001`; current transient cleanup; `G-CONFIG`,`G-PRIV`; clock/restart/multi-node tests |
| `AUTH-LC-ABU-007` | Template + `E-ABUSE`; current authenticated create/resend source/tested | Management-operation buckets govern later admission; successful management is counted | Owner/admin authority and operation keys differ from accept; one-process lock only; outcome/restart/multi-node unresolved | Abuse class §5.3; HD=temporary cleanup only; P=n/a | Bounded operation/scope/outcome/count/time; no raw actor/subject/source | `CHOICE-008`/`REC-AUTH-ABUSE-001`; authenticated single-node; `G-AUTH`,`G-CONFIG`,`G-PRIV`; create/resend outcome/threshold/restart tests |
| `AUTH-LC-ABU-010` | Template + `E-ABUSE`; current anonymous acceptance source/tested | Accept-operation source/subject/combined/global buckets govern later admission; unsuccessful outcomes count, success clears under ABU-008 | Anonymous authority and success behavior differ from management; one-process lock only; source derivation/outcome/restart/multi-node unresolved | Abuse class §5.3; HD=temporary cleanup only; P=n/a | Bounded accept/scope/outcome/count/time; no raw material/subject/source; generic throttle | `CHOICE-008`/`REC-AUTH-ABUSE-001`; anonymous single-node; `G-AUTH`,`G-CONFIG`,`G-PRIV`,`G-EXPOSE`; failed/throttled/policy/success distinction tests |
| `AUTH-LC-ABU-008` | Template + `E-ABUSE`; current successful invitation-accept source/tested | Subject/actor-subject accept scopes clear; source/actor/global remain | Repeat clear idempotent; concurrent process/multi-node behavior unresolved | Abuse class §5.3; HD=temporary cleanup only; P=n/a | Clear categories/counts only; no bucket keys; no UI | `CHOICE-008`/`REC-AUTH-ABUSE-001`; implemented single-node; same gates; selective-clear/remaining-scope tests |
| `AUTH-LC-ABU-009` | Template + `E-ABUSE`; current invitation clock source/tested | Retention-expired/throttle-elapsed/empty state stops governing; restart clears all | Lazy prune on check/record; boundary and multi-node unresolved | Source default 1h; positively transient HD=yes; P=n/a | Aggregate prune category/count only; never bucket key; no UI | `CHOICE-008`/`REC-AUTH-ABUSE-001`; current transient cleanup; `G-CONFIG`,`G-PRIV`; clock/restart/multi-node tests |
| `AUTH-LC-POL-001` | Template + `E-FACTOR`; draft schema/read only | Draft has no eligibility effect; later activate or discard distinct; re-entry/version editing unresolved | Create/edit retry, version conflict, concurrency and draft uniqueness unresolved; block mutation | Policy class §5.3; HD=no; P=unresolved | Required draft/version/actor/reason/outcome, no secret config; blocked UI | `CHOICE-011`; unimplemented; #394/#465/#785; `G-AUTH`,`G-LOCK`,`G-SCHEMA`,`G-CONTRACT`,`G-CLIENT`,`G-RET`,`G-PRIV`; draft/version/concurrency tests |
| `AUTH-LC-POL-003` | Template + `E-FACTOR`; active/retired schema, no mutation runtime | Activated version governs later eligibility; predecessor retirement and existing authority effects unresolved | Atomic single-active replacement, effective-time, retry/version conflict and owner-lockout concurrency unresolved | Policy class §5.3; HD=no; P=unresolved | Required prior/new version/status/effective time/actor/reason; blocked UI | `CHOICE-011`; unimplemented; same owners/gates; activation/replacement/weakening/last-owner/session-factor tests |
| `AUTH-LC-POL-004` | Template + `E-FACTOR`; retired schema, no mutation runtime | Retired version excluded after safe successor/default; no credential/factor/session re-entry | Successor selection, atomic retirement, retry/version conflict and concurrency unresolved | Policy class §5.3; HD=no; P=unresolved | Required prior/new status/successor/version/actor/reason; blocked UI | `CHOICE-011`; unimplemented; same owners/gates; retirement/fallback/no-restore tests |
| `AUTH-LC-POL-002` | Template + `E-INVITE`; existing-policy replacement source-broken/provider-divergent | Existing-policy change is not successful replacement; disabled-plus-grace readout and acceptance disagree | Untracked predecessor is not retired; relational unique-active index rejects insertion while in-memory may retain multiple active rows | Policy class §5.3; HD=no; P=unresolved | Change attempt actor/version/outcome only; do not report success; no unsafe config | `CHOICE-008/011`; incomplete; #784/#785; `G-AUTH`,`G-LOCK`,`G-RET`,`G-PRIV`; tracked retirement, unique-active, retry/version/grace tests |
| `AUTH-LC-POL-005` | Template + `E-INVITE`; current initial-policy creation source | First active policy governs later invitation eligibility; no predecessor retirement or account/session mutation | Policy/audit share one save; concurrent first create/version selection, rollback and weakening/grace consistency unproved | Policy class §5.3; HD=no; P=unresolved | Safe actor/version/prior-new mode/grace/outcome only; no unsafe config | `CHOICE-008/011`; implemented/first-create partial; #784/#785; `G-AUTH`,`G-LOCK`,`G-RET`,`G-PRIV`; authz/concurrent first-create/version/audit rollback/grace tests |
| `AUTH-LC-AUD-001` | Template + `E-AUDIT`; append writers/partial tests | Security-authorized read future; correction additive; bootstrap audit can remain after non-relational credential failure | Writer retry/idempotency and complete inventory unresolved; bootstrap rollback is relational-only and other compensation exceptions are separate | Audit class §5.3; HD=no except AUD-002/003; P=unresolved | Exact bounded audit contract; truthful partial outcome; no secret/private payload or removal UI | `CHOICE-001/009`; partial; #724/#774/#973/#785; `G-AUTH`,`G-CONTRACT`,`G-CLIENT`,`G-RET`,`G-PRIV`,`G-DEST`; bootstrap stranding, authz/pagination/hold/export/disposal tests |
| `AUTH-LC-AUD-002` | Template + `E-AUDIT`/`E-IDENTITY`/`E-INVITE`; current invitation-compensation source | Selected invited-account audit rows become unreadable; invitation restoration separate | Account rather than correlation selection can delete concurrent evidence; invitation ordering, retry/failure partial | Failed-workflow cleanup HD=yes only; general P=unresolved | Outcome/count/correlation needed; never deleted payload; no UI | `CHOICE-008/009`; invitation evidence-loss gap; #784/#724; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; ordering/broad-selection/concurrent-event/retry/failure tests |
| `AUTH-LC-AUD-003` | Template + `E-AUDIT`/`E-IDENTITY`; current admin-compensation source | Selected admin-created-account audit rows become unreadable; no invitation transition | Account rather than correlation selection can delete concurrent evidence; actor context, retry/failure partial | Failed-workflow cleanup HD=yes only; general P=unresolved | Outcome/count/correlation needed; never deleted payload; no UI | `CHOICE-001/009`; admin evidence-loss gap; #785/#724; `G-AUTH`,`G-LOCK`,`G-RET`,`G-PRIV`,`G-DEST`; actor/broad-selection/concurrent-event/retry/failure tests |

### 5.3 Retention, Audit, Privacy, Follow-Up, And Acceptance

`Operationally required, duration unresolved` means current security behavior
needs some bounded evidence, but no reviewed duration exists. `Source-defined
validity` describes usability only; it is not a retention or disposal period.

| Row ID(s) | Retention classification and trigger | Retained safe evidence / privacy boundary | Current audit evidence and gaps | Open choice, follow-up lane, manual gate, runtime acceptance |
| --- | --- | --- | --- | --- |
| `AUTH-LC-SES-001`–`AUTH-LC-SES-015` | Source-defined session validity; raw credentials are transient immediate-handoff material, not lifecycle evidence; post-expiry/revocation metadata retention is operationally required but duration unresolved. | Session/account/family IDs, status, issued/expires/last-seen/revoked time, bounded reason/device label; never persist/audit raw access/refresh material or expose token hashes, full IP, or unbounded agent. Local sign-in holds raw values in process between creation and final-audit completion; failed-path disposal/zeroization is unproved. | Local, refresh-authorized, and passkey access-only issuance plus validation/use and trigger-specific self/selected/all/password-change/reset/replay/expiry/account-unavailable revocation roots exist. Provider rollback, local final-audit handoff, old-proof issuance, validation ordering, family validation, passkey handoff, and selection/rotation races are partial or absent. | `AUTH-LC-CHOICE-001/002/005/007/009/010/012/013`; #338/#339/#394/#724/#773/#774/#788/#1059 and `REC-AUTH-PASSKEY-SESSION-001`; auth/security/privacy/destructive gates. Accept each issuance/revocation authority and root, provider failure/repair, material lifetime, old-password serialization, handoff/MFA, expiry/family isolation, validation ordering, rotation races, redaction, and retained-copy disposition tests. |
| `AUTH-LC-REF-001`–`AUTH-LC-REF-023` | Source-defined idle/absolute validity; active initial/replacement hashes are operational credential state, while rotated/replayed/revoked hash/lineage retention is operationally required for replay and revocation evidence; durations and orphan handling are unresolved. | Family/credential/session IDs, status, lineage, issued/idle/absolute/consumed/revoked/last-rotated times, bounded reason; no raw refresh value or exposed hash. Local sign-in and refresh rotation both create raw strings before their final success boundary; failed async task/state-machine reference lifetime and disposal/zeroization are unproved, and material must never be retained as evidence. | Initial/replacement issuance, predecessor and family rotation mutation, active-family replay, terminal-family replay rewrite, active-family expiry, terminal-family expiry rewrite, credential replay/expiry/account-unavailable roots, self/selected/account sign-out, password-change, and password-reset roots exist. Credential classification precedes family classification; active credentials in revoked/replayed families cause failure audit without family mutation. The relational two-rotation loser can throw before replay cascade/audits; discovery/exclusion/rotation races can escape revocation cascades; provider boundaries differ; local final-audit failure can strand active authority; family audits lack exact subject correlation. | `AUTH-LC-CHOICE-001/002/007/009/010/012/013`; #338/#339/#788/#724/#773/#774/#1059. Accept refresh failure-path material lifetime, family last-rotation monotonicity and per-root audit, every credential/family source combination, each terminal rewrite's evidence integrity/idempotency, nested-transaction replay, each trigger/root's proof, exclusion, atomic rollback, discovery/rotation races, lineage, provider cascade failure, account isolation, blast radius, and no-secret evidence. |
| `AUTH-LC-PWD-001`–`AUTH-LC-PWD-012` | Active verifier is operational credential material. Creation/rehash persist a one-way verifier, never plaintext; superseded verifier is overwritten. Disabled/revoked retention, re-entry, and erasure are unresolved. | Algorithm/version/parameters, status and timestamps only where needed; never password, verifier, salt/pepper detail, submitted values, recovery proof, or previous verifier in audit. | Bootstrap credential creation follows the pre-root save and only relational source can roll both back; invitation/admin compensation differ. Credential-writer events omit authenticated actor; disable/re-enable/revoke runtime is absent. | `AUTH-LC-CHOICE-001/002/009/010`; #339/#338/#788/#784/#785/#724. Accept provider-specific creation failure/stranding, actor attribution, collision/redaction, verification/replacement/rehash ordering, recovery/session effects and secret-erasure tests. |
| `AUTH-LC-ACC-001`–`AUTH-LC-ACC-008` | Account evidence is operationally required; duration/disposal unresolved. Deletion marker is not purge authority. | Account/profile link, bounded status/time/actor/reason; keep bootstrap, invitation, and admin authorities distinct. | Non-relational bootstrap can strand an active no-password account; invitation/admin compensation are separate. Disable/re-enable/delete runtime is absent. | `AUTH-LC-CHOICE-001/008`; #788/#785/#784/#724. Accept exact-one bootstrap with provider failure recovery, per-authority collision/rollback, last-owner safety, cascades, dependencies/holds/copies and restore denial. |
| `AUTH-LC-ID-001`–`AUTH-LC-ID-009` | Active link/tombstone state is operationally required; duration unresolved. | Provider type/name, stable bounded subject/reference, lifecycle metadata; no provider payload/raw contact in audit. | Bootstrap local link can remain after non-relational credential failure; invitation/admin compensation differ. Provider link/unlink/re-link runtime is absent. | `AUTH-LC-CHOICE-001/003/008`; #787/#773/#785/#784. Accept provider-specific bootstrap failure, per-authority uniqueness/rollback, takeover, tombstone re-entry, last-method/session impact and redaction. |
| `AUTH-LC-ROL-001`–`AUTH-LC-ROL-008` | Role history is operationally required; assignment row lacks removal history. | Account ID, exact role, actor/time/reason; no client-supplied authority. | Bootstrap roles can remain on a non-relational stranded account; invitation/admin compensation differ; general mutation runtime is absent. | `AUTH-LC-CHOICE-001/004/008`; #785/#464/#465/#784. Accept provider-specific bootstrap failure, per-authority rollback/authz, duplicate/elevation, stale-request, self-demotion and concurrent last-owner tests. |
| `AUTH-LC-PKY-001`–`AUTH-LC-PKY-008` | Enrolled public-key metadata is operational credential state; terminal duration unresolved. Assertion use updates counters/use times, and rename mutates labels on terminal rows. | Server credential ID/hash, public key only while justified, counter/backup/transport, bounded label/status/times/reason; no private material/raw assertion/full attestation. | Enrollment and step-up can commit credential/challenge/audit before policy-readout response fails; retry conflicts. Step-up response freshness defaults to 300 seconds while authorization uses 900 seconds and a pre-operation-filter ten-proof cap. Sign-in can commit credential/challenge/session before audit failure. Completion misses policy revalidation, broad-state mutations exist, binding/options and monotonic-counter behavior are incomplete; disable/re-enable/cleanup absent. | `AUTH-LC-CHOICE-005/006/012`; #394/#776/#724 plus `REC-AUTH-PASSKEY-SESSION-001`. Accept post-save handoff/reconciliation, time/operation/eviction freshness parity, uniqueness/concurrency/policy, use/binding/options, audit rollback, source-state/evidence integrity, session decision, counter/replay, and disposition tests. |
| `AUTH-LC-TOTP-001`–`AUTH-LC-TOTP-013` | Pending setup validity is source-defined. Initial/replacement setup and enrollment completion can commit before post-save policy-readout failure; raw secret/manual-key lifetime and disposal are unproved. Each TOTP verification decrypts the enrolled seed into a mutable byte array whose disposal/zeroization is unproved. An unprotect exception exits before enrollment or challenge failure persistence/audit, leaving pending state. Expiry attempts ignore a failed `TrySaveAsync` result, so conflict does not prove persisted expiry. Protected enrolled secret is operational material; terminal secret retention for history is forbidden. | Factor ID/type/status/times/policy/reason and bounded label; never decrypted or protected secret, provisioning URI/key, or submitted OTP in evidence. | Enrollment-start audit exists, but predecessor correlation and unavailable-account denial audit are absent. Non-relational replacement rollback, expiry-save truth, enrollment handoff, enrollment/use decrypt/key-rotation failure and healthy-sibling behavior, active-use seed lifetime, broad source states, use races, disable/re-enable, erasure and cleanup remain gaps. | `AUTH-LC-CHOICE-005/006/009/012`; #394/#465/#776/#724. Accept provider failure/result truth, post-save failure/retry/stranded recovery, enrollment/use unprotect failure with bounded result/attempt/audit/retry/replacement/sibling behavior, per-root audit, setup and active-use material disposal/no-redisplay, races, terminal guards/erasure, last-factor/recovery, sessions and redaction. |
| `AUTH-LC-RCV-001`–`AUTH-LC-RCV-014` | Active salted verifiers are operational credential material. Batch and verifier roots plus first and replacement display-once issuance are distinct. Consumed/replaced/revoked metadata retention is required for security review, but verifier retention duration is unresolved and never justified solely by hashing. | Batch/code IDs, counts, status and times, safe challenge/correlation linkage; never raw code, submitted code, verifier/hash/salt in read/audit. Raw generated codes exist only for immediate display; their lifetime/disposal across failed saves and post-save policy-readout failure is unproved. | Post-save failure can strand authority without handing codes to the caller. An initial retry without replacement conflicts; a replacement retry replaces the stranded set and issues another set, never redisplaying the first. Audit identifies batch, not individual verifiers/counts. Consumption is sequential-only, aggregates can lose updates, broad revoke rewrites states, rollback is unproved, and expiry runtime absent. | `AUTH-LC-CHOICE-005/006/009/012`; #775/#776/#724 and `REC-AUTH-MFA-SIGNIN-001`. Accept per-root rollback/audit, failure injection, deterministic retry outcomes and recovery UX, display/no-redisplay/material lifetime, concurrent generation/consumption, exact aggregates, source-state/idempotency, expiry and verifier disposition. |
| `AUTH-LC-CHL-001`–`AUTH-LC-CHL-020` | Source-defined short validity; issuance, authority/purpose success/failure, and terminal retention are distinct. | IDs, purpose/factor/status, bounded context/attempts/times/reason only; no raw reusable challenge/authenticator payload. | Enrollment/MFA/step-up option issuance can commit before readout/option handoff fails, stranding live challenges and allowing duplicates on retry. Passkey step-up completion can likewise commit before readout response failure. MFA/passkey responses default to 300-second freshness while authorization uses 900 seconds plus a ten-proof cap; MFA `sign_in` freshness and null-bound default step-up authorize nothing. Already-at-max blocking is unsaved; threshold save failure is ignored. Issuance audit, binding, policy revalidation, atomicity and actor coverage differ by flow. | `AUTH-LC-CHOICE-005/006/009/012`; #394/#724/#775/#776 and existing recommendation owners. Accept each post-save issuance/completion handoff/retry/cleanup, purpose/binding/time/operation/eviction freshness truth, both block-save boundaries, per-purpose authority/audit, binding, duplicates, policy, atomic attempts/completion, replay and retention. |
| `AUTH-LC-RST-001`–`AUTH-LC-RST-012` | Link validity is source-defined; issuance, replacement, expiry, terminal updates, scope denial, replay, and dependency denial are distinct. `CleanupEligibleAtUtc` is never assigned; disposition unresolved. | Safe IDs, purpose/scope/status/hash version, lifecycle/check times and bounded categories; no material, exposed hash, identifier/email, provider payload, or password. | Request initiation resolves the identifier and attempts its audit save before abuse/readiness gates; save failure can bypass those gates and the generic response without recording a throttle attempt. Wrong-scope precedes replay classification; terminal checks mutate evidence; concurrent issuance can create links; dependency denial stays pending. Relational completion couples roots, but non-relational password replacement can commit before request/session/audit failure, with materially different retry state. No cleanup exists. | `AUTH-LC-CHOICE-002/007/009/012/013`; #339/#338/#724 plus `REC-AUTH-ABUSE-001`. Accept pre-gate provider-save failure/response/retry/admission, issuance/material lifetime, every scope/status and completion failure boundary, repair/retry, atomic consume/replace/cascades, dependency recovery, replay/expiry, audit and redaction. |
| `AUTH-LC-INV-001`–`AUTH-LC-INV-004`, `AUTH-LC-INV-006`–`AUTH-LC-INV-009`, `AUTH-LC-INV-011`–`AUTH-LC-INV-015` | Seven-day validity and 90-day cleanup eligibility are source values, not approved retention policy. Authenticated selected-batch expiry, over-batch list/get projection, revoke/resend denial and create-duplicate denial, anonymous pre-proof batch expiry, and target-specific post-lookup expiry are distinct. Full normalized email and lookup verifier remain after terminal transitions until cleanup; terminal duration/minimization/holds/disposition are unresolved. | Invitation ID/status, masked display, target role, true actor/context, account IDs, lifecycle times and safe categories; never expose full contact/hash or retain raw link/body/provider payload. | Cleanup is capped at 50; authenticated cleanup discards known actor/correlation, over-batch rows can remain pending while projected expired, terminally denied, or blocking replacement creation, and anonymous target expiry uses a separate save before denial audit. Acceptance actor is wrong, revoke reason discarded, compensation events absent, and terminal minimization absent. | `AUTH-LC-CHOICE-008/009`; #784/#724/#973. Accept each expiry authority/proof/batch/projection/denial/failure boundary, actor/correlation, status truth/reconciliation, pending versus terminal needs, delivery/grace/rotation/single-use, rollback, concurrency, minimization and evidence. |
| `AUTH-LC-INV-005` | Current runtime hard-deletes terminal rows after the 90-day eligibility timestamp when authenticated list/get/create/revoke/resend operations invoke cleanup before primary work. Policy classification and each management/read trigger's authority are unresolved/manual; retention expiry is insufficient authority. | Current aggregate audit keeps workflow, counts/status categories and timing bucket, but no row IDs; sufficiency unresolved. | `invitation.cleanup_completed` exists; no per-row disposal event or hold/copy proof. | `AUTH-LC-CHOICE-008`; #784/#724. Accept authenticated trigger authorization, read/write failure order, holds/copies, warning/confirmation, batch retry/idempotency and audit sufficiency. |
| `AUTH-LC-INV-010` | The same current hard-delete can be invoked by any well-formed anonymous acceptance request before abuse/policy/secret validation. This public trigger is a separate unresolved destructive/exposure authority; retention expiry is insufficient authority. | Aggregate audit only; no accepting actor proof, row IDs, or per-row disposal evidence. Never copy submitted material/contact into cleanup evidence. | Invalid-secret callers can trigger cleanup; hostile-input isolation, failure order, holds/copies and rollback proof are absent. | `AUTH-LC-CHOICE-008`; #784/#724 with exposure/destructive/security/privacy gates. Accept anonymous trigger authorization, invalid-secret isolation, generic disclosure, holds/copies, batch retry/idempotency and audit sufficiency. |
| `AUTH-LC-ABU-001`–`AUTH-LC-ABU-010` | Process-memory admission state, not durable evidence: default retention is 30 minutes for sign-in and one hour for reset/invitation; pruning, eviction, and restart remove state. Authenticated invitation management and anonymous acceptance use distinct operation/authority roots. | Only bounded non-reversible categories, operation/scope/outcome/count/timing; never raw identifier/contact/source address, material, provider payload, or bucket key. | Single-process transitions exist. Sign-in success clears some history before final audit/credential response; invitation management successes count while acceptance success clears selected scopes. Distributed consistency/source derivation/privacy/audit remain unresolved. | `REC-AUTH-ABUSE-001` and #338/#339/#784/#724. Accept authority/operation separation, final-boundary failures, thresholds, selective clears, clock/eviction/restart/multi-node behavior, redaction, and no durable-secret tests. |
| `AUTH-LC-POL-001`–`AUTH-LC-POL-005` | Draft has no authority; active version is authoritative; retired policy evidence is operationally required, duration unresolved. Initial invitation-policy creation is separate from predecessor replacement. | Policy ID/version, bounded modes/counts/status/effective/retired times, actor/reason/correlation; no keys/secrets or unsafe payload. | Initial invitation policy/audit creation works; existing-policy replacement is source-broken/provider-divergent; general draft/activate/retire mutations lack runtime proof. | `AUTH-LC-CHOICE-008/011`; #394/#465/#784/#785. Accept first-create and replacement concurrency/rollback independently, lifecycle, safe fallback, uniqueness, weakening/last-owner gates and authority impact. |
| `AUTH-LC-AUD-001`–`AUTH-LC-AUD-003` | Policy-defined/configurable retention intent, but numeric duration, holds, read/export, retention clock and general disposition are unresolved. Audit retention is separate from credential-material retention. Invitation and admin non-relational compensation separately delete selected new-account audit rows and are not general purge authority. | Only minimum actor/subject/action/outcome/time/correlation/request and bounded reason/transition metadata. Never credential material, local identifiers/emails without approval, request/response bodies, provider/private payloads, or deleted event payload copied into compensation evidence. | Substantial writers/redaction tests exist; each authority's account-wide selection can remove concurrent evidence. Complete lifecycle inventory, authorized reads, export, general purge, and compensation-loss evidence do not. | `AUTH-LC-CHOICE-001/008/009`; #721/#724/#774/#973/#784/#785. Privacy/destructive/manual gate. Accept per-authority actor/selection/race/failure, append-only correction, pagination/authz, hold, export redaction, disposal accounting and no-secret scans. |
| `AUTH-LC-PWD-010/012`, `AUTH-LC-ACC-007/008`, `AUTH-LC-ID-006/009`, `AUTH-LC-ROL-006/008`, `AUTH-LC-INV-008/009`, `AUTH-LC-PRO-001/005`, `AUTH-LC-AUD-002/003` | Current non-relational invitation and admin compensation authorities are positively scoped failed-workflow cleanup, not retention expiry or general purge. Invitation compensation additionally retains/restores its invitation row; admin provisioning has no invitation transition. | Retain only bounded authority/actor/root/count/outcome/correlation evidence if separately authorized; never copy deleted verifier, identifier/contact, role-private, profile, invitation material/hash, audit payload, or other private payload. | No complete per-root compensation audit exists; partial failure can leave inconsistent subsets, and account-wide audit selection can remove concurrent evidence. | `AUTH-LC-CHOICE-001/002/003/004/008/009`; #784/#785/#724 plus root owners. Accept invitation and authenticated-admin authorities separately, exact-root selection, dependencies, ordering, retry/idempotency, concurrency, failure stages, provider equivalence, invitation restoration, and no-secret evidence. |
| `AUTH-LC-PRO-001`–`AUTH-LC-PRO-005` | Active account-linked profile is operational product/identity-adjacent state. Creation evidence and later profile/domain-data retention, unlinking and disposition are unresolved; account deletion is separate and no purge follows from account status. | Profile/account IDs, creation authority, bounded status/time/actor/outcome only; no submitted identifier/contact, password, provider payload, or unbounded profile fields in auth audit. | Bootstrap/invitation/admin workflows emit bundle-level creation audit; invitation/admin compensation are distinct roots, but not complete separately correlated profile lifecycle events. | `AUTH-LC-CHOICE-001/008/009`; #785/#784/#724. Privacy/destructive/account-boundary gates. Accept per-authority creation/compensation, authz/collision/rollback, exact correlation, profile/domain dependencies, account deletion separation, retention and redaction. |

### 5.4 Manual-Gate Registry

These gates are not required to merge this documentation-only policy. They are
separate pending gates for the exact downstream behavior referenced by rows in
section 5.2. No gate has approval evidence, and satisfying one never satisfies
another.

| Gate ID | Required / status | Gate and decision owner | Approval evidence | Downstream work blocked |
| --- | --- | --- | --- | --- |
| `G-AUTH` | Yes / pending | Auth/session/security behavior and policy; focused row owner plus human security review | None; pending | Any new or changed lifecycle acceptance, cascade, material response, replay response, or policy enforcement |
| `G-LOCK` | Yes / pending where referenced | Owner/admin and last-sign-in-method lockout safety; #785/#788/#394 | None; pending | Account, role, identity, password, passkey, or MFA disable/re-enable/revoke administration |
| `G-SCHEMA` | Yes / pending where referenced | Persistence shape/migration; focused schema owner and human schema review | None; pending | New lifecycle fields, constraints, tombstones, secret erasure, compaction, or cleanup state |
| `G-CONTRACT` | Yes / pending where referenced | OpenAPI/generated clients; focused contract lane | None; pending | New/changed endpoints, states, results, error shapes, and generated methods |
| `G-CLIENT` | Yes / pending where referenced | Mobile, user-web, or admin-web owner as separate lanes | None; pending | Controls, warnings, confirmations, readouts, accessibility, and localized catalog adoption |
| `G-RET` | Yes / pending | Retention clock, holds, minimum evidence, and copy disposition; #724 plus record-family owner | None; pending | Any claimed duration, retention expiry processing, compaction, or disposal eligibility |
| `G-PRIV` | Yes / pending | Privacy/redaction and minimum-data decision; #724/#973 plus security owner | None; pending | New metadata/read/export, terminal verifier/hash retention, or evidence exposure |
| `G-DEST` | Yes / pending | Destructive purge/hard-delete authorization and operational proof; human destructive gate | None; pending | Physical deletion, purge, backup/replica disposition, or continuation/broadening of invitation cleanup as settled policy |
| `G-PROV` | Yes / pending where referenced | Provider/delivery/configuration owner; #403/#773/#784 | None; pending | Email/provider claims, enabled delivery, credentials/configuration, or provider-specific payload handling |
| `G-CONFIG` | Yes / pending where referenced | Security/abuse runtime configuration and distributed-state owner; focused row owner plus human security/operations review | None; pending | Threshold/window/capacity/source-trust changes, persistent/distributed abuse state, restart/failover behavior, or proxy-derived identity configuration |
| `G-EXPOSE` | Yes / pending where referenced | Public/admin exposure; #777 or the private admin-entry owner | None; pending | Public registration/ceremony/reset/invitation exposure or a new admin lifecycle surface |

### 5.5 Exact Surface, Metadata, And Disposition Fields

This subsection completes the remaining reusable-schema fields for every exact
row. Profiles are values, not defaults to be guessed:

- `S-SELF-API`: existing authenticated API action; no complete shipped client
  surface is claimed. If exposed, use section 5.1's exact wording.
- `S-PUBLIC-API`: existing bounded anonymous/protocol API surface with generic
  failure presentation; no broader public exposure is authorized.
- `S-ADMIN-API`: existing private authenticated admin API action/readout; no
  complete admin-web UI is claimed.
- `S-PROTOCOL`: not a management control; existing sign-in, refresh, reset,
  invitation, or ceremony protocol presents only the section 5.1 outcome.
- `S-NOT-VISIBLE`: not user-visible; internal time, lineage, policy, cleanup,
  or evidence transition.
- `S-BLOCKED-SELF` / `S-BLOCKED-ADMIN`: proposed wording only; no current API
  or UI exists and the referenced gates block exposure.

Metadata applicability is `applicable` for every profile. `M-SESSION` requires
session/account/status/issue/expiry/revoke/reason/actor/version fields;
`M-REFRESH` family/credential/session IDs, status/lineage, issue/idle/absolute/
consume/revoke/replay/reason/version fields; `M-PASSWORD` credential/account,
status, verifier algorithm/version/parameters, create/update/disable/revoke
times, actor/reason/version but never prior/current verifier material;
`M-ACCOUNT`, `M-IDENTITY`, `M-ROLE`, and `M-PROFILE` require their stable subject IDs,
bounded prior/new status or relationship/role, applicable timestamps,
actor/reason/version; `M-PROFILE` additionally limits auth evidence to its
account link and creation authority rather than copying profile fields;
`M-PASSKEY`, `M-TOTP`, `M-RECOVERY`, and `M-CHALLENGE`
require the safe fields enumerated in section 5.3 plus their status and terminal
timestamps, actor/reason and version, excluding section 3 material;
`M-RESET` and `M-INVITE` require their section 5.3 safe IDs/status/hash-version
and lifecycle/delivery timestamps/categories but no raw material or exposed
hash/contact; `M-ABUSE` requires only bounded operation/scope/outcome/count/
timing/throttle categories or non-reversible references, never raw bucket keys,
identifiers, contacts, or source addresses; `M-POLICY` requires ID/version/status/effective/retired times and
actor/reason/version; `M-AUDIT` requires bounded actor/subject/action/outcome/
time/reason/correlation/request/transition metadata. A named field absent from
current persistence is a gap, not a claim that the field exists.

`D-ORDINARY` means hard-delete `ineligible`: target classification is an
authoritative security record/material, conditions are not met, and consequence
warning/confirmation are `not applicable` because this transition is not a
delete. Purge is `unresolved`: a separate action is required; retention/hold,
dependency/copy, authority, audit, retry/concurrency, warning, explicit
confirmation if user-initiated, and `G-RET`/`G-PRIV`/`G-DEST` remain pending.
`D-TEMPORARY` permits only current positively process-local, non-authoritative
abuse-bucket expiry/clear/eviction; it is not auth-record/audit purge authority
and creates no precedent for a future durable store. `D-FAILED-WORKFLOW`
records only current non-relational compensation of positively identified
new-account bundle rows; audit selection/races and compensation failure remain
gaps, and it is not general account or audit purge authority.
`D-INVITE-CLEANUP` records current hard-delete fact only: target is an
authoritative terminal invitation and current row conditions are terminal
status plus `CleanupEligibleAtUtc`. The cleanup is not a user-presented action,
but authenticated management/read calls invoke it before primary work and a
well-formed anonymous acceptance request invokes it before abuse, policy, or
secret validation; no warning/confirmation exists, and current code does not
prove trigger authorization, holds, or copies. Continued hard-delete and purge
policy remain `unresolved`, require a separate reviewed action/policy, warning
and confirmation if ever user-initiated, and are blocked by
`G-AUTH`/`G-RET`/`G-PRIV`/`G-DEST`/`G-EXPOSE`.

| Exact row | Exact surface profile | Metadata profile | Hard-delete / purge profile |
| --- | --- | --- | --- |
| `AUTH-LC-SES-001` | `S-SELF-API` | `M-SESSION` | `D-ORDINARY` |
| `AUTH-LC-SES-002` | `S-SELF-API` | `M-SESSION` | `D-ORDINARY` |
| `AUTH-LC-SES-003` | `S-SELF-API` | `M-SESSION` | `D-ORDINARY` |
| `AUTH-LC-SES-004` | `S-PROTOCOL` | `M-SESSION` | `D-ORDINARY` |
| `AUTH-LC-SES-005` | `S-PUBLIC-API` | `M-SESSION` + `M-REFRESH` | `D-ORDINARY` |
| `AUTH-LC-SES-006` | `S-PUBLIC-API` | `M-SESSION` | `D-ORDINARY` |
| `AUTH-LC-SES-007` | `S-PROTOCOL` | `M-SESSION` | `D-ORDINARY` |
| `AUTH-LC-SES-008` | `S-PROTOCOL` | `M-SESSION` + `M-REFRESH` | `D-ORDINARY` |
| `AUTH-LC-SES-009` | `S-SELF-API` | `M-SESSION` | `D-ORDINARY` |
| `AUTH-LC-SES-010` | `S-SELF-API` | `M-SESSION` | `D-ORDINARY` |
| `AUTH-LC-SES-011` | `S-SELF-API` | `M-SESSION` + `M-PASSWORD` | `D-ORDINARY` |
| `AUTH-LC-SES-012` | `S-PUBLIC-API` | `M-SESSION` + `M-RESET` | `D-ORDINARY` |
| `AUTH-LC-SES-013` | `S-PROTOCOL` | `M-SESSION` + `M-REFRESH` | `D-ORDINARY` |
| `AUTH-LC-SES-014` | `S-PROTOCOL` | `M-SESSION` + `M-REFRESH` | `D-ORDINARY` |
| `AUTH-LC-SES-015` | `S-PROTOCOL` | `M-SESSION` + `M-REFRESH` | `D-ORDINARY` |
| `AUTH-LC-REF-001` | `S-PROTOCOL` | `M-REFRESH` | `D-ORDINARY` |
| `AUTH-LC-REF-002` | `S-PROTOCOL` | `M-REFRESH` | `D-ORDINARY` |
| `AUTH-LC-REF-003` | `S-PROTOCOL` | `M-REFRESH` | `D-ORDINARY` |
| `AUTH-LC-REF-004` | `S-PUBLIC-API` | `M-REFRESH` | `D-ORDINARY` |
| `AUTH-LC-REF-005` | `S-PUBLIC-API` | `M-REFRESH` | `D-ORDINARY` |
| `AUTH-LC-REF-006` | `S-PROTOCOL` | `M-REFRESH` + `M-SESSION` | `D-ORDINARY` |
| `AUTH-LC-REF-007` | `S-SELF-API` | `M-REFRESH` | `D-ORDINARY` |
| `AUTH-LC-REF-008` | `S-SELF-API` | `M-REFRESH` | `D-ORDINARY` |
| `AUTH-LC-REF-009` | `S-SELF-API` | `M-REFRESH` | `D-ORDINARY` |
| `AUTH-LC-REF-010` | `S-SELF-API` | `M-REFRESH` | `D-ORDINARY` |
| `AUTH-LC-REF-011` | `S-SELF-API` | `M-REFRESH` | `D-ORDINARY` |
| `AUTH-LC-REF-012` | `S-SELF-API` | `M-REFRESH` | `D-ORDINARY` |
| `AUTH-LC-REF-013` | `S-SELF-API` | `M-REFRESH` + `M-PASSWORD` | `D-ORDINARY` |
| `AUTH-LC-REF-014` | `S-SELF-API` | `M-REFRESH` + `M-PASSWORD` | `D-ORDINARY` |
| `AUTH-LC-REF-015` | `S-PUBLIC-API` | `M-REFRESH` + `M-RESET` | `D-ORDINARY` |
| `AUTH-LC-REF-016` | `S-PUBLIC-API` | `M-REFRESH` + `M-RESET` | `D-ORDINARY` |
| `AUTH-LC-REF-017` | `S-PROTOCOL` | `M-REFRESH` | `D-ORDINARY` |
| `AUTH-LC-REF-018` | `S-PROTOCOL` | `M-REFRESH` | `D-ORDINARY` |
| `AUTH-LC-REF-019` | `S-PROTOCOL` | `M-REFRESH` | `D-ORDINARY` |
| `AUTH-LC-REF-020` | `S-PROTOCOL` | `M-REFRESH` | `D-ORDINARY` |
| `AUTH-LC-REF-021` | `S-PROTOCOL` | `M-REFRESH` | `D-ORDINARY` |
| `AUTH-LC-REF-022` | `S-PROTOCOL` | `M-REFRESH` | `D-ORDINARY` |
| `AUTH-LC-REF-023` | `S-PROTOCOL` | `M-REFRESH` | `D-ORDINARY` |
| `AUTH-LC-PWD-001` | `S-SELF-API` | `M-PASSWORD` | `D-ORDINARY` |
| `AUTH-LC-PWD-002` | `S-PUBLIC-API` | `M-PASSWORD` + `M-RESET` | `D-ORDINARY` |
| `AUTH-LC-PWD-003` | `S-BLOCKED-ADMIN` | `M-PASSWORD` | `D-ORDINARY` |
| `AUTH-LC-PWD-011` | `S-BLOCKED-SELF`/`S-BLOCKED-ADMIN` | `M-PASSWORD` | `D-ORDINARY` |
| `AUTH-LC-PWD-004` | `S-BLOCKED-ADMIN` | `M-PASSWORD` | `D-ORDINARY` |
| `AUTH-LC-PWD-005` | `S-PUBLIC-API` | `M-PASSWORD` | `D-ORDINARY` |
| `AUTH-LC-PWD-006` | `S-PUBLIC-API` | `M-PASSWORD` | `D-ORDINARY` |
| `AUTH-LC-PWD-007` | `S-ADMIN-API` | `M-PASSWORD` | `D-ORDINARY` |
| `AUTH-LC-PWD-008` | `S-PROTOCOL` | `M-PASSWORD` | `D-ORDINARY` |
| `AUTH-LC-PWD-009` | `S-PROTOCOL` | `M-PASSWORD` | `D-ORDINARY` |
| `AUTH-LC-PWD-010` | `S-NOT-VISIBLE` | `M-PASSWORD` | `D-FAILED-WORKFLOW` |
| `AUTH-LC-PWD-012` | `S-NOT-VISIBLE` | `M-PASSWORD` | `D-FAILED-WORKFLOW` |
| `AUTH-LC-ACC-001` | `S-BLOCKED-ADMIN` | `M-ACCOUNT` | `D-ORDINARY` |
| `AUTH-LC-ACC-002` | `S-BLOCKED-ADMIN` | `M-ACCOUNT` | `D-ORDINARY` |
| `AUTH-LC-ACC-003` | `S-BLOCKED-ADMIN` | `M-ACCOUNT` | `D-ORDINARY` |
| `AUTH-LC-ACC-004` | `S-PUBLIC-API` | `M-ACCOUNT` | `D-ORDINARY` |
| `AUTH-LC-ACC-005` | `S-PUBLIC-API` | `M-ACCOUNT` | `D-ORDINARY` |
| `AUTH-LC-ACC-006` | `S-ADMIN-API` | `M-ACCOUNT` | `D-ORDINARY` |
| `AUTH-LC-ACC-007` | `S-NOT-VISIBLE` | `M-ACCOUNT` | `D-FAILED-WORKFLOW` |
| `AUTH-LC-ACC-008` | `S-NOT-VISIBLE` | `M-ACCOUNT` | `D-FAILED-WORKFLOW` |
| `AUTH-LC-ID-001` | `S-BLOCKED-SELF`/`S-BLOCKED-ADMIN` | `M-IDENTITY` | `D-ORDINARY` |
| `AUTH-LC-ID-002` | `S-PUBLIC-API` | `M-IDENTITY` | `D-ORDINARY` |
| `AUTH-LC-ID-003` | `S-PUBLIC-API` | `M-IDENTITY` | `D-ORDINARY` |
| `AUTH-LC-ID-004` | `S-ADMIN-API` | `M-IDENTITY` | `D-ORDINARY` |
| `AUTH-LC-ID-005` | `S-BLOCKED-SELF`/`S-BLOCKED-ADMIN` | `M-IDENTITY` | `D-ORDINARY` |
| `AUTH-LC-ID-006` | `S-NOT-VISIBLE` | `M-IDENTITY` | `D-FAILED-WORKFLOW` |
| `AUTH-LC-ID-009` | `S-NOT-VISIBLE` | `M-IDENTITY` | `D-FAILED-WORKFLOW` |
| `AUTH-LC-ID-007` | `S-BLOCKED-SELF`/`S-BLOCKED-ADMIN` | `M-IDENTITY` | `D-ORDINARY` |
| `AUTH-LC-ID-008` | `S-BLOCKED-SELF`/`S-BLOCKED-ADMIN` | `M-IDENTITY` | `D-ORDINARY` |
| `AUTH-LC-ROL-001` | `S-PUBLIC-API` | `M-ROLE` | `D-ORDINARY` |
| `AUTH-LC-ROL-002` | `S-BLOCKED-ADMIN` | `M-ROLE` | `D-ORDINARY` |
| `AUTH-LC-ROL-003` | `S-BLOCKED-ADMIN` | `M-ROLE` | `D-ORDINARY` |
| `AUTH-LC-ROL-004` | `S-PUBLIC-API` | `M-ROLE` | `D-ORDINARY` |
| `AUTH-LC-ROL-005` | `S-ADMIN-API` | `M-ROLE` | `D-ORDINARY` |
| `AUTH-LC-ROL-006` | `S-NOT-VISIBLE` | `M-ROLE` | `D-FAILED-WORKFLOW` |
| `AUTH-LC-ROL-008` | `S-NOT-VISIBLE` | `M-ROLE` | `D-FAILED-WORKFLOW` |
| `AUTH-LC-ROL-007` | `S-BLOCKED-ADMIN` | `M-ROLE` | `D-ORDINARY` |
| `AUTH-LC-PKY-001` | `S-SELF-API` | `M-PASSKEY` | `D-ORDINARY` |
| `AUTH-LC-PKY-002` | `S-BLOCKED-SELF`/`S-BLOCKED-ADMIN` | `M-PASSKEY` | `D-ORDINARY` |
| `AUTH-LC-PKY-003` | `S-BLOCKED-SELF`/`S-BLOCKED-ADMIN` | `M-PASSKEY` | `D-ORDINARY` |
| `AUTH-LC-PKY-004` | `S-SELF-API` | `M-PASSKEY` | `D-ORDINARY` |
| `AUTH-LC-PKY-005` | `S-PROTOCOL` | `M-PASSKEY` | `D-ORDINARY` |
| `AUTH-LC-PKY-006` | `S-SELF-API` | `M-PASSKEY` | `D-ORDINARY` |
| `AUTH-LC-PKY-007` | `S-PUBLIC-API` | `M-PASSKEY` + `M-SESSION` | `D-ORDINARY` |
| `AUTH-LC-PKY-008` | `S-SELF-API` | `M-PASSKEY` | `D-ORDINARY` |
| `AUTH-LC-TOTP-001` | `S-SELF-API` | `M-TOTP` | `D-ORDINARY` |
| `AUTH-LC-TOTP-002` | `S-SELF-API` | `M-TOTP` | `D-ORDINARY` |
| `AUTH-LC-TOTP-011` | `S-SELF-API` | `M-TOTP` | `D-ORDINARY` |
| `AUTH-LC-TOTP-012` | `S-SELF-API` | `M-TOTP` | `D-ORDINARY` |
| `AUTH-LC-TOTP-003` | `S-PROTOCOL` | `M-TOTP` | `D-ORDINARY` |
| `AUTH-LC-TOTP-008` | `S-PROTOCOL` | `M-TOTP` | `D-ORDINARY` |
| `AUTH-LC-TOTP-004` | `S-BLOCKED-SELF`/`S-BLOCKED-ADMIN` | `M-TOTP` | `D-ORDINARY` |
| `AUTH-LC-TOTP-005` | `S-BLOCKED-SELF`/`S-BLOCKED-ADMIN` | `M-TOTP` | `D-ORDINARY` |
| `AUTH-LC-TOTP-006` | `S-SELF-API` | `M-TOTP` | `D-ORDINARY` |
| `AUTH-LC-TOTP-007` | `S-SELF-API` | `M-TOTP` | `D-ORDINARY` |
| `AUTH-LC-TOTP-009` | `S-SELF-API` | `M-TOTP` | `D-ORDINARY` |
| `AUTH-LC-TOTP-010` | `S-PROTOCOL` | `M-TOTP` + `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-TOTP-013` | `S-PROTOCOL` | `M-TOTP` + `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-RCV-001` | `S-SELF-API` | `M-RECOVERY` | `D-ORDINARY` |
| `AUTH-LC-RCV-002` | `S-PROTOCOL` | `M-RECOVERY` | `D-ORDINARY` |
| `AUTH-LC-RCV-003` | `S-SELF-API` | `M-RECOVERY` | `D-ORDINARY` |
| `AUTH-LC-RCV-004` | `S-NOT-VISIBLE` | `M-RECOVERY` | `D-ORDINARY` |
| `AUTH-LC-RCV-005` | `S-SELF-API` | `M-RECOVERY` | `D-ORDINARY` |
| `AUTH-LC-RCV-006` | `S-SELF-API` | `M-RECOVERY` | `D-ORDINARY` |
| `AUTH-LC-RCV-007` | `S-NOT-VISIBLE` | `M-RECOVERY` | `D-ORDINARY` |
| `AUTH-LC-RCV-008` | `S-SELF-API` | `M-RECOVERY` | `D-ORDINARY` |
| `AUTH-LC-RCV-009` | `S-SELF-API` | `M-RECOVERY` | `D-ORDINARY` |
| `AUTH-LC-RCV-010` | `S-SELF-API` | `M-RECOVERY` | `D-ORDINARY` |
| `AUTH-LC-RCV-011` | `S-SELF-API` | `M-RECOVERY` | `D-ORDINARY` |
| `AUTH-LC-RCV-012` | `S-SELF-API` | `M-RECOVERY` | `D-ORDINARY` |
| `AUTH-LC-RCV-013` | `S-SELF-API` | `M-RECOVERY` | `D-ORDINARY` |
| `AUTH-LC-RCV-014` | `S-PROTOCOL` | `M-RECOVERY` | `D-ORDINARY` |
| `AUTH-LC-CHL-001` | `S-PROTOCOL` | `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-CHL-002` | `S-PROTOCOL` | `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-CHL-003` | `S-PROTOCOL` | `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-CHL-004` | `S-PROTOCOL` | `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-CHL-005` | `S-PROTOCOL` | `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-CHL-006` | `S-BLOCKED-SELF` | `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-CHL-007` | `S-PROTOCOL` | `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-CHL-008` | `S-PUBLIC-API` | `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-CHL-009` | `S-SELF-API` | `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-CHL-010` | `S-PUBLIC-API` | `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-CHL-011` | `S-SELF-API` | `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-CHL-012` | `S-PROTOCOL` | `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-CHL-013` | `S-PROTOCOL` | `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-CHL-014` | `S-PROTOCOL` | `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-CHL-015` | `S-PROTOCOL` | `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-CHL-016` | `S-PROTOCOL` | `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-CHL-017` | `S-SELF-API` | `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-CHL-018` | `S-SELF-API` | `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-CHL-019` | `S-PROTOCOL` | `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-CHL-020` | `S-PROTOCOL` | `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-RST-001` | `S-NOT-VISIBLE` | `M-RESET` | `D-ORDINARY` |
| `AUTH-LC-RST-002` | `S-PUBLIC-API` | `M-RESET` | `D-ORDINARY` |
| `AUTH-LC-RST-003` | `S-PUBLIC-API` | `M-RESET` | `D-ORDINARY` |
| `AUTH-LC-RST-004` | `S-PUBLIC-API` | `M-RESET` | `D-ORDINARY` |
| `AUTH-LC-RST-005` | `S-NOT-VISIBLE` | `M-RESET` | `D-ORDINARY` |
| `AUTH-LC-RST-006` | `S-PUBLIC-API` | `M-RESET` | `D-ORDINARY` |
| `AUTH-LC-RST-007` | `S-PUBLIC-API` | `M-RESET` | `D-ORDINARY` |
| `AUTH-LC-RST-008` | `S-PUBLIC-API` | `M-RESET` | `D-ORDINARY` |
| `AUTH-LC-RST-009` | `S-PUBLIC-API` | `M-RESET` | `D-ORDINARY` |
| `AUTH-LC-RST-010` | `S-PUBLIC-API` | `M-RESET` | `D-ORDINARY` |
| `AUTH-LC-RST-011` | `S-PUBLIC-API` | `M-RESET` | `D-ORDINARY` |
| `AUTH-LC-RST-012` | `S-PUBLIC-API` | `M-RESET` | `D-ORDINARY` |
| `AUTH-LC-INV-001` | `S-PUBLIC-API` | `M-INVITE` | `D-ORDINARY` |
| `AUTH-LC-INV-002` | `S-ADMIN-API` | `M-INVITE` | `D-ORDINARY` |
| `AUTH-LC-INV-003` | `S-ADMIN-API` | `M-INVITE` | `D-ORDINARY` |
| `AUTH-LC-INV-004` | `S-ADMIN-API` | `M-INVITE` | `D-ORDINARY` |
| `AUTH-LC-INV-005` | `S-ADMIN-API` | `M-INVITE` | `D-INVITE-CLEANUP` |
| `AUTH-LC-INV-006` | `S-ADMIN-API` | `M-INVITE` | `D-ORDINARY` |
| `AUTH-LC-INV-007` | `S-ADMIN-API` | `M-INVITE` | `D-ORDINARY` |
| `AUTH-LC-INV-008` | `S-NOT-VISIBLE` | `M-INVITE` | `D-FAILED-WORKFLOW` |
| `AUTH-LC-INV-009` | `S-NOT-VISIBLE` | `M-INVITE` | `D-FAILED-WORKFLOW` |
| `AUTH-LC-INV-010` | `S-PUBLIC-API` | `M-INVITE` | `D-INVITE-CLEANUP` |
| `AUTH-LC-INV-011` | `S-PUBLIC-API` | `M-INVITE` | `D-ORDINARY` |
| `AUTH-LC-INV-012` | `S-PUBLIC-API` | `M-INVITE` | `D-ORDINARY` |
| `AUTH-LC-INV-013` | `S-ADMIN-API` | `M-INVITE` | `D-ORDINARY` |
| `AUTH-LC-INV-014` | `S-ADMIN-API` | `M-INVITE` | `D-ORDINARY` |
| `AUTH-LC-INV-015` | `S-ADMIN-API` | `M-INVITE` | `D-ORDINARY` |
| `AUTH-LC-PRO-001` | `S-NOT-VISIBLE` | `M-PROFILE` | `D-FAILED-WORKFLOW` |
| `AUTH-LC-PRO-002` | `S-PUBLIC-API` | `M-PROFILE` | `D-ORDINARY` |
| `AUTH-LC-PRO-003` | `S-PROTOCOL` | `M-PROFILE` | `D-ORDINARY` |
| `AUTH-LC-PRO-004` | `S-ADMIN-API` | `M-PROFILE` | `D-ORDINARY` |
| `AUTH-LC-PRO-005` | `S-NOT-VISIBLE` | `M-PROFILE` | `D-FAILED-WORKFLOW` |
| `AUTH-LC-ABU-001` | `S-PROTOCOL` | `M-ABUSE` | `D-TEMPORARY` |
| `AUTH-LC-ABU-002` | `S-NOT-VISIBLE` | `M-ABUSE` | `D-TEMPORARY` |
| `AUTH-LC-ABU-003` | `S-NOT-VISIBLE` | `M-ABUSE` | `D-TEMPORARY` |
| `AUTH-LC-ABU-004` | `S-PUBLIC-API` | `M-ABUSE` | `D-TEMPORARY` |
| `AUTH-LC-ABU-005` | `S-PROTOCOL` | `M-ABUSE` | `D-TEMPORARY` |
| `AUTH-LC-ABU-006` | `S-NOT-VISIBLE` | `M-ABUSE` | `D-TEMPORARY` |
| `AUTH-LC-ABU-007` | `S-ADMIN-API` | `M-ABUSE` | `D-TEMPORARY` |
| `AUTH-LC-ABU-008` | `S-NOT-VISIBLE` | `M-ABUSE` | `D-TEMPORARY` |
| `AUTH-LC-ABU-009` | `S-NOT-VISIBLE` | `M-ABUSE` | `D-TEMPORARY` |
| `AUTH-LC-ABU-010` | `S-PUBLIC-API` | `M-ABUSE` | `D-TEMPORARY` |
| `AUTH-LC-POL-001` | `S-BLOCKED-ADMIN` | `M-POLICY` | `D-ORDINARY` |
| `AUTH-LC-POL-003` | `S-BLOCKED-ADMIN` | `M-POLICY` | `D-ORDINARY` |
| `AUTH-LC-POL-004` | `S-BLOCKED-ADMIN` | `M-POLICY` | `D-ORDINARY` |
| `AUTH-LC-POL-002` | `S-ADMIN-API` | `M-POLICY` | `D-ORDINARY` |
| `AUTH-LC-POL-005` | `S-ADMIN-API` | `M-POLICY` | `D-ORDINARY` |
| `AUTH-LC-AUD-001` | `S-NOT-VISIBLE` | `M-AUDIT` | `D-ORDINARY` |
| `AUTH-LC-AUD-002` | `S-NOT-VISIBLE` | `M-AUDIT` | `D-FAILED-WORKFLOW` |
| `AUTH-LC-AUD-003` | `S-NOT-VISIBLE` | `M-AUDIT` | `D-FAILED-WORKFLOW` |

## 6. Explicit Open Choices

Each choice is blocked until its named owner records a reviewed decision. A
current runtime fact is not approval to broaden or perpetuate unsafe behavior.

| Choice ID | Exact question and affected rows | Why current authority cannot answer | Safe blocked posture, owner/gate, downstream work |
| --- | --- | --- | --- |
| `AUTH-LC-CHOICE-001` | What exact creation, disable, re-enable, and deletion rules govern accounts and their sessions/families, challenges, reset/invitation material, identities, password/factors, roles, profile/domain data, evidence/copies, notifications, owner recovery, and re-entry? (`ACC-*`) | Bootstrap, invitation, and guarded admin provisioning create materially different root bundles. Bootstrap rollback is relational-only: non-relational credential failure can strand a saved active account/profile/identity/owner roles/audit without a password or recovery path. Admin non-relational compensation is not general deletion authority. Disable/delete runtime, recovery posture, and last-owner policy are absent; anonymous factor verification also misses later availability checks. | Preserve bounded creation authority; expose no new mutation/deletion. #788/#785/#394/#724/#784; manual owner-lockout/auth/privacy/schema/destructive gates. Accept exact-one bootstrap plus relational rollback and non-relational stranded-root recovery, invited/admin rollback/collision, challenge timing, dependencies/holds/copies, and restore denial. |
| `AUTH-LC-CHOICE-002` | How are creation, verification, replacement, reset and rehash serialized/failure-atomic across providers; when may credentials change lifecycle; what recovery/session/material rules apply? (`PWD-*`, `SES-005`) | Non-relational bootstrap can commit account roots before credential failure. Other concurrent verifier paths can last-write; non-relational session revocation can fail after password replacement, reset may remain reusable, old proof can race issuance, and disabled credential recovery/runtime is absent. | Preserve bounded behavior, require bootstrap and replacement failure injection plus proof-to-session ordering, expose no disable mutation without recovery/last-method proof, and never retain predecessor verifier as history. #339/#338/#788/#784/#785; auth/security/schema/privacy gates. |
| `AUTH-LC-CHOICE-003` | How are local/provider identity creation collisions handled, and what provider proof plus remaining-method checks permit link/unlink/relink with what session response? (`ID-*`) | Bootstrap, invitation, and guarded admin provisioning create local links but focused relational collision/rollback proof is incomplete. Provider-link authority is architecture-defined/default-disabled without OIDC runtime; terminal lifecycle has only lookup plus `DisabledAtUtc`. | Preserve bounded local-link creation; expose no provider link/unlink UI/API until stable-subject collision/takeover, rollback, lockout, session, audit, and redaction rules are approved. #787/#773/#785/#784; provider, lockout, security, privacy gates. |
| `AUTH-LC-CHOICE-004` | How are product roles created under each provisioning authority, then generally assigned, changed, or removed with last-owner, self-lockout, concurrency, next-request/in-flight authorization and audit safety? (`ROL-*`) | Bootstrap, invitation, and admin provisioning create different exact assignments under different actors and rollback boundaries. No general mutation/removal lifecycle or removal history exists. Current authentication reloads assignments for every request, so a committed change affects the next request without session/token refresh, but cannot recall already-authorized in-flight work. | Preserve only the three bounded creation authorities and current per-request read behavior; expose no general role mutation. Require next-request and in-flight ordering tests with the eventual mutation owner. #785/#464/#465/#784; manual owner-lockout plus API/schema/UI gates. |
| `AUTH-LC-CHOICE-005` | What anti-lockout, discoverable/non-discoverable options behavior, completion policy revalidation, allowed lifecycle and metadata-mutation source states, factor eligibility/use, concurrency/idempotency, session/challenge response, monotonic anti-replay/use state, terminal-evidence mutability, and material-erasure rules apply to passkey/TOTP/recovery lifecycle? (`PKY-*`, `TOTP-*`, `RCV-*`, `CHL-*`) | Passkey enrollment/sign-in/step-up and TOTP enrollment/TOTP/recovery verification do not recheck support policy at completion. Resident keys are preferred rather than required, yet stored hashes cannot reconstruct descriptors and options return an empty allow-list for non-discoverable credentials. Passkey sign-in drops its session credential and can commit credential/challenge/session before a later audit-save failure; concurrent assertions can regress the stored counter/miss replay suspicion. Initial TOTP setup/replacement and initial recovery-code generation/replacement are distinct. TOTP use can race lifecycle changes/reuse a timestep across challenges, its decrypted mutable seed is not proved zeroized, and enrollment/use unprotect exceptions bypass bounded result and attempt/audit persistence; enrolled-factor failure also prevents same-request healthy-sibling fallback. Concurrent distinct recovery codes can lose batch counter updates/regress use time; rename endpoints mutate terminal metadata. Other uniqueness, broad revoke, last-factor/session and terminal-secret rules are partial. | Keep precise current behavior and classify initial-versus-replacement issuance, display/no-redisplay, options usability, handoff/audit rollback, setup and active-use secret lifetime/disposal, enrollment/use decrypt/key-rotation result/audit/retry/replacement and sibling behavior, all-factor policy changes, uniqueness, eligibility/use, monotonic counter/time, exact-one consumption and exact aggregate counts, broad-state rename/revoke, terminal evidence integrity, repeat, and races partial; add no runtime or secret disposition here. #394/#775/#776/#465/#724 and passkey/MFA #965 owners; manual auth/recovery/secret/schema gate. |
| `AUTH-LC-CHOICE-006` | How are challenge issuance/completion handoff authority, binding, duplicates, freshness, policy, proof uniqueness, attempts, anti-replay, terminal persistence, retention and cleanup governed? (`CHL-*`, `PKY-007/008`, `RCV-002`) | MFA public create is unusable; anonymous verification binding is missing; passkey sign-in binding is unenforced. Option issuance and passkey step-up completion can commit before response failure. MFA/passkey completion responses use configurable 300-second defaults, but authorization uses a hard-coded 900-second window after taking only the ten newest proofs before operation matching. MFA `sign_in` success advertises freshness that the evaluator excludes, and omitted purpose/operation creates a null-bound default step-up that no normalized operation can use. Already-at-max block is never saved and threshold-save failure is ignored. Issuance/failure audit and actor differ; policy/counters/attempts can replay, regress, or collapse. | Keep authority/purpose/binding-specific rows and broken handoffs explicit; require post-save issuance/completion fault/retry/reconciliation/cleanup, response-versus-authorization time/operation/candidate-cap parity, sign-in and null-default result truth, both block persistence paths, per-purpose audit/actor/binding, atomicity and retention; authorize no purge. #394/#775/#724/`REC-AUTH-MFA-SIGNIN-001` plus #965 owners. |
| `AUTH-LC-CHOICE-007` | How are anonymous password-reset initiation, issuance, and consumption made atomic and enumeration-resistant; what should dependency denial do to pending material; and what bounded retention/disposition applies to terminal rows and hashes, including replay evidence and `CleanupEligibleAtUtc`? (`RST-*`) | Anonymous initiation resolves its identifier and attempts an unguarded audit save before abuse/readiness/provider gates; save failure reaches none of those gates or the intended generic response and records no throttle attempt. Concurrent issuance can leave multiple pending links; concurrent completion can read the same pending row without a status-qualified transition/concurrency token; dependency denial leaves pending material reusable if the dependency recovers; disabled credentials cannot use current reset; cleanup marker is unused. | Preserve generic initiation intent, but require pre-gate save-failure/response/retry/admission evidence; treat issuance/completion concurrency and denial disposition as partial, retain current rows, and do not expose hashes/add cleanup. #339/#724/`REC-AUTH-ABUSE-001`; security/privacy/provider/exposure/destructive gates. |
| `AUTH-LC-CHOICE-008` | What creation concurrency, grace, delivery, acceptance rollback/actor, revoke reason, initial-policy/replacement, and authenticated/public cleanup/expiry authorities govern invitations; what terminal disposition applies? (`INV-*`, `POL-002/005`) | Initial policy creation works but replacement is broken. Acceptance actor is wrong, revoke drops reason, grace disagrees, resend rotates on retry, authenticated operations trigger capped cleanup without retaining their known actor/correlation, over-batch rows can remain pending while list/get projects expiry, revoke/resend terminally denies, or create reports a duplicate and cannot replace the expired contact row; anonymous acceptance triggers capped pre-proof cleanup plus a distinct post-lookup target expiry/save. | Preserve proven bounded paths; do not claim replacement or approved purge. Require initial-policy and replacement races separately, actor/reason/acceptance proof, and each cleanup/expiry trigger's authorization, actor/correlation, batch selection, persisted-versus-projected status truth, duplicate-block reconciliation and failure ordering. #784/#724; provider/exposure/destructive/security/privacy gate. |
| `AUTH-LC-CHOICE-009` | What audit-event retention clock, duration, holds, authorized read/export, actor/subject correlation and terminal disposition apply, and how must non-relational failed-bundle compensation preserve or account for evidence? (`AUD-*`) | Architecture says configurable/bounded but defines no value or general purge runtime. Compensation can remove broader account evidence. Credential change/denial and authenticated challenge failures can omit actor; refresh family revocation can omit family identity; passkey sign-in/step-up issuance is unaudited. | Authorize no ordinary removal/purge. Require exact actor/subject/correlation, per-purpose event inventory, exact compensation selection, failure/race/loss accounting, retention and redaction before runtime change. #724/#774/#973/#784/#785; privacy/destructive/manual gate. |
| `AUTH-LC-CHOICE-010` | How are local, passkey, and refresh-authorized session issuance/use, credential-proof binding, refresh rotation, replay response, and revocation serialized; how must validation respond to family status; and how are lineage/last-seen retention governed? (`SES-*`, `REF-*`, `PWD-009`) | Local sign-in creates a full family, refresh creates replacement session/credential, and passkey sign-in creates unusable access-only state. Old proof can escape replacement selection; validation can race/regress and ignores family status; selection cascades can miss overlapping rotation. The relational two-rotation loser invokes a nested transaction before disposing the rolled-back outer transaction and throws before replay cascade/audits. Retention/copy disposition are undefined. | Classify each issuance authority as concurrency-partial and the relational loser replay branch as broken; promise neither universal invalidation nor old-proof exclusion; keep terminal rows unusable and authorize no cleanup. #338/#339/#394/#1059/#724; require nested-transaction, relational rotation/replay, refresh issuance, validate-revoke/monotonic-time tests. |
| `AUTH-LC-CHOICE-011` | What draft creation/edit, activation/replacement, retirement, version/effective-time/retention rules govern general security and invitation policy rows, and how do changes safely affect sessions/factors/owners? (`POL-*`) | Persisted general-policy shapes distinguish draft/active/retired, but no general mutation lifecycle exists. Invitation replacement is broken/provider-divergent because the existing active row is loaded untracked. Complete cross-policy effects do not exist. | Use current safe read/default and initial invitation-policy creation only; expose no general draft/activate/retire mutations and do not claim invitation replacement. #394/#465/#784/#785; manual security/owner-lockout gate plus atomic single-active/version/effective-time proof. |
| `AUTH-LC-CHOICE-012` | Which immediate responses may contain bearer/setup/display/ceremony material; what no-redisplay proof and failed-path or active-use secret lifetime apply? | Sign-in/refresh create raw credentials before final success. TOTP setup/recovery retain display material across post-save readout; passkey/MFA option issuance can strand live challenges, TOTP/passkey enrollment and passkey step-up completion can commit before success response, and passkey sign-in discards its bearer. TOTP verification repeatedly decrypts a mutable seed without proved zeroization. Recovery retries conflict or replace rather than redisplay. | Require explicit setup/display/ceremony and active-use secret lifetime, post-save failure injection, deterministic retry/reconciliation/cleanup UX, no-redisplay, and no-leak/disposal/zeroization evidence. `REC-AUTH-DOC-DRIFT-001` plus auth/invitation/reset owners; manual auth/security gate. |
| `AUTH-LC-CHOICE-013` | Must account-wide session revocation query refresh families/credentials directly, and how should exclusion of the current session interact with a family shared by another selected session? What must “all sessions” promise? (`SES-003`, `PWD-001/002`) | `RevokeActiveSessionsForAccountAsync` filters access sessions by `ExpiresAtUtc > now` and derives family IDs only through credentials linked to those selected sessions. When password change excludes the current session, every family linked to it is removed from candidates even if another selected session shares that family. Current tests do not prove revocation of valid refresh authority linked only to an expired session or retained through that shared-family exclusion. | Describe current behavior as partial; do not promise universal refresh invalidation. Reuse #338/#339 in `auth-session-security`; manual auth/security gate before a focused runtime fix and later contract/client wording. |

## 7. Retention Posture By Family

| Family | Classification |
| --- | --- |
| Active password, TOTP and recovery verifier material | Operational credential material while active; not lifecycle evidence. Terminal material disposition unresolved; reusable-material retention for history forbidden. |
| Access sessions and refresh families/credentials | Validity source-defined; terminal lineage operationally required for revocation/replay, duration unresolved. |
| Account, identity, and role lifecycle | Operationally required evidence, duration unresolved; no purge authority. |
| Passkey credential metadata/public material | Operationally required for active use and bounded terminal collision/replay review; duration unresolved; passkey private material absent. |
| Challenges/ceremonies | Source-defined transient validity; terminal safe-metadata retention/cleanup unresolved. |
| Password-reset requests | Source-defined validity; terminal evidence and hash disposition unresolved; current cleanup marker unused. |
| Invitations | Seven-day validity and current 90-day terminal hard-cleanup are source-defined implementation facts; continued disposal authority unresolved/manual-gated. |
| Security/invitation policies | Draft/active/retired version evidence operationally required; duration unresolved; no general security-policy mutation lifecycle exists and invitation replacement is broken/provider-divergent. |
| Auth audit | Policy-defined/configurable intent; operationally required but duration, holds and general disposal unresolved. Current non-relational failed-bundle compensation can delete selected new-account events and is an evidence-integrity gap, not general purge authority. |
| In-memory abuse-throttle buckets | Source-defined transient operational state: current defaults retain sign-in entries 30 minutes and reset/invitation entries one hour, with lazy prune, selective clears, capacity eviction where configured, and total loss on restart. Not durable audit evidence; future durable/distributed retention remains unresolved. |

No row equates retention expiry with purge. No row authorizes physical deletion
because a value is hashed, encrypted, disabled, revoked, consumed, expired, or
old.

## 8. Current Evidence And Stale-Source Reconciliation

Current implementation evidence includes:

- session and refresh families:
  `services/api/src/Settleora.Api/Auth/Sessions/AuthSessionRuntimeService.cs`,
  `AuthRefreshSessionRuntimeService.cs`, and their focused tests;
- password credential/change/reset:
  `Auth/Credentials/AuthCredentialWorkflowService.cs`,
  `Auth/PasswordChange/CurrentAccountPasswordChangeService.cs`,
  `Auth/PasswordReset/LocalPasswordResetService.cs`, and focused tests;
- guarded admin-local-user provisioning:
  `Auth/AdminUsers/AdminLocalUserService.cs` and
  `AdminLocalUserEndpointTests.cs`;
- passkey/MFA/recovery/challenges:
  `Auth/Passkeys/PasskeyRuntimeService.cs`, `Auth/Mfa/MfaRuntimeService.cs`,
  `Auth/Policy/AuthSecurityPolicyService.cs`, and
  `AuthMfaPasskeySecurityRegressionTests.cs`;
- invitation lifecycle and current physical cleanup:
  `Auth/Invitations/InvitationManagementService.cs`,
  `InvitationAcceptanceService.cs`, `InvitationLifecycleCleanupService.cs`,
  and focused invitation tests;
- persistence shapes and constraints:
  `Domain/Auth/*.cs`, `Persistence/SettleoraDbContext.cs`, and migrations
  `20260501143101_AddAuthCredentialsSessionsAuditSchemaFoundation`,
  `20260503092157_AddRefreshSessionFamilySchemaFoundation`,
  `20260624143555_AddAuthMfaPasskeySchemaFoundation`,
  `20260706041357_AddAuthPasswordResetRequestsFoundation`,
  `20260708085411_AddAuthInvitationsFoundation`, and
  `20260708105314_AddAuthInvitationPolicyRuntime`;
- canonical routes and generated methods in
  `packages/contracts/openapi/settleora.v1.yaml`,
  `packages/client-web/src/generated/`, and
  `packages/client-dart/lib/generated/`; and
- bounded audit/redaction evidence in credential, session, sign-in, reset,
  invitation, MFA and passkey `Ef*AuditWriter`/audit-writer classes plus
  `PasswordResetAuditRedactionAcceptanceTests.cs` and
  `AuthMfaPasskeySecurityRegressionTests.cs`.

The current code proves records and selected transitions, not production
readiness, public exposure, provider enablement, every stated policy, or
complete audit coverage. In particular, the older
[identity foundation](AUTH_IDENTITY_FOUNDATION.md),
[credential/session design](AUTH_CREDENTIALS_SESSIONS_AUDIT_DESIGN.md),
[MFA/passkey architecture](AUTH_MFA_PASSKEY_ARCHITECTURE.md), passkey/TOTP
contract design documents, and parts of the
[Day 1 PRD](../prd/MVP_DAY1_SCOPE.md) contain stale “future/unimplemented”
statements or conflicting response rules. They must not erase current source
facts or be silently rewritten by #721.

### 8.1 Pinned Revision And Live-State Verification

Every bare `#nnn` owner/evidence reference in the transition matrices and
choices incorporates this registry as its row-level
`issue_pr_live_state_verification` field. States were re-fetched from GitHub at
`2026-09-15T03:16:00Z` (`2026-09-15 11:16 HKT`); a later consumer must refresh
them rather than assume they remain current. Implementation evidence was
inspected against source revision
`a93409c306133ef7b9c4d1861f36f69f724b011e` (the unchanged runtime base of this
documentation branch). This common registry avoids repeating mutable state in
180 rows without weakening the per-row owner reference.

| Evidence references | Verified live state / pinned revision |
| --- | --- |
| #338, #339, #369, #394, #403, #464, #465, #724, #773, #774, #775, #776, #777, #784, #785, #787, #788, #1059 | `OPEN` at the verification timestamp; existing focused/umbrella owners, not implementation approval. |
| #716, #717, #718, #719, #720, #721, #946, #961, #973 | `OPEN` at the verification timestamp; coordination, sibling, consumer, deferred, or audit work as described by the referencing row/section. |
| #975 | `OPEN` with GitHub `state_reason=reopened` at the verification timestamp; later acceptance consumer only. |
| #960, #965 | `CLOSED`, `state_reason=completed` at the verification timestamp; merged taxonomy prerequisite and completed auth/session audit evidence respectively. |
| PR #1238 | `OPEN`, base `main` at the verification timestamp; candidate policy PR, not yet merge evidence. The timestamp is the pin for this mutable PR; exact reviewed head identity belongs in its review/merge evidence and the task report. |

## 9. Follow-Up Recommendations And Duplicate Prevention

The pre-mutation live issue/PR and repository search found no pre-existing #721
branch, PR, or merged equivalent. This task subsequently created the retained
candidate branch and PR #1238 recorded in §8.1; no duplicate implementation PR
or alternate policy was found. Existing owners are reused:

| Recommendation | Existing owner / canonical lane | Focus and gate |
| --- | --- | --- |
| Session/credential replacement and refresh-family completeness | #338/#339; `auth-session-security` | Resolve `AUTH-LC-CHOICE-002/007/010/013` so old-password proof cannot issue new authority outside a concurrent change/reset cascade, validation metadata/status ordering is safe, and self/selected/all-session sign-out plus password-change/reset per-root behavior matches exact authority, rollback, lineage, and shared-family semantics; separate runtime first, then contract/client copy. Manual auth/security gate. |
| Account/credential/identity/role lifecycle decisions | #788/#785/#787/#339/#724; `auth-session-security`, later split schema/contract/admin UI/retention lanes | Resolve choices 001–004 with provisioning collision/rollback and authority-specific role creation across bootstrap, invitation, and admin workflows; password rehash/change/reset serialization; provider-link proof/takeover; last-owner/recovery safety; account deletion distinct from profile/domain-data deletion; and no implied physical purge. Manual auth/owner-lockout/privacy/destructive gates. |
| Factor/passkey/recovery lifecycle and terminal material | #394/#775/#776/#465/#724; `auth-session-security`, split schema/contract/client lanes | Resolve initial-versus-replacement TOTP/recovery issuance and no-redisplay, TOTP enrollment/use decrypt/key-rotation failure plus bounded result/attempt/audit/retry/replacement/sibling behavior and active-seed disposal, enrollment/cancel concurrency, passkey monotonic-counter updates/replay suspicion, recovery's TOTP-only prerequisite, same-code atomic consumption and distinct-code batch-counter updates, broad rename/revoke source states, terminal-evidence mutation/material under choices 005–006. Manual recovery/secret/security gates. |
| Challenge retention/abuse | `REC-AUTH-MFA-SIGNIN-001` and `REC-AUTH-ABUSE-001`; docs decision then split runtime/config lanes | Repair anonymous MFA issuance and enforce pending-flow/account/session/purpose binding; reconcile `sign_in` freshness, null-bound default step-up, 300/900-second time divergence and ten-proof pre-operation-filter eviction; resolve nonterminal challenge failure and atomic attempts/blocking, cross-challenge TOTP/recovery-proof replay, passkey counter monotonicity, and expiry/replay/completion/cleanup. Reconcile sign-in/reset/invitation bucket record/selective-clear/prune/capacity/restart/multi-node behavior without treating transient state as audit. Manual security/proxy/privacy/config gates. |
| Passkey ceremony/session handoff | `REC-AUTH-PASSKEY-SESSION-001`; docs decision then split auth runtime and contract lanes | Keep all-completion policy revalidation, enrollment uniqueness/challenge atomicity, and the currently discarded sign-in session credential/usable handoff with the #965 owner; #721 adds lifecycle wording only. |
| Reset initiation, retention and atomicity | #339/#724 and `REC-AUTH-ABUSE-001`; `auth-session-security` plus provider/exposure and later retention lanes | Preserve enumeration-resistant anonymous initiation; define atomic issuance/consume, disabled-credential recovery, dependency-denial disposition, bounded terminal row/hash disposition, and cleanup evidence. Manual security/privacy/provider/exposure/destructive gates. |
| Invitation policy, evidence and retention cleanup | #784/#724; focused docs decision, then separate `auth-session-security` runtime if approved | Repair relational policy replacement, acceptance and authenticated-cleanup actor/correlation attribution, and revoke-reason evidence; reconcile grace readout/enforcement, capped authenticated expiry plus over-batch projection/revoke-resend denial/create-duplicate blocking, and anonymous pre-validation cleanup; decide whether 90-day hard delete preserves sufficient evidence/holds. No automatic implementation issue is created. |
| Session/family tombstone retention | #338/#1059/#724; `auth-session-security` | Define bounded lineage/hash retention and later cleanup. Manual security/privacy/destructive gates. |
| Audit retention/read/export | #724/#774/#973/#784/#785; separate auth runtime, contract, clients and retention lanes | Define clocks/holds/read authorization/redaction/disposal and reconcile non-relational failed-bundle audit deletion with exact-correlation selection and loss-of-evidence accounting. Manual privacy/destructive gates. |
| Canonical doc/contract drift | `REC-AUTH-DOC-DRIFT-001` and `REC-AUTH-CONTRACT-DRIFT-001` | Preserve #965's separate docs decision and OpenAPI/generated-client tasks; manual security gate where required. |
| Security notifications | #369/#973 | Consume only source-owned events after lifecycle decisions; do not infer events from rows or UI. |

No new GitHub issue is required to complete #721. #717 and #716 remain open;
#961 remains a later synthesis consumer; #975 and #946 remain inactive. This
document does not start #718, #719, #720, #961, or any #965 recommendation.

## 10. Non-Goals And Stop Conditions

This policy does not implement account disable/re-enable, identity unlink,
role mutation, credential/factor material erasure, retention cleanup, purge,
notifications, audit read/export, provider delivery, public/admin exposure,
session handoff, MFA sign-in binding, or any other runtime behavior. It does
not approve schemas/migrations, OpenAPI/generated clients, UI/Figma, secrets,
configuration, deployment, destructive data work, or production operations.

Stop future work when a source conflict is unresolved; an owner/admin could be
locked out; reusable terminal authentication material would be retained merely
for history; a purported orphan is not positively non-authoritative; a purge
lacks retention/hold/dependency/copy proof; or a required security, privacy,
schema, contract, UI, provider, deployment, exposure, or destructive manual
gate is pending.

## 11. Validation And Acceptance Contract

This document must retain exactly 180 unique `AUTH-LC-*` transition rows and 13
unique `AUTH-LC-CHOICE-*` records, all required record families, valid relative
links, exact source/test/symbol existence,
secret-material exclusions, one-path scope, no runtime diff, shared-template
field coverage, strong independent review, local auth/security factual review,
exact-head CI/GitHub review, and zero unresolved actionable threads. Any source
change invalidates earlier candidate-bound validation and both fresh reviews.
