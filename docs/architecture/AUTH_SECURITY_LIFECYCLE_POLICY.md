# Auth And Security Lifecycle Policy

Issue: [#721](https://github.com/tommytang213/Settleora/issues/721)

Parent chain: [#716](https://github.com/tommytang213/Settleora/issues/716) →
[#717](https://github.com/tommytang213/Settleora/issues/717) → #721; shared
template: [#960](https://github.com/tommytang213/Settleora/issues/960).

## 1. Decision And Authority

This document defines Day 1 lifecycle wording and retention posture for
server-mode authentication and security records. It applies the
[shared lifecycle taxonomy](../planning/DAY1_LIFECYCLE_POLICY_TEMPLATE.md) to
the current repository at `a93409c306133ef7b9c4d1861f36f69f724b011e` (tree
`956339fed075713be12f3d74c0742eb2cc4414ac`). It is policy and factual
reconciliation only: it does not change runtime behavior, persistence,
contracts, clients, configuration, providers, exposure, or data.

API/domain auth services remain authoritative for server-mode account,
identity, role, credential, factor, challenge, session, policy, and audit
writes. A client label or generated method never proves authorization or an
accepted transition. Local-only mode has no server auth account/session
records: device biometric, app-PIN, and local secure-storage lifecycle belongs
to a separate local-device security owner. Nothing here turns local profile
state into a server identity.

The original #721 wording is stale where it calls passkeys, TOTP/MFA, recovery
codes, and challenges “future” families. The merged
[auth completeness audit](../qa/AUTH_DAY1_COMPLETENESS_AUDIT.md) and current
source prove persisted records and substantial API runtime for each. Older
architecture text is retained as historical design evidence only where it
conflicts with current source. `REC-AUTH-DOC-DRIFT-001` remains the owner for
reconciling those canonical documents and the unresolved immediate one-time
response rules.

## 2. Vocabulary And User-Visible Wording

- **Revoke** means make a grant, credential, factor, session, invitation, or
  lineage unusable. It does not mean erase the row or its evidence.
- **Disable** means block eligibility under a reversible administrative or
  policy condition. It is not automatically revoke and never implies that
  existing sessions were invalidated.
- **Expire** means validity ended at a server-controlled time or policy limit.
  It is not an actor-initiated revocation and is not a purge trigger.
- **Rotate / replace** means create new authority or verifier state and make
  the predecessor unusable. It does not erase predecessor evidence.
- **Unlink** means end an identity-provider relationship. It is not account
  disablement, profile deletion, or historical-reference removal.
- **Retain** means preserve the minimum justified record or safe metadata for
  enforcement, replay detection, investigation, or audit. A stored hash is not
  automatically safe or necessary to retain indefinitely.
- **Reactivate / re-enable** is a new authorized transition after current
  policy, recovery, dependency, and lockout checks. It is not silent restore.
- **Retention-expired** means a retention condition ended. It does not execute
  hard deletion or prove disposal eligibility.
- **Purge / hard delete** is a separate irreversible disposition action. No
  ordinary account-security control should be labelled “Delete” when the
  actual effect is revoke, disable, replace, or unlink.

Approved presentation verbs when a client later exposes the corresponding
currently implemented API actions are
“Sign out”, “Sign out all sessions”, “Revoke session”, “Change password”,
“Reset password”, “Revoke passkey”, “Cancel authenticator setup”, “Revoke
authenticator”, “Generate new recovery codes”, and “Revoke recovery codes”.
“Remove passkey/factor” is acceptable only when adjacent copy says the
credential is revoked and security history may remain. Account administration
must use “Disable account” and “Re-enable account”; identity administration
must use “Unlink identity”; role administration must name the exact role
assignment or removal.

## 3. Secret-Material Boundary

Lifecycle history must never preserve reusable authentication material merely
to prove that an event happened. Documentation, logs, audit, notifications,
metrics, traces, reports, examples, and later metadata/read APIs must never
contain plaintext or reversible passwords; raw access or refresh credentials;
raw password-reset or invitation material; raw recovery codes; submitted OTPs;
TOTP seeds, provisioning URIs, or QR payloads; passkey private material; raw
WebAuthn assertions or reusable challenges; provider tokens/payloads; or
private signing, encryption, pepper, or data-protection keys.

Current persistence uses password verifiers, purpose-bound token/material
hashes, challenge verifiers, recovery-code salted verifiers, protected TOTP
payloads, and passkey public material. Those values remain security-sensitive.
Their existence does not authorize indefinite retention. In particular:

- a revoked password verifier may not be kept merely as lifecycle evidence;
- a revoked/disabled TOTP factor must not retain decryptable reusable secret
  material merely for history;
- terminal reset/invitation hashes and recovery-code verifiers need an explicit
  enforcement or replay-detection purpose and bounded retention decision;
- passkey public keys and credential lookup hashes are not private keys, but
  their terminal retention still needs a bounded policy; and
- access/refresh hashes may be retained only as long as a defined lookup,
  revocation, replay, or investigation purpose requires them.

Immediate successful issuance/display responses are transport boundaries, not
lifecycle evidence. Current source returns access/refresh credentials once,
TOTP setup material during enrollment start, and recovery codes once at batch
generation. Canonical documents conflict over those response rules. This
policy neither expands nor resolves that behavior; `AUTH-LC-CHOICE-012` keeps
the manual security decision open.

## 4. Row Contract And Common Rules

The two matrices in section 5 together form each policy row. These common
values apply to every row unless its cell states otherwise, satisfying the
shared-template fields without repeating unsafe boilerplate:

- **Authority mode/workspace:** server mode, within one self-hosted workspace.
- **Authoritative boundary:** the API/domain auth service named by current
  source; clients and workers cannot accept transitions.
- **Actor/authorization:** current authenticated account for self-service and
  owner/admin or system only where explicitly designed. Anonymous protocol
  callers are separately bounded: local sign-in presents an identifier and
  password proof; password-reset initiation presents an identifier but must
  receive an enumeration-resistant no-authority result; refresh presents a
  refresh credential; invitation/reset completion presents its purpose-bound
  material; passkey sign-in and MFA challenge completion present a
  server-issued challenge plus the required authenticator proof. Current MFA
  challenge issuance/completion does not prove the intended anonymous
  pending-flow binding described in `AUTH-LC-CHL-008`; therefore the bounded
  scope is a requirement, not current credit. Protocol proofs authorize only their exact
  protocol transition; they are not a current-account actor, and the server
  must enforce the persisted account/session/purpose/credential/factor binding.
  Possession of an ID, route, or generated method is not authorization.
- **Preconditions:** authoritative current state, account availability, target
  ownership, policy eligibility, fresh step-up where the current factor runtime
  requires it, and a persistence transaction/concurrency guard where the
  transition can race. Unimplemented administration also requires lockout and
  last-owner proof.
- **Ordinary read/mutation effect:** terminal security authority is excluded
  from authentication use. Retained rows may remain available only to an
  authorized bounded security/audit read; ordinary clients must not infer
  authority from cached status.
- **Persisted lifecycle metadata:** stable subject ID, bounded prior/new status,
  applicable created/issued/updated/expiry/consumed/revoked/disabled/replaced
  timestamps, safe actor/reason/correlation fields, and a concurrency/version
  mechanism where a future mutation is added. Absence of one of these fields
  in current source is an implementation gap, not permission to invent it.
- **Idempotency/replay/concurrency:** current endpoint behavior is credited only
  where source/tests prove it. Otherwise the retry result and conflicting
  transition outcome are unresolved and the future mutation is blocked.
  Replaying one-time material never restores usability.
- **Dependency checks:** account state, role/last-owner safety, remaining sign-in
  methods, current and family-linked sessions, factors/recovery, policy version,
  audit, notifications, and backup/export/replica disposition where relevant.
  No current source proves complete backup/export/replica disposition.
- **Hard-delete/purge:** `ineligible` for ordinary user/admin actions.
  `unresolved` for a separate retention cleanup; it requires explicit target
  classification, retention and hold proof, all dependency/copy checks,
  authority, audit, retry/concurrency safety, consequence warning and explicit
  confirmation when user-initiated, plus every destructive/privacy/security
  manual gate. Retention expiry alone is insufficient.
- **Audit:** where applicable, record success and security-relevant denied,
  blocked, expired, replay, and conflict outcomes with actor/account ID when
  safely resolved, subject type/ID, action, bounded prior/new status, reason
  category, UTC timestamp, correlation/request/idempotency metadata, and no
  secret or private payload. The matrices do not claim an event exists unless
  implementation evidence says so.
- **Client presentation:** refresh server state after mutation; show precise
  lost-usability and session impact; preserve generic public failure shapes;
  never claim deletion, restoration, delivery, or notification that the server
  did not prove; expose a blocked/unavailable state rather than deciding policy
  locally.
- **Manual gates:** policy/runtime, schema/migration, OpenAPI/generated clients,
  platform UI/Figma, provider/configuration, public/admin exposure, destructive
  disposal, and privacy gates remain separate. A satisfied docs review does not
  satisfy any implementation gate.
- **Runtime acceptance:** future work needs focused service/endpoint tests,
  transaction and concurrent/replay tests, authorization/lockout tests,
  redaction/no-secret scans, OpenAPI/generated parity when applicable, client
  state tests, and manual hostile/provider/device/exposure evidence where the
  lane requires it. This policy is never runtime proof.

## 5. Lifecycle Transition Matrix

### 5.1 Transition, Authority, And Usability

| Row ID | Record family and user-visible action | Internal transition and actor | Authentication / authorization / session effect | Reversibility, dependency checks, implementation evidence |
| --- | --- | --- | --- | --- |
| `AUTH-LC-SES-001` | Current access session — **Sign out** | Current account requests `active → revoked`; `AuthSessionRuntimeService` executes with `user_sign_out`. | The selected access credential and family authority observed by the revoke transaction stop. If refresh rotation overlaps revocation after its selections, the replacement session can be inserted outside those sets; session validation does not consult family status, so its access credential can remain usable. Other independent families remain usable. Roles are unchanged. | Irreversible for revoked rows; sign in creates new authority. Implemented and idempotent for the post-validation already-revoked race, but rotation-versus-revoke authority is partial under `AUTH-LC-CHOICE-010`. Evidence: `SignOutEndpoints`, `AuthSessionRuntimeService`, `SessionRevocationEndpointTests`. Owner #338. |
| `AUTH-LC-SES-002` | Selected owned access session — **Revoke session** | Current account requests an owned session `active → revoked`; not-owned/missing/already-terminal targets use a non-disclosing unavailable result. | Target access credential and observed linked family authority stop working; an overlapping refresh rotation can insert a replacement session outside the selected revoke sets, and that access credential can remain usable. Unrelated families remain usable. Authorization records are unchanged. | Irreversible for revoked rows; reauthentication creates new authority. Implemented sequentially; target ownership and current-session self-revoke are tested, while rotation-versus-revoke is partial under `AUTH-LC-CHOICE-010`. Owner #338/#774. |
| `AUTH-LC-SES-003` | Account sessions — **Sign out all sessions** | Current account requests each unexpired active access session selected by `RevokeActiveSessionsForAccountAsync` `→ revoked`; the service revokes refresh families discovered through credentials linked to those selected sessions. | Selected bearer sessions and discovered family continuity stop. A valid refresh credential whose only linked access session is already expired is not discovered; overlapping rotation can also insert a replacement session after selection, and session validation does not consult the revoked family. Either access authority can remain usable. Account, identities, password/factors, and roles remain. | Irreversible for revoked rows; later sign-in is possible. Implemented but cascade/concurrency completeness is partial under `AUTH-LC-CHOICE-010/013`; owner #338. |
| `AUTH-LC-SES-004` | Access session — not user-initiated: **Expired** | Server validation observes `ExpiresAtUtc` reached; the row can remain stored even if its status is still `active` until another path materializes status. | Credential cannot authenticate or authorize; expiry does not revoke unrelated sessions or change roles. | Irreversible for that session. Source-defined validity from `Settleora:Auth:Sessions`; no expiry cleanup exists. Owner #338/#1059. |
| `AUTH-LC-SES-005` | Access session/refresh family/refresh credential — **Sign in** with local password | After local identity/account/password verification, `AuthRefreshSessionRuntimeService` atomically creates an active access session, active family, and active refresh credential and returns both raw credentials only at the immediate response boundary. Current local sign-in does not require the MFA challenge flow. | Returned credentials authenticate/refresh until expiry, rotation, or revocation; roles are read from current assignments. Retry creates independent authority. If verification of an old password overlaps change/reset, issuance can occur after those workflows selected revocation targets, leaving a new old-proof-derived family usable. | Issuance is not reversible; revoke/expire/rotate are separate. Implemented sequentially; credential-replacement-versus-issuance serialization is partial under `AUTH-LC-CHOICE-002/010`, MFA binding remains `REC-AUTH-MFA-SIGNIN-001`, and immediate-material policy remains `AUTH-LC-CHOICE-012`. Owner #338/#339. |
| `AUTH-LC-SES-006` | Access-only session — passkey sign-in reports **Signed in** | Successful passkey completion calls `AuthSessionRuntimeService.CreateSessionAsync`, which persists an active access session, but the completion response omits `RawSessionToken` and the endpoint sets no cookie/header. | The new record exists, but the caller receives no bearer credential and therefore cannot use that session for a later authenticated request. No refresh family is created. | The record can expire/revoke but issuance is unusable to its caller. Current source behavior is incomplete under `REC-AUTH-PASSKEY-SESSION-001`; do not present successful authenticated handoff. Owner #394/#338. |
| `AUTH-LC-SES-007` | Active access session — not separately user-visible: **Validate/use session** | A successful authenticated request validates the bearer, account availability, and session time/status, then updates `LastSeenAtUtc`/`UpdatedAtUtc`, writes validation audit, and returns the current actor/roles. Family status is not consulted. | The request may proceed with current roles; validation does not extend expiry or create refresh authority. Concurrent validation can load `active` before revocation and later overwrite timestamps/audit after revoke; out-of-order validations can regress last-seen metadata. | Use is repeatable, not restore/reactivation. Implemented sequentially; validation-versus-revoke ordering, monotonic last-seen, and family-aware denial are partial under `AUTH-LC-CHOICE-010`. Owner #338/#774. |
| `AUTH-LC-REF-001` | Refresh credential — not user-visible: **Rotate** | Valid `active` credential is consumed as `rotated`, linked to a new credential/session in the same active family; the relational branch uses a status-qualified update inside a transaction. | Old refresh material cannot refresh; new access/refresh authority is usable. Rotation does not extend family absolute expiry or change roles. | Irreversible for predecessor; retry with predecessor is replay, not success replay. Implementation contains the relational guard, but focused tests use EF in-memory and do not prove the relational transaction/race branch. Owner #338; future sender constraint #1059. |
| `AUTH-LC-REF-002` | Refresh credential/family — user sees generic **Sign in again** after detected reuse | Reuse of rotated/consumed/revoked material marks the linkable family `replayed` and revokes the active sessions/credentials observed by that replay transaction. Unknown material stays generic without a resolved subject. | Observed/selected family-linked access sessions and refresh credentials become unusable. An overlapping rotation can insert replacement authority after those selections, and access-session validation does not consult family status, so the new access credential can remain usable. Unrelated families remain unless separately revoked. | Irreversible for rows successfully transitioned. Implemented sequentially for linkable replay, but replay-versus-rotation family authority is concurrency-unsafe under `AUTH-LC-CHOICE-010`; abuse/notification consumers remain separate. Owner #338. |
| `AUTH-LC-REF-003` | Refresh credential/family — user sees generic **Sign in again** after expiry | Server time observes credential idle/absolute expiry or family absolute expiry and materializes expired family/credential plus revoked family-linked access authority under the matched path. | Identified family continuity stops; unrelated families and role assignments remain. | Irreversible for that family. Implemented; no cleanup/retention period follows from expiry. Owner #338/#1059. |
| `AUTH-LC-PWD-001` | Local password credential — **Change password** | Current account proves current password; API replaces verifier fields in the same active credential row. This is replacement, not credential revocation. Two concurrent requests can both verify the same predecessor and report success while the last committed verifier determines which new password works. | New password from the last writer works; the other concurrently acknowledged password may fail. Current flow preserves the current session and invokes account-session revocation excluding it. Only selected unexpired sessions and families discovered through them are candidates; moreover, if the excluded current session shares a discovered family with another selected session, that whole family is removed from the revocation candidates and its active refresh credential can remain usable. The limitations in `AUTH-LC-CHOICE-002/013` apply. Roles/identity link remain. | Not reversible; user may change again. Implemented sequentially, with replacement concurrency and cascade/client surface partial. Evidence: `AuthCredentialWorkflowService`, `CurrentAccountPasswordChangeService`, `CurrentAccountPasswordChangeEndpointTests`. Owner #339/#338/#774. |
| `AUTH-LC-PWD-002` | Local password/reset material — **Reset password** | A valid pending reset request is consumed and API replaces the active password verifier, revokes other outstanding reset requests, and invokes account-session revocation without excluding a session. Concurrent completions can both read the same pending request and replace the same credential; both can report success while the last committed verifier wins. | After an uncontested commit, the new password works and old password fails. Under the concurrent race, an earlier acknowledged new password can fail. Selected unexpired access sessions and refresh families discovered through them stop; refresh authority linked only through an already-expired access session can remain usable under `AUTH-LC-CHOICE-013`. Identity and role assignments remain. | Not reversible; a later reset creates new material. Implemented sequentially with consume-and-replace concurrency and cascade partial under `AUTH-LC-CHOICE-002/007/013`; delivery default-disabled. Owner #339/#338/#773. |
| `AUTH-LC-PWD-003` | Local password credential — **Disable credential** | Proposed reversible administrative `active → disabled`; schema supports the target, but no current endpoint/service performs it. | Verification refuses disabled credentials. Current reset issuance, reset account resolution, and `ResetLocalPasswordAsync` also require an active credential, so password reset cannot recover a disabled password. Existing-session and alternate-method effects remain unresolved. | Conditionally re-enableable only through a separately authorized recovery/re-enable path; current reset is not that path. Unimplemented and blocked by `AUTH-LC-CHOICE-002`/last-method lockout proof. Owner #788/#339. |
| `AUTH-LC-PWD-004` | Local password credential — **Revoke credential** | Proposed terminal `active/disabled → revoked`; schema supports the target, but no current endpoint/service performs it. | Verifier stops future password sign-in; exact existing-session and fallback effects are unresolved. | Irreversible for that verifier; replacement requires a distinct proofed workflow. Blocked by `AUTH-LC-CHOICE-002`. Owner #788/#339. |
| `AUTH-LC-PWD-005` | Local password credential — bootstrap **Create password** | Setup-only bootstrap passes plaintext to the credential workflow, which persists only a one-way verifier and safe algorithm parameters for the newly created first-owner account. | Password sign-in becomes available for that account; no session is issued by bootstrap. | Credential creation rolls back with the bootstrap transaction on failure; later change/reset/revoke are distinct. Implemented, but relational concurrency evidence follows the bootstrap gap in `AUTH-LC-CHOICE-001`. Owner #339/#785. |
| `AUTH-LC-PWD-006` | Local password credential — invitation **Create password** | A valid invitation accept supplies plaintext to the credential workflow, which persists only a one-way verifier and safe parameters for the newly created invited account. | Password sign-in becomes available; invitation acceptance does not issue a session. | Credential/account/identity creation is intended to roll back together on failure; relational acceptance behavior lacks focused tests. Implemented sequentially under `AUTH-LC-CHOICE-008`. Owner #339/#784. |
| `AUTH-LC-PWD-007` | Local password credential — admin **Create local user password** | An authenticated owner/admin passes plaintext to the credential workflow during guarded local-user creation; only a one-way verifier and safe parameters are persisted. | Password sign-in becomes available for the created account; provisioning issues no session. | Creation is intended to roll back with the account bundle. The relational transaction is source-defined; non-relational compensation physically removes only rows created by the failed workflow. Implemented and endpoint-tested; relational rollback/race proof remains partial under `AUTH-LC-CHOICE-001/002`. Owner #339/#785. |
| `AUTH-LC-PWD-008` | Active local password credential — not separately user-visible: **Upgrade password protection** | During successful password verification, when current hashing policy requires rehash, `VerifyLocalPasswordAsync` replaces the verifier plus algorithm/version/parameters and clears `RequiresRehash`; the submitted password and predecessor verifier are not retained as history. | The same password remains usable under upgraded protection; no session, role, identity, or account state changes solely because of rehash. | Replacement is not rollback or credential revocation. Implemented sequentially; concurrent verify/change/reset/rehash last-writer behavior and missing dedicated lifecycle audit are partial under `AUTH-LC-CHOICE-002`. Owner #339. |
| `AUTH-LC-PWD-009` | Active local password credential — not separately user-visible: **Verify password** | Successful `VerifyLocalPasswordAsync` updates `LastVerifiedAtUtc`/`UpdatedAtUtc` and emits credential-verification audit even when rehash is unnecessary; the submitted password is not retained. | Verification is proof for the exact calling workflow only; it creates no session itself and does not change roles, identity, or account status. A later sign-in issuance is a separate transition. | Repeatable proof, not replacement/reactivation. Implemented sequentially; verify-versus-change/reset/disable/rehash ordering and out-of-order last-verified metadata are partial under `AUTH-LC-CHOICE-002`. Owner #339. |
| `AUTH-LC-ACC-001` | Auth account — **Disable account** | Proposed administrative `active → disabled`; schema supports status/`DisabledAtUtc`, but no mutation runtime exists. | Session validation, primary sign-in/refresh, reset completion, and mapped passkey paths reject unavailable accounts, but anonymous TOTP/recovery challenge verification does not reload account availability after challenge issuance. Whether disable must transactionally revoke sessions/material and invalidate outstanding challenges is unresolved; roles remain assignments but are ineligible while account is disabled. | Conditionally reversible only through explicit re-enable. Requires last-owner and alternate-auth recovery proof. Blocked by `AUTH-LC-CHOICE-001`; owner #788/#785. |
| `AUTH-LC-ACC-002` | Auth account — **Re-enable account** | Proposed authorized `disabled → active` after current policy and recovery checks; no runtime exists. It must not clear audit or silently restore credentials/sessions/factors. | Account eligibility may return, but each identity, credential, factor, role and session must be independently revalidated; old revoked/expired authority stays unusable. | Conditional, not restore. Blocked by `AUTH-LC-CHOICE-001`; owner #788/#785. |
| `AUTH-LC-ACC-003` | Auth account — **Delete account** | Proposed account-level deletion distinct from disablement, identity unlinking, and profile deletion. Persistence has `DeletedAtUtc` and current auth consumers treat that marker as unavailable, but no authorized mutation/cascade exists. | Future sign-in/refresh/account use must stop. Exact effects on sessions, credentials/factors, identities, roles, invitations/reset material, profile/domain data, evidence, and copies are unresolved; no physical deletion is authorized. | Re-entry/restore is undefined and must not be promised. Blocked by `AUTH-LC-CHOICE-001`; manual auth, owner-lockout, privacy, schema, and destructive gates. Owner #788/#785/#724. |
| `AUTH-LC-ACC-004` | Auth account — setup-only **Create first owner** | Anonymous bootstrap may create the first active account only while the account table is empty; relational source uses a serializable transaction plus table lock and creates the linked profile, local identity, password credential, and owner/admin/user assignments with audit. | Establishes first-owner authentication/authorization but issues no session. Bootstrap becomes unavailable once an account exists. | Creation is not a reactivation. Intended atomic rollback is source-defined; relational race coverage remains incomplete under `AUTH-LC-CHOICE-001`. Owner #785/#339. |
| `AUTH-LC-ACC-005` | Auth account — invitation **Create invited user** | A valid pending invitation authorizes creation of an active account plus linked profile, local identity, local password credential, and bounded `user` role within the acceptance transaction. | Establishes later local-password eligibility but issues no session and grants no owner/admin role. | Creation is not reactivation. Source has a serializable relational branch/compensation, but focused tests are in-memory; concurrency/rollback evidence is partial under `AUTH-LC-CHOICE-008`. Owner #784/#339. |
| `AUTH-LC-ACC-006` | Auth account — admin **Create local user** | An authenticated owner/admin creates one active account plus profile, local identity, password credential, bounded `user` role, and creation audit. Relational source uses a serializable transaction; the in-memory/non-relational fallback compensates a failed bundle by deleting the positively identified rows it just created. | Establishes future local-password and `user`-role eligibility but issues no session and grants no owner/admin role. | Creation is not reactivation. Compensation is temporary failed-workflow cleanup, not general account deletion authority. Endpoint tests do not prove the relational race/rollback branch; `AUTH-LC-CHOICE-001` remains open. Owner #785/#339. |
| `AUTH-LC-ID-001` | Provider identity link — **Unlink identity** | Proposed relationship disable/unlink; schema has `DisabledAtUtc` but no unlink runtime. Local and OIDC links must not be conflated. | Stops future resolution through that identity only. It must not disable/delete account/profile, erase historical subject linkage, remove roles, or silently revoke other sign-in methods. Session effect is unresolved. | Re-link is a distinct proofed transition, not restore. Must preserve at least one safe owner/admin sign-in path. Blocked by `AUTH-LC-CHOICE-003`; owner #787/#773/#785. |
| `AUTH-LC-ID-002` | Local identity link — bootstrap **Create first-owner identity** | Setup-only bootstrap creates a local identity bound to the new first-owner account and normalized identifier inside the bootstrap transaction. | Enables later local identity resolution; it is not a session or provider credential. | Creation rolls back with bootstrap failure; collision/concurrency behavior depends on relational constraints/lock and lacks complete race evidence. `AUTH-LC-CHOICE-001/003`; owner #785/#787. |
| `AUTH-LC-ID-003` | Local identity link — invitation **Create invited identity** | Valid invitation acceptance creates a local identity bound to the new account using the invitation's normalized contact identifier. | Enables later local identity resolution; it neither signs in the caller nor links an OIDC provider. | Intended rollback is coupled to acceptance; focused relational collision/concurrency evidence is absent. `AUTH-LC-CHOICE-003/008`; owner #784/#787. |
| `AUTH-LC-ID-004` | Local identity link — admin **Create local-user identity** | Guarded owner/admin provisioning creates a local identity bound to the new account after a preflight and in-transaction normalized-identifier check. | Enables later local identity resolution; it is not a session or provider credential. | Relational creation/rollback is source-defined, while endpoint tests do not prove the serializable collision branch; non-relational failed-workflow compensation removes only the just-created link. `AUTH-LC-CHOICE-001/003`; owner #785/#787. |
| `AUTH-LC-ID-005` | Provider identity link — **Link external identity** | Proposed default-disabled OIDC/provider-proof transition creates a link from stable issuer/provider subject to an existing or newly authorized account; no current OIDC runtime implements it. | May enable future provider identity resolution only after current account, collision, takeover, and remaining-method checks; it must not silently merge profiles, grant roles, or create a session outside the approved sign-in ceremony. | Link is a new relationship, not reactivation; unlink/relink are distinct. Unimplemented and blocked by `AUTH-LC-CHOICE-003`; owner #787/#773/#785. |
| `AUTH-LC-ROL-001` | Product role assignment — bootstrap **Assign first-owner roles** | Setup-only bootstrap atomically creates exact `owner`, `admin`, and `user` assignments for the first account under the empty-system authority. | Establishes first-owner authorization after server revalidation; it does not issue a session. | Creation rolls back with bootstrap failure; later change/removal is distinct. Implemented with relational race proof partial under `AUTH-LC-CHOICE-001/004`; owner #785/#464/#465. |
| `AUTH-LC-ROL-002` | Product role assignment — **Change [old role] to [new role]** | Proposed atomic remove-and-assign or replacement semantics; current source defines neither the transition nor history shape. | Future authorization changes; current-claim/session refresh and last-owner effects are unresolved. | Not a silent restore; blocked by `AUTH-LC-CHOICE-004`. Owner #785/#464/#465. |
| `AUTH-LC-ROL-003` | Product role assignment — **Remove [role]** | Proposed authorized removal/logical terminal transition; current row has no removal metadata and no mutation API. | Future role eligibility stops after authoritative revalidation; authentication credentials do not thereby become revoked. | New assignment later is a new event. Last-owner/self-lockout and session behavior block implementation under `AUTH-LC-CHOICE-004`. |
| `AUTH-LC-ROL-004` | Product role assignment — invitation **Assign user role** | Successful invitation acceptance creates exactly the bounded `user` assignment for the new account under valid invitation-material authority. | Establishes later `user` authorization; it grants no owner/admin role and issues no session. | Creation is coupled to invitation acceptance and intended rollback; later removal is distinct. Implemented sequentially, with relational rollback/collision partial under `AUTH-LC-CHOICE-004/008`; owner #784/#785. |
| `AUTH-LC-ROL-005` | Product role assignment — admin **Assign created-user role** | Guarded owner/admin local-user provisioning creates exactly the bounded `user` assignment and records the provisioning actor. | Establishes later `user` authorization; it grants no owner/admin role and issues no session. | Creation rolls back transactionally or through failed-bundle compensation; later removal is distinct. Implemented and endpoint-tested, with relational rollback/race proof partial under `AUTH-LC-CHOICE-001/004`; owner #785/#464/#465. |
| `AUTH-LC-PKY-001` | Passkey credential — **Revoke passkey** (or **Remove passkey** with explanatory copy) | Current owner passes the active policy/actor-dependent step-up check and, when that check requires it, supplies fresh proof; service changes retained credential `enrolled → revoked` with actor/reason/correlation metadata. | Credential stops future enrollment lookup/sign-in/step-up use. Current code does not revoke existing sessions or other factors/roles. | Irreversible for that credential; enroll a new passkey. Implemented and idempotent for already revoked. Anti-lockout/session review remains `AUTH-LC-CHOICE-005`. Owner #394/#776; session handoff recommendation remains separate. |
| `AUTH-LC-PKY-002` | Passkey credential — **Disable passkey** | Proposed `enrolled → disabled`; schema supports the status, but current self-service runtime exposes revoke only. | Disabled credential must be excluded from authentication; session and remaining-factor effects are unresolved. | Conditional re-entry only by a separately approved transition. Unimplemented; `AUTH-LC-CHOICE-005`. Owner #394/#776. |
| `AUTH-LC-PKY-003` | Passkey credential — **Re-enable passkey** | Proposed `disabled → enrolled` after current account, policy, ownership, compromise, and recovery checks; no runtime exists. | May restore future credential use but never revives expired/revoked sessions or challenges. | Conditional, not restore of earlier authority. Unimplemented; `AUTH-LC-CHOICE-005`. Owner #394/#776. |
| `AUTH-LC-PKY-004` | Owned non-revoked passkey record — generic **Revoke passkey** endpoint | Current revoke accepts `pending`, `enrolled`, or `disabled` and changes it `→ revoked` after the policy/actor-dependent step-up check. | Target record stops future use; existing sessions, other credentials/factors, and roles remain. | Irreversible for that row. Implemented broad source-state behavior; whether the endpoint should be narrowed and its concurrent outcome are unresolved in `AUTH-LC-CHOICE-005`. Owner #394/#776. |
| `AUTH-LC-PKY-005` | Passkey credential — **Finish passkey setup** | Current authenticated owner completes a bound enrollment challenge; service verifies the ceremony, checks current account availability and observed credential-ID collision, creates an `enrolled` credential with public-key/lifecycle metadata, consumes the challenge, and writes enrollment audit. It does not revalidate passkey policy at completion. | The new credential becomes eligible for future passkey use even if policy changed after options issuance. Existing sessions, other factors, and roles remain. Later assertions update the credential signature counter, but concurrent out-of-order assertions can overwrite a higher committed counter with a lower value and miss replay suspicion because no conditional monotonic update/concurrency token exists. | Enrollment is not reversible; revoke is separate. Implemented sequentially, but completion-time policy, concurrent challenge completion, credential uniqueness, and assertion-counter monotonicity are partial under `AUTH-LC-CHOICE-005/006`. Owner #394/#776. |
| `AUTH-LC-PKY-006` | Owned passkey record — **Rename passkey** | Current owner passes the policy/actor-dependent step-up check and may replace `DisplayLabel`/`UpdatedAtUtc` on any owned record; loading does not restrict `pending`, `enrolled`, `disabled`, or `revoked` status. Audit records `passkey.renamed` with bounded metadata. | Label changes do not change authentication usability, credential status, sessions, factors, account, or roles. A terminal record remains terminal but its retained evidence metadata is mutable. | Repeat is last-writer metadata replacement, not reactivation. Implemented sequentially; allowed terminal source states, concurrent rename/revoke ordering, and evidence immutability expectations are unresolved under `AUTH-LC-CHOICE-005`. Owner #394/#776. |
| `AUTH-LC-TOTP-001` | Pending TOTP factor — **Cancel authenticator setup** | Current owner cancels `pending → revoked`. | Pending factor never authenticates; existing enrolled factors/sessions remain. | Terminal for that setup. Implemented; terminal secret disposition remains `AUTH-LC-CHOICE-005`. Owner #394/#776. |
| `AUTH-LC-TOTP-002` | Pending TOTP factor — start replacement setup | Starting a new enrollment sequentially marks observed earlier pending rows `→ revoked`, then creates fresh pending material. | Observed earlier setup material stops working; enrolled factors/sessions remain. Concurrent starts can each observe no current pending row and create multiple pending factors. | Irreversible replacement of observed pending material; concurrency is partial under `AUTH-LC-CHOICE-005`. Owner #394/#776. |
| `AUTH-LC-TOTP-003` | Pending TOTP factor — **Setup expired** | Server verification observes the deadline, changes `pending → expired`, and saves through `TrySaveAsync`. | Expired setup cannot enroll/authenticate; other authority remains. | Terminal for that setup. Persistence is source-proven; no focused expiry or concurrency test was found. `AUTH-LC-CHOICE-005`; owner #394/#776. |
| `AUTH-LC-TOTP-008` | Pending TOTP factor — **Finish authenticator setup** | Current owner submits a valid OTP for the pending setup; service changes `pending → enrolled`, clears setup expiry, retains the protected TOTP secret for authentication, and writes enrollment audit. | The factor becomes eligible for TOTP verification. Existing sessions, other factors, recovery batches, and roles remain unchanged. | Enrollment is not reversible; disable/revoke is separate. Implemented sequentially, but concurrent verify-versus-cancel/revoke lacks an atomic source-status guard under `AUTH-LC-CHOICE-005`. Owner #394/#776. |
| `AUTH-LC-TOTP-004` | Enrolled TOTP factor — **Disable authenticator** | Proposed `enrolled → disabled`; schema supports the status, but current self-service runtime exposes revoke only. | Disabled factor must not authenticate. Exact session, recovery, and policy-compliance effects are unresolved. | Conditional re-entry only after approved checks. Unimplemented; `AUTH-LC-CHOICE-005`. Owner #394/#465/#776. |
| `AUTH-LC-TOTP-005` | Disabled TOTP factor — **Re-enable authenticator** | Proposed `disabled → enrolled`; no runtime exists. | May restore future TOTP use; must not revive sessions/challenges or bypass current policy. | Conditional, not silent restore. Unimplemented; `AUTH-LC-CHOICE-005`. Owner #394/#465/#776. |
| `AUTH-LC-TOTP-006` | Enrolled TOTP factor — **Revoke authenticator** | Current owner passes the policy/actor-dependent step-up check and, when required, supplies fresh proof; service changes retained factor `enrolled → revoked`. | Factor stops future TOTP authentication; current code does not revoke existing sessions, challenges, recovery batches, or other factors. | Irreversible for that factor; re-enroll creates new protected material. Implemented, but protected-secret disposition and anti-lockout/session response are unresolved under `AUTH-LC-CHOICE-005`. |
| `AUTH-LC-TOTP-007` | Owned non-revoked TOTP factor — generic **Revoke authenticator** endpoint | Current generic factor revoke accepts `pending`, `enrolled`, `disabled`, or `expired` and changes it `→ revoked` after the policy/actor-dependent step-up check; this overlaps pending cancellation with a different audit reason. | Target factor/setup stops any future use; sessions, challenges, recovery batches, and other factors remain. | Irreversible for that row. Implemented broad source-state behavior; whether the endpoint should be narrowed is unresolved in `AUTH-LC-CHOICE-005`. Owner #394/#776. |
| `AUTH-LC-TOTP-009` | Owned MFA factor record — **Rename authenticator** | Current owner passes the policy/actor-dependent step-up check and may replace `DisplayLabel`/`UpdatedAtUtc` on any owned factor; loading does not restrict `pending`, `enrolled`, `disabled`, `expired`, or `revoked` status. Audit records `mfa.factor_metadata_updated` with bounded factor type/outcome metadata. | Label changes do not change authentication usability, factor status, sessions, challenges, recovery batches, account, or roles. A terminal record remains terminal but its retained evidence metadata is mutable. | Repeat is last-writer metadata replacement, not re-enable/reactivation. Implemented sequentially; allowed terminal source states, concurrent rename/revoke ordering, and evidence immutability expectations are unresolved under `AUTH-LC-CHOICE-005`. Owner #394/#776. |
| `AUTH-LC-RCV-001` | Recovery-code batch/verifiers — **Generate new recovery codes** | With an enrolled TOTP factor and the policy/actor-dependent step-up result satisfied, old active batch `→ replaced`, unused verifiers `→ replaced`, then a new active batch is generated. An enrolled passkey alone does not satisfy the current prerequisite. | All unused old codes stop working; already consumed evidence remains consumed. Sessions, factors, roles are unchanged. | Irreversible for old batch. Implemented sequentially for TOTP-backed accounts; broader factor eligibility is unresolved, and concurrent generation is not guarded. Owner #775/#776. |
| `AUTH-LC-RCV-002` | One recovery-code verifier — not a management action: **Used** | Successful bound challenge changes one observed `unused → consumed` and decrements batch count through ordinary tracked updates. Two concurrent challenges can each read the same unused verifier and both commit success. | After an uncontested commit the code cannot satisfy another challenge; concurrent submissions can currently authorize two scoped outcomes. It does not re-enable/rotate a factor or alter session lifetime. | Irreversible after commit, but single-use is proven only sequentially. Atomic consume is partial under `AUTH-LC-CHOICE-005/006`; owner #775. Password-primary MFA binding remains `REC-AUTH-MFA-SIGNIN-001`. |
| `AUTH-LC-RCV-003` | Recovery-code batch — **Revoke recovery codes** | Current owner passes the policy/actor-dependent step-up result and changes batch `active → revoked` plus all unused verifiers `→ revoked`. | Revoked unused codes stop working. Current code does not revoke sessions or factors. | Irreversible for the batch; generate a new batch if policy permits. Implemented; verifier disposition remains `AUTH-LC-CHOICE-005`. |
| `AUTH-LC-RCV-004` | Recovery-code batch/verifier — **Expired** | Schema defines expiry states but current management runtime does not materialize a batch/code expiry transition. | Expired material must not authenticate; exact trigger and factor/session effects are unresolved. | Terminal for affected material. Unimplemented; `AUTH-LC-CHOICE-005`. Owner #775/#776. |
| `AUTH-LC-RCV-005` | Owned replaced, expired, or already-revoked recovery batch — generic **Revoke recovery codes** endpoint | Current service accepts any owned batch, unconditionally writes `→ revoked`, refreshes revoke/update/reason fields, revokes unused verifiers, and appends another audit event. | Any unused verifier is unusable; existing sessions/factors remain. | No state restoration, but repeated revoke is not state/audit idempotent. Implemented broad source-state behavior; desired guard is unresolved in `AUTH-LC-CHOICE-005`. Owner #775/#776. |
| `AUTH-LC-CHL-001` | MFA challenge — not user-visible: **Verified** | Successful MFA proof changes an accessible pending challenge `→ verified` and records consumption time. Anonymous verification loads by challenge ID/type without enforcing persisted account/session predicates; for TOTP it also does not persist/reject the accepted timestep, so one current code can satisfy separate pending challenges. | Intended scope may proceed, but the anonymous completion path does not prove pending-flow/account/session binding. Success never extends refresh lifetime. Cross-challenge TOTP proof can authorize multiple outcomes. | Irreversible per challenge. Implemented sequentially only after a challenge exists; anonymous binding, proof replay, and completion concurrency are partial under `AUTH-LC-CHOICE-006`; password-primary binding remains `REC-AUTH-MFA-SIGNIN-001`. |
| `AUTH-LC-CHL-002` | Passkey challenge — not user-visible: **Consumed** | Successful passkey ceremony changes `pending → consumed`. Policy support is checked when enrollment/sign-in/step-up options are created but is not revalidated by any of their completion paths. Sign-in creates an `AuthSession` but omits its raw token from the response, and the endpoint sets no credential cookie/header. | Challenge cannot be reused after a successful commit. Enrollment and step-up can complete despite intervening policy disable; passkey sign-in can likewise create a persisted session but gives the caller no bearer credential for a subsequent request. | Irreversible. Sequential ceremony behavior is implemented; usable sign-in handoff is absent, while every completion's concurrency and policy revalidation remain partial under `AUTH-LC-CHOICE-005/006` and `REC-AUTH-PASSKEY-SESSION-001`. |
| `AUTH-LC-CHL-003` | MFA/passkey challenge — **Expired** | Server time blocks verification after `ExpiresAtUtc`; preparation helpers set tracked `pending → expired`, but the verification callers return without saving that change, so the persisted row can remain `pending`. | No auth result; existing sessions/factors remain. | Terminal by validity clock, but durable status materialization is partial/unproven. Retention/status reconciliation is `AUTH-LC-CHOICE-006`. |
| `AUTH-LC-CHL-004` | MFA/passkey challenge — **Blocked** | Sequential attempt/policy checks can change a pending challenge `→ blocked`. Concurrent invalid attempts can each read the same count and overwrite it with the same increment because no atomic increment/concurrency guard exists. | No auth result once blocked; concurrent guesses can be undercounted and delay blocking. Any broader session response requires separate policy. | Terminal after a successful block commit. Implemented sequentially but concurrency-unsafe under `AUTH-LC-CHOICE-006`. |
| `AUTH-LC-CHL-005` | MFA/passkey challenge — **Failed** | Schema supports `failed`; current mapped services generally increment attempts and may block rather than consistently materialize this status. | No auth result. | Terminal when used; partial/unresolved by flow under `AUTH-LC-CHOICE-006`. |
| `AUTH-LC-CHL-006` | MFA/passkey challenge — **Cancelled** | Schema supports `cancelled`; no complete current cancellation transition was found. | No auth result; other authority remains. | Terminal; unimplemented and blocked by `AUTH-LC-CHOICE-006`. |
| `AUTH-LC-CHL-007` | MFA/passkey challenge — **Replay detected** | Schema supports `replay_detected`; current challenge services reject terminal reuse but do not consistently materialize this status. | No auth result; suspicion-driven family/session response is unresolved. | Terminal; partial/unresolved under `AUTH-LC-CHOICE-006`. |
| `AUTH-LC-CHL-008` | MFA challenge — **Start verification** | The route is anonymous and accepts `PendingAuthFlowId`, but the service ignores that field and rejects a missing authenticated actor. With an actor it creates an account/session-bound pending challenge after policy and factor/recovery checks. | Pre-session anonymous MFA issuance is unusable, so this cannot complete the intended sign-in handoff. Conversely, anonymous verification can load an existing challenge without its persisted account/session predicates. No challenge creation itself extends a session or consumes a factor/code. | Intended anonymous flow is broken; authenticated creation works sequentially but does not cure the completion binding gap. Duplicate pending, pending-flow authority, and races remain `AUTH-LC-CHOICE-006`. Owner #394 and `REC-AUTH-MFA-SIGNIN-001`. |
| `AUTH-LC-CHL-009` | Passkey enrollment challenge — **Start passkey setup** | Authenticated actor with available account/session and passkey policy support creates a pending enrollment challenge bound to account/session. | Creates ceremony state only; no credential exists until successful completion. | Expires by source clock; repeated calls create separate challenges. Implemented; completion-time policy and concurrency remain `AUTH-LC-CHOICE-005/006`. Owner #394. |
| `AUTH-LC-CHL-010` | Passkey sign-in challenge — **Start passkey sign-in** | Anonymous caller under enabled passkey policy creates a pending sign-in challenge; account binding is populated only when candidate credentials resolve to exactly one account. | Creates bounded ceremony state only and grants no session authority. | Expires by source clock; repeated calls create separate challenges. Implemented; identifier disclosure, duplicate pending state, completion handoff, and policy change remain with `AUTH-LC-CHOICE-005/006` and `REC-AUTH-PASSKEY-SESSION-001`. |
| `AUTH-LC-CHL-011` | Passkey step-up challenge — **Start verification** | Authenticated actor with bound account/session, bounded operation category, enrolled passkey, and policy support creates a pending step-up challenge. | Creates scoped ceremony state only; it does not itself satisfy step-up or alter session lifetime. | Expires by source clock; repeated calls create separate challenges. Implemented; completion policy/concurrency remains `AUTH-LC-CHOICE-005/006`. Owner #394. |
| `AUTH-LC-RST-001` | Password-reset request — not user-visible: **Replace/reset link** | Sequential issuance of newer material changes the pending requests it observed `→ revoked`, sets replaced metadata and links them to the replacement. Concurrent issuances can each miss the other's insertion and leave multiple pending requests. | Observed old reset material cannot reset the password; concurrent newly issued links can both remain usable. Account credential/sessions remain until successful completion. | Irreversible for replaced material. Implemented sequentially; issuance concurrency is partial under `AUTH-LC-CHOICE-007`. Raw material only crosses the immediate delivery boundary. Owner #339. |
| `AUTH-LC-RST-002` | Password-reset request — **Reset password** | Valid pending request `→ consumed`; see `AUTH-LC-PWD-002` for credential and session effects. | Consumed material is unusable; later reuse becomes suspicious replay with a generic public failure. | Irreversible. Implemented. Owner #339. |
| `AUTH-LC-RST-003` | Password-reset request — **Expired reset link** | Server observes `ExpiresAtUtc` and changes request `pending → expired` during a matched completion attempt. | Material cannot reset a password; account, credential, roles, identities and existing sessions are otherwise unchanged. | Terminal. Implemented lazily; no cleanup runtime. Owner #339. |
| `AUTH-LC-RST-004` | Password-reset request — generic **Link unavailable** | Reuse of consumed/revoked/replaced material changes matched request `→ suspicious_replay`. By contrast, account, identity, credential, or policy denial updates only check/update timestamps and audit; a still-pending request is not revoked. | No credential or session authority is granted for that attempt. Public response stays generic. Dependency-denied pending material can become usable again if the dependency recovers before expiry. | Terminal only for material changed to suspicious replay; dependency denial is non-terminal. Implemented in part; retry/guess limiting and disposition remain unresolved. Owners #339 and `REC-AUTH-ABUSE-001`. |
| `AUTH-LC-RST-005` | Pending password-reset requests — not user-visible: **Revoke outstanding reset links** | Successful reset completion changes other observed pending requests `→ revoked`; sequential newer issuance uses the distinct replacement transition in `AUTH-LC-RST-001`. Account/policy denial does not revoke them. | Revoked material cannot reset a password; it does not itself replace the credential. | Terminal for each revoked request. Implemented on successful completion; concurrency, trigger completeness, and retention remain `AUTH-LC-CHOICE-007`. Owner #339. |
| `AUTH-LC-RST-006` | Password-reset workflow — **Request password reset** | Anonymous caller submits an identifier to a bounded, enumeration-resistant endpoint. After abuse/policy and delivery-readiness handling, the orchestrator may resolve an eligible active local credential and invoke issuance/replacement; unknown or ineligible identifiers return the same public outcome without creating usable material. | The public response grants no authentication/session authority and must not reveal whether an account exists. Any pending request/material creation is separately governed by `AUTH-LC-RST-001`; raw material may cross only an approved immediate provider-delivery boundary. | A repeated request is a new attempt, not replay of success. Implemented with delivery default-disabled; abuse, provider truth, and issuance concurrency remain `AUTH-LC-CHOICE-007`. Owner #339 and `REC-AUTH-ABUSE-001`. |
| `AUTH-LC-INV-001` | Invitation — **Accept invitation** | Valid pending invitation is transactionally changed `→ accepted` after policy, secret, account, and credential checks. The acceptance service requires capability `enabled`; it ignores `PendingInviteGraceWhenDisabled`, even though the policy readout can report public acceptance enabled from that grace flag. Acceptance audit incorrectly writes the inviter as `ActorAuthAccountId` even though the actor is the anonymous bearer-material caller and the created account is already the subject. | Invitation becomes single-use; accepted account gets only the approved `user` role. The invitation is not a session and does not sign the user in. A disabled capability currently rejects existing pending invitations despite the grace readout. | Terminal for invitation; rollback logic restores pending only if account-creation persistence fails. Runtime is sequentially implemented, but grace, relational behavior, and actor attribution are partial under `AUTH-LC-CHOICE-008`; capability/provider default-off. Owner #784. |
| `AUTH-LC-INV-002` | Pending invitation — **Resend invitation** with ready requested delivery | When delivery is requested, readiness passes, and composition succeeds, the invitation remains `pending`; server commits a rotated secret hash before handing new raw material to delivery. | Prior link stops redeeming; only the newly generated material can redeem until expiry, even if its send later fails or is ambiguous. No account/session/role changes occur. | Old material is irreversibly replaced. Implemented sequentially and non-idempotent; send outcome remains separately truthful. Owner #784. |
| `AUTH-LC-INV-003` | Pending invitation — **Revoke invitation** | Authorized owner/admin changes `pending → revoked`, records actor/time, and sets current cleanup eligibility. The API accepts a reason, but service, persistence, and audit ignore it. | Link stops redeeming immediately. No accepted account, credential, session, identity, or role is removed. | Terminal. Implemented sequentially for status/usability but reason evidence is absent and revoke-versus-accept remains partial under `AUTH-LC-CHOICE-008`. Owner #784. |
| `AUTH-LC-INV-004` | Pending invitation — **Expired invitation** | Server time changes `pending → expired` lazily/on bounded lifecycle cleanup; public redemption remains generic. | Link cannot redeem; no account/session/role change. | Terminal. Implemented. Owner #784. |
| `AUTH-LC-INV-005` | Terminal invitation — not ordinary UI: **retention cleanup** | Current runtime physically deletes up to 50 accepted/revoked/expired rows after `CleanupEligibleAtUtc` and writes aggregate cleanup audit. Any well-formed anonymous invitation-acceptance request invokes this global cleanup before abuse, policy, or submitted-secret validation, including an invalid-secret request. | Deletion does not alter an already accepted account, but removes row-level invitation evidence. The trigger is public/protocol-driven even though cleanup is not presented as a user action. | Irreversible current behavior; not endorsed as settled Day 1 purge authority. Trigger authorization, holds/copies, and evidence sufficiency are blocked by `AUTH-LC-CHOICE-008`; owner #784/#724. |
| `AUTH-LC-INV-006` | Pending invitation — **Resend invitation** without ready requested delivery | When delivery is not requested, readiness fails, or composition is unavailable, the invitation stays `pending`, `UpdatedAtUtc` changes, delivery state is recorded, and the existing secret hash is not rotated. | Existing link remains redeemable until its original expiry; no new material is emitted and no account/session/role change occurs. | No credential lifecycle replacement occurs. Implemented and tested; client must show the returned non-sent delivery state and must not claim the old link was revoked. Owner #784. |
| `AUTH-LC-INV-007` | Invitation — **Create invitation** | Authorized owner/admin under enabled capability and bounded abuse/contact/role rules creates a pending invitation with lookup hash, seven-day expiry, delivery state, and creation audit; raw material crosses only the immediate optional delivery boundary after persistence. | The link may later be accepted, resent/replaced, revoked, or expired; creation grants no account/session/role authority. | Retry is a new request, while the pending-recipient uniqueness constraint can reject overlap; delivery-after-commit can be ambiguous. Implemented sequentially; relational uniqueness/race/provider truth remains `AUTH-LC-CHOICE-008`. Owner #784. |
| `AUTH-LC-POL-001` | General auth security policy — **Change security policy** | Schema supports draft/active/retired versions, but current `AuthSecurityPolicyService` is read/default focused and no general mutation API exists. | A future active version governs later eligibility; it must not silently delete factors, revive authority, or lock out owners. | Unimplemented; old versions must be retired/retained, not silently overwritten. `AUTH-LC-CHOICE-011`; owners #394/#465/#785. |
| `AUTH-LC-POL-002` | Invitation policy — **Change invitation policy** | Authorized mutation intends to retire the current active row and add a new active version. However, `LoadActivePolicyAsync` uses `AsNoTracking`, so retirement assignments are not persisted; with an existing active row the relational filtered unique index rejects insertion of the replacement, while in-memory behavior can retain multiple active rows. | Initial creation can govern invitation behavior, but a subsequent change cannot be credited as a successful replacement. The readout also treats disabled-plus-grace as publicly acceptable while acceptance requires capability `enabled`. Existing accounts/sessions are unchanged. | Initial creation works; replacement is incomplete, not implemented. Blocked by `AUTH-LC-CHOICE-008/011`; owner #784/#785. Requires tracked or conditional retirement plus relational replacement/version-race tests. |
| `AUTH-LC-AUD-001` | Auth audit events — not user-removable: **Retain security evidence** | Append bounded event evidence; later retention expiry is a condition, not deletion. No current audit purge/read/export runtime exists. | Audit rows never authenticate or authorize. Account/factor/session revocation must not erase their evidence. | Events are immutable evidence; correction is additive. Retention/disposal blocked by `AUTH-LC-CHOICE-009`; owners #721/#724/#774/#973. |

### 5.2 Per-Row Template Completion

The following evidence keys keep citations concise without conflating policy
authority with implementation. Each mapped key is part of that exact row; the
text before **Implementation** is decision authority, while the source/test
list after it is evidence only:

- `E-SESSION`: the shared template plus
  [credential/session design](AUTH_CREDENTIALS_SESSIONS_AUDIT_DESIGN.md).
  **Implementation:** `AuthSessionRuntimeService`,
  `AuthRefreshSessionRuntimeService`, and session,
  refresh-rotation, password-change, and password-reset endpoint tests.
- `E-IDENTITY`: the shared template plus
  [identity foundation](AUTH_IDENTITY_FOUNDATION.md).
  **Implementation:** `AuthAccount`, `AuthIdentity`, `SystemRoleAssignment`, current account/identity
  resolution, bootstrap/invitation source, and authorization tests.
- `E-FACTOR`: the shared template,
  [MFA/passkey architecture](AUTH_MFA_PASSKEY_ARCHITECTURE.md), and
  [MFA/passkey policy audit](AUTH_MFA_PASSKEY_POLICY_AUDIT.md),
  with unresolved choices in this policy controlling conflicts.
  **Implementation:** `PasskeyRuntimeService`, `MfaRuntimeService`,
  `AuthSecurityPolicyService`, and
  `AuthMfaPasskeySecurityRegressionTests`.
- `E-RESET`: the shared template and password-reset sections in
  [identity foundation](AUTH_IDENTITY_FOUNDATION.md) and
  [credential/session design](AUTH_CREDENTIALS_SESSIONS_AUDIT_DESIGN.md).
  **Implementation:**
  `LocalPasswordResetService`, its audit writers, and reset/redaction tests.
- `E-INVITE`: the shared template and
  [invitation policy audit](AUTH_INVITATION_POLICY_AUDIT_READINESS.md).
  **Implementation:**
  invitation management/acceptance/policy/cleanup services and focused tests.
- `E-AUDIT`: the shared template, the
  [Day 1 scope](../prd/MVP_DAY1_SCOPE.md), and `AUTH-LC-CHOICE-009`.
  **Implementation:** current audit model/writers and redaction tests.

Notation in this table is normative: `HD` is hard-delete eligibility and `P`
is purge/disposal eligibility. `HD=no; P=unresolved` means the lifecycle action
cannot delete the authoritative row and a later separate disposition remains
blocked. `retry=current` credits only the result described in section 5.1 and
current tests; otherwise retry and concurrent conflict are explicitly
`unresolved`. All retained evidence uses section 3's redaction boundary and
section 4's bounded audit fields. “Refresh” means refresh authoritative server
state and show the precise usability effect; it does not mean refresh-token
rotation. Compact `CHOICE-nnn` references in this table mean the exact stable
`AUTH-LC-CHOICE-nnn` record in section 6; “same gates” means the complete gate
set on the immediately preceding named row, not an omitted gate decision.

| Exact row | Decision authority / implementation evidence | Ordinary read, later mutation, and re-entry | Idempotency, replay, retry, concurrency | Retention, hard delete, purge | Audit, privacy, client | Choice; status; owner/lane; manual gates; runtime acceptance |
| --- | --- | --- | --- | --- | --- | --- |
| `AUTH-LC-SES-001` | Template + `E-SESSION`; current source/tested sequentially | Revoked observed session/family excluded after commit; overlapping rotation can leave a new usable session; new sign-in only | Already-terminal retry bounded and selected cascade transactional, but rotation can insert outside selected sets and validation ignores family status | Session class §5.3; HD=no; P=unresolved | Success/denial/family transition, no credential; do not promise every concurrent authority ended | `CHOICE-010`; partial authority race; #338/auth; `G-RET`,`G-PRIV`,`G-DEST`; relational rotation/revoke/validation tests |
| `AUTH-LC-SES-002` | Template + `E-SESSION`; current source/tested sequentially | Target/observed family excluded; overlapping rotation can leave a new usable session; unrelated authority remains | Missing/not-owned/terminal stays non-disclosing, but rotation can insert outside selected sets and validation ignores family status | Session class §5.3; HD=no; P=unresolved | Actor/target/outcome, no credential; refresh list without universal revoke claim | `CHOICE-010`; partial authority race; #338/#774 auth/client; `G-RET`,`G-PRIV`,`G-DEST`; ownership/relational rotation-race tests |
| `AUTH-LC-SES-003` | Template + `E-SESSION`; current source/tested sequentially | Selected unexpired sessions excluded; undiscovered refresh lineage or overlapping-rotation session may remain; new sign-in only | Zero-selected is success; transaction covers selected rows/families but not a concurrent replacement inserted after selection, and validation ignores family status | Session class §5.3; HD=no; P=unresolved | Actor/account/count/outcome, no credential; do not promise every refresh/access authority is revoked | `CHOICE-010/013`; partial lineage/authority race; #338; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; expired-linkage/relational rotation-race regressions |
| `AUTH-LC-SES-004` | Template + `E-SESSION`; current validation source | Exclude when expired even if stored status is active; new sign-in only | Repeated validation denies; status-materialization race unresolved | Session class §5.3; HD=no; P=unresolved | Expiry denial if safely attributable; generic client reauth | `CHOICE-010`; partial lazy state; #338/#1059; `G-RET`,`G-PRIV`,`G-DEST`; time/race/cleanup tests |
| `AUTH-LC-SES-005` | Template + `E-SESSION`; current local-sign-in source/tested sequentially | New session/family/refresh credential readable; later revoke/rotate/expire distinct; old-proof issuance can escape concurrent change/reset cascade | One transaction creates all new rows, but password verification is outside credential-version binding and account-session revocation selections; retry creates independent authority; MFA prerequisite absent | Session/refresh classes §5.3; HD=no; P=unresolved | Issue safe IDs/times/device only; raw access/refresh only immediate response, never audit | `CHOICE-002/010/012`; partial replacement race/MFA binding; #338/#339/`REC-AUTH-MFA-SIGNIN-001`; `G-AUTH`,`G-RET`,`G-PRIV`; old-password verify-versus-change/reset issuance, retry, MFA, redaction tests |
| `AUTH-LC-SES-006` | Template + `E-SESSION`; current passkey source/tested sequentially | Active session row exists but caller cannot use it because bearer is not returned/set; revoke/expire distinct | Create succeeds, but response drops raw token; retry can create more unusable-to-caller rows | Session class §5.3; HD=no; P=unresolved | Audit safe session/challenge IDs only, no raw/hash; do not show signed-in success | `CHOICE-005/010/012`; incomplete handoff; #394/#338/`REC-AUTH-PASSKEY-SESSION-001`; `G-AUTH`,`G-CONTRACT`,`G-CLIENT`,`G-RET`,`G-PRIV`; usable-credential/no-leak/retry tests |
| `AUTH-LC-SES-007` | Template + `E-SESSION`; current validation source/tested sequentially | Valid request receives current actor/roles; last-seen metadata changes; expiry unchanged; no refresh authority created | Status/account/time checked before ordinary tracked timestamp update; validation-versus-revoke can save metadata/audit after revoke, out-of-order use can regress last-seen, and family status is ignored | Session class §5.3; HD=no; P=unresolved | Validation outcome/session/account/time/correlation and bounded device only; never bearer/hash; no user-facing transition claim | `CHOICE-010`; partial concurrency/family validation; #338/#774; `G-AUTH`,`G-RET`,`G-PRIV`; relational validate-revoke/out-of-order last-seen/family-status tests |
| `AUTH-LC-REF-001` | Template + `E-SESSION`; current source, in-memory tests | Predecessor excluded after commit; replacement remains eligible; no predecessor re-entry | Relational code uses status-qualified consume/transaction, but current focused tests do not execute that provider branch; predecessor replay is sequentially tested, relational race proof unresolved | Refresh class §5.3; HD=no; P=unresolved | Rotation lineage/outcome, never raw/hash; client stores only immediate replacement boundary | `CHOICE-010/012`; partial evidence; #338/#1059; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; relational atomic-race/no-secret tests |
| `AUTH-LC-REF-002` | Template + `E-SESSION`; current source/tested sequentially | Replayed observed family authority excluded; overlapping rotation can insert a replacement access session outside selected sets and leave it usable | Linkable replay cascades and unknown stays generic sequentially, but replay-versus-rotation is authority-unsafe because validation ignores family status | Refresh class §5.3; HD=no; P=unresolved | Replay/family safe IDs and outcome; generic reauth without universal revoke promise | `CHOICE-010`; partial authority race; #338; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; linkable/unknown/relational replay-rotation/validation tests |
| `AUTH-LC-REF-003` | Template + `E-SESSION`; current source/tested | Expired family excluded; new sign-in only | Repeated attempt denies; matched family expiry cascade transactional | Refresh class §5.3; HD=no; P=unresolved | Expiry category, no material/hash; generic reauth | `CHOICE-010`; implemented; #338/#1059; `G-RET`,`G-PRIV`,`G-DEST`; clock/family tests |
| `AUTH-LC-PWD-001` | Template + `E-SESSION`; current source/tested sequentially | Last committed verifier remains active; concurrent acknowledged replacement can lose; current session remains; undiscovered refresh lineage or a family shared with the excluded current session may remain | Same-password/current-proof results defined, but credential update has no concurrency token/status-qualified guard; selected-session transaction does not serialize two replacement reads, and family discovery/exclusion makes cascade completeness partial | Password class §5.3; HD=no; P=unresolved | Change/denial/selected revoke; never old/new/verifier; do not promise both concurrent changes or every other refresh ended | `CHOICE-002/012/013`; partial replacement/cascade; #339/#338/#774; `G-AUTH`,`G-CLIENT`; proof/relational race/expired-linkage/shared-family/no-secret tests |
| `AUTH-LC-PWD-002` | Template + `E-RESET`; current source/tested sequentially | Last committed verifier active after success; an earlier concurrently acknowledged password can fail; reset requests/selected sessions excluded; undiscovered refresh lineage may remain | Replay is generic, but consume and credential replacement lack atomic guards; concurrent completions can both succeed with last-writer verifier state; each transaction does not serialize cross-request replacement or discover complete lineage | Password/reset classes §5.3; HD=no; P=unresolved | Consume/change/selected revoke, no material/password; require sign-in without promising every acknowledged concurrent password or universal refresh revoke | `CHOICE-002/007/012/013`; partial consume/replacement/cascade; #339/#338/#773; `G-AUTH`,`G-PROV`,`G-RET`,`G-PRIV`; concurrent distinct-password completion/expired-linkage/redaction tests |
| `AUTH-LC-PWD-003` | Template + `E-IDENTITY`; schema/consumer evidence, no mutation | Disabled verifier excluded from sign-in and current reset issuance/resolution/replacement; re-enable/recovery path absent | Idempotency/retry/concurrency unresolved; disable would lock out password reset, so block mutation pending separately authorized recovery/last-method proof | Password class §5.3; HD=no; P=unresolved | Required success/denial/actor/reason, no verifier; unavailable UI; never present reset as recovery | `CHOICE-002`; unimplemented/lockout-blocked; #788/#339; `G-AUTH`,`G-LOCK`,`G-SCHEMA`,`G-CONTRACT`,`G-CLIENT`,`G-PRIV`; disabled-password reset denial/recovery/session tests |
| `AUTH-LC-PWD-004` | Template + `E-IDENTITY`; schema only | Revoked verifier excluded; no re-entry; replacement is distinct | Idempotency/retry/concurrency unresolved; block mutation | Password class §5.3; HD=no; P=unresolved | Required revoke/denial, no verifier; unavailable UI | `CHOICE-002`; unimplemented; #788/#339; same gates as PWD-003; terminal/session/material tests |
| `AUTH-LC-PWD-005` | Template + `E-IDENTITY`; current bootstrap source/tested in-memory | Active verifier usable after bootstrap; no session issued; change/reset/revoke distinct | Coupled to bootstrap transaction; relational lock/rollback/race coverage incomplete | Password class §5.3; HD=no; P=unresolved | Credential-created safe ID/algorithm version only, never plaintext/verifier; show bootstrap created | `CHOICE-001/002/012`; source-implemented/test-partial; #339/#785; `G-AUTH`,`G-LOCK`,`G-PRIV`; relational rollback/collision/no-secret tests |
| `AUTH-LC-PWD-006` | Template + `E-IDENTITY`; current invitation source/tested in-memory | Active verifier usable after acceptance; no session issued; change/reset/revoke distinct | Coupled to acceptance transaction/compensation; relational rollback/race coverage absent | Password class §5.3; HD=no; P=unresolved | Credential-created safe ID/algorithm version only, never plaintext/verifier; generic public acceptance | `CHOICE-002/008/012`; source-implemented/test-partial; #339/#784; `G-AUTH`,`G-PROV`,`G-PRIV`; relational rollback/collision/no-secret tests |
| `AUTH-LC-PWD-007` | Template + `E-IDENTITY`; current admin-local-user source/endpoint-tested | Active verifier usable after provisioning; no session issued; change/reset/revoke distinct | Relational transaction and non-relational compensation exist; endpoint suite does not prove relational rollback/race behavior | Password class §5.3; HD=no; P=unresolved | Credential-created safe ID/algorithm version only, never plaintext/verifier; admin sees created summary | `CHOICE-001/002/012`; source-implemented/test-partial; #339/#785; `G-AUTH`,`G-LOCK`,`G-PRIV`; relational rollback/collision/no-secret tests |
| `AUTH-LC-PWD-008` | Template + `E-IDENTITY`; current verify/rehash source, focused outcome tests | Same password remains eligible with upgraded verifier parameters; no session/role/identity change; predecessor verifier has no re-entry | Rehash occurs after successful verification through ordinary tracked replacement; verify-versus-change/reset/rehash serialization and dedicated audit are partial | Password class §5.3; HD=no; P=unresolved | Safe algorithm/version upgrade category only; never submitted password, predecessor/current verifier, salt/pepper detail | `CHOICE-002`; source-implemented/concurrency-partial; #339; `G-AUTH`,`G-PRIV`; rehash success/failure, out-of-order replacement, and no-secret audit tests |
| `AUTH-LC-PWD-009` | Template + `E-IDENTITY`; current verification source/tested sequentially | Exact caller gains bounded proof result only; last-verified/updated metadata changes; no session until separate issuance | Ordinary tracked timestamps/audit occur after proof; verification-versus-change/reset/disable/rehash and out-of-order metadata lack atomic ordering/version binding | Password class §5.3; HD=no; P=unresolved | Verification success/denial with safe credential/account/correlation only; never password/verifier/hash; no proof redisplay | `CHOICE-002/010`; partial ordering/issuance binding; #339/#338; `G-AUTH`,`G-RET`,`G-PRIV`; old-proof issuance, out-of-order verification, replacement, no-secret audit tests |
| `AUTH-LC-ACC-001` | Template + `E-IDENTITY`; schema/consumer evidence | Disabled account restricted; re-enable only explicit; dependent mutation unresolved | Idempotency/retry/atomic cascade/last-owner race unresolved | Account class §5.3; HD=no; P=unresolved | Required actor/reason/prior-new/denials; blocked admin UI | `CHOICE-001`; unimplemented; #788/#785; `G-AUTH`,`G-LOCK`,`G-SCHEMA`,`G-CONTRACT`,`G-CLIENT`,`G-PRIV`; last-owner/cascade tests |
| `AUTH-LC-ACC-002` | Template + `E-IDENTITY`; schema only | Re-evaluate every credential/factor/role/session; never revive terminal authority | Idempotency/retry/concurrency and recovery proofs unresolved | Account class §5.3; HD=no; P=unresolved | Required actor/reason/revalidation; blocked admin UI | `CHOICE-001`; unimplemented; #788/#785; same gates as ACC-001; revalidation/denial tests |
| `AUTH-LC-ACC-003` | Template + `E-IDENTITY`; deletion marker/consumer evidence, no mutation | Deleted account excluded by current auth consumers; cascade, profile/domain-data relationship, evidence, restore and re-entry unresolved | Authorization, idempotency, concurrency, last-owner, dependency/hold/copy and logical-versus-physical disposition unresolved | Account class §5.3; HD=no here; P=unresolved/manual | Required actor/prior-new/reason/dependency plan, no private data; blocked admin UI | `CHOICE-001`; unimplemented; #788/#785/#724; `G-AUTH`,`G-LOCK`,`G-SCHEMA`,`G-CONTRACT`,`G-CLIENT`,`G-RET`,`G-PRIV`,`G-DEST`; full cascade/hold/restore-denial tests |
| `AUTH-LC-ACC-004` | Template + `E-IDENTITY`; current bootstrap source/tested in-memory | First active account/profile/identity/credential/roles readable; bootstrap closes; no session issued | Relational serializable/table-lock source exists; focused relational race/rollback proof absent | Account/identity/password/role classes §5.3; HD=no; P=unresolved | Bootstrap success/denial safe IDs/roles, never identifier/password/verifier; show created | `CHOICE-001/002/003/004`; source-implemented/test-partial; #785/#339; `G-AUTH`,`G-LOCK`,`G-PRIV`; exact-one relational race/rollback tests |
| `AUTH-LC-ACC-005` | Template + `E-INVITE`; current acceptance source/tested in-memory | New active account/profile/local identity/password/user role readable; no session issued | Serializable relational branch exists in source, but focused suites use in-memory; collision/concurrent accept/rollback evidence partial | Account/identity/password/role/invite classes §5.3; HD=no; P=unresolved | Acceptance safe IDs/status/role, never contact/password/verifier/material; generic public result | `CHOICE-001/002/003/004/008`; source-implemented/test-partial; #784/#339; `G-AUTH`,`G-PROV`,`G-PRIV`; relational collision/single-use/rollback tests |
| `AUTH-LC-ACC-006` | Template + `E-IDENTITY`; current admin-local-user source/endpoint-tested | New active account/profile/local identity/password/user role owner/admin-readable; no session issued | Serializable relational transaction exists; non-relational fallback compensates only its created bundle; focused relational collision/rollback proof absent | Account/identity/password/role classes §5.3; failed-bundle cleanup only; general HD=no; P=unresolved | Creation safe account/profile IDs/status/role and bounded workflow outcome; never identifier/password/verifier | `CHOICE-001/002/003/004/012`; source-implemented/test-partial; #785/#339; `G-AUTH`,`G-LOCK`,`G-PRIV`; relational collision/rollback/audit/no-secret tests |
| `AUTH-LC-ID-001` | Template + `E-IDENTITY`; disabled field, no runtime | Unlinked identity excluded; other methods unchanged; relink distinct | Retry/concurrency/provider-collision/last-method result unresolved | Identity class §5.3; HD=no; P=unresolved | Required actor/provider-safe subject/reason, no provider payload; blocked UI | `CHOICE-003`; unimplemented; #787/#773/#785; `G-AUTH`,`G-LOCK`,`G-SCHEMA`,`G-CONTRACT`,`G-CLIENT`,`G-PROV`,`G-PRIV`; takeover/lockout tests |
| `AUTH-LC-ID-002` | Template + `E-IDENTITY`; current bootstrap source/tested in-memory | Local identity resolves to first-owner account; unlink distinct; no session issued | Bootstrap transaction/constraints source-present; relational collision/race proof incomplete | Identity class §5.3; HD=no; P=unresolved | Provider type/name and safe identity/account IDs, never raw identifier in audit; show bootstrap created | `CHOICE-001/003`; source-implemented/test-partial; #785/#787; `G-AUTH`,`G-LOCK`,`G-PRIV`; relational uniqueness/rollback/redaction tests |
| `AUTH-LC-ID-003` | Template + `E-INVITE`; current acceptance source/tested in-memory | Local identity resolves to invited account; unlink distinct; no session issued | Acceptance relational branch/constraints source-present; focused relational collision/race proof absent | Identity class §5.3; HD=no; P=unresolved | Provider type/name and safe identity/account IDs, never contact identifier; generic public result | `CHOICE-003/008`; source-implemented/test-partial; #784/#787; `G-AUTH`,`G-PROV`,`G-PRIV`; relational uniqueness/rollback/redaction tests |
| `AUTH-LC-ID-004` | Template + `E-IDENTITY`; current admin-local-user source/endpoint-tested | Local identity resolves to admin-created account; unlink distinct; no session issued | Preflight plus in-transaction check/constraint exist; focused relational collision/race proof absent; failed non-relational bundle is compensated | Identity class §5.3; failed-bundle cleanup only; general HD=no; P=unresolved | Provider type/name and safe identity/account IDs, never submitted identifier in audit; admin sees created summary | `CHOICE-001/003`; source-implemented/test-partial; #785/#787; `G-AUTH`,`G-LOCK`,`G-PRIV`; relational uniqueness/rollback/redaction tests |
| `AUTH-LC-ID-005` | Template + `E-IDENTITY`; architecture/default-disabled capability, no OIDC runtime | Provider link would become eligible only after approved provider proof/account binding; unlink/relink distinct; no implicit role/session/profile merge | Actor/proof, stable issuer-subject uniqueness, takeover/relink, retry/rollback, remaining-method and session outcomes unresolved | Identity class §5.3; HD=no; P=unresolved | Required provider-safe stable reference/account/actor/outcome; never provider credential/token/payload | `CHOICE-003`; unimplemented; #787/#773/#785; `G-AUTH`,`G-LOCK`,`G-SCHEMA`,`G-CONTRACT`,`G-CLIENT`,`G-PROV`,`G-PRIV`; collision/takeover/rollback/session/redaction tests |
| `AUTH-LC-ROL-001` | Template + `E-IDENTITY`; current bootstrap source/tested in-memory | Exact owner/admin/user assignments readable; no session issued; later change/remove distinct | Coupled to empty-system bootstrap transaction/table lock; focused relational exact-one/rollback proof absent | Role class §5.3; HD=no; P=unresolved | Bootstrap actor/system context, exact roles/account and outcome; never credential material | `CHOICE-001/004`; source-implemented/test-partial; #785/#464/#465; `G-AUTH`,`G-LOCK`; relational exact-one/rollback/authz tests |
| `AUTH-LC-ROL-002` | Template + `E-IDENTITY`; no runtime | Old/new authorization read effect and re-entry unresolved | Atomic replacement/idempotency/retry/concurrency unresolved | Role class §5.3; HD=no; P=unresolved | Required prior/new role + actor/reason; blocked UI | `CHOICE-004`; unimplemented; same owners/gates; stale-claim/last-owner tests |
| `AUTH-LC-ROL-003` | Template + `E-IDENTITY`; no runtime | Removed role excluded; later assignment is new event | Idempotency/retry/concurrency/last-owner unresolved | Role class §5.3; HD=no; P=unresolved | Required removal/denial + actor/reason; blocked UI | `CHOICE-004`; unimplemented; same owners/gates; self-demotion/concurrency tests |
| `AUTH-LC-ROL-004` | Template + `E-INVITE`; current acceptance source/tested in-memory | Exact user assignment readable after acceptance; no owner/admin role or session; later removal distinct | Coupled to invitation single-use/account bundle; focused relational concurrency/rollback proof absent | Role/invite classes §5.3; HD=no; P=unresolved | Invitation/account/role/outcome safe IDs/categories; never invitation/password material or contact | `CHOICE-004/008`; source-implemented/test-partial; #784/#785; `G-AUTH`,`G-PROV`,`G-PRIV`; relational rollback/single-use/authz tests |
| `AUTH-LC-ROL-005` | Template + `E-IDENTITY`; current admin-local-user source/endpoint-tested | Exact user assignment owner/admin-readable; no owner/admin role or session; later removal distinct | Coupled to guarded provisioning transaction/compensation; focused relational collision/rollback proof absent | Role class §5.3; failed-bundle cleanup only; general HD=no; P=unresolved | Provisioning actor/account/exact role/outcome; never identifier/password/verifier | `CHOICE-001/004`; source-implemented/test-partial; #785/#464/#465; `G-AUTH`,`G-LOCK`,`G-PRIV`; relational rollback/authz/audit tests |
| `AUTH-LC-PKY-001` | Template + `E-FACTOR`; current source/tested sequentially | Revoked credential excluded; metadata owner-read may remain; new enrollment only | Already-revoked retry is sequentially idempotent; ownership/policy-dependent step-up checked; ordinary tracked update leaves concurrent revoke/audit unproved | Passkey class §5.3; HD=no; P=unresolved | Revoke/denial safe credential ID, no assertion/private data; refresh factor list | `CHOICE-005`; partial concurrency; #394/#776; `G-AUTH`,`G-LOCK`,`G-RET`,`G-PRIV`,`G-DEST`; post-revoke/concurrent/session/disposition tests |
| `AUTH-LC-PKY-002` | Template + `E-FACTOR`; schema only | Disabled credential excluded; re-enable only explicit | Idempotency/retry/concurrency unresolved; block mutation | Passkey class §5.3; HD=no; P=unresolved | Required disable/denial, no ceremony payload; unavailable UI | `CHOICE-005`; unimplemented; #394/#776; `G-AUTH`,`G-LOCK`,`G-SCHEMA`,`G-CONTRACT`,`G-CLIENT`,`G-PRIV`; factor/session tests |
| `AUTH-LC-PKY-003` | Template + `E-FACTOR`; schema only | Revalidate credential/account/policy; no session revival | Idempotency/retry/concurrency/compromise response unresolved | Passkey class §5.3; HD=no; P=unresolved | Required re-enable/denial; unavailable UI | `CHOICE-005`; unimplemented; same owners/gates; compromise/re-entry tests |
| `AUTH-LC-PKY-004` | Template + `E-FACTOR`; current source/tested sequentially | Any owned non-revoked passkey row becomes revoked after commit; no re-entry | Broad pending/enrolled/disabled acceptance is current; ordinary tracked update leaves concurrent result/audit and desired source guard unresolved | Passkey class §5.3; HD=no; P=unresolved | Revoke/source-state/reason safe metadata, no assertion/private data; refresh list | `CHOICE-005`; partial broad/concurrent behavior; #394/#776; same gates as PKY-001; source-state/concurrency tests |
| `AUTH-LC-PKY-005` | Template + `E-FACTOR`; current source/tested sequentially | Enrolled credential eligible after commit; revoke distinct; completion does not revalidate policy; later counter must be monotonic but can regress under concurrent assertions | Account/challenge binding and observed collision check exist, but concurrent insertion, policy change, and higher-then-lower assertion-counter commits lack atomic guards; stale assertion can miss replay suspicion | Passkey class §5.3; HD=no; P=unresolved | Enrollment/assertion outcome plus safe credential/challenge ID and bounded counter category, no attestation/assertion/private material; refresh list | `CHOICE-005/006`; partial concurrency/policy/anti-replay; #394/#776; `G-AUTH`,`G-LOCK`,`G-RET`,`G-PRIV`,`G-DEST`; duplicate/policy and relational out-of-order monotonic-counter/replay/redaction tests |
| `AUTH-LC-PKY-006` | Template + `E-FACTOR`; current self-service source/tested sequentially | Label/updated time changes on any owned source state; usability/status and all other authority unchanged | Ownership and policy-dependent step-up checked; ordinary update is last-writer and can overlap revoke; terminal evidence metadata remains mutable | Passkey class §5.3; HD=no; P=unresolved | Rename success/denial with safe credential ID/reason only; no prior label, assertion, key, or private payload; refresh list | `CHOICE-005`; implemented broad-state/concurrency-partial; #394/#776; `G-AUTH`,`G-RET`,`G-PRIV`; terminal-state, rename/revoke race, retry/audit-redaction tests |
| `AUTH-LC-TOTP-001` | Template + `E-FACTOR`; current source/tested sequentially | Pending setup excluded after commit; new enrollment allowed | Already-unavailable generic sequentially; ownership checked; no concurrency token/status-qualified update, so concurrent cancel/revoke audit/outcome is unproved | TOTP class §5.3; HD=no; P=unresolved | Cancel/denial safe ID, never protected secret/OTP; refresh setup state | `CHOICE-005`; partial concurrency; #394/#776; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; cancel-race/secret-erasure tests |
| `AUTH-LC-TOTP-002` | Template + `E-FACTOR`; current source/tested sequentially | Observed prior pending excluded; new pending setup eligible | Unguarded read-then-insert and no unique active/pending constraint allow concurrent starts to create multiple pending factors | TOTP class §5.3; HD=no; P=unresolved | Replacement lifecycle, no setup secret in audit; show only current setup while acknowledging conflict | `CHOICE-005/012`; partial concurrency; #394/#776; `G-AUTH`,`G-RET`,`G-PRIV`; atomic/unique concurrent-begin and no-secret tests |
| `AUTH-LC-TOTP-003` | Template + `E-FACTOR`; current source, focused expiry test absent | Expired setup excluded; restart allowed | Source saves lazy expiry; retry and concurrent verification outcome lack focused tests | TOTP class §5.3; HD=no; P=unresolved | Expiry/failure, no OTP/secret; show setup expired | `CHOICE-005`; source-implemented/test-partial; #394/#776; `G-RET`,`G-PRIV`,`G-DEST`; clock/race/disposition tests required |
| `AUTH-LC-TOTP-008` | Template + `E-FACTOR`; current source/tested sequentially | Enrolled factor becomes eligible; no re-entry to pending; cancel/revoke are separate; completion does not recheck TOTP policy | OTP verification/audit exist, but ordinary tracked update leaves verify-versus-cancel/revoke concurrency unguarded and intervening policy disable does not block enrollment | TOTP class §5.3; HD=no; P=unresolved | Enrollment/failure safe factor ID/status only, no OTP/secret/provisioning URI; show enrolled only with accurate policy state | `CHOICE-005`; partial concurrency/policy; #394/#776; `G-AUTH`,`G-LOCK`,`G-RET`,`G-PRIV`,`G-DEST`; atomic source-state/race/policy-change/secret-redaction tests |
| `AUTH-LC-TOTP-004` | Template + `E-FACTOR`; schema only | Disabled factor excluded; re-enable explicit only | Idempotency/retry/concurrency/last-factor unresolved | TOTP class §5.3; HD=no; P=unresolved | Required disable/denial, no protected secret; unavailable UI | `CHOICE-005`; unimplemented; #394/#465/#776; `G-AUTH`,`G-LOCK`,`G-SCHEMA`,`G-CONTRACT`,`G-CLIENT`,`G-PRIV`; lockout/secret tests |
| `AUTH-LC-TOTP-005` | Template + `E-FACTOR`; schema only | Revalidate factor/account/policy; no session revival | Idempotency/retry/concurrency/secret validity unresolved | TOTP class §5.3; HD=no; P=unresolved | Required re-enable/denial; unavailable UI | `CHOICE-005`; unimplemented; same owners/gates; re-entry/compromise tests |
| `AUTH-LC-TOTP-006` | Template + `E-FACTOR`; current source/tested sequentially | Revoked factor excluded after commit; new enrollment only | Already-revoked idempotent sequentially; ownership/policy-dependent step-up checked; no concurrency token/status-qualified transition | TOTP class §5.3; HD=no; P=unresolved | Revoke/denial safe ID, no secret/OTP; refresh factor list | `CHOICE-005`; partial concurrency; #394/#465/#776; `G-AUTH`,`G-LOCK`,`G-RET`,`G-PRIV`,`G-DEST`; revoke-race/last-factor/session/secret-erasure tests |
| `AUTH-LC-TOTP-007` | Template + `E-FACTOR`; current source/tested sequentially | Any owned non-revoked factor row becomes revoked after commit; no re-entry | Broad source-state acceptance and policy-dependent step-up are current; desired source guard and concurrent transition outcome unresolved | TOTP class §5.3; HD=no; P=unresolved | Revoke/source-state/reason safe metadata, no secret/OTP; refresh factor/setup state | `CHOICE-005`; partial broad/concurrent behavior; #394/#776; same gates as TOTP-006; pending/expired/disabled and concurrency tests |
| `AUTH-LC-TOTP-009` | Template + `E-FACTOR`; current self-service source/tested sequentially | Label/updated time changes on any owned source state; usability/status and all other authority unchanged | Ownership and policy-dependent step-up checked; ordinary update is last-writer and can overlap cancel/revoke; terminal evidence metadata remains mutable | TOTP class §5.3; HD=no; P=unresolved | Metadata-update success/denial with safe factor ID/type/reason only; no prior label, protected secret, URI, or OTP; refresh list | `CHOICE-005`; implemented broad-state/concurrency-partial; #394/#776; `G-AUTH`,`G-RET`,`G-PRIV`; terminal-state, rename/revoke race, retry/audit-redaction tests |
| `AUTH-LC-RCV-001` | Template + `E-FACTOR`; current source/tested sequentially | Old batch/verifiers excluded; new batch active; enrolled TOTP is required and passkey-only does not qualify | Explicit replacement required; concurrent calls can create multiple active batches; retry cannot safely redisplay prior codes | Recovery class §5.3; HD=no; P=unresolved | Replace/generate counts, no codes/verifiers; display once only | `CHOICE-005/012`; partial eligibility/concurrency; #775/#776; `G-AUTH`,`G-LOCK`,`G-RET`,`G-PRIV`,`G-DEST`; TOTP/passkey eligibility, atomic-active-batch race/no-redisplay tests |
| `AUTH-LC-RCV-002` | Template + `E-FACTOR`; current source/tested sequentially | Consumed verifier excluded after commit; batch remains as count permits; concurrent challenges can both obtain scoped success | Ordinary tracked update has no conditional consume/concurrency token; replay denies only after committed state, so single-use is sequential-only | Recovery class §5.3; HD=no; P=unresolved | Consumption safe IDs/counts, no code/verifier; refresh batch count without unconditional exact-one claim | `CHOICE-005/006`; partial atomic consumption; #775; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; same-code/two-challenge relational exact-one/depletion tests |
| `AUTH-LC-RCV-003` | Template + `E-FACTOR`; current source/tested sequentially for active batch | Revoked batch/unused verifiers excluded after commit; new batch distinct | Active batch and unused verifiers update together; ordinary tracked updates leave concurrent revoke/use unresolved | Recovery class §5.3; HD=no; P=unresolved | Revoke/counts, no codes/verifiers; refresh list | `CHOICE-005`; partial concurrency; #775/#776; same gates as RCV-002; revoke/use race/disposition tests |
| `AUTH-LC-RCV-004` | Template + `E-FACTOR`; schema only | Expired material excluded; replacement distinct | Trigger/idempotency/retry/concurrency unresolved | Recovery class §5.3; HD=no; P=unresolved | Required expiry/counts, no code/verifier; blocked/unavailable state | `CHOICE-005`; unimplemented; #775/#776; `G-AUTH`,`G-SCHEMA`,`G-RET`,`G-PRIV`,`G-DEST`; clock/depletion tests |
| `AUTH-LC-RCV-005` | Template + `E-FACTOR`; current source/tested sequentially | Any owned batch is rewritten revoked; later generation remains distinct | Replaced/expired/revoked accepted; repeated revoke rewrites timestamps/reason and audit, so not idempotent; concurrent outcome unresolved | Recovery class §5.3; HD=no; P=unresolved | Revoke/source-state/counts, no codes/verifiers; refresh list | `CHOICE-005`; partial broad/non-idempotent behavior; #775/#776; same gates as RCV-003; source-state/retry/concurrency tests |
| `AUTH-LC-CHL-001` | Template + `E-FACTOR`; current MFA source/tested sequentially | Verified challenge terminal after commit; intended bound scope unproved for anonymous verification; same TOTP can satisfy another pending challenge; completion ignores intervening factor-policy disable | Anonymous load omits account/session predicates; no conditional consume/concurrency token or accepted TOTP timestep/proof identity; intervening policy disable does not block completion | Challenge class §5.3; HD=no; P=unresolved | Purpose/factor/outcome/attempts, no OTP/verifier; do not claim pending-flow/account/session binding, proof uniqueness, or policy revalidation | `CHOICE-005/006`; partial anonymous binding/completion/proof/policy; #394/`REC-AUTH-MFA-SIGNIN-001`; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; pending-flow/account/session binding, atomic completion, replay, policy-change tests |
| `AUTH-LC-CHL-002` | Template + `E-FACTOR`; current passkey source/tested sequentially | Consumed challenge terminal after commit; enrollment/sign-in/step-up completion does not revalidate policy; sign-in persists a session whose raw credential is discarded instead of returned/set | No conditional status update/concurrency token; concurrent completions can succeed; intervening policy disable does not block any completion; sign-in can create unusable-to-caller sessions | Challenge class §5.3; HD=no; P=unresolved | Purpose/credential/outcome, no assertion/challenge/session token; do not claim the caller is signed in | `CHOICE-005/006`; missing handoff/partial concurrency/all-completion policy revalidation; #394/`REC-AUTH-PASSKEY-SESSION-001`; same gates; atomic binding/usable-handoff/all-purpose policy-change tests |
| `AUTH-LC-CHL-003` | Template + `E-FACTOR`; current clock-denial source/tested | Clock-expired challenge is unusable even when persisted status remains pending; new ceremony required | Repeated completion denies by clock; helper mutation is not saved on early return, and concurrency/status reconciliation is unresolved | Challenge class §5.3; HD=no; P=unresolved | Expiry/purpose safe IDs only when persisted/audited, no material; show expired/generic public failure | `CHOICE-006`; partial/unpersisted status; #394; `G-RET`,`G-PRIV`,`G-DEST`; clock/persistence/race tests |
| `AUTH-LC-CHL-004` | Template + `E-FACTOR`; current source/tested sequentially | Blocked challenge excluded after commit; new ceremony per abuse policy | Attempt increment/block uses ordinary tracked state; concurrent invalid submissions can collapse multiple guesses into one recorded increment and defer threshold blocking | Challenge class §5.3; HD=no; P=unresolved | Block/denial/attempt bucket, no submitted factor; generic public failure | `CHOICE-006`; partial concurrency; #394/REC-ABUSE; `G-AUTH`,`G-RET`,`G-PRIV`,`G-EXPOSE`; atomic increment/threshold/distributed tests |
| `AUTH-LC-CHL-005` | Template + `E-FACTOR`; schema/partial flow evidence | Failed challenge excluded if materialized; next mutation unresolved | Retry/status materialization/concurrency unresolved | Challenge class §5.3; HD=no; P=unresolved | Required failure category, no submitted factor; generic failure | `CHOICE-006`; partial; #394/REC-ABUSE; same gates as CHL-004; per-flow transition tests |
| `AUTH-LC-CHL-006` | Template + `E-FACTOR`; schema only | Cancelled challenge excluded; new ceremony distinct | Idempotency/retry/concurrency unresolved; no exposed mutation | Challenge class §5.3; HD=no; P=unresolved | Required cancel actor/reason; blocked UI | `CHOICE-006`; unimplemented; #394; `G-AUTH`,`G-SCHEMA`,`G-CONTRACT`,`G-CLIENT`,`G-RET`,`G-PRIV`; authorization/race tests |
| `AUTH-LC-CHL-007` | Template + `E-FACTOR`; schema/terminal-reject evidence | Replay-detected challenge excluded; session response unresolved | Terminal replay rejects; status materialization/concurrent response incomplete | Challenge class §5.3; HD=no; P=unresolved | Replay/purpose safe IDs, no payload; generic reauth/failure | `CHOICE-006`; partial; #394/REC-ABUSE; `G-AUTH`,`G-RET`,`G-PRIV`,`G-EXPOSE`; replay/session tests |
| `AUTH-LC-CHL-008` | Template + `E-FACTOR`; anonymous route/authenticated-only service mismatch | Anonymous no-actor creation denied and `PendingAuthFlowId` ignored; authenticated calls create account/session-bound pending state; completion/expiry distinct | Pre-session issuance is broken; anonymous verification later omits persisted account/session predicates; authenticated policy/factor checks and repeated creation do not establish pending-flow binding | Challenge class §5.3; HD=no; P=unresolved | Safe challenge/account/session/purpose/pending-flow category/times only, no verifier/proof; do not show usable anonymous options | `CHOICE-005/006`; broken anonymous handoff/partial authenticated issuance; #394/REC-MFA-SIGNIN; `G-AUTH`,`G-CONTRACT`,`G-CLIENT`,`G-RET`,`G-PRIV`; no-actor issuance, pending-flow/account/session binding, duplicate/policy tests |
| `AUTH-LC-CHL-009` | Template + `E-FACTOR`; current passkey source/tested | Pending enrollment challenge bound to account/session; credential absent until completion | Options-time policy/account checks exist; repeated calls create separate challenges and completion does not recheck policy | Challenge class §5.3; HD=no; P=unresolved | Safe challenge/account/session/purpose/times only, no raw challenge/attestation; show options | `CHOICE-005/006`; partial handoff policy; #394; same gates; binding/duplicate/policy-change tests |
| `AUTH-LC-CHL-010` | Template + `E-FACTOR`; current anonymous passkey source/tested | Pending sign-in challenge may have no account binding; grants no authority until completion | Options-time policy check exists; candidate ambiguity intentionally leaves account null; duplicate/pending/concurrency and completion handoff partial | Challenge class §5.3; HD=no; P=unresolved | Safe challenge/purpose/times and bounded candidate state, no raw assertion/private data; generic options | `CHOICE-005/006`; partial anonymous handoff; #394/`REC-AUTH-PASSKEY-SESSION-001`; `G-AUTH`,`G-EXPOSE`,`G-PRIV`; enumeration/binding/duplicate tests |
| `AUTH-LC-CHL-011` | Template + `E-FACTOR`; current passkey source/tested | Pending step-up challenge bound to account/session/operation; grants no authority until completion | Options-time policy/enrolled-credential checks exist; repeated calls create separate challenges and completion does not recheck policy | Challenge class §5.3; HD=no; P=unresolved | Safe challenge/account/session/operation/times only, no raw assertion; show options | `CHOICE-005/006`; partial handoff policy; #394; `G-AUTH`,`G-RET`,`G-PRIV`; binding/duplicate/policy-change tests |
| `AUTH-LC-RST-001` | Template + `E-RESET`; current source/tested sequentially | Replaced observed request excluded; newest sequential pending may proceed; concurrent new requests can both remain pending | New issue revokes the pending set it observes transactionally, but no unique active-status constraint, concurrency token, or conditional replacement prevents a two-link issuance race | Reset class §5.3; HD=no; P=unresolved | Replacement/requested events, no email/material/hash; generic delivery response | `CHOICE-007`; partial issuance concurrency; #339/#724; `G-AUTH`,`G-PROV`,`G-RET`,`G-PRIV`,`G-DEST`; atomic replacement/relational race/redaction tests |
| `AUTH-LC-RST-002` | Template + `E-RESET`; current source/tested sequentially | Consumed request excluded after commit; later request distinct | No status-qualified consume/concurrency token; concurrent completions can both read pending and report success; later replay becomes suspicious | Reset class §5.3; HD=no; P=unresolved | Consume/change/revoke events, no password/material; prompt sign-in | `CHOICE-007`; partial concurrency; #339; same gates as RST-001; atomic pending-to-consumed relational/session tests |
| `AUTH-LC-RST-003` | Template + `E-RESET`; current source/tested | Expired request excluded; later request distinct | Sequential repeated completion is generic; concurrent lazy-expiry outcome remains unproved | Reset class §5.3; HD=no; P=unresolved | Expiry/denial safe metadata, no identifier/material; generic failure | `CHOICE-007`; implemented sequentially; #339/#724; `G-RET`,`G-PRIV`,`G-DEST`; concurrent clock/redaction tests required |
| `AUTH-LC-RST-004` | Template + `E-RESET`; current source/tested in part | Suspicious-replay request excluded; dependency-denied pending request remains eligible if dependencies recover before expiry | Reuse generic; account/identity/credential/policy denial only refreshes check/update times and audit, while guessing/rate/concurrent classification remains incomplete | Reset class §5.3; HD=no; P=unresolved | Replay/denial without identifiers/material; generic failure | `CHOICE-007`; partial/non-terminal denial; #339/REC-ABUSE; `G-AUTH`,`G-RET`,`G-PRIV`,`G-EXPOSE`; dependency-recovery/guessing/replay tests |
| `AUTH-LC-RST-005` | Template + `E-RESET`; current source/tested successful-completion path | Revoked pending requests excluded; new issuance per policy | Successful completion performs an observed-set revoke in the current transaction; denial does not revoke, and trigger/race completeness is unresolved | Reset class §5.3; HD=no; P=unresolved | Revoke reason/count safe metadata, no material; no distinct public disclosure | `CHOICE-007`; implemented bounded success path; #339/#724; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; trigger/dependency/race tests |
| `AUTH-LC-RST-006` | Template + `E-RESET`; current anonymous endpoint/orchestrator source/tested | Public result is enumeration-resistant and grants no authority; eligible flow may proceed to distinct request issuance, others create none | Identifier submission precedes account proof; abuse/policy/readiness/provider outcomes bounded, but delivery default-off and issuance race remains | Reset class §5.3; HD=no; P=unresolved | Requested/denied safe category/bucket/correlation only; never identifier/account existence/material/hash/provider payload; generic public response | `CHOICE-007/012`; partial abuse/provider/issuance; #339/REC-ABUSE; `G-AUTH`,`G-PROV`,`G-EXPOSE`,`G-RET`,`G-PRIV`; enumeration/readiness/provider/no-material/race tests |
| `AUTH-LC-INV-001` | Template + `E-INVITE`; current source/tested in-memory | Accepted invitation excluded; created roots independently readable/usable; disabled-plus-grace readout does not permit actual acceptance | Serializable relational branch exists, but focused suites use in-memory; constraints/rollback/concurrency/grace partial; audit misattributes inviter as actor instead of bearer-material acceptance context | Invite class §5.3; HD=no for action; P=unresolved | Accept/denial safe IDs/categories, no contact/material; inviter only contextual metadata, not accepting actor; generic public result | `CHOICE-008`; partial relational/grace/audit attribution; #784; `G-AUTH`,`G-PROV`,`G-EXPOSE`,`G-RET`,`G-PRIV`,`G-DEST`; relational grace/single-use/rollback and actor-audit tests |
| `AUTH-LC-INV-002` | Template + `E-INVITE`; current source/tested | Same pending row remains; old material excluded only on ready requested-delivery path | Non-idempotent: every ready retry generates/commits another hash before send, invalidating the prior generated link; timeout/send failure leaves delivery ambiguity | Invite class §5.3; HD=no; P=unresolved | Resend/delivery category, no contact/material/hash; show exact known result and never promise which timed-out link arrived | `CHOICE-008`; partial retry semantics; #784; same gates as INV-001; idempotency/delivery-ambiguity/race tests |
| `AUTH-LC-INV-003` | Template + `E-INVITE`; current source/tested status path | Revoked invitation excluded; no re-entry | Sequential status/authorization bounded; supplied reason is discarded by service/persistence/audit; concurrent revoke/accept unproved | Invite class §5.3; HD=no for revoke; P=unresolved | Current actor/subject/time only; required bounded reason is absent, no contact/material; refresh admin list without claiming reason evidence | `CHOICE-008`; partial reason/concurrency; #784/#724; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`; reason persistence/audit/redaction and concurrent authz/race tests |
| `AUTH-LC-INV-004` | Template + `E-INVITE`; current source/tested | Expired invitation excluded; no re-entry | Lazy/batch expiry deterministic; repeated redemption generic | Invite class §5.3; HD=no for expiry; P=unresolved | Expiry/denial safe metadata, no material; generic public failure | `CHOICE-008`; implemented; #784/#724; `G-RET`,`G-PRIV`,`G-DEST`; clock/batch tests |
| `AUTH-LC-INV-005` | Template + `E-INVITE`; current destructive source/tested | Row becomes unreadable; accepted account remains; no re-entry; any well-formed anonymous accept request can trigger global cleanup before its abuse/policy/secret checks | Batch 50/order current; retries process survivors; public trigger authorization and cross-copy/hold concurrency are unproved | Current HD=yes after 90-day marker; policy HD/P=unresolved, not authorized here | Aggregate counts/categories only; row evidence sufficiency unresolved; no ordinary cleanup UI, but public protocol invocation exists | `CHOICE-008`; implemented fact/policy-blocked; #784/#724; `G-AUTH`,`G-RET`,`G-PRIV`,`G-DEST`,`G-EXPOSE`; trigger-auth/invalid-secret/holds/copies/idempotency/disposal audit tests |
| `AUTH-LC-INV-006` | Template + `E-INVITE`; current source/tested | Pending row and existing material remain eligible; later ready resend/accept/revoke allowed | Repeated non-ready resend updates state/audit but does not rotate; concurrency with accept/revoke needs terminal-state proof | Invite class §5.3; HD=no; P=unresolved | Non-sent delivery category, no contact/material/hash; explicitly do not claim replacement | `CHOICE-008`; implemented; #784; `G-AUTH`,`G-PROV`,`G-RET`,`G-PRIV`; no-rotation/provider/race tests |
| `AUTH-LC-INV-007` | Template + `E-INVITE`; current source/tested sequentially | Pending invitation owner-readable; accept/resend/revoke/expire distinct | Capability/actor/contact/role/abuse checks and pending-recipient constraint exist; concurrent create and delivery-after-commit outcome partial | Invite class §5.3; HD=no; P=unresolved | Creation/delivery safe ID/categories, no contact/material/hash/provider payload; show exact sent/not-sent result | `CHOICE-008/012`; partial relational/provider; #784; `G-AUTH`,`G-PROV`,`G-RET`,`G-PRIV`,`G-EXPOSE`; uniqueness/race/delivery/no-secret tests |
| `AUTH-LC-POL-001` | Template + `E-FACTOR`; schema/read only | Active/default read governs; mutation/re-entry/version retirement unresolved | Version/idempotency/effective-time/concurrency unresolved; block mutation | Policy class §5.3; HD=no; P=unresolved | Required actor/prior-new version/reason, no secret config; read-only/blocked UI | `CHOICE-011`; unimplemented mutation; #394/#465/#785; `G-AUTH`,`G-LOCK`,`G-SCHEMA`,`G-CONTRACT`,`G-CLIENT`,`G-RET`,`G-PRIV`; version/weakening tests |
| `AUTH-LC-POL-002` | Template + `E-INVITE`; initial creation works, replacement source-broken/provider-divergent | Initial active policy can govern; existing-policy change is not a successful replacement; disabled-plus-grace readout and acceptance enforcement disagree | `AsNoTracking` active row is not retired; relational unique-active index rejects replacement while in-memory may leave multiple active rows; version/grace behavior incomplete | Policy class §5.3; HD=no; P=unresolved | Initial/change attempt actor/version/outcome only; do not report replacement success; no unsafe config | `CHOICE-008/011`; incomplete; #784/#785; `G-AUTH`,`G-LOCK`,`G-RET`,`G-PRIV`; relational tracked/conditional retirement, unique-active, retry/version/grace tests |
| `AUTH-LC-AUD-001` | Template + `E-AUDIT`; append writers/partial tests | Security-authorized read future; no row mutation/re-entry; correction additive | Writer retry/idempotency varies and complete inventory is unresolved | Audit class §5.3; HD=no; P=unresolved | Exact bounded audit contract; no secret/private payload; no ordinary removal UI | `CHOICE-009`; partial; #724/#774/#973; `G-AUTH`,`G-CONTRACT`,`G-CLIENT`,`G-RET`,`G-PRIV`,`G-DEST`; authz/pagination/hold/export/disposal tests |

### 5.3 Retention, Audit, Privacy, Follow-Up, And Acceptance

`Operationally required, duration unresolved` means current security behavior
needs some bounded evidence, but no reviewed duration exists. `Source-defined
validity` describes usability only; it is not a retention or disposal period.

| Row ID(s) | Retention classification and trigger | Retained safe evidence / privacy boundary | Current audit evidence and gaps | Open choice, follow-up lane, manual gate, runtime acceptance |
| --- | --- | --- | --- | --- |
| `AUTH-LC-SES-001`–`AUTH-LC-SES-007` | Source-defined session validity; issued raw credentials belong only at an approved immediate handoff; post-expiry/revocation metadata retention is operationally required but duration unresolved. | Session/account/family IDs, status, issued/expires/last-seen/revoked time, bounded reason/device label; never raw access/refresh material, token hash in client/audit, full IP, or unbounded agent. | Local issuance, passkey access-only issuance, validation/use, revocation and family events exist; old-proof issuance, validation ordering, family validation, passkey handoff, and retention cleanup/readout are partial or absent. | `AUTH-LC-CHOICE-002/005/010/012`; #338/#394/#1059 and `REC-AUTH-PASSKEY-SESSION-001`; auth/security/privacy/destructive gates. Accept old-password issuance serialization, handoff/MFA, expiry/revoke/family isolation, validate-versus-revoke/monotonic-last-seen, rotation-versus-revoke, family-aware validation, redaction, and retained-copy disposition tests. |
| `AUTH-LC-REF-001`–`AUTH-LC-REF-003` | Source-defined idle/absolute validity; rotated/replayed hash/lineage retention is operationally required for replay detection, duration unresolved. | Family/credential/session IDs, status, lineage, issued/idle/absolute/consumed/revoked times, bounded reason; no raw refresh value or exposed hash. | Rotation, replay, expiry and family-revoke events exist. | `AUTH-LC-CHOICE-010`; #338/#1059. Accept atomic race/retry, linkable/unknown replay, family-only blast radius, and no-secret evidence. |
| `AUTH-LC-PWD-001`–`AUTH-LC-PWD-009` | Active verifier is operational credential material. Creation and rehash persist a one-way verifier, never submitted plaintext; superseded verifier is overwritten rather than retained as history. Verification updates safe use timestamps. Disabled/revoked verifier retention and erasure are unresolved. | Algorithm/version/parameters, status and timestamps only where needed; never password, verifier, salt/pepper detail, submitted values, or previous verifier in audit. | Bootstrap/invitation/admin-local-user creation plus verify/change/reset audit exists; dedicated rehash and credential disable/revoke lifecycle audit do not. | `AUTH-LC-CHOICE-001/002/010`; #339/#338/#788/#784/#785. Manual auth/security and schema/privacy gate. Accept creation rollback/collision/redaction, verification/replacement/rehash ordering, old-proof session issuance, reset denial for disabled credentials, session effects, disable/re-enable/revoke, and secret-erasure tests. |
| `AUTH-LC-ACC-001`–`AUTH-LC-ACC-006` | Account creation/lifecycle evidence operationally required; duration/disposal unresolved. Deletion-marker presence is not physical-purge authority. | Account/profile link, bounded status/time/actor/reason; keep bootstrap/invitation/admin-provisioning authority distinct and account deletion separate from disablement, identity unlinking, and profile/domain-data lifecycle. | Bootstrap, invitation, and admin-local-user creation audit exists; architecture requires disable/re-enable/delete/denied audit, whose mutation runtime is absent. | `AUTH-LC-CHOICE-001/008`; #788/#785/#784/#724. Owner-lockout/privacy/destructive manual gates. Accept exact-one bootstrap, invitation/admin collision/rollback, last-owner concurrency, recovery, session/material cascade, dependencies/holds/copies, re-entry/restore and denial audit. |
| `AUTH-LC-ID-001`–`AUTH-LC-ID-005` | Active link state and tombstone retention are operationally required to resolve identities, prevent unsafe relink/collision, and explain access; duration unresolved. | Provider type/name, stable bounded subject or non-reversible reference, create/disable/unlink metadata; no provider tokens/payloads or raw contact in audit. | Bootstrap/invitation/admin local-link creation exists; provider link and unlink event/runtime are absent. | `AUTH-LC-CHOICE-001/003/008`; #787/#773/#785/#784. Provider/security/privacy gate. Accept local and provider-proof uniqueness/rollback, takeover, last-sign-in-method, session impact and redaction. |
| `AUTH-LC-ROL-001`–`AUTH-LC-ROL-005` | Role history operationally required for authorization/audit; current assignment row lacks removal history. Duration unresolved. | Account ID, exact role, actor, assigned/removed time and reason; no client-supplied authority. | Bootstrap, invitation, and admin-local-user bounded assignments exist under distinct authorities; general assign/change/remove audit absent. | `AUTH-LC-CHOICE-001/004/008`; #785/#464/#465/#784. Owner-lockout/API/schema/UI gates. Accept per-authority rollback/authz, stale-claim/session, self-demotion and concurrent last-owner tests. |
| `AUTH-LC-PKY-001`–`AUTH-LC-PKY-006` | Enrolled public-key metadata is operational credential state; revoked/disabled metadata is operationally required for collision/replay/security review; duration unresolved. Current rename runtime can mutate retained labels/timestamps on every owned source state, including terminal rows. | Server credential ID, credential-ID hash, public key only while justified, counter/backup/transport categories, bounded label, status/times/reason; no private material, raw assertion, full attestation payload. | Enrollment/assertion/revoke/failure/rename audit exists; enrollment completion does not revalidate policy, generic revoke and rename accept broad source states, and concurrent out-of-order assertion commits can regress the stored counter/miss replay suspicion; disable/re-enable and terminal cleanup do not exist. | `AUTH-LC-CHOICE-005`; #394/#776 plus `REC-AUTH-PASSKEY-SESSION-001`. Accept enrollment uniqueness/concurrency/policy, source-state, terminal rename/evidence integrity, last-factor/step-up, post-transition sign-in, session decision, relational monotonic-counter/replay, and disposition tests. |
| `AUTH-LC-TOTP-001`–`AUTH-LC-TOTP-009` | Pending setup validity is source-defined. Enrolled protected secret is operational authentication material. Terminal metadata duration unresolved; reusable protected secret retention after terminal state is forbidden merely for history. Current rename runtime can mutate retained labels/timestamps on every owned source state. | Factor ID/type/status/times/policy/reason and bounded label; lifecycle evidence must exclude decryptable secret, provisioning URI/key, submitted OTP. | Enrollment/cancel/revoke/failure/metadata-update audit exists; generic revoke and rename accept broad source states; disable/re-enable, terminal secret erasure, and cleanup absent. | `AUTH-LC-CHOICE-005`; #394/#465/#776. Secret-management/schema/security gates. Accept enrollment/cancel races, source-state/terminal-rename guards, rename/revoke ordering, atomic secret erasure/rotation, last-factor/recovery, sessions, retry and audit-redaction. |
| `AUTH-LC-RCV-001`–`AUTH-LC-RCV-005` | Active salted verifiers are operational credential material. Consumed/replaced/revoked metadata retention is required for security review, but verifier retention duration is unresolved and never justified solely by hashing. | Batch/code IDs, counts, status and times, safe challenge/correlation linkage; never raw code, submitted code, verifier/hash/salt in read/audit. | Generation/use/revoke events exist; same-code consumption is sequential-only, generic revoke rewrites every owned batch source state, and expiry runtime does not exist. | `AUTH-LC-CHOICE-005/006`; #775/#776 and `REC-AUTH-MFA-SIGNIN-001`. Accept display-once/no-redisplay, relational same-code exact-one consumption, source-state/idempotency, replacement/revoke race, depletion, expiry and verifier disposition. |
| `AUTH-LC-CHL-001`–`AUTH-LC-CHL-011` | Source-defined short validity; pending issuance and terminal challenge retention/cleanup unresolved. | IDs, purpose/factor/status, bounded verifier/hash/context, attempts and lifecycle times/reason only while needed; no raw reusable challenge or full authenticator payload. | Passkey issuance and challenge events exist. MFA route/service authority is broken for anonymous initiation: `PendingAuthFlowId` is ignored, no-actor creation is denied, and anonymous verification omits persisted account/session predicates. Policy revalidation and terminal-status coverage also differ by flow. | `AUTH-LC-CHOICE-005/006`; #394, `REC-AUTH-MFA-SIGNIN-001`, `REC-AUTH-ABUSE-001`. Accept usable anonymous issuance, pending-flow/account/session/purpose binding, duplicate-pending behavior, completion-time policy, expiry, atomic attempts, cross-challenge proof replay, concurrent completion and cleanup. |
| `AUTH-LC-RST-001`–`AUTH-LC-RST-006` | Link validity is source-defined by approved reset delivery options; terminal evidence retention and `CleanupEligibleAtUtc` use are unresolved. Anonymous initiation itself retains no reusable material in its public response. | Request/account/credential IDs where safe, purpose/status/hash version, issue/expiry/consume/revoke/replace/replay/check times, safe delivery/bucket/correlation categories; no raw material, exposed hash, identifier, email, provider payload, or new password. | Requested/issued/consumed/denied/replay/revocation/session-revoke audit exists; no cleanup. | `AUTH-LC-CHOICE-002/007/012`; #339/#724 plus `REC-AUTH-ABUSE-001`. Accept enumeration-resistant initiation, unknown/matched material, concurrent issuance and same-request distinct-password consume/replace, disabled-credential denial, dependency recovery, replay, expiry, cleanup and redaction. |
| `AUTH-LC-INV-001`–`AUTH-LC-INV-004`, `AUTH-LC-INV-006/007` | Seven-day validity and 90-day cleanup eligibility are current source values, not generally approved retention policy. | Invitation ID, status, contact kind, masked display, target `user` role, true transition actor/context, account IDs, lifecycle times, bounded reason and safe delivery/correlation categories; no raw link/secret/hash, full email, body, SMTP/provider payload. | Create/revoke/accept/resend/delivery/cleanup events exist, but acceptance misattributes inviter as actor and revoke discards the submitted reason; public failures are bounded. | `AUTH-LC-CHOICE-008`; #784/#724. Accept create uniqueness/delivery, grace consistency, conditional rotation, single-use, relational rollback, true actor/context, revoke reason, provider truth, concurrency and row-level evidence requirements. |
| `AUTH-LC-INV-005` | Current runtime hard-deletes terminal rows after the 90-day eligibility timestamp, and any well-formed anonymous accept request invokes cleanup before its abuse/policy/secret validation. Policy classification and trigger authority are unresolved/manual decisions; retention expiry is not sufficient authority under the shared taxonomy. | Current aggregate audit keeps workflow, counts/status categories and timing bucket, but no row IDs. Whether that is sufficient evidence is unresolved. | `invitation.cleanup_completed` exists; no per-row disposal event or hold/copy proof. | `AUTH-LC-CHOICE-008`; focused #784/#724 auth-runtime and retention-policy follow-up, with exposure/destructive/security/privacy gates. Accept trigger authorization/order, invalid-secret isolation, holds, dependencies, backups/replicas, dry-run, batch retry/idempotency, per-row/aggregate audit sufficiency and rollback boundaries before continued authorization. |
| `AUTH-LC-POL-001`–`AUTH-LC-POL-002` | Active version is authoritative; retired policy evidence is operationally required, duration unresolved. | Policy ID/version, bounded modes/counts/status/effective/retired times, actor/reason/correlation; no keys/secrets or full unsafe configuration payload. | Invitation initial creation/audit exists, but existing-policy replacement is source-broken/provider-divergent; security policy is read/runtime-default with no general mutation proof. | `AUTH-LC-CHOICE-008/011`; #394/#465/#784/#785. Accept relational tracked/conditional retirement, unique-active/version race, safe-default fallback, weakening/last-owner gates, effective-time behavior, and session/factor impact. |
| `AUTH-LC-AUD-001` | Policy-defined/configurable intent, but numeric duration, holds, read/export, retention clock and disposition are unresolved. Audit retention is separate from credential-material retention. | Only minimum actor/subject/action/outcome/time/correlation/request and bounded reason/transition metadata. Never credential material, local identifiers/emails without separate approval, request/response bodies, or provider/private payloads. | Substantial writers/redaction tests exist; complete lifecycle event inventory, authorized reads, export and purge do not. | `AUTH-LC-CHOICE-009`; #721/#724/#774/#973. Privacy/destructive/manual gate. Accept append-only correction, pagination/authz, hold, export redaction, disposal accounting and no-secret scans. |

### 5.4 Manual-Gate Registry

These gates are not required to merge this documentation-only policy. They are
separate pending gates for the exact downstream behavior referenced by rows in
section 5.2. No gate has approval evidence, and satisfying one never satisfies
another.

| Gate ID | Required / status | Gate and decision owner | Approval evidence | Downstream work blocked |
| --- | --- | --- | --- | --- |
| `G-AUTH` | Yes / pending | Auth/session/security behavior and policy; focused row owner plus human security review | None; pending | Any new or changed lifecycle acceptance, cascade, material response, replay response, or policy enforcement |
| `G-LOCK` | Yes / pending where referenced | Owner/admin and last-sign-in-method lockout safety; #785/#788/#394 | None; pending | Account, role, identity, password, passkey, or MFA disable/re-enable/revoke administration |
| `G-SCHEMA` | Yes / pending where referenced | Persistence shape/migration; focused schema owner and human schema review | None; pending | New lifecycle fields, constraints, tombstones, secret erasure, compaction, or cleanup state |
| `G-CONTRACT` | Yes / pending where referenced | OpenAPI/generated clients; focused contract lane | None; pending | New/changed endpoints, states, results, error shapes, and generated methods |
| `G-CLIENT` | Yes / pending where referenced | Mobile, user-web, or admin-web owner as separate lanes | None; pending | Controls, warnings, confirmations, readouts, accessibility, and localized catalog adoption |
| `G-RET` | Yes / pending | Retention clock, holds, minimum evidence, and copy disposition; #724 plus record-family owner | None; pending | Any claimed duration, retention expiry processing, compaction, or disposal eligibility |
| `G-PRIV` | Yes / pending | Privacy/redaction and minimum-data decision; #724/#973 plus security owner | None; pending | New metadata/read/export, terminal verifier/hash retention, or evidence exposure |
| `G-DEST` | Yes / pending | Destructive purge/hard-delete authorization and operational proof; human destructive gate | None; pending | Physical deletion, purge, backup/replica disposition, or continuation/broadening of invitation cleanup as settled policy |
| `G-PROV` | Yes / pending where referenced | Provider/delivery/configuration owner; #403/#773/#784 | None; pending | Email/provider claims, enabled delivery, credentials/configuration, or provider-specific payload handling |
| `G-EXPOSE` | Yes / pending where referenced | Public/admin exposure; #777 or the private admin-entry owner | None; pending | Public registration/ceremony/reset/invitation exposure or a new admin lifecycle surface |

### 5.5 Exact Surface, Metadata, And Disposition Fields

This subsection completes the remaining reusable-schema fields for every exact
row. Profiles are values, not defaults to be guessed:

- `S-SELF-API`: existing authenticated API action; no complete shipped client
  surface is claimed. If exposed, use section 5.1's exact wording.
- `S-PUBLIC-API`: existing bounded anonymous/protocol API surface with generic
  failure presentation; no broader public exposure is authorized.
- `S-ADMIN-API`: existing private authenticated admin API action/readout; no
  complete admin-web UI is claimed.
- `S-PROTOCOL`: not a management control; existing sign-in, refresh, reset,
  invitation, or ceremony protocol presents only the section 5.1 outcome.
- `S-NOT-VISIBLE`: not user-visible; internal time, lineage, policy, cleanup,
  or evidence transition.
- `S-BLOCKED-SELF` / `S-BLOCKED-ADMIN`: proposed wording only; no current API
  or UI exists and the referenced gates block exposure.

Metadata applicability is `applicable` for every profile. `M-SESSION` requires
session/account/status/issue/expiry/revoke/reason/actor/version fields;
`M-REFRESH` family/credential/session IDs, status/lineage, issue/idle/absolute/
consume/revoke/replay/reason/version fields; `M-PASSWORD` credential/account,
status, verifier algorithm/version/parameters, create/update/disable/revoke
times, actor/reason/version but never prior/current verifier material;
`M-ACCOUNT`, `M-IDENTITY`, and `M-ROLE` require their stable subject IDs,
bounded prior/new status or relationship/role, applicable timestamps,
actor/reason/version; `M-PASSKEY`, `M-TOTP`, `M-RECOVERY`, and `M-CHALLENGE`
require the safe fields enumerated in section 5.3 plus their status and terminal
timestamps, actor/reason and version, excluding section 3 material;
`M-RESET` and `M-INVITE` require their section 5.3 safe IDs/status/hash-version
and lifecycle/delivery timestamps/categories but no raw material or exposed
hash/contact; `M-POLICY` requires ID/version/status/effective/retired times and
actor/reason/version; `M-AUDIT` requires bounded actor/subject/action/outcome/
time/reason/correlation/request/transition metadata. A named field absent from
current persistence is a gap, not a claim that the field exists.

`D-ORDINARY` means hard-delete `ineligible`: target classification is an
authoritative security record/material, conditions are not met, and consequence
warning/confirmation are `not applicable` because this transition is not a
delete. Purge is `unresolved`: a separate action is required; retention/hold,
dependency/copy, authority, audit, retry/concurrency, warning, explicit
confirmation if user-initiated, and `G-RET`/`G-PRIV`/`G-DEST` remain pending.
`D-INVITE-CLEANUP` records current hard-delete fact only: target is an
authoritative terminal invitation and current row conditions are terminal
status plus `CleanupEligibleAtUtc`. The cleanup is not a user-presented action,
but a well-formed anonymous acceptance request invokes it before abuse, policy,
or secret validation; no warning/confirmation exists, and current code does not
prove trigger authorization, holds, or copies. Continued hard-delete and purge
policy remain `unresolved`, require a separate reviewed action/policy, warning
and confirmation if ever user-initiated, and are blocked by
`G-AUTH`/`G-RET`/`G-PRIV`/`G-DEST`/`G-EXPOSE`.

| Exact row | Exact surface profile | Metadata profile | Hard-delete / purge profile |
| --- | --- | --- | --- |
| `AUTH-LC-SES-001` | `S-SELF-API` | `M-SESSION` | `D-ORDINARY` |
| `AUTH-LC-SES-002` | `S-SELF-API` | `M-SESSION` | `D-ORDINARY` |
| `AUTH-LC-SES-003` | `S-SELF-API` | `M-SESSION` | `D-ORDINARY` |
| `AUTH-LC-SES-004` | `S-PROTOCOL` | `M-SESSION` | `D-ORDINARY` |
| `AUTH-LC-SES-005` | `S-PUBLIC-API` | `M-SESSION` + `M-REFRESH` | `D-ORDINARY` |
| `AUTH-LC-SES-006` | `S-PUBLIC-API` | `M-SESSION` | `D-ORDINARY` |
| `AUTH-LC-SES-007` | `S-PROTOCOL` | `M-SESSION` | `D-ORDINARY` |
| `AUTH-LC-REF-001` | `S-PROTOCOL` | `M-REFRESH` | `D-ORDINARY` |
| `AUTH-LC-REF-002` | `S-PROTOCOL` | `M-REFRESH` | `D-ORDINARY` |
| `AUTH-LC-REF-003` | `S-PROTOCOL` | `M-REFRESH` | `D-ORDINARY` |
| `AUTH-LC-PWD-001` | `S-SELF-API` | `M-PASSWORD` | `D-ORDINARY` |
| `AUTH-LC-PWD-002` | `S-PUBLIC-API` | `M-PASSWORD` + `M-RESET` | `D-ORDINARY` |
| `AUTH-LC-PWD-003` | `S-BLOCKED-ADMIN` | `M-PASSWORD` | `D-ORDINARY` |
| `AUTH-LC-PWD-004` | `S-BLOCKED-ADMIN` | `M-PASSWORD` | `D-ORDINARY` |
| `AUTH-LC-PWD-005` | `S-PUBLIC-API` | `M-PASSWORD` | `D-ORDINARY` |
| `AUTH-LC-PWD-006` | `S-PUBLIC-API` | `M-PASSWORD` | `D-ORDINARY` |
| `AUTH-LC-PWD-007` | `S-ADMIN-API` | `M-PASSWORD` | `D-ORDINARY` |
| `AUTH-LC-PWD-008` | `S-PROTOCOL` | `M-PASSWORD` | `D-ORDINARY` |
| `AUTH-LC-PWD-009` | `S-PROTOCOL` | `M-PASSWORD` | `D-ORDINARY` |
| `AUTH-LC-ACC-001` | `S-BLOCKED-ADMIN` | `M-ACCOUNT` | `D-ORDINARY` |
| `AUTH-LC-ACC-002` | `S-BLOCKED-ADMIN` | `M-ACCOUNT` | `D-ORDINARY` |
| `AUTH-LC-ACC-003` | `S-BLOCKED-ADMIN` | `M-ACCOUNT` | `D-ORDINARY` |
| `AUTH-LC-ACC-004` | `S-PUBLIC-API` | `M-ACCOUNT` | `D-ORDINARY` |
| `AUTH-LC-ACC-005` | `S-PUBLIC-API` | `M-ACCOUNT` | `D-ORDINARY` |
| `AUTH-LC-ACC-006` | `S-ADMIN-API` | `M-ACCOUNT` | `D-ORDINARY` |
| `AUTH-LC-ID-001` | `S-BLOCKED-SELF`/`S-BLOCKED-ADMIN` | `M-IDENTITY` | `D-ORDINARY` |
| `AUTH-LC-ID-002` | `S-PUBLIC-API` | `M-IDENTITY` | `D-ORDINARY` |
| `AUTH-LC-ID-003` | `S-PUBLIC-API` | `M-IDENTITY` | `D-ORDINARY` |
| `AUTH-LC-ID-004` | `S-ADMIN-API` | `M-IDENTITY` | `D-ORDINARY` |
| `AUTH-LC-ID-005` | `S-BLOCKED-SELF`/`S-BLOCKED-ADMIN` | `M-IDENTITY` | `D-ORDINARY` |
| `AUTH-LC-ROL-001` | `S-PUBLIC-API` | `M-ROLE` | `D-ORDINARY` |
| `AUTH-LC-ROL-002` | `S-BLOCKED-ADMIN` | `M-ROLE` | `D-ORDINARY` |
| `AUTH-LC-ROL-003` | `S-BLOCKED-ADMIN` | `M-ROLE` | `D-ORDINARY` |
| `AUTH-LC-ROL-004` | `S-PUBLIC-API` | `M-ROLE` | `D-ORDINARY` |
| `AUTH-LC-ROL-005` | `S-ADMIN-API` | `M-ROLE` | `D-ORDINARY` |
| `AUTH-LC-PKY-001` | `S-SELF-API` | `M-PASSKEY` | `D-ORDINARY` |
| `AUTH-LC-PKY-002` | `S-BLOCKED-SELF`/`S-BLOCKED-ADMIN` | `M-PASSKEY` | `D-ORDINARY` |
| `AUTH-LC-PKY-003` | `S-BLOCKED-SELF`/`S-BLOCKED-ADMIN` | `M-PASSKEY` | `D-ORDINARY` |
| `AUTH-LC-PKY-004` | `S-SELF-API` | `M-PASSKEY` | `D-ORDINARY` |
| `AUTH-LC-PKY-005` | `S-PROTOCOL` | `M-PASSKEY` | `D-ORDINARY` |
| `AUTH-LC-PKY-006` | `S-SELF-API` | `M-PASSKEY` | `D-ORDINARY` |
| `AUTH-LC-TOTP-001` | `S-SELF-API` | `M-TOTP` | `D-ORDINARY` |
| `AUTH-LC-TOTP-002` | `S-SELF-API` | `M-TOTP` | `D-ORDINARY` |
| `AUTH-LC-TOTP-003` | `S-PROTOCOL` | `M-TOTP` | `D-ORDINARY` |
| `AUTH-LC-TOTP-008` | `S-PROTOCOL` | `M-TOTP` | `D-ORDINARY` |
| `AUTH-LC-TOTP-004` | `S-BLOCKED-SELF`/`S-BLOCKED-ADMIN` | `M-TOTP` | `D-ORDINARY` |
| `AUTH-LC-TOTP-005` | `S-BLOCKED-SELF`/`S-BLOCKED-ADMIN` | `M-TOTP` | `D-ORDINARY` |
| `AUTH-LC-TOTP-006` | `S-SELF-API` | `M-TOTP` | `D-ORDINARY` |
| `AUTH-LC-TOTP-007` | `S-SELF-API` | `M-TOTP` | `D-ORDINARY` |
| `AUTH-LC-TOTP-009` | `S-SELF-API` | `M-TOTP` | `D-ORDINARY` |
| `AUTH-LC-RCV-001` | `S-SELF-API` | `M-RECOVERY` | `D-ORDINARY` |
| `AUTH-LC-RCV-002` | `S-PROTOCOL` | `M-RECOVERY` | `D-ORDINARY` |
| `AUTH-LC-RCV-003` | `S-SELF-API` | `M-RECOVERY` | `D-ORDINARY` |
| `AUTH-LC-RCV-004` | `S-NOT-VISIBLE` | `M-RECOVERY` | `D-ORDINARY` |
| `AUTH-LC-RCV-005` | `S-SELF-API` | `M-RECOVERY` | `D-ORDINARY` |
| `AUTH-LC-CHL-001` | `S-PROTOCOL` | `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-CHL-002` | `S-PROTOCOL` | `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-CHL-003` | `S-PROTOCOL` | `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-CHL-004` | `S-PROTOCOL` | `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-CHL-005` | `S-PROTOCOL` | `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-CHL-006` | `S-BLOCKED-SELF` | `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-CHL-007` | `S-PROTOCOL` | `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-CHL-008` | `S-PUBLIC-API` | `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-CHL-009` | `S-SELF-API` | `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-CHL-010` | `S-PUBLIC-API` | `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-CHL-011` | `S-SELF-API` | `M-CHALLENGE` | `D-ORDINARY` |
| `AUTH-LC-RST-001` | `S-NOT-VISIBLE` | `M-RESET` | `D-ORDINARY` |
| `AUTH-LC-RST-002` | `S-PUBLIC-API` | `M-RESET` | `D-ORDINARY` |
| `AUTH-LC-RST-003` | `S-PUBLIC-API` | `M-RESET` | `D-ORDINARY` |
| `AUTH-LC-RST-004` | `S-PUBLIC-API` | `M-RESET` | `D-ORDINARY` |
| `AUTH-LC-RST-005` | `S-NOT-VISIBLE` | `M-RESET` | `D-ORDINARY` |
| `AUTH-LC-RST-006` | `S-PUBLIC-API` | `M-RESET` | `D-ORDINARY` |
| `AUTH-LC-INV-001` | `S-PUBLIC-API` | `M-INVITE` | `D-ORDINARY` |
| `AUTH-LC-INV-002` | `S-ADMIN-API` | `M-INVITE` | `D-ORDINARY` |
| `AUTH-LC-INV-003` | `S-ADMIN-API` | `M-INVITE` | `D-ORDINARY` |
| `AUTH-LC-INV-004` | `S-PROTOCOL` | `M-INVITE` | `D-ORDINARY` |
| `AUTH-LC-INV-005` | `S-PUBLIC-API` | `M-INVITE` | `D-INVITE-CLEANUP` |
| `AUTH-LC-INV-006` | `S-ADMIN-API` | `M-INVITE` | `D-ORDINARY` |
| `AUTH-LC-INV-007` | `S-ADMIN-API` | `M-INVITE` | `D-ORDINARY` |
| `AUTH-LC-POL-001` | `S-BLOCKED-ADMIN` | `M-POLICY` | `D-ORDINARY` |
| `AUTH-LC-POL-002` | `S-ADMIN-API` | `M-POLICY` | `D-ORDINARY` |
| `AUTH-LC-AUD-001` | `S-NOT-VISIBLE` | `M-AUDIT` | `D-ORDINARY` |

## 6. Explicit Open Choices

Each choice is blocked until its named owner records a reviewed decision. A
current runtime fact is not approval to broaden or perpetuate unsafe behavior.

| Choice ID | Exact question and affected rows | Why current authority cannot answer | Safe blocked posture, owner/gate, downstream work |
| --- | --- | --- | --- |
| `AUTH-LC-CHOICE-001` | What exact creation, disable, re-enable, and deletion rules govern accounts and their sessions/families, challenges, reset/invitation material, identities, password/factors, roles, profile/domain data, evidence/copies, notifications, owner recovery, and re-entry? (`ACC-*`) | Bootstrap, invitation, and guarded admin provisioning create materially different root bundles; focused relational creation race/rollback evidence is incomplete. The admin workflow's non-relational failed-bundle compensation is not general deletion authority. Disable/delete markers and consumers exist, but no authorized terminal transition/cascade, re-enable/delete recovery posture, or last-owner policy exists; deletion remains distinct from profile deletion and physical purge. Anonymous TOTP/recovery verification also misses post-issuance availability checks. | Preserve current bounded creation authority; expose no new mutation or physical deletion. #788/#785/#394/#724/#784; manual owner-lockout/auth/privacy/schema/destructive gates. Acceptance must cover exact-one bootstrap, invited/admin-root rollback and collision, disable/delete-between-challenge-and-verification, dependencies/holds/copies, and restore denial. |
| `AUTH-LC-CHOICE-002` | How are bootstrap/invitation/admin-provisioning creation, ordinary verification, user-driven replacement, reset replacement, and automatic rehash serialized; when may a credential be disabled/re-enabled/revoked; what recovery exists; what happens to sessions; and when is verifier material erased? (`PWD-*`, `SES-005`) | Creation is coupled to multi-root workflows whose relational race proof is incomplete. Concurrent verify/change/reset/rehash paths can overwrite metadata/verifier state without a shared conditional transition; two reset completions can both report success while last-writer wins. Old-password verification can race replacement and issue a new family after revocation selection. Current reset cannot recover a disabled credential; disable/re-enable/revoke runtime and terminal-material retention are absent. | Preserve bounded creation/verification/rehash, treat relational replacement and proof-to-session binding as partial, expose no disable mutation without separate recovery/last-method proof, and never retain a predecessor verifier solely as history. #339/#338/#788/#784/#785; auth/security/schema/privacy gates. |
| `AUTH-LC-CHOICE-003` | How are local/provider identity creation collisions handled, and what provider proof plus remaining-method checks permit link/unlink/relink with what session response? (`ID-*`) | Bootstrap, invitation, and guarded admin provisioning create local links but focused relational collision/rollback proof is incomplete. Provider-link authority is architecture-defined/default-disabled without OIDC runtime; terminal lifecycle has only lookup plus `DisabledAtUtc`. | Preserve bounded local-link creation; expose no provider link/unlink UI/API until stable-subject collision/takeover, rollback, lockout, session, audit, and redaction rules are approved. #787/#773/#785/#784; provider, lockout, security, privacy gates. |
| `AUTH-LC-CHOICE-004` | How are product roles created under each provisioning authority, then generally assigned, changed, or removed with last-owner, self-lockout, concurrency, current-claim/session and audit safety? (`ROL-*`) | Bootstrap, invitation, and admin provisioning create different exact assignments under different actors and rollback boundaries. No general mutation/removal lifecycle or removal history exists. | Preserve only the three bounded creation authorities; expose no general role mutation. #785/#464/#465/#784; manual owner-lockout plus API/schema/UI gates. |
| `AUTH-LC-CHOICE-005` | What anti-lockout, options/completion policy revalidation, allowed lifecycle and metadata-mutation source states, factor eligibility, concurrency/idempotency, session/challenge response, monotonic anti-replay state, terminal-evidence mutability, and material-erasure rules apply to passkey/TOTP/recovery lifecycle? (`PKY-*`, `TOTP-*`, `RCV-*`, `CHL-*`) | Passkey enrollment/sign-in/step-up and TOTP enrollment/TOTP/recovery verification do not recheck support policy at completion. Passkey sign-in drops its session credential; concurrent passkey assertions can regress the stored counter/miss replay suspicion; rename endpoints mutate labels/timestamps even on terminal records; passkey uniqueness, broad revoke states, TOTP begin/enroll/cancel, recovery eligibility/generation and same-code consumption, last-factor/session and terminal-secret rules are also partial. | Keep precise current behavior and classify handoff, all-factor policy changes, uniqueness, eligibility, monotonic counter, exact-one consumption, broad-state rename/revoke, terminal evidence integrity, repeat, and races partial; add no runtime or secret disposition here. #394/#775/#776/#465 and passkey/MFA #965 owners; manual auth/recovery/secret/schema gate. |
| `AUTH-LC-CHOICE-006` | How are challenge issuance authority, pending-flow/account/session/purpose binding, duplicate-pending state, completion-time policy, proof/verifier uniqueness, attempt counting, credential anti-replay state, and terminal transitions made atomic, and how is retention/cleanup governed? (`CHL-*`, `PKY-005`, `RCV-002`) | MFA create is exposed anonymously but denies no-actor requests and ignores `PendingAuthFlowId`; anonymous verification omits persisted account/session predicates. Other issuance authorities differ and repeated calls create separate pending challenges. Completion misses policy revalidation, proofs/verifiers can cross challenges, counters/attempts can regress or collapse, and consume is not conditional. | Classify MFA pre-session handoff as broken and other flows precisely; treat binding, duplicate pending, policy handoff, replay, counters, attempts and completion as security/concurrency-partial; no purge authority. #394/#775/`REC-AUTH-MFA-SIGNIN-001` plus auth-retention and #965 owners; security/privacy/destructive gate. |
| `AUTH-LC-CHOICE-007` | How are anonymous password-reset initiation, issuance, and consumption made atomic and enumeration-resistant; what should dependency denial do to pending material; and what bounded retention/disposition applies to terminal rows and hashes, including replay evidence and `CleanupEligibleAtUtc`? (`RST-*`) | Anonymous initiation has distinct identifier/abuse/provider authority and a no-material public outcome. Concurrent issuance can leave multiple pending links; concurrent completion can read the same pending row without a status-qualified transition/concurrency token; dependency denial leaves pending material reusable if the dependency recovers; disabled credentials cannot use current reset; cleanup marker is unused. | Preserve generic initiation, treat issuance/completion concurrency and denial disposition as partial, retain current rows, and do not expose hashes/add cleanup. #339/#724/`REC-AUTH-ABUSE-001`; security/privacy/provider/exposure/destructive gates. |
| `AUTH-LC-CHOICE-008` | What creation concurrency, grace, idempotency/delivery, acceptance rollback/actor attribution, revoke-reason evidence, policy replacement, and cleanup-trigger authorization rules govern invitations, and should terminal cleanup hard-delete after 90 days or retain/compact evidence? (`INV-*`, `POL-002`) | Create relies on pending-recipient uniqueness/delivery after commit; acceptance relational tests are absent and audit misattributes inviter as actor; revoke discards reason. Existing-policy replacement does not track retirement, so relational insertion fails while in-memory can retain multiple active rows. Grace readout/enforcement disagree, resend rotates on ready retry, and anonymous acceptance invokes global cleanup before validation. | Preserve only proven initial/bounded paths; do not claim policy replacement, correct actor/reason evidence, relational acceptance, grace, retry idempotency, or approved purge authority. #784/#724; manual provider/exposure/destructive/security/privacy gate. |
| `AUTH-LC-CHOICE-009` | What audit-event retention clock, duration, holds, authorized read/export and terminal disposition apply? (`AUD-001`) | Architecture says configurable/bounded but defines no value or runtime. | Retain; no ordinary removal or purge. #724/#774/#973; privacy/destructive/manual gate. |
| `AUTH-LC-CHOICE-010` | How are session issuance/use, credential-proof binding, refresh rotation, replay response, and revocation serialized; how must validation respond to family status; and how are lineage/last-seen retention governed? (`SES-*`, `REF-*`, `PWD-009`) | Local sign-in creates a full family while passkey sign-in creates an unusable access-only session. Old-password proof can issue after change/reset revocation selection; validation timestamps can race revocation/regress; revocation/replay can miss overlapping rotation; access validation ignores family status. Retention/copy disposition are undefined. | Describe exact current transitions as concurrency-partial; promise neither universal family invalidation nor old-proof exclusion; keep terminal rows unusable and authorize no cleanup. #338/#339/#394/#1059/#724; auth/security/privacy/destructive gate with relational proof-issuance, validate-revoke/monotonic-time, rotation-revoke/replay tests. |
| `AUTH-LC-CHOICE-011` | What mutation/version/effective-time/retention rules govern general security and invitation policy rows, and how do changes safely affect sessions/factors/owners? (`POL-001/002`) | Persisted shapes exist, but invitation replacement is broken/provider-divergent because the existing active row is loaded untracked; general security-policy mutation/lifecycle and complete cross-policy effects do not exist. | Use current safe read/default and initial invitation-policy creation only; do not claim replacement. #394/#465/#784/#785; manual security/owner-lockout gate plus relational conditional-retirement proof. |
| `AUTH-LC-CHOICE-012` | Which immediate creation/issuance responses may contain access/refresh, invitation/reset, TOTP setup, or recovery-code display-once material, and what no-redisplay proof is required? | Current runtime/contracts cross bounded immediate-delivery boundaries, while several canonical documents prohibit such material in all API responses. Runtime evidence cannot resolve normative policy; passkey session creation currently discards rather than safely hands off its bearer. | No behavior change here. `REC-AUTH-DOC-DRIFT-001` plus existing auth/invitation/reset owners; manual auth/security policy gate, then separate runtime/contract/client tasks if the decision changes behavior. |
| `AUTH-LC-CHOICE-013` | Must account-wide session revocation query refresh families/credentials directly, and how should exclusion of the current session interact with a family shared by another selected session? What must “all sessions” promise? (`SES-003`, `PWD-001/002`) | `RevokeActiveSessionsForAccountAsync` filters access sessions by `ExpiresAtUtc > now` and derives family IDs only through credentials linked to those selected sessions. When password change excludes the current session, every family linked to it is removed from candidates even if another selected session shares that family. Current tests do not prove revocation of valid refresh authority linked only to an expired session or retained through that shared-family exclusion. | Describe current behavior as partial; do not promise universal refresh invalidation. Reuse #338/#339 in `auth-session-security`; manual auth/security gate before a focused runtime fix and later contract/client wording. |

## 7. Retention Posture By Family

| Family | Classification |
| --- | --- |
| Active password, TOTP and recovery verifier material | Operational credential material while active; not lifecycle evidence. Terminal material disposition unresolved; reusable-material retention for history forbidden. |
| Access sessions and refresh families/credentials | Validity source-defined; terminal lineage operationally required for revocation/replay, duration unresolved. |
| Account, identity, and role lifecycle | Operationally required evidence, duration unresolved; no purge authority. |
| Passkey credential metadata/public material | Operationally required for active use and bounded terminal collision/replay review; duration unresolved; passkey private material absent. |
| Challenges/ceremonies | Source-defined transient validity; terminal safe-metadata retention/cleanup unresolved. |
| Password-reset requests | Source-defined validity; terminal evidence and hash disposition unresolved; current cleanup marker unused. |
| Invitations | Seven-day validity and current 90-day terminal hard-cleanup are source-defined implementation facts; continued disposal authority unresolved/manual-gated. |
| Security/invitation policies | Active/retired version evidence operationally required; duration unresolved. |
| Auth audit | Policy-defined/configurable intent; operationally required but duration, holds and disposal unresolved. |

No row equates retention expiry with purge. No row authorizes physical deletion
because a value is hashed, encrypted, disabled, revoked, consumed, expired, or
old.

## 8. Current Evidence And Stale-Source Reconciliation

Current implementation evidence includes:

- session and refresh families:
  `services/api/src/Settleora.Api/Auth/Sessions/AuthSessionRuntimeService.cs`,
  `AuthRefreshSessionRuntimeService.cs`, and their focused tests;
- password credential/change/reset:
  `Auth/Credentials/AuthCredentialWorkflowService.cs`,
  `Auth/PasswordChange/CurrentAccountPasswordChangeService.cs`,
  `Auth/PasswordReset/LocalPasswordResetService.cs`, and focused tests;
- guarded admin-local-user provisioning:
  `Auth/AdminUsers/AdminLocalUserService.cs` and
  `AdminLocalUserEndpointTests.cs`;
- passkey/MFA/recovery/challenges:
  `Auth/Passkeys/PasskeyRuntimeService.cs`, `Auth/Mfa/MfaRuntimeService.cs`,
  `Auth/Policy/AuthSecurityPolicyService.cs`, and
  `AuthMfaPasskeySecurityRegressionTests.cs`;
- invitation lifecycle and current physical cleanup:
  `Auth/Invitations/InvitationManagementService.cs`,
  `InvitationAcceptanceService.cs`, `InvitationLifecycleCleanupService.cs`,
  and focused invitation tests;
- persistence shapes and constraints:
  `Domain/Auth/*.cs`, `Persistence/SettleoraDbContext.cs`, and migrations
  `20260501143101_AddAuthCredentialsSessionsAuditSchemaFoundation`,
  `20260503092157_AddRefreshSessionFamilySchemaFoundation`,
  `20260624143555_AddAuthMfaPasskeySchemaFoundation`,
  `20260706041357_AddAuthPasswordResetRequestsFoundation`,
  `20260708085411_AddAuthInvitationsFoundation`, and
  `20260708105314_AddAuthInvitationPolicyRuntime`;
- canonical routes and generated methods in
  `packages/contracts/openapi/settleora.v1.yaml`,
  `packages/client-web/src/generated/`, and
  `packages/client-dart/lib/generated/`; and
- bounded audit/redaction evidence in credential, session, sign-in, reset,
  invitation, MFA and passkey `Ef*AuditWriter`/audit-writer classes plus
  `PasswordResetAuditRedactionAcceptanceTests.cs` and
  `AuthMfaPasskeySecurityRegressionTests.cs`.

The current code proves records and selected transitions, not production
readiness, public exposure, provider enablement, every stated policy, or
complete audit coverage. In particular, the older
[identity foundation](AUTH_IDENTITY_FOUNDATION.md),
[credential/session design](AUTH_CREDENTIALS_SESSIONS_AUDIT_DESIGN.md),
[MFA/passkey architecture](AUTH_MFA_PASSKEY_ARCHITECTURE.md), passkey/TOTP
contract design documents, and parts of the
[Day 1 PRD](../prd/MVP_DAY1_SCOPE.md) contain stale “future/unimplemented”
statements or conflicting response rules. They must not erase current source
facts or be silently rewritten by #721.

## 9. Follow-Up Recommendations And Duplicate Prevention

Live issue/PR and repository searches at the 2026-09-15 HKT checkpoint found no
existing #721 branch, PR, or merged equivalent. Existing owners are reused:

| Recommendation | Existing owner / canonical lane | Focus and gate |
| --- | --- | --- |
| Session/credential replacement and refresh-family completeness | #338/#339; `auth-session-security` | Resolve `AUTH-LC-CHOICE-002/010/013` so old-password proof cannot issue new authority outside a concurrent change/reset cascade, validation metadata/status ordering is safe, and sign-out-all/change/reset wording matches exact lineage/shared-family behavior; separate runtime first, then contract/client copy. Manual auth/security gate. |
| Account/credential/identity/role lifecycle decisions | #788/#785/#787/#339/#724; `auth-session-security`, later split schema/contract/admin UI/retention lanes | Resolve choices 001–004 with provisioning collision/rollback and authority-specific role creation across bootstrap, invitation, and admin workflows; password rehash/change/reset serialization; provider-link proof/takeover; last-owner/recovery safety; account deletion distinct from profile/domain-data deletion; and no implied physical purge. Manual auth/owner-lockout/privacy/destructive gates. |
| Factor/passkey/recovery lifecycle and terminal material | #394/#775/#776/#465; `auth-session-security`, split schema/contract/client lanes | Resolve enrollment/cancel concurrency, passkey monotonic-counter updates/replay suspicion, recovery's TOTP-only prerequisite and same-code atomic consumption, broad rename/revoke source states and terminal-evidence mutation, and terminal material under choices 005–006 without recreating merged runtime. Manual recovery/secret/security gates. |
| Challenge retention/abuse | `REC-AUTH-MFA-SIGNIN-001` and `REC-AUTH-ABUSE-001`; docs decision then split runtime/config lanes | Repair anonymous MFA issuance and enforce pending-flow/account/session/purpose binding; resolve cross-challenge TOTP/recovery-proof replay, passkey counter monotonicity, atomic attempts/blocking, expiry/replay/completion and cleanup. Manual security/proxy gates. |
| Passkey ceremony/session handoff | `REC-AUTH-PASSKEY-SESSION-001`; docs decision then split auth runtime and contract lanes | Keep all-completion policy revalidation, enrollment uniqueness/challenge atomicity, and the currently discarded sign-in session credential/usable handoff with the #965 owner; #721 adds lifecycle wording only. |
| Reset initiation, retention and atomicity | #339/#724 and `REC-AUTH-ABUSE-001`; `auth-session-security` plus provider/exposure and later retention lanes | Preserve enumeration-resistant anonymous initiation; define atomic issuance/consume, disabled-credential recovery, dependency-denial disposition, bounded terminal row/hash disposition, and cleanup evidence. Manual security/privacy/provider/exposure/destructive gates. |
| Invitation policy, evidence and retention cleanup | #784/#724; focused docs decision, then separate `auth-session-security` runtime if approved | Repair relational policy replacement, true acceptance-actor attribution, and revoke-reason evidence; reconcile grace readout/enforcement and anonymous pre-validation cleanup; decide whether 90-day hard delete preserves sufficient evidence/holds. No automatic implementation issue is created. |
| Session/family tombstone retention | #338/#1059/#724; `auth-session-security` | Define bounded lineage/hash retention and later cleanup. Manual security/privacy/destructive gates. |
| Audit retention/read/export | #724/#774/#973; separate auth runtime, contract, clients and retention lanes | Define clocks/holds/read authorization/redaction/disposal. Manual privacy/destructive gates. |
| Canonical doc/contract drift | `REC-AUTH-DOC-DRIFT-001` and `REC-AUTH-CONTRACT-DRIFT-001` | Preserve #965's separate docs decision and OpenAPI/generated-client tasks; manual security gate where required. |
| Security notifications | #369/#973 | Consume only source-owned events after lifecycle decisions; do not infer events from rows or UI. |

No new GitHub issue is required to complete #721. #717 and #716 remain open;
#961 remains a later synthesis consumer; #975 and #946 remain inactive. This
document does not start #718, #719, #720, #961, or any #965 recommendation.

## 10. Non-Goals And Stop Conditions

This policy does not implement account disable/re-enable, identity unlink,
role mutation, credential/factor material erasure, retention cleanup, purge,
notifications, audit read/export, provider delivery, public/admin exposure,
session handoff, MFA sign-in binding, or any other runtime behavior. It does
not approve schemas/migrations, OpenAPI/generated clients, UI/Figma, secrets,
configuration, deployment, destructive data work, or production operations.

Stop future work when a source conflict is unresolved; an owner/admin could be
locked out; reusable terminal authentication material would be retained merely
for history; a purported orphan is not positively non-authoritative; a purge
lacks retention/hold/dependency/copy proof; or a required security, privacy,
schema, contract, UI, provider, deployment, exposure, or destructive manual
gate is pending.

## 11. Validation And Acceptance Contract

This document must retain exactly 82 unique `AUTH-LC-*` transition rows and 13
unique `AUTH-LC-CHOICE-*` records, all required record families, valid relative
links, exact source/test/symbol existence,
secret-material exclusions, one-path scope, no runtime diff, shared-template
field coverage, strong independent review, local auth/security factual review,
exact-head CI/GitHub review, and zero unresolved actionable threads. Any source
change invalidates earlier candidate-bound validation and both fresh reviews.
