{
	admin off
	auto_https off
}

https://{$SETTLEORA_HTTPS_HOSTNAME}:8443 {
	tls /run/settleora-tls/tls.crt /run/settleora-tls/tls.key

	# Request access logging stays disabled so URLs, headers, and client metadata
	# are not persisted by this repository package.
	reverse_proxy api:8080
}
